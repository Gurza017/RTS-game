# CLAUDE.md

Краткая карта проекта для Claude Code. Подробная история замеров и разборов
багов вынесена в `docs/ENGINEERING_LOG.md` — читать её только по конкретному
вопросу, в контекст она не грузится.

## Проект

3D-RTS на **Godot .NET 4.7.1 (mono)**, рендер **GL Compatibility**. GDScript +
C#-солвер `csharp/ArmyCore.cs`. Код и комментарии — по-русски. Отвечать
пользователю тоже **по-русски**.

## Архив: где что искать

Этот файл держит АРХИТЕКТУРУ, ПРАВИЛА и МЕХАНИКИ. История заходов —
в `docs/`, в контекст не грузится, читать по конкретному вопросу:

- **`docs/ARCHIVE_SPRINTS.md`** — полная история спринтов и ТЗ (разборы жалоб
  по скриншотам авг. 2026, спринты 13-20, BigStand 1-5, ТЗ 14-19.09.2026):
  что именно чинилось, какими числами и почему требование разворачивалось.
  Там же — панель ветеранства и UI по скриншотам, знамёна и знаменосец, белый
  флаг паники, ворота крепости и клик по зданию, тролль/логово/овцы,
  башня/бараки/крыши, голосовое управление, гноллы, туши, монах.
- **`docs/ENGINEERING_LOG.md`** — замеры, разборы багов, «Известные красные».
- **`docs/BALANCE_MATH.md`** — расчёт баланса; **`docs/PERF_6000.md`** и
  **`docs/PLAN_D_70FPS.md`** — потолки производительности и что уже измерено
  и отвергнуто; **`docs/BIGSTAND_2026-09-16.md` / `-17.md`** — планы BigStand;
  **`docs/GAME_ANALYSIS_2026-09-19.md`** — аудит двух партий;
  **`docs/CONFIG_NOTES_2026-09-14.md`** — комментарии, снятые из конфигов.

**РАЗВОРОТЫ ТРЕБОВАНИЙ ВЛАДЕЛЬЦА — НЕ РЕГРЕССИИ.** Прежде чем «чинить» код по
красному стенду, проверь в архиве, не утверждает ли проверка требование,
которое только что развернули. Ручки, которые качались туда-сюда и которые
нельзя переписывать без ЯВНОГО ТЗ: режим строя `[2]` (пять разворотов),
кольца/аура монаха, дальность огня башни, площадка сбора, отбрасывание от
тарана, громкость марша.


## Сборка и проверка

- Редактор: `Godot_v4.7.1-stable_mono_win64`. Standard-сборка проект НЕ откроет.
  Бинарь: `E:\Downloads\Godot_v4.7.1-stable_mono_win64\Godot_v4.7.1-stable_mono_win64\Godot_v4.7.1-stable_mono_win64_console.exe`
- C#: `dotnet build "Ten Thousand Spearmen.csproj"` (нужен .NET SDK 8+).
- Компиляция всех скриптов: `<godot> --headless --import` (ошибки парсинга — в stdout).
- Дым: `<godot> --headless --path . res://scenes/Main.tscn --quit-after 400`.
- Стенд: `<godot> --headless --path . res://qa_<имя>/Test.tscn`. Провалы искать
  по строкам **«НЕ ПРОШЛО»** и **«провалов:»** (не по `FAIL`).
  **Не у всех стендов сцена зовётся `Test.tscn`:** у `qa_army` их три
  (`Matrix`, `MatrixLive`, `SelfTest`), у оконных — `Shot.tscn`. Скрипт,
  гоняющий шлюз списком папок, обязан это учитывать: «файла нет» выглядит в
  сводке как «ноль провалов».
- **Сводка шлюза обязана считать и `SCRIPT ERROR`**: ошибка внутри блока стенда
  роняет его молча, вердикты не печатаются, а «провалов: 0» остаётся.
- **Ручки правок читаются из командной строки любым стендом**
  (`-- knob=perf_config.имя:значение`, разбирает `GameManager._ready`). В логе
  печатается «ручка perf_config.… = …»; нет строки — ручка не применена, и
  «A/B по ручке» сравнивает один и тот же код.
- Полный шлюз запускать отсоединённо (`Start-Process`), а не фоновым Bash:
  десятиминутный лимит режет прогон на середине.

### Состав раздачи (голос живёт НЕ в pck)

**Экспорт .exe — это ТРИ вещи рядом, и любая недостача ломает игру молча.**
1. `.exe` (+ `.pck`, если Embed PCK выключен);
2. `data_<имя проекта>_windows_x86_64` — managed-сборки И нативные
   `libvosk / libgcc / libstdc++ / libwinpthread`. Без неё Godot показывает
   «.NET assemblies not found» и не стартует вовсе;
3. **`voice_models/vosk-model-small-ru-0.22`** (88 МБ). В PCK модели нет
   НАМЕРЕННО: нативной libvosk нужен настоящий путь на диске, из pck она
   читать не умеет, а тысячи файлов модели Godot пытался бы импортировать.
   Забыли — игра стартует НЕМОЙ: под иконкой V «V — нет модели», и это
   читается как «пропало голосовое управление» (20.09.2026: экспорт уехал из
   корня проекта, где модель лежала рядом, в соседнюю папку — и голос исчез,
   хотя в редакторе всё работало).

Собирает и ПРОВЕРЯЕТ состав `tools/make_release.ps1 -Exe <путь к .exe>`
(бросает, если в раздаче нет `libvosk.dll` или модели). Поиск модели —
`VoiceControl.model_dirs()`: проект → рядом с .exe → `data_*` → `user://`,
внутри каждого каталога годится любая папка `vosk-model*`. Стережёт
`qa_voice_cmd` B2в.

### Оконные стенды (headless их слеп)

В headless счётчик вызовов отрисовки всегда ноль, буфер глубины не
заполняется, не компилируется НИ ОДИН шейдер, теней и трупов на экране нет
вовсе, а `AudioServer` ставит пустой драйвер (анализатор спектра читает
тишину; PCM через `AudioEffectCapture` при этом ЧИТАЕТСЯ — dummy микширует).
Оконные: `qa_visual_smoke/Shot`, `qa_troll_ring/Shot`, `qa_monk_vfx/Shot`,
`qa_roof_visual_fix/Shot`, `qa_wound/Shot`, `qa_banner/Shot`, `qa_vetui/Shot`,
`qa_rally_zone/Shot`, `qa_troll_impact/Shot`, `qa_shot9`, `qa_shot10`,
`qa_shotvis`, `qa_shotcorpse`, `qa_fx`, `qa_veg/Test`, `qa_march_perf`,
`qa_audio3`, `qa_mass3k`, `qa_fps`, `qa_stress_report`.

- **ВИЗУАЛЬНЫЙ ДЫМ — `qa_visual_smoke/Shot` (26 проверок). ОБЯЗАТЕЛЕН ПОСЛЕ
  ЛЮБОЙ ПРАВКИ ОТРИСОВКИ** — `DEPTH`, `render_priority`, `depth_draw`, порога
  среза альфы, размера квада, привязки спрайта к земле. Headless-шлюз такие
  правки не подтверждает ВООБЩЕ. Запуск:
  `<godot> --path . res://qa_visual_smoke/Shot.tscn -- --out=<путь без .png>`.
  Разбор — в разделе «Визуальный дым».

### Главный шлюз после правок логики

`qa_bugpass qa_goblin qa_spear qa_guard qa_hold qa_settle qa_crowd qa_squad
qa_vet qa_target_lock qa_combat_lock qa_disengage qa_army qa_res2 qa_ai qa_ai2
qa_volley qa_fog qa_recon qa_forge qa_sel2 qa_corpse qa_wound qa_facing
qa_orders qa_leash qa_cavalry qa_push qa_bldattack qa_difficulty qa_pivot
qa_wall qa_balance qa_keep qa_panic qa_mega_battle qa_clash qa_full_game_4k
qa_sprint13 qa_monk_healing qa_river_crossing qa_boar_rider_charge
qa_swordsmen_charge qa_performance_stress qa_gnoll_behavior qa_volley_fix
qa_gnoll_fix qa_monk_vfx qa_monk qa_gold_mine qa_formation_keys qa_goblin_mine
qa_goblin_loop qa_ai_loop qa_aggro_fix qa_menu_loader qa_audio18 qa_audio
qa_audio2 qa_voice qa_sprint18b qa_voice_cmd qa_bones_fix qa_lair_ai
qa_sprint19 qa_sprint19b qa_sprint19c qa_nav qa_archer_bench qa_helpbuild
qa_reform qa_sprint20 qa_keep_elite qa_worker_queue qa_ai_scenarios
qa_fix_pack qa_home_guard qa_tz0918 qa_tz0919 qa_combat_gate qa_monk_fix
qa_archer_fix qa_monk_auto qa_cliff_bypass qa_big_goblin_pair qa_reserve_sleep
qa_building_block qa_ai_audit qa_audio_update qa_building_navigation
qa_garrison_archers_test qa_monk_move qa_reform_idle qa_lair_freeze
qa_wave_wake qa_scout_raid qa_vet_ui2 qa_sheep_herding
qa_sheep_performance_test qa_cliff_and_squad_behavior`

**Длинная партия — зонд `qa_ai_loop/Smoke`** (`-- minutes=N`, ускорение ×4,
~1.3 мин стены на игровую минуту): гонять после правок ИИ и условий победы,
SCRIPT ERROR в нём — регрессия.

### Чем что стережётся (по темам)

- **Навигация и рельеф:** `qa_nav` (сетка, A*, брод), `qa_cliff_bypass`
  (обход плато отрядами), `qa_building_navigation` (обтекание застройки, сдача
  ресурсов у ворот), `qa_building_block` (фундамент непроходим),
  `qa_river_crossing`, `qa_map` (карта, плато, река, тайлы).
- **Строй и бой:** `qa_wall` (стыки строя, толпа), `qa_clash` (что делает со
  строем сам контакт), `qa_mega_battle` (1500 на 1500, просачивание),
  `qa_mass_siege` (700 на 700 с базой), `qa_disengage`, `qa_formation_keys`,
  `qa_reform`, `qa_reform_idle` (шторм смыкания), `qa_combat_gate`.
- **Рода войск:** `qa_spear`, `qa_cavalry`/`qa_boar_rider_charge`,
  `qa_archer_*` (логика, упреждение, снайпер, фикс фокуса), `qa_monk_*`
  (лечение, автономия, VFX, ветка кузницы), `qa_troll*`, `qa_big_goblin*`,
  `qa_gnoll_*`, `qa_sheep*`.
- **ИИ:** `qa_ai`, `qa_ai2`, `qa_ai_scenarios`, `qa_ai_loop`, `qa_ai_audit`,
  `qa_goblin*`, `qa_lair_ai`, `qa_home_guard`, `qa_reserve_sleep`,
  `qa_scout_raid`, `qa_wave_wake`.
- **Постройки и гарнизон:** `qa_keep`, `qa_tower`, `qa_roof_garrison`,
  `qa_garrison_archers_test`, `qa_rally_zone`, `qa_spawnlane`,
  `qa_worker_queue`, `qa_helpbuild`, `qa_gold_mine`.
- **Интерфейс:** `qa_hud5`, `qa_ui9`, `qa_sel2`, `qa_vetui`, `qa_vet_ui2`,
  `qa_hud_afford`, `qa_menu_loader`.
- **Звук:** `qa_audio`, `qa_audio2`, `qa_audio18`, `qa_audio_update`,
  `qa_voice`, `qa_voice_cmd`; спектр и громкость марша — оконный `qa_audio3`;
  огибающая лупов — зонды `qa_march_env/Probe`, `qa_audio_env/Probe|Slice`.
- **Сложность и сохранение:** `qa_difficulty` (58 проверок: пресеты,
  нейтральность Normal, файл, круговой рейс слепка, ПОЛНЫЙ ЦИКЛ — файл →
  новая сцена Main с тем же зерном).

### Измерители (вердиктов нет, в шлюз не входят)

- **`qa_ab`** — честный A/B ЧЕРЕДОВАНИЕМ по `tick_meter` на живой сцене
  (1440 копейщиков): база и правка подряд, раундами, порядок фаз в раунде
  переворачивается. Единственный законный способ подтвердить правку
  производительности.
- **`qa_bigstand`** (`Test` — синтетика на 3886, `Owner` — сценарий владельца:
  живая партия со всеми ИИ, 32 отряда у пня; `-- scale=2.5` даёт 5000).
  По умолчанию ЧЕСТНЫЙ (`detail=0`); `detail=1` включает разбор по веткам.
  Печатает стену кадра, распределение кадров, GC, ведомых ядром, приказы по
  источникам (`cmd_meter`), расталкивание (`sep_meter`), навигацию.
- **`qa_melee_bench`** — профиль замеса по родам войск (`class_meter`),
  атрибуция пиковых кадров, аллокации C#. `mult=5` — масштаб партии владельца.
- **`qa_arrow_bench`**, **`qa_3000_archers_bench`**, **`qa_archer_bench`** —
  стрелы и сканы стрелков. **`qa_fogperf/Probe`** — цена пересчёта тумана.
  **`qa_gnoll_bench`**, **`qa_infantry_radar`** — цена тика и перехватов.
  **`qa_long_battle`** (`-- minutes=60`) — долгая партия с вердиктами по
  последней минуте. **`qa_stress_report`** (оконный) — отчёт в шаблоне
  владельца. **`qa_full_game_4k`** — настоящая партия на 4000 (инварианты в
  headless, FPS в окне; печатает ПИКИ кадра, а не средний FPS).

### Правила замера

1. **Мониторы `Performance.TIME_*` для разбора кадра НЕ ГОДЯТСЯ** — при снятом
   ограничении кадров усредняются по кадрам отрисовки. Честные числа даёт
   `perf_config.tick_meter` / `vis_meter`.
2. **Профиль (`profile_physics`, `class_meter`) завышает тик на 25-35 %** —
   пара `get_ticks_usec` вокруг ветки, идущей тысячу раз в кадр, стоит
   соизмеримо с ней. Доли веток читать ТОЛЬКО как ранжирование.
3. **Разброс между прогонами ±3-5 к/с на неизменном коде.** Правку меньше
   миллисекунды подтверждать только чередованием.
4. **База снимается в том же заходе, что и правка.** Вчерашние числа
   сравнивать с сегодняшними нельзя.
5. **A/B на HEAD требует `git stash` + `dotnet build`** (DLL собирается из
   stash-а) и обратно.
6. **Абсолютное время в стендах врёт на загруженной машине** — перед замером
   проверять `Get-CimInstance Win32_Process -Filter "Name LIKE '%Godot%'"`;
   отдельный стенд, запущенный рядом со шлюзом, врёт по стенным часам.

### Ловушки стендов (каждая давала ложный вердикт)

- **Стенд, который ждёт, пока кто-то куда-то дойдёт, ждёт `physics_frame`,** а
  не `process_frame`: при `Engine.max_fps = 0` отрисовка обгоняет физику.
  Ждать надо СВОЙСТВО с потолком в физкадрах, а не фиксированное число кадров
  (тик бойца шардирован — на живой карте до трёх кадров на один тик).
- **Ошибка внутри корутины стенда убивает её молча** (сцена живёт, вывод
  буферизован). **Сторож-таймер обязателен.** Стенд, висящий без вывода
  вовсе, — парс-ошибка в его `.gd`: смотреть ГОЛОВУ лога.
- **Стенды не хардкодят числа из конфига** — проверять СВОЙСТВО: большинство
  «красных» краснело от разворота требования, а не от поломки кода.
- **Камеру замораживать** (`main.set_process(false)`, `cam.set_process(false)`)
  и пересчитывать экранную точку В ТОМ ЖЕ КАДРЕ, в каком делается клик или
  разбор: объектив доводит фокус и уезжает на метры между двумя кликами.
- **`root.push_input(e)` без `in_local_coords = true`** применяет обратное
  растяжение окна: точка (640, 360) уезжает в (17928, 12209).
- **Стенд на случайной карте — не доказательство:** сценарий, зависящий от
  окружения, обязан ставить это окружение сам (вычистить лес, выбрать ровную
  сухую площадку `_flat_spot`, поставить своё дерево). Плато тоже «ровное» —
  клик по бойцу на нём промахивается по высоте.
- **Площадку зонда держать В ПОЛЕ КАРТЫ:** габарит армии задаёт размер ячейки
  сетки ядра, и бойцы за тысячу метров дают ячейку 4 м вместо игровой 1 м.
  Ряд за кромкой карты даёт ложное дрожание (зажим границы против
  расталкивания).
- **Вода и маршрут — правило партии:** стенд, снявший `world_bounds_enabled`,
  не видит ни реки, ни маршрутов (`qa_map` C включает границы явно).
- **`squad_members()` — НЕ ЧИТАТЕЛЬ:** опустевший отряд он РАСПУСКАЕТ прямо в
  геттере. Состав для чтения брать напрямую из `squads[sid]["members"]`.
- **Правило 5 на сырой ссылке:** `(u as Unit)` по списку, где кто-то уже
  погиб, бросает исключение; освобождённый объект в Variant равен `null`.
- **Дикие (тролли, гноллы, охрана, резерв) вмешиваются в замер:** стенд обязан
  их замораживать (`set_tick(false)`) или ставить свой пень; тролль без логова
  усыновляет логово ПАРТИИ и уходит к нему за сотню метров.
- **`.gd` не патчить во время фонового шлюза:** сцена, стартовавшая между двумя
  файлами патча, падает Parse Error, а сводка врёт «провалов: 0».
- **Git Bash heredoc схлопывает `\`** (и в python-строках тоже): правки `.gd` с
  продолжением строки — только Edit/Write или скриптом-файлом.
- **`class_name` нового узла в стенде брать через `preload`:** имя попадает в
  кэш классов только после импорта редактором.
- **Новый потребитель `AudioManager.rng` или `randi()` сдвигает сеяные
  лотереи** — стенд, утверждающий исход одного жребия, обязан мерить серию.

### Известные красные (проверять A/B на HEAD, прежде чем «чинить»)

`qa_wall` A1 (1-3 просочившихся из 48, 4 прогона из 6), `qa_mega_battle` B1-1
и B2-1 (ножевые пороги, мигают под нагрузкой шлюза), `qa_cavalry` E4,
`qa_troll` B5/B6/D1/D2, `qa_big_goblin` A3-A5, `qa_settle` C2, `qa_squad` 8,
`qa_fog` D3/D4, `qa_volley` C1 (разброс залпа случаен). Разбор — в
`docs/ENGINEERING_LOG.md`, раздел «Известные красные».

## Архитектура

**Автозагрузки:** `ResourceManager` (банк по фракциям) и `GameManager` (отряды,
улучшения, размещение построек, `unit_grid` / `far_units` / `sel_decals` и **весь
тик армии**).

**У юнита нет физтела и своего тика.** `Unit extends Node3D`. Точка входа —
`Unit.tick_physics()` из `GameManager._physics_process` по плоскому массиву
`_live_units`, и `Unit.tick_visual()` из `_process`. Оба прохода **шардированы**
по кадрам (`perf_config.shards_for`, визуальный — на шард больше от 2000 бойцов).
`collision_mask` везде 0, столкновений нет; движение — прямое интегрирование в
`position`.

**Иерархия:** `Unit` → `Worker`/`Spearman`/`Archer`/`Warrior`/`Monk`/гоблины;
`Building` → `Castle`/`Barracks`/`Smithy`/`Mine`/`House`/`GoblinHut`;
`ResourceNode`, `MineCluster`. Сцены только `MainMenu.tscn` и `Main.tscn` —
остальное строится кодом.

**Ядро армии (`scripts/army/ArmySoA.gd` → `csharp/ArmyCore.cs`).** Колонки, плоская
сетка соседей, реестр стволов, пакетный шаг и пакетное расталкивание живут в C#.
`ArmySoA` — фасад, владелец массивов — солвер. **Ячейка сетки держит по списку
на ФРАКЦИЮ** (`ArmyCore.Factions`), а не «свои/чужие»: раньше слот считался как
`fac == 0 ? 0 : 1`, и третья сторона молча склеилась бы со второй.

**ПОКАДРОВЫЕ ПРОХОДЫ ИДУТ ПО ПЛОТНОМУ СПИСКУ ЖИВЫХ СТРОК, И ОН ОТСОРТИРОВАН**
(`ArmyCore._liveRows`, сент. 2026 — пункт roadmap закрыт). Ёмкость только
растёт (массивы под ПИК армии, назад не сжимаются), прежняя граница `_top`
упиралась в самого верхнего живого и при рассеянных потерях не опускалась
(замер `qa_ab/Probe`: выбили нижние 1900 из 2000 — вхолостую 1900 строк).
Теперь `RecomputeBounds`, `FillGrid`, `BatchSeparation` и `BatchCombat` идут
по списку живых. **ОТСОРТИРОВАННОСТЬ — НЕ ПРИХОТЬ, А СОХРАНЕНИЕ ПОВЕДЕНИЯ:**
`FillGrid` строит списки ячеек вставкой в голову, порядок обхода строк задаёт
порядок СОСЕДЕЙ, а по нему разрешаются ничьи в `EnemyAt`/`BestEnemy`/
`NearestOfSide`. Плотный список со свапом перестал бы быть отсортированным
после первой смерти и «поехал» бы выбором цели на десятках стендов; сдвиг с
двоичным поиском (сотни событий в секунду — микросекунды) даёт В ТОЧНОСТИ
прежний порядок. `_top`/`Top()` оставлены — их читают стенды. Признак
занятости по-прежнему ОТДЕЛЬНЫЙ массив, а не бит в `_flags`: флаги
переписывают и пакетная запись поз, и `SetFlag` из GDScript.

**Фракций три** (`Constants.FACTION_*`, `FACTION_COUNT`): игрок, красный ИИ и
**гоблины** — третья сторона, враждебная всем и не участвующая в условии победы.
Группы узлов раздаёт `Constants.unit_group()` / `building_group()`; «все чужие
здания» — `Constants.enemy_building_groups()`.

## Правила, нарушение которых стоило дорого

1. **Границу GDScript↔C# пересекать пачками или один раз, никогда в цикле по
   бойцам** (переход ≈ 1.5–2.5 мкс). Шаги и позы идут очередью, одним вызовом.
2. **`global_position` — свойство, а не поле.** Под неподвижным `World` локальная
   `position` даёт то же число даром (проверяет `Unit._local_xform`).
3. **Всё, что одинаково для всей армии, читать раз в кадр и передавать
   аргументом.** Восемь таких чтений в `tick_visual` стоили больше миллисекунды.
4. **Ни `String`, ни `Array` в покадровом пути.** Секторы, ключи и роды поз —
   целые числа и готовые таблицы.
5. **`is_instance_valid` проверять на СЫРОЙ ссылке, до приведения типа:**
   `x as Unit` и типизированное присваивание на освобождённом объекте не
   возвращают null, а бросают исключение.
6. **`remove_child()` перед `queue_free()`**, если детей контейнера считают в том
   же кадре: освобождение отложено.
7. **Союзники друг друга не толкают**: расталкивание монотонно и только наружу,
   сил избегания добавлять нельзя — это уже давало «дрожат и застревают».
8. **Никаких `DirAccess`-переборов каталогов ради ассетов** — в экспорте имена
   другие; проверять `SpriteSheetParser.folder_has()`. PCK регистрозависим.
9. **`var x := arr[i]` не выводит тип** (Variant) — писать `var x: String = arr[i]`.
10. **Стенды не хардкодят числа из конфига** — проверять СВОЙСТВО: большинство
    «красных» стендов краснело от разворота требования, а не от поломки кода.
11. **Стенд, который ждёт, пока кто-то куда-то дойдёт, ждёт `physics_frame`,**
    а не `process_frame`: при `Engine.max_fps = 0` отрисовка обгоняет физику.
12. **Абсолютное время в стендах врёт на загруженной машине.** Перед замером
    проверять `Get-CimInstance Win32_Process -Filter "Name LIKE '%Godot%'"`.
13. **УМОЛЧАНИЕ У `Dictionary.get(k, X)` ВЫЧИСЛЯЕТСЯ ВСЕГДА, ДАЖЕ ПРИ
    ПОПАДАНИИ В КЛЮЧ.** `d.get(sid, {})` и `d.get(cat, [])` создают НОВЫЙ
    контейнер на каждый вызов — это правило 4, спрятанное за удобным
    синтаксисом. В покадровом пути писать `var v: Variant = d.get(k)` и
    проверять на `null`. Замер: 0.140 против 0.078 мкс на вызов. Ловушка была
    в `squad_target_get`, `squad_course`, `squad_has_formation` (зовутся из
    хода фаланги и авто-агро) и в `AudioManager.play_3d` (звук в свалке —
    сотни вызовов в секунду).
14. **ОТВЕТ ПАРОЙ `[значение, нашли ли]` — ТОЖЕ АЛЛОКАЦИЯ.** 0.157 мкс против
    0.021 у ответа значением. Признак «не нашли» выражается самим значением
    (`Vector3.INF`, `Vector2.INF`) — так уже сделано у `squad_centre_xz` и
    `ArmyCore.PosOr`. Разобрано на `squad_enemy_point` против прежнего
    `squad_enemy_pos`.

## Отряды и бой (главное)

- **Приоритет приказов:** замок цели игрока (ПКМ) → заслон, в который упёрлись
  телом → авто-агро. Замок снимают только приказ на движение, отход и полное
  истребление вражеского отряда.
- **Замок читается и в ПОКОЕ** (`_check_auto_aggro`, первая ветка, до стойки).
  Без этого лучники подменяли приказ молча: стрелок не преследует
  (`Archer.pursues_target` = false), назначенная цель отходит за дальность,
  `_process_attack` бросает её и переводит бойца в покой — а покой обслуживает
  авто-агро, и оно выдавало ближайшего, то есть чужих копейщиков. Пока цель в
  пределах `_lock_sight_range()` (вдвое дальше оружия), другую брать нельзя;
  дальше — приказ исчерпан и замок снимается сам. Стенд `qa_target_lock`, блок F.
- **«Ударили вплотную» под замком — это длина руки, а не дальность оружия.**
  Ответ на удар в `take_damage` перебивает приказ игрока; у лучника
  `attack_range` = 20 м, и порог брался оттуда. Под замком порог тот же, что у
  перехвата заслона (`LOCK_BLOCKER_RANGE`).
- **Центр отряда — МЕДИАНА по осям** (`GameManager._centroid_of`): среднее у
  расколотого отряда садится в пустое поле между кучами, и туда уезжали и
  звезда, и точка залпа, и цель ИИ.
- **Сплочённость** (`_cohesion_guard`): оторвавшийся и ничем не занятый боец
  получает разовый приказ вернуться, с остыванием. Приказ, а не сила.
- **ФАЛАНГА ХОДИТ СТРОЕМ ПО ПРИКАЗУ, НО НЕ ХОДИТ ПООДИНОЧКЕ.** Жалоба была
  одна — «крайние копейщики выбегают из строя», — а решений понадобилось два,
  и первое оказалось неверным. Глухой замок позиции
  (`STANCES.defense.lock_position = true`) жалобу закрыл и тут же создал
  другую: фаланга перестала ходить по приказу игрока. Ручка осталась, но стоит
  в `false`.
- **НАСТОЯЩАЯ ПРИЧИНА ВЫБЕГА — ТОЧКА, А НЕ РАЗРЕШЕНИЕ ДВИГАТЬСЯ.** Приказ атаки
  раздаётся каждому бойцу отдельно, и каждый получал ОДНУ И ТУ ЖЕ цель —
  координату вражеской модели. Шеренга честно шла в одну точку, то есть
  схлопывалась; первыми это видно на флангах, им идти дальше всех. Теперь стена
  ПЕРЕНОСИТСЯ ЦЕЛИКОМ: `Unit._wall_goal` смещает точку каждого на тот же
  вектор, на который сдвигается центр отряда, и взаимное расположение не
  меняется вовсе (замер `qa_bugpass` L2: ширина строя 6.30 → 6.30 м).
- **ИНДИВИДУАЛЬНОЙ ИНИЦИАТИВЫ У ФАЛАНГИ НЕТ ВОВСЕ.** Всё, что ниже хода стены в
  `_process_attack`, — способы сблизиться самому (подтянуть ряд, доступить по
  сцепке, обойти свалку, догнать), и копейщик в обороне не вправе ни на один.
  **ПОРЯДОК ВОРОТ ВАЖЕН:** запрет стоит ПОСЛЕ `_phalanx_march`, а не до него —
  поставь его выше, и фаланга не пойдёт вообще (ровно это и вышло при попытке
  закрыть выбег глухим замком).
- **Смыкание рядов работает при любом положении ручки замка**
  (`Unit.allow_reform_move`, выдаёт `SquadFormation.close_ranks`): без него
  потери и проход союзников сквозь строй оставляли бы в фаланге вечные дыры.
  Разрешение выдаётся на секунды, а не «пока не дойдёт»: дойти боец может и
  никогда.
- **Авто-агро в обороне** берёт только то, что **перед фронтом строя**
  (`_in_phalanx_front`) и в пределах оружия.
- **Сцепка `_melee_grip`:** заслон, взятый по физическому контакту, не
  сбрасывается на границе дальности — боец доступает до него. Без этого он
  бесконечно бегал челноком у чужого строя и в рубку не входил.
- **Отряд «в бою»** (`squad_in_combat`) блокирует смыкание рядов.
- **ДРЁМА ПЕРЕЗАРЯДКИ ЖИВЁТ В ЯДРЕ** (`Unit._atk_snooze` + `ArmyCore.TickSnooze`,
  сент. 2026, этап C): боец, стоящий В УПОР к цели и ждущий остывания удара, в
  GDScript-автомат боя не входит вовсе (гейт в диспетчере `tick_physics`);
  таймер и стражу цели (жива, в пределах `reach` с люфтом
  `ATK_SNOOZE_REACH_SLACK` — без люфта качание свалки будило весь фронт и
  съедало весь выигрыш) ведёт C#-проход. Пробуждение — СОБЫТИЕ: за
  `ATK_SNOOZE_SPARE` до готовности, по гибели/уходу цели; дрёму снимает смена
  цели, остаток кулдауна возвращается в `_attack_timer` (на время дрёмы поле
  заморожено — `_sweep_volleys` видит честное «не готов»). Лучник в залпе не
  дремлет по построению. **Честная цена по A/B чередованием (qa_mass3k, ручка
  `perf_config.atk_snooze`): +0.47 мс тика.** Это ТРЕТИЙ замер закона «смена
  языка окупается только у пакетной работы»: однородное ожидание — отдаёт,
  разнородные решения боя — нет (первые два — в шапке
  `perf_config.batch_combat`). Межпрогонные сравнения тут врут: фаза без
  дрёмы уплывала на 7 FPS между прогонами одного кода — только чередование.
- **СКВОЗЬ ЧУЖОЙ СТРОЙ НЕ ПРОХОДИТ НИКТО** — ни бег, ни отход. У отхода
  (`retreating`) такое послабление БЫЛО, и оно же оказалось жалобой владельца
  «красные рыцари пробегают сквозь фалангу, как призраки»: отряд ИИ, решивший
  уйти в замок, шёл домой по прямой и проходил строй насквозь. Замер
  `qa_mega_battle` B2-3: **141 отходящий рыцарь из 151**, при том что обычным
  приказом сквозь строй не проходил НИ ОДИН (B2-1: 0 из 495) — дыра была ровно
  одна. Убрано в ОБОИХ местах сразу: `csharp/ArmyCore.cs` (горячий путь,
  `FRetreating` снят из маски прохода) и `Unit._move_blocked` (запасная дорога);
  правило обязано совпадать в обеих, иначе его просто нет. Вечного трения о
  чужую шеренгу это не создаёт: отряд в контакте не отступает вовсе
  (`AICfg.AI_NO_RETREAT_IN_MELEE`), а своих ему по-прежнему раздвигает
  `SEP_PASS_RELIEF`. Проход остался у ДВУХ признаков: `FClearEnemy` (коридор
  отряда) и `FOrderPass` — заработанный билет игрока на выход из свалки.
  Бег (`sprinting`) сквозь тела не проходил и раньше, он только не вступает в бой.
- **БИЛЕТ ОТХОДА ВЕРНУЛСЯ, НО КАК БИЛЕТ НА ВЫХОД** (`Unit._retreat_pass`,
  сент. 2026). Снятие прохода у отхода ЦЕЛИКОМ тоже оказалось замерено:
  `qa_spear` C9 «внутрь замка зашли 3 из 6» — половина отряда, отозванного в
  гарнизон, застревала о противника на месте боя. Мирит их тот же приём, что
  у приказа игрока: пока боец не отошёл от точки начала отхода дальше
  `PASS_CLEAR_DIST` (2.5 м), он получает ТОТ ЖЕ бит `F_ORDER_PASS` — смысл у
  обоих один, «выбраться из тел, в которых уже стою», и второго признака не
  заведено.
- **БИЛЕТ ПРОХОДА СУЖАЕТ ТЕЛО ДО ЯДРА, А НЕ СНИМАЕТ ЕГО**
  (`ArmyCore.PassCoreFrac` = `Unit.PASS_CORE_FRAC` = 0.55, то есть ядро
  0.30 м при `BLOCK_RADIUS` 0.55). Любой билет раньше снимал проверку чужих
  тел ЦЕЛИКОМ, а 2.5 м «выхода» — это больше глубины фаланги в контакте
  (`qa_clash`: четыре шеренги сжимаются до 1.2 м): мечник, получивший приказ
  в упор к строю, проходил его насквозь. С ядром сквозь щель между двумя
  телами шеренги (0.17-0.28 м до ближайшего центра) не пройти ни с каким
  билетом. **ЦЕНА НАЗВАНА ЧИСЛОМ:** пройти между двумя телами можно только
  в просвет шире ДВУХ ядер (0.60 м), а самый редкий строй в игре стоит по
  `squad_spacing` 0.55 — то есть с ядром 0.30 билет не проводит НИ СКВОЗЬ
  ОДИН строй, включая кольцо окружения. Это выбор в пользу «сквозь строй не
  проходит никто». **РЕШЕНИЕ ВЛАДЕЛЬЦА (03.09.2026): ЯДРО 0.55 ЗАФИКСИРОВАНО,
  ФАЛАНГА МОНОЛИТНА, ВЫХОД ИЗ ПЛОТНОГО ОКРУЖЕНИЯ СКВОЗЬ ТЕЛА НЕ ТРЕБУЕТСЯ.**
  Ядро 0.25 открыло бы стоящий строй (просвет 0.55), ядро 0.17 — сжатый в
  бою (0.35): промежуточного значения, которое разделяет «кольцо» и
  «шеренгу», не существует — геометрически это одна щель. `qa_disengage` D2
  переписан под решение и стережёт обе его стороны: D2a — сквозь кольцо с
  просветом 0.29 м (теснее боевой шеренги) боец НЕ выходит, D2b — из кольца
  с просветом шире двух ядер выходит с первого приказа, то есть билет
  работает там, где ему положено.
- **ПОД БИЛЕТОМ ТЕЛО ЗАПРЕЩАЕТ СБЛИЖЕНИЕ, А НЕ ПРЕБЫВАНИЕ; БЕЗ БИЛЕТА — КАК
  БЫЛО** (`ScanBlock` / `EnemyBlock`, аргумент `awayOk`, 03.09.2026).
  Блокируется любой шаг, чья новая точка лежит внутри радиуса тела, — и боец,
  уже оказавшийся внутри, не может шагнуть ни вдоль, ни прочь. Под билетом
  это гибельно: ядро 0.30 меньше игрового просвета 0.33, внутри него боец
  бывает законно. Поэтому под билетом шаг, УВЕЛИЧИВАЮЩИЙ дистанцию до тела,
  проходит (в обеих дорогах и в разведении своими — оно тоже зовёт
  `ScanBlock` и вправе вытолкнуть бойца с билетом прочь от чужого тела).
  **ДЛЯ ВСЕХ это правило пробовали и откатили:** `qa_crowd` A1/A3 — толпа из
  24 своих у спящей орды переставала замирать (дрожание 0.0000 → 0.0647 м),
  прижатый своими к чужому телу полз вдоль него вместо того, чтобы встать.
- **ПОД БИЛЕТОМ БОЕЦ СКОЛЬЗИТ ВДОЛЬ ЧУЖОГО ТЕЛА, БЕЗ БИЛЕТА — ВСТАЁТ.** Нормаль
  от тела смотрит НАРУЖУ, а ветка скольжения идущего снимает составляющую при
  `into > 0`, то есть наружную; шаг внутрь остаётся, второй проход его
  блокирует, и пехотинец у чужого тела стоит. Для обычного бойца это и есть
  тюнингованное «упёрся — дерись» (qa_wall C1, «щель 0.1 м»), и трогать его
  нельзя. Билет прохода (`FOrderPass`: выход по приказу и билет отхода)
  заведён ровно на то, чтобы ВЫБРАТЬСЯ, и под ним снимается ВНУТРЕННЯЯ
  составляющая. **И ЧУТЬ НАРУЖУ** (`PassSlideOut` = `Unit.PASS_SLIDE_OUT` = четверть шага
  вдоль нормали): нормаль считана в НОВОЙ точке прямого шага, а она ближе к
  телу, чем текущая, и «касательная» через неё проходит на миллиметры БЛИЖЕ
  к телу, чем стоит боец (0.302 против 0.3035 м) — правило «прочь» её
  честно отвергало, и боец стоял 12 с. Ловушка зонда: касательная, взятая по
  нормали в ТЕКУЩЕЙ точке, отвечает «свободно», и опыт «прямой запрос
  `enemy_block`» показывал зелёное там, где ядро отказывало каждый кадр.
  Замер `qa_spear` C9 (отзыв в гарнизон мимо одного врага в 0.30-0.32 м):
  без правок 5 из 6, с правилом «прочь» и скольжением без сдвига наружу —
  по-прежнему 5 из 6, со сдвигом — 6 из 6, при ядре 0.55. Правило в ОБЕИХ
  дорогах (`BatchMoveRows` и `Unit._move_blocked`). То же
  ядро стережёт бегущего: его скольжение по касательной шло БЕЗ второго
  прохода и в рваной шеренге заводило внутрь тел. **A/B на `qa_full_game_4k`
  (400 на 400, одна сцена, одно зерно): ядро 0 → 148 живых внутри 0.30 м от
  чужого тела в миг контакта (8.9 %), ядро 0.55 → 9 (0.5 %).** Число ОБЯЗАНО
  совпадать в обеих дорогах (пакетный шаг и `_move_blocked`): 03.09.2026
  ядро в C# было найдено в нуле при 0.55 в GDScript — это и была база A/B,
  не возвращённая на место. Стережёт `qa_full_game_4k` B1.
- **Залп лучников** (`forge_config` archer_1d): точка пересчитывается **в момент
  выстрела** (`squad_volley_point`), радиус тучи не ниже `VOLLEY_MIN_SPREAD` —
  иначе залп схлопывался в точку и добивал уже мёртвого.
- **У дальности ЖЁСТКИЙ ПОТОЛОК** (`STATS.archer.attack_range_cap`). Она растёт
  от кузницы и вписывается прямо в поле бойца при рождении — потолка не было
  вовсе, и на прокачанной кузнице лучник доставал через пол-экрана. Потолок
  ограничивает СУММУ, а не прибавку, и держится в ДВУХ местах: при рождении и
  при выдаче исследования уже стоящим на карте
  (`GameManager._apply_range_bonus_now`) — второй путь и уводил дальность за
  все пределы.
- **СТРЕЛОК НЕ ИДЁТ В РУКОПАШНУЮ ПО КЛИКУ** (`Unit._prefer_in_range`). Приказ
  атаки назначается по ОТРЯДУ, а жертву выбирает `squad_pick_member` —
  «наименее обстрелянный», то есть чаще всего НЕ ближайший. Стрелку доставалась
  модель с дальнего края чужого строя, и он шёл к ней прямо сквозь тех, кто
  стоял в двух метрах. Теперь тому, кто НЕ ПРЕСЛЕДУЕТ, назначенная цель
  подменяется на бойца ТОГО ЖЕ отряда, до которого уже дострелить. Пехоты это
  не касается вовсе: `pursues_target()` у неё true, и первая же строка
  возвращает вход.
- **Упреждение** — `ARCHER_LEAD_FACTOR` с потолком `ARCHER_LEAD_MAX`; промахи по
  бегущему даёт разброс, а не вынос. Кучность и темп зависят от `ARCHER_DRILL`.
- **Смена состояния FSM = срочный пересчёт позы** (`_wake_process`): без этого
  рабочий шёл к замку, махая топором.
- **Привязка спрайта к земле считается на каждую ленту** (`_anim_bottom_px`):
  кольцо выделения лежит на земле, и ноги обязаны быть там же при любой позе.
- **Фаза зацикленной анимации у каждого бойца своя** (`Unit._anim_offset`, от
  номера узла): иначе сотня бойцов дышит в один такт. Незацикленные ленты
  (удары) всегда начинаются с нулевого кадра.
- **БУФЕРЫ MultiMesh АРМИИ И ПОКАДРОВЫЙ ДОГОН КАРТИНКИ ЖИВУТ В ЯДРЕ**
  (`ArmyCore.Rb`/`BatchVisual`/`RowBind`, сент. 2026, этап C.1). GDScript
  (FarUnitRenderer) владеет жизненным циклом бакетов и СОБЫТИЙНЫМИ записями
  (смена ленты, кадр походки, урон, скрытие — теперь делегаты в ядро);
  сглаживание нарисованной точки, боббинг и запись позиций всех привязанных
  строк ведёт один C#-проход каждый кадр отрисовки, подача —
  `MultimeshSetBuffer` из ядра. Разлёт от тарана (`F_VIS_SELF` = 1 << 19)
  ведёт картинку сам; `draw_position()` привязанной строки читает ядро.
  Замер ЧЕРЕДОВАНИЕМ (ручка `perf_config.vis_core_path`): кадр логики
  −0.76 мс. Стенды читают буфер через `rb_slot`/`rb_dirty` (qa_wound).
- **КАДР ЛЕНТЫ ЛИСТАЕТ ЯДРО** (`ArmyCore.RowAnim` ← `Unit._push_row_anim`
  из `_set_anim` и `Spearman._apply_dir_tex`, ручка `perf_config.anim_core`,
  этап E1, 09.09.2026). Параметры ленты (кадров, к/с, зацикленность, фаза)
  уходят в строку ПО СОБЫТИЮ и БЕЗУСЛОВНО — ручку переключают на живой сцене,
  и колонки обязаны быть свежими; номер кадра ведёт `BatchVisual` и пишет в
  слот только на смену, ДО ветки разлёта (летящий тоже листает). GDScript не
  зовёт `_advance_look_frame`/`set_frame` у привязанной строки. При переезде
  между бакетами `RowBind` ставит кадр из своей фазы: GDScript-копия
  `_look_frame` у ведомой ядром ленты не листается. **Стоящий с зацикленным
  покоем теперь СПИТ по картинке** (`_process_can_sleep` и переопределение у
  копейщика): дышит за него ядро — снято прежнее «многокадровый покой не
  усыпляется».
- **КОЛЬЦА, ТЕНИ И ПОЛОСКИ ЗДОРОВЬЯ ВЕДЁТ ЯДРО** (`ArmyCore.DecalBind` /
  `HpBind`, ручка `perf_config.decal_core`, этап E2). Слои
  `SelectionDecalRenderer` и `HpBarRenderer` держат буферы в тех же Rb, что
  бакеты бойцов (`RbCreate`, `use_colors = true`, раскладка 16 float;
  материалы колец instance-цвет не читают), полный трансформ пишется по
  событию (`RbWriteXform`), позицию и долю жизни ведёт `BatchVisual` из
  нарисованной точки строки, подача — `RbFlush`. GDScript-обход `update_all`
  остался ЗАПАСНОЙ дорогой для бойца без слота в общей отрисовке (туман,
  гарнизон). Прежний путь — `draw_position()` (переход границы) плюс 24
  float в `PackedFloat32Array` на каждого выделенного в каждом кадре — на
  2000 выделенных и 4856 полосках стоил **71 мс кадра логики** (`qa_visab`).
  Отвязка — по событию снятия слота на СЫРОЙ ссылке (правило 5), `Release`
  строки чистит привязки сам.
- **ПОЗА ПЕРЕСЧИТЫВАЕТСЯ ПО СОБЫТИЮ, РАСПИСАНИЕ — СТРАХОВКА** (этап E3,
  ручка `perf_config.pose_events`, `pose_safety_mult` 6). Полный разбор
  (`_update_sprite_anim` + `_update_sprite_flip` + переезд между бакетами)
  взводят: смена состояния, живого ряда, взгляда или направления хода больше
  чем на 20° (`POSE_TURN_COS2`, скалярное произведение без sqrt), истечение
  замка удара (`_lock_pending`), поворот камеры (`GameManager._cam_epoch`,
  аргумент `cam_epoch` в `tick_visual` — и в переопределении у мечника),
  возврат в кадр (`seen_now`), смена цели (`set_attack_target`). Замер
  `qa_visab` D на марше: −0.07 мс кадра логики — то есть периодический
  разбор и раньше стоил мало (vis_pose 0.5 мс/кадр на 4000); ценность правки
  — в том, что поза больше не запаздывает на такт (до полусекунды на большой
  армии), а не в миллисекундах.
- **ПОЛЁТ СТРЕЛЫ ВЕДЁТ ЯДРО — ХАК ФИЗТИКА №1, «ДЕКЛАРАТИВНЫЕ СТРЕЛЫ»**
  (`ArmyCore.ArrowLaunch` / `BatchArrows` / `TakeArrowEvents`, ручка
  `perf_config.arrow_core`). На время полёта стрела — ЗАПИСЬ (старт, конец,
  высота дуги, темп), узел без `_process`; ядро одним проходом двигает все
  полёты, пишет позицию и ось в слот общего MultiMesh и ищет попадание ТЕМ
  ЖЕ `EnemyAt` по XZ-радиусу `HIT_RADIUS` (правило сохранено дословно: стрела
  бьёт того, над кем пролетает в 0.35 м по земле, — высота не проверялась и
  раньше). Наружу — события пачкой раз в кадр (`ArrowRenderer.drain_events`
  → `Arrow.core_event`: удар, здание, втыкание — тот же код, что был в
  `_process`). Узел остаётся для втыкания, тела, растворения и пула; отмена
  полёта — в `_despawn`. Замер `qa_full_game_4k` (окно): цена стрел «логика
  полёта, узлы» 1.52 мс кадра → в шуме (−0.46), проход ядра `arrow_core`
  0.40 мс/кадр при 60-70 в полёте. A1-A3 (висящие, буфер, потолок) зелёные,
  `qa_volley` 23/23, `qa_corpse`, `qa_bldattack` зелёные.
- **ХАК №2 «ОТРЯД КАК СТЕНА-ОТРЕЗОК» НЕ ПОСТРОЕН — ЕГО ПОТОЛОК ИЗМЕРЕН
  И РАВЕН НУЛЮ** (`qa_ab` блоки F/F2, ручка `ArmyCore.SkipBodyScan`,
  09.09.2026). Проверка чужих тел на шаге (`ScanBlock`/`EnemyBlock`)
  выключалась ЦЕЛИКОМ — никакая замена её отрезками не выиграет больше,
  чем полное её отсутствие: рубка 3.12 → 3.04 мс тика (+0.09, 2.7 %, в
  шуме), сближение 3.80 → 3.95 (хуже: без тел строй уплотняется и дерётся
  гуще). Тик рубки держат решения боя в GDScript (`process_attack`,
  `atk_strike`, `atk_find_enemy` ≈ 5 мс кадра из 8.7), а не геометрия;
  ручка и блоки оставлены как измеритель.
- **ТИК АРМИИ НА ПОЛОВИННОЙ ЧАСТОТЕ ПОСТРОЕН И ВЫКЛЮЧЕН ПО ЗАМЕРУ**
  (`perf_config.army_tick_div` = 1, этап C.2). Глобальные 30 Гц движка сняты
  за один прогон шлюза (удвоенная дельта мелких сцен — qa_wall поймал
  протекание; часы стендов в физкадрах). Половинный тик армии с компенсацией
  шардов (личные 15 Гц бойца сохранены) измерен A/B чередованием ДВАЖДЫ:
  экономия ~5% физтика-за-секунду, кадр логики +2.2-2.7 мс, FPS −0.7…−1.3 —
  редкий тяжёлый тик ломает пейсинг. Ручка и A/B-блок qa_mass3k остаются.
  **ФАЗЫ ШАРДОВ СЧИТАЮТСЯ ОТ `GameManager.army_ticks`, НЕ ОТ НОМЕРА КАДРА
  ДВИЖКА:** при делителе и чётном числе шардов кадровая фаза замирала, и
  половина армии не тикала вовсе.
- **ПАКЕТНЫЕ ПРОХОДЫ ЯДРА МНОГОПОТОЧНЫ** (`ArmyCore.CoreThreads`, этап D2):
  фаза РАСЧЁТА разведения и BatchVisual считаются диапазонами строк через
  Parallel.For. Двухфазность разведения БЕЗУСЛОВНАЯ (расчёт читает снимок,
  применение — последовательное: вода, узлы), поэтому потоки не меняют
  результат ни на бит. Замер чередованием: +0.52 мс тика, +1.9 FPS. Потокам
  разрешена ТОЛЬКО чистая математика по колонкам — ни GDScript, ни дерева
  сцены, ни RenderingServer. Ручка `perf_config.core_threads`.
- **МАСКА ТУМАНА СЧИТАЕТСЯ В ЯДРЕ** (`ArmyCore.FogRefresh`, этап D3): штампы,
  «разведано» и сборка RGBA — C#; GDScript собирает источники и хранит копии
  lit/seen (is_lit бойца — без границы языков). +0.9 FPS. Ручка
  `perf_config.fog_core`.
- **КАРТИНКА ВСЕХ СТРЕЛ — ОДИН MultiMesh** (`ArrowRenderer` +
  `shaders/mm_arrow.gdshader`, этап D3): ось и дизер-растворение едут в
  instance-цвете, буфер — Rb-инфраструктура ядра. Узел Arrow остался логикой.
  36 торчащих = +1 вызов отрисовки (был +36). Порог среза — общебакетный
  uniform (гашение экземпляра до него не дотягивается — qa_corpse K1).
  **«СТРЕЛЫ ЗАВИСАЮТ ПАЧКАМИ В ВОЗДУХЕ» — СЛОТ ЗНАЕТ ТОЛЬКО ТО, ЧТО В НЕГО
  ЗАПИСАЛИ.** Пока стрела была узлом со своим спрайтом, перенос узла в точку
  втыкания переносил и картинку; в общем MultiMesh слот писала `_apply_axis`
  ЕЩЁ В КОНЦЕ ДУГИ, на высоте груди цели, а следующая запись с верной точкой
  шла из `_set_fade` через сорок секунд. Лечение — явный `_push_visual()` в
  `_stick_into_ground` после переноса. Стенд `qa_full_game_4k` A1 меряет
  подъём торчащей над грунтом по буферу (`rb_slot`): 0.20-0.23 м у всех,
  висящих ноль.
  Прежняя строка «стрела — это узел со своим вызовом отрисовки» ниже по файлу
  описывает ИСТОРИЮ и потолки (потолки в силе — узлы-то остались).
- **ТЫЛОВОЙ НАПОР СЧИТАЕТ ЯДРО** (`ArmyCore.RearPressPass`, этап D1, сент.
  2026): пехотинец с рангом >= 2 в дерущемся отряде давит к точке боя шагом от
  ядра, его GDScript-тик пропускается; АРЕНДА из `_recalc_melee`, пробуждения
  событиями (упор/удар/смена цели/паника). НЕ на конницу (съедал кабанам
  разгон) и не под замком. Замер qa_ab: **+0.34 мс тика на фазе сближения**;
  в замершей рубке ноль — глубокого тыла там нет (qa_clash: полторы шеренги),
  этих бойцов уже накрывает дрёма. Автопилот подхода (`AutopilotPass`)
  построен и ВЫКЛЮЧЕН по замеру (+0.008): обязательные ворота «только чистый
  путь» (без них qa_wall A1 уезжал с 1-3 на 5-8 просочившихся) не оставили
  ему сцены. Спринт D остановлен на gate-check D1 — разбор и пересчитанный
  потолок в `docs/PLAN_D_70FPS.md` и ENGINEERING_LOG.
- **МНОГОКАДРОВЫЙ ПОКОЙ НЕ УСЫПЛЯЕТСЯ ПО КАРТИНКЕ** (`Unit._process_can_sleep`,
  сент. 2026). Кадры в общей отрисовке листает ТОЛЬКО визуальный тик — узел
  спрайта на паузе (`_look_detach_node`), — и уснувший боец замирает на
  случайном кадре idle. Ровно это и было «лучники и рыцари еле дышат, а
  копейщик покачивается»: fps у всех лент одинаковый (6 к/с), но правило «спать
  можно, только когда листать нечего» держал один копейщик. Поднято в базу
  (`_look_loop and _look_frames > 1` — не спим) и продублировано у мечника: его
  переопределение не зовёт super, а комментарий «AnimatedSprite3D крутит цикл
  сам» устарел ещё при переезде на MultiMesh.

## Ударная конница (кабаны)

- **Натиск с разгона и постоянное продавливание — РАЗНЫЕ механики**, и путать
  их нельзя. Продавливание (`_apply_push`) идёт всю драку, на каждый удар;
  натиск (`_charge_impact`) — одно событие на один подход.
- **Разгон взводится ОДИН РАЗ на подход** (`Unit._charge_ready`), и одного
  этого мало: условие взвода — ДИСТАНЦИЯ до цели, а в свалке цель меняется
  каждые пару секунд, и новая нередко оказывается за десять метров. Условие
  выполнялось снова, СТОЯ НА МЕСТЕ, — отсюда «разлёт дважды за одно
  соприкосновение». Поэтому проверяется не намерение, а ФАКТ: сколько всадник
  реально проехал с момента взвода (`charge_min_runup`, точка `_charge_from`).
  В плотном бою столько не проехать.
- **УДАР ЛОВИТСЯ ПО КОНТАКТУ, А НЕ ПО АРИФМЕТИКЕ ДИСТАНЦИЙ.** Тик бойца
  шардирован, и «дистанция стала меньше дальности» замечалось с опозданием:
  всадник успевал въехать в строй, и только потом срабатывал разлёт («сначала
  залетают, потом разлетаются»). `_enemy_contact` ставится в тот же кадр, когда
  шаг реально упёрся в чужое тело, — это и есть миллисекунда первого касания.
- **Удар с разгона считается от МАКСИМАЛЬНОГО запаса ЖЕРТВЫ**
  (`charge_impact_frac`), а не от урона седока: сминает вес и скорость кабана.
  Накрывает круг `charge_splash` вокруг точки касания — «первый ряд контакта»,
  а не отряд и не одну модель.
- **ОТБРАСЫВАНИЕ ТЕПЕРЬ СКОРОСТЬ, А НЕ ПЕРЕНОС** (`Unit._fling_vel`,
  `_tick_fling`). ТРЕБОВАНИЕ РАЗВЁРНУТО ВЛАДЕЛЬЦЕМ: «пехотинцы отлетают
  резкими брызгами, практически телепортируясь». Прежнее решение (см. абзац
  ниже, оставлен как история) переносило тело на 2.2 м ОДНИМ кадром, а
  плавность изображала картинка — она догоняла тело замедленным сглаживанием.
  Догоняет она ПОЗУ, а не движение: боец всё равно исчезал из строя и появлялся
  в стороне. Теперь у бойца есть ЗАТУХАЮЩАЯ СКОРОСТЬ — единственное поле
  скорости в проекте, заведённое ровно под этот эффект, — и её интегрирует
  физический тик. Дальность удара не изменилась: `v0` считается так, чтобы за
  `FLING_SEC` пройти заказанные метры. Замер: старт 14.6 м/с = **0.24 м за
  кадр** при пороге телепорта 2 м.
- **ЛЕТИТ ПО ВЕКТОРУ УДАРА, А НЕ ВБОК** (`CHARGE_FLING_BACK` 0.92 против
  `CHARGE_FLING_SIDE` 0.36). Тоже разворот требования: раньше было
  преимущественно вбок, чтобы строй разошёлся и открыл проход. Боковая доля
  оставлена малой намеренно — снеси её совсем, и весь накрытый ряд поедет
  строго параллельно, оставаясь такой же плотной стенкой метром позади.
- **ВСАДНИК НЕ ВЯЗНЕТ В ПЕРВОЙ ШЕРЕНГЕ** (`charge_breakthrough` 1.8 м). Удар
  был ОДНИМ СОБЫТИЕМ, после которого всадник переходил в обычную рубку — вся
  его масса уходила в один кадр. Теперь та же скорость выдаётся и ему самому,
  вперёд: он въезжает в пробитую дыру. Это НЕ новая ветка автомата состояний,
  а тот же `_tick_fling`, и потому оно само складывается с продавливанием.
- **ПЛАВНЫЙ КАНАЛ — ТОЛЬКО У КОННИЦЫ, И ЭТО ЗАМЕР, А НЕ ПОЛОВИНЧАТОСТЬ.**
  Сначала через скорость пошёл ЛЮБОЙ толчок, включая пехотный. Толкают в рубке
  всех и постоянно, а едущий по скорости боец платит КАЖДЫЙ КАДР записью точки,
  `sync_row` и высотой рельефа — вместо одной записи на удар. Пехотный шаг при
  этом четыре сантиметра, и плавность там никто не увидит. Через скорость идёт
  только `charge_push_mult > 1.0`.
- **Отбрасывание — прямое смещение, а не импульс** (ИСТОРИЯ: так было до
  разворота требования выше). Физтел в игре нет вовсе,
  скорость никем не интегрируется между кадрами; импульс потребовал бы своего
  поля и своей ветки в тике. Направление ПРЕИМУЩЕСТВЕННО ВБОК: строй должен
  РАЗОЙТИСЬ, открыв проход, а назад его двигает продавливание, и дублировать
  его здесь означало бы удвоить ту же силу. Разметка гнётся вместе с телами
  (`squad_dent`), иначе смыкание вернуло бы всех на места.
- **Копейщики держат лобовой навал** (`Unit.repels_charge`, переопределяют оба
  копейщика). Это свойство РОДА ВОЙСК, а не стойки: копья опущены навстречу
  коннице и в «атаке», иначе фаланга была бы уязвима ровно тогда, когда идёт
  вперёд. С фланга и с тыла не работает — там и проходит конница. Лоб считается
  по `CHARGE_FRONT_COS`, и это ТО ЖЕ число, что у `PHALANX_FRONT_COS`:
  «в лоб» обязано означать одно и то же в обеих механиках.
- **Толчок идёт на КАЖДЫЙ удар** (`Unit.PUSH_EVERY` = 1). Прежняя десятка
  давала копейщику толчок раз в двадцать секунд — механики на экране не было
  вовсе. Плавность держит не редкость, а размер шага (потолок 0.4 м ×
  `PUSH_GLOBAL_SCALE`). Паритет при равном напоре сломаться не может:
  `_apply_push` выходит на неположительной разнице.
- **Напор и упор КЭШИРУЮТСЯ** (`Unit._refresh_power`, ключ — версия таблицы
  бонусов + стойка + мораль + push_force). С постоянным расчётом они считаются
  на каждый удар и дважды (свой и чужой), а складываются из чтений в
  автозагрузке. Замер qa_mass_battle на 5000: без кэша переход на «каждый удар»
  стоил около миллисекунды свалки, с кэшем — ничего сверх разброса прогонов.
- **Строку ядра толчок НЕ сводит.** Разбор наложения читает колонку, а не узел,
  и до ближайшего тика цели видит её на старом месте — но строка сходится сама,
  а толчок накапливается (qa_cavalry E1: 42 сдвинутых из 46, худший на 7.9 м).
  Дописывать точку в колонку прямо в `_apply_push` пробовали и не оставили:
  пользу замером подтвердить не вышло, а разброс прогонов там шире эффекта.

- **`push_force` САМ ПО СЕБЕ НИЧЕГО НЕ РЕШАЕТ.** Смещение считается как
  `clampf(разница напора × 0.15, 0.04, 0.4)`, и потолок срабатывает уже при
  разнице в три единицы: кабан с `push_force` 15 двигал строй ровно на
  столько же, на сколько мечник с тройкой. Рабочие рычаги — два новых поля
  в `unit_stats_config.STATS`:
  - `charge_push_mult` (2.0) применяется **ПОСЛЕ** потолка. До него его съел
    бы тот же `clampf`;
  - `push_every` (2 против пехотных 10). Прежний строй продавливался на семь
    сантиметров раз в десять секунд — этого на экране нет вовсе.
- **`GameManager.squad_dent()` гнёт РАЗМЕТКУ, а не только тела.** Отброшенный
  боец вернулся бы на своё место при первом смыкании рядов, и от удара не
  осталось бы следа. Места в `DENT_RADIUS` от точки удара уезжают назад с
  затуханием — получается вмятина, а не сдвиг всей линии. Потолок
  `DENT_MAX_TOTAL` (4 м) обязателен: без него отряд под долгим напором уехал
  бы разметкой за край карты. Обнуляется новым приказом строем.
- **Смыкание рядов здесь НЕ зовётся.** Оно шлёт `command_move` каждому бойцу,
  а тот снимает цель атаки: в разгар рубки это остановило бы бой (ровно от
  этого и заведено `squad_in_combat`). Вмятину подхватит первое штатное
  смыкание, когда драка кончится.
- Урона всё это не касается вовсе — двигаются только точки. Стенд
  `qa_cavalry` (12 проверок) стережёт и это ограничение.

## Гоблины (`scripts/goblin/`)

- `goblin_config.gd` — ПОВЕДЕНИЕ орды (деревня, спячка, волны, отход). Сила
  удара и запас жизни — в `unit_stats_config.STATS` рядом с людьми.
- Юниты — обычные `Unit` без единой особой ветки: бой, строй, туман, ветеранство
  и общая отрисовка достаются им от базы. Сквозь чужой строй они не проходят не
  потому, что это где-то написано, а потому, что для них НЕ написано исключений.
- `GoblinHut extends Castle` — ради готового гарнизона (принять разбитый отряд,
  спрятать, лечить, доукомплектовать, выпустить). Замок вынес настройку полей в
  `_configure()` именно ради этого наследника: `super._ready()` не умеет звать
  метод через голову родителя. Хижина даёт еду, склада не имеет.
- Расписание (СПРИНТ 17, спячки больше нет — `DORMANT_UNTIL_SEC` 0, механизм
  сна `set_physics_process(false)` + `F_DORMANT` оставлен): мирная фаза
  `PEACE_SEC` 600 (гарнизон, патруль границы) → волна в центр → HARASS (рудник
  холма, рейды, разведка) → штурм не раньше `ASSAULT_EARLIEST_SEC` 2100 по
  разведанной базе → оборона деревни и найм заново → снова центр. Центр
  считается взятым, ТОЛЬКО когда орда до него дошла.
- **ПРИКАЗЫ ОРДЫ: РЕШЕНИЕ СРАЗУ, ИСПОЛНЕНИЕ ПОРЦИЯМИ И ТОЛЬКО ПО ДЕЛУ**
  (сент. 2026). `_issue_orders` выдавал command_attack/command_move КАЖДОМУ из
  тысячи гоблинов каждый такт — горб 46-72 мс раз в 2 с (зонд
  `qa_mass3k/AIProbe`), на кадре свалки — стабильные ~2.4 мс «мышления ИИ».
  Перенесены оба приёма красного ИИ: отряд, уже втянутый в бой, приказов не
  получает вовсе (цели раздают сцепка и авто-агро), марш в ту же точку — не
  чаще `REISSUE_KEEPALIVE_SEC`, а раздача нарезана очередью
  (`GoblinAI._drain_orders`, по `ORDER_BUDGET_MEMBERS` бойцов за кадр).
  Итог: 476 → 86 мс на 900 кадров, максимальный горб 72 → 12 мс.
- Число шардов выводится из ХОДЯЩИХ (`GameManager.active_units`), а не из
  размера реестра: 850 спящих иначе загоняли игру в два шарда с первой секунды.
- Деревня — плотная сетка хижин с шагом `HUT_STEP` (6 м) на расчищенной
  площадке (`GOBLIN_VILLAGE_CLEAR` в резерве, до посадки леса). Хижина
  АНИМИРОВАНА: `Building.sprite_fps()` > 0 листает ленту (дым из трубы), фаза
  своя у каждой (`sprite_frame_phase`).
- Строй орды — ТОЛПА, а не каре: места по спирали золотого угла плюс
  детерминированный сдвиг (`goblin_config.horde_offset`); эти же точки идут в
  разметку отряда, чтобы смыкание после боя собирало толпу.
- У гоблинов СВОЙ `PIXEL_SIZE` и высота центра спрайта считается ПО ЛЕНТЕ, а не
  константой — поэтому ноги остаются на земле при любой смене масштаба. Размер
  выведен из одного числа `goblin_config.SIZE_SCALE` (сейчас 1.7, заказ
  владельца): от него же растянуты `HORDE_SPOT`, `SQUAD_SPACING` и радиус
  расталкивания. Менять `PIXEL_SIZE` в одиночку нельзя.
- **Радиус расталкивания у бойца ЛИЧНЫЙ** (`Unit.sep_radius()` → колонка
  `_sepR` в `ArmyCore`, ставится один раз при рождении строки). Ноль означает
  «как у всех», то есть общую `Unit.SEP_MIN_DIST`; гоблины возвращают своё
  число. Без этой колонки выбор был из двух плохих: либо орда слипается в
  ковёр, либо ради неё раздвигается вся людская пехота.
- Массовые стенды деревню выключают (`perf_config.goblin_village = false`): она
  растягивает габарит сетки на свой угол карты и портит замер армии.

## Плотность строя и разведение отрядов

- **Личный круг бойца выводится из САМОГО ПЛОТНОГО СТРОЕВОГО ИНТЕРВАЛА в
  проекте** — тогда разбор наложения физически не может спорить ни с одним
  построением. `PERSONAL_RADIUS` 0.25 при `Building.squad_spacing` 0.55.
  Прежние 0.175 были выведены из 0.35 и за интервалом не поехали, когда тот
  подняли, — оттого тысяча бойцов и сминалась в пятно вдвое уже своей разметки.
- **`ARRIVE_RADIUS` жёстко связан с `SEP_MIN_DIST` и обязан быть БОЛЬШЕ.**
  Иначе боец, идущий в точку, где стоит союзник, останавливается разбором
  наложения ЗА пределами допуска — то есть никогда не отчитается о прибытии.
  Стережёт `qa_push` D0б (и `qa_crowd` A0).
- **ОТРЯДЫ НЕ ВСТАЮТ ДРУГ НА ДРУГА** (`GameManager.free_squad_spot`). Жалоба
  владельца: несколько отрядов, посланных в одну точку (или новобранцы на точку
  сбора, где уже кто-то стоит), накладываются и непрерывно толкаются.
  **РАСТАЛКИВАНИЕМ ЭТО НЕ ЛЕЧИТСЯ** — оно разводит ТЕЛА и делает это верно, но
  про отряды не знает: две сотни бойцов, которым велено стоять в одном месте,
  оно будет разводить вечно, потому что каждый лезет на свою точку разметки, а
  точки совпадают. Лечить надо ПРИКАЗ: точка назначения проверяется на
  занятость один раз, в момент выдачи, и при занятости отходит вбок.
- **МЕСТО ОТРЯДА — ЭТО КУДА ОН ИДЁТ, А НЕ ГДЕ ОН СЕЙЧАС** (`_squad_spot_centre`
  берёт центр `post_pos`, а живой центроид — лишь запасной ответ). Первая
  версия смотрела на текущий центр масс, и этого не хватило ровно в том случае,
  ради которого всё писалось: игрок шлёт отряды в одну точку ОДИН ЗА ДРУГИМ.
  Второй приказ отдаётся, пока первый отряд ещё в пути и физически далеко, — по
  текущему центру он место не занимает, и оба приходят друг на друга (замер:
  одна перекрывающаяся пара, 1.04 м). Занятым место делает ПРИКАЗ, а не тело.
- **ОТРЯДЫ ОДНОГО КЛИКА ДРУГ ДРУГУ НЕ ПОМЕХА** (аргумент `ignore`). Их уже
  развела сетка блоков (`_issue_group_grid_move`); посчитай их занятыми — и
  группа разъедется вместо блока, разведённая дважды.
- **Бойцы РАЗНЫХ отрядов держатся шире, чем свои по шеренге**
  (`Unit.SEP_CROSS_SQUAD`, считает `ArmyCore.BatchSeparation`). Это и есть
  «десять отрядов не спрессовываются в комок размером в два». Отталкивания
  ЦЕНТРОВ отрядов нет и не будет: центр — величина производная (медиана), и
  двигать по ней людей означало бы завести вторую механику перемещения с
  силами и затуханием — ровно то, от чего в проекте однажды уже отказались.
  Отряд 0 («без отряда»: рабочие, одиночки, гарнизон) расширения не получает.
- **Приказ атаки нескольким отрядам раскладывается ПО ШИРИНЕ чужого фронта**
  (`SelectionManager.frontline_targets`). Цель по-прежнему ОДНА НА ОТРЯД —
  внутри отряда её разбирает `squad_pick_member`, — но левый наш отряд идёт на
  левый фланг противника, правый на правый. Ось считается тем же способом, что
  и у ИИ (`EnemyAI._enemy_front_normal`): ковариация XZ и `atan2`. Один скан на
  клик, покадрового пути здесь нет. Меньше двух отрядов или меньше трёх врагов
  рядом — раскладки нет вовсе, приказ идёт прежним путём слово в слово.

## Приказы, погоня и строй ИИ

- **РЕШЕНИЕ ОБ ОТХОДЕ ФИКСИРУЕТСЯ ПО ВРЕМЕНИ.** Отряд ИИ подходил, получал
  стрелы, начинал убегать, разворачивался и умирал в спину: такт размышления
  пересчитывал цели и выдавал отступающему новую роль. Теперь замка ДВА, и оба
  обязательны, потому что решение и его исполнение живут в разных местах:
  роль держит `EnemyAI._retreat_locked` (единственная точка, через которую роли
  вообще меняются, — `_set_role`), а исполнение держит `Unit.retreat_locked()`
  — до бойца доходят приказы не только от ИИ (смыкание рядов, сплочённость,
  возврат на пост), и любой звал `command_move` без `keep_retreat`, то есть
  гасил отход. Сроки держатся ОДНИМ числом на обоих концах
  (`Unit.RETREAT_LOCK_SEC` = `AICfg.RETREAT_MIN_SEC`).
- **Отменить отход могут ровно две вещи:** ближний бой (отряд спиной к врагу не
  отступает, а гибнет) и истечение срока. Прибытие в замок снимает его
  принудительно — `end_retreat(true)`.
- **СМЫКАНИЕ — ОДНО ДЕЙСТВИЕ И ОТРЯДНОЕ. ПОШТУЧНОГО ВОЗВРАТА НЕТ И НЕ БУДЕТ.**
  Был заведён второй, «лёгкий» путь: он слал `command_move` ОТДЕЛЬНЫМ бойцам —
  тем, кто съехал с места и ничем не занят, — и работал даже посреди боя.
  Задумка была в том, что дерущихся он не трогает. **НА ЭКРАНЕ ЭТО ОБЕРНУЛОСЬ
  ХАОСОМ**, и обе беды следуют прямо из «поштучно и в бою»:
  ОТРЯД РАЗРЫВАЛО (боец, у которого в свалке на секунду не стало цели, считался
  бездельником и уходил на свой `post_pos` — а тот остался там, откуда отряд
  ВЫШЕЛ; за ним второй, третий, и отряд вытягивался цепочкой прочь от схватки);
  РАБОЧИЕ БРОСАЛИ РАБОТУ (рабочий числится отрядом из одного человека, а
  `command_move` сбрасывает и рубку, и стройку, и добычу).
  Лечение — не третий костыль, а ОТКАЗ ОТ ВТОРОГО: действие снова одно,
  `squad_close_ranks`, только вне боя и только по отряду.
- **ЦЕНА КОСТЫЛЯ БЫЛА НЕ ТАМ, ГДЕ ЕЁ МЕРИЛИ.** A/B «выключить оба обхода»
  показал 0.11 мс — и я на этом успокоился. Ошибка в том, что мерилась цена
  САМОГО ОБХОДА, а не того, ЧТО ОН ДЕЛАЛ: каждый `command_move` будит бойца,
  метит позу грязной и переводит его в MOVING — то есть отнимает у него сон.
  После отката `qa_fps` на 2010 юнитах: **175 FPS против 134**, физтик 3.27
  против 4.44 мс. **Мерить надо последствия, а не вызов.**
- **РАБОЧИХ СТРОЙ НЕ КАСАЕТСЯ ВООБЩЕ** (`REFORM_SKIPS_WORKERS`). Разметка у
  отряда-из-одного-рабочего завестись МОЖЕТ (её достраивает
  `squad_close_ranks`), и дальше смыкание сбивало бы ему всю работу.
- **СТРОЙ СМЫКАЕТСЯ САМ, КОГДА ЕГО РАЗВАЛИЛИ** (`GameManager._sweep_reform`).
  Прежнее смыкание взводилось ТОЛЬКО боем (`squad_mark_hit`), а проход своих
  сквозь строй боем не является — отметка не ставилась, и дыры оставались до
  первой стычки. Редкий обход (раз в `REFORM_SWEEP_SEC`) смотрит, не уехали ли
  бойцы СТОЯЩЕГО отряда от своих мест, и смыкает. Причина отъезда не важна
  вовсе, и это главное достоинство: отдельный детектор «прохода союзника»
  ошибался бы и на расталкивании, и на толчке конницы, и на возврате из
  гарнизона — а лечение у всех этих случаев одно.


- **ШИРИНА ОТРЯДА СЧИТАЕТСЯ ТЕМ ЖЕ ПОСТРОЕНИЕМ, КАКИМ ЕГО И РАЗВОРАЧИВАЮТ**
  (`EnemyAI._squad_cols` / `_squad_width` / `_screen_step_for`). Шаг заслона уже
  выводился, а не стоял числом, — и всё равно давал кучу, потому что выводился
  из ОБЩЕЙ шестёрки колонок (`SQUAD_COLS`), а `_issue_plan` раскладывает
  копейщиков по ЧИСЛУ ШЕРЕНГ: 60 человек на `PHALANX_RANKS` = 4 это 15 колонок,
  то есть 8.7 м по фронту против планируемых 5.68. Семь отрядов перекрывались
  на три метра с лишним и сходились в одно пятно. Ширина считается по ПОЛНОМУ
  штату (доучившийся отряд занимает своё место и никого не двигает) и теперь
  участвует ВЕЗДЕ, где отряды разводят: заслон у базы, рубеж
  (`DEFENSE_LINE_WIDTH` — пожелание, ширина отряда — факт, побеждает факт),
  последний рубеж и боевая волна.
- **ЛИШНИЙ ЭШЕЛОН ПЕРВОЙ ЛИНИИ УХОДИТ ВПЕРЁД, А НЕ НАЗАД.** Как только ширина
  стала считаться честно, семь отрядов копейщиков перестали помещаться в
  `SCREEN_MAX_WIDTH`, и второй эшелон уехал назад РОВНО НА ЛИНИЮ ЛУЧНИКОВ
  (13 − 7 = 6 м при лучниках на 7 м): копейщики встали своим стрелкам в
  затылок, зазор между постами вышел 1.0 м. Позади копейщиков места нет и быть
  не может — там лучники, а за ними замок. Назад эшелонируются ТОЛЬКО лучники,
  и только до пола `SCREEN_MIN_DEPTH`. Ловят это две проверки `qa_ai`:
  «копейщики впереди лучников» и «посты гарнизона разнесены».
- **КУРС СТРОЯ ЗАДАЁТ ПЛАНИРОВЩИК И ЗАМОРАЖИВАЕТ** (`sq["face"]`, четвёртый
  аргумент `_set_role`). Гарнизону, заслону и последнему рубежу курс выведен из
  мест замка и базы игрока, то есть постоянен. Пока курс считался в момент
  выдачи (`_order_course` → `_enemy_front_normal`), он брался по ОБЛАКУ ЖИВЫХ
  БОЙЦОВ ИГРОКА: те шевелятся, главная ось облака гуляет, и каждые две секунды
  заслон получал новый угол разметки и переставлялся всем составом. Побочно это
  сняло скан всей группы игрока на КАЖДЫЙ стоящий отряд.
- **ЗАМОРОЖЕННАЯ ЦЕЛЬ (STAGNANT TARGET): СТОЯЩИЙ ЗАСЛОН ПРИКАЗ НЕ ПЕРЕИЗДАЁТ**
  (`EnemyAI._posts_intact`). Цель не менялась, курс задан планом, противника в
  зоне нет — раскладка мест не изменилась ни на сантиметр, и `_issue_plan`
  выдавал бы `set_stance` + `command_move` каждому бойцу впустую: раз в две
  секунды вся оборона разом делала шаг («пульсирующая мышца» из жалобы).
  Проверяется СВОЙСТВО — стоит ли строй по местам, — а не время. Раскладка в
  проверке считается ТЕМ ЖЕ кодом, что и в приказе, иначе проверка судила бы об
  одном строе, а приказ выдавал другой. Идущий отряд отвечает «не трогать»: он
  и так исполняет прошлый приказ. Стенд `qa_ai2`, блок H.
- **Заслон ИИ у базы: шаг между отрядами ВЫВОДИТСЯ, а не задан числом.**
  Стояла `SCREEN_WIDTH` = 18 м на весь фронт, делённая на число отрядов: при
  семи отрядах копейщиков центры выходили в 2.6 м друг от друга, а отряд из
  шестидесяти человек занимает около трёх метров в ширину и почти шесть в
  глубину. Места перекрывались, четыреста бойцов сходились в точку,
  расталкивание их разбрасывало, следующий такт гнал обратно — тот самый цикл
  «сжатие → выталкивание → сжатие». Теперь шаг равен ширине отряда плюс
  просвет (`EnemyAI._screen_step`), а не поместившиеся в линию уходят во второй
  эшелон (`SCREEN_MAX_WIDTH` / `SCREEN_ROW_DEPTH`).
- **НИ ОДИН ПОСТ ЗАСЛОНА НЕ СМОТРИТ В ТЫЛ.** Заслон стоит МЕЖДУ замком и
  противником; пост за замком не заслоняет ничего, а идут к нему сквозь
  собственные постройки. Эшелоны зажаты полом `SCREEN_MIN_DEPTH`, а фланговые
  мечники расходятся ВШИРЬ, а не вглубь: разнос по глубине уводил третью пару
  на −4 м за замок (поймал `qa_ai`, «гарнизон стоит заслоном лицом к врагу»).


- **МАКРО-СОСТОЯНИЙ В ОБОРОНЕ ТРИ, А НЕ ДВА** (`EnemyAI._update_recap`).
  `DEFENSIVE_MODE` знал «держать рубеж» и «последний рубеж у замка», и оба
  оборонительные; хуже, выход из последнего рубежа требует, чтобы противника у
  рубежа не осталось вовсе, — а он и не уйдёт, если центр его. Армия копилась
  до полного лимита и стояла. Третье состояние — **ОТВОЕВАТЬ ЦЕНТР**
  (`ROLE_RECAP`): центр под игроком И армия набрана от `RECAP_ARMY_FRACTION`
  (доля считается ПО БОЙЦАМ — отряды у разных родов разного размера) → дома
  остаётся `RECAP_HOME_GUARD` отрядов на род, всё остальное идёт единым фронтом
  на центр. **Проверяется ПЕРВЫМ, до последнего рубежа:** оба состояния
  срабатывают на одну обстановку («центр потерян»), и порядок проверок и есть
  приоритет. **Гистерезис двусторонний:** выйти можно отвоевав центр ИЛИ
  растеряв армию ниже `RECAP_ABORT_FRACTION`; без второго выхода разбитая волна
  ползла бы на центр по одному, без зазора между порогами ИИ менял бы решение
  каждый такт. Роль своя, а не `ROLE_ASSAULT`: штурм идёт на БАЗУ игрока и в
  обороне запрещён (это стережёт `qa_ai`), а здесь цель — спорная земля.
  Стенд `qa_ai2`, блок I.
- **ЗДАНИЕ ПОВОДКОМ ПОГОНИ НЕ ОГРАНИЧИВАЕТСЯ.** Поводок (`PURSUIT_LIMIT`, 7 м)
  придуман против РАСТЯГИВАНИЯ ОТРЯДА ЗА УБЕГАЮЩИМ. Дом не убегает никуда —
  а поводок его не различал, и на этом ломался весь приказ «снести лагерь»:
  снеся первую хижину и перейдя на соседнюю, боец делал к ней те самые семь
  метров, поводок рвался, СНИМАЛ ЗАМОК ПРИКАЗА и гнал бойца на пост — за
  полкарты. Жалоба владельца звучала как две разные («сносят одно здание и
  приказ слетает» и «бойцы бегут из боя на базу»), а причина одна.
- **ЗАМОК НА ПОСТРОЙКЕ ПЕРЕХОДИТ НА СОСЕДНЮЮ** (`_next_enemy_building`,
  `BUILDING_CHAIN_RANGE` 26 м). У замка на ОТРЯДЕ следующего выдаёт
  `squad_pick_member`; постройки в отряды не собраны, и «соседний дом» надо
  искать по карте. Радиус узкий намеренно: это «тот же лагерь», а не «любая
  постройка на карте». При переходе СНИМАЕТСЯ ЯКОРЬ ПОГОНИ — новый дом это
  новый подход.
- **ПРИКАЗ ИСЧЕРПАН — ВСТАЁМ ЗДЕСЬ** (`_hold_where_i_stand`). Пост бойца
  остаётся там, откуда отряд вышел, и после снятия замка авто-агро с возвратом
  на пост тянут его обратно. Пост переносится в текущую точку: приказ выполнен
  там, где боец стоит.
- **ЯКОРЬ ПОГОНИ — ОДИН НА ОТРЯД** (`GameManager.squad_pursuit_anchor`).
  Личный якорь и был причиной «отряд растягивается на полкарты»: передний боец
  касался цели раньше и ближе, задний позже и дальше, и дальше каждый принимал
  СВОЁ решение — передние гнали, задние срывали поводок и уходили на пост.
  Теперь якорь ставит первый дотянувшийся, меряются от него все, и поводок
  кончается у всех разом. Читается он ВНУТРИ условия `_pursuit_anchored and
  _attack_is_forced`, а не до него: это словарный поиск в автозагрузке, а ветка
  подхода — самая горячая в тике.
- **Поводок погони** (`Unit.PURSUIT_LIMIT`, 7 м): якорь ставится в точке
  ПЕРВОГО касания приказанной цели, а не в момент приказа — иначе сорвался бы
  законный приказ «атаковать вон тех, в сорока метрах». Дальше поводка боец
  забывает цель, снимает замок и возвращается на `post_pos` приказом. Не путать
  с `AGGRO_LEASH` (14 м): тот ограничивает ИНИЦИАТИВУ бойца от поста и на
  прямой приказ не действует вовсе.
- **Цена бега:** `SPRINT_SPEED_FACTOR` 1.35 и `SPRINT_DAMAGE_MULT` 1.35.
  Множитель урона применяется ПОСЛЕДНИМ, уже поверх щита: это открытость
  бегущего, а не пробитие брони.
- **Марш-перехват уже есть** и равен `attack_range + INTERCEPT_MARGIN` (0.8 м) —
  то есть 2–3 м у пехоты. Отдельного «detect_range» заводить не нужно.
- **Строй ИИ встаёт параллельно чужому фронту.** `EnemyAI._enemy_front_normal`
  берёт главную ось облака игрока (ковариация XZ, угол через `atan2`), а
  `_slot_at` раскладывает смещения по осям СТРОЯ, а не карты. До этого курс
  считался честно, но разметка его не читала — отряд всегда строился решёткой
  по осям карты и упирался в диагональную линию углом.
- **Фаланга в бою смотрит на драку, а не по курсу.** `Unit._phalanx_dir()` вне
  боя берёт `squad_course` (курс последнего приказа — по нему считаются ряды),
  но при `squad_in_combat` переключается на `squad_enemy_dir`. Иначе незанятые
  бойцы шагали ПО КУРСУ, то есть мимо сцепившейся линии, в пустое поле.
- **Плотность группы отрядов:** `SelectionManager.GROUP_CELL_GAP` 1.0 и потолок
  габарита ячейки `GROUP_CELL_MAX` 20 м. Ячейка одна на всех и берётся по
  максимуму — без потолка один растянутый отряд в выделении задавал ячейку всем
  остальным, и компактные блоки разъезжались по полю.
- **Контур приказанного/наведённого ЗДАНИЯ — свой меш и свой слой**
  (`UnitVisuals.building_ring_mesh`). Кольцо бойца — тор в 35 см о ДВЕНАДЦАТИ
  сегментах; растянутое до замка (×11) оно давало толстый угловатый
  многоугольник — и по толщине трубки, и по грубости обвода. Здешний тор
  единичного радиуса, тонкий и о 64 сегментах, а масштаб равен половине
  габарита постройки. Разные меши в одном `MultiMesh` не живут, поэтому у
  зданий отдельный слой — ленивый, то есть ноль вызовов, пока ни на одну
  постройку не навелись.
- **МЕТКА ПРИКАЗА ГАСНЕТ ПО ПРИБЫТИЮ — ЗНАЧИТ, ЕЁ НАДО ГАСИТЬ И ИНАЧЕ.**
  Рабочий, которого послали в точку, а потом отправили строить или рубить, до
  неё не дойдёт НИКОГДА — и кольцо остаётся висеть в чистом поле («фантомные
  круги, которых игрок не задавал»). Новая работа снимает прежний приказ
  (`squad_clear_order` в `Worker.command_build`/`command_gather`), расформирование
  отряда — тоже, а пустой отряд метку не удерживает: `_centroid_of` пустого
  списка отвечает НУЛЁМ, и от него до цели всегда далеко.
- **Указатели приказов:** `GameManager.squad_orders` (sid → вид/точка/цель),
  слои `set_move_marks` / `set_order_targets` в `SelectionDecalRenderer`.
  Это ОТРАЖЕНИЕ приказа для глаз, боевая логика его не читает — второе
  хранилище приказов заводить нельзя. Метка точки снимается сама по
  `ORDER_MARK_ARRIVE` (3 м от медианного центра), подсветка цели — с её
  гибелью. Стенды `qa_facing`, `qa_orders`.
## Навигация: сетка, маршруты, препятствия

**`NavigationServer` и `PhysicsServer` в проекте НЕ ИСПОЛЬЗУЮТСЯ ВОВСЕ.**
Вся навигация — своя: маска и A* в ядре (`ArmyCore.BuildNavGrid` / `NavPath` /
`NavLineBlocked` / `NavSpread`, `NAV_CELL` 2 м), нить с зазором и отжимом
углов. Просьба «оптимизировать NavigationServer» означает покадровые вызовы
наружу из тика — их здесь нет.

- **ТРИ ИСТОЧНИКА НЕПРОХОДИМОСТИ, И ПРАВИЛА У НИХ РАЗНЫЕ.**
  1. **Скалы** — маска крутизны (`BuildCliffMask`, `CLIFF_SLOPE` 0.45; порог
     стены у плато свой, общий 0.30 лежал бы в полосе пологого спуска). Шаг
     скользит вдоль обрыва с ПАМЯТЬЮ стороны (`_cliffSide`), пологий спуск —
     дорога.
  2. **Вода** — `RiverWater` в ядре (не вызов `is_water` из C# на каждый шаг),
     «вода там, где грунт ниже зеркала» (`wet_depth`, `SHORE_DRY` 0.10; полоса
     по ширине русла оставляла проходимым залитый верх откоса). Брод — дорога
     приказом, У КАЖДОГО СВОЯ ПОЛОСА (`Main.ford_route`, `Unit._ford_stage`);
     полоса считается от центра ГРУППЫ (`squad_set_ford_lane`), иначе отряды
     одного клика сходятся в одну нитку.
  3. **Фундаменты построек** — `Building.block_circles` →
     `GameManager.register_building_obstacle` → `ArmyCore.RegisterObstacle`:
     ряд кругов от передней стены (`BLOCK_FRONT` 0.35) до задней грани
     коробки. СВОЯ запись, не ствол: `FTrunkIgnore` сквозь дом не ведёт.
     Не держат руина, загон и пень (у пня ствол `LAIR_TRUNK_R`).
- **ЯЧЕЙКА — СТЕНА, ЕСЛИ ЗАДЕВАЕТ КРУГ** (`NavSync`, прямоугольник против
  круга с зазором `NavBldPad` 0.25). Прежнее «центр ячейки внутри круга»
  пропускало башню между центрами четырёх ячеек: A* её не видел, обходил ШАГ
  скольжением, сторона — чётность строки ядра, и соседи по шеренге расходились
  по разные стороны здания.
- **ЗДАНИЕ — НЕ СКАЛА.** Штраф дистанции до стены (`_navWall`) считают только
  скалы и вода; сосед дома стоит `NavBldAdjCost` 0.5, боковой зазор нити у
  дома `NavBldSidePad` 0.9, угол отжимается на `NavBldCornerPush` 1.0 и
  ПОДТЯГИВАЕТСЯ к точному кругу (`BldHug`, зазор `NavBldHug` 0.8) — отряд идёт
  в 0.2-0.6 м от края фундамента вместо крюка в 6.5 м. Лес — стоимость
  (`_navTree`), гиганту чаща от трёх стволов непрозрачна.
- **ОТСТУП ОТ СКАЛ — СВОЙСТВО РОДА ВОЙСК** (`Unit.nav_clearance()`: пехота
  1.5, конница 2.2, гигант 3.5, тролль 4.0; кэш `_nav_clear`, класс — в ключах
  кэшей). Он идёт во ВСЕ запросы к сетке: штраф у стены, зазор нити (clear/2),
  отжим угла (на весь отступ; `NavCornerPush` ищет ближайшую стену в радиусе
  отжима — обход по восьми соседям углы на 2-3 клетке не отжимал вовсе) и
  видимость прямой.
- **ДОРОГА ОДНА НА ОТРЯД, ОТ ЦЕНТРА** (`GameManager.squad_leg`, кэш 120
  кадров, ключ — ячейка 16 м с обоих концов плюс класс и ступень полуширины).
  Личный A* на каждого бойца был пиком 60-100 мс кадра при раздаче приказов
  орды. Чужой ответ бойцу отдаётся только после `ArmyCore.NavReusable`
  (прямые от его точки к первому углу и от последнего к цели свободны).
  **Нить и угол отжимаются на ПОЛУШИРИНУ СТРОЯ** (`NavPath(..., halfW)`,
  `SQUAD_ROUTE_HALF_W`), а дорога отряда проверяется ПОЛОСОЙ
  (`NavBandBlockedBld` в раннем выходе `NavPath`) — иначе фланг, упёршийся в
  башню, строил личный обход с другой стороны, и отряд рвался надвое.
  Боковой разнос по смещению бойца от центра (`NavSpread`, `NAV_LAT_MAX`) —
  колонна, а не нитка; смещение, легшее в дом, гасится.
- **ОБХОД ЕСТЬ У ЛЮБОГО ХОДА, НЕ ТОЛЬКО У `command_move`** (`Unit._nav_dir`
  поверх `_atk_waypoint`): подход к цели, возврат на пост и все четыре фазы
  рабочего (стройка, жила, склад, овца) раньше шли ПРЯМОЙ сквозь горы.
  Прямая проверяется раз в `NAV_RECHECK_SEC` (при свободной — реже,
  `NAV_RECHECK_FREE_SEC`), вплотную (`NAV_DIR_MIN` 2 м) — прямо; снимок
  `nav_active` раз в кадр, `nav_blocked` — кэш на физкадр.
  **Обход у цели-ЗДАНИЯ ведёт к сектору кольца или к точке перед стеной, а не
  к центру** (центр лежит в фундаменте).
- **ТОЧКА ПРИКАЗА ВНУТРИ ФУНДАМЕНТА ВЫНОСИТСЯ НАРУЖУ** (`nearest_passable` →
  `in_building`, как у воды). **Концы нити у стены прощаются**
  (`NavLineBlockedPad`: проба в пределах ячейки от конца, который сам вне
  кругов) — ворота лежат в ячейке постройки, иначе к ним «дороги нет».
  Сдача ресурсов — у ворот (`Building.deposit_point` / `at_deposit`), центр
  склада с тыла недостижим.
- **ТУПИК — НЕ В СТЕНУ** (`_nav_dead_end` ← `nav_last_ok`): инициатива бросает
  цель, приказ игрока стоит и переспрашивает; авто-агро за обрыв не идёт
  (`nav_unreachable`). Компоненты связности (`NavBuildComponents`) отсекают
  заведомо недостижимую цель до заливки A*.
- **`Packed*Array` ИЗ КЭША ИДЁТ ПО ССЫЛКЕ.** `append_array` на ответе
  `nav_route` правит САМ кэш: маршруты росли 3, 5 … 25 точек «угол-угол-угол»,
  боец доходил до цели и шёл назад по второму кругу («бегают по кругу»).
  Перед любой правкой ответа навигации — `duplicate()`.
- **ЦЕНА (зонд `qa_bigstand/NavProbe`):** переход GDScript → фасад → C# ~7 мкс,
  `nav_line_blocked` 10-13, попадание в кэш 15-25, полный A* через плато
  160 м — 1.47 мс. Часы A* идут всегда (`nav_worst_usec`, `nav_miss_key/reuse`,
  строка «НАВИГАЦИЯ» в отчёте стендов); кто заказал маршрут — `nav_who` /
  `nav_req_class`. Сетка строится ОДИН раз при генерации карты и лениво
  досинхронизируется по смене реестра построек.
- **ВОДА, ГРАНИЦЫ И МАРШРУТ — ПРАВИЛО ПАРТИИ:** стенд, снявший
  `world_bounds_enabled`, не видит ни реки, ни фундаментов, ни маршрутов.


## ТЗ 20.09.2026: горы против зданий, спавн, мёртвая остановка, стойки, UI

Стенд — **`qa_cliff_and_squad_behavior`** (блоки A-E, headless,
`-- only=abcde`). Ниже только то, что нельзя вывести из кода.

- **ОТСТУП У СКАЛ И У ДОМОВ РАЗНЫЙ, И ЭТО ДВА РАЗНЫХ ЧИСЛА** (п. 1). Заход
  19.09 свёл обход ЗДАНИЙ к тесным 0.2-0.6 м (`NavBldSidePad` /
  `NavBldCornerPush` / `NavBldHug`) — и тем же числом прижал войска к склонам:
  отряд шёл впритирку к обрыву и вытягивался в нитку. Теперь у скалы и воды к
  габариту агента прибавляется **`ArmyCore.NavCliffExtra` 1.0 м** — в штраф
  шага у стены, в зазор нити и в отжим угла. **В проверку «точка в стене» он
  НЕ входит**: узкий брод иначе стал бы непроходимым, а A* и так обязан его
  находить.
- **УГОЛ РЕЛЬЕФА ЗАМЕНЯЕТСЯ ДУГОЙ** (`ArmyCore.NavSmooth` / `NavArcCorner`).
  Ломаная A* давала одну точку поворота: колонна доходила до неё,
  разворачивалась на месте и растягивалась. Выступ аппроксимируется
  ОКРУЖНОСТЬЮ вокруг ближайшей непроходимой ячейки (радиус = расстояние до
  неё), угол заменяется дугой из 2-6 граней по ~20° (`NavArcStep`), по
  `NavArcShare` дуги с каждой стороны. **Сглаживание никогда не делает путь
  непроходимым:** каждая точка проверяется `NavBlockedAtC`, каждое звено —
  `NavLineBlockedPad`, не прошло — угол остаётся как был. Только СКАЛА
  (`_nav[i] == 1`): у дома угол подтянут вплотную намеренно.
- **ЛИШНИЕ УГЛЫ СНИМАЮТСЯ ПОСЛЕ ОТЖИМА** (`NavPrune`). Натягивание выбирает
  самую дальнюю видимую точку, но `NavCornerPush` и `BldHug` двигают её ПОСЛЕ
  выбора — и соседи угла нередко видят друг друга напрямую. Такой угол — крюк,
  а у ворот казармы он ложился ПОЗАДИ бойца.
- **ОБЕ ПРАВКИ РЕЛЬЕФА — ПОД ОДНОЙ РУЧКОЙ `perf_config.nav_cliff_arc`**
  (они про одно: обход горы). **A/B на `qa_cliff_and_squad_behavior`, блок A:**
  нить обхода выступа 3 угла с медианой поворота 29° и худшим 44° → **14 углов,
  медиана 18°, худший 33-41°**; малый габарит блока при огибании у лучников
  50 → 58-67 %, у копейщиков 48 → 51-53 %, время прохода то же (27-33 с).
  **МЕТРИКА ШУМНАЯ** (у конницы 69 % против 47-70 % между прогонами одного
  кода) — стенд судит по абсолютному потолку вытянутости (`ELONG_MAX` 6.0:
  двадцать человек в линию дают 15-20), а разницу A/B читать только парой
  прогонов в одном часу.
- **ВЫХОД ИЗ СОБСТВЕННЫХ ВОРОТ — НЕ ПОМЕХА** (п. 2, `NavBandBlockedBld`).
  «Призрачный угол» был полосой отряда: боец появляется на площадке в
  двух-шести метрах от стены, полоса ±halfW задевала ТО ЖЕ здание, из которого
  он вышел, и звала A* — тот честно огибал казарму. Теперь участок у конца,
  который сам стоит вплотную к дому (`BldDepth` у начала/конца), из проверки
  полосы выпадает: здание, у стены которого путь начинается или кончается,
  обойти нельзя и не нужно.
- **МЁРТВАЯ ОСТАНОВКА** (п. 3, `GameManager._formation_settle`,
  `REFORM_GIVEUP` 3). Откат ×2 (до 8 с) растягивал дёрганье, но не прекращал
  его НИКОГДА: у леса и построек ячейки разметки недостижимы в принципе, и
  отряд переступал до конца партии (скриншоты 13-15 — один отряд, три
  секунды). После трёх безуспешных смыканий подряд строй ПРИНИМАЕТСЯ КАК ЕСТЬ:
  разметка и посты переносятся на текущие точки, отряд помечается
  `reform_settled` и обходом больше не трогается. Снимают признак бой и любая
  новая разметка (`squad_set_formation`). Счётчик — `formation_settles`.
- **СТОЙКА НЕ ДВИГАЕТ НИКОГО И НИКОГДА** (п. 4.1). Прежде без перестроения
  обходился только СТОЯЩИЙ отряд (ТЗ 14.09 п. 8), а идущий получал
  `spear_wall_form` — три шеренги вокруг центра масс и `command_move` каждому.
  Теперь `on_squad_stance` всегда зовёт только `_formation_in_place`; три
  шеренги ложатся при следующем растяге ПКМ (`_block_formation_slots` читает
  `spear_wall_ready`).
- **«СТЕНА КОПИЙ» ДЕРЖИТ ПИКИ ВПЕРЁД ВСЕГДА** (`Spearman._spear_leveled`,
  ветка `wall`): перк снимает и требование «стойка ЗАЩИТА или бой», и
  гистерезис по ряду — щетина по всей глубине строя, в том числе на марше.
  На бегу фаланги по-прежнему нет: двойной ПКМ распускает строй, это правило
  старше. **Разницу режимов строя мерить только на роде войск БЕЗ стены** —
  с ней оба режима неразличимы.
- **«ЗАЩИТА» ЛУЧНИКУ ХОДЬБУ НЕ РЕЖЕТ** (п. 4.2, виртуал
  `Unit.stance_slows_move()`, у `Archer` false). `STANCES.defense.
  move_speed_mult` 0.65 писан под щит и опущенные копья; у стрелка ни того,
  ни другого нет. Там же снят лобовой сектор: стрелок в «Защите» бьёт всё, до
  чего дострелит (`_is_ranged()` в ветке `_stance_holds_ground`), — сектор
  писан под щетину пик, а не под «стой и веди огонь».
- **РАДИУС ИНИЦИАТИВЫ 10 → 5 м** (п. 4.3, `Unit.AGGRO_RADIUS`). Заказ: «агрятся
  только после остановки и ТОЛЬКО если враг в пяти метрах». СТРЕЛЬБЫ С МЕСТА
  это не касается: `watch` в `_check_auto_aggro` берётся как
  `max(aggro_radius, attack_range)`. У орды радиус по-прежнему её обзор
  (`aggro_radius()`), так что «истукан» к ней не возвращается.
- **ЗВУК** (п. 5): рык рыцарей вдвое реже (`KNIGHT_ANGRY_GAP` 2 → 4 с и окно
  категории 0.4 → 0.8) и тише на 35 % (−3.0 → −6.7 дБ); возгласы орды
  (`goblin_attack` / `_death` / `_laugh` / `_grunt`) тише на 30 % (−3.1 дБ).
- **UI** (п. 6): из ряда перков карточки убраны узлы кузницы
  (`HUD.BONUS_ROW_SHOWS_FORGE` false — они одинаковы у всех отрядов рода войск
  и о конкретном отряде не сообщают ничего; `qa_vet_ui2` D3/D4 переписаны на
  разворот). Плашка гарнизона больше не висит НАД панелью отдельным узлом
  (она пересобиралась своим тактом, а панель — своим: отсюда «дёргается») —
  теперь это КОЛОНКА В САМОЙ ПАНЕЛИ: подпись «Гарнизон» и
  `GARRISON_MINI_SLOTS` 7 мелких слотов сеткой 4 + 3, пустые места видны
  тускло, лишние сверх семи — «+N». Вправо её переставляет
  `_sync_panel_grid_widths` (ветки панели зовут `_show_garrison` до кнопок
  найма, и порядок иначе пришлось бы чинить в трёх местах). Прежняя полоса
  оставлена как `_show_garrison_strip_legacy`.
- **`qa_sprint18b` B10/B11 КРАСНЫ ОТ ЧИСЕЛ КОНФИГА, А НЕ ОТ КОДА, И ЭТО
  АРИФМЕТИКА.** Владелец поставил троллю `charge_range` 16 -> 10 при
  `charge_min_runup` 6.0 и дубине `attack_range` 4.5: разгон взводится за 10 м
  до цели, а удар ловится на 4.5 м, то есть фактический пробег 5.5 м НИКОГДА
  не дотягивает до шести, `_charge_impact` выходит на первой же проверке, и до
  стены копий дело не доходит вовсе («ударов стены 0, замедлен=false»). B11
  падает следом: неостановленный тролль топчет тот же строй. Проверено A/B —
  красное и при `knob=nav_cliff_arc:0`, и при `AGGRO_RADIUS` 10; на HEAD (то
  есть со СТАРЫМ конфигом владельца в stash) B10 зелёный. **Лечится одним
  числом владельца** (`charge_range` >= `charge_min_runup` + `attack_range`,
  то есть 10.5+ при нынешних), код трогать нечего.

## Отрисовка армии: LOD, анимация и метки под ногами

- **LOD-радиус СЛЕДУЕТ ЗА ЗУМОМ, а не стоит на 90 м.** `_view_r2` —
  максимум из `perf_config.lod_radius` и видимой на земле полосы, которую
  сообщает камера (`RTSCamera._lod_ground_radius`). Пока радиус был
  постоянным, отдалённая ортокамера показывала 250+ м, и всё дальше 90 м от
  ЦЕНТРА ЭКРАНА считалось «невидимым», оставаясь на экране.
- **Признак `seen` означает «стоит ли считать позу», а НЕ «рисуется ли».**
  Общий `MultiMesh` рисует всех зарегистрированных (`extra_cull_margin`
  16384 м), поэтому **перенос слота пишется ВСЕГДА**, без оглядки на LOD
  (`Unit._sync_far_render`, ветка `move_to`). Эти две строки и давали разом
  три жалобы: пехота ехала с замершими ногами, рабочие рубили без анимации, а
  жёлтые кольца отставали от бойцов — кольцо берёт нарисованную точку, которую
  сглаживание двигает каждый кадр, а сам спрайт стоял до следующего полного
  пересчёта (раз в `vis_shards × anim_every` = до двадцати кадров отрисовки).
- **Кадр ленты уезжает в буфер СРАЗУ** (`FarUnitRenderer.Slot.set_frame`, одна
  запись во float). `_advance_look_frame` листает походку на своих 6-10 к/с, а
  полный разбор спрайта идёт раз в `anim_every` — без отдельной записи кадра
  анимация шла со скоростью полного разбора, то есть 2-3 раза в секунду.
- **Выделение хранит и МАССИВ, и СЛОВАРЬ членства** (`SelectionManager._sel_set`).
  Проверка `node in selected_units` линейна, а выделение разворачивается по
  отрядам — стоимость выходила квадратичной: замер `qa_fps` на 2000 бойцах
  давал **1271 мс** на один «выделить всё», то есть полторы секунды замершей
  игры. Со словарём — 71 мс.
- Что сколько стоит на 4000 бойцов в окне 1920x1080 (замер `qa_fps`, база
  50 FPS = 20 мс): физический тик армии **3.3 мс**, туман 1.6, спрайты армии
  0.8, деревня гоблинов 0.65, HUD 0.66, мышление ИИ 0.2, растительность ~0.
  Вызовов отрисовки 496 — **узкое место не в них**. Верхние ветки тика:
  `process_move`, `rank_recompute`, `grid_update` (по ~14% каждая).
  **ДОЛИ ВЕТОК ЧИТАТЬ ТОЛЬКО КАК РАНЖИРОВАНИЕ.** Честный A/B (авг. 2026) дал у
  `grid_update` 0.40 мс на тик вместо профильных 1.18, у `check_auto_aggro` —
  0.31 вместо 0.65. Разбор — `docs/PERF_6000.md`, раздел 6.

## Порядок перекрытия спрайтов: глубина по точке на земле («Y-sort»)

- **ЖАЛОБА:** «пехотинцы проваливаются под спрайты камней — камень перекрывает
  ноги солдата, стоящего ПЕРЕД ним». Причина ГЕОМЕТРИЧЕСКАЯ, а не в слоях:
  квад стоит вертикально, камера смотрит под 45°, и точка выше по Y ближе к
  камере на sin45 ≈ 0.71 м на каждый метр высоты. Верхушка камня честно
  оказывается ближе собственного основания и перекрывает всё, что стоит перед
  ней ниже. Буфер глубины при этом работает правильно — неверна постановка
  задачи: у плоской декорации не должно быть «глубины по пикселям».
- **ЛЕЧЕНИЕ: глубина у всего спрайта ОДНА и берётся у его ТОЧКИ НА ЗЕМЛЕ**
  (`DEPTH` в `cyl_billboard` и `mm_unit_sprite`, ручка `ground_depth`). Тогда
  порядок решает то же, что решает игрок глазом, — кто ближе стоит; сортировка
  остаётся аппаратной, ни второго прохода, ни ручной сортировки не нужно.
  Проекция считается во ФРАГМЕНТНОМ шейдере: в NDC глубина нелинейна, и
  интерполяция между вершинами дала бы не плоскость, а поверхность второго
  порядка.
- **ТОЧКУ НА ЗЕМЛЕ НЕЛЬЗЯ ВЫВОДИТЬ ИЗ ГЕОМЕТРИИ КВАДА, И ЭТО СТОИЛО ВСЕЙ АРМИИ,
  УТОНУВШЕЙ В ГРУНТ.** У декораций (`cyl_billboard`) «низ квада лежит на
  грунте» верно ПО ПОСТРОЕНИЮ: узел поднят ровно на половину высоты
  (`position.y = size.y * 0.5`). У бойца НЕ ТАК И НЕ БЫЛО НИКОГДА — его квад
  привязан к земле ЦЕНТРОМ КАРТИНКИ (`Unit.SPRITE_BASE_Y` = 0.42 м), а между
  низом квада и ступнями лежит пустое поле кадра: 56 px из 192 у рабочего и
  122 px из 320 у копейщика. Правило было перенесено из одного шейдера в другой
  ДОСЛОВНО и записано в комментарий как утверждение факта — то есть получило
  вид проверенного. Ошибка глубины равна этому полю в метрах: **0.87 м у
  рабочего, 1.85 м у копейщика**, и грунт честно перекрывал всё, что ниже, —
  у рабочего оставалась макушка, у копейщика одно остриё пики. **Чем выше
  лист, тем глубже тонет.**
- **ЧИСЛО ПРИХОДИТ ОТ ТОГО, КТО ЕГО ЗНАЕТ** (`mm_unit_sprite`, uniform
  `foot_drop` ← `FarUnitRenderer.Bucket.set_foot`). Это РОВНО `base_y` слота,
  уже растянутая: шейдер отсчитывает вниз ОТ НАЧАЛА КООРДИНАТ СЛОТА, а оно и
  стоит на `грунт + base_y`. Первая попытка подала сюда «расстояние от низа
  квада до ступней» (`half_h - base_y`) — число осмысленное и как раз равное
  прежней ошибке, но вычитаемое не оттуда: остаётся `half_h - 2*base_y`, то
  есть у копейщика 1.25 м из прежних 1.85, и армия тонет по-прежнему, только
  мельче. Поймал `qa_visual_smoke`.
- **ПОЧЕМУ UNIFORM, А НЕ ДАННЫЕ ЭКЗЕМПЛЯРА.** Все четыре канала instance-цвета
  заняты (кадр, зеркало, вспышка, доля жизни), а `custom_data` в GL
  Compatibility держит открытый баг движка (см. шапку `FarUnitRenderer`).
  Ручка при этом ОТРЯДНОЙ величиной и не является: бакет заведён ПО ЛЕНТЕ,
  а привязка ног — свойство ленты. Ставится она при регистрации первого бойца
  и при обновлении внешности, но **записью только на изменение**: установка
  uniform'а — обращение в сервер отрисовки, а число постоянно, пока бакет жив.
- **ПОДАВАТЬ НАДО В ОБОИХ МЕСТАХ.** У копейщика `sheet_frame()` отдаёт общий
  `SPRITE_BASE_Y`, а настоящая привязка ленты (`_sprite_base_y`, компенсация
  разных направленных листов) доезжает только до пути обновления внешности.
  Ставить ручку лишь при регистрации значит промахнуться ровно на неё.

## Визуальный дым: объект на экране виден целиком

**`qa_visual_smoke/Shot` (ОКОННЫЙ, 19 проверок).** Заведён потому, что правка
глубины утопила всю армию в грунт, а **шлюз из тридцати пяти стендов остался
зелёным**: он весь headless, а там буфер глубины не заполняется вовсе и не
компилируется ни один шейдер. У утонувшего бойца при этом не меняется НИ ОДНО
число, которое стенды проверяют, — та же позиция, та же строка в ядре, тот же
запас жизни. **Ловить это можно только по картинке.**

- **ДВА СНИМКА ОДНОГО КАДРА:** без объектов (фон) и с объектами. Видимая часть —
  пиксели, ИЗМЕНИВШИЕСЯ между снимками внутри экранного прямоугольника квада.
  Метрика не зависит ни от цвета травы, ни от того, что ещё стоит на карте.
- **ВЕРДИКТА НА ОБЪЕКТ ДВА, И ЛОВЯТ ОНИ РАЗНОЕ.** «Силуэт цел» — доля видимых
  пикселей от ожидаемых (порог 80 %). «Низ рисунка на месте» — где лежит нижняя
  видимая кромка относительно нижней НАРИСОВАННОЙ, **в метрах**: это СВОЙСТВО,
  эталона не требует вовсе и называет поломку числом (1.85 м у копейщика).
- **ЭКРАННАЯ ПЛОЩАДЬ КВАДА — ПРОЕКЦИЯ ЕГО УГЛОВ, А НЕ АРИФМЕТИКА С КОСИНУСАМИ.**
  У бойца компенсация наклона вшита в размер меша, у дерева её делает шейдер;
  одной формулы на оба случая нет, а камера знает правду про оба.
- **ОЖИДАНИЕ СЧИТАЕТСЯ ПО МОДЕЛИ ОТРИСОВКИ.** У бойца срез 0.15, и впечатанная
  в арт тень идёт ДИЗЕРОМ (рисуется с вероятностью, равной альфе) — покрытие
  есть СУММА альфы; у декорации срез 0.5 без дизера — их СЧЁТ. **Нижнюю кромку
  при этом судят по порогу 0.5, а не 0.15:** дизер тени разность снимков ловит
  неохотно, и у целого бойца выходило «кромка поднята на +0.30 м» на ровном
  месте.
- **ЛОВУШКИ САМОГО СТЕНДА, ВСЕ ТРИ ДАВАЛИ ЛОЖНЫЙ ВЕРДИКТ.** Шейдер опознавать
  по `resource_path`, а НЕ подстрокой в `shader.code` (имя файла внутри файла
  упоминаться не обязано — не нашлось ни одного слоя армии, прятать было
  нечего, и разность двух ОДИНАКОВЫХ снимков дала «видно 0 px» у всех сразу).
  Высота кадра обязана вмещать и ширину ряда, и САМЫЙ ВЫСОКИЙ квад (срезанный
  верх замка читался как потеря силуэта). **У дерева нет узла вовсе** — лес
  рисуется общим `VegetationRenderer`, квад бакета ЕДИНИЧНОЙ высоты, настоящий
  размер приходит масштабом слота: брать его надо у `ResourceNode._veg_slot`, а
  не искать «ближайший экземпляр» в буфере (освобождённые слоты остаются там со
  своими старыми координатами).
- **ЗАМОК И ДЕРЕВО В СТЕНДЕ — ЭТО КАЛИБРОВКА, А НЕ ПОЛНОТА.** Они правкой не
  затронуты, и их зелёные 99 % доказывают, что метрика меряет поломку, а не
  «всё подряд». Стенд без такой пары не годится вовсе.

## Разлёт от тарана виден глазом

- **Виноват был НЕ удар, а порог привязки картинки.** Спрайт и так догоняет
  тело плавно (`Unit._smoothed`), но прыжок дальше `VIS_SNAP_SQ` (2 м) считается
  телепортом и рисуется мгновенно — иначе спрайт полз бы через полкарты после
  гарнизона или постановки стендом. Отбрасывание при таране равно
  `charge_knockback` = 2.2 м, то есть ровно ЗА этим порогом: удар честно двигал
  бойца, а картинка честно телепортировалась следом.
- **Лечение — метка `_fling_left`**, а не поле скорости. Пока метка жива,
  предохранитель не срабатывает, а доля догона берётся своя (`FLING_LERP`,
  помедленнее обычной). Импульс потребовал бы своего поля и своей ветки в тике
  ради эффекта длиной в полсекунды, а физтел в игре нет вовсе.
- **Летящий не засыпает по картинке** — по той же причине, что и боец с
  непогасшей вспышкой: уснувший перестаёт тикать, и спрайт остался бы лежать
  там, откуда бойца сбило. Такт метки идёт там же, где такт вспышки.
- Стенд — `qa_cavalry`, F5в/F5г.

## Конец партии

- **Победа объявляется по СНЕСЁННОЙ КРЕПОСТИ и отсутствию живых**, а не по
  полностью пустой карте (`Main._faction_beaten`). Прежнее условие требовало
  нуля зданий и висело, пока игрок не обойдёт карту за последним домиком.
- **ИИ не закладывает крепость, когда строить её некому** (`_has_living_units`).
  Закладка проверяла только деньги: фундамент вставал, попадал в группу зданий
  фракции, достроить его без единого рабочего было нельзя — и условие победы не
  выполнялось НИКОГДА. Это и есть «крепость уходит в режим повторной постройки».
- **Живые считаются проверкой `is_dead()`, а не размером группы:** павший уходит
  из неё лишь в конце кадра (`queue_free` отложен).
- **ПОРАЖЕНИЕ ИГРОКА — ДВА ПРАВИЛА (10.09.2026, `Main._player_defeated`):**
  крепость снесена И отстроить нельзя (цена замка не по карману ИЛИ нет живого
  рабочего и нет заложенной крепости); либо ни построек, ни бойцов не
  осталось — сразу. Пока крепость стоит или её можно отстроить — играем.
- Стенд — `qa_bugpass`, блок I.

## Урон видно на бойце: вспышка и окровавленность

- **Оба состояния едут в СВОБОДНЫХ каналах instance-цвета** (`COLOR.b` — сила
  вспышки, `COLOR.a` — доля жизни), где их читает `mm_unit_sprite.gdshader`.
  Ноль новых узлов, ноль новых листов спрайтов и **ноль дополнительных вызовов
  отрисовки** — замер `qa_wound/Shot`: 52 вызова и у целого строя, и у
  окровавленного, и в момент вспышки. Раненого арта не существует и не будет,
  поэтому «подбитость» делается подкраской той же текстуры.
- **Отклик на удар пишется В ТОМ ЖЕ ФИЗИЧЕСКОМ КАДРЕ** (`take_damage` →
  `_push_damage_shade`), а не ближайшим визуальным тиком. Тик шардирован: на
  большой армии боец получает его раз в три-четыре кадра, и вспышка длиной
  `HIT_FLASH_SEC` (0.07 с) успевала бы догореть до первой отрисовки. Наружу при
  этом ничего не уходит — запись идёт в теневой буфер бакета, а он подаётся
  одним `flush()` в конце кадра, как и всегда.
- **`Bucket.write()` обязан переписывать и эти два числа.** Полная запись идёт
  при СМЕНЕ ЛЕНТЫ, то есть при каждом переезде в другой бакет, а в свалке они
  постоянны: с прежними константами «вспышки нет, боец цел» раненый мигал бы
  чистым спрайтом всю драку. Стенд `qa_wound`, проверка D2.
- **С непогасшей вспышкой боец НЕ ЗАСЫПАЕТ.** Уснувший перестаёт тикать, а
  значит и гасить вспышку: в буфере навсегда осталось бы подмешанное белое, и
  обстрелянный неподвижный строй стоял бы засвеченным.
- **Лечение ловится сравнением, а не вызовом.** Лечат бойца из четырёх разных
  мест; вместо оповещения из каждого визуальный тик сверяет `current_health` с
  тем, что уже отдано в отрисовку. Цена на целом неподвижном строю — два
  сравнения float на бойца и НИ ОДНОЙ записи (стенд `qa_wound`, F1/F2).
- **Одним множителем крови не добиться.** У копейщика синий плащ, красного
  канала в нём почти нет, и любой множитель делает его только ТЕМНЕЕ — первый
  снимок дал не «окровавлен», а «посерел». Поэтому поверх множителя идёт
  крохотная ДОБАВКА бурого (`wound_blood`), и в тех же тёмных участках. Больше
  — и силуэт светлеет, теряя рисунок, то есть возвращается та самая «грязь»,
  ради ухода от которой в `mm_corpse` выбран множитель.
- Ручки: пороги, цвета и пульсация — uniform'ы шейдера (`wound_hi` 0.85,
  `wound_lo` 0.40 и ниже), длительность и пик вспышки — `Unit.HIT_FLASH_SEC` /
  `HIT_FLASH_PEAK`. Пик НЕ единица намеренно: полностью белый силуэт стирает
  рисунок и в свалке читается как дыра в строю.
- Стенды: `qa_wound/Test` (16 проверок, headless — доезжают ли числа до буфера
  и когда) и `qa_wound/Shot` (оконный — лесенка 100/90/70/50/30/10 % запаса,
  вспышка, раненые в строю; пороги там не проверяются, их судят глазом).

## Разбор кадра и цена тика

- **Мониторы `Performance.TIME_PHYSICS_PROCESS`/`TIME_PROCESS` для разбора
  кадра НЕ ГОДЯТСЯ** — при снятом ограничении кадров они усредняются по кадрам
  отрисовки и на одной и той же сцене дают то 10, то 20 мс. Честные числа даёт
  `perf_config.tick_meter` / `vis_meter`: одна пара `get_ticks_usec` вокруг
  ВСЕГО `_physics_process` и ВСЕГО `_process`.
- **Физика идёт фиксированные 60 Гц.** При 50 к/с на кадр приходится 1.2
  физического тика, и цена тика НА КАДР выше самого тика. Без этой поправки
  разбор кадра не сходится — именно из-за неё A/B-замер «выключить тик армии»
  показывал 3.3 мс там, где счётчик даёт 8.
- Разбор на 4000 юнитов, 1920x1080 (`qa_fps`, авг. 2026): кадр ~19.5 мс =
  физтик ~9.5 + кадр логики ~4.5 + движок и рендер ~5.5, **50-52 FPS**.
  **Отрисовка не при чём**: ~250 вызовов, скрытие всех спрайтов армии даёт
  меньше миллисекунды.
- **«ОТРЯД В БОЮ» СПРАШИВАЛИ ИЗ ПОКАДРОВОГО ПУТИ, А СЧИТАЛИ ОБХОДОМ СОСТАВА —
  ЭТО БЫЛА КВАДРАТИЧНОСТЬ ПО РАЗМЕРУ ОТРЯДА.** `GameManager.squad_in_combat`
  зовут `Unit._phalanx_dir` (то есть каждый копейщик обороны на каждом ходу
  строя), `_check_auto_aggro` и `_update_live_rank`, а ответ строился через
  `squad_members()` — тот на КАЖДЫЙ вызов собирает новый массив живых и
  обходит состав. Отряд из шестидесяти, спрашивающий сам себя шестьюдесятью
  бойцами шестьдесят раз в секунду, — это 3600 посещений и 60 массивов в
  секунду НА ОДИН ОТРЯД. Дёшево это выглядело только из-за раннего выхода по
  окну «нас недавно задели» (3 с), которое закрывает разгар свалки, — а на
  СБЛИЖЕНИИ и в стоянии под обстрелом окно молчит, и платился полный обход.
  Лечение — ответ на ОДИН ФИЗИЧЕСКИЙ КАДР (`perf_config.squad_combat_cache`,
  пункт 1б) плюс чтение состава НАПРЯМУЮ, минуя `squad_members()`: тот не
  читатель, он распускает опустевший отряд прямо в геттере.
  **Замер `qa_ab` (1440 копейщиков, 24 отряда, чередование, 6 раундов):**
  стояние в стойке ЗАЩИТА в виду друг друга — физтик **14.89 → 6.17 мс**
  (−8.7 мс, 58 %); та же сцена в стойке АТАКА — 2.76 → 2.67 мс, то есть
  в пределах разброса. Разница между сценариями и есть доказательство, что
  меряется именно эта ветка: в атаке фаланговый путь не работает вовсе.
- **A/B ЧЕРЕДОВАНИЕМ ТЕПЕРЬ ЕСТЬ ОТДЕЛЬНЫМ СТЕНДОМ — `qa_ab` (headless).**
  Он поднимает живую сцену, снимает базу и правку ПОДРЯД по `tick_meter` и
  повторяет это раундами, поэтому дрейф машины попадает в обе выборки
  одинаково. Ручку правки берёт из `perf_config`; чтобы померить следующую
  оптимизацию, ей нужен такой же выключатель.
- **ВИЗУАЛЬНЫЙ ПРОХОД ИДЁТ НА ПЯТИ ШАРДАХ** (`perf_config.vis_shards_extra`
  = 2 при армии от `vis_extra_from`). Это единственная правка того захода,
  которая выдержала честный замер: кадр логики −0.7…−0.9 мс, FPS +1…+5, физтик
  НЕ меняется — шарды движения так и остались тремя. Цена — примерно +0.1 м к
  отставанию нарисованной точки от логической; побочная выгода — кольцо
  выделения и спрайт обновляются одним шардом и больше не расходятся вовсе.
- **Разброс между прогонами ±3-5 к/с на НЕИЗМЕННОМ коде.** Правку меньше чем на
  миллисекунду этим замером подтвердить нельзя — но и «по веткам профиля» её
  подтверждать НЕЛЬЗЯ ТОЖЕ (см. `docs/PERF_6000.md`, раздел 6): у ветки,
  которая идёт тысячу раз в кадр, измеритель стоит соизмеримо с ней самой.
  Способ один — A/B по `tick_meter` ЧЕРЕДОВАНИЕМ, база и правка подряд.
- **Ряды фаланги считаются ПАЧКОЙ НА ОТРЯД** (`ArmyCore.SquadRanks` ←
  `GameManager._push_squad_ranks`, зовётся из `_recalc_corridor`: там уже
  собраны состав и строки). Персональный `allies_ahead` стоил 8.3 мкс на
  бойца, из которых сам скан 2.5 — остальное путь наружу. Направление берётся
  ОДНО на отряд, двумя обращениями к словарю; спрашивать `_phalanx_dir()` у
  каждого бойца НЕЛЬЗЯ — первая версия так и делала, и ветка `squad_corridor`
  выросла с 0.78 до 2.19 мс. Отряд без курса считает ряды по-старому.
- **Тик юниту переключают только `Unit.set_tick()` / `set_draw()`.** Они ведут
  поля-зеркала `tick_on`/`draw_on`, по которым идёт обход армии: прежние
  `is_processing()` / `is_physics_processing()` — обращения в движок, и их было
  под две с половиной тысячи в кадре. Переопределить сами движковые методы
  нельзя, GDScript считает это ошибкой, поэтому имена свои.

## Цель приказа: чужой боец и чужое ЗДАНИЕ равноправны

- **Здание было законной целью и раньше**: маска клика включает
  `LAYER_BUILDINGS`, ПКМ доходит до `command_attack`, лучник по стене стреляет.
  Не было ОБРАТНОЙ СВЯЗИ — весь путь подсветки был написан под
  `pick["target"] as Unit` и на постройке молча отдавал null. С точки зрения
  игрока это и есть «здание нельзя выбрать целью»: единственный способ узнать,
  сработало ли, — кликнуть и смотреть, полетят ли стрелы. Лечится
  `SelectionManager.enemy_building_under_cursor` и приёмом `Building` в оба
  слоя колец (`set_hover_units` / `set_order_targets`).
- **Кольцо под зданием растягивается по его основанию**
  (`SelectionDecalRenderer.target_ring_scale`), и масштабируются ТОЛЬКО оси X и
  Z: тор лежит горизонтально, и общий масштаб поднял бы его над землёй колесом.
- **Дистанция до здания меряется до СТЕНЫ, а не до центра коробки**
  (`Unit._target_pad` / `reach()`). `global_position` постройки лежит в центре,
  а коробка бывает восемь метров: пока считали до центра, «дойти на дистанцию
  удара» означало для копейщика ВОЙТИ ВНУТРЬ здания (масок столкновений нет, он
  туда честно заходил), а для лучника — встать вплотную к стене вместо
  двадцати метров. Поправка держится ПОЛЕМ и считается один раз при смене цели:
  дистанцию читают четыре горячие ветки в каждом кадре, и проверять там тип
  цели значило бы платить приведением типа за бойца, у которого поправка ноль.
- Стенд — `qa_bldattack` (12 проверок).

## Расформирование отряда (клавиша Delete)

- `SelectionManager.disband_selected()`: боец получает СМЕРТЬ, а не
  `queue_free`. Смерть — единственный путь, на котором он снимает с себя строку
  в ядре армии, место в сетке, счётчик атакующих у цели и место в отряде, а на
  поле оставляет ТЕЛО. Прямое удаление узла обошло бы всё это.
- Только СВОИ и только бойцы: здания попадают в `selected_units` (панель замка
  работает через то же выделение), и снос собственного замка по случайному
  нажатию — не та цена, которую игрок готов платить за удобство.
- Список копируется ДО первой смерти: гибель дёргает `remove_from_squad`,
  `_disband_squad` и `on_selection_changed`, то есть правит тот самый массив.
- Стенд — `qa_corpse`, блок N.

## Поле боя и строй в атаке — коротко

Полный разбор (тела, стрелы, знамёна, четыре попытки переноса стены на атаку)
— в `docs/ARCHIVE_SPRINTS.md`. Здесь только то, что нужно знать всегда:

- **ТРУП — ДЕКОРАЦИЯ, А НЕ ЮНИТ** (`CorpseRenderer`, слот в общем MultiMesh,
  узла на павшего нет). Ставится ПОСЛЕ того, как боец снял строку ядра, сетку
  и тик. Лежит плашмя своим шейдером `mm_corpse` (без билборда), угол
  ограничен `COFFIN_SPREAD` вокруг «поперёк экрана», поза — ПОКОЙ, а не та, в
  которой застала смерть. Потолок `MAX_CORPSES` 5000, срок 15 минут,
  разложение 30 с; **срок проверяется только у ГОЛОВЫ очереди** (она
  упорядочена по времени укладки), **часы у слоя свои** (на паузе тела лежат).
- **ПОРОГ СРЕЗА АЛЬФЫ У ТРУПА И У ЖИВОГО РАЗНЫЙ НАМЕРЕННО** (0.5 против 0.15):
  впечатанная в арт тень у живого возвращена ДИЗЕРОМ, у лежащего срезана
  целиком — иначе чёрное пятно сбоку от головы. **Порог среза не снимать
  НИКОГДА:** материал со срезом идёт непрозрачным проходом, записанную ALPHA
  там не читают, и обнулённый порог выпускает на экран весь квад чёрным
  (так делалось растворение стрелы). Растворение — дизером.
- **ПОЛЁТ СНАРЯДА ВЕДЁТ ЯДРО** (`ProjectileFire` / `BatchArrows` /
  `StuckTick`): на время полёта снаряд — ЗАПИСЬ, узла нет; попадание ищет
  `EnemyAt` по XZ-радиусу, события приходят пачкой раз в кадр, урон
  агрегируется по жертве (один `take_damage` за кадр), звук — один на группу в
  клетке 32 м. Потолок торчащих `MAX_STUCK_ARROWS` 160 на поле, в теле —
  РОВНО ОДНА (убит стрелой — та самая; убит в рукопашной — ни одной). Против
  потолков считаются ТОЛЬКО негаснущие — иначе лавина вытеснения.
- **СЛОТ ЗНАЕТ ТОЛЬКО ТО, ЧТО В НЕГО ЗАПИСАЛИ.** «Стрелы зависают пачками» —
  это перенос узла в точку втыкания без записи в буфер: у слота своя точка,
  и её надо толкать явно.
- **ПЕРЕНОС СТЕНЫ СТРОЯ НА ПРИКАЗ АТАКИ ПРОБОВАЛИ ЧЕТЫРЕЖДЫ И СНЯЛИ.** Весь
  выигрыш формации давала ОСТАНОВКА БОЯ, а не геометрия: после починки
  сломанных замка цели и длительности схватки выигрыш исчез целиком (1.08
  против базовых 1.04-1.09). **На подходе строй и так сохраняется** (сдвиг
  формы 0.70 м с переносом и без) — разваливается он В КОНТАКТЕ, и пятую
  попытку по этому пути начинать не стоит.
- **КОПЬЁ ОПУСКАЕТ ТОТ, КТО ДЕРЁТСЯ, А НЕ ТОЛЬКО ТОТ, КОМУ ПРИКАЗАЛИ**
  (`Spearman._charging`), а ряд шеренги считается ВСЕМУ отряду, не только
  обороне (`_push_squad_ranks` без фильтра по стойке). Порядок работ здесь
  решал всё: сперва ранг, потом правило — иначе гистерезис пропускает
  поголовно.

## Пивоты построек и деревьев: клик, кольцо, основание рисунка

- **ЗДАНИЕ КЛИКАЕТСЯ ПО КАРТИНКЕ, А НЕ ПО КОРОБКЕ КОНФИГА**
  (`Building._fit_pick_to_sprite`). Постройка — ВЕРТИКАЛЬНЫЙ билборд, и рисуется
  она выше своей коробки в разы: у хижины гоблинов коробка 4×3.5×4, а нарисована
  она на 5.2 м вверх, да ещё домноженных на `V_STRETCH` ≈ 1.41 — верхушка стоит
  на 7.4 м, вдвое выше коробки. Луч мыши идёт под 45°: клик в ВЕРХНЮЮ половину
  картинки пролетал НАД коробкой и втыкался в землю перед домом, приказ читался
  как «идти в точку», отряд шёл в одну общую точку и там топтался. Это и есть
  «мечники не доходят до дистанции удара, сбиваются в кучу и дёргаются».
- **ФОРМА ПОПАДАНИЯ — ПЛАСТИНА В ПЛОСКОСТИ СПРАЙТА, А НЕ ВЫСОКАЯ КОРОБКА.**
  Коробку пришлось бы тянуть и вверх, и в глубину, и тогда луч, нацеленный в
  ЗЕМЛЮ ПЕРЕД зданием, проходил бы сквозь её нижнюю переднюю часть — клик по
  пустой траве читался бы как клик по дому. Высота берётся УЖЕ РАСТЯНУТОЙ:
  компенсацию наклона делает шейдер в МИРОВЫХ координатах, от нижней кромки
  квада вверх.
- **ФОРМУ ЗАВОДИТ ОДНО МЕСТО** (`Building._add_pick_shape`). `_build_visual`
  переопределяют Замок, Хижина и Стройплощадка, и каждый заводил свой
  `CollisionShape3D`. Пока это была просто коробка из конфига, сходило с рук; с
  подгонкой под рисунок забытая ссылка означает молча оставленное старое
  поведение — ровно это и вышло с хижиной с первой попытки.
- **КОЛЛИЗИЙ У ЗДАНИЯ НЕТ НИКАКИХ, КРОМЕ ЛУЧА МЫШИ.** У юнитов
  `collision_mask` = 0, размещение считается по `build_size` напрямую. Форма
  обслуживает ровно `_pick_at`.
- **КОЛЬЦО НА ЗЕМЛЕ ОПИСЫВАЕТ РИСУНОК, А НЕ КОРОБКУ** (`Building.ring_radius` /
  `ring_center`). У хижины коробка 4 м при нарисованных 3.28 — кольцо выходило в
  полтора раза шире дома и наполовину торчало перед ним. И поднимается оно на
  ОСНОВАНИЕ рисунка: под стенами у спрайта прозрачное поле, и кольцо, честно
  положенное на грунт, оказывалось под домом на траве.
- **ПОЛЕ ПОД РИСУНКОМ МЕРЯЕТСЯ В КАДРЕ, А РИСУЕТСЯ РАСТЯНУТЫМ.** Та же ошибка
  была у кольца под деревом (`ResourceNode.hover_ring_lift`): поле считалось в
  долях кадра и умножалось на высоту квада, но шейдер растительности тянет квад
  от нижней кромки вверх в `V_STRETCH` раз. Кольцо поднималось на 71% нужного —
  то есть всё равно оставалось «на траве под деревом», ради чего поле и начали
  мерить. Стенд `qa_pivot`, блок C.

## Приказ игрока выводит бойца из свалки телом, а не только вниманием

- **ЖАЛОБА:** «отряд в плотной рубке не идёт с первого правого клика, нужен
  второй». Замок `FORCED_MOVE_SEC` уже глушил перехват марша и ответный удар —
  то есть ВНИМАНИЕ бойца. Но шаг всё равно упирался в чужой строй
  (`_move_blocked` → `enemy_block`): в свалке враги стоят СО ВСЕХ СТОРОН, лобовая
  составляющая шага съедается целиком, боковая упирается на втором проходе, и
  функция выходит, НЕ СДВИНУВ БОЙЦА НИ НА САНТИМЕТР. Приказ принят честно —
  состояние MOVING, цель записана, — просто исполнить его нечем.
- **Лечение — `Unit._forced_move_pass()`:** пока тикает замок приказа игрока,
  чужие тела бойцу не преграда, ровно как отходящему в замок (`retreating`).
  Окно КОРОЧЕ самого замка (`FORCED_MOVE_PASS_SEC` 1.5 с против 3 с): полутора
  секунд хватает выйти из тел (около трёх метров, две шеренги), а все три
  позволили бы приказом «идти туда» проходить сквозь оборону насквозь —
  сквозной проход в этой игре есть ровно у одного режима, отхода в замок.
- **Своей ПЕРЕМЕННОЙ не заводим:** `_move_lock` уже означает «это приказ игрока
  и он непрерываем», вторая про то же самое разошлась бы с ней. А вот БИТ в
  колонке ядра завести пришлось (`ArmySoA.F_ORDER_PASS` = `ArmyCore.FOrderPass`
  = 1 << 17): признак читает солвер.
- **ПРАВКА ТОЛЬКО В `_move_blocked` — ЭТО ПРАВКА В ЗАПАСНОЙ ДОРОГЕ, И ОНА НЕ
  РАБОТАЕТ ВОВСЕ.** Первая версия послабления стояла именно там и на живой
  армии не дала НИЧЕГО: горячий путь шага пакетный (`_commit_step` →
  `GameManager.queue_step` → `ArmyCore.BatchStep`), а `_move_blocked` — запасная
  дорога для бойца без строки в ядре. Замер стендом `qa_disengage` D2:
  окружённый боец проходил **0.00 м** за секунду после приказа; с битом в
  колонке — 1.97 м. Любое правило движения, добавленное в GDScript, обязано
  ехать и в `StepFlagMask`, иначе его просто нет.

## Туман войны: кто себя прячет

- **ИСТОЧНИКИ ПО БОЙЦАМ СОБИРАЕТ ЯДРО** (`ArmyCore.FogRefreshRows`,
  09.09.2026). Маска и так считалась в C# (этап D3), но СПИСОК источников
  строил GDScript обходом всех `_live_units`: `global_position` (свойство,
  правило 2), `is_instance_valid`, `is_dead`, словарь — на каждого из 4000
  ради 74 ячеек-источников. Замер зондом `qa_fogperf`: **4.59 мс на
  пересчёт, из них 3.38 — этот обход**; пересчёт идёт раз в 0.15 с и ложится
  в ОДИН кадр — на кадре в 14 мс это и есть «микролаг» раз в девять кадров.
  Теперь ядро идёт по плотному списку живых строк своей фракции
  (`FPosValid`, не мёртв), радиус обзора берёт из колонки `_atkRange` по той
  же формуле `vision_radius`, сливает по ячейке `SRC_CELL` с наибольшим
  радиусом — те же 74 источника, **1.56 мс**. GDScript собирает только
  постройки и постоянные засветы. Ручка прежняя — `perf_config.fog_core`;
  без неё работает старый путь целиком.
- **ГАРНИЗОН — НЕ ИСТОЧНИК ОБЗОРА И НЕ ЦЕЛЬ:** у бойца вне карты снят
  `FPosValid` (`Unit.set_off_map` ← `Castle.absorb_unit`), а ядро отбирает
  источники по этому же признаку. Разбор — в разделе «Постройки: башня».
- **Боец прячется от чужих глаз САМ**, веткой внутри `tick_visual`
  (`Unit._hide_in_fog`). Значит, любой, кому погасили ВИЗУАЛЬНЫЙ тик, теряет
  единственный способ спрятаться и светится сквозь неразведанную черноту.
  Ровно это и делала спячка деревни гоблинов: `_set_dormant` гасил и физику, и
  визуал, и семьсот спящих были видны игроку через туман.
  **Спячка вправе гасить `set_tick`, но НЕ `set_draw`.** Стенд `qa_fog`, D7.
- Платы за это почти нет: стоящий боец гасит визуальный тик сам
  (`_proc_sleeping`), а будит его редкий общий обход
  `GameManager._wake_returned_far_units`. Спрятанный туманом при этом
  продолжает тикать (ветка тумана делает ранний `return`) — иначе ему нечем
  было бы вернуться на свет, когда разведка подойдёт снова.

## Звук

`AudioManager` — автозагрузка. **Каналов четыре, и это не избыточность:**
`play_3d` — бой (3D-пул, туман, лимиты по категориям, отсечка
`SFX_CULL_DISTANCE`); `play_ui` — щелчки интерфейса (короткие, по кругу,
`PROCESS_MODE_ALWAYS`, звучат на паузе); `play_voice` — РЕПЛИКИ приказов
(секунды длиной, два голоса; положи их в пул щелчков — десять заказов в один
кадр обрубят приказ на полуслове); `play_horn` / `play_chorus` — горн орды и
хор. Марш — свой пул лупов.

- **СРЕЗ ВЕРХА У 3D-ГОЛОСОВ — УМОЛЧАНИЕ ДВИЖКА, А НЕ БАНК.**
  `AudioStreamPlayer3D.attenuation_filter_cutoff_hz` = 5000 Гц при
  `attenuation_filter_db` = −24 включён у КАЖДОГО трёхмерного голоса, пока его
  не выключишь явно, и режет 4-16 кГц (металлический лязг) НА ЛЮБОЙ
  дистанции, включая нулевую. Снимается одной строкой —
  `attenuation_filter_cutoff_hz = 20500` (`AudioManager._open_filter()` ставит
  всем пулам). Замер — оконный `qa_audio3` анализатором спектра; он мерит
  НАКЛОН (отставание верха от низа), а не уровень: 3D-голос тише плоского
  эталона во всех полосах сразу, и по уровню исправный тракт краснел бы.
- **ЛИМИТЕРА НА МАСТЕРЕ НЕТ** (`AudioEffectHardLimiter` снят по заказу: он
  первым делом срезает транзиенты — а лязг это они, — и в большом бою пригибал
  весь звук под самый громкий источник). `_ensure_limiter()` оставлен и СНИМАЕТ
  эффект, если тот появился. Запас держат только громкости.
- **МУЗЫКА:** плейлист с паузами (первый трек на 10-й минуте, затем 10 минут
  тишины). У меню СВОЙ трек; ресурс партии обязательно раскручивается
  `_unloop()` — он общий с меню, а меню его зацикливает.
- **НОВАЯ СЛУЧАЙНОСТЬ — ТОЛЬКО ИЗ `AudioManager.rng`.** Партия сеется
  (`seed(world_seed)`), и один лишний `randf()` в общем потоке сдвигает ВСЕ
  жребии (так таймер рыка при рождении тролля перекрасил половину стендов).

### Марш пехоты

- **ИСТОЧНИК — ОТРЯД, А НЕ БОЕЦ**, вход пакетный (`AudioManager.march_report`
  ← `GameManager._sweep_march_audio` раз в `MARCH_SWEEP_SEC`, опрашивает
  ВЫБОРКУ бойцов). Тысяча бойцов — это тысяча лупов; отряд же и есть то, что
  марширует. **Опрос состояния, а не события «пошёл/встал»:** события пришлось
  бы расставить по всем путям (приказ, прибытие, бой, отход, гарнизон,
  гибель), и любой забытый оставил бы вечно топающий луп — луп, в отличие от
  удара мечом, сам не кончается НИКОГДА. По той же причине марш глушится явно
  в паузе, при выходе в меню и в `stop_all_music`.
- **ГОЛОСОВ ДВА — ШАГ И БЕГ, РАЗНЫМИ ФАЙЛАМИ.** История: шесть КОПИЙ одного
  файла (складываются когерентно — пришлось раздавать питч и сдвиг старта,
  владелец услышал это как «поломанный звук») → один чистый луп → два голоса
  разными файлами. Их сумма некогерентна по построению, разводить нечем и
  незачем. Голос выдаётся БЛИЖАЙШЕМУ отряду В СВОЁМ СОСТОЯНИИ (иначе два
  шагающих займут оба, и бегущий мимо не зазвучит вовсе).
- **ГРОМКОСТЬ ВЫВЕДЕНА ИЗ БЮДЖЕТА, А НЕ ПОДОБРАНА НА СЛУХ:** некогерентная
  сумма N источников растёт как √N, потолок на голос =
  `MARCH_BUDGET / sqrt(MARCH_VOICES)`. Поднимать громкость можно только ВМЕСТЕ
  с бюджетом и понимая, чем платит бой. Текущие `MARCH_WALK_DB` −16.0,
  `MARCH_RUN_DB` −14.1.
- **ГРОМКОСТЬ НЕ ЗАВИСИТ ОТ ЗАТУХАНИЯ ДВИЖКА** (`ATTENUATION_DISABLED`):
  слушатель висит на камере, и её высота — это зум, то есть громкость марша
  задавало бы колесо мыши. Марш — СЛОЙ АТМОСФЕРЫ, панорама (лево-право)
  осталась. Что «рядом», решает окно камеры (см. «Аудио-окно камеры»).
- **«БАХ» НА ОСТАНОВКЕ — ЭТО `stop()` НА НЕПРЕРЫВНОМ ЛУПЕ.** Лечение из трёх
  частей, нужны все: затухание вместо `stop()` (`_march_release` + хвост),
  плавный вход (`MARCH_FADE_IN`) и ЗАПАС против чехарды (`MARCH_KEEP_BIAS` —
  держащий голос отряд считается ближе, иначе два соседних отряда отбирают
  голос друг у друга по нескольку раз в секунду). В пуле на один голос БОЛЬШЕ,
  чем `MARCH_VOICES`: догорающий хвост не возвращается в стопку свободных.
- **ЩЕЛЧОК НА СТЫКЕ ПЕТЛИ — ЭТО НАБИВКА MP3**, а не обрыв волны: кодировщик
  дописывает в начало миллисекунды мусора, и точка возврата попадает в него.
  Лечится движковым `loop_offset` (`LOOP_TRIM_SEC` 0.008); у бегового лупа
  оттуда же вырезана тишина начала (`MARCH_RUN_START_SEC`).
- **ПИТЧ ВЫСТАВЛЯЕТСЯ ЯВНО, А НЕ «ОСТАВЛЯЕТСЯ КАК ЕСТЬ»:** голоса берутся из
  пула, и без возврата к 1.0 следующий марш унаследует расстройку предыдущего.
  Питч тянет и ВЫСОТУ ТОНА — растяжения времени без смены высоты у
  `AudioStreamPlayer` нет вовсе: нужен другой темп — нужен другой сэмпл.
- **ЧТО НЕ СЛЫШНО, ТО И НЕ ДЕКОДИРУЕТСЯ:** отсечка стоит ДО раздачи голосов
  (лупы лежат в mp3 — самом дорогом формате). Из тумана не топают.
- **СЛОЙ «ЗВОН ДОСПЕХОВ» СНЯТ ОКОНЧАТЕЛЬНО — ДВА ОТКАТА ПОДРЯД.** Вывод:
  звон снаряжения — это десятки мелких НЕСИНХРОННЫХ призвуков, а любой
  отдельный удар поверх НЕПРЕРЫВНОГО лупа читается как посторонний стук,
  сколько его ни рандомизируй. Свести их мог бы только свой сэмпл кольчуги.
  Третьей попытки быть не должно — стережёт `qa_voice` D4б.

### Реплики команд и голоса

- **У РЕПЛИК ДВА ОКНА ПОВТОРА, И ОБА НУЖНЫ:** своё на событие
  (`VOICE_LIMITS`, 9 с — игрок щёлкает ПКМ подряд, поправляя точку) и общее
  (`VOICE_GLOBAL_GAP` — своё окно не спасает от наложения РАЗНЫХ реплик).
  «Держать строй» звучит ЧЕРЕЗ РАЗ (`VOICE_ALTERNATE`; пропущенный вызов всё
  равно ставит `_voice_last`, то есть окно считается и от пропуска).
- **ЦЕПОЧКА** (`play_voice_chain`): рог, а следом клич — два `play_voice`
  подряд этого не дают (их разводит общее окно, и правильно). Клич вступает
  под затухающий хвост рога. У приказа атаки звучит ТОЛЬКО рог — «вперёд,
  марш» поверх него читается как чужая реплика.
- **МАССОВОСТЬ СЧИТАЕТСЯ В БОЕВЫХ ОТРЯДАХ** (`MASS_ORDER_SQUADS` 5,
  `selected_combat_squad_count`): рабочий числится отрядом ИЗ ОДНОГО, и пять
  крестьян давали «боевой клич фаланги». **Признак «боевой» — это ОРУЖИЕ**
  (`squad_is_combat`: `attack_damage > 0`) плюс виртуал `Unit.is_combatant()`
  (рабочий и монах отвечают `false`): список типов пришлось бы дополнять, и
  забытая строка вернула бы клич. Место одно на все каналы.
- **МАРШ ЗВУЧИТ И НА ПРИКАЗЕ АТАКИ:** отряд идёт к цели в `ATTACKING`, и
  «идёт» отличается от «дерётся» ДИСТАНЦИЕЙ, а не состоянием.
- **РЕПЛИКИ ПРИКАЗОВ — ПУЛ ИЗ ПЯТИ** (`ORDER_REPLIES`: куски одного mp3 по
  таймкодам from/to, снятым зондом; `play(from)` + `_tick_voice_clips` глушит
  на `to`), без повтора подряд.
- **ВОЗГЛАСЫ РОДОВ ВОЙСК — ХУКИ-СВОЙСТВА** (`Unit._sfx_grunt` / `_sfx_hurt` /
  `_sfx_move_grunt` / `_sfx_shout` / `_shout_chance` / `_sfx_death`; база
  молчит). Раздаёт `AudioManager.squad_voice(cat, key, at, chance, gap)` —
  окно НА ОТРЯД и ОДИН жребий на окно. Файлы орды и тролля переименованы в
  ASCII (правило 8). Нарезка пака грюнтов — `qa_audio_env/Slice`.
- **ХОР СМЕХА — ТОЛЬКО ЗА УБИЙСТВО ГОБЛИНОМ И ТОЛЬКО В МАЛОЙ СТЫЧКЕ**
  (`_on_squad_wiped`: добил гоблин не-тролль, отрядов орды рядом не больше
  `LAUGH_MAX_SQUADS`); рык тролля — на его убийство с шансом
  `TROLL_VICTORY_CHANCE`. Слышимость проверяется ОДИН раз, пока точка ещё
  освещена павшим.
- **ЛОВУШКИ:** живой `GameManager` в пустой сцене шлёт ПУСТОЙ отчёт о марше
  четыре раза в секунду и гасит отряд стенда (`set_physics_process(false)`);
  слушателя надо завести ЯВНО (`Viewport.get_audio_listener_3d()` возвращает
  только назначенный `AudioListener3D`, камера им не считается);
  `stream_paused` у незвучащего плеера не ставится.


## Баланс: TTK, мораль, экономика, ветеранство (авг. 2026)

**Расчётный документ — `docs/BALANCE_MATH.md`.** Здесь только то, что обязан
знать код; числа и их обоснование там. Стенд — **`qa_balance`** (20 проверок,
headless): формула брони, дуэли, схватка отрядов, паника, перки, темп экономики.

- **БРОНЯ СНИЖАЕТ УРОН ДОЛЕЙ, А НЕ ВЫЧИТАНИЕМ**
  (`unit_stats_config.damage_after_armor`, `ARMOR_SOFTNESS` = 25):
  `прошло = урон × A / (A + защита)`. Вычитание было сверхлинейным (каждое
  следующее очко брони дороже предыдущего) и ЗАВИСЕЛО ОТ ВЕЛИЧИНЫ УДАРА — одна
  и та же броня резала 60 % урона лучника и 25 % урона рыцаря, то есть цена
  очка скакала от того, кто по тебе бьёт. Долевая формула не даёт нуля НИКОГДА
  (при защите 500 проходит 4.8 %), и бессмертие невозможно по построению, а не
  по страховочному порогу. **Формула живёт в ОДНОМ месте** — и бой, и стенды, и
  панель статов обязаны звать её, а не повторять арифметику.
- **ЗАПАС ЖИЗНИ ПОДНЯТ ПОЧТИ ВДВОЕ, УРОН — ПОЧТИ НЕТ.** Копейщик 70 → 180 HP
  при уроне 15 → 14. Дуэль копейщиков была 9.3 с (пять ударов), стала 30 с
  (пятнадцать). Схватка отрядов 60 на 60: перелом на 31-й секунде, полное
  истребление на 57-й — ровно заказанное окно.
- **МОРАЛЬ — ЧИСЛО ОТРЯДА, А НЕ БОЙЦА** (`GameManager._sweep_morale`). Паникует
  и бежит отряд целиком; держать её на бойце значило бы усреднять шестьдесят
  чисел на каждую проверку и заводить шестьдесят таймеров ступора вместо одного.
- **ПОТЕРИ СЧИТАЮТСЯ ДОЛЕЙ ПОЛНОГО ШТАТА** (`squad_full_size` — НАИБОЛЬШИЙ
  состав за жизнь отряда, пишется в `add_to_squad`). Брать штат из конфига
  нельзя: отряд бывает неполным (последние из барака, огрызок после боя), и
  тогда гибель ВСЕГО состава снимала бы пятую часть морали — паника не
  наступала бы никогда. Поймал `qa_balance` C1.
- **ПОРОГОВ ПАНИКИ ДВА, И ОБА ОБЯЗАТЕЛЬНЫ.** Абсолютный (ниже 30 % морали)
  ловит отряд, который перемололи; относительный (вдвое ниже, чем у того, с кем
  дерусь) ловит другое — свежий отряд против ветеранов ломается ДО того, как
  потеряет половину людей. Без второго новобранцы дрались бы с легендой на
  равных до последнего человека.
- **ХРУПКОСТЬ РОДОВ ВОЙСК ВЫВОДИТСЯ ИЗ ОДНОГО ЧИСЛА.** Порог общий, а стартовая
  мораль разная — и лучник (45) ломается на 17 % потерь, копейщик (65) на 39 %,
  мечник (75) на 50 %, конница (100) на 78 %. Ни одной особой ветки в коде под
  это нет.
- **СТУПОР — ЭТО ПОТЕРЯ УПРАВЛЕНИЯ.** Двадцать секунд отряд не принимает
  приказов (`Unit._panicked` гейтит `command_move` и `command_attack`), не
  атакует (гейт в `_check_auto_aggro`) и получает защиту ×0.7. Добить его в это
  время можно и нужно: бой выигрывается сломом, а не полным истреблением.
- **ПРИКАЗ НА БЕГСТВО ОТДАЁТСЯ ДО ВЗВОДА ПРИЗНАКА** (`Unit.panic_flee`): иначе
  он отбился бы о собственный запрет, и паникующий строй остался бы стоять на
  месте с флагом над головой.
- **БЕЛЫЙ ФЛАГ — НЕ ВТОРОЙ УЗЕЛ, А СОСТОЯНИЕ ЗНАМЕНИ ОТРЯДА**
  (`SquadBanner.shown_white`). Разбор — ниже, в разделе «Белый флаг паники:
  два сценария, один узел».
- **АУРА ЛЕГЕНДЫ ВХОДИТ В ПОРОГ, А НЕ В САМУ МОРАЛЬ.** Иначе уход легенды с
  поля резал бы соседям накопленное число, и они паниковали бы от одного её
  отъезда.
- **НА КАЖДОЙ СТУПЕНИ РОВНО ТРИ КАРТОЧКИ, И ВСЕ ТРИ ИЗ ОДНОЙ ТРОЙКИ СТАТОВ**
  (`_VET_BONUS_TEMPLATE`, ступени 1-6). Заказ владельца: «выбор из пяти
  карточек — информационный шум». Было 3/4/5 по ступеням, а вторичные
  параметры брались из всех двенадцати ключей. Правило «ровно три ненулевых
  модификатора» соблюдалось и раньше — сменился не СЧЁТ, а НАБОР: теперь
  только атака, броня и запас жизни. Смысл в СРАВНИМОСТИ: раньше игрок выбирал
  между «+2 атаки и +4 морали» и «+2 брони и +0.3 напора», то есть между
  величинами из разных измерений; теперь вопрос один и тот же на всех
  ступенях — удар, защита или живучесть.
- **У КАЖДОЙ КАРТОЧКИ ОДИН ПАРАМЕТР В МИНУС, И БЕЗ НЕГО ВЫБОРА НЕТ ВОВСЕ.**
  Иначе это «возьми что нравится, всё равно всё хорошее». Минус всегда равен
  единице и всегда бьёт по тому стату, который карточка НЕ качает. Лестница
  прибавки прежняя: лычки (1-3) +2 к главному, флаги (4-6) +3, штандарт (7)
  даёт ПРАВИЛО вместо цифры. Запас жизни считается в своих единицах (+14 и
  +20) — та же доля от базы, что +2 и +3 у атаки с бронёй.
- **ПЯТЬ ЛЕГЕНДАРНЫХ ПЕРКОВ НЕ ТРОНУТЫ** (подтверждение владельца). Сокращение
  до трёх касается только числовых ступеней: седьмая даёт правило, и пять
  разных правил шумом не являются — их выбирают ОДИН РАЗ за жизнь отряда.
  Проверяет `qa_balance` D2 («три карточки на ступень») и D2б («параметры
  только из тройки»).
- **СЕДЬМОЙ ГРЕЙД ДАЁТ ПРАВИЛО, А НЕ ЦИФРУ.** Пять легендарных перков помечены
  id вида `legend_*` и лежат в том же списке `squads[sid]["chosen"]`, что и
  обычные выборы, — поэтому сохранение партии подхватывает их само, без
  отдельного поля и отдельной ветки в `SaveLoadManager`. Спрашивает их
  `GameManager.squad_has_perk`. Три числовых параметра у каждого оставлены
  намеренно: панель награды собирает подпись из ненулевых модификаторов и
  показала бы пустую плашку.
- **ПРОВЕРКА ПЕРКА — БИТ, А НЕ ПОИСК СТРОКИ** (`unit_stats_config.perk_bit` /
  `perk_mask_of`, порядок битов — `LEGEND_PERK_IDS`). `squad_has_perk` искал id
  линейным перебором строк по списку выбранных наград, а спрашивают его из
  `squad_armor_pierce`, то есть из `take_damage` жертвы — на КАЖДОЕ списание
  урона. У самого горячего спрашивающего бит ещё и снят один раз в поле
  (`_bit_pierce`): даже словарный поиск по строке — это хэш строки.
- **СЛОВО ПЕРКОВ ВЫВОДИТСЯ ИЗ СПИСКА, А НЕ ВЕДЁТСЯ ПО СОБЫТИЮ**
  (`GameManager._perk_bits_of`). Здесь было две неверных версии подряд, и обе
  стоит помнить. Ведение ПО СОБЫТИЮ (из `apply_veteran_choice`) пропускает
  загрузку партии и стенды, которые пишут `chosen` НАПРЯМУЮ. Сверка ПО ДЛИНЕ
  списка пропускает подмену равной длины — `qa_balance` меняет
  `["legend_steadfast"]` на `["legend_pierce"]`, и D5 краснеет ровно на этом.
  Верный признак свежести — ССЫЛКА НА МАССИВ ПЛЮС РАЗМЕР: присвоение кладёт
  новый массив, `append` правит прежний. Второго источника правды при этом не
  заводится — истина по-прежнему в `chosen`, слово только выводится.
- **ПЕРКИ СПРАШИВАЮТСЯ ТАМ, ГДЕ ЕСТЬ КЭШ.** «Таран» читается в
  `Unit._refresh_power` (напор кэшируется), а не в `_apply_push`, — иначе
  словарный поиск в автозагрузке платился бы на КАЖДЫЙ удар. «Стена щитов» — в
  `_incoming_damage_factor`, и её проверка стоит ПОСЛЕДНЕЙ в цепочке условий,
  чтобы рукопашная за неё не платила.
- **`Warrior._incoming_damage_factor` ОБЯЗАН ЗВАТЬ `super`.** Там стоял
  безусловный ранний выход при опущенном щите — с ним легендарная «Стена щитов»
  у отряда мечников не работала бы вовсе, и заметить это можно было бы только
  по числам.
- **ЭКОНОМИКА НАМЕРЕННО СКРОМНАЯ.** Рабочий приносит 28 единиц в минуту (цикл
  5 с + ходка ~7.7 с на 6 единиц), половина цикла — ходка. Стартовый запас
  урезан с двадцати тысяч до 550/380/340/200: прежние числа — отладочные, с
  ними первые полчаса партии не существует. Бараки и кузница подешевели втрое
  (320/150/220 и 450/300/180): при прежних ценах на одни бараки уходило
  двадцать минут, то есть вся ветка улучшений была выключена на полпартии.
- **ДРЕВО КУЗНИЦЫ СВЕДЕНО В ТРИ ТИРА ПО РЯДАМ** (1 — I, 2-3 — II, 4-5 — III).
  Цена и время — по РЯДУ, бонус — по ТИРУ, и ни один узел не даёт больше
  8-12 % от базы. Первое исследование копится 2.7 минуты при бригаде 4+4;
  стартового запаса не хватает и на два узла — дилемма «броня копейщикам ИЛИ
  дальность лучникам» встаёт с первой же кузницы.

## Аудио-окно камеры (09.09.2026)

Заказ: «звук ходьбы слышен со всей карты и перегружает поток». Марш теперь
слышен только в ОКНЕ ВОКРУГ ЦЕНТРА ЭКРАНА и гаснет с отдалением камеры.

- **ОКНО МЕРЯЕТСЯ ОТ ФОКУСА, А НЕ ОТ СЛУШАТЕЛЯ** (`GameManager.view_point`,
  `AudioManager.march_focus`). Слушатель висит на камере, а камера стоит
  ПОЗАДИ фокуса на десятки метров (наклон 45°): мерить от неё значило бы
  двигать окно вместе с наклоном. Слушатель остался для ПАНОРАМЫ.
  Ловушка стенда: `qa_voice` строил площадку у слушателя — с окном в 40 м
  вся площадка лежала за ним, и блок D проверял бы отсечку вместо раздачи.
- **ЧИСЛА:** до `MARCH_FULL_RADIUS` (15 м) полная громкость, до
  `MARCH_FADE_RADIUS` (40 м) квадратичный спад в ноль, дальше голос не
  выдаётся вовсе (луп не декодируется). Сверху окно режется видимой полосой
  (`march_cutoff` = min(40, `view_radius`)): «только видимые на экране».
- **ЗУМ ГАСИТ** (`march_zoom_gain` = (1 − t)^`MARCH_ZOOM_POW`, t от камеры
  через `update_view_point(..., zoom_t)`): вплотную 1, на пределе 0. Это
  разворот прежнего «от зума громкость не зависит» — то правило снимало
  СКАЧКИ обратно-квадратичного затухания через высоту слушателя; здесь
  зависимость монотонная и плавная. На нуле голоса не раздаются, а не
  глушатся громкостью.
- **ТЕМП:** шаг `MARCH_WALK_PITCH` 1.08 («отстаёт от ног»), бег
  `MARCH_RUN_PITCH` 1.12 → 1.23. **У БЕГОВОГО ЛУПА ВЫРЕЗАНА ТИШИНА В НАЧАЛЕ
  ФАЙЛА** (`MARCH_RUN_START_SEC` 0.78): magiaz-soldiers_marching молчит
  0.78 с, первый сапог на 1.10 с; плюс такт обхода 0.25 и вход 0.12 — больше
  секунды между «побежали» и «затопали». Стартует и ЗАЦИКЛИВАЕТСЯ с этой
  точки (`loop_offset`), иначе тишина возвращалась бы на каждом круге.
- **ЗВУК В HEADLESS МЕРИТСЯ:** dummy-драйвер AudioServer микширует, и
  `AudioEffectCapture` на своей шине отдаёт PCM (`qa_march_env/Probe` —
  огибающая по окнам 20 мс). Это способ узнать про файл то, чего не видно
  в редакторе: тишину, набивку, пики.

## Конфиги — источник истины

- `scripts/unit_stats_config.gd` — характеристики, стойки, ветеранство, цены,
  стартовые ресурсы, баллистика лучника, сплочённость, **формула брони, мораль
  и паника, легендарные перки**. **Правит владелец.**
  **СТАРТОВЫЙ ЗАПАС ИГРОКА — 20 000 КАЖДОГО РЕСУРСА, ЗАФИКСИРОВАН ВЛАДЕЛЬЦЕМ
  10.09.2026: «никогда не сбрасывай и не меняй мои настройки ресурсов».** Не
  трогать ни при каком красном стенде и ни в каком разборе баланса; стенд,
  которому нужен «скромный запас», судит по запасу ИИ (`AI_STARTING_RESOURCES`,
  так переписан `qa_balance` E2). Прежняя запись «стартовый запас урезан до
  550/380/340/200» в разделе «Баланс» — история.
  Расчёт всего перечисленного — `docs/BALANCE_MATH.md`.
- `scripts/forge_config.gd` — древо кузницы (`UNITS[unit][cell]`, одна таблица);
  узел древа = слот улучшения, поэтому вся машинерия исследований работает без
  ветвлений.
- `scripts/ai_start_army_limit.gd` — лимиты, тактики, сбор волны, поведение ИИ,
  а также **`START_SQUADS`** — стартовые отряды красного ИИ с явным рангом
  (по умолчанию список ПУСТ: старт равный, это стережёт `qa_ai`, проверка 2).
- `scripts/game_difficulty_config.gd` — пресеты `Easy` / `Normal` / `Hard`.
- `scripts/SaveLoadManager.gd` — сохранение и загрузка партии.
- `scripts/perf_config.gd` — переключатели и шарды; `tick_meter`/`vis_meter` дают
  честное время, `profile_physics` — только доли (завышает сумму ~1.8×).
- `BALANCE.md` — где какое число. `ASSETS.md` — куда класть файлы.

## Известные ограничения

Нет мультиплеера; поиск пути — сетка A* ядра (скалы, вода, фундаменты). Фракция в меню меняет только папку спрайтов.

**МАСШТАБ ЗАМЕРЕН ТОЧНО (26.08.2026), И СОСТАВ АРМИИ РЕШАЕТ БОЛЬШЕ ЧИСЛЕННОСТИ.**
`qa_mass_siege` на 3000×3000 смешанным составом (как в партии) укладывается в
бюджет: марш 10.5, свалка 11.2, затяжной бой **11.6-12.9 мс** против 16.6 —
проверка B3 ПРОШЛО. `qa_mass_battle` на тех же 6000 даёт 18.1 мс и FAIL, и это
ожидаемо: там шесть тысяч ЧИСТЫХ копейщиков, все до одного идущие в контакт.
15000 не укладываются и на марше.

Разбор по веткам, история правок и то, что из них окупилось, — в
**`docs/PERF_6000.md`** (раздел 6 — заход по `grid_update` и `check_auto_aggro`).
**Стратегия следующего прыжка (70+ FPS на 4000, этапы D1-D3: тыловой напор и
автопилот подхода в ядре → тред-пул пакетных проходов → туман/стрелы/буферы) —
в `docs/PLAN_D_70FPS.md`**; там же — почему халвинг частоты тика и полный порт
FSM в C# уже измерены и в план не входят.
Оттуда же выводы, которые стоит знать до того, как браться за следующую
оптимизацию:
- **РАЗБИВКА ПО ВЕТКАМ ГОДИТСЯ ДЛЯ РАНЖИРОВАНИЯ, НО НЕ ДЛЯ БЮДЖЕТА.** Профиль
  ставит пару `Time.get_ticks_usec()` вокруг КАЖДОЙ ветки КАЖДОГО бойца, и у
  ветки, которая идёт 1100-1600 раз в кадр, измеритель стоит соизмеримо с ней
  самой. Замер авг. 2026: профиль обещал 1.83 мс на `check_auto_aggro` +
  `grid_update`, честный A/B по `tick_meter` дал **0.85**. У веток, которые
  зовутся РАЗ В КАДР (`squad_corridor`, `sep_overlap`, `batch_move`), завышения
  почти нет — их числа читаются как есть.
- **ПЕРЕД ТЕМ КАК ЧИНИТЬ ВЕТКУ, ЕЁ НАДО ВЫКЛЮЧИТЬ И ПОСМОТРЕТЬ НА ТИК.**
  `squad_corridor` выглядел крупнейшей целью (0.94 мс/кадр), а выключение его
  сделало тик ХУЖЕ на 0.8 мс: коридор раздаёт отрядные `_clear_trunk` /
  `_clear_enemy`, и без них каждый боец сам лезет в сетку. Он стоит 0.9 и
  экономит 1.7.
- **КАСКАД ПО КАДРАМ И КУРСОР ВМЕСТО `append` ПРОВЕРЕНЫ И НЕ ОКУПАЮТСЯ.**
  Авто-агро и так входит только в `State.IDLE`, частоту задаёт таймер плюс
  отрядный кэш цели, а фазы разведены при рождении. Очередь поз дорога не
  проверкой ёмкости, а самим числом колонок.
- **«Сон задних шеренг» проверен дважды и отвергнут дважды.** Механика уже
  написана (`perf_config.battle_lines`) и выключена по замеру: экономятся не
  лишние вычисления, а бойцы, переставшие доходить до врага (контакт падает с
  25 % до 6 %).
- **База замера снимается в том же заходе, что и правка.** Вчерашние числа
  сравнивать с сегодняшними нельзя: правка, ничего не менявшая, «ухудшила» тик
  на 1.6 мс — уехала база, а не код.
**Список пре-существующих «красных» стендов (не регрессий) — в
`docs/ENGINEERING_LOG.md`, раздел «Известные красные».** Коротко: перед тем как
чинить код по красному стенду, проверьте, не утверждает ли проверка требование,
которое владелец только что развернул, и не ждёт ли она `process_frame` там, где
нужен `physics_frame`.

## Стресс-стенд `qa_mass_siege`: 700 на 700 с базой

**Правки боевого поведения проверяются ЗДЕСЬ, а не на стендах из десяти
бойцов.** Заказ владельца дословно: «стерильные тесты показывают зелёный дым,
а в реальном бою игра ломается». Сцена: две смешанные армии по 700 (копейщики
0.45, лучники 0.25, мечники 0.20, кабаны 0.10, отряды по 35) плюс база
противника из шести построек. Пять разделов, девять вердиктов: марш (разброс,
«бег на месте»), проход сквозь союзный строй, рабочие, бой с новым приказом
после него, осада. Тик печатается в каждом разделе.

- **ПОРЯДОК РАЗДЕЛОВ ЗНАЧИМ: осада идёт ПОСЛЕДНЕЙ.** Она переставляет пять
  отрядов к базе, и если мерить бой после неё, в замер попадают отряды,
  бредущие обратно через полкарты. Именно это давало разброс то 5.8, то 24.6 м
  на НЕИЗМЕННОМ коде.
- **Осада меряется У ЗДАНИЯ, а не по дороге к нему.** Первая версия отдавала
  приказ оттуда, где отряды стояли, — между ними и базой была вся чужая армия,
  и до постройки не доходил никто (сектора розданы верно, отставание 33-45 м),
  а «ближайшие центры 1.9 м» описывали ОБЩУЮ СВАЛКУ на полпути. Теперь отряды
  ставятся ОДНОЙ КУЧЕЙ с тыла базы — в том самом худшем состоянии, на которое
  жаловался владелец.
- **Проверки — СВОЙСТВА, а не круглые числа.** «Центры отрядов не ближе 4 м»
  невыполнимо по геометрии: пять отрядов стоят на периметре постройки, и
  соседние центры не могут разойтись дальше хорды `2·r·sin(дуга/2n)`. Стенд
  считает эту хорду сам и сравнивает с ней.

## Форма отряда при приказе: зажим выводится из ДИАГОНАЛИ БЛОКА

`SelectionManager._issue_march_keeping_shape` переносит строй как есть, но
смещение каждого бойца от центра ЗАЖИМАЕТСЯ: кто стоял дальше, получает слот
на границе круга в том же направлении. Без зажима растянутый боем отряд
переносил свою «колбасу» в разметку (замер: растянутость разметки 19.9 м).

**Радиус считается по полудиагонали блока (`√n · интервал · SHAPE_CLAMP_SLACK`),
а НЕ по кругу равной площади.** Первая версия брала `√n · 0.5 · интервал` и
вышла ТУЖЕ реального строя: у шеренги 10×3 полудиагональ ~3.05 м, а круг
равной площади — 2.38 м. Зажим сминал в круг ОБЫЧНЫЙ приказ (поймал
`qa_squad`, проверка 5: габариты разъезжались в 1.3 раза).

**Центр берётся МЕДИАНОЙ** (`GameManager._centroid_of`) — как и везде в
проекте. Стенд, сверяющий перенос смещений, обязан брать опору ТЕМ ЖЕ
способом с обеих сторон, иначе он меряет разницу среднего и медианы
(0.1-0.2 м), а не искажение строя.

## `post_pos` — это НЕ разметка отряда

**Личный пост бойца переписывает ЛЮБОЙ `command_move`** — и подзыв
сплочённости, и «встать где стою» после исчерпанного приказа. Это не порча
строя: пост означает «где меня поставили» и служит поводком авто-агро.
**Строевые места лежат отдельно — в `squads[sid]["slots"]`**, и именно по ним
смыкаются ряды. Стенд, меряющий «растянутость разметки» по `post_pos`,
краснеет на каждом законном подзыве отставшего; мерить надо `slots`.

Там же вторая ловушка: **огрызок отряда в замер разметки не идёт.** Двое
выживших с далёкими слотами дают «растянутую разметку» на пустом месте.

## Приказ атаки на постройку: сектора по ДУГЕ ПОДХОДА

`SelectionManager._ring_squads_around` раздаёт каждому выделенному отряду свою
точку вокруг постройки (кэш `GameManager.squad_attack_anchor`, читает подход
бойца в `Unit._process_attack`). Пять отрядов, посланных на одно здание, шли к
ОДНОЙ точке — его центру — и слипались в кашу.

- **Дуга разворачивается К ТЕМ, КТО ПРИШЁЛ** (`RING_ARC`, 150°), а не по всей
  окружности. **Поиска пути в игре нет вовсе:** сектор за постройкой
  недостижим — боец идёт по прямой, упирается в стену, дальность удара
  сходится, и он остаётся с ближней стороны, а отряд при этом рвётся надвое
  (замер: отставание 12.4 м у двух отрядов из пяти, разброс отряда 41.6 м).
  Это и есть «облепить со всех ДОСТУПНЫХ сторон».
- **Сектор достаётся по ФАКТИЧЕСКОМУ азимуту отряда**, причём азимут меряется
  от оси подхода и приводится в (−π, π]: иначе порядок рвётся там, где угол
  переходит через π, и отряды меняются местами, пересекая друг друга.
- **Кэш нужен потому, что одного приказа на движение мало:** следующий же
  `command_attack` повёл бы всех в центр здания, и куча вернулась бы.

## Анимация ходьбы — от ФАКТА перемещения, а не от намерения

`Unit.moved_recently()`: смещение замеряется в начале `tick_physics`, признак
держится `MOVE_ANIM_HOLD_MS` (180 мс) при скорости выше
`MOVE_ANIM_MIN_SPEED`. Прежнее условие `velocity.length() > 0.1` давало «бег на
месте»: `velocity` — это НАМЕРЕНИЕ идти, а упёршийся в чужой строй боец
намерение сохраняет и перебирает ногами стоя. Позу стоящего выдаёт
`Spearman._standing_key()`.

## Рабочие — ОДИНОЧНЫЕ АГЕНТЫ

`GameManager.SINGLE_AGENT_TYPES` / `squad_is_single_agent(sid)`. Выходя из
постройки, такой отряд не строится в шеренгу: места берутся по спирали
золотого угла (`Building.SINGLE_AGENT_SPREAD`), полоса выхода не задаётся.
Смыкание рядов рабочих не касается (`REFORM_SKIPS_WORKERS`).

## Проход сквозь союзный строй: послабление в СОЛВЕРЕ

`ArmyCore.BatchSeparation(..., passRelief)` (`Unit.SEP_PASS_RELIEF` = 0.45).
Послабление действует ТОЛЬКО между бойцами РАЗНЫХ отрядов и ТОЛЬКО когда один
идёт, а другой стоит: идущий продавливается сквозь стоящую шеренгу, а два
стоящих строя друг в друга не влезают. Замер `qa_mass_siege`: 35 из 35
пересекают чужой строй за 4.2-4.5 с, завязших ноль.

**Метрика прохода меряет ФАКТ ПЕРЕСЕЧЕНИЯ, а не «дошла половина».** Прежняя
версия выходила по «половина в покое» и меряла тем самым скорость самого
быстрого — застрявшие в счёт не попадали вовсе. Время считается ФИЗИЧЕСКИМИ
КАДРАМИ (правило 12), завязшие — через две секунды после того, как пересёк
девятый десяток.

## Сложность партии: Easy / Normal / Hard

- **ПРЕСЕТ — ЭТО МНОЖИТЕЛИ ПОВЕРХ БАЗОВЫХ ТАБЛИЦ, А НЕ ВТОРАЯ КОПИЯ БАЛАНСА**
  (`scripts/game_difficulty_config.gd`). В файле нет ни одного базового числа:
  лимиты берутся из `ai_start_army_limit`, спячка и доход орды — из
  `goblin_config`, стартовый запас — из `unit_stats_config`. Копия неизбежно
  разъехалась бы: владелец правит лимит в своей таблице, а на «обычной»
  сложности ничего не меняется, потому что там лежит вторая, забытая цифра.
- **НИКТО НЕ ЧИТАЕТ `PRESETS` НАПРЯМУЮ.** Все спрашивают функции файла — они
  принимают базу и возвращают изменённое. Точки чтения: `EnemyAI` (лимиты
  отрядов и рабочих, мирная фаза, потолок армии), `ResourceManager`
  (стартовый запас), `Building.queue_unit` (время найма),
  `ConstructionSite` (время стройки), `GoblinAI` (спячка), `GoblinHut`
  (доход), `Main` (оба стартовых состава).
- **NORMAL ОБЯЗАН БЫТЬ В ТОЧНОСТИ ПРЕЖНЕЙ ИГРОЙ** — все его множители равны
  единице. Сложности появились поверх баланса, который настраивали без них;
  сдвинься здесь хоть одно число, и прежние замеры перестали бы что-либо
  значить. Стережёт `qa_difficulty`, блок B (восемь проверок).
- **КРУТЯТСЯ ТОЛЬКО ЛИМИТЫ И ТЕМП.** Ни одна ручка не трогает урон, запас
  жизни и броню: боец на Hard дерётся ровно так же, как на Easy. Тогда
  «сложнее» означает «больше и раньше», а не «у врага другие правила», и
  любой замер боя остаётся сравнимым между уровнями.
- **СЛОЖНОСТЬ ХРАНИТСЯ СТАТИКОЙ, А НЕ В `GameManager`**: она нужна ДО того,
  как автозагрузка успевает что-либо решить (главное меню строится первым), и
  нужна она конфигам — а конфиг, лезущий в автозагрузку, тянул бы всё дерево
  движка в стенд, который его не поднимал. Выбор переживает перезапуск игры
  (`user://difficulty.cfg`).
- **СДВИГ РАНГА ОРДЫ НЕ КАСАЕТСЯ НОВОБРАНЦЕВ** (`goblin_vet_shift`): отряды с
  рангом 0 не трогаются ни на одной сложности. Иначе на Hard вся орда вышла бы
  ветеранской, а это уже другой сценарий, а не сложность.
- В интерфейсе: ряд из трёх кнопок в главном меню (с строкой-подсказкой под
  ним) и такой же ряд в меню паузы. **В паузе прямо на экране написано, что
  действует сразу, а что со следующей партии:** лимиты и темп читаются вживую,
  а стартовый запас и стартовые отряды раздаются один раз при закладке.

## Стартовые отряды: одна таблица на обе фракции

- **СОСТАВ, ЧИСЛЕННОСТЬ И РАНГ ЗАДАЮТСЯ ПОСТРОЧНО** — `goblin_config.START_SQUADS`
  у орды и `ai_start_army_limit.START_SQUADS` у красного ИИ. Формат один и тот
  же: `{"unit", "count", "vet", "picks"}`, где `count: 0` означает уставной
  размер отряда, а `vet` — уровень по `VET_BANNER_TIERS`.
- **ПОЧЕМУ СПИСОК, А НЕ ТРИ ЧИСЛА.** Прежнее описание орды (состав
  `ARMY_COMPOSITION` + «первым пяти выдать серебро с четырьмя наградами»)
  умело ровно один сценарий. Дать кабанам ранг выше, чем пехоте, или вывести
  один отряд-легенду было НЕЛЬЗЯ, не переписав код спавна.
- **`ARMY_COMPOSITION` И `ARMY_SQUADS` БОЛЬШЕ НЕ КОНСТАНТЫ**, а функции
  `goblin_config.army_composition()` / `army_squads()`, выведенные из
  `START_SQUADS`: два независимых списка разъехались бы, и правка стартовой
  орды молча меняла бы то, что она потом донанимает.
- **РАЗДАЧА НАГРАД ОДНА НА ВСЕ ФРАКЦИИ** — `Main.grant_squad_veterancy`. Она
  идёт тем же путём, каким награду берёт игрок (`apply_veteran_choice`), и
  второй реализации в проекте быть не должно.

## Сохранение и загрузка партии

- **МИР НЕ СОХРАНЯЕТСЯ, ОН ПЕРЕСЕВАЕТСЯ.** В файл уходит ЗЕРНО
  (`Main.world_seed`), а рельеф, лес, кусты, вода и россыпи руды строятся из
  него заново. Тысячи деревьев весили бы больше всей остальной партии и
  читались бы дольше, чем генерируются. Побочная выгода: замер, сделанный на
  «той самой карте», теперь можно повторить.
- **ЗЕРНО ЧИТАЕТСЯ В `Main._ready()`, И БОЛЬШЕ НИГДЕ.** Лес и вода сажаются
  там же, то есть ДО `start_game()`; зерно, поставленное позже, дало бы чужой
  рельеф под сохранёнными постройками. Отсюда и двухходовка: слепок кладётся
  в `GameManager.pending_load`, сцена перезапускается, `start_game()` в самом
  конце зовёт `SaveLoadManager.apply`.
- **БОЙЦЫ ЛОЖАТСЯ КОЛОНКАМИ** (`PackedInt32Array` / `PackedFloat32Array`), а
  не списком словарей — тем же приёмом, что и в ядре армии. Шесть тысяч
  бойцов это ≈170 КБ и один `store_var`; в JSON это были бы десятки мегабайт
  и разбор в секунды.
- **ФОРМАТ БЕЗОПАСЕН К ВЕРСИЯМ ДВУМЯ РАЗНЫМИ СПОСОБАМИ.** Поля читаются по
  именам и всегда с умолчанием (добавили ключ — старые файлы читаются);
  `FORMAT_VERSION` поднимают, когда меняется СМЫСЛ существующего поля, и
  тогда старый файл честно отклоняется. Отдельно живёт `WORLD_GEN_VERSION` —
  её поднимает тот, кто правит генератор карты: то же зерно дало бы другой
  мир, и такое сохранение грузить нельзя.
- **`store_var(..., false)` — full_objects ВЫКЛЮЧЕН НАМЕРЕННО.** В слепке нет
  и не должно быть ссылок на узлы: разреши их — и в файл уехал бы кусок
  дерева сцены, а чтение такого файла означало бы исполнение чужого кода.
- **ЧТО ВОССТАНАВЛИВАЕТСЯ:** склад, изученное в кузнице, постройки с их
  запасом жизни, отряды (ранг, счёт убийств, невыбранные награды, список
  выбранных, докупленные способности) и бойцы (род войск, сторона, отряд,
  точка, текущее здоровье). **ЧТО НЕТ:** стрелы, тела, туман, недострои,
  очереди найма и ПРИКАЗЫ — после загрузки войско стоит. Приказ ссылается на
  конкретный узел цели, а узлы после загрузки другие.
- **ЖИВОЕ СНОСИТСЯ СМЕРТЬЮ, А НЕ `queue_free`** (`_wipe_live_world`): только
  на этом пути боец снимает с себя строку в ядре армии, место в сетке соседей
  и место в отряде — то же правило, что у расформирования отряда.
- **ИССЛЕДОВАНИЯ ВОЗВРАЩАЮТСЯ ЧЕРЕЗ `finish_research`, А НЕ ЧИСЛАМИ ИЗ ФАЙЛА.**
  В файле лежит СПИСОК id; бонусы пересчитываются из таблиц, поэтому правка
  баланса в `forge_config` доезжает и до старых сохранений.
- Три слота (`user://save_N.tts`), подпись слота — время и сложность. Кнопки:
  меню паузы (сохранить и загрузить) и главное меню (загрузить; ряд не
  показывается вовсе, если сохранений нет).
- Стенд — `qa_difficulty`, блоки E–H (H поднимает НОВУЮ сцену Main из слепка
  и сверяет рельеф в трёх точках: зерно доехало — карта та же).
