# CLAUDE.md

Краткая карта проекта для Claude Code. Подробная история замеров и разборов
багов вынесена в `docs/ENGINEERING_LOG.md` — читать её только по конкретному
вопросу, в контекст она не грузится.

## Проект

3D-RTS на **Godot .NET 4.7.1 (mono)**, рендер **GL Compatibility**. GDScript +
C#-солвер `csharp/ArmyCore.cs`. Код и комментарии — по-русски. Отвечать
пользователю тоже **по-русски**.

## Сборка и проверка

- Редактор: `Godot_v4.7.1-stable_mono_win64`. Standard-сборка проект НЕ откроет.
  Бинарь: `E:\Downloads\Godot_v4.7.1-stable_mono_win64\Godot_v4.7.1-stable_mono_win64\Godot_v4.7.1-stable_mono_win64_console.exe`
- C#: `dotnet build "Ten Thousand Spearmen.csproj"` (нужен .NET SDK 8+).
- Компиляция всех скриптов: `<godot> --headless --import` (ошибки парсинга — в stdout).
- Дым: `<godot> --headless --path . res://scenes/Main.tscn --quit-after 400`.
- Стенд: `<godot> --headless --path . res://qa_<имя>/Test.tscn`. Провалы искать
  по строкам **«НЕ ПРОШЛО»** и **«провалов:»** (не по `FAIL`).
  **Не у всех стендов сцена зовётся `Test.tscn`:** у `qa_army` их три
  (`Matrix`, `MatrixLive`, `SelfTest`), у оконных — `Shot.tscn`. Скрипт,
  гоняющий шлюз списком папок, обязан это учитывать: «файла нет» выглядит в
  сводке как «ноль провалов».
- Windowed-only: `qa_shot9`, `qa_shot10`, `qa_shotvis`, `qa_shotcorpse`,
  `qa_wound/Shot`, `qa_banner/Shot`, `qa_fx`, `qa_veg/Test`, `qa_march_perf`. Только они и видят
  отрисовку: в headless счётчик вызовов отрисовки всегда ноль, а теней и трупов
  на экране нет вовсе. **Оконные стенды — единственный способ узнать, что
  шейдер вообще компилируется:** headless не собирает ни одного.
- **ВИЗУАЛЬНЫЙ ДЫМ — `qa_visual_smoke/Shot` (ОКОННЫЙ, 19 проверок).
  ОБЯЗАТЕЛЕН ПОСЛЕ ЛЮБОЙ ПРАВКИ ОТРИСОВКИ** — `DEPTH`, `render_priority`,
  `depth_draw`, порога среза альфы, размера квада, привязки спрайта к земле.
  Headless-шлюз такие правки не подтверждает ВООБЩЕ. Запуск:
  `<godot> --path . res://qa_visual_smoke/Shot.tscn -- --out=<путь без .png>`.
  Разбор — в разделе «Визуальный дым: объект на экране виден целиком».
- Сложность и сохранение партии — **`qa_difficulty`** (58 проверок, headless:
  пресеты, нейтральность Normal, монотонность ряда, файл, круговой рейс
  слепка, восстановление на живой карте, ПОЛНЫЙ ЦИКЛ — файл → новая сцена
  Main с тем же зерном — и построение экранов меню).
- Главный шлюз после правок логики: `qa_bugpass qa_goblin qa_spear qa_guard
  qa_hold qa_settle qa_crowd qa_squad qa_vet qa_target_lock qa_combat_lock
  qa_disengage qa_army qa_res2 qa_ai qa_ai2 qa_volley qa_fog qa_recon qa_forge
  qa_sel2 qa_corpse qa_wound qa_facing qa_orders qa_leash qa_cavalry qa_push
  qa_bldattack qa_difficulty qa_pivot qa_wall qa_balance qa_keep qa_panic
  qa_mega_battle qa_clash qa_full_game_4k qa_sprint13`. **Сводка шлюза обязана считать и
  `SCRIPT ERROR`**: ошибка внутри блока стенда роняет его молча, вердикты не
  печатаются, а «провалов: 0» остаётся (03.09.2026: так прятались блок F
  `qa_goblin` и блок B2 `qa_mega_battle` — тот самый, что стережёт проход
  рыцарей сквозь фалангу).
  **`qa_ai` проверка 5 БОЛЬШЕ НЕ КРАСНАЯ** (31.08.2026): она требовала по семь
  гарнизонных отрядов каждого типа при `SQUAD_LIMIT` лучников 5 и мечников 6,
  то есть арифметически невозможного; ожидание выведено как
  `min(HOME_GUARD_PER_TYPE, squad_limit)`. Там же вычищены `qa_spear` A10
  (ждал по стенным часам), `qa_spawnlane` D1 (мерил снос ворот не от того
  центра) и `qa_rally2` A1 (строил стену вдвое реже игровой) — разбор в
  разделе «Чистка красных стендов». **`qa_wall` A1 держит известный красный**
  (1-3 просочившихся из 48, 4 прогона из 6) — разбор в
  `docs/ENGINEERING_LOG.md`.
- Звук: `qa_audio`, `qa_audio2` (пул, утечки, цена) и **`qa_voice`** (речь
  команд, марш пехоты, тема меню).
- **Тролль, логово, месть гоблинов — `qa_troll`** (24 проверки, headless);
  **таран, топтание, дуга дубины, сбивание с ног, кровь тролля —
  `qa_troll_impact`** (17, headless; `Shot.tscn` — оконный снимок раненого
  тролля со сбитыми копейщиками, шейдер с пятнами компилируется только там);
  **карта ×5 по площади, река с бродом, гора, тайлы — `qa_map`** (24); **монах-целитель
  — `qa_monk`** (13). Разбор — в разделе «Босс-тролль, новая карта, монах».
- **ЦЕНА ОДНОГО ПЕРЕСЧЁТА ТУМАНА — зонд `qa_fogperf/Probe`** (headless,
  вердиктов нет): полный `refresh()` по частям — сбор источников по бойцам,
  ядро, заливка. Заведён 09.09.2026, когда «микролаги» оказались кадром
  пересчёта маски: 4.59 мс на 4000 бойцах, из них 3.38 — обход `_live_units`
  в GDScript ради 74 ячеек-источников. **`qa_full_game_4k` печатает ПИКИ
  КАДРА** (худший и p95 по стенным часам между кадрами) — средний FPS фризов
  не видит вовсе.
- **Башня, стрелковая, дома, лимит населения, аудио-окно — `qa_tower`**
  (46 проверок, headless: меню построек по цветовой папке, обзор башни в
  тумане, гарнизон только из лучников, часовой, ПКМ-выпуск, руины в долю,
  **стрельба гарнизона с площадки, неуязвимость укрытых и их отсутствие в
  сетке ядра (B8б-B8д, B13)**, найм лучников в стрелковой, три дома и ворота
  лимита, закон громкости марша). **`qa_march_env/Probe`** — зонд огибающей лупов марша
  (AudioEffectCapture в headless — dummy-драйвер микширует, PCM читается).
  Разбор — в разделе «Постройки: башня, стрелковая, дома» и «Аудио-окно».
- **Голосовое управление — `qa_voice_cmd`** (27 проверок, headless: словарь и
  сопоставление по типам войск, загрузка модели Vosk в фоне, шесть команд на
  живой сцене, отход фаланги как последовательность, границы; блок F гоняет
  WAV-образцы из `qa_voice_cmd/samples`, если они есть). Само распознавание проверяется ушами в окне: V, сказать, отпустить.
  Разбор — в разделе «Голосовое управление».
- **СПЕКТР И ГРОМКОСТЬ МАРША — `qa_audio3` (ОКОННЫЙ, 12 проверок).** Меряет
  АНАЛИЗАТОРОМ СПЕКТРА на шине SFX, что именно теряет звук по дороге до
  динамика, и проверяет затухание с расстоянием и плавность остановки.
  **Только в окне:** в headless движок ставит пустой звуковой драйвер, и
  анализатор читает тишину — та же слепота, что у буфера глубины.
  Разбор — в разделе «Срез верха у 3D-голосов».
- Смыкание строя после прохода союзника и знамя орды — **`qa_reform`**.
- Панель ветеранства (награды, тултип, значок ранга) — **`qa_vetui`**
  (`Test` — headless, `Shot` — оконный).
- Пивоты построек и деревьев (клик по картинке, кольцо на земле) — **`qa_pivot`**
  (10 проверок, headless).
- **Бой в толпе — `qa_wall`** (13 проверок, headless; A1 — известный красный,
  см. `docs/ENGINEERING_LOG.md`): щетина пик с ПЕРВОГО
  переключения стойки, проход сквозь живой строй, доля дерущихся из тех, кто
  дотягивается, и десять отрядов на одной хижине гоблинов (сектора, вторая
  линия, эшелонирование). Заведён потому, что обе жалобы владельца («протекают
  сквозь фалангу», «дерутся два-три крайних») живут на СТЫКАХ, которых на
  стенде из десяти бойцов нет вовсе. Блок C добавлен третьей жалобой — марш ЗА
  спину противнику из уже начатого боя.
  **A1 МЕРИТ ЦЕЛОСТЬ МЕСТНО И В МИГ ПЕРЕХОДА, А НЕ ДОЛЕЙ ВСЕГО ФРОНТА.**
  Три вражеских отряда идут на ОДНУ приманку в центре, то есть 48 мечников
  давят на среднюю треть из 24 копейщиков; общий порог считал такой участок
  целым (16 из 24 в середине при 74% по фронту), а перешедший линию успевал
  уйти к приманке, и прорыв приписывался ЧУЖОМУ участку. Разброс на неизменном
  коде был 0-18 человек. Теперь решение принимается один раз, в миг перехода,
  по живым в полосе ±0.9 м впереди точки.
- **Белый флаг паники — `qa_panic`** (19 проверок, headless): два сценария
  (новобранцы против ветеранов), отсутствие второго узла, судьба флага при
  снятии паники и при гибели отряда, езда флага на знаменосце и связность
  разбега. Разбор — в разделе «Белый флаг паники: два сценария, один узел».
- **Площадка сбора у здания — `qa_rally_zone`** (13 проверок, headless;
  `Shot` — оконный снимок двух заказов на рядах): прямоугольник 18 м
  вплотную к воротам, три заказа появляются НА площадке с первого кадра, в
  проёме ворот никого, отряды не накладываются, с флажком — сбор на площадке
  и марш к точке, рамка площадки НЕ рисуется (заказ 10.09.2026).
- **Золотой рудник — `qa_gold_mine`** (24 проверки, headless): два ничейных
  рудника на плато, захват пехотой за CAPTURE_SEC, флажок цвета стороны,
  доход 10 + 1 за рабочего, рабочие внутри (WORKER_CAP 5, скрыты как
  гарнизон), смена владельца перекрашивает рабочих, чужие защитники блокируют
  захват, руина Destroyed и восстановление рабочим любой стороны.
- **Старт партии, застройка по туману, башня, тролль, овца —
  `qa_start_build`** (30, headless): крепости на старте нет и пять рабочих
  есть, старт сдвинут от угла, зелёной плашки зоны и зелёной рамки края карты
  нет, туман открыт только вокруг бригады; строить можно там, где нет тумана,
  и нельзя в тумане; крепость даёт +10 рабочих и +5 отрядов и расширяет обзор;
  башня прочная и дорогая, на настиле до десяти лучников, стрела бьёт лучника
  (труп у подножия, резервист встаёт), копьё — только стены; у тролля нет тени,
  кольцо овальное, прицел в туше, стрелы торчат, окружение пробивается, туша
  падает под углом; овца — пять ударов ножом до смерти, 300 еды за 20 ходок,
  размножение (числа развёрнуты спринтом 13, см. его раздел).
- **Тактический разум гоблинов — `qa_goblin_tactics`** (25, headless):
  штурм базы (сбор на линии в `ASSAULT_STANDOFF` от края базы без боя,
  «провокатор» — щуп на фланг и отход в режиме отхода, укол конницы и отход,
  смена вектора, общий навал; отход всей армией при запасе ниже
  `ASSAULT_RETREAT_HP` в лагерь вне башен, лечение у костров, новая волна;
  веер и откатные волны), марш в центр с конным дозором впереди тела и
  штурмом точки контакта, оборона деревни (рубеж пехоты у костров без
  выбега, фланговые уколы конницы с отходом, раненый отряд у костров).
- **Агро-цепочка логова и скорость тролля в бою — `qa_troll_aggro`** (15,
  headless): ×1.2 к шагу с целью и назад без неё, первый удар по стражу →
  два помощника с приказом на отряд обидчика, повторные удары и порог трети
  у первой группы никого не зовут, выбитая группа → бонусная пара, у неё
  прежняя подмога на трети и зачистка после гибели.
- **Декорации, гноллы, загон для овец, спецприёмы — `qa_sprint13`** (80,
  headless): Deco 01-18 одним бакетом на файл плюс указатели вдоль маршрута и
  пугала у зон; гнолл быстрее и слабее орды, кость летит СВОИМ слоем и своим
  пулом, промахи есть, кайт от подошедшей пехоты, стая 3 отряда у пня, волна
  по удару и откат после третьей; рудника в меню рабочего нет, загон есть,
  «Вместимость: X / 20 овец», потолок у замка 5 и период вдвое дольше; пять
  ударов до смерти, туша на боку без мигания, 300 еды за 20 ходок; четвёртый
  слот кузницы даром и включён сразу; «Яростный Набег» пятёркой ударов и
  «Натиск Фаланги» двумя фазами; мечник в два слота лимита; монахов три.
- **Живые иконки доступности, цена иконками, реплики через раз, шаги −30 % —
  `qa_hud_afford`** (13, headless): кнопки найма перекрашиваются по сигналу
  склада без перевыделения, реестр чистится со сменой панели, узел кузницы
  без денег — красная рамка по сигналу, плашка узла без буквенных цен,
  «держать строй» true/false/true, окна ≥ 9 с, марш −14.1/−12.2 дБ в бюджете.
- **Строитель, кража овцы, мясо, лист камня — `qa_worker_sheep`** (15,
  headless): молоток только после прихода к площадке, рабочий берёт овцу
  (над головой, лента «несёт мясо»), несёт к складу, режет ножом пять раз с
  миганием, мясо в склад, загон в приоритете, отмена приказом, лист
  Pawn_Run_Stone чёрного рабочего 1152×192 на шесть кадров без золота.
- **Содержание армии едой — `qa_food_economy`** (16, headless): тождество
  «десять домов = 30 рабочих + 15 отрядов» из констант, расход за 5 с,
  «−N» в панели, голод (мораль вне боя тает до пола 40 % и не ниже),
  кузница вдвое дольше бараков, bonus_build удваивает темп площадки.
- **Овцы у логова и голод тролля — `qa_sheep`** (17 проверок, headless):
  ленты из resources/sheep, стадо 5 → +2 раз в 5 мин до 10, перебежки раз в
  10 с на 5-10 м без выхода за радиус выпаса, тролль подходит и ест (овца
  исчезает, +20 % запаса, звук `nom_nom`).
- **Ворота, гарнизон и клик по зданию — `qa_keep`** (37 проверок, headless):
  узел `SpawnPoint` у дверей и покрытие зоны клика у ЗАМКА, ХИЖИНЫ и БАРАКОВ,
  вход в гарнизон у ворот, снятие колец и полосок с вошедшего, приоритет клика
  «здание против бойца за ним» и полосы выхода. Разбор — в разделе «Ворота
  крепости, гарнизон и клик по зданию».
- **Баланс — `qa_balance`** (20 проверок, headless): формула брони, дуэли
  моделей, схватка двух отрядов 1 на 1, мораль и паника, легендарные перки,
  темп экономики. Расчёт — в `docs/BALANCE_MATH.md`.
- **A/B визуального прохода — `qa_visab`** (headless): 2000 своих маршем
  туда-обратно + 2000 чужих, ВСЯ армия игрока выделена (2000 колец и теней),
  полоски по Alt (4856); чередованием меряет КАДР ЛОГИКИ (`vis_meter`) по
  ручкам `anim_core` / `decal_core`. Замер 09.09.2026: кольца и полоски в
  ядре — **79.2 → 7.8 мс кадра логики (−71 мс)**; кадр ленты в ядре — −0.2 мс
  на марше (выигрыш сна проявляется на стоящей армии).
- **Честный A/B оптимизаций — `qa_ab`** (headless): поднимает живую сцену из
  1440 копейщиков и снимает базу и правку ПОДРЯД по `tick_meter`, раундами.
  Вердиктов не печатает — это измеритель, а не проверка, и в шлюз он не
  входит. Ручку меряемой правки берёт из `perf_config`. Четыре блока: стойка
  ЗАЩИТА в виду врага, сплошная рубка, пакетный бой в рубке и пакетный бой на
  СБЛИЖЕНИИ. **Порядок фаз внутри раунда переворачивается** — иначе весь дрейф
  живой сцены систематически достаётся правке; в блоках рубки бойцы
  БЕССМЕРТНЫ, а в блоке сближения сцена возвращается в исходную точку перед
  каждой фазой, чтобы обе мерились с одной дистанции.
- **НАСТОЯЩАЯ ПАРТИЯ НА ЧЕТЫРЁХ ТЫСЯЧАХ — `qa_full_game_4k` (4 проверки,
  headless годится для инвариантов, окно — для FPS).** Заведён 03.09.2026
  после того, как синтетические стенды были зелёными, а на реальном билде
  стрелы висели в воздухе пачками, мечники протекали сквозь фалангу и кадр
  падал. Две смешанные армии по 2000 ДВУМЯ ФРОНТАМИ (копейщики в первом
  эшелоне, лучники в тылу — простой `sort()` ставил «Archer» первым и делал
  из рубки перестрелку), у каждой стороны замок игрока/ИИ на своём якоре,
  `WORKER_LIMIT` рабочих с доставкой на склад и три стройплощадки; ИИ, орда,
  туман, HUD на месте. Три фазы: экономика без боя, рубка с потерями
  (снимки раз в 5 ИГРОВЫХ секунд: стрелы в воздухе / буфер / реестр,
  ВЗАИМОПРОНИКНОВЕНИЕ живых тел, экономика), цена подсистем на замороженной
  численности. **Расписание стенда — В ФИЗКАДРАХ (правило 11):** первая
  версия ждала кадры отрисовки, и «30 с рубки» в headless при 137 FPS
  укладывались в две секунды игры — 0 погибших, 0 стрел, «сдано +0»; зонд
  честно обвинял экономику. **B1 (внутри ядра чужого тела < 1 %) судит
  только снимки ЛИНЕЙНОГО боя** (в контакте не меньше `LINE_CONTACT_MIN`):
  в остатках боя конница добивает разрозненных, а толчки и разлёт пишут
  координаты без проверки тел — доля внутри ядра среди последних десятков
  контактных доходит до трети, и ни у одного из них нет билета прохода
  (окно `enemy_overlap_count_flagged` считает по битам). **Источник найден и
  закрыт (Часть 3, 03.09.2026):** разлёт от тарана проверял чужие тела
  только у всадника (`_fling_solid`), сбитый пехотинец летел 2.2 м без
  проверки и в окружении влетал внутрь второго врага; толчок удара
  (`_apply_push`) вдавливал жертву в чужое тело без проверки вовсе. Оба
  теперь идут через ту же проверку `enemy_block(BLOCK_RADIUS)`, что и
  пакетный шаг: **движений мимо проверки тел в проекте больше нет.** Запуск:
  `<godot> --path . res://qa_full_game_4k/Test.tscn -- player=N ai=N clash=СЕК`.
- **Профиль партии на трёх тысячах — `qa_mass3k` (ОКОННЫЙ).** Три армии
  смешанного состава сходятся в одну кучу, у обеих сторон рабочие на добыче,
  туман/HUD/растительность/деревня на месте. Печатает разбор кадра, цену
  подсистем и A/B чередованием по настройкам. **ФАЗА 3 (сент. 2026) — реальные
  потери:** смертность возвращается, волна заходит с края, кадр меряется при
  ~100 смертях/с с живым ветеранством и укладкой тел. Замер на 1500+800+1000:
  гибель/опыт/трупы стоят +0.7…+1.7 мс кадра — НЕ убийца; убийца — сам тик
  свалки (`process_attack` ~25% тика) плюс стабильные 2.3-2.5 мс мышления ИИ
  на кадр. Разбор — в `docs/ENGINEERING_LOG.md`, раздел «Сентябрь 2026».
- Массовые замеры: `qa_mass_perf`, `qa_mass_battle`, `qa_fx`. Headless-стенды
  молчат до конца и печатают одну таблицу — так обязан вести себя любой новый.
- **Цена подсистем в кадре — `qa_fps` (оконный).** Собирает сцену КАК В ПАРТИИ
  (ИИ, туман, деревня, HUD, растительность) и гасит подсистемы ПО ОДНОЙ,
  возвращая обратно: цена считается против среднего двух базовых замеров
  вокруг. Лестницей «гасим и не возвращаем» мерить нельзя — мир между
  ступенями живёт, и разность соседних строк меряет его жизнь, а не
  подсистему (первая версия так «намеряла», что выключение тумана отнимает
  17 кадров). Считать надо МИЛЛИСЕКУНДЫ: кадры не складываются.

## Архитектура

**Автозагрузки:** `ResourceManager` (банк по фракциям) и `GameManager` (отряды,
улучшения, размещение построек, `unit_grid` / `far_units` / `sel_decals` и **весь
тик армии**).

**У юнита нет физтела и своего тика.** `Unit extends Node3D`. Точка входа —
`Unit.tick_physics()` из `GameManager._physics_process` по плоскому массиву
`_live_units`, и `Unit.tick_visual()` из `_process`. Оба прохода **шардированы**
по кадрам (`perf_config.shards_for`, визуальный — на шард больше от 2000 бойцов).
`collision_mask` везде 0, столкновений нет; движение — прямое интегрирование в
`position`.

**Иерархия:** `Unit` → `Worker`/`Spearman`/`Archer`/`Warrior`/`Monk`/гоблины;
`Building` → `Castle`/`Barracks`/`Smithy`/`Mine`/`House`/`GoblinHut`;
`ResourceNode`, `MineCluster`. Сцены только `MainMenu.tscn` и `Main.tscn` —
остальное строится кодом.

**Ядро армии (`scripts/army/ArmySoA.gd` → `csharp/ArmyCore.cs`).** Колонки, плоская
сетка соседей, реестр стволов, пакетный шаг и пакетное расталкивание живут в C#.
`ArmySoA` — фасад, владелец массивов — солвер. **Ячейка сетки держит по списку
на ФРАКЦИЮ** (`ArmyCore.Factions`), а не «свои/чужие»: раньше слот считался как
`fac == 0 ? 0 : 1`, и третья сторона молча склеилась бы со второй.

**ПОКАДРОВЫЕ ПРОХОДЫ ИДУТ ПО ПЛОТНОМУ СПИСКУ ЖИВЫХ СТРОК, И ОН ОТСОРТИРОВАН**
(`ArmyCore._liveRows`, сент. 2026 — пункт roadmap закрыт). Ёмкость только
растёт (массивы под ПИК армии, назад не сжимаются), прежняя граница `_top`
упиралась в самого верхнего живого и при рассеянных потерях не опускалась
(замер `qa_ab/Probe`: выбили нижние 1900 из 2000 — вхолостую 1900 строк).
Теперь `RecomputeBounds`, `FillGrid`, `BatchSeparation` и `BatchCombat` идут
по списку живых. **ОТСОРТИРОВАННОСТЬ — НЕ ПРИХОТЬ, А СОХРАНЕНИЕ ПОВЕДЕНИЯ:**
`FillGrid` строит списки ячеек вставкой в голову, порядок обхода строк задаёт
порядок СОСЕДЕЙ, а по нему разрешаются ничьи в `EnemyAt`/`BestEnemy`/
`NearestOfSide`. Плотный список со свапом перестал бы быть отсортированным
после первой смерти и «поехал» бы выбором цели на десятках стендов; сдвиг с
двоичным поиском (сотни событий в секунду — микросекунды) даёт В ТОЧНОСТИ
прежний порядок. `_top`/`Top()` оставлены — их читают стенды. Признак
занятости по-прежнему ОТДЕЛЬНЫЙ массив, а не бит в `_flags`: флаги
переписывают и пакетная запись поз, и `SetFlag` из GDScript.

**Фракций три** (`Constants.FACTION_*`, `FACTION_COUNT`): игрок, красный ИИ и
**гоблины** — третья сторона, враждебная всем и не участвующая в условии победы.
Группы узлов раздаёт `Constants.unit_group()` / `building_group()`; «все чужие
здания» — `Constants.enemy_building_groups()`.

## Правила, нарушение которых стоило дорого

1. **Границу GDScript↔C# пересекать пачками или один раз, никогда в цикле по
   бойцам** (переход ≈ 1.5–2.5 мкс). Шаги и позы идут очередью, одним вызовом.
2. **`global_position` — свойство, а не поле.** Под неподвижным `World` локальная
   `position` даёт то же число даром (проверяет `Unit._local_xform`).
3. **Всё, что одинаково для всей армии, читать раз в кадр и передавать
   аргументом.** Восемь таких чтений в `tick_visual` стоили больше миллисекунды.
4. **Ни `String`, ни `Array` в покадровом пути.** Секторы, ключи и роды поз —
   целые числа и готовые таблицы.
5. **`is_instance_valid` проверять на СЫРОЙ ссылке, до приведения типа:**
   `x as Unit` и типизированное присваивание на освобождённом объекте не
   возвращают null, а бросают исключение.
6. **`remove_child()` перед `queue_free()`**, если детей контейнера считают в том
   же кадре: освобождение отложено.
7. **Союзники друг друга не толкают**: расталкивание монотонно и только наружу,
   сил избегания добавлять нельзя — это уже давало «дрожат и застревают».
8. **Никаких `DirAccess`-переборов каталогов ради ассетов** — в экспорте имена
   другие; проверять `SpriteSheetParser.folder_has()`. PCK регистрозависим.
9. **`var x := arr[i]` не выводит тип** (Variant) — писать `var x: String = arr[i]`.
10. **Стенды не хардкодят числа из конфига** — проверять СВОЙСТВО: большинство
    «красных» стендов краснело от разворота требования, а не от поломки кода.
11. **Стенд, который ждёт, пока кто-то куда-то дойдёт, ждёт `physics_frame`,**
    а не `process_frame`: при `Engine.max_fps = 0` отрисовка обгоняет физику.
12. **Абсолютное время в стендах врёт на загруженной машине.** Перед замером
    проверять `Get-CimInstance Win32_Process -Filter "Name LIKE '%Godot%'"`.
13. **УМОЛЧАНИЕ У `Dictionary.get(k, X)` ВЫЧИСЛЯЕТСЯ ВСЕГДА, ДАЖЕ ПРИ
    ПОПАДАНИИ В КЛЮЧ.** `d.get(sid, {})` и `d.get(cat, [])` создают НОВЫЙ
    контейнер на каждый вызов — это правило 4, спрятанное за удобным
    синтаксисом. В покадровом пути писать `var v: Variant = d.get(k)` и
    проверять на `null`. Замер: 0.140 против 0.078 мкс на вызов. Ловушка была
    в `squad_target_get`, `squad_course`, `squad_has_formation` (зовутся из
    хода фаланги и авто-агро) и в `AudioManager.play_3d` (звук в свалке —
    сотни вызовов в секунду).
14. **ОТВЕТ ПАРОЙ `[значение, нашли ли]` — ТОЖЕ АЛЛОКАЦИЯ.** 0.157 мкс против
    0.021 у ответа значением. Признак «не нашли» выражается самим значением
    (`Vector3.INF`, `Vector2.INF`) — так уже сделано у `squad_centre_xz` и
    `ArmyCore.PosOr`. Разобрано на `squad_enemy_point` против прежнего
    `squad_enemy_pos`.

## Отряды и бой (главное)

- **Приоритет приказов:** замок цели игрока (ПКМ) → заслон, в который упёрлись
  телом → авто-агро. Замок снимают только приказ на движение, отход и полное
  истребление вражеского отряда.
- **Замок читается и в ПОКОЕ** (`_check_auto_aggro`, первая ветка, до стойки).
  Без этого лучники подменяли приказ молча: стрелок не преследует
  (`Archer.pursues_target` = false), назначенная цель отходит за дальность,
  `_process_attack` бросает её и переводит бойца в покой — а покой обслуживает
  авто-агро, и оно выдавало ближайшего, то есть чужих копейщиков. Пока цель в
  пределах `_lock_sight_range()` (вдвое дальше оружия), другую брать нельзя;
  дальше — приказ исчерпан и замок снимается сам. Стенд `qa_target_lock`, блок F.
- **«Ударили вплотную» под замком — это длина руки, а не дальность оружия.**
  Ответ на удар в `take_damage` перебивает приказ игрока; у лучника
  `attack_range` = 20 м, и порог брался оттуда. Под замком порог тот же, что у
  перехвата заслона (`LOCK_BLOCKER_RANGE`).
- **Центр отряда — МЕДИАНА по осям** (`GameManager._centroid_of`): среднее у
  расколотого отряда садится в пустое поле между кучами, и туда уезжали и
  звезда, и точка залпа, и цель ИИ.
- **Сплочённость** (`_cohesion_guard`): оторвавшийся и ничем не занятый боец
  получает разовый приказ вернуться, с остыванием. Приказ, а не сила.
- **ФАЛАНГА ХОДИТ СТРОЕМ ПО ПРИКАЗУ, НО НЕ ХОДИТ ПООДИНОЧКЕ.** Жалоба была
  одна — «крайние копейщики выбегают из строя», — а решений понадобилось два,
  и первое оказалось неверным. Глухой замок позиции
  (`STANCES.defense.lock_position = true`) жалобу закрыл и тут же создал
  другую: фаланга перестала ходить по приказу игрока. Ручка осталась, но стоит
  в `false`.
- **НАСТОЯЩАЯ ПРИЧИНА ВЫБЕГА — ТОЧКА, А НЕ РАЗРЕШЕНИЕ ДВИГАТЬСЯ.** Приказ атаки
  раздаётся каждому бойцу отдельно, и каждый получал ОДНУ И ТУ ЖЕ цель —
  координату вражеской модели. Шеренга честно шла в одну точку, то есть
  схлопывалась; первыми это видно на флангах, им идти дальше всех. Теперь стена
  ПЕРЕНОСИТСЯ ЦЕЛИКОМ: `Unit._wall_goal` смещает точку каждого на тот же
  вектор, на который сдвигается центр отряда, и взаимное расположение не
  меняется вовсе (замер `qa_bugpass` L2: ширина строя 6.30 → 6.30 м).
- **ИНДИВИДУАЛЬНОЙ ИНИЦИАТИВЫ У ФАЛАНГИ НЕТ ВОВСЕ.** Всё, что ниже хода стены в
  `_process_attack`, — способы сблизиться самому (подтянуть ряд, доступить по
  сцепке, обойти свалку, догнать), и копейщик в обороне не вправе ни на один.
  **ПОРЯДОК ВОРОТ ВАЖЕН:** запрет стоит ПОСЛЕ `_phalanx_march`, а не до него —
  поставь его выше, и фаланга не пойдёт вообще (ровно это и вышло при попытке
  закрыть выбег глухим замком).
- **Смыкание рядов работает при любом положении ручки замка**
  (`Unit.allow_reform_move`, выдаёт `SquadFormation.close_ranks`): без него
  потери и проход союзников сквозь строй оставляли бы в фаланге вечные дыры.
  Разрешение выдаётся на секунды, а не «пока не дойдёт»: дойти боец может и
  никогда.
- **Авто-агро в обороне** берёт только то, что **перед фронтом строя**
  (`_in_phalanx_front`) и в пределах оружия.
- **Сцепка `_melee_grip`:** заслон, взятый по физическому контакту, не
  сбрасывается на границе дальности — боец доступает до него. Без этого он
  бесконечно бегал челноком у чужого строя и в рубку не входил.
- **Отряд «в бою»** (`squad_in_combat`) блокирует смыкание рядов.
- **ДРЁМА ПЕРЕЗАРЯДКИ ЖИВЁТ В ЯДРЕ** (`Unit._atk_snooze` + `ArmyCore.TickSnooze`,
  сент. 2026, этап C): боец, стоящий В УПОР к цели и ждущий остывания удара, в
  GDScript-автомат боя не входит вовсе (гейт в диспетчере `tick_physics`);
  таймер и стражу цели (жива, в пределах `reach` с люфтом
  `ATK_SNOOZE_REACH_SLACK` — без люфта качание свалки будило весь фронт и
  съедало весь выигрыш) ведёт C#-проход. Пробуждение — СОБЫТИЕ: за
  `ATK_SNOOZE_SPARE` до готовности, по гибели/уходу цели; дрёму снимает смена
  цели, остаток кулдауна возвращается в `_attack_timer` (на время дрёмы поле
  заморожено — `_sweep_volleys` видит честное «не готов»). Лучник в залпе не
  дремлет по построению. **Честная цена по A/B чередованием (qa_mass3k, ручка
  `perf_config.atk_snooze`): +0.47 мс тика.** Это ТРЕТИЙ замер закона «смена
  языка окупается только у пакетной работы»: однородное ожидание — отдаёт,
  разнородные решения боя — нет (первые два — в шапке
  `perf_config.batch_combat`). Межпрогонные сравнения тут врут: фаза без
  дрёмы уплывала на 7 FPS между прогонами одного кода — только чередование.
- **СКВОЗЬ ЧУЖОЙ СТРОЙ НЕ ПРОХОДИТ НИКТО** — ни бег, ни отход. У отхода
  (`retreating`) такое послабление БЫЛО, и оно же оказалось жалобой владельца
  «красные рыцари пробегают сквозь фалангу, как призраки»: отряд ИИ, решивший
  уйти в замок, шёл домой по прямой и проходил строй насквозь. Замер
  `qa_mega_battle` B2-3: **141 отходящий рыцарь из 151**, при том что обычным
  приказом сквозь строй не проходил НИ ОДИН (B2-1: 0 из 495) — дыра была ровно
  одна. Убрано в ОБОИХ местах сразу: `csharp/ArmyCore.cs` (горячий путь,
  `FRetreating` снят из маски прохода) и `Unit._move_blocked` (запасная дорога);
  правило обязано совпадать в обеих, иначе его просто нет. Вечного трения о
  чужую шеренгу это не создаёт: отряд в контакте не отступает вовсе
  (`AICfg.AI_NO_RETREAT_IN_MELEE`), а своих ему по-прежнему раздвигает
  `SEP_PASS_RELIEF`. Проход остался у ДВУХ признаков: `FClearEnemy` (коридор
  отряда) и `FOrderPass` — заработанный билет игрока на выход из свалки.
  Бег (`sprinting`) сквозь тела не проходил и раньше, он только не вступает в бой.
- **БИЛЕТ ОТХОДА ВЕРНУЛСЯ, НО КАК БИЛЕТ НА ВЫХОД** (`Unit._retreat_pass`,
  сент. 2026). Снятие прохода у отхода ЦЕЛИКОМ тоже оказалось замерено:
  `qa_spear` C9 «внутрь замка зашли 3 из 6» — половина отряда, отозванного в
  гарнизон, застревала о противника на месте боя. Мирит их тот же приём, что
  у приказа игрока: пока боец не отошёл от точки начала отхода дальше
  `PASS_CLEAR_DIST` (2.5 м), он получает ТОТ ЖЕ бит `F_ORDER_PASS` — смысл у
  обоих один, «выбраться из тел, в которых уже стою», и второго признака не
  заведено.
- **БИЛЕТ ПРОХОДА СУЖАЕТ ТЕЛО ДО ЯДРА, А НЕ СНИМАЕТ ЕГО**
  (`ArmyCore.PassCoreFrac` = `Unit.PASS_CORE_FRAC` = 0.55, то есть ядро
  0.30 м при `BLOCK_RADIUS` 0.55). Любой билет раньше снимал проверку чужих
  тел ЦЕЛИКОМ, а 2.5 м «выхода» — это больше глубины фаланги в контакте
  (`qa_clash`: четыре шеренги сжимаются до 1.2 м): мечник, получивший приказ
  в упор к строю, проходил его насквозь. С ядром сквозь щель между двумя
  телами шеренги (0.17-0.28 м до ближайшего центра) не пройти ни с каким
  билетом. **ЦЕНА НАЗВАНА ЧИСЛОМ:** пройти между двумя телами можно только
  в просвет шире ДВУХ ядер (0.60 м), а самый редкий строй в игре стоит по
  `squad_spacing` 0.55 — то есть с ядром 0.30 билет не проводит НИ СКВОЗЬ
  ОДИН строй, включая кольцо окружения. Это выбор в пользу «сквозь строй не
  проходит никто». **РЕШЕНИЕ ВЛАДЕЛЬЦА (03.09.2026): ЯДРО 0.55 ЗАФИКСИРОВАНО,
  ФАЛАНГА МОНОЛИТНА, ВЫХОД ИЗ ПЛОТНОГО ОКРУЖЕНИЯ СКВОЗЬ ТЕЛА НЕ ТРЕБУЕТСЯ.**
  Ядро 0.25 открыло бы стоящий строй (просвет 0.55), ядро 0.17 — сжатый в
  бою (0.35): промежуточного значения, которое разделяет «кольцо» и
  «шеренгу», не существует — геометрически это одна щель. `qa_disengage` D2
  переписан под решение и стережёт обе его стороны: D2a — сквозь кольцо с
  просветом 0.29 м (теснее боевой шеренги) боец НЕ выходит, D2b — из кольца
  с просветом шире двух ядер выходит с первого приказа, то есть билет
  работает там, где ему положено.
- **ПОД БИЛЕТОМ ТЕЛО ЗАПРЕЩАЕТ СБЛИЖЕНИЕ, А НЕ ПРЕБЫВАНИЕ; БЕЗ БИЛЕТА — КАК
  БЫЛО** (`ScanBlock` / `EnemyBlock`, аргумент `awayOk`, 03.09.2026).
  Блокируется любой шаг, чья новая точка лежит внутри радиуса тела, — и боец,
  уже оказавшийся внутри, не может шагнуть ни вдоль, ни прочь. Под билетом
  это гибельно: ядро 0.30 меньше игрового просвета 0.33, внутри него боец
  бывает законно. Поэтому под билетом шаг, УВЕЛИЧИВАЮЩИЙ дистанцию до тела,
  проходит (в обеих дорогах и в разведении своими — оно тоже зовёт
  `ScanBlock` и вправе вытолкнуть бойца с билетом прочь от чужого тела).
  **ДЛЯ ВСЕХ это правило пробовали и откатили:** `qa_crowd` A1/A3 — толпа из
  24 своих у спящей орды переставала замирать (дрожание 0.0000 → 0.0647 м),
  прижатый своими к чужому телу полз вдоль него вместо того, чтобы встать.
- **ПОД БИЛЕТОМ БОЕЦ СКОЛЬЗИТ ВДОЛЬ ЧУЖОГО ТЕЛА, БЕЗ БИЛЕТА — ВСТАЁТ.** Нормаль
  от тела смотрит НАРУЖУ, а ветка скольжения идущего снимает составляющую при
  `into > 0`, то есть наружную; шаг внутрь остаётся, второй проход его
  блокирует, и пехотинец у чужого тела стоит. Для обычного бойца это и есть
  тюнингованное «упёрся — дерись» (qa_wall C1, «щель 0.1 м»), и трогать его
  нельзя. Билет прохода (`FOrderPass`: выход по приказу и билет отхода)
  заведён ровно на то, чтобы ВЫБРАТЬСЯ, и под ним снимается ВНУТРЕННЯЯ
  составляющая. **И ЧУТЬ НАРУЖУ** (`PassSlideOut` = `Unit.PASS_SLIDE_OUT` = четверть шага
  вдоль нормали): нормаль считана в НОВОЙ точке прямого шага, а она ближе к
  телу, чем текущая, и «касательная» через неё проходит на миллиметры БЛИЖЕ
  к телу, чем стоит боец (0.302 против 0.3035 м) — правило «прочь» её
  честно отвергало, и боец стоял 12 с. Ловушка зонда: касательная, взятая по
  нормали в ТЕКУЩЕЙ точке, отвечает «свободно», и опыт «прямой запрос
  `enemy_block`» показывал зелёное там, где ядро отказывало каждый кадр.
  Замер `qa_spear` C9 (отзыв в гарнизон мимо одного врага в 0.30-0.32 м):
  без правок 5 из 6, с правилом «прочь» и скольжением без сдвига наружу —
  по-прежнему 5 из 6, со сдвигом — 6 из 6, при ядре 0.55. Правило в ОБЕИХ
  дорогах (`BatchMoveRows` и `Unit._move_blocked`). То же
  ядро стережёт бегущего: его скольжение по касательной шло БЕЗ второго
  прохода и в рваной шеренге заводило внутрь тел. **A/B на `qa_full_game_4k`
  (400 на 400, одна сцена, одно зерно): ядро 0 → 148 живых внутри 0.30 м от
  чужого тела в миг контакта (8.9 %), ядро 0.55 → 9 (0.5 %).** Число ОБЯЗАНО
  совпадать в обеих дорогах (пакетный шаг и `_move_blocked`): 03.09.2026
  ядро в C# было найдено в нуле при 0.55 в GDScript — это и была база A/B,
  не возвращённая на место. Стережёт `qa_full_game_4k` B1.
- **Залп лучников** (`forge_config` archer_1d): точка пересчитывается **в момент
  выстрела** (`squad_volley_point`), радиус тучи не ниже `VOLLEY_MIN_SPREAD` —
  иначе залп схлопывался в точку и добивал уже мёртвого.
- **У дальности ЖЁСТКИЙ ПОТОЛОК** (`STATS.archer.attack_range_cap`). Она растёт
  от кузницы и вписывается прямо в поле бойца при рождении — потолка не было
  вовсе, и на прокачанной кузнице лучник доставал через пол-экрана. Потолок
  ограничивает СУММУ, а не прибавку, и держится в ДВУХ местах: при рождении и
  при выдаче исследования уже стоящим на карте
  (`GameManager._apply_range_bonus_now`) — второй путь и уводил дальность за
  все пределы.
- **СТРЕЛОК НЕ ИДЁТ В РУКОПАШНУЮ ПО КЛИКУ** (`Unit._prefer_in_range`). Приказ
  атаки назначается по ОТРЯДУ, а жертву выбирает `squad_pick_member` —
  «наименее обстрелянный», то есть чаще всего НЕ ближайший. Стрелку доставалась
  модель с дальнего края чужого строя, и он шёл к ней прямо сквозь тех, кто
  стоял в двух метрах. Теперь тому, кто НЕ ПРЕСЛЕДУЕТ, назначенная цель
  подменяется на бойца ТОГО ЖЕ отряда, до которого уже дострелить. Пехоты это
  не касается вовсе: `pursues_target()` у неё true, и первая же строка
  возвращает вход.
- **Упреждение** — `ARCHER_LEAD_FACTOR` с потолком `ARCHER_LEAD_MAX`; промахи по
  бегущему даёт разброс, а не вынос. Кучность и темп зависят от `ARCHER_DRILL`.
- **Смена состояния FSM = срочный пересчёт позы** (`_wake_process`): без этого
  рабочий шёл к замку, махая топором.
- **Привязка спрайта к земле считается на каждую ленту** (`_anim_bottom_px`):
  кольцо выделения лежит на земле, и ноги обязаны быть там же при любой позе.
- **Фаза зацикленной анимации у каждого бойца своя** (`Unit._anim_offset`, от
  номера узла): иначе сотня бойцов дышит в один такт. Незацикленные ленты
  (удары) всегда начинаются с нулевого кадра.
- **БУФЕРЫ MultiMesh АРМИИ И ПОКАДРОВЫЙ ДОГОН КАРТИНКИ ЖИВУТ В ЯДРЕ**
  (`ArmyCore.Rb`/`BatchVisual`/`RowBind`, сент. 2026, этап C.1). GDScript
  (FarUnitRenderer) владеет жизненным циклом бакетов и СОБЫТИЙНЫМИ записями
  (смена ленты, кадр походки, урон, скрытие — теперь делегаты в ядро);
  сглаживание нарисованной точки, боббинг и запись позиций всех привязанных
  строк ведёт один C#-проход каждый кадр отрисовки, подача —
  `MultimeshSetBuffer` из ядра. Разлёт от тарана (`F_VIS_SELF` = 1 << 19)
  ведёт картинку сам; `draw_position()` привязанной строки читает ядро.
  Замер ЧЕРЕДОВАНИЕМ (ручка `perf_config.vis_core_path`): кадр логики
  −0.76 мс. Стенды читают буфер через `rb_slot`/`rb_dirty` (qa_wound).
- **КАДР ЛЕНТЫ ЛИСТАЕТ ЯДРО** (`ArmyCore.RowAnim` ← `Unit._push_row_anim`
  из `_set_anim` и `Spearman._apply_dir_tex`, ручка `perf_config.anim_core`,
  этап E1, 09.09.2026). Параметры ленты (кадров, к/с, зацикленность, фаза)
  уходят в строку ПО СОБЫТИЮ и БЕЗУСЛОВНО — ручку переключают на живой сцене,
  и колонки обязаны быть свежими; номер кадра ведёт `BatchVisual` и пишет в
  слот только на смену, ДО ветки разлёта (летящий тоже листает). GDScript не
  зовёт `_advance_look_frame`/`set_frame` у привязанной строки. При переезде
  между бакетами `RowBind` ставит кадр из своей фазы: GDScript-копия
  `_look_frame` у ведомой ядром ленты не листается. **Стоящий с зацикленным
  покоем теперь СПИТ по картинке** (`_process_can_sleep` и переопределение у
  копейщика): дышит за него ядро — снято прежнее «многокадровый покой не
  усыпляется».
- **КОЛЬЦА, ТЕНИ И ПОЛОСКИ ЗДОРОВЬЯ ВЕДЁТ ЯДРО** (`ArmyCore.DecalBind` /
  `HpBind`, ручка `perf_config.decal_core`, этап E2). Слои
  `SelectionDecalRenderer` и `HpBarRenderer` держат буферы в тех же Rb, что
  бакеты бойцов (`RbCreate`, `use_colors = true`, раскладка 16 float;
  материалы колец instance-цвет не читают), полный трансформ пишется по
  событию (`RbWriteXform`), позицию и долю жизни ведёт `BatchVisual` из
  нарисованной точки строки, подача — `RbFlush`. GDScript-обход `update_all`
  остался ЗАПАСНОЙ дорогой для бойца без слота в общей отрисовке (туман,
  гарнизон). Прежний путь — `draw_position()` (переход границы) плюс 24
  float в `PackedFloat32Array` на каждого выделенного в каждом кадре — на
  2000 выделенных и 4856 полосках стоил **71 мс кадра логики** (`qa_visab`).
  Отвязка — по событию снятия слота на СЫРОЙ ссылке (правило 5), `Release`
  строки чистит привязки сам.
- **ПОЗА ПЕРЕСЧИТЫВАЕТСЯ ПО СОБЫТИЮ, РАСПИСАНИЕ — СТРАХОВКА** (этап E3,
  ручка `perf_config.pose_events`, `pose_safety_mult` 6). Полный разбор
  (`_update_sprite_anim` + `_update_sprite_flip` + переезд между бакетами)
  взводят: смена состояния, живого ряда, взгляда или направления хода больше
  чем на 20° (`POSE_TURN_COS2`, скалярное произведение без sqrt), истечение
  замка удара (`_lock_pending`), поворот камеры (`GameManager._cam_epoch`,
  аргумент `cam_epoch` в `tick_visual` — и в переопределении у мечника),
  возврат в кадр (`seen_now`), смена цели (`set_attack_target`). Замер
  `qa_visab` D на марше: −0.07 мс кадра логики — то есть периодический
  разбор и раньше стоил мало (vis_pose 0.5 мс/кадр на 4000); ценность правки
  — в том, что поза больше не запаздывает на такт (до полусекунды на большой
  армии), а не в миллисекундах.
- **ПОЛЁТ СТРЕЛЫ ВЕДЁТ ЯДРО — ХАК ФИЗТИКА №1, «ДЕКЛАРАТИВНЫЕ СТРЕЛЫ»**
  (`ArmyCore.ArrowLaunch` / `BatchArrows` / `TakeArrowEvents`, ручка
  `perf_config.arrow_core`). На время полёта стрела — ЗАПИСЬ (старт, конец,
  высота дуги, темп), узел без `_process`; ядро одним проходом двигает все
  полёты, пишет позицию и ось в слот общего MultiMesh и ищет попадание ТЕМ
  ЖЕ `EnemyAt` по XZ-радиусу `HIT_RADIUS` (правило сохранено дословно: стрела
  бьёт того, над кем пролетает в 0.35 м по земле, — высота не проверялась и
  раньше). Наружу — события пачкой раз в кадр (`ArrowRenderer.drain_events`
  → `Arrow.core_event`: удар, здание, втыкание — тот же код, что был в
  `_process`). Узел остаётся для втыкания, тела, растворения и пула; отмена
  полёта — в `_despawn`. Замер `qa_full_game_4k` (окно): цена стрел «логика
  полёта, узлы» 1.52 мс кадра → в шуме (−0.46), проход ядра `arrow_core`
  0.40 мс/кадр при 60-70 в полёте. A1-A3 (висящие, буфер, потолок) зелёные,
  `qa_volley` 23/23, `qa_corpse`, `qa_bldattack` зелёные.
- **ХАК №2 «ОТРЯД КАК СТЕНА-ОТРЕЗОК» НЕ ПОСТРОЕН — ЕГО ПОТОЛОК ИЗМЕРЕН
  И РАВЕН НУЛЮ** (`qa_ab` блоки F/F2, ручка `ArmyCore.SkipBodyScan`,
  09.09.2026). Проверка чужих тел на шаге (`ScanBlock`/`EnemyBlock`)
  выключалась ЦЕЛИКОМ — никакая замена её отрезками не выиграет больше,
  чем полное её отсутствие: рубка 3.12 → 3.04 мс тика (+0.09, 2.7 %, в
  шуме), сближение 3.80 → 3.95 (хуже: без тел строй уплотняется и дерётся
  гуще). Тик рубки держат решения боя в GDScript (`process_attack`,
  `atk_strike`, `atk_find_enemy` ≈ 5 мс кадра из 8.7), а не геометрия;
  ручка и блоки оставлены как измеритель.
- **ТИК АРМИИ НА ПОЛОВИННОЙ ЧАСТОТЕ ПОСТРОЕН И ВЫКЛЮЧЕН ПО ЗАМЕРУ**
  (`perf_config.army_tick_div` = 1, этап C.2). Глобальные 30 Гц движка сняты
  за один прогон шлюза (удвоенная дельта мелких сцен — qa_wall поймал
  протекание; часы стендов в физкадрах). Половинный тик армии с компенсацией
  шардов (личные 15 Гц бойца сохранены) измерен A/B чередованием ДВАЖДЫ:
  экономия ~5% физтика-за-секунду, кадр логики +2.2-2.7 мс, FPS −0.7…−1.3 —
  редкий тяжёлый тик ломает пейсинг. Ручка и A/B-блок qa_mass3k остаются.
  **ФАЗЫ ШАРДОВ СЧИТАЮТСЯ ОТ `GameManager.army_ticks`, НЕ ОТ НОМЕРА КАДРА
  ДВИЖКА:** при делителе и чётном числе шардов кадровая фаза замирала, и
  половина армии не тикала вовсе.
- **ПАКЕТНЫЕ ПРОХОДЫ ЯДРА МНОГОПОТОЧНЫ** (`ArmyCore.CoreThreads`, этап D2):
  фаза РАСЧЁТА разведения и BatchVisual считаются диапазонами строк через
  Parallel.For. Двухфазность разведения БЕЗУСЛОВНАЯ (расчёт читает снимок,
  применение — последовательное: вода, узлы), поэтому потоки не меняют
  результат ни на бит. Замер чередованием: +0.52 мс тика, +1.9 FPS. Потокам
  разрешена ТОЛЬКО чистая математика по колонкам — ни GDScript, ни дерева
  сцены, ни RenderingServer. Ручка `perf_config.core_threads`.
- **МАСКА ТУМАНА СЧИТАЕТСЯ В ЯДРЕ** (`ArmyCore.FogRefresh`, этап D3): штампы,
  «разведано» и сборка RGBA — C#; GDScript собирает источники и хранит копии
  lit/seen (is_lit бойца — без границы языков). +0.9 FPS. Ручка
  `perf_config.fog_core`.
- **КАРТИНКА ВСЕХ СТРЕЛ — ОДИН MultiMesh** (`ArrowRenderer` +
  `shaders/mm_arrow.gdshader`, этап D3): ось и дизер-растворение едут в
  instance-цвете, буфер — Rb-инфраструктура ядра. Узел Arrow остался логикой.
  36 торчащих = +1 вызов отрисовки (был +36). Порог среза — общебакетный
  uniform (гашение экземпляра до него не дотягивается — qa_corpse K1).
  **«СТРЕЛЫ ЗАВИСАЮТ ПАЧКАМИ В ВОЗДУХЕ» — СЛОТ ЗНАЕТ ТОЛЬКО ТО, ЧТО В НЕГО
  ЗАПИСАЛИ.** Пока стрела была узлом со своим спрайтом, перенос узла в точку
  втыкания переносил и картинку; в общем MultiMesh слот писала `_apply_axis`
  ЕЩЁ В КОНЦЕ ДУГИ, на высоте груди цели, а следующая запись с верной точкой
  шла из `_set_fade` через сорок секунд. Лечение — явный `_push_visual()` в
  `_stick_into_ground` после переноса. Стенд `qa_full_game_4k` A1 меряет
  подъём торчащей над грунтом по буферу (`rb_slot`): 0.20-0.23 м у всех,
  висящих ноль.
  Прежняя строка «стрела — это узел со своим вызовом отрисовки» ниже по файлу
  описывает ИСТОРИЮ и потолки (потолки в силе — узлы-то остались).
- **ТЫЛОВОЙ НАПОР СЧИТАЕТ ЯДРО** (`ArmyCore.RearPressPass`, этап D1, сент.
  2026): пехотинец с рангом >= 2 в дерущемся отряде давит к точке боя шагом от
  ядра, его GDScript-тик пропускается; АРЕНДА из `_recalc_melee`, пробуждения
  событиями (упор/удар/смена цели/паника). НЕ на конницу (съедал кабанам
  разгон) и не под замком. Замер qa_ab: **+0.34 мс тика на фазе сближения**;
  в замершей рубке ноль — глубокого тыла там нет (qa_clash: полторы шеренги),
  этих бойцов уже накрывает дрёма. Автопилот подхода (`AutopilotPass`)
  построен и ВЫКЛЮЧЕН по замеру (+0.008): обязательные ворота «только чистый
  путь» (без них qa_wall A1 уезжал с 1-3 на 5-8 просочившихся) не оставили
  ему сцены. Спринт D остановлен на gate-check D1 — разбор и пересчитанный
  потолок в `docs/PLAN_D_70FPS.md` и ENGINEERING_LOG.
- **МНОГОКАДРОВЫЙ ПОКОЙ НЕ УСЫПЛЯЕТСЯ ПО КАРТИНКЕ** (`Unit._process_can_sleep`,
  сент. 2026). Кадры в общей отрисовке листает ТОЛЬКО визуальный тик — узел
  спрайта на паузе (`_look_detach_node`), — и уснувший боец замирает на
  случайном кадре idle. Ровно это и было «лучники и рыцари еле дышат, а
  копейщик покачивается»: fps у всех лент одинаковый (6 к/с), но правило «спать
  можно, только когда листать нечего» держал один копейщик. Поднято в базу
  (`_look_loop and _look_frames > 1` — не спим) и продублировано у мечника: его
  переопределение не зовёт super, а комментарий «AnimatedSprite3D крутит цикл
  сам» устарел ещё при переезде на MultiMesh.

## Ударная конница (кабаны)

- **Натиск с разгона и постоянное продавливание — РАЗНЫЕ механики**, и путать
  их нельзя. Продавливание (`_apply_push`) идёт всю драку, на каждый удар;
  натиск (`_charge_impact`) — одно событие на один подход.
- **Разгон взводится ОДИН РАЗ на подход** (`Unit._charge_ready`), и одного
  этого мало: условие взвода — ДИСТАНЦИЯ до цели, а в свалке цель меняется
  каждые пару секунд, и новая нередко оказывается за десять метров. Условие
  выполнялось снова, СТОЯ НА МЕСТЕ, — отсюда «разлёт дважды за одно
  соприкосновение». Поэтому проверяется не намерение, а ФАКТ: сколько всадник
  реально проехал с момента взвода (`charge_min_runup`, точка `_charge_from`).
  В плотном бою столько не проехать.
- **УДАР ЛОВИТСЯ ПО КОНТАКТУ, А НЕ ПО АРИФМЕТИКЕ ДИСТАНЦИЙ.** Тик бойца
  шардирован, и «дистанция стала меньше дальности» замечалось с опозданием:
  всадник успевал въехать в строй, и только потом срабатывал разлёт («сначала
  залетают, потом разлетаются»). `_enemy_contact` ставится в тот же кадр, когда
  шаг реально упёрся в чужое тело, — это и есть миллисекунда первого касания.
- **Удар с разгона считается от МАКСИМАЛЬНОГО запаса ЖЕРТВЫ**
  (`charge_impact_frac`), а не от урона седока: сминает вес и скорость кабана.
  Накрывает круг `charge_splash` вокруг точки касания — «первый ряд контакта»,
  а не отряд и не одну модель.
- **ОТБРАСЫВАНИЕ ТЕПЕРЬ СКОРОСТЬ, А НЕ ПЕРЕНОС** (`Unit._fling_vel`,
  `_tick_fling`). ТРЕБОВАНИЕ РАЗВЁРНУТО ВЛАДЕЛЬЦЕМ: «пехотинцы отлетают
  резкими брызгами, практически телепортируясь». Прежнее решение (см. абзац
  ниже, оставлен как история) переносило тело на 2.2 м ОДНИМ кадром, а
  плавность изображала картинка — она догоняла тело замедленным сглаживанием.
  Догоняет она ПОЗУ, а не движение: боец всё равно исчезал из строя и появлялся
  в стороне. Теперь у бойца есть ЗАТУХАЮЩАЯ СКОРОСТЬ — единственное поле
  скорости в проекте, заведённое ровно под этот эффект, — и её интегрирует
  физический тик. Дальность удара не изменилась: `v0` считается так, чтобы за
  `FLING_SEC` пройти заказанные метры. Замер: старт 14.6 м/с = **0.24 м за
  кадр** при пороге телепорта 2 м.
- **ЛЕТИТ ПО ВЕКТОРУ УДАРА, А НЕ ВБОК** (`CHARGE_FLING_BACK` 0.92 против
  `CHARGE_FLING_SIDE` 0.36). Тоже разворот требования: раньше было
  преимущественно вбок, чтобы строй разошёлся и открыл проход. Боковая доля
  оставлена малой намеренно — снеси её совсем, и весь накрытый ряд поедет
  строго параллельно, оставаясь такой же плотной стенкой метром позади.
- **ВСАДНИК НЕ ВЯЗНЕТ В ПЕРВОЙ ШЕРЕНГЕ** (`charge_breakthrough` 1.8 м). Удар
  был ОДНИМ СОБЫТИЕМ, после которого всадник переходил в обычную рубку — вся
  его масса уходила в один кадр. Теперь та же скорость выдаётся и ему самому,
  вперёд: он въезжает в пробитую дыру. Это НЕ новая ветка автомата состояний,
  а тот же `_tick_fling`, и потому оно само складывается с продавливанием.
- **ПЛАВНЫЙ КАНАЛ — ТОЛЬКО У КОННИЦЫ, И ЭТО ЗАМЕР, А НЕ ПОЛОВИНЧАТОСТЬ.**
  Сначала через скорость пошёл ЛЮБОЙ толчок, включая пехотный. Толкают в рубке
  всех и постоянно, а едущий по скорости боец платит КАЖДЫЙ КАДР записью точки,
  `sync_row` и высотой рельефа — вместо одной записи на удар. Пехотный шаг при
  этом четыре сантиметра, и плавность там никто не увидит. Через скорость идёт
  только `charge_push_mult > 1.0`.
- **Отбрасывание — прямое смещение, а не импульс** (ИСТОРИЯ: так было до
  разворота требования выше). Физтел в игре нет вовсе,
  скорость никем не интегрируется между кадрами; импульс потребовал бы своего
  поля и своей ветки в тике. Направление ПРЕИМУЩЕСТВЕННО ВБОК: строй должен
  РАЗОЙТИСЬ, открыв проход, а назад его двигает продавливание, и дублировать
  его здесь означало бы удвоить ту же силу. Разметка гнётся вместе с телами
  (`squad_dent`), иначе смыкание вернуло бы всех на места.
- **Копейщики держат лобовой навал** (`Unit.repels_charge`, переопределяют оба
  копейщика). Это свойство РОДА ВОЙСК, а не стойки: копья опущены навстречу
  коннице и в «атаке», иначе фаланга была бы уязвима ровно тогда, когда идёт
  вперёд. С фланга и с тыла не работает — там и проходит конница. Лоб считается
  по `CHARGE_FRONT_COS`, и это ТО ЖЕ число, что у `PHALANX_FRONT_COS`:
  «в лоб» обязано означать одно и то же в обеих механиках.
- **Толчок идёт на КАЖДЫЙ удар** (`Unit.PUSH_EVERY` = 1). Прежняя десятка
  давала копейщику толчок раз в двадцать секунд — механики на экране не было
  вовсе. Плавность держит не редкость, а размер шага (потолок 0.4 м ×
  `PUSH_GLOBAL_SCALE`). Паритет при равном напоре сломаться не может:
  `_apply_push` выходит на неположительной разнице.
- **Напор и упор КЭШИРУЮТСЯ** (`Unit._refresh_power`, ключ — версия таблицы
  бонусов + стойка + мораль + push_force). С постоянным расчётом они считаются
  на каждый удар и дважды (свой и чужой), а складываются из чтений в
  автозагрузке. Замер qa_mass_battle на 5000: без кэша переход на «каждый удар»
  стоил около миллисекунды свалки, с кэшем — ничего сверх разброса прогонов.
- **Строку ядра толчок НЕ сводит.** Разбор наложения читает колонку, а не узел,
  и до ближайшего тика цели видит её на старом месте — но строка сходится сама,
  а толчок накапливается (qa_cavalry E1: 42 сдвинутых из 46, худший на 7.9 м).
  Дописывать точку в колонку прямо в `_apply_push` пробовали и не оставили:
  пользу замером подтвердить не вышло, а разброс прогонов там шире эффекта.

- **`push_force` САМ ПО СЕБЕ НИЧЕГО НЕ РЕШАЕТ.** Смещение считается как
  `clampf(разница напора × 0.15, 0.04, 0.4)`, и потолок срабатывает уже при
  разнице в три единицы: кабан с `push_force` 15 двигал строй ровно на
  столько же, на сколько мечник с тройкой. Рабочие рычаги — два новых поля
  в `unit_stats_config.STATS`:
  - `charge_push_mult` (2.0) применяется **ПОСЛЕ** потолка. До него его съел
    бы тот же `clampf`;
  - `push_every` (2 против пехотных 10). Прежний строй продавливался на семь
    сантиметров раз в десять секунд — этого на экране нет вовсе.
- **`GameManager.squad_dent()` гнёт РАЗМЕТКУ, а не только тела.** Отброшенный
  боец вернулся бы на своё место при первом смыкании рядов, и от удара не
  осталось бы следа. Места в `DENT_RADIUS` от точки удара уезжают назад с
  затуханием — получается вмятина, а не сдвиг всей линии. Потолок
  `DENT_MAX_TOTAL` (4 м) обязателен: без него отряд под долгим напором уехал
  бы разметкой за край карты. Обнуляется новым приказом строем.
- **Смыкание рядов здесь НЕ зовётся.** Оно шлёт `command_move` каждому бойцу,
  а тот снимает цель атаки: в разгар рубки это остановило бы бой (ровно от
  этого и заведено `squad_in_combat`). Вмятину подхватит первое штатное
  смыкание, когда драка кончится.
- Урона всё это не касается вовсе — двигаются только точки. Стенд
  `qa_cavalry` (12 проверок) стережёт и это ограничение.

## Гоблины (`scripts/goblin/`)

- `goblin_config.gd` — ПОВЕДЕНИЕ орды (деревня, спячка, волны, отход). Сила
  удара и запас жизни — в `unit_stats_config.STATS` рядом с людьми.
- Юниты — обычные `Unit` без единой особой ветки: бой, строй, туман, ветеранство
  и общая отрисовка достаются им от базы. Сквозь чужой строй они не проходят не
  потому, что это где-то написано, а потому, что для них НЕ написано исключений.
- `GoblinHut extends Castle` — ради готового гарнизона (принять разбитый отряд,
  спрятать, лечить, доукомплектовать, выпустить). Замок вынес настройку полей в
  `_configure()` именно ради этого наследника: `super._ready()` не умеет звать
  метод через голову родителя. Хижина даёт еду, склада не имеет.
- Расписание: спячка до 30:00 (`set_physics_process(false)` + `F_DORMANT`, чтобы
  пакетное расталкивание тоже её пропускало) → волна в центр → оборона деревни и
  найм заново → снова центр → добить слабейшего. Центр считается взятым, ТОЛЬКО
  когда орда до него дошла.
- **ПРИКАЗЫ ОРДЫ: РЕШЕНИЕ СРАЗУ, ИСПОЛНЕНИЕ ПОРЦИЯМИ И ТОЛЬКО ПО ДЕЛУ**
  (сент. 2026). `_issue_orders` выдавал command_attack/command_move КАЖДОМУ из
  тысячи гоблинов каждый такт — горб 46-72 мс раз в 2 с (зонд
  `qa_mass3k/AIProbe`), на кадре свалки — стабильные ~2.4 мс «мышления ИИ».
  Перенесены оба приёма красного ИИ: отряд, уже втянутый в бой, приказов не
  получает вовсе (цели раздают сцепка и авто-агро), марш в ту же точку — не
  чаще `REISSUE_KEEPALIVE_SEC`, а раздача нарезана очередью
  (`GoblinAI._drain_orders`, по `ORDER_BUDGET_MEMBERS` бойцов за кадр).
  Итог: 476 → 86 мс на 900 кадров, максимальный горб 72 → 12 мс.
- Число шардов выводится из ХОДЯЩИХ (`GameManager.active_units`), а не из
  размера реестра: 850 спящих иначе загоняли игру в два шарда с первой секунды.
- Деревня — плотная сетка хижин с шагом `HUT_STEP` (6 м) на расчищенной
  площадке (`GOBLIN_VILLAGE_CLEAR` в резерве, до посадки леса). Хижина
  АНИМИРОВАНА: `Building.sprite_fps()` > 0 листает ленту (дым из трубы), фаза
  своя у каждой (`sprite_frame_phase`).
- Строй орды — ТОЛПА, а не каре: места по спирали золотого угла плюс
  детерминированный сдвиг (`goblin_config.horde_offset`); эти же точки идут в
  разметку отряда, чтобы смыкание после боя собирало толпу.
- У гоблинов СВОЙ `PIXEL_SIZE` и высота центра спрайта считается ПО ЛЕНТЕ, а не
  константой — поэтому ноги остаются на земле при любой смене масштаба. Размер
  выведен из одного числа `goblin_config.SIZE_SCALE` (сейчас 1.7, заказ
  владельца): от него же растянуты `HORDE_SPOT`, `SQUAD_SPACING` и радиус
  расталкивания. Менять `PIXEL_SIZE` в одиночку нельзя.
- **Радиус расталкивания у бойца ЛИЧНЫЙ** (`Unit.sep_radius()` → колонка
  `_sepR` в `ArmyCore`, ставится один раз при рождении строки). Ноль означает
  «как у всех», то есть общую `Unit.SEP_MIN_DIST`; гоблины возвращают своё
  число. Без этой колонки выбор был из двух плохих: либо орда слипается в
  ковёр, либо ради неё раздвигается вся людская пехота.
- Массовые стенды деревню выключают (`perf_config.goblin_village = false`): она
  растягивает габарит сетки на свой угол карты и портит замер армии.

## Плотность строя и разведение отрядов

- **Личный круг бойца выводится из САМОГО ПЛОТНОГО СТРОЕВОГО ИНТЕРВАЛА в
  проекте** — тогда разбор наложения физически не может спорить ни с одним
  построением. `PERSONAL_RADIUS` 0.25 при `Building.squad_spacing` 0.55.
  Прежние 0.175 были выведены из 0.35 и за интервалом не поехали, когда тот
  подняли, — оттого тысяча бойцов и сминалась в пятно вдвое уже своей разметки.
- **`ARRIVE_RADIUS` жёстко связан с `SEP_MIN_DIST` и обязан быть БОЛЬШЕ.**
  Иначе боец, идущий в точку, где стоит союзник, останавливается разбором
  наложения ЗА пределами допуска — то есть никогда не отчитается о прибытии.
  Стережёт `qa_push` D0б (и `qa_crowd` A0).
- **ОТРЯДЫ НЕ ВСТАЮТ ДРУГ НА ДРУГА** (`GameManager.free_squad_spot`). Жалоба
  владельца: несколько отрядов, посланных в одну точку (или новобранцы на точку
  сбора, где уже кто-то стоит), накладываются и непрерывно толкаются.
  **РАСТАЛКИВАНИЕМ ЭТО НЕ ЛЕЧИТСЯ** — оно разводит ТЕЛА и делает это верно, но
  про отряды не знает: две сотни бойцов, которым велено стоять в одном месте,
  оно будет разводить вечно, потому что каждый лезет на свою точку разметки, а
  точки совпадают. Лечить надо ПРИКАЗ: точка назначения проверяется на
  занятость один раз, в момент выдачи, и при занятости отходит вбок.
- **МЕСТО ОТРЯДА — ЭТО КУДА ОН ИДЁТ, А НЕ ГДЕ ОН СЕЙЧАС** (`_squad_spot_centre`
  берёт центр `post_pos`, а живой центроид — лишь запасной ответ). Первая
  версия смотрела на текущий центр масс, и этого не хватило ровно в том случае,
  ради которого всё писалось: игрок шлёт отряды в одну точку ОДИН ЗА ДРУГИМ.
  Второй приказ отдаётся, пока первый отряд ещё в пути и физически далеко, — по
  текущему центру он место не занимает, и оба приходят друг на друга (замер:
  одна перекрывающаяся пара, 1.04 м). Занятым место делает ПРИКАЗ, а не тело.
- **ОТРЯДЫ ОДНОГО КЛИКА ДРУГ ДРУГУ НЕ ПОМЕХА** (аргумент `ignore`). Их уже
  развела сетка блоков (`_issue_group_grid_move`); посчитай их занятыми — и
  группа разъедется вместо блока, разведённая дважды.
- **Бойцы РАЗНЫХ отрядов держатся шире, чем свои по шеренге**
  (`Unit.SEP_CROSS_SQUAD`, считает `ArmyCore.BatchSeparation`). Это и есть
  «десять отрядов не спрессовываются в комок размером в два». Отталкивания
  ЦЕНТРОВ отрядов нет и не будет: центр — величина производная (медиана), и
  двигать по ней людей означало бы завести вторую механику перемещения с
  силами и затуханием — ровно то, от чего в проекте однажды уже отказались.
  Отряд 0 («без отряда»: рабочие, одиночки, гарнизон) расширения не получает.
- **Приказ атаки нескольким отрядам раскладывается ПО ШИРИНЕ чужого фронта**
  (`SelectionManager.frontline_targets`). Цель по-прежнему ОДНА НА ОТРЯД —
  внутри отряда её разбирает `squad_pick_member`, — но левый наш отряд идёт на
  левый фланг противника, правый на правый. Ось считается тем же способом, что
  и у ИИ (`EnemyAI._enemy_front_normal`): ковариация XZ и `atan2`. Один скан на
  клик, покадрового пути здесь нет. Меньше двух отрядов или меньше трёх врагов
  рядом — раскладки нет вовсе, приказ идёт прежним путём слово в слово.

## Приказы, погоня и строй ИИ

- **РЕШЕНИЕ ОБ ОТХОДЕ ФИКСИРУЕТСЯ ПО ВРЕМЕНИ.** Отряд ИИ подходил, получал
  стрелы, начинал убегать, разворачивался и умирал в спину: такт размышления
  пересчитывал цели и выдавал отступающему новую роль. Теперь замка ДВА, и оба
  обязательны, потому что решение и его исполнение живут в разных местах:
  роль держит `EnemyAI._retreat_locked` (единственная точка, через которую роли
  вообще меняются, — `_set_role`), а исполнение держит `Unit.retreat_locked()`
  — до бойца доходят приказы не только от ИИ (смыкание рядов, сплочённость,
  возврат на пост), и любой звал `command_move` без `keep_retreat`, то есть
  гасил отход. Сроки держатся ОДНИМ числом на обоих концах
  (`Unit.RETREAT_LOCK_SEC` = `AICfg.RETREAT_MIN_SEC`).
- **Отменить отход могут ровно две вещи:** ближний бой (отряд спиной к врагу не
  отступает, а гибнет) и истечение срока. Прибытие в замок снимает его
  принудительно — `end_retreat(true)`.
- **СМЫКАНИЕ — ОДНО ДЕЙСТВИЕ И ОТРЯДНОЕ. ПОШТУЧНОГО ВОЗВРАТА НЕТ И НЕ БУДЕТ.**
  Был заведён второй, «лёгкий» путь: он слал `command_move` ОТДЕЛЬНЫМ бойцам —
  тем, кто съехал с места и ничем не занят, — и работал даже посреди боя.
  Задумка была в том, что дерущихся он не трогает. **НА ЭКРАНЕ ЭТО ОБЕРНУЛОСЬ
  ХАОСОМ**, и обе беды следуют прямо из «поштучно и в бою»:
  ОТРЯД РАЗРЫВАЛО (боец, у которого в свалке на секунду не стало цели, считался
  бездельником и уходил на свой `post_pos` — а тот остался там, откуда отряд
  ВЫШЕЛ; за ним второй, третий, и отряд вытягивался цепочкой прочь от схватки);
  РАБОЧИЕ БРОСАЛИ РАБОТУ (рабочий числится отрядом из одного человека, а
  `command_move` сбрасывает и рубку, и стройку, и добычу).
  Лечение — не третий костыль, а ОТКАЗ ОТ ВТОРОГО: действие снова одно,
  `squad_close_ranks`, только вне боя и только по отряду.
- **ЦЕНА КОСТЫЛЯ БЫЛА НЕ ТАМ, ГДЕ ЕЁ МЕРИЛИ.** A/B «выключить оба обхода»
  показал 0.11 мс — и я на этом успокоился. Ошибка в том, что мерилась цена
  САМОГО ОБХОДА, а не того, ЧТО ОН ДЕЛАЛ: каждый `command_move` будит бойца,
  метит позу грязной и переводит его в MOVING — то есть отнимает у него сон.
  После отката `qa_fps` на 2010 юнитах: **175 FPS против 134**, физтик 3.27
  против 4.44 мс. **Мерить надо последствия, а не вызов.**
- **РАБОЧИХ СТРОЙ НЕ КАСАЕТСЯ ВООБЩЕ** (`REFORM_SKIPS_WORKERS`). Разметка у
  отряда-из-одного-рабочего завестись МОЖЕТ (её достраивает
  `squad_close_ranks`), и дальше смыкание сбивало бы ему всю работу.
- **СТРОЙ СМЫКАЕТСЯ САМ, КОГДА ЕГО РАЗВАЛИЛИ** (`GameManager._sweep_reform`).
  Прежнее смыкание взводилось ТОЛЬКО боем (`squad_mark_hit`), а проход своих
  сквозь строй боем не является — отметка не ставилась, и дыры оставались до
  первой стычки. Редкий обход (раз в `REFORM_SWEEP_SEC`) смотрит, не уехали ли
  бойцы СТОЯЩЕГО отряда от своих мест, и смыкает. Причина отъезда не важна
  вовсе, и это главное достоинство: отдельный детектор «прохода союзника»
  ошибался бы и на расталкивании, и на толчке конницы, и на возврате из
  гарнизона — а лечение у всех этих случаев одно.


- **ШИРИНА ОТРЯДА СЧИТАЕТСЯ ТЕМ ЖЕ ПОСТРОЕНИЕМ, КАКИМ ЕГО И РАЗВОРАЧИВАЮТ**
  (`EnemyAI._squad_cols` / `_squad_width` / `_screen_step_for`). Шаг заслона уже
  выводился, а не стоял числом, — и всё равно давал кучу, потому что выводился
  из ОБЩЕЙ шестёрки колонок (`SQUAD_COLS`), а `_issue_plan` раскладывает
  копейщиков по ЧИСЛУ ШЕРЕНГ: 60 человек на `PHALANX_RANKS` = 4 это 15 колонок,
  то есть 8.7 м по фронту против планируемых 5.68. Семь отрядов перекрывались
  на три метра с лишним и сходились в одно пятно. Ширина считается по ПОЛНОМУ
  штату (доучившийся отряд занимает своё место и никого не двигает) и теперь
  участвует ВЕЗДЕ, где отряды разводят: заслон у базы, рубеж
  (`DEFENSE_LINE_WIDTH` — пожелание, ширина отряда — факт, побеждает факт),
  последний рубеж и боевая волна.
- **ЛИШНИЙ ЭШЕЛОН ПЕРВОЙ ЛИНИИ УХОДИТ ВПЕРЁД, А НЕ НАЗАД.** Как только ширина
  стала считаться честно, семь отрядов копейщиков перестали помещаться в
  `SCREEN_MAX_WIDTH`, и второй эшелон уехал назад РОВНО НА ЛИНИЮ ЛУЧНИКОВ
  (13 − 7 = 6 м при лучниках на 7 м): копейщики встали своим стрелкам в
  затылок, зазор между постами вышел 1.0 м. Позади копейщиков места нет и быть
  не может — там лучники, а за ними замок. Назад эшелонируются ТОЛЬКО лучники,
  и только до пола `SCREEN_MIN_DEPTH`. Ловят это две проверки `qa_ai`:
  «копейщики впереди лучников» и «посты гарнизона разнесены».
- **КУРС СТРОЯ ЗАДАЁТ ПЛАНИРОВЩИК И ЗАМОРАЖИВАЕТ** (`sq["face"]`, четвёртый
  аргумент `_set_role`). Гарнизону, заслону и последнему рубежу курс выведен из
  мест замка и базы игрока, то есть постоянен. Пока курс считался в момент
  выдачи (`_order_course` → `_enemy_front_normal`), он брался по ОБЛАКУ ЖИВЫХ
  БОЙЦОВ ИГРОКА: те шевелятся, главная ось облака гуляет, и каждые две секунды
  заслон получал новый угол разметки и переставлялся всем составом. Побочно это
  сняло скан всей группы игрока на КАЖДЫЙ стоящий отряд.
- **ЗАМОРОЖЕННАЯ ЦЕЛЬ (STAGNANT TARGET): СТОЯЩИЙ ЗАСЛОН ПРИКАЗ НЕ ПЕРЕИЗДАЁТ**
  (`EnemyAI._posts_intact`). Цель не менялась, курс задан планом, противника в
  зоне нет — раскладка мест не изменилась ни на сантиметр, и `_issue_plan`
  выдавал бы `set_stance` + `command_move` каждому бойцу впустую: раз в две
  секунды вся оборона разом делала шаг («пульсирующая мышца» из жалобы).
  Проверяется СВОЙСТВО — стоит ли строй по местам, — а не время. Раскладка в
  проверке считается ТЕМ ЖЕ кодом, что и в приказе, иначе проверка судила бы об
  одном строе, а приказ выдавал другой. Идущий отряд отвечает «не трогать»: он
  и так исполняет прошлый приказ. Стенд `qa_ai2`, блок H.
- **Заслон ИИ у базы: шаг между отрядами ВЫВОДИТСЯ, а не задан числом.**
  Стояла `SCREEN_WIDTH` = 18 м на весь фронт, делённая на число отрядов: при
  семи отрядах копейщиков центры выходили в 2.6 м друг от друга, а отряд из
  шестидесяти человек занимает около трёх метров в ширину и почти шесть в
  глубину. Места перекрывались, четыреста бойцов сходились в точку,
  расталкивание их разбрасывало, следующий такт гнал обратно — тот самый цикл
  «сжатие → выталкивание → сжатие». Теперь шаг равен ширине отряда плюс
  просвет (`EnemyAI._screen_step`), а не поместившиеся в линию уходят во второй
  эшелон (`SCREEN_MAX_WIDTH` / `SCREEN_ROW_DEPTH`).
- **НИ ОДИН ПОСТ ЗАСЛОНА НЕ СМОТРИТ В ТЫЛ.** Заслон стоит МЕЖДУ замком и
  противником; пост за замком не заслоняет ничего, а идут к нему сквозь
  собственные постройки. Эшелоны зажаты полом `SCREEN_MIN_DEPTH`, а фланговые
  мечники расходятся ВШИРЬ, а не вглубь: разнос по глубине уводил третью пару
  на −4 м за замок (поймал `qa_ai`, «гарнизон стоит заслоном лицом к врагу»).


- **МАКРО-СОСТОЯНИЙ В ОБОРОНЕ ТРИ, А НЕ ДВА** (`EnemyAI._update_recap`).
  `DEFENSIVE_MODE` знал «держать рубеж» и «последний рубеж у замка», и оба
  оборонительные; хуже, выход из последнего рубежа требует, чтобы противника у
  рубежа не осталось вовсе, — а он и не уйдёт, если центр его. Армия копилась
  до полного лимита и стояла. Третье состояние — **ОТВОЕВАТЬ ЦЕНТР**
  (`ROLE_RECAP`): центр под игроком И армия набрана от `RECAP_ARMY_FRACTION`
  (доля считается ПО БОЙЦАМ — отряды у разных родов разного размера) → дома
  остаётся `RECAP_HOME_GUARD` отрядов на род, всё остальное идёт единым фронтом
  на центр. **Проверяется ПЕРВЫМ, до последнего рубежа:** оба состояния
  срабатывают на одну обстановку («центр потерян»), и порядок проверок и есть
  приоритет. **Гистерезис двусторонний:** выйти можно отвоевав центр ИЛИ
  растеряв армию ниже `RECAP_ABORT_FRACTION`; без второго выхода разбитая волна
  ползла бы на центр по одному, без зазора между порогами ИИ менял бы решение
  каждый такт. Роль своя, а не `ROLE_ASSAULT`: штурм идёт на БАЗУ игрока и в
  обороне запрещён (это стережёт `qa_ai`), а здесь цель — спорная земля.
  Стенд `qa_ai2`, блок I.
- **ЗДАНИЕ ПОВОДКОМ ПОГОНИ НЕ ОГРАНИЧИВАЕТСЯ.** Поводок (`PURSUIT_LIMIT`, 7 м)
  придуман против РАСТЯГИВАНИЯ ОТРЯДА ЗА УБЕГАЮЩИМ. Дом не убегает никуда —
  а поводок его не различал, и на этом ломался весь приказ «снести лагерь»:
  снеся первую хижину и перейдя на соседнюю, боец делал к ней те самые семь
  метров, поводок рвался, СНИМАЛ ЗАМОК ПРИКАЗА и гнал бойца на пост — за
  полкарты. Жалоба владельца звучала как две разные («сносят одно здание и
  приказ слетает» и «бойцы бегут из боя на базу»), а причина одна.
- **ЗАМОК НА ПОСТРОЙКЕ ПЕРЕХОДИТ НА СОСЕДНЮЮ** (`_next_enemy_building`,
  `BUILDING_CHAIN_RANGE` 26 м). У замка на ОТРЯДЕ следующего выдаёт
  `squad_pick_member`; постройки в отряды не собраны, и «соседний дом» надо
  искать по карте. Радиус узкий намеренно: это «тот же лагерь», а не «любая
  постройка на карте». При переходе СНИМАЕТСЯ ЯКОРЬ ПОГОНИ — новый дом это
  новый подход.
- **ПРИКАЗ ИСЧЕРПАН — ВСТАЁМ ЗДЕСЬ** (`_hold_where_i_stand`). Пост бойца
  остаётся там, откуда отряд вышел, и после снятия замка авто-агро с возвратом
  на пост тянут его обратно. Пост переносится в текущую точку: приказ выполнен
  там, где боец стоит.
- **ЯКОРЬ ПОГОНИ — ОДИН НА ОТРЯД** (`GameManager.squad_pursuit_anchor`).
  Личный якорь и был причиной «отряд растягивается на полкарты»: передний боец
  касался цели раньше и ближе, задний позже и дальше, и дальше каждый принимал
  СВОЁ решение — передние гнали, задние срывали поводок и уходили на пост.
  Теперь якорь ставит первый дотянувшийся, меряются от него все, и поводок
  кончается у всех разом. Читается он ВНУТРИ условия `_pursuit_anchored and
  _attack_is_forced`, а не до него: это словарный поиск в автозагрузке, а ветка
  подхода — самая горячая в тике.
- **Поводок погони** (`Unit.PURSUIT_LIMIT`, 7 м): якорь ставится в точке
  ПЕРВОГО касания приказанной цели, а не в момент приказа — иначе сорвался бы
  законный приказ «атаковать вон тех, в сорока метрах». Дальше поводка боец
  забывает цель, снимает замок и возвращается на `post_pos` приказом. Не путать
  с `AGGRO_LEASH` (14 м): тот ограничивает ИНИЦИАТИВУ бойца от поста и на
  прямой приказ не действует вовсе.
- **Цена бега:** `SPRINT_SPEED_FACTOR` 1.35 и `SPRINT_DAMAGE_MULT` 1.35.
  Множитель урона применяется ПОСЛЕДНИМ, уже поверх щита: это открытость
  бегущего, а не пробитие брони.
- **Марш-перехват уже есть** и равен `attack_range + INTERCEPT_MARGIN` (0.8 м) —
  то есть 2–3 м у пехоты. Отдельного «detect_range» заводить не нужно.
- **Строй ИИ встаёт параллельно чужому фронту.** `EnemyAI._enemy_front_normal`
  берёт главную ось облака игрока (ковариация XZ, угол через `atan2`), а
  `_slot_at` раскладывает смещения по осям СТРОЯ, а не карты. До этого курс
  считался честно, но разметка его не читала — отряд всегда строился решёткой
  по осям карты и упирался в диагональную линию углом.
- **Фаланга в бою смотрит на драку, а не по курсу.** `Unit._phalanx_dir()` вне
  боя берёт `squad_course` (курс последнего приказа — по нему считаются ряды),
  но при `squad_in_combat` переключается на `squad_enemy_dir`. Иначе незанятые
  бойцы шагали ПО КУРСУ, то есть мимо сцепившейся линии, в пустое поле.
- **Плотность группы отрядов:** `SelectionManager.GROUP_CELL_GAP` 1.0 и потолок
  габарита ячейки `GROUP_CELL_MAX` 20 м. Ячейка одна на всех и берётся по
  максимуму — без потолка один растянутый отряд в выделении задавал ячейку всем
  остальным, и компактные блоки разъезжались по полю.
- **Контур приказанного/наведённого ЗДАНИЯ — свой меш и свой слой**
  (`UnitVisuals.building_ring_mesh`). Кольцо бойца — тор в 35 см о ДВЕНАДЦАТИ
  сегментах; растянутое до замка (×11) оно давало толстый угловатый
  многоугольник — и по толщине трубки, и по грубости обвода. Здешний тор
  единичного радиуса, тонкий и о 64 сегментах, а масштаб равен половине
  габарита постройки. Разные меши в одном `MultiMesh` не живут, поэтому у
  зданий отдельный слой — ленивый, то есть ноль вызовов, пока ни на одну
  постройку не навелись.
- **МЕТКА ПРИКАЗА ГАСНЕТ ПО ПРИБЫТИЮ — ЗНАЧИТ, ЕЁ НАДО ГАСИТЬ И ИНАЧЕ.**
  Рабочий, которого послали в точку, а потом отправили строить или рубить, до
  неё не дойдёт НИКОГДА — и кольцо остаётся висеть в чистом поле («фантомные
  круги, которых игрок не задавал»). Новая работа снимает прежний приказ
  (`squad_clear_order` в `Worker.command_build`/`command_gather`), расформирование
  отряда — тоже, а пустой отряд метку не удерживает: `_centroid_of` пустого
  списка отвечает НУЛЁМ, и от него до цели всегда далеко.
- **Указатели приказов:** `GameManager.squad_orders` (sid → вид/точка/цель),
  слои `set_move_marks` / `set_order_targets` в `SelectionDecalRenderer`.
  Это ОТРАЖЕНИЕ приказа для глаз, боевая логика его не читает — второе
  хранилище приказов заводить нельзя. Метка точки снимается сама по
  `ORDER_MARK_ARRIVE` (3 м от медианного центра), подсветка цели — с её
  гибелью. Стенды `qa_facing`, `qa_orders`.

## Отрисовка армии: LOD, анимация и метки под ногами

- **LOD-радиус СЛЕДУЕТ ЗА ЗУМОМ, а не стоит на 90 м.** `_view_r2` —
  максимум из `perf_config.lod_radius` и видимой на земле полосы, которую
  сообщает камера (`RTSCamera._lod_ground_radius`). Пока радиус был
  постоянным, отдалённая ортокамера показывала 250+ м, и всё дальше 90 м от
  ЦЕНТРА ЭКРАНА считалось «невидимым», оставаясь на экране.
- **Признак `seen` означает «стоит ли считать позу», а НЕ «рисуется ли».**
  Общий `MultiMesh` рисует всех зарегистрированных (`extra_cull_margin`
  16384 м), поэтому **перенос слота пишется ВСЕГДА**, без оглядки на LOD
  (`Unit._sync_far_render`, ветка `move_to`). Эти две строки и давали разом
  три жалобы: пехота ехала с замершими ногами, рабочие рубили без анимации, а
  жёлтые кольца отставали от бойцов — кольцо берёт нарисованную точку, которую
  сглаживание двигает каждый кадр, а сам спрайт стоял до следующего полного
  пересчёта (раз в `vis_shards × anim_every` = до двадцати кадров отрисовки).
- **Кадр ленты уезжает в буфер СРАЗУ** (`FarUnitRenderer.Slot.set_frame`, одна
  запись во float). `_advance_look_frame` листает походку на своих 6-10 к/с, а
  полный разбор спрайта идёт раз в `anim_every` — без отдельной записи кадра
  анимация шла со скоростью полного разбора, то есть 2-3 раза в секунду.
- **Выделение хранит и МАССИВ, и СЛОВАРЬ членства** (`SelectionManager._sel_set`).
  Проверка `node in selected_units` линейна, а выделение разворачивается по
  отрядам — стоимость выходила квадратичной: замер `qa_fps` на 2000 бойцах
  давал **1271 мс** на один «выделить всё», то есть полторы секунды замершей
  игры. Со словарём — 71 мс.
- Что сколько стоит на 4000 бойцов в окне 1920x1080 (замер `qa_fps`, база
  50 FPS = 20 мс): физический тик армии **3.3 мс**, туман 1.6, спрайты армии
  0.8, деревня гоблинов 0.65, HUD 0.66, мышление ИИ 0.2, растительность ~0.
  Вызовов отрисовки 496 — **узкое место не в них**. Верхние ветки тика:
  `process_move`, `rank_recompute`, `grid_update` (по ~14% каждая).
  **ДОЛИ ВЕТОК ЧИТАТЬ ТОЛЬКО КАК РАНЖИРОВАНИЕ.** Честный A/B (авг. 2026) дал у
  `grid_update` 0.40 мс на тик вместо профильных 1.18, у `check_auto_aggro` —
  0.31 вместо 0.65. Разбор — `docs/PERF_6000.md`, раздел 6.

## Порядок перекрытия спрайтов: глубина по точке на земле («Y-sort»)

- **ЖАЛОБА:** «пехотинцы проваливаются под спрайты камней — камень перекрывает
  ноги солдата, стоящего ПЕРЕД ним». Причина ГЕОМЕТРИЧЕСКАЯ, а не в слоях:
  квад стоит вертикально, камера смотрит под 45°, и точка выше по Y ближе к
  камере на sin45 ≈ 0.71 м на каждый метр высоты. Верхушка камня честно
  оказывается ближе собственного основания и перекрывает всё, что стоит перед
  ней ниже. Буфер глубины при этом работает правильно — неверна постановка
  задачи: у плоской декорации не должно быть «глубины по пикселям».
- **ЛЕЧЕНИЕ: глубина у всего спрайта ОДНА и берётся у его ТОЧКИ НА ЗЕМЛЕ**
  (`DEPTH` в `cyl_billboard` и `mm_unit_sprite`, ручка `ground_depth`). Тогда
  порядок решает то же, что решает игрок глазом, — кто ближе стоит; сортировка
  остаётся аппаратной, ни второго прохода, ни ручной сортировки не нужно.
  Проекция считается во ФРАГМЕНТНОМ шейдере: в NDC глубина нелинейна, и
  интерполяция между вершинами дала бы не плоскость, а поверхность второго
  порядка.
- **ТОЧКУ НА ЗЕМЛЕ НЕЛЬЗЯ ВЫВОДИТЬ ИЗ ГЕОМЕТРИИ КВАДА, И ЭТО СТОИЛО ВСЕЙ АРМИИ,
  УТОНУВШЕЙ В ГРУНТ.** У декораций (`cyl_billboard`) «низ квада лежит на
  грунте» верно ПО ПОСТРОЕНИЮ: узел поднят ровно на половину высоты
  (`position.y = size.y * 0.5`). У бойца НЕ ТАК И НЕ БЫЛО НИКОГДА — его квад
  привязан к земле ЦЕНТРОМ КАРТИНКИ (`Unit.SPRITE_BASE_Y` = 0.42 м), а между
  низом квада и ступнями лежит пустое поле кадра: 56 px из 192 у рабочего и
  122 px из 320 у копейщика. Правило было перенесено из одного шейдера в другой
  ДОСЛОВНО и записано в комментарий как утверждение факта — то есть получило
  вид проверенного. Ошибка глубины равна этому полю в метрах: **0.87 м у
  рабочего, 1.85 м у копейщика**, и грунт честно перекрывал всё, что ниже, —
  у рабочего оставалась макушка, у копейщика одно остриё пики. **Чем выше
  лист, тем глубже тонет.**
- **ЧИСЛО ПРИХОДИТ ОТ ТОГО, КТО ЕГО ЗНАЕТ** (`mm_unit_sprite`, uniform
  `foot_drop` ← `FarUnitRenderer.Bucket.set_foot`). Это РОВНО `base_y` слота,
  уже растянутая: шейдер отсчитывает вниз ОТ НАЧАЛА КООРДИНАТ СЛОТА, а оно и
  стоит на `грунт + base_y`. Первая попытка подала сюда «расстояние от низа
  квада до ступней» (`half_h - base_y`) — число осмысленное и как раз равное
  прежней ошибке, но вычитаемое не оттуда: остаётся `half_h - 2*base_y`, то
  есть у копейщика 1.25 м из прежних 1.85, и армия тонет по-прежнему, только
  мельче. Поймал `qa_visual_smoke`.
- **ПОЧЕМУ UNIFORM, А НЕ ДАННЫЕ ЭКЗЕМПЛЯРА.** Все четыре канала instance-цвета
  заняты (кадр, зеркало, вспышка, доля жизни), а `custom_data` в GL
  Compatibility держит открытый баг движка (см. шапку `FarUnitRenderer`).
  Ручка при этом ОТРЯДНОЙ величиной и не является: бакет заведён ПО ЛЕНТЕ,
  а привязка ног — свойство ленты. Ставится она при регистрации первого бойца
  и при обновлении внешности, но **записью только на изменение**: установка
  uniform'а — обращение в сервер отрисовки, а число постоянно, пока бакет жив.
- **ПОДАВАТЬ НАДО В ОБОИХ МЕСТАХ.** У копейщика `sheet_frame()` отдаёт общий
  `SPRITE_BASE_Y`, а настоящая привязка ленты (`_sprite_base_y`, компенсация
  разных направленных листов) доезжает только до пути обновления внешности.
  Ставить ручку лишь при регистрации значит промахнуться ровно на неё.

## Визуальный дым: объект на экране виден целиком

**`qa_visual_smoke/Shot` (ОКОННЫЙ, 19 проверок).** Заведён потому, что правка
глубины утопила всю армию в грунт, а **шлюз из тридцати пяти стендов остался
зелёным**: он весь headless, а там буфер глубины не заполняется вовсе и не
компилируется ни один шейдер. У утонувшего бойца при этом не меняется НИ ОДНО
число, которое стенды проверяют, — та же позиция, та же строка в ядре, тот же
запас жизни. **Ловить это можно только по картинке.**

- **ДВА СНИМКА ОДНОГО КАДРА:** без объектов (фон) и с объектами. Видимая часть —
  пиксели, ИЗМЕНИВШИЕСЯ между снимками внутри экранного прямоугольника квада.
  Метрика не зависит ни от цвета травы, ни от того, что ещё стоит на карте.
- **ВЕРДИКТА НА ОБЪЕКТ ДВА, И ЛОВЯТ ОНИ РАЗНОЕ.** «Силуэт цел» — доля видимых
  пикселей от ожидаемых (порог 80 %). «Низ рисунка на месте» — где лежит нижняя
  видимая кромка относительно нижней НАРИСОВАННОЙ, **в метрах**: это СВОЙСТВО,
  эталона не требует вовсе и называет поломку числом (1.85 м у копейщика).
- **ЭКРАННАЯ ПЛОЩАДЬ КВАДА — ПРОЕКЦИЯ ЕГО УГЛОВ, А НЕ АРИФМЕТИКА С КОСИНУСАМИ.**
  У бойца компенсация наклона вшита в размер меша, у дерева её делает шейдер;
  одной формулы на оба случая нет, а камера знает правду про оба.
- **ОЖИДАНИЕ СЧИТАЕТСЯ ПО МОДЕЛИ ОТРИСОВКИ.** У бойца срез 0.15, и впечатанная
  в арт тень идёт ДИЗЕРОМ (рисуется с вероятностью, равной альфе) — покрытие
  есть СУММА альфы; у декорации срез 0.5 без дизера — их СЧЁТ. **Нижнюю кромку
  при этом судят по порогу 0.5, а не 0.15:** дизер тени разность снимков ловит
  неохотно, и у целого бойца выходило «кромка поднята на +0.30 м» на ровном
  месте.
- **ЛОВУШКИ САМОГО СТЕНДА, ВСЕ ТРИ ДАВАЛИ ЛОЖНЫЙ ВЕРДИКТ.** Шейдер опознавать
  по `resource_path`, а НЕ подстрокой в `shader.code` (имя файла внутри файла
  упоминаться не обязано — не нашлось ни одного слоя армии, прятать было
  нечего, и разность двух ОДИНАКОВЫХ снимков дала «видно 0 px» у всех сразу).
  Высота кадра обязана вмещать и ширину ряда, и САМЫЙ ВЫСОКИЙ квад (срезанный
  верх замка читался как потеря силуэта). **У дерева нет узла вовсе** — лес
  рисуется общим `VegetationRenderer`, квад бакета ЕДИНИЧНОЙ высоты, настоящий
  размер приходит масштабом слота: брать его надо у `ResourceNode._veg_slot`, а
  не искать «ближайший экземпляр» в буфере (освобождённые слоты остаются там со
  своими старыми координатами).
- **ЗАМОК И ДЕРЕВО В СТЕНДЕ — ЭТО КАЛИБРОВКА, А НЕ ПОЛНОТА.** Они правкой не
  затронуты, и их зелёные 99 % доказывают, что метрика меряет поломку, а не
  «всё подряд». Стенд без такой пары не годится вовсе.

## Помощь на стройке: механизм цел, жалобу воспроизвести не удалось

**Стенд — `qa_helpbuild` (12 проверок, headless).** Заведён по повторной жалобе
«второй рабочий встаёт в ступор при помощи на стройке» (та же, что однажды уже
чинилась защёлкой прихода `Worker._build_settled`).

- **НИ ОДНА ПРОВЕРКА НЕ СБРАСЫВАЛАСЬ.** Замерено: защёлка `_build_settled`
  взводится, трое рабочих встают в артель (`builder_count` растёт), диспетчер
  `_try_join_construction` доводит приказ, клик по площадке берёт ЕЁ (и вверху
  рисунка, и по низу фундамента, и полной маской `order_pick_mask()`), а
  **переназначение С РУБКИ на стройку чистое**: рабочий бросает дерево, реально
  проходит расстояние и встаёт в артель.
- **ЖАЛОБА ПРИ ЭТОМ НАСТОЯЩАЯ, И ЭТОТ СЦЕНАРИЙ ЕЁ НЕ ЛОВИТ.** Записано, чтобы
  следующий заход не начинал с нуля и не «чинил» целое.
- **ГЛАВНЫЙ ПОДОЗРЕВАЕМЫЙ НА СЛЕДУЮЩИЙ ЗАХОД — НЕ СТРОЙКА, А ЗАСТРЯВШИЙ
  РАБОЧИЙ.** На скриншоте владельца рабочий стоит ВНУТРИ дерева и дёргается на
  месте. Такой не дойдёт никуда, и «ступор у стройки» — то, как это выглядит.
  Зонд ставит рабочих в чистом поле и потому этого не видит: сценарий надо
  начинать с бойца, застрявшего в стволе.

### Правила для стендов, которые целятся по экрану

Все три пойманы на себе в этом заходе, каждое давало УВЕРЕННЫЙ ложный диагноз.

- **КАМЕРУ ЗАМОРАЖИВАТЬ** (`main.set_process(false)`, `cam.set_process(false)`)
  **и пересчитывать экранную точку В ТОМ ЖЕ КАДРЕ, в каком делается разбор.**
  Объектив продолжает доводить фокус, и точка, посчитанная один раз, через
  десяток кадров указывает в другое место. Зонд трижды подряд «нашёл
  регрессию»: первый разбор брал стройку, следующий — то пустую траву (цель
  null), то случайное дерево, и из этого уже был построен вывод про
  `LAYER_RESOURCES`. Все три находки оказались дрейфом объектива.
- **СТЕНД НА СЛУЧАЙНОЙ КАРТЕ — НЕ ДОКАЗАТЕЛЬСТВО.** Два прогона одного кода
  дали `null` и «ДЕРЕВО»: лес вокруг площадки сеется заново. Сценарий,
  зависящий от окружения, обязан ставить это окружение сам (вычистить
  `resource_nodes` вокруг и посадить своё дерево), а не надеяться на зерно.
- **КЛИК ПРОВЕРЯТЬ ПОЛНОЙ МАСКОЙ** (`order_pick_mask()`), а не «юниты плюс
  постройки»: у выделенного РАБОЧЕГО маска шире (в ней ресурсы), и узкая
  зелёная там, где игра могла бы краснеть.
- **ОШИБКА ВНУТРИ КОРУТИНЫ СТЕНДА УБИВАЕТ ЕЁ МОЛЧА.** Сцена живёт, вывод в
  трубу буферизован и наружу не попадает вовсе, прогон висит минутами без
  единого признака (поймано на `sm.on_selection_changed()` — метод живёт у
  `GameManager`, не у `SelectionManager`). **Сторож-таймер обязателен**;
  в этом заходе он понадобился трижды — `qa_visual_smoke`, `qa_audio3`,
  `qa_helpbuild`.

## Разлёт от тарана виден глазом

- **Виноват был НЕ удар, а порог привязки картинки.** Спрайт и так догоняет
  тело плавно (`Unit._smoothed`), но прыжок дальше `VIS_SNAP_SQ` (2 м) считается
  телепортом и рисуется мгновенно — иначе спрайт полз бы через полкарты после
  гарнизона или постановки стендом. Отбрасывание при таране равно
  `charge_knockback` = 2.2 м, то есть ровно ЗА этим порогом: удар честно двигал
  бойца, а картинка честно телепортировалась следом.
- **Лечение — метка `_fling_left`**, а не поле скорости. Пока метка жива,
  предохранитель не срабатывает, а доля догона берётся своя (`FLING_LERP`,
  помедленнее обычной). Импульс потребовал бы своего поля и своей ветки в тике
  ради эффекта длиной в полсекунды, а физтел в игре нет вовсе.
- **Летящий не засыпает по картинке** — по той же причине, что и боец с
  непогасшей вспышкой: уснувший перестаёт тикать, и спрайт остался бы лежать
  там, откуда бойца сбило. Такт метки идёт там же, где такт вспышки.
- Стенд — `qa_cavalry`, F5в/F5г.

## Конец партии

- **Победа объявляется по СНЕСЁННОЙ КРЕПОСТИ и отсутствию живых**, а не по
  полностью пустой карте (`Main._faction_beaten`). Прежнее условие требовало
  нуля зданий и висело, пока игрок не обойдёт карту за последним домиком.
- **ИИ не закладывает крепость, когда строить её некому** (`_has_living_units`).
  Закладка проверяла только деньги: фундамент вставал, попадал в группу зданий
  фракции, достроить его без единого рабочего было нельзя — и условие победы не
  выполнялось НИКОГДА. Это и есть «крепость уходит в режим повторной постройки».
- **Живые считаются проверкой `is_dead()`, а не размером группы:** павший уходит
  из неё лишь в конце кадра (`queue_free` отложен).
- **ПОРАЖЕНИЕ ИГРОКА — ДВА ПРАВИЛА (10.09.2026, `Main._player_defeated`):**
  крепость снесена И отстроить нельзя (цена замка не по карману ИЛИ нет живого
  рабочего и нет заложенной крепости); либо ни построек, ни бойцов не
  осталось — сразу. Пока крепость стоит или её можно отстроить — играем.
- Стенд — `qa_bugpass`, блок I.

## Урон видно на бойце: вспышка и окровавленность

- **Оба состояния едут в СВОБОДНЫХ каналах instance-цвета** (`COLOR.b` — сила
  вспышки, `COLOR.a` — доля жизни), где их читает `mm_unit_sprite.gdshader`.
  Ноль новых узлов, ноль новых листов спрайтов и **ноль дополнительных вызовов
  отрисовки** — замер `qa_wound/Shot`: 52 вызова и у целого строя, и у
  окровавленного, и в момент вспышки. Раненого арта не существует и не будет,
  поэтому «подбитость» делается подкраской той же текстуры.
- **Отклик на удар пишется В ТОМ ЖЕ ФИЗИЧЕСКОМ КАДРЕ** (`take_damage` →
  `_push_damage_shade`), а не ближайшим визуальным тиком. Тик шардирован: на
  большой армии боец получает его раз в три-четыре кадра, и вспышка длиной
  `HIT_FLASH_SEC` (0.07 с) успевала бы догореть до первой отрисовки. Наружу при
  этом ничего не уходит — запись идёт в теневой буфер бакета, а он подаётся
  одним `flush()` в конце кадра, как и всегда.
- **`Bucket.write()` обязан переписывать и эти два числа.** Полная запись идёт
  при СМЕНЕ ЛЕНТЫ, то есть при каждом переезде в другой бакет, а в свалке они
  постоянны: с прежними константами «вспышки нет, боец цел» раненый мигал бы
  чистым спрайтом всю драку. Стенд `qa_wound`, проверка D2.
- **С непогасшей вспышкой боец НЕ ЗАСЫПАЕТ.** Уснувший перестаёт тикать, а
  значит и гасить вспышку: в буфере навсегда осталось бы подмешанное белое, и
  обстрелянный неподвижный строй стоял бы засвеченным.
- **Лечение ловится сравнением, а не вызовом.** Лечат бойца из четырёх разных
  мест; вместо оповещения из каждого визуальный тик сверяет `current_health` с
  тем, что уже отдано в отрисовку. Цена на целом неподвижном строю — два
  сравнения float на бойца и НИ ОДНОЙ записи (стенд `qa_wound`, F1/F2).
- **Одним множителем крови не добиться.** У копейщика синий плащ, красного
  канала в нём почти нет, и любой множитель делает его только ТЕМНЕЕ — первый
  снимок дал не «окровавлен», а «посерел». Поэтому поверх множителя идёт
  крохотная ДОБАВКА бурого (`wound_blood`), и в тех же тёмных участках. Больше
  — и силуэт светлеет, теряя рисунок, то есть возвращается та самая «грязь»,
  ради ухода от которой в `mm_corpse` выбран множитель.
- Ручки: пороги, цвета и пульсация — uniform'ы шейдера (`wound_hi` 0.85,
  `wound_lo` 0.40 и ниже), длительность и пик вспышки — `Unit.HIT_FLASH_SEC` /
  `HIT_FLASH_PEAK`. Пик НЕ единица намеренно: полностью белый силуэт стирает
  рисунок и в свалке читается как дыра в строю.
- Стенды: `qa_wound/Test` (16 проверок, headless — доезжают ли числа до буфера
  и когда) и `qa_wound/Shot` (оконный — лесенка 100/90/70/50/30/10 % запаса,
  вспышка, раненые в строю; пороги там не проверяются, их судят глазом).

## Разбор кадра и цена тика

- **Мониторы `Performance.TIME_PHYSICS_PROCESS`/`TIME_PROCESS` для разбора
  кадра НЕ ГОДЯТСЯ** — при снятом ограничении кадров они усредняются по кадрам
  отрисовки и на одной и той же сцене дают то 10, то 20 мс. Честные числа даёт
  `perf_config.tick_meter` / `vis_meter`: одна пара `get_ticks_usec` вокруг
  ВСЕГО `_physics_process` и ВСЕГО `_process`.
- **Физика идёт фиксированные 60 Гц.** При 50 к/с на кадр приходится 1.2
  физического тика, и цена тика НА КАДР выше самого тика. Без этой поправки
  разбор кадра не сходится — именно из-за неё A/B-замер «выключить тик армии»
  показывал 3.3 мс там, где счётчик даёт 8.
- Разбор на 4000 юнитов, 1920x1080 (`qa_fps`, авг. 2026): кадр ~19.5 мс =
  физтик ~9.5 + кадр логики ~4.5 + движок и рендер ~5.5, **50-52 FPS**.
  **Отрисовка не при чём**: ~250 вызовов, скрытие всех спрайтов армии даёт
  меньше миллисекунды.
- **«ОТРЯД В БОЮ» СПРАШИВАЛИ ИЗ ПОКАДРОВОГО ПУТИ, А СЧИТАЛИ ОБХОДОМ СОСТАВА —
  ЭТО БЫЛА КВАДРАТИЧНОСТЬ ПО РАЗМЕРУ ОТРЯДА.** `GameManager.squad_in_combat`
  зовут `Unit._phalanx_dir` (то есть каждый копейщик обороны на каждом ходу
  строя), `_check_auto_aggro` и `_update_live_rank`, а ответ строился через
  `squad_members()` — тот на КАЖДЫЙ вызов собирает новый массив живых и
  обходит состав. Отряд из шестидесяти, спрашивающий сам себя шестьюдесятью
  бойцами шестьдесят раз в секунду, — это 3600 посещений и 60 массивов в
  секунду НА ОДИН ОТРЯД. Дёшево это выглядело только из-за раннего выхода по
  окну «нас недавно задели» (3 с), которое закрывает разгар свалки, — а на
  СБЛИЖЕНИИ и в стоянии под обстрелом окно молчит, и платился полный обход.
  Лечение — ответ на ОДИН ФИЗИЧЕСКИЙ КАДР (`perf_config.squad_combat_cache`,
  пункт 1б) плюс чтение состава НАПРЯМУЮ, минуя `squad_members()`: тот не
  читатель, он распускает опустевший отряд прямо в геттере.
  **Замер `qa_ab` (1440 копейщиков, 24 отряда, чередование, 6 раундов):**
  стояние в стойке ЗАЩИТА в виду друг друга — физтик **14.89 → 6.17 мс**
  (−8.7 мс, 58 %); та же сцена в стойке АТАКА — 2.76 → 2.67 мс, то есть
  в пределах разброса. Разница между сценариями и есть доказательство, что
  меряется именно эта ветка: в атаке фаланговый путь не работает вовсе.
- **A/B ЧЕРЕДОВАНИЕМ ТЕПЕРЬ ЕСТЬ ОТДЕЛЬНЫМ СТЕНДОМ — `qa_ab` (headless).**
  Он поднимает живую сцену, снимает базу и правку ПОДРЯД по `tick_meter` и
  повторяет это раундами, поэтому дрейф машины попадает в обе выборки
  одинаково. Ручку правки берёт из `perf_config`; чтобы померить следующую
  оптимизацию, ей нужен такой же выключатель.
- **ВИЗУАЛЬНЫЙ ПРОХОД ИДЁТ НА ПЯТИ ШАРДАХ** (`perf_config.vis_shards_extra`
  = 2 при армии от `vis_extra_from`). Это единственная правка того захода,
  которая выдержала честный замер: кадр логики −0.7…−0.9 мс, FPS +1…+5, физтик
  НЕ меняется — шарды движения так и остались тремя. Цена — примерно +0.1 м к
  отставанию нарисованной точки от логической; побочная выгода — кольцо
  выделения и спрайт обновляются одним шардом и больше не расходятся вовсе.
- **Разброс между прогонами ±3-5 к/с на НЕИЗМЕННОМ коде.** Правку меньше чем на
  миллисекунду этим замером подтвердить нельзя — но и «по веткам профиля» её
  подтверждать НЕЛЬЗЯ ТОЖЕ (см. `docs/PERF_6000.md`, раздел 6): у ветки,
  которая идёт тысячу раз в кадр, измеритель стоит соизмеримо с ней самой.
  Способ один — A/B по `tick_meter` ЧЕРЕДОВАНИЕМ, база и правка подряд.
- **Ряды фаланги считаются ПАЧКОЙ НА ОТРЯД** (`ArmyCore.SquadRanks` ←
  `GameManager._push_squad_ranks`, зовётся из `_recalc_corridor`: там уже
  собраны состав и строки). Персональный `allies_ahead` стоил 8.3 мкс на
  бойца, из которых сам скан 2.5 — остальное путь наружу. Направление берётся
  ОДНО на отряд, двумя обращениями к словарю; спрашивать `_phalanx_dir()` у
  каждого бойца НЕЛЬЗЯ — первая версия так и делала, и ветка `squad_corridor`
  выросла с 0.78 до 2.19 мс. Отряд без курса считает ряды по-старому.
- **Тик юниту переключают только `Unit.set_tick()` / `set_draw()`.** Они ведут
  поля-зеркала `tick_on`/`draw_on`, по которым идёт обход армии: прежние
  `is_processing()` / `is_physics_processing()` — обращения в движок, и их было
  под две с половиной тысячи в кадре. Переопределить сами движковые методы
  нельзя, GDScript считает это ошибкой, поэтому имена свои.

## Цель приказа: чужой боец и чужое ЗДАНИЕ равноправны

- **Здание было законной целью и раньше**: маска клика включает
  `LAYER_BUILDINGS`, ПКМ доходит до `command_attack`, лучник по стене стреляет.
  Не было ОБРАТНОЙ СВЯЗИ — весь путь подсветки был написан под
  `pick["target"] as Unit` и на постройке молча отдавал null. С точки зрения
  игрока это и есть «здание нельзя выбрать целью»: единственный способ узнать,
  сработало ли, — кликнуть и смотреть, полетят ли стрелы. Лечится
  `SelectionManager.enemy_building_under_cursor` и приёмом `Building` в оба
  слоя колец (`set_hover_units` / `set_order_targets`).
- **Кольцо под зданием растягивается по его основанию**
  (`SelectionDecalRenderer.target_ring_scale`), и масштабируются ТОЛЬКО оси X и
  Z: тор лежит горизонтально, и общий масштаб поднял бы его над землёй колесом.
- **Дистанция до здания меряется до СТЕНЫ, а не до центра коробки**
  (`Unit._target_pad` / `reach()`). `global_position` постройки лежит в центре,
  а коробка бывает восемь метров: пока считали до центра, «дойти на дистанцию
  удара» означало для копейщика ВОЙТИ ВНУТРЬ здания (масок столкновений нет, он
  туда честно заходил), а для лучника — встать вплотную к стене вместо
  двадцати метров. Поправка держится ПОЛЕМ и считается один раз при смене цели:
  дистанцию читают четыре горячие ветки в каждом кадре, и проверять там тип
  цели значило бы платить приведением типа за бойца, у которого поправка ноль.
- Стенд — `qa_bldattack` (12 проверок).

## Расформирование отряда (клавиша Delete)

- `SelectionManager.disband_selected()`: боец получает СМЕРТЬ, а не
  `queue_free`. Смерть — единственный путь, на котором он снимает с себя строку
  в ядре армии, место в сетке, счётчик атакующих у цели и место в отряде, а на
  поле оставляет ТЕЛО. Прямое удаление узла обошло бы всё это.
- Только СВОИ и только бойцы: здания попадают в `selected_units` (панель замка
  работает через то же выделение), и снос собственного замка по случайному
  нажатию — не та цена, которую игрок готов платить за удобство.
- Список копируется ДО первой смерти: гибель дёргает `remove_from_squad`,
  `_disband_squad` и `on_selection_changed`, то есть правит тот самый массив.
- Стенд — `qa_corpse`, блок N.

## Поле боя: тела павших и стрелы

- **Труп — декорация, а не юнит.** `CorpseRenderer` (слой `GameManager.corpses`)
  кладёт тело слотом в общий `MultiMesh`, узла на павшего нет. Ставится из
  `Unit._die()` → `_leave_corpse()`, то есть ПОСЛЕ того, как боец снял с себя
  строку в ядре армии, сетку и тик. Ленту, кадр и масштаб труп берёт из самого
  бойца (`Unit.sheet_frame`) — поэтому работает для всех фракций и родов войск
  без единой развилки.
- **Труп лежит, а не стоит:** свой шейдер `mm_corpse.gdshader` БЕЗ билборда,
  базис кладёт квад плашмя и крутит вокруг вертикали. Угол и зеркало выводятся
  из номера узла — детерминированно, иначе стенд не проверить.
- **Угол тела ОГРАНИЧЕН, а не свободен** (`COFFIN_SPREAD` ±45° вокруг
  «поперёк экрана», опора — `_screen_side_yaw` от камеры). Рисунок на квадре —
  стоящая фигура: совпади её ось с вертикалью экрана, и лежащий неотличим от
  стоящего. Именно это выглядело как «труп с копьём в небо».
- **Поза копейщика — ЩИТ (`defence_*`), а не покой.** В `idle` у него копьё
  вертикально вверх, и это давало тот же эффект «стоит».
- **Поза павшего — покой, а не та, в которой застала смерть** (`Unit.corpse_frame`,
  своя ветка у `Spearman` с его направленными листами). У ленты удара нарисован
  белый след — на завале эти следы читались как клочья тумана.
- **Тело НЕПРОЗРАЧНО, растворение — дизером.** Полупрозрачные трупы мерцали:
  прозрачные объекты сортируются по расстоянию, у каждой ленты свой MMI, и
  пласты менялись местами при движении камеры. Плюс своя ступенька высоты
  каждому телу (`LAYER_STEP`, 64 слоя по мм) — против z-fighting в завале.
- **ЧЁРНЫЕ КЛЯКСЫ ПОД БОЙЦАМИ И ТРУПАМИ ДАВАЛ АРТ, А НЕ ТЕНИ.** В каждом листе
  юнита под ступнями впечатан овал полупрозрачной краской: во всех 361 листах
  `assets/factions` частичных значений альфы ровно три — 69, 79 и 80 из 255.
  Материал спрайта НЕПРОЗРАЧНЫЙ (срез по альфе, иначе в строю мерцает
  сортировка), а срез не умеет полутонов: при пороге 0.15 краска проходила и
  печаталась ПЛОТНЫМ пятном (15,18,26). У живого это чёрная лужа под ногами,
  у лежащего — то же пятно СБОКУ ОТ ГОЛОВЫ, потому что квад повёрнут плашмя.
  Лечение разное: живому пятно возвращено ДИЗЕРОМ (`mm_unit_sprite`,
  упорядоченная матрица 2×2 в координатах экрана — покрытие, равное альфе),
  трупу срезано целиком (`mm_corpse`, порог 0.5). **Порог среза у трупа и у
  живого разный намеренно, выравнивать нельзя.**
- **Тень трупа своя** (`ShadowLayer` + `UnitVisuals.corpse_shadow_mesh`):
  КРУГЛОЕ пятно, НЕ поворачивается вместе с телом (овал по оси лежащего
  читался как «тень стоит»), `SHADOW_FRAC` 0.26 квада ≈ 0.9 м, альфа 0.16 —
  вдвое бледнее тени живого (0.28), потому что на завале тени складываются.
  Тугим его сделали и ради перерисовки: было 0.55 квада, то есть под каждым
  телом полупрозрачный блин почти в два метра. Ещё туже (0.15) нельзя — тело
  лежит НАД своей тенью и целиком её закрывает, видно только ободок.
- **ЗАВАЛ ЧИТАЕТСЯ ОБЪЁМОМ, А НЕ КОВРОМ.** Три вещи, все даром:
  притенение слоёв (`TINT_DARKEST` 0.62 → 1.0, множитель едет в альфе
  instance-цвета и считается один раз при укладке), разнокалиберность тел
  (`SIZE_JITTER` ±7%, вписан прямо в базис трансформа) и общее пятно под кучей
  — это перекрывающиеся тени тел, отдельного слоя под них не заводили.
  Притенение УМНОЖАЕТ цвет, а не подмешивает чёрный: пиксель-арт держит свою
  палитру, и подмес постороннего цвета выглядит грязью. Нижняя граница 0.62
  обязательна — ниже возвращается та самая «чёрная клякса».
- **Потолок `MAX_CORPSES` = 5000, срок жизни 15 минут, разложение 30 секунд.**
  Число вызовов отрисовки от потолка не зависит вовсе — оно равно числу РАЗНЫХ
  ЛЕНТ на поле. Единственная статья, которая растёт, — подача буфера
  (`set_buffer` гонит бакет целиком, 320 КБ на пять тысяч), поэтому доля
  разложения уезжает в буфер шагами `DISSOLVE_WRITE_STEP`, а не каждый кадр.
- **Срок жизни проверяется ТОЛЬКО У ГОЛОВЫ ОЧЕРЕДИ** (`CorpseRenderer._scan`).
  Очередь упорядочена по времени укладки, срок у всех один — значит истекает он
  строго по порядку. Спрашивать возраст у каждого из пяти тысяч означало бы
  платить пятью тысячами сравнений в кадр за событие раз в четверть часа.
  Снятые места выбрасываются пачкой (`_compact`), а не `erase` по значению.
- **Часы у слоя СВОИ, а не `Time.get_ticks_msec()`**: на паузе тела обязаны
  лежать, а не доживать свои пятнадцать минут за спиной у игрока. Стенду они же
  дают возможность не ждать четверть часа взаправду (`age_shift`).
- **Разложение — это НЕ просто прозрачность.** Сперва тело землеет и темнеет
  (шейдер считает это из той же доли `COLOR.b`), и только потом рассыпается
  дизером (`dither_start`). Тело, начавшее редеть с первой секунды, полфазы
  стоит полупрозрачным, и на завале сквозь него видно соседние тела.
  Бурый ЗАМЕЩАЕТ обесцвеченный тон, а не подмешивается к исходному: подмес дал
  бы грязь поверх рисунка (та же причина, что у притенения слоёв).
- **Растворений два, и путать их нельзя:** `DISSOLVE_SEC` (30 с) — штатное
  разложение по сроку жизни, `FADE_SEC` (2 с) — спешное вытеснение по потолку.
  Доля в буфер считается от СВОЕЙ длительности (`Corpse.fade_total`), иначе
  вытесненное тело прыгало бы на пятнадцатую долю сразу.
- **Против лимита считаются ТОЛЬКО негаснущие:
  догорающий остаётся в списке, и учёт по всей длине давал лавину — каждая
  новая смерть отправляла гаснуть ещё одно тело, и поле после боя оставалось
  пустым.
- **Стрела — это УЗЕЛ со своим мешем и своим материалом, то есть отдельный
  вызов отрисовки.** Общего `MultiMesh` у стрел нет и быть не может даром: ось
  у каждой своя, квад разворачивает вокруг неё шейдер. Замер на живой сцене
  (`qa_shotcorpse`): 36 торчащих стрел добавляют к кадру ровно 36 вызовов.
  Отсюда два ограничителя, и оба обязательны:
  - `CorpseRenderer.MAX_ARROWS_PER_CORPSE` = **1**. Убит стрелой — в теле
    остаётся ТА САМАЯ стрела; убит в рукопашной — ни одной, и это выходит само
    собой, потому что втыкать их некому. Прежнее «две-три при залпе» требовало
    РОЖДАТЬ недостающие отдельными узлами, и пятьсот тел стоили полутора тысяч
    вызовов отрисовки;
  - `GameManager.MAX_STUCK_ARROWS` = **160** на всё поле, вытеснение самых
    старых через `fade_out_in`. Против потолка считаются ТОЛЬКО негаснущие —
    иначе лавина, та же, что ловилась на телах. Срок при этом остался
    заказанным: ограничивается ЧИСЛО, а не время.
- **СРОК ТОРЧАЩИХ СЧИТАЕТ ОДИН ОБХОД, А НЕ СТО ШЕСТЬДЕСЯТ УЗЛОВ**
  (`GameManager._sweep_stuck_arrows`, вход `Arrow.tick_stuck`). Воткнувшаяся
  стрела держала СВОЙ `_process` ради одного сложения float — нотификация
  движка на каждую декорацию в каждом кадре. Реестр торчащих здесь и так есть
  (он держит потолок), и обойти его циклом дешевле, чем сто шестьдесят раз
  войти в GDScript из движка; тот же приём и та же причина, что у тел павших.
  **Обход идёт С КОНЦА:** истёкшая стрела уходит в пул сама, а `_despawn`
  зовёт `forget_stuck_arrow`, то есть правит этот же массив посреди цикла.
  Живёт обход в `_process`, а не в физике: считается РАСТВОРЕНИЕ, то есть
  картинка, и на паузе стрелы обязаны стоять — ровно так вёл себя их
  собственный `_process` до переезда.
- **Стрела по ЗДАНИЮ бьёт по назначенной цели, а не сканом.** Бойца стрела
  находит сама (`ArmyCore.EnemyAt`), но здание в сетке соседей не состоит, и
  выстрелы по стенам просто втыкались в землю. Перебирать группу зданий на
  каждую стрелу в каждом кадре полёта нельзя — стрелок и так знает свою цель и
  кладёт её в `Arrow._hit_node`; проверка одна и в самом конце дуги.
- **Сроков у стрелы два, и путать их нельзя:** `MAX_FLIGHT_SEC` (10.5) —
  страховка от зависшей в полёте, `STUCK_LIFETIME` (45) — сколько торчащая
  лежит на поле, с растворением за `STUCK_FADE`. У стрелы в теле СВОЕГО срока
  нет (`Arrow._in_corpse`) — она видна, пока видно тело, тело же включает ей
  растворение (`Arrow.fade_out_in`), и `_process` у неё ВЫКЛЮЧЕН: считать
  нечего. На время растворения снимается порог среза альфы: срез не умеет
  гасить плавно.
- **ЧЁРНЫЙ ПРЯМОУГОЛЬНИК ПОД ГАСНУЩЕЙ СТРЕЛОЙ — ЭТО СНЯТЫЙ ПОРОГ СРЕЗА.**
  Растворение делалось так: `modulate.a` вниз и `alpha_scissor` в НОЛЬ, «срез
  не умеет гасить плавно». Но материал со срезом Godot относит к НЕПРОЗРАЧНОМУ
  проходу, записанную `ALPHA` там не читают вовсе, и порог оставался
  единственным, что отсекало пустоту вокруг стрелы. Обнулили порог — весь квад
  поехал на экран своим цветом (0,0,0). **Порог среза не снимать НИКОГДА.**
  Растворение делает дизер 4×4 в координатах экрана (`axis_billboard`,
  uniform `fade`) — тот же приём, что у тел и у тени бойца. Стенд `qa_corpse`,
  проверки K1/K2.
- **Угол втыкания разбросан на ±12°** и по вертикали, и вокруг неё
  (`Arrow._stick_jitter`). Одного поворота вокруг вертикали мало — стрелы легли
  бы веером, но всё так же одинаково круто. Считается от точки, а не через
  `randf()`: два прогона одного боя обязаны дать одно поле.
- **Воткнувшаяся стрела стоит круто** (`STUCK_MIN_DOWN` 1.0 → ровно 45° после
  нормировки). Прежние 0.35 давали девятнадцать градусов, и при камере в 45°
  такая стрела читалась не как воткнувшаяся, а как зависшая в воздухе —
  горизонтальное древко поверх спрайтов дерущихся.
- **Попадание ищет солвер, а не GDScript** (`ArmyCore.EnemyAt` →
  `SpatialGrid.enemy_at`). `query_radius()` строил новый Godot-массив на каждую
  стрелу в каждом кадре полёта; в воздухе висит четверть всех стрелков.
- Крови и ран нет намеренно — это первая очередь. Стенды: `qa_corpse` (41
  проверка, headless) и `qa_shotcorpse` (оконный: снимки живых, завала,
  одиночных тел и стрел + счёт вызовов отрисовки). Знамёна снимает
  `qa_banner/Shot`, а не он: дублировать снимок незачем.

## Панель ветеранства: выбор награды (редизайн авг. 2026)

Семь пунктов заказа владельца по скриншотам. Стенды — **`qa_vetui/Test`**
(40 проверок, headless) и **`qa_vetui/Shot`** (оконный: панель, наведение,
значок ранга).

- **ЗНАЧОК ЗАСЛУЖЕННОГО РАНГА ПОКАЗЫВАЕТ ЛЫЧКИ, А НЕ ЗВЕЗДУ**
  (`HUD._add_alert_chevrons`). Горят ТЕКУЩИЕ лычки, мигает ЗАСЛУЖЕННАЯ, и у
  них РАЗНЫЕ твины: «уже моё» и «вот-вот моё» должны читаться порознь.
  «Текущее» считается как `squad_level − squad_pending`, а не по уровню
  отряда: уровень поднимается в момент заслуги, и по нему новая лычка уже
  горела бы. Глифы берутся из `veteran_badge_text` — того же места, что у
  портрета; разбираются посимвольно, потому что грейд меняет не только ЧИСЛО
  лычек (на 4-м они разворачиваются, 3-й и 6-й добавляют наконечник): общий
  префикс — «есть», хвост — «прибавится», префикса нет — мигает весь значок.
- **ДЛИННОЙ ПОДПИСИ У ЗНАЧКА НЕТ ВОВСЕ** (`tooltip_text` снят): строка
  «Отряд опытных копейщиков — заслужен ранг…» растягивалась через полверха
  экрана. **Название отряда со званием при этом ОСТАЁТСЯ** и в подписи панели,
  и в шапке таблицы статов — прямое требование владельца, отданное поправкой
  по ходу правки. Убрана только СЛУЖЕБНАЯ строка «★ Ветеран N — выберите
  награду»; на её месте стоят флажки рангов.
- **КЛИК ПО ЗНАЧКУ ПЕРЕВОДИТ КАМЕРУ ПЛАВНО И РАБОТАЕТ НА ПАУЗЕ**
  (`RTSCamera.glide_to` → `_tick_glide`, `Main.glide_camera_to`). Награду
  выбирают на паузе, а `pan_to` швыряет объектив одним кадром. Глиссада
  считается в `_process` камеры — она в `PROCESS_MODE_ALWAYS`, своего таймера
  не нужно. **Любое ручное движение камеры глиссаду отменяет** (`_pan_by`,
  `pan_to`): камера, уползающая из-под пальцев, — худшее, что может быть в RTS.
- **ПОДСВЕТКА ОТРЯДА — ЭЛЕМЕНТ HUD, А НЕ ЕЩЁ ОДИН СЛОЙ КОЛЕЦ В МИРЕ**
  (`HUD._ping_squad` / `_tick_ping`). Слои колец ведёт `GameManager._process`,
  а он PAUSABLE: на паузе буфер не подаётся вовсе, и подсветка не появилась бы
  ровно в том случае, ради которого её просили. Плюс кольцо под ногами в гуще
  боя перекрыто спрайтами, а метка поверх толпы — нет. Кольцо рисуется кодом и
  кэшируется (`static var _ping_tex`), пульс — СЖАТИЕ к отряду: оно само
  указывает внутрь.
- **ЛЕВАЯ ОСЬ ОДНА НА ВЕСЬ ЛЕВЫЙ СТОЛБЕЦ** (`HUD.PANEL_LEFT`). Панель
  крепилась к 6, панель статов и полоса гарнизона — к 8; на стыке двух тёмных
  плашек ступенька в два пикселя видна сразу.
- **ИКОНКИ УЖЕ ВЗЯТЫХ НАГРАД ЖИВУТ В ПРАВОЙ ЧАСТИ ТАБЛИЦЫ СТАТОВ**, а не
  отдельным рядом под панелью: строки статов короткие, место справа пустовало,
  а лишний ряд снизу толкал нижнюю панель вверх. Ряд стал СЕТКОЙ в две колонки
  (`BONUS_COL_COUNT`) — семь иконок подряд в узкую колонку не влезают. Имя узла
  прежнее (`BonusRow`): по нему ряд ищут стенды.
- **КНОПКИ ВЫБОРА — КРУПНЕЕ НА 40% И С КВАДРАТНОЙ ТОНКОЙ РАМКОЙ**
  (`VET_BTN_SCALE`, `_style_vet_button`). Стиль ставится ПОВЕРХ того, что выдал
  `_cmd`: своей ветки в `_cmd` заводить нельзя — там стиль общий на все кнопки
  панели, и квадратная рамка расползлась бы на стойки, найм и приказы.
- **ФЛАЖКИ РАНГА «БЫЛО → СТАЛО» — ОБРЕЗОК НАСТОЯЩЕГО ЗНАМЕНИ** (`_vet_flag`,
  `AtlasTexture` по `BannerArt.texture_for`), а не свой набор значков: разойтись
  с знаменем в мире они так не могут. Первый уровень показывает ОДИН флажок:
  «было» у него нет вовсе, и рисовать пустую рамку значило бы врать.
- **ПРЕДПРОСМОТР НАГРАДЫ ПИШЕТСЯ В СВОЮ СТРОКУ ТАБЛИЦЫ** (`_preview_stats`,
  реестр `_stat_rows`/`_stat_base`). Строки статов заведены ПАРАМИ «ключ →
  текст», и ключ тот же, что у модификаторов после `modifier_stat_name`, —
  иначе пришлось бы искать строку по подписи и завести вторую таблицу
  соответствий. **Ничего «фиксировать» по клику не надо и НЕЛЬЗЯ:** клик
  применяет награду, панель пересобирается штатно, и числа приходят настоящими,
  из полей бойца. Фиксация предпросмотра была бы вторым источником правды о
  статах. Модификатор без своей строки (мораль, темп) в таблицу не попадает —
  он виден в плашке.
- **ПЛАШКА НАГРАДЫ — ИКОНКА И СПИСОК МОДИФИКАТОРОВ, БОЛЬШЕ НИЧЕГО**
  (`_show_vet_tip`). Прежние четыре служебные строки владелец вычеркнул все:
  «Награда за уровень N» повторяет флажки, «+1 к урону каждой модели» ВРАЛА
  (награда даёт НАБОР модификаторов, а строка печатала первый), «Убийств
  отряда» дублирует шкалу опыта, «Уже выбрано» — значок на портрете. Строки
  собираются из `nonzero_modifiers` в порядке `BONUS_KEYS`, подпись — из
  `HUD.MOD_SHORT_LABELS` (это способ НАЗВАТЬ число в интерфейсе; само число —
  из конфига, поэтому правка баланса доезжает сама).
- **ПЛАШКА ВСТАЁТ СПРАВА ОТ ПАНЕЛИ, А НЕ НАД КНОПКОЙ.** Общее правило
  интерфейса («окно строго над наведённой кнопкой») здесь не годится: над рядом
  наград висит таблица статов, и плашка легла бы ровно на неё — то есть закрыла
  бы предпросмотр, ради которого игрок и навёл курсор.

### Таблица статов: три колонки, значки вместо слов (авг. 2026)

Заказ владельца по скриншоту: панель уменьшить, длинные названия заменить
значками, модификаторы и знак `=` поставить строго друг под другом, итог —
по правому краю. Стенды: `qa_vetui` (45), `qa_hud5` (35), `qa_ui9` (43),
`qa_sel2` (35).

- **СТРОКА РАЗБИРАЕТСЯ НА ТРИ ЧАСТИ ЕЩЁ ДО СБОРКИ УЗЛОВ** (`HUD._stat_parts` /
  `_stat_parts_cut` возвращают `[название, середина, итог]`). Прежде строка была
  ОДНИМ текстом («Health 180 +14 = 194»), и выровнять в ней что-либо
  невозможно в принципе: у `Label` нет табуляторов, а колонки в моноширинном
  тексте разъедутся на первом же двузначном числе. `_stat_formula` осталась
  обёрткой, склеивающей те же три части, — её зовут подписи и стенды.
- **ПАНЕЛЬ — `GridContainer` НА ТРИ КОЛОНКИ** (`name = "StatGrid"`), а не VBox
  из строк. Выравнивание «модификаторы и `=` друг под другом» — это и есть
  определение сетки; сделать его из независимых строк нельзя, они не знают
  ширин друг друга.
- **`_stat_rows[key]` УКАЗЫВАЕТ НА ПРАВУЮ ЯЧЕЙКУ.** Предпросмотр награды
  (`_preview_stats`) дописывает прибавку в ту же ячейку, где стоит итог, и
  потому не изменился ни на строку: реестр строк вёлся и раньше, поменялось
  только то, КАКОЙ узел в нём лежит.
- **ЗНАЧКИ БЕРУТСЯ ИЗ `STAT_ICONS`, И ЭТО ТЕ ЖЕ ФАЙЛЫ, ЧТО У КАРТОЧЕК НАГРАД**
  (сердце — запас, меч — атака, щит — броня и защита, сломанный меч — интервал
  атаки, лук — дальность). Общий источник обязателен: разойдись значок в
  таблице со значком на карточке, и предпросмотр перестал бы читаться как
  «эта карточка качает вот эту строку». Стату без своего значка
  (скорость, напор, мораль) ячейка достаётся пустой — заводить ради них
  случайную картинку значило бы врать глазом.
- **РАЗМЕРЫ: `STAT_PANEL_W` 320 → 268, `STAT_FONT_PX` 12 → 11,
  `STAT_ICON_PX` 13, `STAT_TOTAL_W` 46.** Ширина итоговой колонки ЖЁСТКАЯ —
  без неё правый край прыгал бы от числа к числу, и «по правому краю»
  выполнялось бы только для самой длинной строки.
- **ЗНАК ПЕЧАТАЕТ ОДНА ФУНКЦИЯ** (`HUD._signed`). До неё минус приезжал из
  самого числа, а плюс дописывался вручную — и отрицательная прибавка выходила
  как «+ -1». Обе формулы (полная и урезанная) зовут её.

## Правка интерфейса и пивотов по скриншотам (авг. 2026)

Восемь скриншотов владельца, восемь разворотов и починок. Ниже только то, что
нельзя вывести из кода.

- **ШИРИНА ЛЕВОГО СТОЛБЦА — ОДНО ЧИСЛО НА ДВЕ ПАНЕЛИ** (`HUD._panel_w`). Было:
  у карточки статов ширина стояла константой (`STAT_PANEL_W`), а нижняя панель
  считала свою ПО СОДЕРЖИМОМУ — и содержимое почти всегда шире. Правый край
  нижней панели вылезал за карточку. Теперь ширина = максимум из «сколько нужно
  содержимому» и `STAT_PANEL_W`, и её получают обе. Минимум навязывается ТОЛЬКО
  пока карточка на экране: иначе «Ничего не выбрано» стало бы плашкой в треть
  экрана. Стережёт `qa_vetui` B4.
- **НАЗВАНИЕ ОТРЯДА СТОИТ РОВНО В ОДНОМ МЕСТЕ — В НИЖНЕЙ ПАНЕЛИ, ОДНОЙ СТРОКОЙ
  ОТ ЛЕВОГО КРАЯ.** Из шапки карточки статов оно убрано (дубль, одно под
  другим); исключение одно — карточка РАЗВЕДКИ (`live_hp`), там нижней панели с
  именем нет вовсе. Двухстрочность лечится не шириной колонки, а МЕСТОМ:
  панель стала двухэтажной (строка названия во всю ширину, под ней прежний ряд
  портрет/колонка/очередь/кнопки). Пустая строка прячется целиком
  (`_sync_panel_height`) — иначе панель Замка, где подпись уезжает в свою шапку,
  вырастала на её высоту и сбивала центровку кнопок найма.
- **РЯД ПОД НАЗВАНИЕМ ОБЯЗАН ТЯНУТЬСЯ ПО ВЫСОТЕ** (`hbox.size_flags_vertical =
  EXPAND_FILL`). VBox выдаёт ребёнку ровно минимум, и всё, что внутри ряда
  центровано или прижато к низу (портрет Замка, кнопки найма), считало бы «низ»
  по себе, а не по панели.
- **ЖЁЛТЫЙ КАНТ ВОКРУГ ИКОНОК — 2 px, САМИ ИКОНКИ +30%** (`BTN_SIZE` 22 → 29,
  `VET_BTN_BORDER_W` 3 → 2). Разворот прежнего требования «кнопка ровно вдвое
  меньше сорока четырёх»; стенд `qa_sel2` B1 переписан на два берега — кратно
  меньше 44 и не мельче 22. `CASTLE_PANEL_W` пересчитан вслед за кнопками
  (345 → 366): её размер жёсткий, но выводится из `BTN_SIZE`.
- **ЛЫЧКА НА ЗНАЧКЕ АЛЕРТА БЕЛАЯ** (`ALERT_CHEV_COLOR`), а не цвета грейда: у
  грейдов 1-3 вымпел красный, и на золотой плашке значка красная лычка читалась
  как тревога. Цвет грейда по-прежнему красит знамя в мире, значок на портрете
  и флажки «было → стало».
- **РАДАР-КОЛЬЦА ПО КЛИКУ АЛЕРТА БОЛЬШЕ НЕТ, КАМЕРА ПРЫГАЕТ СРАЗУ.** Разворот:
  плавную глиссаду просили, чтобы объектив не швыряло; на скриншоте видно, чем
  это кончилось — пока камера едет, отряда в кадре ещё нет, а метка над ним уже
  отгорела и стоит в пустой траве. Теперь `focus_camera_on` и ШТАТНЫЕ кольца
  выделения. Механизм `RTSCamera.glide_to` оставлен и проверяется стендом.
  **Кольца приходится подать вручную** (`sel_decals.update_all/flush` в
  `_on_alert_pressed`): их слой ведёт `GameManager._process`, а он PAUSABLE, и
  награду выбирают на паузе — ровно тот случай, ради которого метку когда-то и
  рисовали в HUD.
- **ТЕМП УДАРА И МОРАЛЬ ТЕПЕРЬ СТРОКИ ТАБЛИЦЫ СТАТОВ.** Жалоба: наведя курсор
  на награду, игрок видел в плашке «+3 ATK, +0,2 RATE, +10 MORALE», а в таблице
  дописывалась одна прибавка — у двух других не было своей строки, и
  `_preview_stats` их молча пропускал. Хранятся они ПО-РАЗНОМУ, и читать надо
  по-разному: мораль пишется в поле бойца (база = поле МИНУС `squad_bonus`),
  перезарядка тоже в поле, но ВЫЧИТАНИЕМ, а от кузницы читается вживую при
  каждом ударе. Дальность показывается только тем, у кого `attack_range` больше
  `MELEE_RANGE_MAX`: у пехоты это длина руки, и строка ничего не сообщает.
- **У ПЕРЕЗАРЯДКИ И РАЗБРОСА ЗНАК ПЕЧАТАЕТСЯ ПО СМЫСЛУ, А НЕ ПО КОНФИГУ**
  (`HUD.NEGATIVE_STATS`, `_stat_formula_cut`). Ключи `bonus_cooldown` /
  `bonus_spread` хранятся ПОЛОЖИТЕЛЬНЫМИ и ВЫЧИТАЮТСЯ. Плашка награды и таблица
  статов стоят на экране ОДНОВРЕМЕННО — показать один модификатор в них с
  разными знаками нельзя. Подпись «Attack delay» (а не «Rate» и не «Attack
  speed»): в строке стоят СЕКУНДЫ ПАУЗЫ, и они падают — подпись обязана падать
  вместе с числом. Подписи строк и плашки берутся из одного места
  (`STAT_ROW_LABELS` → `MOD_SHORT_LABELS`).
- **ЗОЛОТАЯ ВСПЫШКА НАГРАДЫ ПОВТОРЯЕТСЯ** (`_flash_pulses`,
  `VET_FLASH_REPEATS` 8 × `VET_FLASH_PERIOD` 5 с). Была одна на открытие
  панели — награду выбирают не сразу, и она успевала догореть раньше, чем игрок
  посмотрел на панель. Цикл КОНЕЧНЫЙ: вечно мигающая панель под рукой
  раздражает, в отличие от значка в углу экрана.
- **МЕНЮ ПАУЗЫ — СПИСОК ДЕЙСТВИЙ, НАСТРОЙКИ ПОД ОДНОЙ КНОПКОЙ**
  (`_pause_options_open`). «Загрузить игру» уезжала за нижнюю кромку не из-за
  размера кнопок: развёрнутыми стояли ряд сложности с подсказкой в три строки,
  три ползунка и переключатель V-Sync, и только под ними начинались кнопки.
  Прокрутка (`_center_scroll_vbox`) — второй рубеж, для развёрнутого вида.
  **CenterContainer внутри ScrollContainer обязан иметь EXPAND_FILL по ОБЕИМ
  осям:** прокрутка выдаёт ребёнку ровно его минимум по своей оси, и свёрнутое
  меню прижималось бы к верхней кромке вместо середины экрана (`qa_audio` G0в).

## Пивоты построек и деревьев: клик, кольцо, основание рисунка

- **ЗДАНИЕ КЛИКАЕТСЯ ПО КАРТИНКЕ, А НЕ ПО КОРОБКЕ КОНФИГА**
  (`Building._fit_pick_to_sprite`). Постройка — ВЕРТИКАЛЬНЫЙ билборд, и рисуется
  она выше своей коробки в разы: у хижины гоблинов коробка 4×3.5×4, а нарисована
  она на 5.2 м вверх, да ещё домноженных на `V_STRETCH` ≈ 1.41 — верхушка стоит
  на 7.4 м, вдвое выше коробки. Луч мыши идёт под 45°: клик в ВЕРХНЮЮ половину
  картинки пролетал НАД коробкой и втыкался в землю перед домом, приказ читался
  как «идти в точку», отряд шёл в одну общую точку и там топтался. Это и есть
  «мечники не доходят до дистанции удара, сбиваются в кучу и дёргаются».
- **ФОРМА ПОПАДАНИЯ — ПЛАСТИНА В ПЛОСКОСТИ СПРАЙТА, А НЕ ВЫСОКАЯ КОРОБКА.**
  Коробку пришлось бы тянуть и вверх, и в глубину, и тогда луч, нацеленный в
  ЗЕМЛЮ ПЕРЕД зданием, проходил бы сквозь её нижнюю переднюю часть — клик по
  пустой траве читался бы как клик по дому. Высота берётся УЖЕ РАСТЯНУТОЙ:
  компенсацию наклона делает шейдер в МИРОВЫХ координатах, от нижней кромки
  квада вверх.
- **ФОРМУ ЗАВОДИТ ОДНО МЕСТО** (`Building._add_pick_shape`). `_build_visual`
  переопределяют Замок, Хижина и Стройплощадка, и каждый заводил свой
  `CollisionShape3D`. Пока это была просто коробка из конфига, сходило с рук; с
  подгонкой под рисунок забытая ссылка означает молча оставленное старое
  поведение — ровно это и вышло с хижиной с первой попытки.
- **КОЛЛИЗИЙ У ЗДАНИЯ НЕТ НИКАКИХ, КРОМЕ ЛУЧА МЫШИ.** У юнитов
  `collision_mask` = 0, размещение считается по `build_size` напрямую. Форма
  обслуживает ровно `_pick_at`.
- **КОЛЬЦО НА ЗЕМЛЕ ОПИСЫВАЕТ РИСУНОК, А НЕ КОРОБКУ** (`Building.ring_radius` /
  `ring_center`). У хижины коробка 4 м при нарисованных 3.28 — кольцо выходило в
  полтора раза шире дома и наполовину торчало перед ним. И поднимается оно на
  ОСНОВАНИЕ рисунка: под стенами у спрайта прозрачное поле, и кольцо, честно
  положенное на грунт, оказывалось под домом на траве.
- **ПОЛЕ ПОД РИСУНКОМ МЕРЯЕТСЯ В КАДРЕ, А РИСУЕТСЯ РАСТЯНУТЫМ.** Та же ошибка
  была у кольца под деревом (`ResourceNode.hover_ring_lift`): поле считалось в
  долях кадра и умножалось на высоту квада, но шейдер растительности тянет квад
  от нижней кромки вверх в `V_STRETCH` раз. Кольцо поднималось на 71% нужного —
  то есть всё равно оставалось «на траве под деревом», ради чего поле и начали
  мерить. Стенд `qa_pivot`, блок C.

## Ряды, копья и строй в атаке (авг. 2026)

- **РЯД ШЕРЕНГИ СЧИТАЕТСЯ ВСЕМУ ОТРЯДУ, А НЕ ТОЛЬКО ОБОРОНЕ.** Гейтов было ДВА,
  и оба говорили одно: `_push_squad_ranks` отсеивал бойцов фильтром
  `not _stance_holds_ground()`, а тик звал `_update_live_rank()` под тем же
  условием. Вне стойки ЗАЩИТА `_live_rank` не считал НИКТО, и он оставался
  нулевым у всех. Снят фильтр в ПАКЕТНОМ проходе — и это ничего не стоит: там
  один вызов солвера на отряд по уже собранному составу, удлиняется только
  массив строк. Дорогим был ЛИЧНЫЙ `allies_ahead` (8.3 мкс на бойца, и почти
  всё — дорога наружу), и он остался ровно там, где был: в фаланге, по таймеру.
- **КОПЬЁ ОПУСКАЕТ ТОТ, КТО ДЕРЁТСЯ, А НЕ ТОЛЬКО ТОТ, КОМУ ПРИКАЗАЛИ**
  (`Spearman._charging`). Прежнее правило («только под прямой приказ по
  конкретной цели») верно наполовину: цель отряд получает и от авто-агро, и от
  подтягивания фланга, и в ответ на удар. **ПОРЯДОК РАБОТ ЗДЕСЬ РЕШАЛ ВСЁ:**
  ровно это правило вводилось раньше и было откачено — `qa_formation` D1 дал
  «копья опустили 19 из 20, фронт 8, тыл 8», потому что ранга вне обороны не
  существовало и гистерезис пропускал поголовно. Сперва ранг, потом правило.
- **ПЕРЕНОС СТЕНЫ НА АТАКУ ПРОБОВАЛИ ЧЕТЫРЕЖДЫ И СНЯЛИ. ЧЕТВЁРТАЯ ПОПЫТКА
  ОБЪЯСНИЛА ПОЧЕМУ.** Заказ «сохраняй геометрию отряда при приказе атаки»
  выглядит закрываемым снятием условия по стойке у `_wall_goal`. В заход
  31.08.2026 он был доведён до конца — отступ перед чужим строем вместо самой
  цели, выдача точки только под замком приказа, конец хода на первом
  контакте — и всё равно снят. **ВЫИГРЫШ ФОРМАЦИИ ДАВАЛА НЕ ГЕОМЕТРИЯ, А
  ОСТАНОВКА БОЯ.** A/B чередованием: сдвиг формы 0.90 против 1.09 — но
  схватка 60 на 60 ломалась на 148-й секунде вместо 32-й, а замок цели
  переставал работать вовсе (0 из 6 и 0 из 4). Обе поломки были найдены и
  починены порознь, и **после починок выигрыш исчез целиком: 1.08 против
  базовых 1.04-1.09.** Полный разбор с числами — в `docs/ENGINEERING_LOG.md`.
- **ЧЕТЫРЕ УСЛОВИЯ, КОТОРЫЕ ПОТРЕБОВАЛА ТА ПОПЫТКА, ВЕРНЫ САМИ ПО СЕБЕ** и
  понадобятся любому, кто возьмётся за строй в атаке снова: стрелков отсекать
  по РОДУ ВОЙСК (`_is_ranged`, а не `pursues_target` — тот у пехоты в обороне
  ложен); здания отсекать по типу цели (у них свой подход по дуге); центр
  строя вести НЕ В ЦЕЛЬ, а на отступ перед ней (иначе передняя половина
  отряда обязана оказаться за чужой линией); и главное — **`forced` это НЕ
  «приказ игрока»**: `command_attack(x, true)` зовут ответ на удар,
  подтягивание фланга и оба ИИ, поэтому точка, считаемая от текущего центра,
  уползала вперёд с каждым полученным ударом (отряд уходил с площадки стенда
  в живую партию и погибал там целиком).
- **НА ПОДХОДЕ СТРОЙ И ТАК СОХРАНЯЕТСЯ — ЭТО ЗАМЕРЕНО.** Сдвиг формы 0.70 м
  и с переносом, и без него. Значит, отряд разваливается НЕ на марше, а в
  контакте, и пятую попытку по этому пути начинать не стоит: мерить надо
  контакт, а не дорогу до него.
## Мега-стенд: 1500 на 1500 (авг. 2026)

**`qa_mega_battle` (17 проверок, headless)** — ровно тот сценарий, который назвал
владелец: 600 копейщиков впереди, 400 лучников за ними, 500 вражеских рыцарей
получают клик ПРЯМО ПО ЛУЧНИКАМ. Семь блоков: строй и знаменосец,
просачивание, осада, приказ ПКМ, чехарда ИИ, ориентация спрайтов и **строй при
приказе АТАКИ (B7: габарит, сдвиг формы, проход сквозь линию)**. Запуск с
параметрами: `-- spears=600 archers=400 knights=500` (по умолчанию столько же).
**B1-1 («отряд держится вокруг знаменосца», предел 11.6 м) — ножевой порог и
мигает:** 12.2-12.7 в красном против 9.5-11.3 в зелёном, и краснеет на ОБЕИХ
сторонах A/B, то есть к правкам строя отношения не имеет.

- **ЗНАМЕНОСЕЦ ЕСТЬ У КАЖДОГО ОТРЯДА, А НЕ ТОЛЬКО У ВЕТЕРАНОВ**
  (`GameManager.squad_bearer` назначает ЛЕНИВО). Он назначался ровно там, где
  заводится ЗНАМЯ (`refresh_squad_banner`), а знамя есть только у отряда со
  званием — то есть у свежего отряда `bearer` был null ВСЕГДА, и весь таргетинг
  «от знаменосца» молча уходил на запасной путь. Замер `qa_mega_battle` B1-1:
  отрядов под замером НОЛЬ из трёх. Знаменосец — это ЛИДЕР (место [ряд 0,
  колонка 0]), а знамя лишь предмет, который он может нести.
- **ПРИКАЗ ИГРОКА ПЕРЕБИВАЛИ ДВА АВТОМАТИЧЕСКИХ ПОДЗЫВА, А НЕ ВОРОТА
  `command_move`.** Жалоба «войска игнорируют приказ и остаются на месте»
  воспроизвелась только на масштабе: приказ ПРИНИМАЛИ ВСЕ (103 из 103 переходят
  в MOVING), а физически уходили 46. У **49 из 50** оставшихся точка приказа
  лежала В ДВУХ ШАГАХ от них — приказ был уже не игрока. Виноваты
  `GameManager._cohesion_guard` и `SquadFormation.close_ranks`: оба шлют
  `command_move` на место РЯДОМ с бойцом, и оба срабатывают именно тогда, когда
  мешают больше всего — отряд, которому велели уходить, по определению растянут
  (передние тронулись, задние ещё в телах). Оба теперь пропускают бойца, пока
  `Unit.player_order_active()`. Итог: **101 из 101**.
- **БИЛЕТ НА ВЫХОД ИЗ СВАЛКИ ГАСНЕТ ПО ФАКТУ ВЫХОДА, А НЕ ПО ЧАСАМ**
  (`Unit._forced_move_pass`, `PASS_CLEAR_DIST` 2.5 м). Срок был временной —
  полторы секунды, около трёх метров хода. Из мелкой стычки хватает, из
  настоящей свалки нет: впереди свой второй эшелон, позади чужая первая
  шеренга. Потолком остаётся сам замок приказа (`FORCED_MOVE_SEC`), поэтому
  прежняя опасность не возвращается: выбравшийся теряет билет РАНЬШЕ прежнего.
- **РЫЦАРИ СКВОЗЬ ФАЛАНГУ НЕ ПРОХОДЯТ, И ЭТО ЗАМЕРЕНО НА 1500**
  (`qa_mega_battle` B2-1: **0 из 489** за линией, 400 из 489 дерутся с
  копейщиками). Первый прогон показал было 9 из 93 — но они ОБХОДИЛИ ФЛАНГ:
  строй конечной ширины обойти можно, и запрещать это некому. **Стенд, меряющий
  просачивание, обязан считать только тех, кто оказался за линией И В ЕЁ
  ПРЕДЕЛАХ ПО ШИРИНЕ**, иначе он меряет обход.
- **МЕРИТЬ ПРОСАЧИВАНИЕ НАДО В ДИНАМИКЕ, А НЕ СНИМКОМ В КОНЦЕ.** Это СОБЫТИЕ:
  рыцарь может пройти сквозь строй и тут же погибнуть у лучников, и снимок его
  не увидит; хуже, к концу боя строя может не остаться вовсе. Стенд берёт
  ХУДШЕЕ значение за всё время, пока строй жив (та же оговорка, что у
  `WALL_INTACT_FRAC` в `qa_wall`).
- **ЧЕХАРДУ ИИ ВОСПРОИЗВЕСТИ НЕ УДАЛОСЬ** (B5: 20 отрядов ИИ, **0 смен роли за
  30 с**, мирная фаза принудительно отменена). Запрошенное правило всё равно
  заведено: `AICfg.AI_NO_RETREAT_RANGE` = 15 м — отряд не отменяет атаку у
  самого противника. Половина требования уже стояла
  (`AI_NO_RETREAT_IN_MELEE`), но КОНТАКТ — это уже удар мечом, а разворот
  происходит раньше: отряд подошёл на дистанцию выстрела, получил стрелу и
  пошёл обратно, ни разу не ударив.
- **ЛОВУШКИ САМОГО СТЕНДА, ВСЕ ТРИ ДАВАЛИ ЛОЖНЫЙ КРАСНЫЙ.** Отряд надо ставить
  БЛОКОМ, а не ломтиком общей шеренги (иначе проверка «держится вокруг
  знаменосца» меряет раскладку стенда). Предел разброса считается от УГЛА, а не
  от центра: знаменосец стоит в [ряд 0, колонка 0], и дальний боец отстоит на
  ПОЛНУЮ диагональ. «Погоня» — это движение К ВРАГУ, а не любое смещение:
  иначе в неё засчитывается штатное смыкание рядов после победы.

## Отряд смотрит глазами знаменосца (авг. 2026)

Заказ владельца из трёх частей: перенести таргетинг на знаменосца, не бегать за
разбитыми и починить осаду. Ниже только то, что нельзя вывести из кода.

- **ОБЩИЙ ОТВЕТ ОТРЯДА О БЛИЖАЙШЕМ ВРАГЕ ИЩЕТ ЗНАМЕНОСЕЦ, А НЕ ПЕРВЫЙ
  СПРОСИВШИЙ** (`Unit._squad_cached_enemy`). Ответ клал тот, чей тик пришёл
  первым в этом кадре, то есть по сути случайный боец: у растянутого отряда
  крайний левый и крайний правый видят РАЗНЫХ ближайших врагов, и общая цель
  отряда зависела от порядка обхода армии. Отсюда «фланги утягивают цели в
  противоположные стороны». Знаменосец — единственная точка отряда, у которой
  есть смысл «центр строя» и которая при этом ЖИВОЕ ТЕЛО: медиана у расколотого
  отряда садится в пустое поле между кучами, и агро от неё смотрело бы в никуда.
- **ЛИЧНЫЙ СКАН ПРИ ЭТОМ НЕ ОТМЕНЁН, И ОТМЕНЯТЬ ЕГО НЕЛЬЗЯ.** Если ответ
  знаменосца бойцу не по руке, он ищет сам — иначе возвращается «пассивная
  фаланга»: второй ряд с недостижимой целью, который не дерётся вовсе (разбор —
  выше, в разделе про `qa_wall`).
- **ПОВОДОК ИНИЦИАТИВЫ МЕРЯЕТСЯ ОТ ЗНАМЕНОСЦА, А НЕ ОТ ЛИЧНОГО ПОСТА**
  (`_check_auto_aggro`, ветка `AGGRO_LEASH`). Личный пост у каждого свой, и
  посты растаскивает вся история отряда — подзыв сплочённости, возврат из
  гарнизона, «встать где стою». Каждый мерил инициативу от своей точки, и отряд
  расползался веером за разными целями. От знаменосца начало отсчёта одно на
  всех. Пост остался ЗАПАСНЫМ ответом: у отряда без знамени знаменосца нет.
- **ЗА БЕГУЩИМИ НЕ ГОНИМСЯ** (там же). Догнать бегущего можно только погоней
  врассыпную — он уходит веером (`_start_panic`), и каждый преследователь
  выбирает своего; именно так строй и рассыпался в конце каждой выигранной
  схватки. Бегущий остаётся законной целью для того, кто ДОТЯГИВАЕТСЯ, но
  поводом СОЙТИ С МЕСТА не является. Признак берётся готовый: паника и отход —
  единственные два режима, в которых враг уходит не по приказу своего игрока.
- **ПРИКАЗ ИГРОКА СНОСИТ И ОТРЯДНУЮ ПАМЯТЬ О БОЕ, А НЕ ТОЛЬКО ЛИЧНУЮ**
  (`GameManager.squad_forget_combat` из `Unit.command_move` при `player_order`).
  `command_move` честно чистил личную память бойца, но у отряда есть своя, общая
  на всех, и она приказ переживала: ТОЧКА У СТЕНЫ с признаком «держать позицию»
  (приказ переводил бойца в MOVING, первое же авто-агро возвращало в ATTACKING,
  и подход читал СТАРЫЙ сектор — то есть тянул отряд обратно к зданию) и ОБЩИЙ
  ОТВЕТ О БЛИЖАЙШЕМ ВРАГЕ (живёт свой срок и раздаётся всем спросившим, включая
  того, кому только что велели уходить). На экране обе беды выглядят одинаково —
  «приказ отдан, отряд остался». **Жалобу «срабатывает только с третьего клика»
  воспроизвести не удалось**: замес 240 бойцов, 40 секунд рубки — 18 из 18
  приняли приказ с первого клика и уехали. Жёстких ворот в `command_move` ровно
  два, паника и замок позиции стойкой.

### Осада: стена по рисунку и разлив по соседним домам

- **КОЛЬЦО ПОДХОДА СЧИТАЕТСЯ ОТ КОРОБКИ, А ДИСТАНЦИЯ УДАРА — ОТ РИСУНКА, И ЭТО
  НЕ РАССОГЛАСОВАНИЕ.** Соблазн свести их к одной величине проверен замером:
  с `ring_radius()` кольцо у замка уезжает на 0.33 м дальше, и `qa_mass_siege`
  S1 краснеет (отставание от своего сектора 3.4 м против 2.8). Задачи у чисел
  разные: дистанция удара отвечает «дотягивается ли копьё до того, что игрок
  ВИДИТ» (край рисунка), а кольцо — «где отряду встать, не влезая в дом», и
  «дом» здесь пятно РАЗМЕЩЕНИЯ (`build_size`). Зазор `RING_STANDOFF` = 4.5 м
  вчетверо больше их разницы. **Вместимость** дуги (`_ring_capacity`) при этом
  считается по РИСУНКУ: она про то, скольким хватит места ДОСТАТЬ до стены.
- **ДИСТАНЦИЯ ДО ЗДАНИЯ СЧИТАЕТСЯ ДО КРАЯ РИСУНКА** (`Unit._pad_of` →
  `Building.ring_radius()`). Поправка бралась из `build_size`, а коробка почти
  всегда КРУПНЕЕ рисунка: у хижины коробка 4×3.5×4 (полугабарит 2.00 м) при
  нарисованных 3.28 м, то есть стена стоит на 1.64. Копейщик останавливался в
  3.50 м от центра — в 1.86 м от нарисованной стены при копье в 2.00 м, и на
  экране между остриём и стеной оставался просвет («тыкают копьями в воздух»).
  Замер после правки: 1.38 м. **Кольцо подхода переведено на ту же величину**
  (`_ring_squads_around`): подходить к одной «стене», а бить по другой нельзя.
- **ЛИШНИЙ ОТРЯД ИДЁТ НА СОСЕДНИЙ ДОМ, А НЕ СТОИТ В ОЧЕРЕДИ**
  (`SelectionManager._same_kind_buildings_near`, `RING_SPILL_RANGE` 26 м — то же
  число, что у `Unit.BUILDING_CHAIN_RANGE`: «тот же лагерь» обязано означать
  одно и то же в обоих правилах). Прежнее «мест у стены столько, сколько их
  есть, остальные ждут» было ЗАКАЗАНО и остаётся — но ПОСЛЕДНИМ средством:
  ждать имеет смысл, только когда бить больше нечего, а в деревне гоблинов
  хижины стоят сеткой с шагом шесть метров. Замер: три отряда на среднюю хижину
  → один ушёл на соседнюю, три дома из трёх под ударом, все снесены, ни один
  отряд не держит позицию.
- **ЦЕЛЬ ЛИШНЕМУ ОТРЯДУ ПЕРЕПИСЫВАЕТ ВЫЗЫВАЮЩИЙ, А НЕ САМА РАСКЛАДКА.**
  `_ring_squads_around` возвращает `sid → постройка`, и это подмешивается в тот
  же словарь `front`, каким раздаются участки чужого фронта
  (`frontline_targets`). Своего `command_attack` внутри раскладки нет намеренно:
  приказ отдавался бы из двух мест, и они разошлись бы на первом же изменении.
- **ОСЬ ПОДХОДА У КАЖДОГО ДОМА СВОЯ** (`_ring_place`): она считается по тем, кого
  ИМЕННО СЮДА послали. Взять её у первого дома значило бы развернуть дугу не
  туда, и отряд обходил бы соседнюю хижину кругом.

## Белый флаг паники: два сценария, один узел (авг. 2026)

Четыре жалобы владельца по двум скриншотам, и три из них — следствие ОДНОГО
решения: белый флаг был ОТДЕЛЬНЫМ узлом над ЦЕНТРОМ отряда. Стенд —
**`qa_panic`** (19 проверок, headless).

- **«ЮНИТ С ДВУМЯ ФЛАГАМИ» — ПРЯМОЕ СЛЕДСТВИЕ ВТОРОГО УЗЛА.** Знамя ветеранства
  висит на знаменосце, белый флаг висел над центром отряда — на экране они
  сходились в одну точку, и у копейщика вырастало два флагштока. Теперь у отряда
  РОВНО ОДИН узел (`squads[sid]["banner"]`), а бой это или сдача — вопрос ЛЕНТЫ
  (`SquadBanner.show_white` / `build`). Ключа `panic_marker` больше нет.
- **«ФЛАГ ЗАСТЫЛ В ВОЗДУХЕ» — СЛЕДСТВИЕ ТОЙ ЖЕ ТОЧКИ.** Центр отряда — МЕДИАНА
  (`_centroid_of`), а паникующий отряд разбегается: медиана садится в пустое
  поле МЕЖДУ разбежавшимися. Замер до правки: **12.22 м** до ближайшего бойца.
  Флаг теперь ездит на знаменосце, как и знамя, то есть всегда на живом теле.
- **У ПАНИКУЮЩЕГО ЗНАМЯ В СЕРЕДИНЕ ТОЛПЫ** (`_bearer_in_middle` отвечает `true`
  и на панику). Правило «первый ряд, крайний левый» писано под ШЕРЕНГУ, а срыв
  снимает разметку (`squad_clear_formation` в `_start_panic`) — «крайний левый»
  вырождается в того, кого выбросило дальше всех, и флаг уезжает на обод кучи.
  Та же беда и то же лечение, что у толпы орды. Сверка знаменосца поэтому идёт
  и БЕЗ разметки: условие в `_update_squad_banners` — «есть slots ИЛИ носим в
  середине».
- **ДВА СЦЕНАРИЯ, И РАЗЛИЧАЮТСЯ ОНИ СУДЬБОЙ УЗЛА, А НЕ ОТТЕНКОМ** (заказ
  владельца):
  - **новобранцы** (звания нет) — узел ЗАВОДИТСЯ на время паники и исчезает с
    ней или с гибелью отряда; на земле не остаётся ничего;
  - **ветераны** — второй узел НЕ заводится, родное знамя меняет ленту, а по
    гибели на земле остаётся БОЕВОЙ штандарт.
- **БОЕВОЙ ШТАНДАРТ У ПАВШЕГО В ПАНИКЕ ПОЛУЧАЕТСЯ САМ, И НА ЭТО ОПЁРТЫ
  НАМЕРЕННО.** `_lay_fallen_banner` собирает упавшее знамя по УРОВНЮ отряда
  (`BannerArt.texture_for`), а не по той ленте, что висела на древке в последнюю
  секунду. У отряда без звания уровень ноль — `_drop_squad_banner` выходит
  сразу, и «на земле ничего» тоже выходит само.
- **РАЗБЕГ И ДАЛЬНОСТЬ БЕГСТВА — РАЗНЫЕ ВЕЛИЧИНЫ** (`PANIC_SPREAD` против
  `PANIC_FLEE_DIST`). Было одно число: каждый получал точку на диске радиусом
  `PANIC_FLEE_DIST` = 20 м вокруг места срыва, и двое с противоположных краёв
  расходились на сорок метров (замер: габарит отряда **37.47 м** на двенадцати
  бойцах). Теперь отряд убегает на `PANIC_FLEE_DIST` ОТ УГРОЗЫ как целое, а
  рассыпается внутри круга `PANIC_SPREAD` = 6 м. Замер после правки: **10.64 м**.
- **ЭТО ВОЗВРАТ ПРЕЖНЕГО ТРЕБОВАНИЯ, И ПОЛОВИНЧАТЫЙ.** Владелец однажды уже
  разворачивал «уходят по вектору от врага» в «во все стороны» (прежний общий
  вектор читался как ОРГАНИЗОВАННЫЙ ОТХОД), а теперь попросил связности. Общий
  вектор вернулся, но «во все стороны» осталось РАЗБРОСОМ вокруг общей точки, а
  не самим бегством: направление у каждого своё, дистанция своя, и строевого
  отхода на экране не возникает.
- **СВЯЗНОСТЬ ДЕРЖИТ ТАКТ МОРАЛИ, А НЕ ОДНА РАЗДАЧА ТОЧЕК**
  (`_panic_regroup` из `_sweep_morale`). Точки раздаются один раз, в момент
  срыва, а за двадцать секунд ступора бойца утащит что угодно — конница, толчок
  в свалке, разбор наложения. Зовут обратно ТЕМ ЖЕ `panic_flee`: паникующий
  приказов не слушает, и это единственная открытая для него дверь. Возвращают
  НЕ в центр, а на границу круга со своей стороны — в центр сошлись бы все
  разом, и паника читалась бы как построение.
- **ИСКАТЬ УЗЛЫ ФЛАГОВ ПО ИМЕНИ НЕЛЬЗЯ** (ловушка для стендов). Имя
  «SquadBanner» ставится узлу ДО внесения в дерево и до дерева не доезжает: в
  дереве он зовётся `@MeshInstance3D@8699`. Опознавать надо по полю
  `shown_white` — его нет ни у одного другого `MeshInstance3D` в проекте.

## Ворота крепости, гарнизон и клик по зданию (авг. 2026)

Три жалобы владельца по одному скриншоту, у всех трёх разные причины.
Стенд — **`qa_keep`** (21 проверка, headless: ворота, вход в гарнизон, кольца,
приоритет клика, полосы выхода).

- **ЖЁЛТЫЕ КОЛЬЦА В ПУСТОМ ПОЛЕ ПОД ЗАМКОМ — ЭТО ГАРНИЗОН, А НЕ СМЕЩЁННЫЙ
  СПАУН.** СМЕРТЬ снимает с бойца все надстроенные слои разом (`Unit._unhook`),
  а уход В ЗАМОК — нет: боец жив, узел на месте, из отряда не выбыл. Кольцо
  выделения — слот в общем слое, ездит оно по НАРИСОВАННОЙ точке, а та
  ЗАМЕРЗАЕТ в тот же кадр, когда бойцу гасят визуальный тик. Замер (`qa_keep`,
  двадцать копейщиков): внутри 20 из 20, колец на карте 20. Лечение —
  `GameManager.forget_on_map` из `Castle.absorb_unit`: выделение, кольцо,
  прицельная подсветка и полоска здоровья. **Снимать надо БЕЗУСЛОВНО**, а не по
  флагу `_selected`: флаг — второе описание того же факта, и разойтись они уже
  могут (та же оговорка, что в `Unit._unhook`).
- **ИЗ ВЫДЕЛЕНИЯ ВОШЕДШЕГО ВЫЧЁРКИВАЕТ `SelectionManager.forget_unit`, И ЗВУКА
  ПРИ ЭТОМ НЕТ** (`on_selection_changed(..., silent = true)`): звук выделения
  означает КЛИК ИГРОКА, а вход в замок — событие игры. То же правило, что у
  закладки и достройки замка.
- **БОЕЦ ИСЧЕЗАЕТ У ДВЕРЕЙ, А НЕ ЗА ПЯТЬ МЕТРОВ ДО НИХ.** Ворота стоят на
  переднем крае НАРИСОВАННОГО основания (у замка 5.23 м от центра), а зачисление
  внутрь шло по `GARRISON_ENTER_RADIUS` = 5 м ВОКРУГ ворот — и отряд, идущий с
  поля, пропадал с экрана в десяти метрах от центра замка, метрах в пяти от его
  края. Теперь радиус ВЫВОДИТСЯ (`Castle._door_radius` = `ARRIVE_RADIUS` +
  два строевых интервала): до самой точки ворот доходит только ГОЛОВА очереди,
  остальных держит разбор наложения, а вошедший освобождает место следующему тем
  же кадром. Замер: худший исчез в 1.23 м от ворот (было 3.8), зашли 20 из 20.
- **ТУГОЙ РАДИУС — ЭТО ПРО КАРТИНКУ, И ПЛАТИТЬ ЗА НЕЁ ЗАВИСШИМ ОТРЯДОМ НЕЛЬЗЯ.**
  `GARRISON_ENTER_RADIUS` не отменён, он стал СТРАХОВКОЙ: отставший, которого не
  пускает к воротам толпа или ствол, через `Castle.DOOR_PATIENCE` зачисляется по
  прежнему широкому допуску. Мягкой блокировки, от которой здесь однажды уже
  лечились («ПРИХОД СЧИТАЕТСЯ ОТ ВОРОТ»), это не возвращает.
- **ПОЛОСУ ВЫХОДА ВЫБИРАЕТ ЗАНЯТОСТЬ, А НЕ СЛЕПОЙ СЧЁТЧИК**
  (`Building._free_exit_lane`). Счётчик рос НА КАЖДЫЙ ЗАКАЗ и по сторонам не
  смотрел вовсе: третий наём из замка собирался в девятнадцати метрах вбок от
  ворот даже в чистом поле — это и есть «спаун не у ворот, а ПРАВЕЕ». Полосы
  заведены против ОДНОВРЕМЕННОГО найма, только этот случай они и обслуживают.
  Сбрасывать счётчик на простое здания уже пробовали и откатили (`qa_rally2`
  E7/E8): «здание простаивает» не значит «место у ворот свободно». Поэтому
  спрашивается СВОЙСТВО — стоит ли кто-то на этой полосе, — тем же приёмом, что
  у `GameManager.free_squad_spot`.
- **ЗАНЯТОСТЬ СЧИТАЕТСЯ ПО ДВУМ ИСТОЧНИКАМ, И ВТОРОЙ ЛЕГКО УПУСТИТЬ.** Заказ
  выходит из дверей по две шеренги в секунду: пока он не вышел, на его полосе
  физически НИКОГО НЕТ, и второй заказ того же кадра нашёл бы её свободной —
  ровно то наложение, ради которого полосы и придуманы. Полоса считается занятой
  и тогда, когда её держит ещё не вышедшая заявка (`_lane_claimed`).
- **ШАГ ПОЛОСЫ СЧИТАЕТСЯ ОДНОЙ ФУНКЦИЕЙ С ТЕМ МЕСТОМ, ГДЕ ПОЛОСА ВЫБИРАЕТСЯ**
  (`Building._lane_step`): посчитай его в `_spawn_one` по-своему — и проверка
  занятости будет судить об одних полосах, а отряд выходить на другие.
- **БОЙЦЫ ПОЯВЛЯЮТСЯ НА ПЛОЩАДКЕ СБОРА, А НЕ В ПРОЁМЕ ВОРОТ** (хак №3,
  `Building.SPAWN_ON_ZONE`, 09.09.2026). `_spawn_one` считает место бойца по
  рядам полос как прежде, но `_place_spawned` ставит его СРАЗУ туда
  (`zone_pos`), а не в ворота с маршем к месту: очередь в проёме, разбор
  наложения у дверей и толчея двух заказов исчезают по построению. С
  флажком отряд собирается на площадке и оттуда идёт к точке строем. Полосы
  выхода остались РЯДАМИ площадки: `rally_zone()` — прямоугольник от
  `SQUAD_EXIT_DISTANCE` до последней полосы, `in_rally_zone(p)` — принадлежность.
  **`rally_zone()` обязан звать `_gate_position()` ПЕРВЫМ**: тот лениво
  ставит фасад и переписывает `spawn_offset`, а прочитанный до него
  `spawn_offset` — начальные (3, 0, 0), и площадка разворачивалась вбок
  (поймал `qa_rally_zone` A4). **Рамка, а не заливка:** прозрачная заливка
  в GL Compatibility гасила под собой спрайты бойцов целиком (снимок
  `qa_rally_zone/Shot`: два отряда пропали), плоский квад тонул в рельефе —
  рисуются четыре непрозрачные полосы по рельефу, только у выделенного.
  Стенды прежнего требования «появились у ворот» переписаны на «на
  площадке»: `qa_spawnlane` C1/D2 (и A1-A3/B1 читают точку бойца, а не
  погашенный `move_target`), `qa_squad` 2.
- **ПОЛОСЫ ЛЕЖАТ КЛИНОМ В 45°, А НЕ ЛИНИЕЙ ВБОК** (`Building._lane_shift`).
  Жалоба владельца со скриншотом: «юниты спавнятся далеко внизу справа по
  диагонали, а не прямо перед дверью». **Дело было НЕ в точке ворот и не в
  направлении выхода** — их проверили первыми и оба чисты: боковая поправка
  рисунка `_draw_cx` у замка всего −0.01 м, отклонение выхода от фасада 0.2°
  (зонд `qa_gate`, G1-G4). **И не в занятости полос:** все отряды стояли на
  своих местах честно. Виновата была ГЕОМЕТРИЯ ВЕЕРА: вперёд у каждой полосы
  стояло одно и то же `SQUAD_EXIT_DISTANCE` = 4 м, а вбок прибавлялся
  `_lane_step` (7.13 м у барака) — то есть уже ПЕРВАЯ боковая полоса уходила
  вбок вдвое дальше, чем вперёд, а четвёртый заказ вставал в **13.89 м вбок
  при 4.47 м вперёд, под 72° к фасаду**. Теперь тот же шаг прибавляется и по
  фасаду: «перед воротами» выполняется ПО ПОСТРОЕНИЮ, а не подбором числа
  (замер после правки — 37°). Боковой разнос полос не тронут вовсе, поэтому
  всё, ради чего полосы заведены, цело; соседние заказы расходятся теперь ещё
  и по глубине.
- **МЕРИТЬ НАДО ТАМ, КУДА ОТРЯД ПРИХОДИТ, А НЕ ГДЕ РОЖДАЕТСЯ.** Первая версия
  зонда снимала бойцов через шесть физкадров после спавна — а они все стоят В
  ВОРОТАХ (вперёд 0.25 м), потому что раскладка исполняется приказом
  `command_move`. Такой замер давал зелёное на любой раскладке. И звать
  `_spawn_one` напрямую тоже нельзя: `lane` там аргумент, а вся боковая
  составляющая живёт в `_free_exit_lane`, то есть зонд с `lane = 0` мерил
  геометрию, которой в партии не бывает.
- **`qa_keep` E2 УТВЕРЖДАЛ СТАРУЮ РАСКЛАДКУ** («соседняя полоса уводит отряд
  вбок РОВНО на шаг полосы») — то самое, что владелец и развернул. Разбито на
  два свойства порознь: E2 меряет боковой разнос (ради него полосы и заведены),
  E2б — что полоса уходит вперёд не меньше, чем вбок.

### Клик по зданию: кто нарисован поверх

- **ЖАЛОБА: «клик по крепости срабатывает с пятого-десятого раза, буквально
  пролетает сквозь спрайт; если рядом пробегают крестьяне, клик перехватывают
  они».** Хитбокс при этом был исправен: пластина подогнана под непрозрачную
  часть рисунка (`Building._fit_pick_to_sprite`), и луч в неё попадал. Ломался
  СЧЁТ.
- **ПРИЧИНА — ПОЛУГАБАРИТ КОРОБКИ ВМЕСТО ВЫСОТЫ РИСУНКА.** Победителя выбирает
  расстояние от основания объекта до ТОЧКИ ЗЕМЛИ под курсором минус его габарит
  в плане. Курсор на стене замка, на 78 % высоты картинки: точка земли лежит в
  **6.28 м ЗА зданием**, а вычиталось 4 м — полугабарит коробки 8×8. Замку
  оставалось 2.28, а рабочему, случайно стоящему в этой точке, доставался ноль.
  Замок проигрывал крестьянину, которого сам же и загораживает (замер `qa_keep`
  D1: под курсором был Worker).
- **РАНГ («боец важнее здания») ЭТОГО НЕ СПАСАЛ, А УСУГУБЛЯЛ** — он и есть то
  правило, по которому рабочий выигрывал. Отменять его нельзя: масок
  столкновений в проекте нет, бойцы свободно стоят на габарите построек, и клик
  по бойцу У СТЕНЫ обязан выбрать бойца.
- **РЕШАЕТ ВЫСОТА, И ТОЛЬКО ПОТОМ РАНГ.** Луч идёт под наклоном, поэтому высота
  и глубина связаны жёстко: точка земли под курсором лежит ровно на `lean × h`
  от основания того, чей рисунок курсор накрыл на высоте `h`. Значит `h`
  считается одним вычитанием и **это единственная величина, по которой можно
  сказать, кто нарисован ПОВЕРХ**: чем выше по своему спрайту накрыт кандидат,
  тем он ближе к камере. У бойца перед стеной `h` ≈ рост, у здания за ним
  `h` ≈ 0 — боец выигрывает; у бойца ЗА зданием `h` = 0 против шести метров у
  стены — выигрывает здание. Ранг решает при РАВНОЙ высоте, то есть когда оба
  нарисованы в одном месте.
- **ПОСТРОЙКА В СПИСКЕ КАНДИДАТОВ — ЭТО ФАКТ, А НЕ ДОПУСК.** Здания попадают в
  разбор ТОЛЬКО лучом, а ловит луч пластину по рисунку: попадание в пластину и
  попадание в картинку — одно событие. Поэтому «промах по земле» зданию не
  считается вовсе (`score = 0`), и раздувать ему круг на земле не нужно —
  раздутый круг сделал бы замок жирной целью и он начал бы перехватывать клики
  по траве у своих стен (стережёт `qa_keep` D3).
- **`set_input_as_handled` тут ни при чём и уже стоит** где надо
  (`SelectionManager._unhandled_input`): клик разбирается ОДИН раз и дальше не
  проваливается. Промах был не в маршрутизации события, а в выборе цели.

## Приказ игрока выводит бойца из свалки телом, а не только вниманием

- **ЖАЛОБА:** «отряд в плотной рубке не идёт с первого правого клика, нужен
  второй». Замок `FORCED_MOVE_SEC` уже глушил перехват марша и ответный удар —
  то есть ВНИМАНИЕ бойца. Но шаг всё равно упирался в чужой строй
  (`_move_blocked` → `enemy_block`): в свалке враги стоят СО ВСЕХ СТОРОН, лобовая
  составляющая шага съедается целиком, боковая упирается на втором проходе, и
  функция выходит, НЕ СДВИНУВ БОЙЦА НИ НА САНТИМЕТР. Приказ принят честно —
  состояние MOVING, цель записана, — просто исполнить его нечем.
- **Лечение — `Unit._forced_move_pass()`:** пока тикает замок приказа игрока,
  чужие тела бойцу не преграда, ровно как отходящему в замок (`retreating`).
  Окно КОРОЧЕ самого замка (`FORCED_MOVE_PASS_SEC` 1.5 с против 3 с): полутора
  секунд хватает выйти из тел (около трёх метров, две шеренги), а все три
  позволили бы приказом «идти туда» проходить сквозь оборону насквозь —
  сквозной проход в этой игре есть ровно у одного режима, отхода в замок.
- **Своей ПЕРЕМЕННОЙ не заводим:** `_move_lock` уже означает «это приказ игрока
  и он непрерываем», вторая про то же самое разошлась бы с ней. А вот БИТ в
  колонке ядра завести пришлось (`ArmySoA.F_ORDER_PASS` = `ArmyCore.FOrderPass`
  = 1 << 17): признак читает солвер.
- **ПРАВКА ТОЛЬКО В `_move_blocked` — ЭТО ПРАВКА В ЗАПАСНОЙ ДОРОГЕ, И ОНА НЕ
  РАБОТАЕТ ВОВСЕ.** Первая версия послабления стояла именно там и на живой
  армии не дала НИЧЕГО: горячий путь шага пакетный (`_commit_step` →
  `GameManager.queue_step` → `ArmyCore.BatchStep`), а `_move_blocked` — запасная
  дорога для бойца без строки в ядре. Замер стендом `qa_disengage` D2:
  окружённый боец проходил **0.00 м** за секунду после приказа; с битом в
  колонке — 1.97 м. Любое правило движения, добавленное в GDScript, обязано
  ехать и в `StepFlagMask`, иначе его просто нет.

## Знамя ветеранства: семь грейдов на копье знаменосца

**Звёзды заменены знамёнами** (заказ владельца, авг. 2026). `VeterancyStar.gd`
оставлен в дереве неиспользуемым — тем же порядком, что и `gold_shimmer.gdshader`.

- **Грейды — `unit_stats_config.VET_BANNER_TIERS`, один источник правды** и на
  картинку (`BannerArt`), и на значок ранга в панели
  (`veteran_badge_text` / `veteran_badge_color`), и на звание
  (`veteran_rank_name` = «Отряд ветеранов копейщиков»: ранг из таблицы грейдов,
  существительное — из `STATS.name_genitive_plural` того рода войск).
  1-3 — красный вымпел (лычки `>`), 4-6 — синий «ласточкин хвост» (лычки `<`),
  7 — штандарт с золотой бахромой и гербом; 3 и 6 добавляют белый наконечник.
- **У ТОЛПЫ ЗНАМЯ В СЕРЕДИНЕ, А НЕ С КРАЮ** (`_bearer_in_middle`). Жалоба
  владельца: «синие флажки торчат в полях там, где ходили орки». Замерами
  проверено, что знамёна НЕ утекают и НЕ дублируются: за весь бой узлов ровно
  столько же, сколько ветеранских отрядов орды (пять из пяти, ни одного
  бесхозного), знамя сидит на своём знаменосце с точностью до смещения древка,
  а упавшее честно ложится плашмя. Беда была в том, КОГО назначают
  знаменосцем: правило «первый ряд, крайний левый» писано под ШЕРЕНГУ, а орда
  ходит ТОЛПОЙ — и «самый выдвинутый вперёд и самый левый» оказывается
  одиночкой НА ОБОДЕ (замер: 5.7 м от центра кучи из сорока). Мелкий тёмный
  гоблин на траве не читается, а яркое полотнище на длинном древке читается
  прекрасно — отсюда «флаг, воткнутый в пустое поле». **Ветеранство орды при
  этом НЕ трогается**: пять стартовых отрядов с серебром — задуманный сценарий
  неравных условий, прямой заказ владельца. Стенд `qa_reform`, блок B.
- **Знамя нарисовано ТЕКСТУРОЙ, а не геометрией, и это не вкус.** Слой тел
  умеет ровно одно — класть плашмя КВАД С ЛЕНТОЙ (`CorpseRenderer`), а упавшее
  знамя обязано запекаться именно туда, чтобы даром получить и потолок, и
  вытеснение, и пятнадцатиминутный срок, и разложение. Знамя-из-треугольников
  туда не положить никак. Побочная выгода: живое и упавшее знамя — ОДНА
  картинка, и разъехаться по виду они не могут.
- **Картинка строится кодом** (`BannerArt`): готовой в ассетах нет, как не было
  и звезды. Обводка — ПОСТ-ПРОХОДОМ по готовому холсту, а не вокруг каждой
  фигуры: фигур четыре (древко, навершие, полотнище, бахрома), и обводка каждой
  порознь дала бы кант ВНУТРИ знамени на их стыках. Цвет лычки кладётся только
  туда, где уже есть полотнище, — иначе шеврон торчит в воздух за краем вымпела.
- **ЗНАМЯ ВИДНО РОВНО ТОГДА, КОГДА ВИДНО ЗНАМЕНОСЦА** (`Unit.is_drawn()` в
  `GameManager._update_squad_banners`). Жалоба владельца со скриншотом: синие
  флажки ветеранов орды стоят в пустой траве и не исчезают. **Дубликатов узлов
  нет и не было** — замерено зондом `qa_reform/Flags` на живой партии: за весь
  бой узлов ровно столько, сколько ветеранских отрядов (5 из 5), бесхозных
  ноль. Флаг был ОДИН и законный, но стоял не там, где хозяин.
  Причина — ЗАМЕРШАЯ НАРИСОВАННАЯ ТОЧКА: боец под пеленой выходит из
  `tick_visual` РАНЬШЕ, чем обновит `_draw_pos` (ранний `return` в ветке
  тумана), и `draw_position()` у него остаётся равной месту последнего
  появления. Знамя ехало по этой точке И ПО НЕЙ ЖЕ спрашивало туман, то есть
  спрашивало «освещено ли место, где хозяин когда-то был». Орда уходит в
  темноту, армия игрока наступает и освещает её вчерашнюю кромку — флаг
  загорается над пустой травой. Теперь спрашивается ФАКТ ОТРИСОВКИ (он разом
  покрывает туман, гарнизон и уход с карты), туман — по ЛОГИЧЕСКОЙ точке, а
  ставится знамя по-прежнему в нарисованную. Замер `qa_reform` B7: со старым
  кодом видимое знамя стояло в **765 м** от своего знаменосца.
- **Узел знамени живёт в МИРЕ, а не ребёнком бойца.** Ребёнок ехал бы по
  ЛОГИЧЕСКОЙ точке, а спрайт — по сглаженной (`Unit.draw_position`): ровно на
  этом обжигались жёлтые кольца выделения. Точку знамени даёт `draw_position()`
  знаменосца каждый кадр — сглаживание там уже сделано, поэтому ни редкого
  пересчёта, ни догоняющего lerp (которые были нужны звезде над центром масс)
  здесь нет вовсе.
- **Размер подбирается ГЛАЗОМ на оконном стенде** (`qa_banner`), и иначе не
  выйдет: на экране высота домножается ещё и на компенсацию наклона камеры
  (`BillboardUtil.V_STRETCH` ≈ 1.41). Первый подбор дал знамя на четверть выше
  копий — пика знаменосца читалась мачтой.
- **Растёт ПОЛОТНИЩЕ, а не пика.** Заказ «увеличить знамёна» выполняется в
  `BannerArt` (`FLAG_H`/`FLAG_LEN`/`STD_*`), а не подъёмом `PIXEL_SIZE`: тот
  тянет и древко, возвращая ту самую мачту. Холст при этом расширяется, поэтому
  сдвиг знамени над бойцом ВЫВОДИТСЯ из холста (`BannerArt.pole_offset_px`), а
  не стоит числом — иначе знамя висит мимо знаменосца после каждого расширения.
- **Вымпел — СИММЕТРИЧНЫЙ треугольник.** Первая версия сужала только нижнюю
  кромку, верхняя шла прямой до конца, и на экране это читалось не вымпелом, а
  флагом с обрезанным краем.
- **У лычки, смотрящей влево, СВОЙ отступ от древка.** База шеврона — его
  основание, а остриё уходит от неё на пол-высоты в сторону `chevron_dir`.
  При общей базе левосмотрящие лычки «ласточкиных хвостов» упирались в древко
  и срезались проверкой «рисовать только по полотнищу».
- **Материал — ВЕТРОВОЙ** (`make_wind_material`): тот же шейдер, что у
  растительности, гнёт квад у верха и оставляет низ на месте, то есть полотнище
  колышется, а древко стоит. Стоит это ноль.

## Знаменосец: передача и падение знамени

- **Знаменосец хранится у ОТРЯДА** (`squads[sid]["bearer"]`), а не полем на
  бойце: поле пришлось бы поддерживать в двух местах и оно переживало бы бойца
  в чужих ссылках. Считается только по СОБЫТИЮ — покадрового пути нет.
- **Первое назначение — ПЕРВЫЙ РЯД, КРАЙНИЙ ЛЕВЫЙ.** «Первый ряд» считается по
  курсу отряда: самый выдвинутый вперёд боец задаёт линию, и все в полосе
  `BEARER_ROW_BAND` от неё — первая шеренга. Квантовать по `formation_row`
  нельзя: в бою разметка не действует, и номера у выживших любые.
- **Выбор идёт ПО МЕСТУ В РАЗМЕТКЕ** (`_bearer_anchor` → `post_pos`), а не по
  текущей точке бойца. Разница решающая при перестроении: смыкание рядов только
  что раздало приказы, но никто ещё не сделал ни шагу — по текущим точкам
  знаменосец вышел бы из середины кучи, и знамя поехало бы с ним в тыл.
- **Знамя возвращается на левый фланг при СМЫКАНИИ РЯДОВ**, и только там:
  смыкание и есть «конец экшн-сцены». В бою знаменосец меняется по другому
  правилу (ближайший к павшему), и трогать его, пока драка идёт, нельзя.
- **Передача — БЛИЖАЙШЕМУ К ПАВШЕМУ**, а не к центру отряда: на экране это
  читается как «подхватил сосед», а выбор по центру уводил бы знамя в тыл через
  полстроя.
- **Последняя точка отряда пишется на КАЖДОМ выбывшем** (`squads[sid]["last_pos"]`),
  а не только на последнем: узнать, что боец последний, можно лишь после
  `members.erase`, а к тому моменту его узел уже уходит из дерева.
- **УКЛАДКА УПАВШЕГО ЗНАМЕНИ ОТЛОЖЕНА НА КАДР.** Приходят сюда из
  `Unit._exit_tree`, то есть когда движок ПЕРЕСТРАИВАЕТ ДЕРЕВО, и `add_child`
  там отбивается — а слою тел он нужен: первое тело каждой ленты заводит свой
  `MultiMeshInstance3D`. Обычные трупы этой грабли не знают, потому что ложатся
  из `take_damage`, посреди физического тика. Уровень и точку снимаем сразу
  (через кадр записи о них уже нет), укладку — через `call_deferred`.
- **ЗНАМЯ РОНЯЕТ ТОЛЬКО ГИБЕЛЬ, А НЕ ПЕРЕВОД.** Отряд пустеет двумя путями, и в
  коде они выглядели одинаково: последнего бойца убили — или его перевели в
  другой отряд (`add_to_squad` первым делом зовёт `remove_from_squad`). ИИ и
  орда перекладывают бойцов каждый такт, и на каждом перекладывании падало
  знамя: за ордой тянулся след из знамён её же ЖИВЫХ отрядов («синие флажки»,
  синие — потому что это грейды 4-6). Признак `wiped` идёт до `_disband_squad`
  из ДВУХ мест, и второе легко упустить: главный путь выбитого отряда — не
  `remove_from_squad`, а `squad_members()`, который расформировывает отряд,
  когда все в нём мертвы.
- Упавшее знамя — ОБЫЧНЫЙ ЖИЛЕЦ СЛОЯ ТЕЛ (`CorpseRenderer.spawn_prop`): та же
  очередь, тот же потолок, те же 15 минут и 30 секунд разложения, своё пятно
  тени. Отличий от тела два и оба осознанные: яркость всегда полная (тело
  притеняется по своей ступеньке в куче, а знамя в куче не лежит) и размер без
  разброса (±7% лечат «штампованных клонов», а знамя на поле одно).
- Стенды: `qa_vet` блок 4в (передача и падение), `qa_corpse` блок O (срок и
  разложение упавшего), `qa_banner/Shot` (оконный: лесенка семи грейдов, знамя
  в строю, упавшее знамя, счёт вызовов отрисовки).

## Золотая жила: анимация — родная, а не шейдерная

- **Спрайт золота — `Gold Stone N_Highlight.png`, а не `Gold Stone N.png`.**
  Суффикс обманывает: это не слой блика поверх жилы, а САМА ЖИЛА, нарисованная
  шестью кадрами (768×128) с ходящим по слиткам глянцем. Обычный файл —
  один статичный кадр. Переливается золото штатным кадровым путём
  (`frame_count` / `frame_fps` / `frame_phase` у `cyl_billboard`), темп у каждой
  жилы свой (`randf_range(5.0, 8.0)`), фаза тоже.
- **Аддитивного второго квада и `gold_shimmer.gdshader` в игре НЕТ.** Блик,
  собранный шейдером поверх грунта, светил по траве пятном; заказчик просил
  ровно обратного — «без дополнительных шейдеров и посторонней подсветки
  травы». Шейдер оставлен в дереве неиспользуемым, с пометкой в шапке.
- **Ленту нельзя обрезать `AtlasTexture`**: он вырезает ОДИН прямоугольник и
  склеил бы шесть кадров в бегущую строку. Лента пересобирается покадрово
  (`ResourceNode._crop_sheet`, кэш `_sheet_cache` по пути исходника). Обрезка
  ОБЩАЯ на все кадры: у кадров разный рисунок, и обрежь каждый по себе — жила
  дёргалась бы в такт анимации.
- **У пересобранной ленты нет `resource_path`,** поэтому
  `BillboardUtil.frame_count` по ней падает на угадывание по пропорции и при
  неквадратном кадре отвечает «один». Рендеру это не мешает (число кадров уходит
  в материал явно), а стенд пропорций читает поле `ResourceNode.sprite_frames_drawn`.
  Стенды `qa_aspect` (C), `qa_gold/Probe`.
- **ПИКСЕЛЬ РУДЫ ПРИВЯЗАН К ЮНИТСКОМУ** (сент. 2026, заказ: «перерастянутый
  комок из гигантских пикселей»). Пиксель бойца 0.0108 м, у руды был 0.034
  (золото) и 0.059 м (камень) плюс дорастяжка зумом до 1.75×. Теперь
  `FRAME_METRES_GOLD` 2.0, `_STONE` 1.55, `ZOOM_MAX` 1.25, `PIECE_TARGET_H`
  0.85 (из неё же выведен `pick_body_h`). **Крупный кусок делается выбором
  крупно НАРИСОВАННОГО варианта, а не зумом**: `Main.ORE_VARIANT_POOLS`
  раздаёт варианты по классам (заполнение кадра у вариантов 20-57%, и
  «big»-кусок со случайным мелким вариантом надувался в разы). Раскладки
  перевёрнуты на «1-2 крупных + 3 средних + мелочь» — координаты кусков не
  тронуты, понижены только классы; `LAYOUT_SPAN_STONE` 0.75.
  **`WORLD_GEN_VERSION` поднята до 3** (на кусок добавился вызов randi()).
  Снимок для глаза — `qa_gold/OreShot` (оконный, кучи ставит сам).

## Туман войны: кто себя прячет

- **ИСТОЧНИКИ ПО БОЙЦАМ СОБИРАЕТ ЯДРО** (`ArmyCore.FogRefreshRows`,
  09.09.2026). Маска и так считалась в C# (этап D3), но СПИСОК источников
  строил GDScript обходом всех `_live_units`: `global_position` (свойство,
  правило 2), `is_instance_valid`, `is_dead`, словарь — на каждого из 4000
  ради 74 ячеек-источников. Замер зондом `qa_fogperf`: **4.59 мс на
  пересчёт, из них 3.38 — этот обход**; пересчёт идёт раз в 0.15 с и ложится
  в ОДИН кадр — на кадре в 14 мс это и есть «микролаг» раз в девять кадров.
  Теперь ядро идёт по плотному списку живых строк своей фракции
  (`FPosValid`, не мёртв), радиус обзора берёт из колонки `_atkRange` по той
  же формуле `vision_radius`, сливает по ячейке `SRC_CELL` с наибольшим
  радиусом — те же 74 источника, **1.56 мс**. GDScript собирает только
  постройки и постоянные засветы. Ручка прежняя — `perf_config.fog_core`;
  без неё работает старый путь целиком.
- **ГАРНИЗОН — НЕ ИСТОЧНИК ОБЗОРА И НЕ ЦЕЛЬ:** у бойца вне карты снят
  `FPosValid` (`Unit.set_off_map` ← `Castle.absorb_unit`), а ядро отбирает
  источники по этому же признаку. Разбор — в разделе «Постройки: башня».
- **Боец прячется от чужих глаз САМ**, веткой внутри `tick_visual`
  (`Unit._hide_in_fog`). Значит, любой, кому погасили ВИЗУАЛЬНЫЙ тик, теряет
  единственный способ спрятаться и светится сквозь неразведанную черноту.
  Ровно это и делала спячка деревни гоблинов: `_set_dormant` гасил и физику, и
  визуал, и семьсот спящих были видны игроку через туман.
  **Спячка вправе гасить `set_tick`, но НЕ `set_draw`.** Стенд `qa_fog`, D7.
- Платы за это почти нет: стоящий боец гасит визуальный тик сам
  (`_proc_sleeping`), а будит его редкий общий обход
  `GameManager._wake_returned_far_units`. Спрятанный туманом при этом
  продолжает тикать (ветка тумана делает ранний `return`) — иначе ему нечем
  было бы вернуться на свет, когда разведка подойдёт снова.

## Звук

- `AudioManager` — автозагрузка. Боевые звуки идут через `play_3d` (3D-пул,
  туман, лимиты по категориям), интерфейс — через `play_ui` (свои плоские
  голоса, `PROCESS_MODE_ALWAYS`, звучат и на паузе).
- Четыре события интерфейса в `UI_BANK`: заказ юнита (`Building.queue_unit`),
  клик по исследованию (`Smithy.research`), выделение здания и выделение отряда
  (`GameManager.on_selection_changed`, только на РЕАЛЬНУЮ смену набора).
  Выделение «от игры» (закладка и достройка замка) идёт с `silent = true`:
  звук выделения означает клик игрока, а не выбор, сделанный самой игрой.
- Музыка — плейлист `MUSIC_PLAYLIST` с паузами: первый трек на 10-й минуте,
  затем 10 минут тишины, затем следующий, по кругу. Трек в партии обязательно
  раскручивается `_unloop()` — ресурс потока общий с меню, а меню его зацикливает.
- **У МЕНЮ СВОЙ ТРЕК** (`MUSIC_MENU`), а не первый из плейлиста партии.
  Заказ владельца, авг. 2026. Побочно это снимает старую связку: раньше меню
  зацикливало ТОТ ЖЕ ресурс, который партия обязана доигрывать до конца.
  `_unloop()` при этом оставлен — `MUSIC_BACKGROUND` по-прежнему в плейлисте,
  и защита дешевле доказательства, что зациклить его больше некому.

### Срез верха у 3D-голосов: умолчание движка, а не банк и не громкость

- **ЖАЛОБА ВЛАДЕЛЬЦА:** «в файле чёткий лязг доспехов и тяжёлый марш, а в игре
  слышна только лёгкая ходьба». Виноват был не банк, не импорт, не питч и не
  громкость, а **`AudioStreamPlayer3D.attenuation_filter_cutoff_hz` = 5000 Гц
  при `attenuation_filter_db` = −24 дБ**. Это «поглощение воздухом», и оно
  включено у КАЖДОГО трёхмерного голоса, пока его не выключишь явно.
- **ЗАМЕР (`qa_audio3`, анализатор спектра, тот же файл против себя без 3D):**
  марш на 30 м — 2-4 кГц −8.3 дБ, 4-8 кГц −20.4, 8-16 кГц −32.5; боевой пул —
  4-8 кГц −22.7, 8-16 кГц −35.2. **Металлический лязг живёт ровно в 4-16 кГц.**
- **РЕЖЕТ НА ЛЮБОЙ ДИСТАНЦИИ, ВКЛЮЧАЯ НУЛЕВУЮ** (столбцы «3D 30 м» и «3D 0 м»
  сходятся до 0.2 дБ): это не «дальний звук глуше», а постоянная порча тембра.
  Снимается одной строкой — `attenuation_filter_cutoff_hz = 20500` (движковый
  способ отключить фильтр совсем), и ставит её `AudioManager._open_filter()`
  ВСЕМ пулам сразу.
- **ЭТО ТА ЖЕ ЖАЛОБА, ЧТО КОГДА-ТО СПИСАЛИ НА ЛИМИТЕР** («юниты будто идут в
  чешках»). Лимитер сняли, помогло отчасти — настоящая причина была здесь, и
  её не нашли, потому что искали в своём коде, а не в умолчаниях движка.
- **СПЕКТР МЕРИТ НАКЛОН, А НЕ УРОВЕНЬ.** Трёхмерный голос тише плоского
  эталона во ВСЕХ полосах сразу (своя панорама, а у боевого ещё и спад по
  расстоянию) — это уровень, к тембру отношения не имеющий. Срез же
  избирателен: валит верх и не трогает низ. Стенд вычитает общий сдвиг по низу
  и середине и судит только отставание верха ОТ НИХ, иначе исправный тракт
  краснел бы на поправке в 3 дБ.

### Марш: затухание по земле и плавная остановка

- **С 09.09.2026 ЗАКОН ГРОМКОСТИ МАРША ДРУГОЙ — см. раздел «Аудио-окно
  камеры» ниже.** Два первых пункта этого подраздела («громкость по
  наземному расстоянию, поделённому на радиус отсечки» и «от зума не
  зависит») описывают ИСТОРИЮ: окно теперь задано метрами от точки фокуса,
  а зум гасит марш до нуля на пределе отдаления. Всё остальное (хвост
  затухания, запас удержания, лишний голос в пуле, «из тумана не топают»)
  в силе.
- **ГРОМКОСТЬ СЧИТАЕТСЯ САМИ, ПО НАЗЕМНОМУ РАССТОЯНИЮ** (`_march_gain_at`,
  `MARCH_FALLOFF_START`). Штатного затухания у марша нет и не будет
  (`ATTENUATION_DISABLED`): слушатель висит на камере, и её высота — это зум,
  то есть громкость марша задавало бы колесо мыши. Но и полного отсутствия
  спада мало — раньше отряд звучал В ПОЛНУЮ СИЛУ до самой границы отсечки и
  там обрывался («бесконечный гул ходьбы со всей карты»). Расстояние делится
  на ТОТ ЖЕ радиус, что у отсечки, а он следует за зумом: смысл выходит
  «слышно, пока отряд на экране, и тем тише, чем ближе он к его краю».
  **Панорама не тронута вовсе** — голос по-прежнему трёхмерный и стоит в точке
  отряда. Замер: на 80 % радиуса доля 0.16, за радиусом голоса нет.
- **«БАХ» НА ОСТАНОВКЕ — ЭТО `stop()` НА НЕПРЕРЫВНОМ ЛУПЕ.** Жалоба: «десять
  отрядов доходят до точки по очереди, и на каждой остановке резкий бах».
  Голос обрывался мгновенно, на случайной точке волны, — это по определению
  щелчок. Хуже: голос ОДИН на всех (`MARCH_VOICES`), и на каждой смене
  ближайшего отряда он не просто обрывался, а тут же запускался заново с
  нулевой позиции, добавляя к обрыву атаку сэмпла.
- **ЛЕЧЕНИЕ — ТРИ ЧАСТИ, И НУЖНЫ ВСЕ.** Затухание вместо `stop()`
  (`_march_release` уводит голос в ХВОСТ, `_tick_march` его досушивает);
  плавный вход у нового отряда (`MARCH_FADE_IN`); и **запас против чехарды**
  (`MARCH_KEEP_BIAS`) — держащий голос отряд считается ближе, чем он есть,
  иначе два идущих рядом отряда отбирают голос друг у друга по нескольку раз
  в секунду.
- **В ПУЛЕ НА ОДИН ГОЛОС БОЛЬШЕ, ЧЕМ `MARCH_VOICES`.** Догорающий хвост не
  возвращается в стопку свободных, пока не замолчит; без запасного голоса
  следующий отряд ждал бы конца затухания, то есть в марше возникала бы дыра.
  Бюджет это выдерживает: два голоса по `MARCH_WALK_DB` дают ровно
  `MARCH_BUDGET`, и то лишь на доли секунды нахлёста.
- **ЛОВУШКА СТЕНДА: ЖИВОЙ `GameManager` МЕШАЕТ ЗАМЕРУ.** Автозагрузка работает
  и в пустой сцене — четыре раза в секунду она шлёт ПУСТОЙ отчёт о марше
  (отрядов-то нет) и гасит отряд, только что заведённый стендом. Первая версия
  ловила на этом «голос не выдан» и обвиняла код. Обход глушится
  `GameManager.set_physics_process(false)`.
- **ВТОРАЯ ЛОВУШКА: СЛУШАТЕЛЯ НАДО ЗАВЕСТИ ЯВНО.** `AudioManager` ищет его
  через `Viewport.get_audio_listener_3d()`, а тот отвечает ТОЛЬКО про явно
  назначенный `AudioListener3D` — камера слушателем работает, но этим методом
  не возвращается. В партии узел ставит `Main`; без него у марша не работают
  ни отсечка по расстоянию, ни сортировка по близости.

### Реплики команд и марш пехоты

- **КАНАЛОВ ТРИ, А НЕ ДВА, И ЭТО НЕ ИЗБЫТОЧНОСТЬ.** `play_ui` — щелчки
  (короткие, по кругу, звучат на паузе); `play_voice` — РЕПЛИКИ («вперёд,
  марш», «держать строй», рог к атаке); `play_3d` — бой. Реплика длится
  СЕКУНДЫ, а голоса интерфейса раздаются по кругу: положи её туда — и четыре
  щелчка подряд (заказ десяти рабочих это десять щелчков в один кадр) обрубят
  приказ на полуслове. Голосов у реплик ДВА: один — и приказ следом за приказом
  молчал бы; много — и команды полезли бы хором, а это не боевой клич, где хор
  заказан намеренно.
- **У РЕПЛИК ДВА ОКНА ПОВТОРА, И ОБА НУЖНЫ.** Своё на событие (`VOICE_LIMITS`,
  5-6 с) — игрок щёлкает ПКМ подряд, поправляя точку, и без окна «вперёд,
  марш!» кричалось бы на каждый щелчок. Общее (`VOICE_GLOBAL_GAP`) — своё окно
  не спасает от наложения РАЗНЫХ реплик: «в атаку» и «держать строй» события
  разные, оба законные, и подряд звучат одно поверх другого.
- **МАССОВОСТЬ СЧИТАЕТСЯ В ОТРЯДАХ, А НЕ В БОЙЦАХ**
  (`SelectionManager.selected_squad_count`, порог `MASS_ORDER_SQUADS` = 5).
  Пять отрядов копейщиков это триста человек, но триста человек могут быть и
  одним отрядом: считай стенд бойцов — порог срабатывал бы всегда.
  Точки подключения: марш — `_issue_formation_move` (туда сходятся ВСЕ ветки
  приказа движения, включая бег двойным ПКМ), рог — ветка `is_attack_cmd` в
  `_handle_right_click`, «держать строй» — `_execute_line_formation` (жест
  завершается на ОТПУСКАНИИ ПКМ). У строя условие через ИЛИ: копейщики любой
  численностью либо массовое выделение.
- **ИСТОЧНИК ТОПОТА — ОТРЯД, А НЕ БОЕЦ.** Тысяча бойцов это тысяча лупов;
  отряд же и есть то, что марширует. Вход один и ПАКЕТНЫЙ
  (`AudioManager.march_report`), собирает его `GameManager._sweep_march_audio`
  раз в `MARCH_SWEEP_SEC`, опрашивая ВЫБОРКУ бойцов (тот же приём, что у
  `_formation_broken`).
- **ОПРОС СОСТОЯНИЯ, А НЕ СОБЫТИЯ «ПОШЁЛ/ВСТАЛ».** События пришлось бы
  расставить по всем путям, где отряд начинает и кончает движение (приказ,
  прибытие, бой, отход, гарнизон, гибель последнего), и любой забытый путь
  оставляет вечно топающий луп над стоящим отрядом — луп, в отличие от удара
  мечом, сам не кончается НИКОГДА. По той же причине марш глушится явно и в
  паузе (`set_paused`), и при выходе в меню, и в `stop_all_music`.
- **`squad_members()` — НЕ ЧИТАТЕЛЬ.** Опустевший отряд он РАСПУСКАЕТ прямо в
  геттере. Обход, опрашивающий все отряды подряд, натыкается на пустые
  постоянно: барак сначала заводит отряд, а бойцов доставляет следующими
  кадрами. Первая версия марш-обхода звала его и уничтожала свежие отряды
  раньше, чем их наполняли, — семь заказов давали один отряд вместо семи
  (поймал `qa_squad`, проверка 8). Соседний `_sweep_reform` зовёт его
  безнаказанно только потому, что до него не доходит: пустой отряд отсеивается
  раньше проверкой на пустую разметку. **Состав для чтения брать напрямую.**
- **МАРШ ИГРАЕТ ОДНИМ ЧИСТЫМ ЛУПОМ, БЕЗ ЕДИНОЙ ОБРАБОТКИ** (`MARCH_VOICES` = 1,
  питч 1.0, старт с нуля). **ЭТО РАЗВОРОТ, И ВАЖНО ПОНИМАТЬ, ЧЕГО ИМЕННО.**
  Здесь стояли ШЕСТЬ голосов, и держались они друг за друга: копии одного файла
  складываются когерентно, поэтому каждому отряду раздавались свой ПИТЧ и свой
  СДВИГ СТАРТА. То есть питч и фаза были не украшением, а ПЛАТОЙ ЗА
  МНОЖЕСТВЕННОСТЬ голосов — убери их, оставив шесть, и получишь клиппинг.
  Владелец услышал в этом «поломанный звук» и попросил чистый оригинальный луп,
  поэтому убрана ПРИЧИНА: голос один, складывать нечего, разводить нечего.
  Марш и так СЛОЙ АТМОСФЕРЫ (затухание снято), поэтому остальные пять голосов
  не давали ни направления, ни информации — только плотность. Панорама
  сохранилась: единственный голос ставится на БЛИЖАЙШИЙ марширующий отряд.
  Отряд, всё ещё попадающий в число ближайших, СВОЙ ГОЛОС СОХРАНЯЕТ — перезапуск
  на каждом обходе превратил бы марш в чавканье четыре раза в секунду.
- **ПИТЧ ВЫСТАВЛЯЕТСЯ ЯВНО, А НЕ «ОСТАВЛЯЕТСЯ КАК ЕСТЬ»**: голоса берутся из
  пула и переиспользуются, и без явного возврата к 1.0 следующий марш
  унаследовал бы расстройку предыдущего. Та же грабля, что у плоского затухания
  в `play_3d`.
- **`MARCH_TEMPO` СНЯТ ВМЕСТЕ С ПИТЧЕМ.** Заказ «пехота идёт быстрее, чем
  звучит» решался множителем 1.15, а питч тянет не только темп, но и ВЫСОТУ
  ТОНА — то есть искажает сам файл. Растяжения времени без смены высоты у
  `AudioStreamPlayer` нет вовсе: если темп понадобится снова, его придётся
  решать ДРУГИМ СЭМПЛОМ, а не обработкой этого.
- **ГРОМКОСТЬ ВЫВЕДЕНА ИЗ БЮДЖЕТА, А НЕ ПОДОБРАНА НА СЛУХ** (`MARCH_BUDGET`
  = 0.5 полной шкалы на весь марш). Некогерентная сумма N источников растёт
  как √N, отсюда потолок на голос ≈ −13.8 дБ при шести голосах. Первые числа
  (−12/−9 дБ) давали бегу 0.87 полной шкалы ОДНИМ топотом — не фон, а
  доминанта поверх боя (поймал `qa_voice`, блок F). Поднимать громкость можно
  только ВМЕСТЕ с бюджетом и понимая, чем платит бой.
- **ЧТО НЕ СЛЫШНО, ТО И НЕ ДЕКОДИРУЕТСЯ.** Затухание делает дальний марш
  неслышимым, но НЕ бесплатным: движок честно декодирует поток, а лупы лежат в
  mp3 — самом дорогом здешнем формате. Отсечка по `MARCH_MAX_DISTANCE` стоит ДО
  раздачи голосов, как `SFX_CULL_DISTANCE` в `play_3d`. **Из тумана не топают**
  — то же правило и та же причина, что у боевых звуков, и для непрерывного лупа
  она даже весомее: по нему слышно не только «там кто-то есть», но и куда идёт.
- **ЛИМИТЕРА НА МАСТЕРЕ БОЛЬШЕ НЕТ** (`AudioEffectHardLimiter` снят по заказу
  владельца: «пропал звон кольчуги и лязг оружия, юниты будто идут в чешках»).
  Хард-лимитер первым делом срезает ТРАНЗИЕНТЫ — короткие яркие атаки, а
  металлический лязг это ровно они; в большом бою он работал постоянно и
  пригибал весь звук под самый громкий источник. `_ensure_limiter()` оставлен и
  СНИМАЕТ эффект, если тот появился с прошлого запуска. Запас держат только
  громкости: у марша он посчитан бюджетом, у остальных — их `db` в `SFX_LIMITS`.
- **ГРОМКОСТЬ МАРША НЕ ЗАВИСИТ ОТ КАМЕРЫ** (`ATTENUATION_DISABLED`). Жалоба:
  «при зуме звук ходьбы скачет от резкого громкого до мягкого». Так и было:
  марш звучал обычным 3D-голосом с обратно-квадратичным затуханием, а слушатель
  висит на RTS-камере — на приближении он оказывался в двух метрах от строя, на
  отдалении за сотню. Громкость марша задавало движение мыши, а не бой. Марш —
  не событие в точке, а СЛОЙ АТМОСФЕРЫ, как лес: затухание снято совсем,
  панорама (лево-право) осталась. Что «рядом», решает отсечка, и она **следует
  за зумом** — тем же приёмом, что радиус LOD.
- **ОТСЕЧКА МАРША МЕРИТСЯ ПО ЗЕМЛЕ, А НЕ В ПРОСТРАНСТВЕ.** Слушатель висит на
  камере, и его ВЫСОТА — это и есть зум: мерь расстояние в трёх измерениях, и
  отдалённая камера отсекала бы марш прямо у себя под ногами. Камерная
  зависимость, которую отсюда и убирали, вернулась бы через заднюю дверь.
  Поймал `qa_voice`, блок E.
- **ГРОМКОСТЬ ПЕРЕСЧИТАНА ПОД ОДИН ГОЛОС.** История ручки длинная и качалась в
  обе стороны: −23/−20 («тихо») → −20.7/−17.7 → −17.2/−14.2 (вместе со снятием
  лимитера и слоем звона доспехов) → −19/−16 при шести голосах. Голос стал
  один, и прежние децибелы означали бы падение громкости в √6 раз — ровно ту
  жалобу «тихо», с которой правка и начиналась. Считается от того же бюджета:
  **`MARCH_WALK_DB` −9.1, `MARCH_RUN_DB` −6.6** — это 0.35 и 0.47 полной шкалы
  при потолке `MARCH_BUDGET` 0.5, то есть чуть выше прежней СУММАРНОЙ громкости
  марша (0.28 и 0.39). Проверяет `qa_voice`, блок F. **Громкость — единственное,
  чем марш теперь регулируется**, и поднимать её можно только вместе с
  бюджетом, понимая, чем за это платит бой.
- **СЛОЙ «ЗВОН ДОСПЕХОВ» СНЯТ ОКОНЧАТЕЛЬНО — ЭТО ВТОРОЙ ОТКАТ ПОДРЯД.** Он
  заводился дважды. Первый раз (`metal_punch_06` с питчем, раз в треть секунды)
  откачен вердиктом «регулярный удар по кастрюле, надетой на голову». Второй
  (четыре разных `Sword_hit_armor`, случайный интервал 0.35-1.10 с, −24 дБ,
  питч +25-75 %) чинил все три названные причины отказа — и получил тот же
  вердикт: «посторонние глухие удары, бах-бах». **ВЫВОД, КОТОРЫЙ СТОИТ
  ЗАПОМНИТЬ:** звон снаряжения — это десятки мелких НЕСИНХРОННЫХ призвуков, а
  любой отдельный удар поверх НЕПРЕРЫВНОГО лупа читается как посторонний стук,
  сколько его ни рандомизируй. Свести их мог бы только свой сэмпл кольчуги, а
  его в проекте нет. Слой доспехов при этом не потерян — он записан ВНУТРИ
  `community-marching-loop`, ради чего луп и выбран. **Третьей попытки поверх
  лупа быть не должно**, и это стережёт `qa_voice` D4б: категории `armor_march`
  нет ни в банке, ни в таблице лимитов.
- **РАБОЧИЕ НЕ ПОЛУЧАЮТ НИ КЛИЧА, НИ РЕПЛИК КОМАНД, И ЛОМАЛОСЬ ЭТО НЕ ТАМ, ГДЕ
  ИСКАЛИ.** Жалоба владельца: «выделяю рамкой пять крестьян — они издают
  громкий боевой клич, будто это фаланга копейщиков». Сам клич (`battle_cry`)
  им не доставался и раньше: `squad_battle_cry` отсеивает отряды без оружия.
  Слышно было ДРУГОЕ — РЕПЛИКУ КОМАНДЫ («вперёд, марш»), а она раздаётся, когда
  приказ МАССОВЫЙ. Массовость же считалась по ЧИСЛУ ОТРЯДОВ, и вот тут рабочие
  и пролезали: каждый из них числится отрядом ИЗ ОДНОГО ЧЕЛОВЕКА (учётная
  единица, иначе его не выделить и не показать в панели), то есть пять
  крестьян — это ровно пять «отрядов». Теперь `_mass_selection` считает
  `selected_combat_squad_count()`.
- **ПРИЗНАК «БОЕВОЙ» — ЭТО ОРУЖИЕ, А НЕ СПИСОК ТИПОВ**
  (`GameManager.squad_is_combat`, `attack_damage > 0`). Рабочий обнуляет урон
  явно (`Worker._ready`), и то же правило само собой накроет любого будущего
  невооружённого — обозника, лекаря, поселенца. Список типов пришлось бы
  дополнять, и забытая строка означала бы вернувшийся клич. Место одно на оба
  канала: по нему же отсеивается и сам клич — два описания «кто здесь солдат»
  неминуемо разошлись бы. Стережёт `qa_voice`, блок C6б-C6е (обоз при войске
  массовости не отнимает: 6 боевых из 13 отрядов — реплика звучит).
- **ЦЕПОЧКА РЕПЛИК** (`play_voice_chain`): приказ атаки большой армией — это
  рог, а следом клич. Два вызова `play_voice` подряд этого не дают: их разводит
  общее окно `VOICE_GLOBAL_GAP` — и правильно, оно защищает от СЛУЧАЙНЫХ
  наложений. Очередь и есть способ отличить «два звука совпали» от «так
  задумано». Клич вступает под затухающий хвост рога (`VOICE_CHAIN_OVERLAP`), а
  не после полной паузы: рог длится четыре с половиной секунды, и пауза
  разорвала бы связь между приказом и откликом.
- **МАРШ ЗВУЧИТ И НА ПРИКАЗЕ АТАКИ.** Отряд, посланный на противника, идёт к
  нему в состоянии `ATTACKING` — цель назначена, а ноги ещё не донёс, — а обход
  марша считал марширующим только `State.MOVING`, и на приказе атаки топот
  пропадал целиком. Теперь «идёт к цели» отличается от «дерётся» по дистанции:
  дальше длины руки — значит шагает.
- **ПОРОГИ РЕПЛИК: ВСЕ ТРИ ОДИНАКОВЫ — ПЯТЬ ОТРЯДОВ.** У «держать строй» было
  «копейщики ЛЮБОЙ численностью ЛИБО пять отрядов», и первая половина
  оказалась спамом: растяг ПКМ — обычный микро-контроль, игрок делает его
  десятки раз за бой двумя-тремя отрядами.
- **У АТАКИ ЗВУЧИТ ТОЛЬКО РОГ.** Была цепочка «рог, затем forward march», и
  владелец её отменил: «вперёд, марш» — реплика МАРША, поверх приказа атаки она
  читается как чужая. Механизм цепочки (`play_voice_chain`) оставлен и
  проверяется стендом: он понадобится, когда приедет своя озвучка атаки.
- Стенд — `qa_voice` (46 проверок: файлы, окна повтора, все четыре триггера
  через настоящие приказы, чистота лупа марша (один голос, питч 1.0, старт с
  нуля, ни одного слоя сверху), молчание рабочих, туман, пауза, бюджет).

## Разбор жалоб по скриншотам боя и интерфейса (авг. 2026, вторая партия)

Восемь пунктов заказа владельца. Ниже только то, что нельзя вывести из кода.
Стенд под первые два — **`qa_wall`** (9 проверок, headless).

- **ОБЩИЙ ОТВЕТ ОТРЯДА О БЛИЖАЙШЕМ ВРАГЕ ГОДИТСЯ НЕ ВСЕМ, И ЭТО И БЫЛА
  «ПАССИВНАЯ ФАЛАНГА».** `Unit._squad_cached_enemy` отдаёт цель, найденную
  ПЕРВЫМ спросившим бойцом отряда. Пока радиус велик (лучник смотрит на 20 м,
  марш — на 10), отряд помещается внутрь этой разницы целиком и ответ честен
  для всех. В РУКОПАШНОЙ он честен ровно для одного человека: у копейщика
  радиус два метра, а отряд стоит пятном в пять-шесть.
  Дальше начиналось главное: боец второй шеренги получал недостижимую цель, в
  стойке «Защита» сходить с места не вправе и цель НЕ БРОСАЕТ — то есть
  навсегда оставался в `State.ATTACKING`. А авто-агро обслуживает ТОЛЬКО
  `State.IDLE`: подошедшего вплотную врага он уже не увидит никогда. Это и есть
  «дерутся два-три крайних копейщика, остальные стоят и смотрят».
  Лечение ДВОЙНОЕ, и нужны обе половины: кэш проверяет дистанцию ДЛЯ
  СПРАШИВАЮЩЕГО (промах = свой скан, как до кэша), а ветка обороны в
  `_process_attack` тем же тактом ищет того, до кого достаёт. Замер `qa_wall`
  A2: из тех, кто дотягивается до врага, дерутся 100 %.
- **ЧУЖОЙ ВЕРНЫЙ ОТВЕТ КЭША НЕ ПЕРЕТИРАЕМ.** Промахнувшийся боец ищет сам, но
  в кэш своё не кладёт: для того, кто ответ положил, он верен, и подмена гоняла
  бы скан по всему отряду по кругу.
- **РАЗБИТАЯ ФАЛАНГА ПРОПУСКАЕТ ПРОТИВНИКА ЗАКОННО.** Первая версия проверки
  «сквозного прохода» считала врагов в тылу В КОНЦЕ боя и краснела всегда — к
  тому времени строя уже не было. Мерить надо на ЖИВОМ строю (`qa_wall`,
  `WALL_INTACT_FRAC`): вопрос стенда не «победил ли кто», а «может ли враг
  оказаться за спиной у целой шеренги».
- **СЕКТОР У ЗДАНИЯ — ЭТО СПОСОБ ПОДОЙТИ, А НЕ МЕСТО, К КОТОРОМУ ПРИВЯЗАН
  БОЕЦ.** Здесь была КАЧЕЛЯ, и она объясняет «отряды дёргаются и бесконечно
  перебегают» целиком: дойдя до точки сектора (`RING_ARRIVE`), боец получал
  обычный путь, делал шаг к стене — и тем самым снова уходил от точки дальше
  порога, после чего подход возвращал его на кольцо. Замер: сто пятьдесят
  копейщиков встали в 4.97 м от хижины при дистанции удара 4.0 и за
  восемнадцать секунд не сняли с неё НИ ОДНОГО очка. Признак `Unit._ring_done`
  взводится в момент прихода и снимается новым приказом.
- **МЕСТ У СТЕНЫ СТОЛЬКО, СКОЛЬКО ИХ ЕСТЬ, И ОСТАЛЬНЫЕ ЖДУТ.** Вместимость
  считается по РАБОЧЕЙ дуге (`SelectionManager.RING_FIGHT_REACH`) — той, с
  которой до стены дотягиваются оружием, — а не по кольцу подхода: дойдя до
  кольца, отряд всё равно идёт к стене, и десять отрядов снова оказываются в
  одной куче. Кому места не хватило, тот получает точку во втором-третьем ряду
  и признак «держать позицию» (`GameManager.squad_attack_hold`). Ждёт он НЕ
  ВЕЧНО: срок `RING_HOLD_SEC` обязателен — иначе выбитую переднюю линию некому
  сменить, постройка о своих обидчиках ничего не сообщает.
- **ШАГ МЕЖДУ СЕКТОРАМИ ВЫВОДИТСЯ ИЗ ШИРИНЫ ОТРЯДА** (`_ring_step`), а не из их
  числа. Прежняя раздача ровных долей дуги не зависела от численности вовсе: у
  хижины дуга 17 м, на десять отрядов по 1.7 м на сектор при ширине отряда из
  пятнадцати человек около трёх — сектора перекрывались вдвое. Тот же приём и
  та же причина, что у заслона ИИ (`EnemyAI._screen_step`).
- **СМЕНА РОЛИ НЕ ВЫДЁРГИВАЕТ ОТРЯД ИИ ИЗ РУКОПАШНОЙ** (`EnemyAI._issue_plan`,
  ветка `need_issue`). Защита `_in_melee` там уже стояла, но СРАБАТЫВАЛА ТОЛЬКО
  когда приказ отряду уже был отдан. Ролей у обороны три, такт размышления
  меняет их на ходу — и сцепившийся отряд получал новую роль вместе со свежим
  `command_move`, а тот у бойца с живой целью цель СНИМАЕТ. Отряд
  разворачивался спиной прямо в контакте, доходил до нового места, авто-агро
  вело его обратно: «подбегают, бьют, отбегают назад и снова бегут». Отход —
  единственное исключение, и оно обязательно.
- **ЗНАМЕНОСЕЦ СТОИТ В [РЯД 0, КОЛОНКА 0] ПО РАЗМЕТКЕ, А НЕ ПО ГЕОМЕТРИИ.**
  Заказ владельца. Прежнее правило («самый выдвинутый вперёд, из них самый
  левый») считает угол по ФАКТИЧЕСКИМ точкам, а они в бою строя не описывают:
  «передний» — это тот, кого вынесло вперёд свалкой. Разметка знает угол точно:
  `slots[0]` по построению — крайнее место ПЕРВОЙ шеренги со стороны левого
  фланга. **Передача павшего знамени идёт туда же** — прежнее «подхватил
  ближайший сосед» читалось на экране хорошо, но за бой расползалось по всему
  отряду и обратно не возвращалось. Плюс раз в `BEARER_RECHECK_SEC` знамя
  сверяется с углом и возвращается само — но НЕ В БОЮ.
- **ТЫЛОВОЙ ЭШЕЛОН РАСТЯГИВАЕТСЯ ВДВОЕ ПРОТИВ СВОЕЙ ДОЛИ**
  (`SelectionManager.BACK_ECHELON_WIDEN`, `BACK_ECHELON_MAX_ROWS`). Честная доля
  по численности даёт одинаковую ПЛОТНОСТЬ, а игрок просит ЛИНИЮ: двадцать
  лучников за восемьюдесятью копейщиками занимали четверть фронта и ложились
  «колбасой» в пять шеренг, из которых стрелять может передняя. Берётся большее
  из двух — удвоенной доли и ширины, при которой эшелон не глубже трёх шеренг.
  **Отдельно выделенный отряд стрелков сюда не попадает вовсе:** он первый и
  единственный эшелон, `front_men` равен его численности, ветка не срабатывает.

### Анимация ходьбы и стойка «Защита»

- **СКОРОСТЬ МЕРИТСЯ ОКНОМ, А НЕ ОДНИМ ТИКОМ** (`Unit.MOVE_ANIM_SAMPLE_MS`).
  Разность между двумя соседними тиками включает не только шаг: разбор
  наложения двигает бойца на `SEP_MAX_STEP` раз в `SEP_INTERVAL`, а это около
  метра в секунду — БОЛЬШЕ порога. В любой тесноте стоящий боец числился
  идущим: «отряд стоит на месте, играет анимация бега». За окно в две десятых
  секунды знакопеременные подталкивания гасятся, а настоящий шаг даёт полметра
  в одну сторону.
- **ВЫДЕРЖКА ПРИЗНАКА ОБЯЗАНА ПЕРЕКРЫВАТЬ ОКНО ЗАМЕРА, А СМЕНА ПРИЗНАКА —
  МЕТИТЬ ПОЗУ ГРЯЗНОЙ.** Полный разбор позы идёт раз в `anim_every`
  ВИЗУАЛЬНЫХ тактов, а такт шардирован: на большой армии это до двадцати пяти
  кадров, больше 0.4 с. Прежняя выдержка 0.18 с была КОРОЧЕ периода опроса —
  признак успевал погаснуть и загореться между двумя опросами, и каждый боец
  ловил его в своей случайной фазе. Это и есть «половина отряда идёт, половина
  замерла в покое». Нужны обе половины лечения: одна выдержка расписание не
  обходит.
- **ФАЛАНГА ВЫСТАВЛЯЕТ КОПЬЯ С ПЕРВОГО КЛИКА, И ВТОРОЕ НАЖАТИЕ НИЧЕГО НЕ
  ЧИНИЛО — ОНО ДАВАЛО ВРЕМЯ.** Складывалось три задержки: ряд пересчитывается
  по таймеру `RANK_RECHECK` (до 0.25 с), личный разброс опускания копья был
  до 1.9 с, и поверх — расписание разбора позы (до 0.4 с). Итого больше двух
  секунд, за которые игрок успевает нажать кнопку второй раз.
  Лечится в трёх местах разом: `Unit.set_stance` взводит `_rank_timer` в ноль,
  `Spearman.DROP_DELAY_MAX_MS` сокращён до 300 мс (волна осталась, шлагбаума
  по-прежнему нет), а истечение личного срока само метит позу грязной
  (`Spearman._update_walk_anim`) — порядок условий там выбран так, чтобы
  обращение к часам стояло ПОСЛЕДНИМ. Стенд `qa_wall`, A0.

### Интерфейс и пивоты (вторая партия)

- **ЧЁРНЫЙ ПРЯМОУГОЛЬНИК У ЦИФРЫ ЗАКАЗА — ЭТО ПУСТОЙ ДВИЖКОВЫЙ ТУЛТИП.**
  `QuietTooltipButton._make_custom_tooltip` возвращал пустой `Control`. Текст он
  действительно убирал, но САМ ПУЗЫРЬ оставался: движок кладёт возвращённый узел
  внутрь `TooltipPanel` и рисует её фон по теме. Правильная точка —
  `_get_tooltip`: пустая строка означает «подсказки нет», и движок не создаёт ни
  узла, ни панели. Поле `tooltip_text` при этом не трогается, и стенды
  по-прежнему находят кнопки по нему.
- **КАРТОЧКА ЮНИТА ВЫРОВНЕНА ПО ЛЕВОМУ КРАЮ НИЖНЕЙ ПАНЕЛИ** (заказ владельца,
  разворот правила «строго над наведённой кнопкой»). Кнопки найма прижаты к
  ПРАВОМУ краю панели, а карточка вдвое шире кнопки и центрировалась на ней —
  её левый край оказывался где угодно, лишь бы не у панели. Кромка берётся из
  живого прямоугольника панели, а не из `PANEL_LEFT`: ширину считает содержимое
  (`_panel_w`). Правило «не ложиться на панель статов» осталось и стоит после.
- **РЯД ЗАКАЗОВ ВЫРОВНЕН ПО ИКОНКЕ ЗДАНИЯ** (`_queue_frame` получает
  `SHRINK_END` при `_castle_boost`). Подпись «Замок N/N HP» лежит внутри панели,
  прижатая к верхней кромке, и ровно под неё опущена крупная иконка Замка; бокс
  очереди же стоял по вертикальному ЦЕНТРУ и поднимался выше иконки, под самую
  строку подписи.
- **КОЛЬЦО ПОД ЗДАНИЕМ БЫЛО НЕ СМЕЩЕНО, А ШИРОКО.** Замер `qa_pivot` B3: у
  хижины подъём кольца 0.421 м при нижней кромке рисунка ровно 0.421 — оно
  лежит там, где надо. «Смещённым» его делал РАЗМЕР: 1.64 м полуширины рисунка
  плюс запас 0.35 давали круг диаметром 3.98 м под домом шириной 3.28. Под
  камерой в 45° ближняя дуга такого круга уходит заметно ниже подошвы, и глазом
  это читается не как «широкое кольцо», а как «кольцо не под домом».
  `SelectionDecalRenderer.BUILD_RING_MARGIN` сокращён до 0.15.
- **ВОРОТА ЛЕЖАТ НА ПЕРИМЕТРЕ РИСУНКА, А НЕ ЗА НИМ** (`Building.gate_depth`).
  Вынос считался от КОРОБКИ ИЗ КОНФИГА, а кольцо на земле — от НАРИСОВАННОГО
  основания, и коробка почти всегда крупнее: боец честно вставал по коробке, то
  есть СНАРУЖИ кольца, на пустой траве. Берётся меньшее из двух — дальше
  периметра рисунка ворота не уезжают никогда. И сдвигаются по X на `_draw_cx`:
  иначе ворота и кольцо описывают разные «середины» одного дома.

### Кусты по краю рудной гряды

- Заказ владельца по скриншоту: и каменная гряда, и золотая жила выходят из
  земли с двумя-тремя мелколистными кустиками по краю
  (`Main._scatter_cluster_bushes`).
- **САЖАЮТСЯ ПО КРАЮ, А НЕ ВНУТРИ:** куски руды кликают мышью и рубят рабочие,
  куст между ними перекрывал бы силуэт куска. Точки берутся на ОВАЛЕ габарита
  кучи — тех же полуосях, что у подсветки, — с разбросом наружу.
- **РАЗМЕР МЕНЬШЕ, ЧЕМ У ЗАРОСЛЕЙ** (1.2–2.0 против 2.2–3.6): это подбивка под
  камень, а не отдельная поросль.
- **`WORLD_GEN_VERSION` ПОДНЯТА ДО 2.** Сама расстановка руды не изменилась, но
  кусты берут числа из ТОГО ЖЕ генератора случайных: поток сдвинулся, и одно
  зерно даёт другой лес и другое озеро. Старое сохранение легло бы постройками
  на чужой рельеф.

## Третья партия правок по скриншотам (авг. 2026): бой, картинка, туман

Стенд под первые три пункта — **`qa_wall`** (13 проверок, блок C добавлен под
марш за спину противнику).

- **«ВЫХОД ИЗ БОЯ» РАЗДЕЛЯЕТСЯ НЕ ВРЕМЕНЕМ, А ФАКТОМ УПОРА.** Признак
  `_disengaging` снимается по приходу в точку приказа — а отряд, посланный ЗА
  спины противника, в точку не приходит НИКОГДА: он упирается в чужой строй и
  дальше не идёт. Тем же признаком заглушены и перехват марша, и ОТВЕТ НА УДАР
  (`take_damage`), поэтому передние шеренги честно стояли в позе ходьбы и
  умирали, ни разу не отмахнувшись.
  **СРОК ПРИЗНАКУ ПРОБОВАЛИ ДАТЬ И ОТКАТИЛИ:** шести секунд лучнику не хватает
  (двадцать пять метров отхода — это пятнадцать), срок истекал на полпути, и
  отряд разворачивался стрелять — ровно та беда, от которой признак и лечит
  (поймал `qa_disengage` A4: ушло 2-3 из 6 вместо шести). Правильный
  разделитель — «иду прочь» против «упёрся и никуда не иду», и оба места
  спрашивают именно его.
- **ФИЗИЧЕСКИЙ УПОР ПЕРЕБИВАЕТ ЛЮБОЙ ЗАМОК ПРИКАЗА** (`Unit._process_move`,
  признак `wall`). Разделены два разных случая, которые раньше глушились одним
  условием. «Враг в дальности оружия» — не повод бросать приказ, ровно от такого
  перехвата замок и заведён. А «мой шаг УПЁРСЯ в чужое тело» (`_enemy_contact`
  ставит солвер в тот же кадр, когда шаг не прошёл) — это тупик: пройти нельзя,
  и единственное осмысленное действие — драться. **Окно прохода приказа
  (`_forced_move_pass`) из упора БОЛЬШЕ НЕ ИСКЛЮЧЕНО** (03.09.2026): пока
  билет снимал проверку тел, упора под ним не бывало; с ядром
  (`PassCoreFrac`) упор под билетом — обычное дело, и с прежним исключением
  боец стоял три секунды замка под ударами, не отвечая (`qa_wall` C1: 7 из
  38 дерущихся). «Реально выбирается» от «упёрся» отличает `moved_recently`.
- **УДАР В УПОР ОТВЕТ ПОЛУЧАЕТ, ЕСЛИ БОЕЦ НИКУДА НЕ ИДЁТ**
  (`Unit._may_answer_blow`). Замок приказа и выход из боя глушили ответный удар
  БЕЗУСЛОВНО. Смысл у обоих один — «не отвлекайся на дальнюю стычку», — но у
  бойца, которого рубят с длины руки и который при этом СТОИТ (впереди чужое
  тело), никакого «иди дальше» нет. Порог дистанции тот же
  `LOCK_BLOCKER_RANGE`, что и у помехи под замком цели, а второе условие —
  `moved_recently()`: оно отвечает по РЕАЛЬНОМУ смещению и потому знает разницу
  между «иду прочь» и «перебираю ногами, упёршись в строй». Одной дистанции
  мало: отходящий лучник получает удары вплотную ровно в первые секунды отхода.
- **ПРОРЫВ ВСАДНИКА КОНЧАЕТСЯ У ЧУЖОГО ТЕЛА** (`Unit._fling_solid`). Разлёт —
  ЕДИНСТВЕННОЕ движение в проекте, которое пишет координату напрямую, минуя
  пакетный шаг со всей его геометрией: ни ствола, ни чужого строя он не замечает.
  Пехоте это безразлично (её несёт ПРОЧЬ от врага), а всаднику после удара
  выдаётся `charge_breakthrough` «вперёд, в пробитую дыру» — и он проезжал сквозь
  любое число шеренг, потому что спросить о них было некому. Это и есть «рыцари
  проникают сквозь строй, разворачиваются на 180° и бьют в спину». Теперь прорыв
  упирается в первое же чужое тело и гаснет, а на бойце ставится `_enemy_contact`
  — тот же признак, что и у упёршегося шага.
- **ЗЕРКАЛО СПРАЙТА БЕРЁТ ФАКТИЧЕСКОЕ НАПРАВЛЕНИЕ ХОДА, А НЕ ВЗГЛЯД**
  (`Unit._mv_dir`). `_facing` — это КУДА БОЕЦ СМОТРИТ, а смотрит он на цель:
  половина отряда на марше успевает подобрать противника и держит взгляд на нём,
  пока тело едет с отрядом в другую сторону. Зеркало считалось по взгляду — и
  боец ехал спиной; вторая половина, оставшаяся в `MOVING`, разворачивалась
  правильно. Отсюда «часть спрайтов развернулась, часть бежит спиной вперёд».
  `_mv_dir` берётся из ТОГО ЖЕ окна замера, что и признак ходьбы, поэтому в него
  не попадает ни расталкивание соседей, ни толчок в свалке.
- **«СТОЮ» ДЛЯ ЗЕРКАЛА СЧИТАЕТСЯ ПО ФАКТУ, А НЕ ПО `velocity`.** velocity — это
  НАМЕРЕНИЕ: у бойца, упёршегося в чужой строй, оно ненулевое, а у едущего по
  разлёту от тарана — наоборот, нулевое.

### Обмер спрайта обязан резать по тому же порогу, что и шейдер

- **`BillboardUtil._OPAQUE_A` = 0.5, а не 0.05, и это НЕ ВКУС.** Пустоту на
  экране отсекает `ALPHA_SCISSOR_THRESHOLD`, и во ВСЕХ билбордах проекта
  (`cyl_billboard`, `veg_multimesh`, `mm_unit_sprite`, `mm_corpse`) он равен 0.5.
  Пиксели между 0.05 и 0.5 НЕ РИСУЮТСЯ ВОВСЕ — а обмер их засчитывал, и всё, что
  от него зависит (кольцо на земле, форма попадания луча, ворота), оказывалось
  ниже и шире рисунка. Замер по Tree1-4: нижняя кромка по «альфа > 0» лежит на
  0.027-0.125 высоты кадра, по срезу 0.5 — на 0.063-0.162; в метрах
  пятиметрового квада это 0.19-0.26 м. Ровно тот отрыв кольца от комля, который
  видно на скриншоте ели. Число обязано ехать вместе со срезом.
- **У ДЕРЕВА БОЛЬШЕ НЕТ СВОЕГО ОБМЕРА.** `ResourceNode.hover_ring_lift` считал
  поле сам, через `Image.get_used_rect()` («непрозрачно всё, где альфа > 0»).
  Теперь он зовёт тот же `BillboardUtil.opaque_rect`, что и постройки: один
  обмер, один порог, один кэш.
- **РАДИУС КОЛЬЦА У ДЕРЕВА СОКРАЩЁН** (`Main.HOVER_TREE_RADIUS` 0.50 → 0.34,
  заказ владельца). Кольцо лежит НА ЗЕМЛЕ, и под камерой в 45° круг диаметром в
  метр под стволом шириной в полметра читается так: дальняя дуга режет комель,
  ближняя лежит на пустой траве.

### Туман войны: обзор сокращён

- **`VISION_MULT` 3.0 → 1.6, `VISION_MIN` 18 → 15, `BUILDING_VISION` 42 → 34.**
  Жалоба со скриншотом: «радиус открытия тумана от отряда слишком огромный,
  открывает почти полкарты». Так и было по арифметике: у лучника дальность 20,
  множитель 3 — обзор 60 м при полукарте 130×73, то есть один отряд стрелков
  раскрывал круг почти во всю ширину карты.
- **НИЖЕ ЕДИНИЦЫ МНОЖИТЕЛЬ ОПУСКАТЬ НЕЛЬЗЯ НИ ПРИ КАКИХ ПРАВКАХ БАЛАНСА:**
  обзор обязан быть больше дальности оружия, иначе стрелок бьёт в темноту, а
  игрок не видит, по кому.

## Баланс: TTK, мораль, экономика, ветеранство (авг. 2026)

**Расчётный документ — `docs/BALANCE_MATH.md`.** Здесь только то, что обязан
знать код; числа и их обоснование там. Стенд — **`qa_balance`** (20 проверок,
headless): формула брони, дуэли, схватка отрядов, паника, перки, темп экономики.

- **БРОНЯ СНИЖАЕТ УРОН ДОЛЕЙ, А НЕ ВЫЧИТАНИЕМ**
  (`unit_stats_config.damage_after_armor`, `ARMOR_SOFTNESS` = 25):
  `прошло = урон × A / (A + защита)`. Вычитание было сверхлинейным (каждое
  следующее очко брони дороже предыдущего) и ЗАВИСЕЛО ОТ ВЕЛИЧИНЫ УДАРА — одна
  и та же броня резала 60 % урона лучника и 25 % урона рыцаря, то есть цена
  очка скакала от того, кто по тебе бьёт. Долевая формула не даёт нуля НИКОГДА
  (при защите 500 проходит 4.8 %), и бессмертие невозможно по построению, а не
  по страховочному порогу. **Формула живёт в ОДНОМ месте** — и бой, и стенды, и
  панель статов обязаны звать её, а не повторять арифметику.
- **ЗАПАС ЖИЗНИ ПОДНЯТ ПОЧТИ ВДВОЕ, УРОН — ПОЧТИ НЕТ.** Копейщик 70 → 180 HP
  при уроне 15 → 14. Дуэль копейщиков была 9.3 с (пять ударов), стала 30 с
  (пятнадцать). Схватка отрядов 60 на 60: перелом на 31-й секунде, полное
  истребление на 57-й — ровно заказанное окно.
- **МОРАЛЬ — ЧИСЛО ОТРЯДА, А НЕ БОЙЦА** (`GameManager._sweep_morale`). Паникует
  и бежит отряд целиком; держать её на бойце значило бы усреднять шестьдесят
  чисел на каждую проверку и заводить шестьдесят таймеров ступора вместо одного.
- **ПОТЕРИ СЧИТАЮТСЯ ДОЛЕЙ ПОЛНОГО ШТАТА** (`squad_full_size` — НАИБОЛЬШИЙ
  состав за жизнь отряда, пишется в `add_to_squad`). Брать штат из конфига
  нельзя: отряд бывает неполным (последние из барака, огрызок после боя), и
  тогда гибель ВСЕГО состава снимала бы пятую часть морали — паника не
  наступала бы никогда. Поймал `qa_balance` C1.
- **ПОРОГОВ ПАНИКИ ДВА, И ОБА ОБЯЗАТЕЛЬНЫ.** Абсолютный (ниже 30 % морали)
  ловит отряд, который перемололи; относительный (вдвое ниже, чем у того, с кем
  дерусь) ловит другое — свежий отряд против ветеранов ломается ДО того, как
  потеряет половину людей. Без второго новобранцы дрались бы с легендой на
  равных до последнего человека.
- **ХРУПКОСТЬ РОДОВ ВОЙСК ВЫВОДИТСЯ ИЗ ОДНОГО ЧИСЛА.** Порог общий, а стартовая
  мораль разная — и лучник (45) ломается на 17 % потерь, копейщик (65) на 39 %,
  мечник (75) на 50 %, конница (100) на 78 %. Ни одной особой ветки в коде под
  это нет.
- **СТУПОР — ЭТО ПОТЕРЯ УПРАВЛЕНИЯ.** Двадцать секунд отряд не принимает
  приказов (`Unit._panicked` гейтит `command_move` и `command_attack`), не
  атакует (гейт в `_check_auto_aggro`) и получает защиту ×0.7. Добить его в это
  время можно и нужно: бой выигрывается сломом, а не полным истреблением.
- **ПРИКАЗ НА БЕГСТВО ОТДАЁТСЯ ДО ВЗВОДА ПРИЗНАКА** (`Unit.panic_flee`): иначе
  он отбился бы о собственный запрет, и паникующий строй остался бы стоять на
  месте с флагом над головой.
- **БЕЛЫЙ ФЛАГ — НЕ ВТОРОЙ УЗЕЛ, А СОСТОЯНИЕ ЗНАМЕНИ ОТРЯДА**
  (`SquadBanner.shown_white`). Разбор — ниже, в разделе «Белый флаг паники:
  два сценария, один узел».
- **АУРА ЛЕГЕНДЫ ВХОДИТ В ПОРОГ, А НЕ В САМУ МОРАЛЬ.** Иначе уход легенды с
  поля резал бы соседям накопленное число, и они паниковали бы от одного её
  отъезда.
- **НА КАЖДОЙ СТУПЕНИ РОВНО ТРИ КАРТОЧКИ, И ВСЕ ТРИ ИЗ ОДНОЙ ТРОЙКИ СТАТОВ**
  (`_VET_BONUS_TEMPLATE`, ступени 1-6). Заказ владельца: «выбор из пяти
  карточек — информационный шум». Было 3/4/5 по ступеням, а вторичные
  параметры брались из всех двенадцати ключей. Правило «ровно три ненулевых
  модификатора» соблюдалось и раньше — сменился не СЧЁТ, а НАБОР: теперь
  только атака, броня и запас жизни. Смысл в СРАВНИМОСТИ: раньше игрок выбирал
  между «+2 атаки и +4 морали» и «+2 брони и +0.3 напора», то есть между
  величинами из разных измерений; теперь вопрос один и тот же на всех
  ступенях — удар, защита или живучесть.
- **У КАЖДОЙ КАРТОЧКИ ОДИН ПАРАМЕТР В МИНУС, И БЕЗ НЕГО ВЫБОРА НЕТ ВОВСЕ.**
  Иначе это «возьми что нравится, всё равно всё хорошее». Минус всегда равен
  единице и всегда бьёт по тому стату, который карточка НЕ качает. Лестница
  прибавки прежняя: лычки (1-3) +2 к главному, флаги (4-6) +3, штандарт (7)
  даёт ПРАВИЛО вместо цифры. Запас жизни считается в своих единицах (+14 и
  +20) — та же доля от базы, что +2 и +3 у атаки с бронёй.
- **ПЯТЬ ЛЕГЕНДАРНЫХ ПЕРКОВ НЕ ТРОНУТЫ** (подтверждение владельца). Сокращение
  до трёх касается только числовых ступеней: седьмая даёт правило, и пять
  разных правил шумом не являются — их выбирают ОДИН РАЗ за жизнь отряда.
  Проверяет `qa_balance` D2 («три карточки на ступень») и D2б («параметры
  только из тройки»).
- **СЕДЬМОЙ ГРЕЙД ДАЁТ ПРАВИЛО, А НЕ ЦИФРУ.** Пять легендарных перков помечены
  id вида `legend_*` и лежат в том же списке `squads[sid]["chosen"]`, что и
  обычные выборы, — поэтому сохранение партии подхватывает их само, без
  отдельного поля и отдельной ветки в `SaveLoadManager`. Спрашивает их
  `GameManager.squad_has_perk`. Три числовых параметра у каждого оставлены
  намеренно: панель награды собирает подпись из ненулевых модификаторов и
  показала бы пустую плашку.
- **ПРОВЕРКА ПЕРКА — БИТ, А НЕ ПОИСК СТРОКИ** (`unit_stats_config.perk_bit` /
  `perk_mask_of`, порядок битов — `LEGEND_PERK_IDS`). `squad_has_perk` искал id
  линейным перебором строк по списку выбранных наград, а спрашивают его из
  `squad_armor_pierce`, то есть из `take_damage` жертвы — на КАЖДОЕ списание
  урона. У самого горячего спрашивающего бит ещё и снят один раз в поле
  (`_bit_pierce`): даже словарный поиск по строке — это хэш строки.
- **СЛОВО ПЕРКОВ ВЫВОДИТСЯ ИЗ СПИСКА, А НЕ ВЕДЁТСЯ ПО СОБЫТИЮ**
  (`GameManager._perk_bits_of`). Здесь было две неверных версии подряд, и обе
  стоит помнить. Ведение ПО СОБЫТИЮ (из `apply_veteran_choice`) пропускает
  загрузку партии и стенды, которые пишут `chosen` НАПРЯМУЮ. Сверка ПО ДЛИНЕ
  списка пропускает подмену равной длины — `qa_balance` меняет
  `["legend_steadfast"]` на `["legend_pierce"]`, и D5 краснеет ровно на этом.
  Верный признак свежести — ССЫЛКА НА МАССИВ ПЛЮС РАЗМЕР: присвоение кладёт
  новый массив, `append` правит прежний. Второго источника правды при этом не
  заводится — истина по-прежнему в `chosen`, слово только выводится.
- **ПЕРКИ СПРАШИВАЮТСЯ ТАМ, ГДЕ ЕСТЬ КЭШ.** «Таран» читается в
  `Unit._refresh_power` (напор кэшируется), а не в `_apply_push`, — иначе
  словарный поиск в автозагрузке платился бы на КАЖДЫЙ удар. «Стена щитов» — в
  `_incoming_damage_factor`, и её проверка стоит ПОСЛЕДНЕЙ в цепочке условий,
  чтобы рукопашная за неё не платила.
- **`Warrior._incoming_damage_factor` ОБЯЗАН ЗВАТЬ `super`.** Там стоял
  безусловный ранний выход при опущенном щите — с ним легендарная «Стена щитов»
  у отряда мечников не работала бы вовсе, и заметить это можно было бы только
  по числам.
- **ЭКОНОМИКА НАМЕРЕННО СКРОМНАЯ.** Рабочий приносит 28 единиц в минуту (цикл
  5 с + ходка ~7.7 с на 6 единиц), половина цикла — ходка. Стартовый запас
  урезан с двадцати тысяч до 550/380/340/200: прежние числа — отладочные, с
  ними первые полчаса партии не существует. Бараки и кузница подешевели втрое
  (320/150/220 и 450/300/180): при прежних ценах на одни бараки уходило
  двадцать минут, то есть вся ветка улучшений была выключена на полпартии.
- **ДРЕВО КУЗНИЦЫ СВЕДЕНО В ТРИ ТИРА ПО РЯДАМ** (1 — I, 2-3 — II, 4-5 — III).
  Цена и время — по РЯДУ, бонус — по ТИРУ, и ни один узел не даёт больше
  8-12 % от базы. Первое исследование копится 2.7 минуты при бригаде 4+4;
  стартового запаса не хватает и на два узла — дилемма «броня копейщикам ИЛИ
  дальность лучникам» встаёт с первой же кузницы.

## Четвёртая партия правок по скриншотам (авг. 2026): коллизии, паника, рабочие

Семь разделов заказа владельца. Ниже только то, что нельзя вывести из кода.

- **ПОСЛАБЛЕНИЕ НА ПРОХОД — БИЛЕТ НА ВЫХОД, А НЕ НА ВХОД** (`Unit._pass_earned`).
  Жалоба: «рыцари при клике протекают сквозь строй стоящих копейщиков,
  получают урон на ходу, но не останавливаются и прорываются к стрелкам». Это
  было НЕ багом расталкивания: `_forced_move_pass` выдавал ЛЮБОМУ бойцу под
  приказом игрока полторы секунды прохода сквозь чужие тела, а всаднику этого
  хватает на четыре метра — то есть на весь строй в глубину. Заводилось
  послабление ради ОДНОГО случая — «отряд В ПЛОТНОЙ РУБКЕ не идёт с первого
  клика», где шаг упирается в тела СО ВСЕХ СТОРОН. Теперь оно ЗАРАБАТЫВАЕТСЯ:
  его получает только тот, кто на момент приказа уже стоит в чужих телах.
- **«СТОЮ В СВАЛКЕ» НЕЛЬЗЯ СПРОСИТЬ У `_enemy_contact`.** Признак ставит
  солвер в тот кадр, когда ШАГ не прошёл, — а боец, стоящий в рубке, никуда не
  шагает вовсе: шагать он начнёт только от этого самого приказа. Замер
  `qa_disengage` D2: с проверкой по `_enemy_contact` окружённый боец проходил
  **0.00 м**. Спрашивается ГЕОМЕТРИЯ (чужое тело на длине руки,
  `LOCK_BLOCKER_RANGE`), и скан здесь бесплатен — это ПРИКАЗ, а не покадровый
  путь.
- **ПОДТЯГИВАНИЕ ФЛАНГА — РАЗВОРОТ ПРАВИЛА «У ФАЛАНГИ НЕТ ИНИЦИАТИВЫ»**
  (`Unit.PULL_UP_RANGE`, ветка в `_check_auto_aggro`). Жалоба: «в бой вступает
  только та часть отряда, что соприкоснулась с врагом». Прежнее правило —
  прямо противоположное и тоже по заказу («крайние копейщики не имеют права
  выламываться из строя»). Примиряют их ТРИ ограничения, и убрать нельзя ни
  одно: только пока **отряд в бою** (вне боя фаланга по-прежнему не сходит с
  места ни за кем), только в **лобовом секторе** (иначе фланговый разворачивается
  на соседа по свалке и «забор» распадается с краёв) и только в пределах
  **семи метров** (шаг в контакт, а не погоня).
- **ПОВОДОК АВТО-АГРО НЕ ДЕЙСТВУЕТ НА ОТРЯД, КОТОРЫЙ УЖЕ ДЕРЁТСЯ.** `AGGRO_LEASH`
  придуман против ИНИЦИАТИВЫ — чтобы свежий отряд не срывался за первым
  замеченным через полкарты. Отряд, уже втянутый в свалку, никакой инициативы
  не проявляет, а поводок держал половину его состава в стороне: пост-то
  остался там, ОТКУДА отряд вышел.
- **БЕЛЫЙ ФЛАГ ГАСНЕТ В `_disband_squad`, А НЕ САМ.** Жалоба: «после
  уничтожения паникующего отряда флаг остаётся торчать из земли». Точку флагу
  каждый кадр даёт `_update_panic_flags`, а он обходит РЕЕСТР отрядов —
  расформированного там уже нет, и обход до него не доходит НИКОГДА. Знамя
  ветеранства этой беды не знало: у него в том же месте своя строка.
- **ПОРОГОВ ПАНИКИ СТАЛО ТРИ, И ТРЕТИЙ СЧИТАЕТ ЖИВЫХ, А НЕ МОРАЛЬ**
  (`PANIC_LAST_MEN` = 6). Заказ владельца: «паника от потерь срабатывает не на
  50 % отряда, а когда остаётся буквально три-семь моделек». Через мораль это
  требование НЕ ВЫРАЗИТЬ: мораль долевая, а требование абсолютное, и у отряда
  из шестидесяти и из двенадцати оно наступает на совершенно разных долях —
  пришлось бы подбирать долю каждому роду войск отдельно. Вместе с этим
  `MORALE_LOSS_PER_DEATH` опущен с 90 до 40: срыв ПО МОРАЛИ уехал далеко за
  половину состава, и два условия перестали спорить за одно число.
- **МАЛЫЙ ОТРЯД ОТ СЧЁТА ЖИВЫХ НЕ СРЫВАЕТСЯ** (`PANIC_LAST_MEN_MIN_ROSTER`):
  иначе огрызок из пяти человек паниковал бы в тот же миг, как вступил в бой,
  не потеряв никого.
- **СЧЁТ ВЕДЁТСЯ В ПЕХОТИНЦАХ, А НЕ В ГОЛОВАХ** (`unit_stats_config.squad_weight`,
  у кабана 2.0; `GameManager._weight_of_squad`). Заказ владельца: «1 всадник =
  2 пехотинца при расчёте морали и силы отряда». Без веса порог «осталось
  шестеро» срывал бы конный отряд из десяти моделей после ЧЕТЫРЁХ потерь.
  Вес лежит в таблице, а не в коде: заведётся второй род конницы — ему хватит
  строки.
- **ПАНИКА РАЗБЕГАЕТСЯ ВО ВСЕ СТОРОНЫ, А НЕ «ПРОЧЬ ОТ ВРАГА».** Разворот
  требования: прежний общий вектор читался на экране не как паника, а как
  ОРГАНИЗОВАННЫЙ ОТХОД — шестьдесят человек ехали одной толпой в одну сторону.
  «Хаотично» при этом НЕ значит `randf()`: два прогона одного боя обязаны
  давать одно поле (то же правило, что у угла втыкания стрелы). Золотой угол по
  номеру бойца плюс сдвиг веера по номеру узла.
- **ВЕТЕРАНОВ ИИ БЕРЕЖЁТ РАНЬШЕ НОВОБРАНЦЕВ** (`AICfg.RETREAT_STRENGTH_VETERAN`
  = 0.30 против общих 0.35). Порог отхода — торг между «сберёг отряд» и
  «оголил рубеж»; новобранец стоит одного найма, а отряд с лычками несёт
  накопленные награды, и его потеря необратима. Одно число на двоих означало бы
  либо разбазаривание ветеранов, либо оборону, разбегающуюся от царапины.
  Условие «не зажат в плотном бою» уже стояло (`AI_NO_RETREAT_IN_MELEE`) и не
  тронуто.
- **ОКНО ЗАМЕРА СМЕЩЕНИЯ ВЫНЕСЕНО В `Unit._sample_movement()`, И ЭТО ОДНА
  ПРИЧИНА НА ДВЕ ЖАЛОБЫ.** «Рабочие несут ресурсы спиной вперёд» и «на стройке
  бегут на месте вместо анимации молотка» — один и тот же корень: `Worker`
  ПЕРЕОПРЕДЕЛЯЕТ `tick_physics` и в трёх своих состояниях (BUILDING /
  GATHERING / RETURNING) до базового тика НЕ ДОХОДИТ. Значит, окно замера у
  работающего не тикало ни разу: `_mv_dir` оставался направлением К дереву,
  пока рабочий едет ОТ него, — и зеркало честно рисовало его спиной.
- **ПОЗА СТРОЙКИ СЧИТАЕТСЯ ПО ФАКТУ, А НЕ ПО `velocity`** — та же грабля, что
  уже ловилась у пехоты: `velocity` это НАМЕРЕНИЕ, и у рабочего, вставшего
  вплотную к срубу, оно ненулевое.
- **ПРИХОД НА СТРОЙКУ ЗАЩЁЛКИВАЕТСЯ** (`Worker._build_settled`). Жалоба:
  «второй рабочий подходит, встаёт рядом и визуально ничего не делает». Здесь
  была КАЧЕЛЯ, та же самая, что у секторов вокруг здания (`Unit._ring_done`):
  оба рабочих идут в ОДНУ точку у стены, разбор наложения отталкивает второго
  на полметра — и тем самым выводит его ЗА порог прихода. Дальше он СНИМАЛ себя
  с артели и шёл обратно, то есть и стройку не ускорял, и анимацию имел ходьбы.
- **ВСЕ В СТРОЮ СМОТРЯТ В ОДНУ СТОРОНУ** (запасной ответ — `squad_course`).
  Курс прихода есть НЕ У КАЖДОГО ПРИКАЗА: смыкание рядов, подзыв сплочённости,
  возврат на пост и возврат из гарнизона зовут `command_move` БЕЗ курса. Такой
  боец оставался со взглядом, который был у него до того, — а был он, как
  правило, на чужого или на точку, откуда он шёл.
- **МЕСТО У ФЛАЖКА СЧИТАЕТСЯ ОДИН РАЗ НА ЗАКАЗ.** Жалоба: «при выходе из
  бараков на точку сбора отряд разделяется на два оторванных куска». Саму точку
  флажка сюда уже снимали однажды и по той же причине (`qa_rally2` F4), но
  СВОБОДНОЕ МЕСТО возле неё считалось внутри `_spawn_one`, то есть НА КАЖДОГО
  БОЙЦА. Отряд выходит шеренга за шеренгой, между первой и последней мир
  успевает измениться, и `free_squad_spot` честно отвечал ДРУГОЕ место: одна
  половина шла к первому ответу, вторая ко второму.
- **ПРОСВЕТ МЕЖДУ БЛОКАМИ ГРУППЫ ВЫВОДИТСЯ ИЗ ФИЗИКИ, А НЕ СТОИТ ЧИСЛОМ**
  (`SelectionManager.GROUP_CELL_GAP` = `SEP_CROSS_SQUAD × SEP_MIN_DIST + 2`).
  Метр был МЕНЬШЕ, чем хочет само расталкивание: бойцы РАЗНЫХ отрядов держатся
  шире своих, и крайние шеренги соседних блоков оказывались внутри чужого
  личного круга — каждый честно шёл в свою точку разметки, а расталкивание тут
  же выталкивало его обратно. Это и есть «отряды наезжают друг на друга и
  дёргаются на месте», тот же цикл «сжатие → выталкивание», что ловился у
  заслона ИИ.
- **ОДИНОЧКА ВЫХОДИТ У САМИХ ВОРОТ** (`Building.SINGLE_AGENT_EXIT_DISTANCE`).
  Отход на четыре метра нужен ОТРЯДУ: пятьдесят человек строятся шеренгами и
  обязаны освободить проём следующему заказу. Рабочий выходит поодиночке и
  прохода не занимает, а четыре метра доставались ему те же — вместе с выносом
  самих ворот у замка это девять метров пустой травы ниже здания.
- **ЗНАМЯ ПОДНЯТО НАД ГОЛОВАМИ** (`SquadBanner.CENTER_Y` 1.34 → 1.70).
  Виновата не высота сама по себе, а ГУСТОТА СТРОЯ: число подбиралось на
  оконном стенде с редкой шеренгой, а в партии знаменосец стоит в
  [ряд 0, колонка 0], то есть ВПЕРЕДИ шестидесяти человек, и под камерой в 45°
  спрайты ЗАДНИХ шеренг рисуются на экране выше него.

## Голосовое управление (03.09.2026)

Кнопка V (зажать и говорить), плашка распознанного текста внизу, отклик
отряда. **Кто:** «копейщики / фаланга», «лучники / стрелки», «мечников /
мечи / рыцари / воины / пехота / гвардия», «конница / всадники / кавалерия»,
«все / армия»; групп в фразе может быть несколько, ни одной не названо — все
боевые (рабочие вне). **Что** (`voice_commands_config.INTENTS`):

| команда | действие |
|---|---|
| «в атаку / напасть / бей» | марш по курсу на 40 м, звучит **горн** |
| «вперёд / марш» | марш по курсу на 20 м, стойка как была |
| «отступать [на N метров]» | отход назад на N м лицом к противнику |
| «стоять / стой / стоп» | встать на месте (приказ идти в свою точку), стойка не трогается |
| «держать позицию / строй / к бою» | стойка «оборона», копья вниз |
| «вольно / отбой» | стойка «атака», копья вверх |

- **ОТХОД ФАЛАНГИ — ПОСЛЕДОВАТЕЛЬНОСТЬ, А НЕ ОДИН ПРИКАЗ** (заказ владельца:
  фаланга не пятится с опущенными копьями). Отряд в стойке «оборона»
  поднимает копья (стойка «атака»), отходит на N м с `face_on_arrive` = курс,
  по приходу (центр у цели или все встали, потолок 25 с) снова опускает копья.
  Ведёт `VoiceControl._reform`, такт раз в 0.25 с; любой новый приказ отряду
  снимает отложенное опускание. Отряд в другой стойке просто отходит.
- **«СТОЯТЬ» — ЭТО `command_move` В СОБСТВЕННУЮ ТОЧКУ:** своей команды
  остановки у бойца нет, а приказ в свою точку прибывает мгновенно, переводит
  в покой и снимает цель атаки, не трогая стойку и курс.
- **ГРУППЫ — ПО ТИПУ ОТРЯДА В РЕЕСТРЕ** (`squads[sid]["type"]`), «все» — по
  `squad_is_combat`. Разбор по выделению не делается намеренно: голос это
  приказ армии, мышь — приказ выделению.
- **СЛОВАРЬ МАЛОЙ МОДЕЛИ ПРОВЕРЕН ПРОБОЙ, И ОН БЕДНЕЕ, ЧЕМ КАЖЕТСЯ.** Файла
  words.txt у сжатого графа нет; слово проверяется предупреждением «Ignoring
  word missing in vocabulary» при создании грамматики (печатается и в
  headless). Модель НЕ знает: «мечники», «мечникам», «меченосцы», «латники»,
  «пикинеры», «копьеносцы», «копейщикам», «щитоносцы», «вперед» без ё. Знает:
  «мечников», «мечника», «мечи», «рыцари», «воины», «солдаты», «бойцы»,
  «пехота», «пехотинцы», «гвардия», «дружина», «щиты», «копейщики»,
  «фаланга», «лучники», «стрелки», «конница», «всадники». Новый синоним
  добавлять только после такой пробы.

- **Распознаёт Vosk, офлайн, в СВОЁМ ПОТОКЕ** (`csharp/VoiceRecognizer.cs`).
  Главный поток за кадр делает две дешёвые вещи: копирует буфер
  `AudioEffectCapture` в очередь и опрашивает очередь результатов. Модель
  `vosk-model-small-ru-0.22` (45 МБ) лежит в `voice_models/` под `.gdignore`
  и в git не входит (`.gitignore`); скачивается с alphacephei.com, см.
  `voice_models/README.md`. Пакет NuGet `Vosk` даёт нативные DLL рядом со
  сборкой.
- **ОБЁРТКОЙ Vosk.dll ПОЛЬЗОВАТЬСЯ НЕЛЬЗЯ: ОНА МАРШАЛИТ СТРОКИ КАК ANSI.**
  Грамматика с кириллицей доходила до библиотеки знаками вопроса («Ignoring
  word missing in vocabulary: '?????'»), и текст ответа вернулся бы таким же.
  Распознаватель зовёт `libvosk` напрямую (`DllImport`), строки идут UTF-8
  байтами и читаются `PtrToStringUTF8`.
- **НАТИВНУЮ DLL ГРУЗИТЬ ЯВНО ПО ПОЛНОМУ ПУТИ.** Godot поднимает игровую
  сборку плагинным контекстом, и `DllImport("libvosk")` папку сборки в поиске
  не видит («Unable to load DLL 'libvosk' or one of its dependencies» при всех
  четырёх файлах рядом). `EnsureNative` грузит `libwinpthread → libgcc →
  libstdc++ → libvosk` через `NativeLibrary.Load` и ставит резолвер на свою
  сборку.
- **ГРАММАТИКА — ЭТО ТОЧНОСТЬ.** Vosk получает список допустимых слов, и всё
  вне списка приходит как `[unk]`. Список СОБИРАЕТСЯ из того же конфига, что
  и сопоставление (`scripts/voice_commands_config.gd`): второго словаря нет.
  Слова только те, что есть в словаре малой модели: «копейщик», «копейщикам»,
  «фаланге», «вперед» без ё она не знает и молча выбрасывает.
- **СОПОСТАВЛЕНИЕ — ПО ОСНОВАМ, КОРОТКИЕ СЛОВА ТОЛЬКО ЦЕЛИКОМ.** «копейщик»
  как основа ловит все формы; «все», «бей», «стой» как основы ловили бы
  «всегда» и «стойкость» — они в списке `exact`. Первая версия требовала
  точного совпадения для любой основы короче пяти букв, и «в атаку» с
  «держать строй» не понимались вовсе (стенд A1/C2).
- **ПРИКАЗ ИДЁТ ТЕМИ ЖЕ ФУНКЦИЯМИ, ЧТО И МЫШЬ** (`set_stance`, `command_move`
  с `player_order`), строй переносится целиком одним вектором на всех.
  Копейщики — по типу отряда в реестре, без разбора выделения. Клич —
  штатный `squad_battle_cry` первого отряда, один, а не хор.
- **В HEADLESS РАСПОЗНАВАТЕЛЬ НЕ СТАРТУЕТ САМ** (`start_recognizer()` явно):
  микрофона там нет, а модель стоит секунду и поток на каждый стенд шлюза.
- **class_name нового узла в стенде брать через preload**: имя попадает в кэш
  классов только после импорта редактором, а стенд без скрипта висит молча
  до сторожа (память проекта, повторено здесь).
- Ловушка Windows: русского голоса синтеза в системе нет (только en-US и
  pt-PT), образцы для блока E надо наговорить.

## Босс-тролль, новая карта, монах (09.09.2026)

Стенды `qa_troll`, `qa_map`, `qa_monk`. Ниже только то, что нельзя вывести
из кода.

- **ТРОЛЛЬ — ОБЫЧНЫЙ Unit ТРЕТЬЕЙ СТОРОНЫ С ПЯТЬЮ ПЕРЕОПРЕДЕЛЕНИЯМИ**
  (`scripts/goblin/Troll.gd`, характеристики в `STATS["troll"]`, поведение
  в `goblin_config.TROLL_*`). Размер ×4 копейщика — свой `pixel_size`
  (0.0307) и центр спрайта по ленте, как у гоблинов. Серия из `TROLL_SWINGS`
  ударов → одышка `TROLL_REST_SEC` (лента Recovery): `_may_strike_now()` =
  false и `tick_physics` не идёт в базу — тролль стоит. Дубина бьёт брызгами
  (`TROLL_SPLASH_*`), таран — те же `charge_*`, что у кабанов, с замахом
  (Windup) на входе в разгон. Тело — последний кадр Troll_Dead.
- **ТАРАН ТРОЛЛЯ ТОПЧЕТ, ДУБИНА БЬЁТ ДУГОЙ, НАКРЫТЫЕ ЛЕЖАТ** (заказ
  09.09.2026, стенд `qa_troll_impact` 17/17). Скорость 2.88 (+20 %),
  патруль 23.4 м (+30 %), `charge_push_mult` 6.6 = конница 5.5 × 1.2
  («на 20 % сильнее» — это множитель ПОСЛЕ потолка шага, а не `push_force`:
  тот потолком съедается, см. `_apply_push`). **Топтание** —
  `Unit._trample_count()` (база 0, тролль `TROLL_TRAMPLE_COUNT` 3): в
  `_charge_impact` круг брызг сортируется по проекции на вектор удара, первые
  N не позади гибнут на месте (`take_damage` сверх запаса — тело, фраг,
  смыкание штатно). **Стенка копий топчущего не останавливает**: он получает
  укол (`charge_counter_frac`) и идёт дальше — иначе фаланга лбом отменяла
  бы босса. **Дубина** (`Troll._strike_damage`) — не радиус вокруг цели, а
  сектор `TROLL_SWEEP_ARC_COS` (±60°) от взгляда на `attack_range + 0.8`,
  ближайшие `TROLL_SWEEP_COUNT` 6: урон, `apply_knockback` на
  `TROLL_SWEEP_KNOCKBACK` (тот же канал скорости, что у конницы) и
  `knock_down(TROLL_KNOCKDOWN_SEC)`.
- **СБИТЫЙ С НОГ — ЭТО ЕГО ЖЕ ТЕЛО ИЗ СЛОЯ ТЕЛ, А НЕ НОВАЯ ПОЗА**
  (`Unit.knock_down` / `_stand_up`, `_down_until_ms`). Общая отрисовка
  умеет только вертикальный билборд; лежащий квад плашмя есть у
  `CorpseRenderer`. Поэтому на срок лежания слот бойца снимается
  (`leave_render`, `set_draw(false)`), а на его месте кладётся тело через
  тот же `corpses.spawn(self, …)`, что у павшего (та же лента, тот же кадр
  покоя, тот же поворот поперёк экрана); `move_now` везёт тело за бойцом,
  пока его несёт разлёт; `remove_now` снимает без растворения при подъёме.
  Ворота в `tick_physics` стоят ПОСЛЕ такта разлёта и окна смещения: сбитый
  обязан долететь. Урон лёжа принимает как обычно; убит лёжа — временное
  тело снимается в `_die`, штатное кладёт `_leave_corpse`. Встал —
  `enter_render`, авто-агро возвращает его в бой (C7: 4 из 5 в бою сразу).
- **ВСПЫШКА И КРОВЬ У ТРОЛЛЯ СВОИ** (`hit_flash_peak()` → 0.22 против общих
  0.85: по нему бьют десятки стрел в секунду, и общий пик держал его
  засвеченным; `blood_spots()` → 1.0 → uniform `blood_spots` бакета,
  `FarUnitRenderer.Bucket.set_blood` при регистрации — свойство ленты, как
  привязка ног). Пятна в `mm_unit_sprite`: кадр режется на 11×11 клеток, в
  клетке по хэшу решается пятно/центр/радиус, плотность растёт с потерей
  запаса, капля вытянута вниз; хэш от клетки КАДРА — пятно едет с телом, а
  не мерцает по экрану. Пехота с нулём платит одно сравнение.
- **ЦЕНА СМЕРТИ — 180 УБИЙСТВ, ДЕЛЁННЫХ ПО УРОНУ** (`Troll._award_kills` →
  `GameManager.credit_kills`): тролль помнит, чей отряд сколько нанёс;
  добивающий удар и так даёт обычный +1, поэтому раздаётся `worth − 1`.
- **ЛОГОВО (`TrollLair.gd`) — Building гоблинов, НЕ Castle:** дерево, колья и
  кости (детерминированно от номера узла, имена `Spike%d`/`Bones%d` — Godot
  переименовывает дубликаты, и стенд считал бы одного), стражи `spawn_guards`,
  подмога `call_guards` на половине запаса (один раз на тролля). Тролли — вне
  реестра вожака орды (`GoblinAI._regroup` их пропускает): стражи, а не волна.
- **МЕСТЬ ГОБЛИНОВ ИДЁТ ПО СВОИМ ЧАСАМ** (`GoblinAI._tick_revenge`,
  `REVENGE_*`): логово зачищено → раз в 600 с 10 отрядов пехоты выходят из
  деревни с ролью `ROLE_REVENGE` и целью на кольце вокруг дерева; они не
  спят вместе с ордой, и приказы им раздаются даже в её спячке
  (`_issue_orders(only_revenge)`). Отряды орды выпускает один путь —
  `Main.spawn_goblin_squad` (стартовая орда переведена на него же).
- **КАРТА ВТРОЕ ПО ПЛОЩАДИ** (`MAP_GROWTH` 2.25 → 450 × 253 м). Линейное ×3
  дало бы ×9 площади и утроило туман, лес и руду сверх бюджета кадра.
- **ВЫСОТА ЗЕМЛИ ЖИВЁТ В ДВУХ ЯЗЫКАХ, И ОБЕ КОПИИ ОБЯЗАНЫ СОВПАДАТЬ.**
  `Main.get_terrain_height` = гармоники + гора (`hill_height`, гауссов колокол
  у замка игрока) − русло (`river_depth`). Ядро считало высоту ТРЕМЯ копиями
  гармоник; теперь `ArmyCore.Height` с теми же горой и рекой, параметры подаёт
  `GameManager.refresh_map_bounds` (`army.set_hill` / `set_river`). Забудь
  одну сторону — боец на холме уйдёт под землю. Нормаль в GDScript теперь
  численная (составную формулу дифференцировать руками незачем).
- **РЕКА ПО ЦЕНТРУ, БРОД ПОСЕРЕДИНЕ, ПОИСКА ПУТИ НЕТ — БЕРЕГ ВЕДЁТ К БРОДУ.**
  `is_water` — полоса вокруг `river_x(z)` вне брода; `slide_around_water` при
  реке скользит вдоль Z в сторону `FORD_Z`; `nearest_land` — поперёк русла.
  **ВОДА ПРОВЕРЯЕТСЯ В ЯДРЕ** (`ArmyCore.RiverWater`), а не вызовом
  `is_water` из C# на каждый шаг: с озером ветка была выключена
  (`water_active = false`), с рекой она платила бы два межъязыковых перехода
  на каждого ходящего. Замер `qa_map` C1: боец, посланный на другой берег,
  доходит через брод (187 м пути при 56 по прямой), в воде 0 кадров.
  `water_active` = озеро ИЛИ река. **РУСЛО СУЩЕСТВУЕТ ТОЛЬКО В ПОЛЕ КАРТЫ**
  (`river_in_field`, в ядре `_riverHalfZ`): формула по z бесконечна, а
  стенды ставят бойцов за краем при x ≈ 0 (z = ±400 — обычный приём) — без
  оговорки `qa_guard` и `qa_goblin` оказывались «в воде» и уезжали вдоль
  берега. **ВОДА — ПРАВИЛО ПАРТИИ, КАК ГРАНИЦЫ МИРА:** стенд, снявший
  `world_bounds_enabled`, воды не видит (`GameManager.is_water` и оба
  пакетных прохода) — иначе река по центру обращала бы половину площадок
  стендов в воду (`qa_panic`: отряд на оси русла разлетался по берегам).
  Стенд, которому нужна река (`qa_map` C), включает границы явно.
  Логово переживает загрузку партии (фабрика `_make_building`,
  регистрация в `_ready`, тролли из слепка подхватывают его лениво).
- **ЗЕМЛЯ ИЗ ТАЙЛОВ — ОДНА СЕТКА, ТРИ ПОВЕРХНОСТИ:** трава/песок
  (Tilemap_Flat, внутренние тайлы (1,1) и (6,1)), гора (Tilemap_Elevation:
  площадка (1,1), стена (1,3) по крутизне), вода (Water.png повтором). UV
  с полупиксельным отступом от кромки тайла, фильтр NEAREST. Пена — статичные
  лоскуты первого кадра Water Foam плашмя (AtlasTexture в StandardMaterial3D
  рисует весь атлас — кадр режется `Image.get_region`); камни брода —
  анимированные листы Rocks_0N тем же материалом, что камни озера.
- **ПЕНА И КАМНИ БРОДА РИСУЮТСЯ ПАКЕТНО** (09.09.2026). Лоскут пены на
  узел и камень на узел — это вызов отрисовки на каждый: ~70 лоскутов вдоль
  русла в 253 м плюс 26 камней. Пена теперь ОДИН `ArrayMesh` (те же квады,
  повороты `Basis.from_euler` в порядке YXZ, как у `rotation_degrees`,
  `extra_cull_margin`, потому что центр меша почти всегда за кадром), камни
  — `MultiMesh` на лист (шейдер билборда берёт начало из `MODEL_MATRIX`, а у
  экземпляра оно своё; размер — масштаб экземпляра, фаза общая на лист).
  Было до 96 вызовов при реке в кадре, стало не больше пяти. `qa_map` E4/E5
  считают экземпляры и квады, а не детей узла. Сама земля и так три меша
  (поле, гора, вода) — там батчить нечего.
- **ПОРТРЕТ МОНАХА — ПО ЦВЕТУ СТОРОНЫ** (`HUD.MONK_AVATARS`,
  `HUD.unit_icon_path(uid, faction)`): синие — `Monk_blue.png`, жёлтые —
  `Avatars_14`, красные — `Avatars_09`; `Avatars_04.png` в паке НЕТ (только
  его `.import`), поэтому «04» из заказа заменён `Monk_blue`. Четыре места
  показа иконок в HUD (портрет своего выделения, портрет чужого, кнопки
  найма, фильтр отрядов) зовут одну функцию; прежняя `Avatars_01` (свиток)
  была портретом не монаха.
- **МОНАХ — ОДИН НА ИГРОКА И ОДИНОЧНЫЙ** (`SQUAD_SIZE_MONKS` 1, `MONK_LIMIT`
  1; ворота в `GameManager.pop_allows` действуют и в стендах — это правило
  бойца, а не партии). Аватар — `Human avatar/Avatars_01` (свиток).
  **ЛЕЧЕНИЕ — РЕДКИЙ ТАКТ, НЕ ПОКАДРОВЫЙ** (`Monk._heal_pulse` раз в
  `MONK_HEAL_TICK`): самый раненый свой в `MONK_HEAL_RADIUS` получает
  `max_health × tick / MONK_HEAL_SEC_PER_MAN`, строка ядра обновляется
  `_soa_push_stats()` (тот же путь, что у «крови за кровь»). Павших не
  поднимает по построению — лечатся только живые из сетки. Лента Heal
  держится `_play_attack_anim` на длину такта.
- **ЛОВУШКИ СТЕНДОВ ЭТОГО ЗАХОДА:** таблица лент снимается с узла ЛЕНИВО —
  `_has_anim` у только что рождённого бойца пуст, сначала `sheet_frame()`;
  враг в 4 м от своих гибнет от авто-агро раньше проверки «чужого не лечат»
  (правило 5 на сырой ссылке); броня режет урон долей, «просадить до
  половины» одним `take_damage(current − half)` нельзя.

## Аудио-окно камеры (09.09.2026)

Заказ: «звук ходьбы слышен со всей карты и перегружает поток». Марш теперь
слышен только в ОКНЕ ВОКРУГ ЦЕНТРА ЭКРАНА и гаснет с отдалением камеры.

- **ОКНО МЕРЯЕТСЯ ОТ ФОКУСА, А НЕ ОТ СЛУШАТЕЛЯ** (`GameManager.view_point`,
  `AudioManager.march_focus`). Слушатель висит на камере, а камера стоит
  ПОЗАДИ фокуса на десятки метров (наклон 45°): мерить от неё значило бы
  двигать окно вместе с наклоном. Слушатель остался для ПАНОРАМЫ.
  Ловушка стенда: `qa_voice` строил площадку у слушателя — с окном в 40 м
  вся площадка лежала за ним, и блок D проверял бы отсечку вместо раздачи.
- **ЧИСЛА:** до `MARCH_FULL_RADIUS` (15 м) полная громкость, до
  `MARCH_FADE_RADIUS` (40 м) квадратичный спад в ноль, дальше голос не
  выдаётся вовсе (луп не декодируется). Сверху окно режется видимой полосой
  (`march_cutoff` = min(40, `view_radius`)): «только видимые на экране».
- **ЗУМ ГАСИТ** (`march_zoom_gain` = (1 − t)^`MARCH_ZOOM_POW`, t от камеры
  через `update_view_point(..., zoom_t)`): вплотную 1, на пределе 0. Это
  разворот прежнего «от зума громкость не зависит» — то правило снимало
  СКАЧКИ обратно-квадратичного затухания через высоту слушателя; здесь
  зависимость монотонная и плавная. На нуле голоса не раздаются, а не
  глушатся громкостью.
- **ТЕМП:** шаг `MARCH_WALK_PITCH` 1.08 («отстаёт от ног»), бег
  `MARCH_RUN_PITCH` 1.12 → 1.23. **У БЕГОВОГО ЛУПА ВЫРЕЗАНА ТИШИНА В НАЧАЛЕ
  ФАЙЛА** (`MARCH_RUN_START_SEC` 0.78): magiaz-soldiers_marching молчит
  0.78 с, первый сапог на 1.10 с; плюс такт обхода 0.25 и вход 0.12 — больше
  секунды между «побежали» и «затопали». Стартует и ЗАЦИКЛИВАЕТСЯ с этой
  точки (`loop_offset`), иначе тишина возвращалась бы на каждом круге.
- **ЗВУК В HEADLESS МЕРИТСЯ:** dummy-драйвер AudioServer микширует, и
  `AudioEffectCapture` на своей шине отдаёт PCM (`qa_march_env/Probe` —
  огибающая по окнам 20 мс). Это способ узнать про файл то, чего не видно
  в редакторе: тишину, набивку, пики.

## Постройки: башня, стрелковая, дома, лимит населения (09.09.2026)

Стенд — **`qa_tower`** (41 проверка). Меню рабочего показывает ВСЕ здания
цветовой папки: замок, бараки, стрелковую, башню, кузницу (картинка
Monastery), три дома; плюс рудник.

- **«ВЫБИРАЮ ВЫШКУ — СТРОИТСЯ ДОМИК» БЫЛ РУДНИКОМ.** У записи `mine` стояла
  иконка Tower.png при спрайте House1: кнопка с башней вела на рудник, а
  рудник рисовался домом. Башня теперь настоящая (`Tower.gd`), у рудника
  иконка золота из HUD и процедурный вид — своей картинки в паке нет, а
  House1 при трёх домах в меню был бы четвёртым домом, добывающим золото.
- **БАШНЯ НАСЛЕДУЕТ ЗАМОК РАДИ ГАРНИЗОНА** (как хижина орды) и отличается
  ПЕРЕОПРЕДЕЛЕНИЯМИ, а не развилками внутри Castle: `garrison_limit()` = 1,
  `garrison_accepts()` = только `TOWER_GARRISON_TYPES` (лучники),
  `_auto_release()` = false (пост, а не лазарет), `vision_radius()` =
  `TOWER_VISION` 35 м (FogOfWar спрашивает постройку), `is_stronghold()` =
  false. **`is Castle` больше не значит «столица»:** зона застройки, кнопка
  «в замок», условие поражения и точка базы у ИИ спрашивают
  `is_stronghold()` — иначе башня продлевала бы зону застройки, принимала
  бы «в замок» любой отряд и держала бы игрока непобеждённым.
- **ЧАСОВОЙ** (`Tower._sync_sentinel`): первый кадр `Archer_Idle` цвета
  фракции квадом на настиле (`SENTINEL_FOOT_FRAC` 0.60 высоты рисунка,
  под ступнями в кадре 56 px из 192). Строится лениво при первом отряде,
  дальше только прячется. Кадр вырезается `Image.get_region`, а не
  `AtlasTexture`: в sampler2D шейдера атлас уезжает целиком.
- **ПКМ ПО БАШНЕ:** отрядом лучников — внутрь (`request_garrison` их и
  отсеивает), любым другим или ПУСТЫМ выделением — гарнизон выходит
  (`SelectionManager._try_release_tower`). Это единственный приказ, который
  принимает пустое выделение: адресат — само здание.
- **РУИНЫ И СТРОЙКА ОБЩИЕ «ДОМОВЫЕ», НА БАШНЕ В ДОЛЮ** (`BUILDINGS.tower.
  ruin_scale` / `construction_scale` 0.6, `Building.ruin_scale()`,
  ConstructionSite читает конфиг). Габарит башни подобран под ВЫСОТУ
  кузницы: 7.48 против 7.33 м.
- **СТРЕЛКОВАЯ** (`Archery.gd`): `TRAINING["archery"]["archer"]`; в бараках
  копейщики и мечники. ИИ строит стрелковую ПОСЛЕ кузницы (темп первых двух
  построек стережёт `qa_ai`) и до неё нанимает лучников в замке — состав
  армии ИИ по времени не изменился.
- **ТРИ ДОМА — ОДИН СКРИПТ** (`House.variant_id` = "house"/"house2"/"house3"
  → ключ BUILDINGS и картинка; ставит стройплощадка или сохранение ДО
  входа в дерево). Баланс одинаковый намеренно; `is_house()` — в конфиге.
- **ЛИМИТ НАСЕЛЕНИЯ — ПРАВИЛО ПАРТИИ, А НЕ БОЙЦА** (`GameManager.
  pop_limit_enabled`, включает `Main.start_game`, когда Main — текущая
  сцена). База `POP_BASE_WORKERS` 6 / `POP_BASE_SQUADS` 3, дом даёт
  `HOUSE_WORKER_SLOTS` 3 / `HOUSE_SQUAD_SLOTS` 3. Считаются ЖИВЫЕ ПЛЮС
  ЗАКАЗАННЫЕ (очередь найма), ворота — `Building.queue_unit` ДО списания.
  Стенды поднимают Main под своим узлом и нанимают по семь отрядов из
  одного барака без единого дома — у них лимит выключен сам; `qa_tower`
  включает его явно. На ИИ не действует: у красного свои потолки. Панель
  ресурсов показывает «раб. N/M · отр. N/M» (`HUD._refresh_pop`), кнопка
  найма краснеет и по лимиту.
- **ПРАВИЛО 5 ПОЙМАНО СНОВА:** счётчик домов брал `b as Building` из кэша
  группы, где снесённый дом ещё лежит, — `is_instance_valid` на сырой
  ссылке до приведения.
- **УКРЫТЫЙ В ЗДАНИИ ОСТАВАЛСЯ В СЕТКЕ ЯДРА, И ЭТО БЫЛО «ЛУЧНИКИ В БАШНЕ
  ПОЛУЧАЮТ УРОН»** (09.09.2026). `absorb_unit` снимал бойца с GDScript-сетки
  (`unit_grid`), из групп и с тика — но цели и стрелы ищут по СЕТКЕ ЯДРА
  (`FillGrid` по живым строкам с `FPosValid`), а строка вошедшего оставалась
  «настоящей» на последней точке у ворот: невидимый, без тика, но
  обстреливаемый. Теперь `Unit.set_off_map(true)` гасит `FPosValid`
  (`ArmyCore.SetOffMap`), выход возвращает его через `sync_row`. Стережёт
  `qa_tower` B8г (поиск своих у ворот отвечает null).
- **УДАР ПО УКРЫТОМУ ПРИНИМАЕТ ЗДАНИЕ** (`Unit.take_damage` → хозяин
  `garrison_host.absorb_damage_for`, для всех наследников Castle). Броня
  бойца не считается — здание её не имеет. Хозяина нет — удар пропадает:
  того, кого нет на карте, бить нельзя. Снесённая башня выпускает отряд
  штатным `_evacuate_on_death`, и он снова уязвим (B13).
- **ГАРНИЗОН БАШНИ СТРЕЛЯЕТ С ПЛОЩАДКИ** (`Tower._tick_fire`). Укрытый
  лучник сам не тикает, поэтому стреляет за него башня: раз в его же
  `_effective_cooldown` зовёт его же `_on_attack_fired` — стрела летит с его
  уроном, кучностью, выучкой и на его счёт убийств; точка вылета —
  настил (`platform_point`, тот же расчёт, что у часового). Цель ОДНА на
  площадку, ближайший чужой любой стороны по `nearest_of_side`, ищется не
  чаще `TOWER_RETARGET_SEC`. **ДАЛЬНОСТЬ ОГНЯ — ШТАТНАЯ ДАЛЬНОСТЬ ЛУЧНИКА,
  А НЕ ОБЗОР БАШНИ** (разворот владельца в тот же день: первая версия била
  на `TOWER_VISION` 35 м). `fire_range()` — наибольший `attack_range` по
  гарнизону; константы дальности у башни больше нет. Башня видит на 35 м, а
  стреляет на 20: `qa_tower` B8а (огонь = лук < обзор) и B8д (враг дальше
  лука, но внутри обзора — ноль выстрелов). **Ссылка на цель НЕТИПИЗИРОВАНА** (правило 5):
  убитая цель освобождается между тиками, и `t: Node3D` бросал исключение
  на каждом кадре. Стенд: B8б (10 выстрелов за 4 с, запас врага убыл),
  B8д (за 35 м — ноль выстрелов).

## Восьмая часть 10.09.2026: скриншоты, тролли, поражение, рудник, рельеф, овцы

Шесть скриншотов владельца плюс два новых механизма. Ниже только то, что
нельзя вывести из кода. Стенды: `qa_rally_zone` 13/13, `qa_spawnlane` 9/9,
`qa_troll_impact` 28/28 (новый блок E), `qa_troll` 24/24, `qa_gold_mine`
24/24 (новый), `qa_sheep` 17/17 (новый), `qa_map` 34/34 (новый блок F),
`qa_visual_smoke` 19/19.

- **ПЛОЩАДКА СБОРА — 18 м ВПЛОТНУЮ К ЗДАНИЮ, РАМКИ НЕТ.** `SQUAD_EXIT_DISTANCE`
  4.0 → 1.0, `GATE_CLEARANCE` 0.9 → 0.5, `EXIT_LANE_GAP` 4.0 → 1.0,
  `ZONE_DEPTH` 18: число рядов площадки ВЫВОДИТСЯ (`Building.exit_lanes`) из
  глубины строя — сколько отрядов этого размера встанут друг за другом с
  просветом, потолок `EXIT_LANES`, пол два; шаг рядов растягивается на всю
  площадку. Прежнее «шаг не теснее бокового 7.13 м» снято — оно и уводило
  пятый заказ на тридцать метров. Рабочий выходит на 0.8 шага россыпи (0.56 м)
  от стены. Рамка спрятана (`ZONE_MARKER_SHOWN` = false), механизм оставлен.
  Стенды прежних чисел переписаны на свойство «проём = круг до первой шеренги»
  (`qa_rally_zone` B2, `qa_squad` 2, `qa_rally2` E2/E5/E7).
- **ТРОЛЛЬ: КРОВЬ СНЯТА, ЗАПАС −30 %, ТЕМП +20 %, ПОДМОГА −30 %.**
  `TROLL_BLOOD_SPOTS` 0 (краснеет общей подкраской раненого), `health` 22680,
  `attack_cooldown` 1.167, подмога из логова один тролль на трети запаса.
  Цена смерти осталась 180 убийств.
- **ТЕЛО ТРОЛЛЯ — ВЕРТИКАЛЬНЫЙ БИЛБОРД, А НЕ КВАД ПЛАШМЯ** (`Troll._leave_corpse`).
  Лента Troll_Dead уже нарисована лежащей; общий слой тел клал её плашмя (ещё
  раз на бок), а квад в 11.8 м на волнистом рельефе (±0.85 м) уходил в грунт —
  это и был «обрезанный спрайт». Свой узел по правилам декораций
  (`cyl_billboard`), доигрывает ленту кадр за кадром (таймер под узлом тела —
  шейдерные часы TIME с момента гибели не отсчитываются, а узел тролля к
  следующему кадру освобождён), лента обрезана по строкам рисунка (пустое поле
  под ступнями 88 px = 2.7 м поднимало бы тело в воздух). **Падает прочь от
  удара:** голова в кадре смотрит влево; обидчик слева по экрану
  (`_last_hit_from` против `camera.basis.x`) — лента зеркалится.
- **КЛИК ПО ТРОЛЛЮ — ВЕСЬ СПРАЙТ ПЛЮС КОЛЬЦО** (`Unit.pick_body_h` /
  `pick_radius` / `ring_scale`, тролль 3.0 м / 1.7 м / ×4.2). В `_pick_at`
  якорь крупного бойца уходит под середину рисунка на `lean × pick_body_h`, а
  радиус — круг у ног плюс проекция полувысоты; сбор кандидатов расширен на
  `BIG_PICK_EXTRA`. Кольцо и тень масштабируются базисом слота
  (`SelectionDecalRenderer._write(idx, pos, k)`) — ядро переписывает только
  точку, базис остаётся; кольца наведения и приказа — тем же `ring_scale`.
- **ОРДА БЬЁТ ПОСТРОЙКИ** (`GoblinAI._nearest_enemy_building`,
  `BUILDING_HUNT_RADIUS` 70 м). «Гоблины стоят у стены» — приказ атаки
  раздавался только по бойцам. Нет бойца в `CENTER_RADIUS` — ближайший чужой
  дом получает тот же `command_attack` (охрана деревни и месть — нет).
- **ПОРАЖЕНИЕ — ДВА ПРАВИЛА** (`Main._player_defeated`): крепость снесена И
  отстроить нельзя (не хватает цены замка ИЛИ нет живого рабочего и нет
  заложенной крепости); либо не осталось ни построек, ни бойцов — сразу.
  Победа над ИИ считается как прежде (`_faction_beaten`).
- **ЗОЛОТОЙ РУДНИК** (`scripts/Mine.gd`, полностью переписан): три картинки
  из `Gold_mine/` (Inactive / Active / Destroyed), фракция
  `Constants.FACTION_NEUTRAL` = 3 со своими группами (`neutral_units` /
  `neutral_buildings`) и ВНЕ `FACTION_COUNT` — в «чужие» не попадает никому.
  Захват: пехота одной стороны в `CAPTURE_RADIUS` 8 м без чужих (гоблины и
  тролли — защитники) `CAPTURE_SEC` 3 с подряд. Флажок — `SquadBanner` с
  `BannerArt.faction_flag_texture(цвет стороны)` (вымпел без лычек), ставится
  ЛОКАЛЬНО: рудник получает точку после `add_child`. Рабочие — тем же утиным
  контрактом, что стройка (`work_position` / `add_builder` — вход внутрь, как
  `Castle.absorb_unit`), `release_all` — ПКМ пустым выделением. Смена
  владельца ПЕРЕКРАШИВАЕТ рабочих внутри: сторона, `army.set_faction`,
  отряд-из-одного, ленты цветовой папки (`_setup_worker_visual` заново,
  `_look_ok` = false). Руина — `ruin_sprite_override()` и
  `ruin_any_faction()` (общие хуки `Building`): отстроить может любой,
  рудник достаётся отстроившему (`GameManager.rebuild_ruin(ruin, faction)`).
  ПКМ по ничейному руднику бойцами — марш, не атака. Два ничейных рудника
  ставит `Main._spawn_gold_mines` (доли 0.34/0.66 пути между замками,
  −34 м по Z), площадки зарезервированы от леса.
- **РЕЛЬЕФ: ПЛАТО СО СПУСКАМИ, РЕКА В НИЗИНЕ, ОТТЕНКИ ТРАВЫ.** Плато
  (`Main.plateau_list` / `plateau_height`, зеркало `ArmyCore.PlateauHeight`
  через `set_plateaus`, сторожит `qa_map` F4): плоская вершина, спуск
  smoothstep-ом, ширина склона по направлению — в секторе спуска
  `PLATEAU_RAMP_GENTLE` 14 м (уклон ~0.23, трава), иначе `_STEEP` 4.5 м
  (обрыв, тайл стены). **Ступеней нет нигде, боец пройдёт везде** (поиска
  пути нет — непроходимых склонов быть не может), обрыв только РИСУЕТСЯ
  стеной. Порог стены у плато свой (`PLATEAU_WALL_SLOPE` 0.45): общий 0.30
  лежал в полосе спуска (гармоники дают до 0.36). Два плато под рудниками
  (спуск к ближнему замку), два по `PLATEAU_SPECS`. Река: `RIVER_DEPTH` 1.5,
  `RIVER_BANK` 3.5 (откос 0.43 — стена), зеркало воды `water_surface_y` —
  ровное поперёк русла на `WATER_DROP` глубины ниже кромки, меш воды
  накрывает и откос (грунт выше зеркала его перекрывает по буферу глубины),
  пена на урезе воды. Оттенки: `Tilemap_colorN` (9×6: (1,1) низина, (6,1)
  вершина, (6,4) стена) — плато лист 3, берег лист 2, пятна в низине листы
  4 и 2 по шуму `grass_shade`; каждый лист — свой меш `TerrainColorN`, гора
  осталась камнем Elevation. `WORLD_GEN_VERSION` 4. **Ловушка визуального
  дыма:** ряд, поставленный в русло или на склон плато, уходит под воду или
  за грунт — `_clear_spot` обязан отступать от откосов и плато.
- **ОВЦЫ — ДЕКОРАЦИЯ С ПОВЕДЕНИЕМ, НЕ Unit** (`scripts/goblin/Sheep.gd`):
  в ядро армии не входят, не цель никому, десять узлов с лёгким `_process`.
  Ленты Idle 8 / Bouncing 6 кадров обрезаны по строкам рисунка, зеркальные
  пересобраны ОДИН РАЗ (статический кэш: `cyl_billboard` нормирует масштаб,
  отрицательный scale.x не переворачивает). Перебежка раз в `SHEEP_HOP_SEC`
  на 5-10 м в радиусе `SHEEP_GRAZE_RADIUS` от логова, вода обходится
  `land_target`, в тумане прячется. Стадо ведёт `TrollLair` (5 на старте,
  +2 раз в 300 с до 10, таймер-узел под логовом — у него нет своего тика).
  **Голод тролля** (`Troll._tick_hunger`): раз в `TROLL_HUNGER_SEC` 150 с
  ±20 % или при стаде > 5 тролль в покое идёт к ближайшей овце, у неё
  замахивается (лента удара), звук `nom_nom` (синтезированные wav в
  `Sounds Human/`), овца исчезает, +20 % запаса. Бой важнее обеда: цель
  атаки снимает обед; патруль на время обеда стоит.

## Девятая часть 10.09.2026: строитель, еда и голод, кража овец, кузница, лист камня

- **МОЛОТОК — ТОЛЬКО ПОСЛЕ ПРИХОДА** (`Worker._update_sprite_anim`,
  `want = "build" if _build_settled else "walk"`). Прежнее «стоит — значит
  молоток» давало удар киянкой на месте в первый кадр приказа (окно замера
  хода ещё пусто) — «забивает гвозди в воздух». Замах (`_animate_chop`) в
  BUILDING тоже только у стены.
- **СОДЕРЖАНИЕ ЕДОЙ** (`GameManager._sweep_food`, `unit_stats_config.
  FOOD_UPKEEP_*`): раз в секунду живые рабочие (отряды-одиночки) и боевые
  отряды стороны едят 1/30 и 0.1 еды/с; списание из склада не ниже нуля
  (`ResourceManager.consume`), недостача = голод. **Баланс — тождество из
  констант:** 10 × HOUSE_FOOD_INCOME/INTERVAL (2.0/8 с, было 4.0) = 2.5 =
  30 × 1/30 + 15 × 0.1 — «десять домов кормят ровно 30 рабочих и 15
  отрядов», отряд ест 60 за 10 минут (ориентир владельца). Голод: мораль
  отрядов вне боя не восстанавливается, а тает `STARVE_MORALE_PER_SEC` до
  `STARVE_MORALE_FLOOR` 40 % — выше порога паники 30 %: голод ослабляет
  армию, а не разгоняет. Красный ИИ содержания не платит
  (`FOOD_UPKEEP_FACTIONS`). HUD: у еды «+приток −расход» в минуту, красным
  при дефиците (`ResourceManager.upkeep_rate`). Еда раньше не тратилась
  нигде — это первый её потребитель.
- **КРАЖА ОВЦЫ И РАЗДЕЛКА** (`Worker.command_steal_sheep`, `Sheep.captured_by`
  / `carry_to` / `drop_at` / `wound_flash` / `butchered`). Овца — не
  ресурс-узел, рабочий уносит её ЦЕЛИКОМ: идти (MOVING, ходьба) → взять
  (RETURNING с грузом «мясо», овца едет над головой `SHEEP_CARRY_Y`) → у
  склада опустить рядом и резать (GATHERING с ножом — лента harvest):
  `SHEEP_CUTS` 5 надрезов по 1.2 с, каждый `MEAT_PER_CUT` 8 еды сразу в склад
  (рабочий стоит у стены), последний ещё `MEAT_FINAL` 20 — туша 60 еды
  против 15/мин у дома. **ЭТИ ЧИСЛА — ИСТОРИЯ:** спринт 13 развернул порядок
  целиком (живая овца сперва уходит НА ВЫПАС, разделка — отдельный приказ,
  пять ударов до смерти, 300 еды за 20 ходок), см. его раздел. **Куда нести решает `_sheep_dest()`**: загон
  (группа `sheep_pens`, метод `accept_sheep`, своя сторона) в приоритете,
  иначе ближайший склад — задел под будущее здание. Любой другой приказ
  бросает овцу (`release_from`), она снова свободна. Луч мыши ловит овцу
  телом на слое ресурсов (`SheepPick`, `_resolve_node` по группе `sheep`);
  ПКМ бойцами по овце — марш в точку. **В headless поза не считается
  визуальным тиком** (боец вне кадра) — стенд зовёт `_update_sprite_anim()`
  сам.
- **КУЗНИЦА:** `build_time` 20 → 40 (вдвое дольше бараков); ключ
  `bonus_build` (доля к темпу площадки, `ConstructionSite._process`) в
  `BONUS_KEYS`, подписях HUD и весах ИИ; в ветке рабочего `2d` +25 % и `4d`
  +50 % к темпу стройки, узлы колонки D (2d/3d/4d) теперь стоят и еду
  (`cost_food` → `upgrade_cost`, `_forge_cost_text` «е»). Темп добычи
  (`bonus_gather`) был и остался.
- **ЛИСТ КАМНЯ ЧЁРНОГО РАБОЧЕГО** был 1020×77 (сжатая перекраска) при
  общих 1152×192 — разбор `_add_frames` делит ширину на высоту и получал
  13 «кадров» по 77 px: отсюда моргание и половинки. Лист пересобран из
  `Pawn_Run Gold` тем же размером: шесть золотых оттенков заменены серыми
  (скрипт в scratchpad, вшит в патч спринта), остальное — как в оригинале.

## Десятая часть 10.09.2026: шаги тише, живые иконки, реплики реже, агро троллей

- **ШАГИ НА 30 % ТИШЕ** (`MARCH_WALK_DB` −11.0 → −14.1, `MARCH_RUN_DB` −9.1
  → −12.2: −3.1 дБ = ×0.7 по амплитуде). Бюджет `MARCH_BUDGET` не тронут —
  громкость ушла вниз от потолка, а не потолок за ней; `qa_voice` F и
  `qa_hud_afford` C3 держат обе стороны (не громче потолка, не громче прежнего
  минус 30 %).
- **ДОСТУПНОСТЬ ИКОНОК ЖИВЁТ ПО СИГНАЛУ, А НЕ ПО ПЕРЕВЫДЕЛЕНИЮ**
  (`HUD._afford_watch` ← `_watch_afford(btn, base, cost, faction, unit_id,
  bld)`, читает `_on_resources_changed` → `_refresh_afford()`). Кнопка найма
  и стройки красилась ОДИН РАЗ при сборке панели (`_cmd` с `_afford_color`),
  и игрок видел «нет денег» до следующего клика по зданию. Реестр хранит
  кнопку, базовый цвет, цену и последний вердикт; перекрашивается только
  сменившийся (`_style_cmd_button` — те же стилбоксы, что у `_cmd`, без
  второго описания рамки). Вердикт = `can_afford` И `pop_allows` — лимит
  населения красит так же, как склад. Реестр чистится вместе с
  `_train_badges` при любой пересборке панели — мёртвых кнопок не держит.
- **КУЗНИЦА: КРАСНАЯ РАМКА У НЕОПЛАТНОГО ДОСТУПНОГО УЗЛА**
  (`_forge_node_style(done, busy, avail, afford)`, `FORGE_LACK_BG/BORDER`,
  `_retint_forge_nodes()` по тому же сигналу). Раньше узел знал три
  состояния (изучен / идёт / доступен) и цену показывал только текстом в
  плашке. Плашка узла теперь показывает цену `_build_cost_row` — тем же
  рядом иконок, что карточка найма (один источник `RES_DEFS`); строки
  «Цена: 300 з» больше нет.
- **«ДЕРЖАТЬ СТРОЙ» ЗВУЧИТ ЧЕРЕЗ РАЗ** (`AudioManager.VOICE_ALTERNATE`,
  счётчик `_voice_alt`): пропущенный вызов всё равно ставит `_voice_last`,
  то есть окно повтора отсчитывается и от пропуска. Окна `VOICE_LIMITS` у
  марша и строя 6 → 9 с. Рог атаки не тронут — он и есть подтверждение
  приказа.
- **ТРОЛЛЬ В БОЮ БЫСТРЕЕ НА 20 %** (`Troll.tick_physics`: `move_speed =
  _base_move_speed × TROLL_COMBAT_SPEED_MULT` при цели или разгоне, назад —
  без них; смена уходит в ядро `_soa_push_stats()`, кэш `_base_speed()`
  ключуется на `move_speed`). Патруль и обед — обычным шагом.
- **АГРО-ЦЕПОЧКА ЛОГОВА** (`TrollLair.on_guard_hit` ← `Troll.take_damage`,
  `on_troll_died`): первый удар по любому стражу — ужаленный берёт обидчика
  целью (`command_attack(attacker, true, true)`, если цели нет), из логова
  выходят `TROLL_AGGRO_HELPERS` 2 с тем же приказом (волна 1); выбита вся
  группа — мгновенно `TROLL_AGGRO_BONUS` 2 (волна 2) на последнего обидчика,
  и только у них `standard_reinforce = true` — прежняя подмога на трети
  запаса (`TROLL_REINFORCE_AT`). Логово считается зачищенным по гибели
  бонусной пары и её подмоги. **Приказ атаки — по ОТРЯДУ обидчика:**
  `squad_pick_member` раздаёт помощникам разных бойцов отряда, стенд
  сверяет `squad_id` цели, а не ссылку. `qa_troll_impact` выключает
  `aggro_enabled` на время проверки разгона — помощники ломали бы замер.
- **СТЕНДЫ, КРАСНЕВШИЕ НА РАЗВОРОТЕ, А НЕ НА КОДЕ:** `qa_voice` F1в
  утверждал «не тише прежних шести голосов» — планка опущена на заказанные
  30 % (×0.7, и не громче прежней); `qa_troll_impact` C7 «вставшие снова в
  бою» держался на ТРЕТЬЕМ тролле — подмоге на трети запаса, которая шла к
  копейщикам и била их; у стартового стража её больше нет, стенд теперь
  оживляет тролля и ждёт ответа на удар; `qa_troll` B8/C/D бил обидчиком
  `sp[1][0]`, павшим в бою (правило 5) — берётся живой боец отряда; серия
  ударов и одышка там меряются при ВЫКЛЮЧЕННОМ агро (три тролля вырезали
  24 копейщика до одышки), агро включается перед блоком C и помощники
  замораживаются — отряд нужен живым для зачёта 180 убийств; `qa_sheep` C3
  ждёт физкадры (под нагрузкой два кадра отрисовки давали третью съеденную
  овцу — стадо > `TROLL_HUNGRY_FLOCK` едят без таймера).
- **ПАПКА ОВЕЦ — `resources/Sheep`, С ЗАГЛАВНОЙ** (правило 8): `Sheep.DIR`
  стоял строчными, в редакторе это только предупреждение «Case mismatch» на
  каждый кадр, в экспорте — пустая лента.

## Одиннадцатая часть 10.09.2026: тактический разум гоблинов при штурме базы

Заказ: орда не «слепая лавина», а штурм с паттернами, разведкой боем и
отходом на лечение. Код — `scripts/goblin/GoblinAttackCommander.gd`, числа —
`goblin_config` (раздел «ТАКТИКА ШТУРМА БАЗЫ»), стенд `qa_goblin_tactics`.

- **КОМАНДИР ВЕДЁТ ТОЛЬКО ФАЗУ ОХОТЫ** (`GoblinAI._issue_orders`,
  `PHASE_HUNT`, ручка `ASSAULT_TACTICS`): «штурм базы» — это она; волна в
  центр карты и оборона деревни остались прежней лавиной. Отряды мести и
  лечащиеся в хижине в план не входят. Стенд задаёт цель охоты
  `hunt_target_override` вместо `_weakest_target`.
- **КОМАНДИР ВЫДАЁТ ПЛАН, А НЕ ПРИКАЗЫ.** Записи для той же очереди
  `_order_queue` (`_issue_squad`: марш / атака / **отход** — новый вид с
  `begin_retreat` + `command_move(keep_retreat)` и РАЗМЕТКОЙ НА ЛАГЕРЬ (не
  снятой: хижина снимает её потому, что отряд уходит в ворота, а у костров он
  остаётся и стоит толпой — `qa_goblin` K3а ловит пустую разметку), и флаг
  `wake` — приказ снимает режим отхода: щуп, вернувшийся к линии, снова идёт
  в бой, а в отходе боец целей не берёт). Правило stagnant target внутри
  командира: марш в ту же точку не чаще `REISSUE_SEC`, атака отряду в бою не
  переиздаётся, отход — один раз на смену состояния.
- **СОСТОЯНИЯ:** `assembling` → паттерн → `full_charge` → (`retreat_and_heal`
  → `idle` → новая волна). Линия сбора — `ASSAULT_STANDOFF` 30 м от
  БЛИЖАЙШЕЙ чужой постройки этой базы (`ASSAULT_BASE_RADIUS`): вне стрелы и
  башни (20 м), внутри обзора построек (34 м). Ось «армия → база»
  замораживается на время штурма (иначе фланги менялись бы местами, пока
  армия стоит на линии). Сбор состоялся по СВОЙСТВУ (доля отрядов у своих
  мест) либо по таймауту — застрявший отряд не держит штурм вечно.
- **ПАТТЕРНЫ:** «провокатор» (`probe`): один пеший щуп на фланг
  (`PROBE_FLANK_OFFSET` поперёк оси) на `PROBE_INFANTRY_SEC`, отход к линии в
  режиме отхода, укол конницы на `PROBE_CAVALRY_SEC` и отход, пауза, другой
  фланг, после `PROBE_CYCLES` — общий навал туда, где чужих меньше
  (`weakest_side` считает по сетке ядра `query_radius`); «откатные волны»
  (`hit_and_run`): конница в лоб на `HR_CAVALRY_SEC`, откат к линии, пехота
  в брешь, конница возвращается через `HR_CAVALRY_BACK_SEC`; затяжной замес
  (`HR_BRAWL_SEC`) с потерей `HR_LOSING_DROP` доли — отход всей армией;
  «веер» (`fan`): три группы по месту на линии, центр первым, фланги
  каскадом `FAN_CASCADE_SEC`. Новая волна — ДРУГОЙ паттерн (не прошлый).
- **ОТХОД И ЛЕЧЕНИЕ:** запас армии (сумма текущего к сумме полного) ниже
  `ASSAULT_RETREAT_HP` 0.5 — все отряды в режиме отхода идут в лагерь:
  деревня, если она ближе `ASSAULT_CAMP_FAR`, иначе передовая точка на оси
  отхода в `ASSAULT_CAMP_DIST` 70 м от края базы (вне башен). Лечение у
  костров — раз в такт вожака на `THINK_INTERVAL` вперёд по
  `CAMP_HEAL_PER_SEC` в `CAMP_HEAL_RADIUS`, только отрядам вне боя
  (покадрового пути нет). Восстановились до `ASSAULT_REARM_HP` и дошли —
  режим отхода снят у всех (`end_retreat(true)`), `waves` растёт, новая
  волна. **Из рукопашной командир отходит** — это заказ («весь отряд по
  команде даёт дёру»); прежнее «из рукопашной не отходят» осталось у отхода
  разбитых в хижину (`_retreat_broken`).
- **РЕЖИМОВ У КОМАНДИРА ТРИ, ПО ФАЗАМ ВОЖАКА** (уточнение владельца в тот
  же день): охота — штурм (выше); **центр — марш с дозором**
  (`plan_advance`, `ST_ADVANCE`): тело идёт к точке линией поперёк живой
  оси, конный отряд-дозор — впереди тела на `VANGUARD_LEAD` (цель дозора едет
  с телом, переиздаётся при уходе дальше `VANGUARD_REISSUE_DIST`); дозор
  заметил чужих в `VANGUARD_SIGHT` — армия в `full_charge` на точку контакта,
  никто не дерётся и чужих у армии нет — снова марш; потери — тот же отход и
  лечение, после восстановления — марш, а не штурм. **Оборона деревни**
  (`plan_defense`, `ST_DEFEND`): угроза — средняя точка чужих в
  `DEFEND_RADIUS + DEFEND_ALERT_PAD` от деревни (по сетке ядра); пехота
  держит РУБЕЖ у костров в `DEFEND_LINE_DIST` от центра в сторону угрозы и
  бьёт только то, что подошло на `DEFEND_ENGAGE` (на убой к врагу не бежит);
  конница заходит с фланга угрозы (`DEFEND_CAV_FLANK` поперёк), колет
  `DEFEND_CAV_STRIKE_SEC`, отходит к деревне на `DEFEND_CAV_BACK_SEC`, сторона
  чередуется; отряд с запасом ниже `ASSAULT_RETREAT_HP` уходит к кострам
  (`wounded`) и возвращается на рубеж от `ASSAULT_REARM_HP`; угрозы нет — все
  у костров, раненые лечатся. Смена режима сбрасывает ход командира
  (`_set_mode`). Отход разбитых в хижину (`_retreat_broken`, 15 % пика)
  остался и работает раньше командира.
- **ЛОВУШКИ СТЕНДА:** деревня выключается (`perf_config.goblin_village`),
  армию ставит стенд; часы вожака двигает стенд (`clock += THINK_INTERVAL`,
  `tick()`, `_drain_orders` до пустой очереди, `THINK_INTERVAL × 60`
  физкадров) — игровое время равно часам, и таймеры паттерна честные;
  «в бою» (`squad_in_combat`) отвечает «да» уже на ПРИКАЗ атаки — «без боя»
  меряется контактом (`squad_engaged`).

## Двенадцатая часть 10.09.2026: старт без крепости, туман как право строить,
## башня с лучниками, тролль, трава, меню

Девять скриншотов владельца и семь блоков заказа. Ниже только то, что нельзя
вывести из кода. Стенд под блоки 1-3 и 5 — `qa_start_build` (30 проверок).

- **ПАРТИЯ ОТКРЫВАЕТСЯ ПЯТЬЮ РАБОЧИМИ, БЕЗ КРЕПОСТИ** (`Main.start_game`).
  Прежний стартовый флоу (фантом замка под курсором, зажатый в квадрат
  `PLAYER_PLACE_HALF` вокруг угла игрока) снят целиком: крепость закладывает
  РАБОЧИЙ кнопкой своей панели, там же, где любое другое здание. Механизм
  фантома (`enter_castle_placement`) оставлен и работает для второй и
  следующих крепостей; `clamp_to_player_start` тоже оставлен — по нему считают
  стартовую площадку камера и стенды. Признак «крепость у игрока была»
  (`_castle_placed`, гейт проверки поражения) ставится ПО ФАКТУ появления
  своей крепости на карте, а не стартовым флоу.
- **СТАРТ СДВИНУТ ОТ УГЛА НА `PLAYER_START_SHIFT` 20 м** по обеим осям:
  камера упиралась в зажим границ, и половина стартового круга обзора уходила
  в черноту. `qa_world` 2i переписан на «симметрия С ПОПРАВКОЙ на сдвиг».
- **СТРОИТЬ МОЖНО ТАМ, ГДЕ НЕТ ТУМАНА** (`Main.can_build_at`, старое имя
  `in_build_radius` оставлено обёрткой — его зовут фантом и клик). Прежнее
  правило «только в `BUILD_RADIUS` 50 м от своей крепости» отменено вместе со
  стартовым замком: партия начинается в чистом поле, и первую крепость ставить
  было бы негде. Правило ОДНО для крепости, башни и любого дома, и оно же
  красит фантом; туман выключен (стенды, отладка) — разрешено всё поле.
  Подсказка при промахе: «Здесь туман войны — место не разведано!».
- **НА СТАРТЕ РАСКРЫТО РОВНО НИЧЕГО** (`_reveal_start_area` больше не зовёт
  `add_permanent_reveal`): круг игроку дают сами рабочие
  (`FogOfWar._collect_unit_sources`, радиус `vision_radius` ≈ `VISION_MIN`
  15 м), а крепость расширяет его до **`CASTLE_VISION` 62 м** (переопределение
  `Castle.vision_radius`; башня по-прежнему `TOWER_VISION` 35). Замер стенда:
  на старте освещено 1.7 % карты, центр карты в тумане. `qa_fog` B3/B4/B6
  переписаны: прежние проверки утверждали ровно то требование, которое
  владелец развернул.
- **ЛИМИТ НАСЕЛЕНИЯ: КРЕПОСТЬ +10 РАБОЧИХ И +5 ОТРЯДОВ**
  (`POP_BASE_WORKERS` 6 → 5, `POP_BASE_SQUADS` 3 → 0, `CASTLE_WORKER_SLOTS`,
  `CASTLE_SQUAD_SLOTS`; `GameManager.castle_count`). База — ровно стартовая
  бригада: пять рабочих и ни одного боевого отряда, то есть первая крепость
  обязательна для армии. Считаются ВСЕ живые крепости, а не только первая:
  «только первая» было бы правилом, которого игрок не видит.
- **ЗЕЛЁНЫЕ ПЛАШКА И РАМКА СНЯТЫ** (`START_ZONE_SHOWN`, `WORLD_BEVEL_SHOWN`
  = false). За краем карты — только чернота (`WorldVoid`) и физические стены
  (`_build_world_walls` вынесена своей функцией). Геометрия бортика оставлена
  под ручкой: она умеет садиться на рельеф и зашивать шов с полем.
- **БАШНЯ: ТРИДЦАТЬ ЛУЧНИКОВ ВНУТРИ, ДЕСЯТЬ НА НАСТИЛЕ**
  (`TOWER_VISIBLE_ARCHERS` 10, `Tower._sync_sentinel` строит ряд спрайтов —
  чётные вправо, нечётные влево от середины настила). Огонь ведут ТОЛЬКО
  видимые: тридцать стрел с одной башни в залп — это батарея, а не башня.
  Ряд смыкается по числу ЖИВЫХ (`garrison_men`), и это и есть «на место
  павшего встаёт резервист изнутри».
- **УРОН ПО БАШНЕ РАЗДЕЛЁН ПО ТИПУ ОРУЖИЯ** (`Tower.take_damage`): дальний
  бой (`attack_range > MELEE_RANGE_MAX` у нападающего, либо чужая постройка)
  бьёт ЛУЧНИКА на настиле, ближний — только стены. Раненый лучник выходит с
  карты обратно (`garrisoned` снят), получает урон ШТАТНЫМ `take_damage` и,
  если погиб, кладёт тело у подножия (`corpse_spot`, веером по счётчику
  падений) — то есть всей обычной машинерией: фраг, ветеранство обидчика, труп
  в общем слое, стрела в теле (`CorpseRenderer.stick_arrows`). Выжил —
  возвращается внутрь. Запас башни 1200 → **6000**, цена 150/60/220 →
  300/200/600, стройка 18 → 30 с.
- **ТРОЛЛЬ: МЕТКА, ПРИЦЕЛ, СТРЕЛЫ, ОКРУЖЕНИЕ, УГОЛ ПАДЕНИЯ.**
  Тени под ним НЕТ вовсе (`Unit.shadow_scale` → 0: тёмное пятно вчетверо шире
  пехотинца читалось как зелёный круг на траве), кольцо стало ОВАЛОМ и шире
  (`Unit.ring_oval` → `TROLL_RING_OVAL` 1.45 × 0.9 поверх `ring_scale` 4.2;
  овальность едет и в кольца наведения/приказа). Стрелок целится в
  **`aim_height()`** — у пехоты грудь 0.8 м, у тролля `TROLL_AIM_HEIGHT` 2.6:
  прежние 0.8 у шестиметровой туши это ступни, и залп уходил в землю.
  Стрелы ТОРЧАТ В ЖИВОЙ ТУШЕ (`arrow_sockets` 8, `Troll.stick_arrow` →
  `Arrow.move_stuck`): точку каждой ведёт сам тролль в своём тике, места — по
  спирали золотого угла; мест нет — гаснет самая старая; гибель гасит все.
  Пехота платит за это ОДНО сравнение числа (`arrow_sockets() > 0`).
  **ОКРУЖЕНИЕ БОЛЬШЕ НЕ ДЕРЖИТ:** при `TROLL_SURROUND_FOES` 4 чужих в дуге
  дубины тролль пробивает тела шагом (поле `Unit.breaks_bodies` → тот же бит
  `F_ORDER_PASS`, что у билета игрока: чужое тело сужается до ядра) и раз в
  `TROLL_SHOVE_SEC` расталкивает кольцо на `TROLL_SHOVE_PUSH` — **в том числе
  на одышке**, поэтому «замер» на экране не читается, а сама одышка (заказ
  09.09.2026) не тронута. Признак — ПОЛЕ, а не виртуальный метод: его читает
  горячий путь шага у каждого шагающего.
  Туша падает под углом: квад тела ставится в мировой ориентации
  (`world_fixed`, ракурс камеры в игре зафиксирован) и кренится вокруг оси
  взгляда на ±`TROLL_DEATH_TILT` 34° — сам рисунок не трогается, обрезок нет.
- **БОЕЦ РИСУЕТСЯ ПОВЕРХ ПЕПЕЛИЩА** (уником `depth_push` в `cyl_billboard`).
  Руина — наземный мусор, но её точка сортировки лежала в ЦЕНТРЕ пятна, а
  нарисована она вверх на всю высоту: тролль, стоящий южнее центра руины,
  честно проигрывал Y-sort и уходил под картинку. Точка сортировки руины
  сдвинута ОТ КАМЕРЫ на половину её нарисованной высоты — и «поверх руины»
  выполняется для всех, кто стоит перед её дальним краем. Разбор глубины по
  точке на земле — в разделе «Порядок перекрытия спрайтов».
- **ТРАВА: ЧИСТО ЗЕЛЁНАЯ И ВДВОЕ МЕЛЬЧЕ** (`GRASS_TINT` 0.62/1.0/0.52 —
  множитель поверх тайлсета, красный и синий срезаны; `GRASS_SUBDIV` 2 —
  клетка рельефа 2.5 м режется на четыре подквада, КАЖДЫЙ со своим полным
  тайлом). Геометрия рельефа и выбор тайла не изменились; гора и песок брода
  не дробятся. `qa_visual_smoke` 19/19 после правки.
- **ОВЦА: ДЕСЯТЬ УДАРОВ НА КУСОК, ПЯТЬ ХОДОК.** ЧИСЛА РАЗВЁРНУТЫ СПРИНТОМ 13
  (пять ударов до смерти, 20 ходок по 15 = 300 еды, туша лежит на боку и НЕ
  мигает) — см. его раздел; ниже описан механизм, он не менялся.
  (`SHEEP_CUTS_PER_MEAT` 10, `SHEEP_MEAT_TRIPS` 5 → 20, `MEAT_PER_TRIP` 12 → 15,
  `SHEEP_CUT_SEC` 1.2 → 0.45).
  Мясо зачисляется НЕ у туши, а ПО ПРИХОДУ на склад (фаза `HAUL`) — это и
  есть заказанная ходка; после неё рабочий возвращается к туше (фаза `BACK`),
  на пятой овца выработана и исчезает. Пока рабочий несёт САМУ овцу, лента
  груза не нужна вовсе (`walk` вместо `carry_meat`): овца едет отдельным
  спрайтом над головой. Мигание надреза мягче: (1.7, 0.45, 0.45) → (1.35,
  0.85, 0.85). **Овца размножается сама** (`SHEEP_SELF_BREED_SEC` 150,
  `SHEEP_CALM_SEC` 25): личный таймер у каждой, любой удар сбрасывает покой,
  потолок стада держит логово. Итог за тушу тот же — 60 еды; прирост склада
  МЕНЬШЕ сданного, и это не потеря: сорок секунд разделки армия ест
  (`_sweep_food`), стенд судит по `meat_delivered`.
- **ПОСЛЕ БОЯ ВОЙСКА РАССЕИВАЮТСЯ** (`EnemyAI._command_scatter_at`,
  `SCATTER_STEP` 16 м, `SCATTER_COLS` 5). Последний рубеж собирает ВСЕ отряды
  к одному якорю; без противника рядом это и есть куча в углу карты. Теперь
  при `_player_combat_near(anchor) == 0` отряды расходятся сеткой шеренгами, а
  при живом противнике рубеж остаётся прежним (там куча — это строй).
- **МЕНЮ: ЗАГОЛОВОК И ЧЕТЫРЕ КНОПКИ** (`MainMenu`, страницы `PageMain` /
  `PageLoad` / `PageOptions`, переключает `show_page`). На главной — START
  (квадратная, без стрелок), Загрузить, Опции, Выход. Подзаголовок, ряд
  сложности со строкой-подсказкой, слоты сохранений и «Опции звука» с главной
  убраны. В опциях: расы и цвета, сложность ВЫПАДАЮЩИМ СПИСКОМ (подсказка
  уровня — строкой под ним), три ползунка звука и отображение — чекбоксы
  «Отображать FPS» и «Камера едет от края экрана». Обе ручки живут на диске
  (`game_settings.load_view_settings` → `user://view_settings.cfg`) и читаются
  партией: HUD ставит видимость плашки FPS при сборке, камера спрашивает
  `edge_pan()` в своём тике. Пустой слот сохранения ВИДЕН, но не нажимается.

## Спринт 14 (11.09.2026): тролль, гноллы, овцы, огонь лучников, приёмы, VFX, башня

Семь блоков заказа плюс две задачи сверх него (VFX лечения монаха и спрайты
этапов башни). Стенды: **`qa_troll_hitbox`** (20), **`qa_gnoll_fix`** (24),
**`qa_worker_sheep_loop`** (22), **`qa_archer_logic`** (19),
**`qa_knights_push`** (28), **`qa_monk_vfx`** (24), **`qa_tower_states`** (23).
Полный разбор — в `docs/ENGINEERING_LOG.md`, раздел «Спринт 14».

- **СТРЕЛЫ НА ЛЕТУ ПРЕВРАЩАЛИСЬ В КОСТИ — ОДИН `int` НА ВСЕ ПОЛЁТЫ.** Номер
  буфера отрисовки в ядре (`ArmyCore._afB`) был СКАЛЯРНЫМ полем, и его
  перезаписывал каждый следующий `ArrowLaunch`: взлетела кость гнолла — и
  `BatchArrows` начинал писать позиции ВСЕХ летящих стрел в буфер костей.
  Раздача снаряда при этом была исправна (свои пулы, свои слои, признак `bone`
  до `_ready`), поэтому первый зонд ничего и не нашёл. **Всё, что описывает
  ОДИН полёт, обязано лежать в колонке полёта**; скаляром в пакетном проходе
  может быть только то, что одинаково для всех его строк. Стережёт
  `qa_gnoll_fix` E.
- **ТОЧКУ ЛЕТЯЩЕГО СНАРЯДА ЗНАЕТ БУФЕР, А НЕ УЗЕЛ.** На время полёта стрела —
  ЗАПИСЬ в ядре: узел стоит там, где взлетел. Стенд, меряющий высоту дуги по
  `global_position`, меряет точку вылета (первая версия `qa_gnoll_fix` C4 так и
  намеряла ровно `GNOLL_THROW_Y`). Читать — `rb_slot` слоя, точка в 3/7/11.
- **ТРОЛЛЬ: ТОНКИЙ ОВАЛ ВМЕСТО ТОЛСТЫХ ДУГ.** Обвод рисуется тем же
  64-сегментным мешем, что у здания (`UnitVisuals.building_ring_mesh`), а не
  растянутым в 4.2 раза кольцом бойца о двенадцати сегментах: 0.023 м трубки
  против 0.168. Признак — свойство `Unit.fine_ring_radius() > 0`, не список
  типов. **Гнёзд под стрелы у тролля больше нет вовсе** (`arrow_sockets()` → 0,
  заказ спринта 12 развёрнут), запас −20 %, прицел в центр массы, доля выстрелов
  намеренно уходит в землю (`aim_miss_chance` / `aim_miss_spread`).
- **ГНОЛЛ НЕ БЕЖИТ НА МЕСТЕ: КАЙТ ПРОВЕРЯЕТСЯ ПО ФАКТУ.** Не сдвинулся за
  `GNOLL_KITE_GIVEUP` больше чем на `GNOLL_KITE_MIN_GAIN` — отход признаётся
  невозможным, глушится на `GNOLL_KITE_BLOCK_SEC`, скорость обнуляется и боец
  уходит из `MOVING` (лупа шагов больше нет). Агро 10 м, бросок 8.5 м, дуга
  ×1.35 (навес выше собственного пути по земле).
- **ЛУЧНИКИ ВСТАЮТ ОТРЯДОМ ПО ПЕРВОМУ ДОСТРЕЛИВШЕМУ**
  (`GameManager._ranged_engaged`, `squad_ranged_engaged_set/get/release`).
  Личный `_engaged_once` верен только для того, кто дошёл, и отряд втягивался
  в противника ниткой по одному. Отметка живёт рядом с якорем погони (общий
  жизненный цикл, снимает обоих новый приказ), но ОТДЕЛЬНО от него: якорь
  ставится и рукопашной пехоте, а «встать и стрелять» — правило только
  дальнобойных.
- **МНОЖИТЕЛЬ ТОЛЧКА ИДЁТ ПОСЛЕ ПОТОЛКА ШАГА, И ТЕПЕРЬ ОН ВИРТУАЛЬНЫЙ**
  (`Unit._push_after_cap()`). Первая версия «Яростного Набега» поднимала только
  НАПОР — и жертву отодвигало на 0.70 м против 0.68 без набега: весь избыток
  съедал `clampf` (потолок 0.4 м срабатывает уже при разнице напора в три
  единицы). Работает ровно то, что у конницы: множитель ПОСЛЕ потолка. После
  правки 2.37 м против 0.82. Тем же признаком теперь идут плавный канал и
  вмятина разметки — без вмятины смыкание рядов вернуло бы строй на место.
- **VFX ЛЕЧЕНИЯ МОНАХА ЖИВЁТ НА ИСЦЕЛЯЕМОМ** (`Monk`, лента `Heal_Effect.png`,
  11 кадров). Узел ОДИН на монаха, а «эффект на юните» — это ПРИВЯЗКА
  (`_heal_vfx_target`): снять эффект значит разорвать её и погасить квад.
  Точка берётся нарисованная (`draw_position`), высота — `aim_height()` цели
  (у тролля 2.7 м, у пехотинца 0.8 — общая константа повесила бы заклинание
  либо в ноги, либо в небо). Пять причин прекращения, каждая своя проверка.
  **`remove_child` в `_exit_tree` движок отбивает** — та же грабля, что у
  укладки упавшего знамени.
- **У БАШНИ СВОИ СПРАЙТЫ ЭТАПОВ** (`Tower_Construction` / `Tower_Destroyed`).
  Набор ищется ПО ID постройки (`game_settings.PROCESS_SPRITES`), а не списком
  исключений; строка таблицы называет корень, потому что файлы лежат рядом с
  постройками, а не в папке «процессов» — копировать их во вторую папку нельзя
  (две копии одной картинки разъедутся). Доли `construction_scale` /
  `ruin_scale` сняты в единицу: они ужимали ЧУЖУЮ «домовую» картинку, а своя
  нарисована ровно по башне. `qa_tower` B3б переписан с снятого требования.

### Что стенды нашли про сам движок правил (не чинилось, знать обязательно)

- **ПРИКАЗ АТАКИ ПО ЦЕЛИ ДАЛЬШЕ `_lock_sight_range()` НЕ ДОЖИВАЕТ ДО НЕЁ.**
  Замок держится, пока цель ближе максимума из двойной дальности оружия и
  2 × `AGGRO_RADIUS` (лучник 40 м, пехота 20 м); дальше приказ считается
  исчерпанным, боец переносит пост и разворачивается. Замер: отряд, посланный
  на врага в 46 м, шёл девять секунд до 30 м и уходил обратно. Стенды ставят
  цель ВНУТРИ этой черты.
- **КЛИК ПО БОЙЦУ НА ВОЗВЫШЕННОСТИ ПРОМАХИВАЕТСЯ.** `SelectionManager._pick_at`
  считает точку земли пересечением луча с плоскостью y = 0 («грунт плоский»):
  боец на плато в 5.65 м уводит её на 6.5 м, мимо любого радиуса поиска.
  `qa_knights_push` выбирает ровную площадку сам (`_flat_spot`).
- **ОТРЯД, ВСТАВШИЙ СТРЕЛЯТЬ, УХОДИТ ОБРАТНО, КОГДА ПРИКАЗ ИСЧЕРПАН** (зонд
  `qa_archer_logic` без вердикта: центр 25.8 → 45.4 м за 20 с). Путь штатный —
  `release_target_lock` плюс `_hold_where_i_stand`; спорно ровно одно: цель
  признаётся недостижимой у стрелка, который по ней СТРЕЛЯЕТ. **Момент снятия
  в headless плавает** (настенные часы против физкадров, правило 12), поэтому
  окно замера в блоке C закрывается по снятию замка, а не по таймеру.

## Конфиги — источник истины

- `scripts/unit_stats_config.gd` — характеристики, стойки, ветеранство, цены,
  стартовые ресурсы, баллистика лучника, сплочённость, **формула брони, мораль
  и паника, легендарные перки**. **Правит владелец.**
  **СТАРТОВЫЙ ЗАПАС ИГРОКА — 20 000 КАЖДОГО РЕСУРСА, ЗАФИКСИРОВАН ВЛАДЕЛЬЦЕМ
  10.09.2026: «никогда не сбрасывай и не меняй мои настройки ресурсов».** Не
  трогать ни при каком красном стенде и ни в каком разборе баланса; стенд,
  которому нужен «скромный запас», судит по запасу ИИ (`AI_STARTING_RESOURCES`,
  так переписан `qa_balance` E2). Прежняя запись «стартовый запас урезан до
  550/380/340/200» в разделе «Баланс» — история.
  Расчёт всего перечисленного — `docs/BALANCE_MATH.md`.
- `scripts/forge_config.gd` — древо кузницы (`UNITS[unit][cell]`, одна таблица);
  узел древа = слот улучшения, поэтому вся машинерия исследований работает без
  ветвлений.
- `scripts/ai_start_army_limit.gd` — лимиты, тактики, сбор волны, поведение ИИ,
  а также **`START_SQUADS`** — стартовые отряды красного ИИ с явным рангом
  (по умолчанию список ПУСТ: старт равный, это стережёт `qa_ai`, проверка 2).
- `scripts/game_difficulty_config.gd` — пресеты `Easy` / `Normal` / `Hard`.
- `scripts/SaveLoadManager.gd` — сохранение и загрузка партии.
- `scripts/perf_config.gd` — переключатели и шарды; `tick_meter`/`vis_meter` дают
  честное время, `profile_physics` — только доли (завышает сумму ~1.8×).
- `BALANCE.md` — где какое число. `ASSETS.md` — куда класть файлы.

## Известные ограничения

Нет поиска пути и мультиплеера. Фракция в меню меняет только папку спрайтов.

**МАСШТАБ ЗАМЕРЕН ТОЧНО (26.08.2026), И СОСТАВ АРМИИ РЕШАЕТ БОЛЬШЕ ЧИСЛЕННОСТИ.**
`qa_mass_siege` на 3000×3000 смешанным составом (как в партии) укладывается в
бюджет: марш 10.5, свалка 11.2, затяжной бой **11.6-12.9 мс** против 16.6 —
проверка B3 ПРОШЛО. `qa_mass_battle` на тех же 6000 даёт 18.1 мс и FAIL, и это
ожидаемо: там шесть тысяч ЧИСТЫХ копейщиков, все до одного идущие в контакт.
15000 не укладываются и на марше.

Разбор по веткам, история правок и то, что из них окупилось, — в
**`docs/PERF_6000.md`** (раздел 6 — заход по `grid_update` и `check_auto_aggro`).
**Стратегия следующего прыжка (70+ FPS на 4000, этапы D1-D3: тыловой напор и
автопилот подхода в ядре → тред-пул пакетных проходов → туман/стрелы/буферы) —
в `docs/PLAN_D_70FPS.md`**; там же — почему халвинг частоты тика и полный порт
FSM в C# уже измерены и в план не входят.
Оттуда же выводы, которые стоит знать до того, как браться за следующую
оптимизацию:
- **РАЗБИВКА ПО ВЕТКАМ ГОДИТСЯ ДЛЯ РАНЖИРОВАНИЯ, НО НЕ ДЛЯ БЮДЖЕТА.** Профиль
  ставит пару `Time.get_ticks_usec()` вокруг КАЖДОЙ ветки КАЖДОГО бойца, и у
  ветки, которая идёт 1100-1600 раз в кадр, измеритель стоит соизмеримо с ней
  самой. Замер авг. 2026: профиль обещал 1.83 мс на `check_auto_aggro` +
  `grid_update`, честный A/B по `tick_meter` дал **0.85**. У веток, которые
  зовутся РАЗ В КАДР (`squad_corridor`, `sep_overlap`, `batch_move`), завышения
  почти нет — их числа читаются как есть.
- **ПЕРЕД ТЕМ КАК ЧИНИТЬ ВЕТКУ, ЕЁ НАДО ВЫКЛЮЧИТЬ И ПОСМОТРЕТЬ НА ТИК.**
  `squad_corridor` выглядел крупнейшей целью (0.94 мс/кадр), а выключение его
  сделало тик ХУЖЕ на 0.8 мс: коридор раздаёт отрядные `_clear_trunk` /
  `_clear_enemy`, и без них каждый боец сам лезет в сетку. Он стоит 0.9 и
  экономит 1.7.
- **КАСКАД ПО КАДРАМ И КУРСОР ВМЕСТО `append` ПРОВЕРЕНЫ И НЕ ОКУПАЮТСЯ.**
  Авто-агро и так входит только в `State.IDLE`, частоту задаёт таймер плюс
  отрядный кэш цели, а фазы разведены при рождении. Очередь поз дорога не
  проверкой ёмкости, а самим числом колонок.
- **«Сон задних шеренг» проверен дважды и отвергнут дважды.** Механика уже
  написана (`perf_config.battle_lines`) и выключена по замеру: экономятся не
  лишние вычисления, а бойцы, переставшие доходить до врага (контакт падает с
  25 % до 6 %).
- **База замера снимается в том же заходе, что и правка.** Вчерашние числа
  сравнивать с сегодняшними нельзя: правка, ничего не менявшая, «ухудшила» тик
  на 1.6 мс — уехала база, а не код.
**Список пре-существующих «красных» стендов (не регрессий) — в
`docs/ENGINEERING_LOG.md`, раздел «Известные красные».** Коротко: перед тем как
чинить код по красному стенду, проверьте, не утверждает ли проверка требование,
которое владелец только что развернул, и не ждёт ли она `process_frame` там, где
нужен `physics_frame`.

## Стресс-стенд `qa_mass_siege`: 700 на 700 с базой

**Правки боевого поведения проверяются ЗДЕСЬ, а не на стендах из десяти
бойцов.** Заказ владельца дословно: «стерильные тесты показывают зелёный дым,
а в реальном бою игра ломается». Сцена: две смешанные армии по 700 (копейщики
0.45, лучники 0.25, мечники 0.20, кабаны 0.10, отряды по 35) плюс база
противника из шести построек. Пять разделов, девять вердиктов: марш (разброс,
«бег на месте»), проход сквозь союзный строй, рабочие, бой с новым приказом
после него, осада. Тик печатается в каждом разделе.

- **ПОРЯДОК РАЗДЕЛОВ ЗНАЧИМ: осада идёт ПОСЛЕДНЕЙ.** Она переставляет пять
  отрядов к базе, и если мерить бой после неё, в замер попадают отряды,
  бредущие обратно через полкарты. Именно это давало разброс то 5.8, то 24.6 м
  на НЕИЗМЕННОМ коде.
- **Осада меряется У ЗДАНИЯ, а не по дороге к нему.** Первая версия отдавала
  приказ оттуда, где отряды стояли, — между ними и базой была вся чужая армия,
  и до постройки не доходил никто (сектора розданы верно, отставание 33-45 м),
  а «ближайшие центры 1.9 м» описывали ОБЩУЮ СВАЛКУ на полпути. Теперь отряды
  ставятся ОДНОЙ КУЧЕЙ с тыла базы — в том самом худшем состоянии, на которое
  жаловался владелец.
- **Проверки — СВОЙСТВА, а не круглые числа.** «Центры отрядов не ближе 4 м»
  невыполнимо по геометрии: пять отрядов стоят на периметре постройки, и
  соседние центры не могут разойтись дальше хорды `2·r·sin(дуга/2n)`. Стенд
  считает эту хорду сам и сравнивает с ней.

## Форма отряда при приказе: зажим выводится из ДИАГОНАЛИ БЛОКА

`SelectionManager._issue_march_keeping_shape` переносит строй как есть, но
смещение каждого бойца от центра ЗАЖИМАЕТСЯ: кто стоял дальше, получает слот
на границе круга в том же направлении. Без зажима растянутый боем отряд
переносил свою «колбасу» в разметку (замер: растянутость разметки 19.9 м).

**Радиус считается по полудиагонали блока (`√n · интервал · SHAPE_CLAMP_SLACK`),
а НЕ по кругу равной площади.** Первая версия брала `√n · 0.5 · интервал` и
вышла ТУЖЕ реального строя: у шеренги 10×3 полудиагональ ~3.05 м, а круг
равной площади — 2.38 м. Зажим сминал в круг ОБЫЧНЫЙ приказ (поймал
`qa_squad`, проверка 5: габариты разъезжались в 1.3 раза).

**Центр берётся МЕДИАНОЙ** (`GameManager._centroid_of`) — как и везде в
проекте. Стенд, сверяющий перенос смещений, обязан брать опору ТЕМ ЖЕ
способом с обеих сторон, иначе он меряет разницу среднего и медианы
(0.1-0.2 м), а не искажение строя.

## `post_pos` — это НЕ разметка отряда

**Личный пост бойца переписывает ЛЮБОЙ `command_move`** — и подзыв
сплочённости, и «встать где стою» после исчерпанного приказа. Это не порча
строя: пост означает «где меня поставили» и служит поводком авто-агро.
**Строевые места лежат отдельно — в `squads[sid]["slots"]`**, и именно по ним
смыкаются ряды. Стенд, меряющий «растянутость разметки» по `post_pos`,
краснеет на каждом законном подзыве отставшего; мерить надо `slots`.

Там же вторая ловушка: **огрызок отряда в замер разметки не идёт.** Двое
выживших с далёкими слотами дают «растянутую разметку» на пустом месте.

## Приказ атаки на постройку: сектора по ДУГЕ ПОДХОДА

`SelectionManager._ring_squads_around` раздаёт каждому выделенному отряду свою
точку вокруг постройки (кэш `GameManager.squad_attack_anchor`, читает подход
бойца в `Unit._process_attack`). Пять отрядов, посланных на одно здание, шли к
ОДНОЙ точке — его центру — и слипались в кашу.

- **Дуга разворачивается К ТЕМ, КТО ПРИШЁЛ** (`RING_ARC`, 150°), а не по всей
  окружности. **Поиска пути в игре нет вовсе:** сектор за постройкой
  недостижим — боец идёт по прямой, упирается в стену, дальность удара
  сходится, и он остаётся с ближней стороны, а отряд при этом рвётся надвое
  (замер: отставание 12.4 м у двух отрядов из пяти, разброс отряда 41.6 м).
  Это и есть «облепить со всех ДОСТУПНЫХ сторон».
- **Сектор достаётся по ФАКТИЧЕСКОМУ азимуту отряда**, причём азимут меряется
  от оси подхода и приводится в (−π, π]: иначе порядок рвётся там, где угол
  переходит через π, и отряды меняются местами, пересекая друг друга.
- **Кэш нужен потому, что одного приказа на движение мало:** следующий же
  `command_attack` повёл бы всех в центр здания, и куча вернулась бы.

## Анимация ходьбы — от ФАКТА перемещения, а не от намерения

`Unit.moved_recently()`: смещение замеряется в начале `tick_physics`, признак
держится `MOVE_ANIM_HOLD_MS` (180 мс) при скорости выше
`MOVE_ANIM_MIN_SPEED`. Прежнее условие `velocity.length() > 0.1` давало «бег на
месте»: `velocity` — это НАМЕРЕНИЕ идти, а упёршийся в чужой строй боец
намерение сохраняет и перебирает ногами стоя. Позу стоящего выдаёт
`Spearman._standing_key()`.

## Рабочие — ОДИНОЧНЫЕ АГЕНТЫ

`GameManager.SINGLE_AGENT_TYPES` / `squad_is_single_agent(sid)`. Выходя из
постройки, такой отряд не строится в шеренгу: места берутся по спирали
золотого угла (`Building.SINGLE_AGENT_SPREAD`), полоса выхода не задаётся.
Смыкание рядов рабочих не касается (`REFORM_SKIPS_WORKERS`).

## Проход сквозь союзный строй: послабление в СОЛВЕРЕ

`ArmyCore.BatchSeparation(..., passRelief)` (`Unit.SEP_PASS_RELIEF` = 0.45).
Послабление действует ТОЛЬКО между бойцами РАЗНЫХ отрядов и ТОЛЬКО когда один
идёт, а другой стоит: идущий продавливается сквозь стоящую шеренгу, а два
стоящих строя друг в друга не влезают. Замер `qa_mass_siege`: 35 из 35
пересекают чужой строй за 4.2-4.5 с, завязших ноль.

**Метрика прохода меряет ФАКТ ПЕРЕСЕЧЕНИЯ, а не «дошла половина».** Прежняя
версия выходила по «половина в покое» и меряла тем самым скорость самого
быстрого — застрявшие в счёт не попадали вовсе. Время считается ФИЗИЧЕСКИМИ
КАДРАМИ (правило 12), завязшие — через две секунды после того, как пересёк
девятый десяток.

## Сложность партии: Easy / Normal / Hard

- **ПРЕСЕТ — ЭТО МНОЖИТЕЛИ ПОВЕРХ БАЗОВЫХ ТАБЛИЦ, А НЕ ВТОРАЯ КОПИЯ БАЛАНСА**
  (`scripts/game_difficulty_config.gd`). В файле нет ни одного базового числа:
  лимиты берутся из `ai_start_army_limit`, спячка и доход орды — из
  `goblin_config`, стартовый запас — из `unit_stats_config`. Копия неизбежно
  разъехалась бы: владелец правит лимит в своей таблице, а на «обычной»
  сложности ничего не меняется, потому что там лежит вторая, забытая цифра.
- **НИКТО НЕ ЧИТАЕТ `PRESETS` НАПРЯМУЮ.** Все спрашивают функции файла — они
  принимают базу и возвращают изменённое. Точки чтения: `EnemyAI` (лимиты
  отрядов и рабочих, мирная фаза, потолок армии), `ResourceManager`
  (стартовый запас), `Building.queue_unit` (время найма),
  `ConstructionSite` (время стройки), `GoblinAI` (спячка), `GoblinHut`
  (доход), `Main` (оба стартовых состава).
- **NORMAL ОБЯЗАН БЫТЬ В ТОЧНОСТИ ПРЕЖНЕЙ ИГРОЙ** — все его множители равны
  единице. Сложности появились поверх баланса, который настраивали без них;
  сдвинься здесь хоть одно число, и прежние замеры перестали бы что-либо
  значить. Стережёт `qa_difficulty`, блок B (восемь проверок).
- **КРУТЯТСЯ ТОЛЬКО ЛИМИТЫ И ТЕМП.** Ни одна ручка не трогает урон, запас
  жизни и броню: боец на Hard дерётся ровно так же, как на Easy. Тогда
  «сложнее» означает «больше и раньше», а не «у врага другие правила», и
  любой замер боя остаётся сравнимым между уровнями.
- **СЛОЖНОСТЬ ХРАНИТСЯ СТАТИКОЙ, А НЕ В `GameManager`**: она нужна ДО того,
  как автозагрузка успевает что-либо решить (главное меню строится первым), и
  нужна она конфигам — а конфиг, лезущий в автозагрузку, тянул бы всё дерево
  движка в стенд, который его не поднимал. Выбор переживает перезапуск игры
  (`user://difficulty.cfg`).
- **СДВИГ РАНГА ОРДЫ НЕ КАСАЕТСЯ НОВОБРАНЦЕВ** (`goblin_vet_shift`): отряды с
  рангом 0 не трогаются ни на одной сложности. Иначе на Hard вся орда вышла бы
  ветеранской, а это уже другой сценарий, а не сложность.
- В интерфейсе: ряд из трёх кнопок в главном меню (с строкой-подсказкой под
  ним) и такой же ряд в меню паузы. **В паузе прямо на экране написано, что
  действует сразу, а что со следующей партии:** лимиты и темп читаются вживую,
  а стартовый запас и стартовые отряды раздаются один раз при закладке.

## Стартовые отряды: одна таблица на обе фракции

- **СОСТАВ, ЧИСЛЕННОСТЬ И РАНГ ЗАДАЮТСЯ ПОСТРОЧНО** — `goblin_config.START_SQUADS`
  у орды и `ai_start_army_limit.START_SQUADS` у красного ИИ. Формат один и тот
  же: `{"unit", "count", "vet", "picks"}`, где `count: 0` означает уставной
  размер отряда, а `vet` — уровень по `VET_BANNER_TIERS`.
- **ПОЧЕМУ СПИСОК, А НЕ ТРИ ЧИСЛА.** Прежнее описание орды (состав
  `ARMY_COMPOSITION` + «первым пяти выдать серебро с четырьмя наградами»)
  умело ровно один сценарий. Дать кабанам ранг выше, чем пехоте, или вывести
  один отряд-легенду было НЕЛЬЗЯ, не переписав код спавна.
- **`ARMY_COMPOSITION` И `ARMY_SQUADS` БОЛЬШЕ НЕ КОНСТАНТЫ**, а функции
  `goblin_config.army_composition()` / `army_squads()`, выведенные из
  `START_SQUADS`: два независимых списка разъехались бы, и правка стартовой
  орды молча меняла бы то, что она потом донанимает.
- **РАЗДАЧА НАГРАД ОДНА НА ВСЕ ФРАКЦИИ** — `Main.grant_squad_veterancy`. Она
  идёт тем же путём, каким награду берёт игрок (`apply_veteran_choice`), и
  второй реализации в проекте быть не должно.

## Сохранение и загрузка партии

- **МИР НЕ СОХРАНЯЕТСЯ, ОН ПЕРЕСЕВАЕТСЯ.** В файл уходит ЗЕРНО
  (`Main.world_seed`), а рельеф, лес, кусты, вода и россыпи руды строятся из
  него заново. Тысячи деревьев весили бы больше всей остальной партии и
  читались бы дольше, чем генерируются. Побочная выгода: замер, сделанный на
  «той самой карте», теперь можно повторить.
- **ЗЕРНО ЧИТАЕТСЯ В `Main._ready()`, И БОЛЬШЕ НИГДЕ.** Лес и вода сажаются
  там же, то есть ДО `start_game()`; зерно, поставленное позже, дало бы чужой
  рельеф под сохранёнными постройками. Отсюда и двухходовка: слепок кладётся
  в `GameManager.pending_load`, сцена перезапускается, `start_game()` в самом
  конце зовёт `SaveLoadManager.apply`.
- **БОЙЦЫ ЛОЖАТСЯ КОЛОНКАМИ** (`PackedInt32Array` / `PackedFloat32Array`), а
  не списком словарей — тем же приёмом, что и в ядре армии. Шесть тысяч
  бойцов это ≈170 КБ и один `store_var`; в JSON это были бы десятки мегабайт
  и разбор в секунды.
- **ФОРМАТ БЕЗОПАСЕН К ВЕРСИЯМ ДВУМЯ РАЗНЫМИ СПОСОБАМИ.** Поля читаются по
  именам и всегда с умолчанием (добавили ключ — старые файлы читаются);
  `FORMAT_VERSION` поднимают, когда меняется СМЫСЛ существующего поля, и
  тогда старый файл честно отклоняется. Отдельно живёт `WORLD_GEN_VERSION` —
  её поднимает тот, кто правит генератор карты: то же зерно дало бы другой
  мир, и такое сохранение грузить нельзя.
- **`store_var(..., false)` — full_objects ВЫКЛЮЧЕН НАМЕРЕННО.** В слепке нет
  и не должно быть ссылок на узлы: разреши их — и в файл уехал бы кусок
  дерева сцены, а чтение такого файла означало бы исполнение чужого кода.
- **ЧТО ВОССТАНАВЛИВАЕТСЯ:** склад, изученное в кузнице, постройки с их
  запасом жизни, отряды (ранг, счёт убийств, невыбранные награды, список
  выбранных, докупленные способности) и бойцы (род войск, сторона, отряд,
  точка, текущее здоровье). **ЧТО НЕТ:** стрелы, тела, туман, недострои,
  очереди найма и ПРИКАЗЫ — после загрузки войско стоит. Приказ ссылается на
  конкретный узел цели, а узлы после загрузки другие.
- **ЖИВОЕ СНОСИТСЯ СМЕРТЬЮ, А НЕ `queue_free`** (`_wipe_live_world`): только
  на этом пути боец снимает с себя строку в ядре армии, место в сетке соседей
  и место в отряде — то же правило, что у расформирования отряда.
- **ИССЛЕДОВАНИЯ ВОЗВРАЩАЮТСЯ ЧЕРЕЗ `finish_research`, А НЕ ЧИСЛАМИ ИЗ ФАЙЛА.**
  В файле лежит СПИСОК id; бонусы пересчитываются из таблиц, поэтому правка
  баланса в `forge_config` доезжает и до старых сохранений.
- Три слота (`user://save_N.tts`), подпись слота — время и сложность. Кнопки:
  меню паузы (сохранить и загрузить) и главное меню (загрузить; ряд не
  показывается вовсе, если сохранений нет).
- Стенд — `qa_difficulty`, блоки E–H (H поднимает НОВУЮ сцену Main из слепка
  и сверяет рельеф в трёх точках: зерно доехало — карта та же).

## Пятая партия правок (31.08.2026): застревание, интерфейс, ворота, звук

### Детектор зацикливания не тикал у рабочего вовсе

- **`Worker.tick_physics` ПЕРЕОПРЕДЕЛЁН, и в трёх своих состояниях (BUILDING /
  GATHERING / RETURNING) он до базового тика НЕ ДОХОДИТ.** Детектор
  зацикливания (`STUCK_CHECK_SEC` → `TRUNK_IGNORE_SEC`) жил прямо внутри
  `Unit._process_move`, то есть у работающего рабочего его не было НИКОГДА.
  Двигается он там сам, через `_move_blocked`, поэтому обход стволов работал —
  а признать себя наматывающим круги было нечем, и вокруг одного комля по
  дороге на стройку он мог ходить до конца партии. **Это третий случай одной
  и той же формы ошибки** (первые два — «рабочие несут ресурсы спиной вперёд»
  и «на стройке бегут на месте»): переопределённый тик не доходит до базового,
  значит не тикает и всё, что там считается.
- Лечение — вынести детектор в `Unit._tick_stuck(delta, dist)` и звать его из
  всех путей движения. Стережёт `qa_helpbuild` E7 (A/B: без правки
  «выключение стволов 0.00 с», с правкой 1.35 с).
- **ВТОРАЯ СТУПЕНЬ — СМЕНА САМОЙ ТОЧКИ** (`Worker.STUCK_REPATH_TICKS` = 4
  такта, то есть две секунды). Срабатывает, только когда первая ступень уже не
  помогла: столько проверок подряд без продвижения означает, что держит НЕ
  ствол, и выключать нечего. Тогда рабочий берёт ДРУГОЕ место на кольце —
  `claim_slot(who, renew = true)`, где прежний слот считается занятым (иначе
  выбор «ближайшего свободного» вернул бы ровно его же, ведь рабочий стоит
  рядом). Свободных нет вовсе — остаётся прежнее место, а не точка за внешним
  кольцом: уехать от жилы дальше, чем стоял, хуже, чем стоять.
- **ТОЧКА ДОБЫЧИ РУДЫ ТЕПЕРЬ ВЫВЕДЕНА, А НЕ СОВПАДАЕТ.** У дерева кольцо
  считается как `TRUNK_RADIUS + TRUNK_CLEARANCE + SLOT_GAP` явно, а у руды
  держалось на совпадении констант (препятствие 0.5 × size_scale при кольце
  1.0 ×). Замер по трём классам кусков: запреты 0.53 / 0.75 / 1.02 против
  колец 0.92 / 1.35 / 1.90 — безопасно, но ровно так однажды молча сломалось
  ДЕРЕВО, когда `SLOT_GAP` уменьшили ради руды. Пол по запретному кругу
  ничего не меняет сегодня и не даст сломать это завтра (`qa_helpbuild` E0б).

### Полосы выхода расходятся ВГЛУБЬ, а не вбок

- **ТРЕТИЙ РАЗВОРОТ ОДНОЙ ЖАЛОБЫ.** История: полосы расходились чисто вбок
  (четвёртый заказ в 13.89 м вбок при 4.47 м вперёд, 72° к фасаду) → добавили
  тот же шаг вперёд, вышел клин 45° (замер 37°) → владелец повторил жалобу
  дословно. И он прав: даже под 37° второй заказ уходит на СЕМЬ метров вбок,
  а четвёртый на четырнадцать.
- **НАПРАВЛЕНИЕ ПОЛОСЫ И ЕЁ ШАГ — РАЗНЫЕ ВЕЛИЧИНЫ, И ПУТАТЬ ИХ НЕЛЬЗЯ.**
  Требование «перед воротами» выполняет НАПРАВЛЕНИЕ (боковой составляющей нет
  вовсе), а вот ШАГ обязан остаться прежним. Первая версия брала шаг равным
  глубине строя (5.40 м у барака против 7.13 м бокового), и отряды встали
  ТЕСНЕЕ, чем стояли: `qa_rally2` E7 «центры не ближе 5 м» упал с 9.37 м до
  4.76 м. Теперь `Building._lane_depth_step` берёт БОЛЬШЕЕ из глубины строя и
  прежнего бокового шага — E7 снова зелёная (6.18 м).
  Замер `qa_gate` после правки: вбок 0.00-0.12 м при 4.1-24.2 м вперёд, 0° к
  фасаду вместо 37°. Очередь уходит вперёд дальше, но вперёд она и должна
  уходить: раньше четвёртый заказ стоял в 18.4 м впереди И в 14 м вбок.
- **СТЕНДЫ, МЕРИВШИЕ ТОЛЬКО БОКОВОЙ РАЗНОС, ПЕРЕПИСАНЫ НА СВОЙСТВО.**
  `qa_keep` E2/E2б и `qa_spawnlane` B1 сравнивали ОДНУ составляющую отступа —
  ту, которой теперь нет по замыслу. Защищаемое свойство при этом прежнее:
  два заказа подряд собираются в РАЗНЫХ местах, и меряется оно расстоянием
  между точками, а не его проекцией.

### Интерфейс: пять правок по заказу владельца

- **Панель рабочего вернулась к стандартной высоте** (`WORKER_PANEL_H_BOOST`
  2.0 → 1.0): было вдвое выше содержимого по прежнему заказу «просторно»,
  стало 71 px по содержимому. Укрупнение ИКОНОК построек не тронуто — оно про
  читаемость мелких картинок зданий, а не про рост панели.
- **Из карточки постройки убраны «Артель» и «Габарит»**, из карточки
  копейщика — «Дальность» и «Удар раз в N с». Все четыре — техническая
  отладка: таблица коэффициентов, размер, который игрок и так видит призраком
  при размещении, и длина копья, которая ничего не сообщает.
- **ЦЕНА — ИКОНКАМИ РЕСУРСОВ** (`HUD._build_cost_row`). Было «50 л + 20 з»:
  однобуквенные суффиксы надо расшифровывать. Иконки берутся из `RES_DEFS` —
  ТОГО ЖЕ места, откуда их берёт верхняя полоса ресурсов, и через тот же
  `_trimmed_icon`: заведи здесь свой список путей, и значок дерева в цене
  однажды разойдётся со значком дерева в полосе. Число красное, когда ресурса
  не хватает.
- **ИНДИКАТОР ДОСТУПНОСТИ НА КНОПКЕ НАЙМА** (`HUD._afford_color`). Было
  `darkened(0.45)` — на тёмной панели это читается как «иконка потусклее».
  Базовый цвет теперь ПОДМЕШИВАЕТСЯ к зелёному или красному; именно
  подмешивается, а не заменяется — цвет рода войск это способ узнать кнопку
  с одного взгляда.
- **ЗНАЧКИ ИЗ ТАБЛИЦЫ СТАТОВ УБРАНЫ** (разворот правки авг. 2026). Их в наборе
  четыре, а строк в таблице до девяти: у скорости, напора и морали значка нет
  вовсе, и ряд получался рваным — часть строк с картинкой, часть без, подписи
  из-за этого не выстраивались в колонку. `STAT_ICONS` остались: их читают
  карточки наград, там значок один на карточку. Порядок строк — Health,
  Attack, Defense, Armor, Push, Morale; скорость и интервал атаки уехали под
  них, но НЕ УБРАНЫ — предпросмотр награды дописывает прибавку в строку по
  ключу стата, и без строки модификатор снова стал бы исчезать.

### Звук марша: два слоя, питч бега, стык петли

- **ГОЛОСОВ СНОВА ДВА, И ЭТО НЕ ВОЗВРАТ К ШЕСТИ.** Шесть голосов были КОПИЯМИ
  ОДНОГО ФАЙЛА: копии складываются КОГЕРЕНТНО, и чтобы шина не клиппила,
  каждому отряду раздавались свои питч и фаза — та самая обработка, которую
  владелец услышал как «поломанный звук». Теперь голоса несут РАЗНЫЕ ФАЙЛЫ
  (шаг и бег), их сумма некогерентна по построению, и разводить их нечем и
  незачем: ни питча по отрядам, ни сдвига старта не вернулось.
- **ГОЛОС ВЫДАЁТСЯ БЛИЖАЙШЕМУ ОТРЯДУ В СВОЁМ СОСТОЯНИИ**, а не двум ближайшим
  вообще: иначе два соседних шагающих отряда займут оба голоса, и бегущий
  мимо не зазвучит вовсе — то есть слоя «бег» не будет ровно тогда, когда он
  информативнее всего.
- **ГРОМКОСТИ ПЕРЕСЧИТАНЫ ПОД ДВА ГОЛОСА, А НЕ ПОДОБРАНЫ НА СЛУХ:** потолок на
  голос = `MARCH_BUDGET / sqrt(MARCH_VOICES)` = 0.354 (−9.03 дБ), отсюда
  `MARCH_WALK_DB` −11.0 и `MARCH_RUN_DB` −9.1. Бег остался громче шага, но
  обоим пришлось подвинуться вниз — это цена второго слоя.
- **ПИТЧ ЕСТЬ ТОЛЬКО У БЕГА** (`MARCH_RUN_PITCH` = 1.12) и только потому, что
  об этом попросили: у бега свой сэмпл, и он уже несёт свою поступь. Питч
  тянет не только темп, но и ВЫСОТУ ТОНА — растяжения времени без смены
  высоты у `AudioStreamPlayer` нет вовсе, — поэтому число скромное. Хочется
  сильнее: это уже другой сэмпл, а не эта ручка.
- **ЩЕЛЧОК НА СТЫКЕ ПЕТЛИ — ЭТО НАБИВКА MP3, А НЕ ОБРЫВ ВОЛНЫ.** Кодировщик
  дописывает в начало файла несколько миллисекунд мусора, и точка возврата
  петли попадает ровно в него. Микро-фейдер поверх ОДНОГО
  `AudioStreamPlayer` сделать нечем — он не даёт ни доступа к буферу, ни
  события «сейчас будет петля», а настоящий кроссфейд потребовал бы ВТОРОГО
  голоса на каждый луп, то есть удвоения бюджета громкости ради восьми
  миллисекунд. Движковая ручка `loop_offset` решает ровно эту задачу:
  `AudioManager.LOOP_TRIM_SEC` = 0.008, и ставится она только маршевым лупам.
## Что делает со строем САМО СТОЛКНОВЕНИЕ — зонд `qa_clash` (31.08.2026)

**`qa_clash/Test.tscn` (6 проверок, headless)** — 20 копейщиков на 20, оба
отряда прямоугольником 5×4 с настоящим строевым интервалом, между фронтами
десять метров. Заведён после того, как замер `qa_mega_battle` B7 показал, что
НА МАРШЕ строй не разваливается вовсе (сдвиг формы 0.70 м и с переносом стены,
и без него): значит, терять вид отряд может только В КОНТАКТЕ, и мерить надо
контакт.

**ЗАМЕР (снимки в одни и те же моменты — до приказа, в миг первого касания,
через десять секунд рубки):**

| | ширина фронта | глубина | средний интервал |
|---|---|---|---|
| до приказа | 2.20 м | 3.81 м | 0.57 м |
| первый контакт (1.08 с) | 2.20 м | 3.80 м | 0.48 м |
| через 10 с боя | 3.09 м | **1.16 м** | **0.35 м** |

- **ФРОНТ НЕ СХЛОПЫВАЕТСЯ — ОН РАСШИРЯЕТСЯ** (2.20 → 3.09 м). Жалоба «отряд
  рассыпается в толпу» на этом сценарии НЕ воспроизводится как схлопывание
  линии: линия становится ШИРЕ и ПЛОТНЕЕ, а не уже.
- **СПЛЮЩИВАЕТСЯ ОТРЯД В ГЛУБИНУ: 3.81 → 1.16 м, то есть до 30 %.** Четыре
  шеренги превращаются в полторы.
- **ЭТО НЕ РАСТАЛКИВАНИЕ И НЕ ТОЛЧОК.** `Unit._apply_push` двигает ТОЛЬКО
  ЧУЖИХ — проверка C1 ставит двух своих вплотную и меряет сдвиг от толчка:
  0.0000 м. Расталкивание монотонно и только наружу, сжимать оно не умеет по
  построению. Уплотняет строй **подтягивание задних рядов**
  (`Unit._should_pull_up`): боец, у которого цель дальше оружия, делает шаг
  вперёд, и в контакте это делают все задние сразу.
- **ЗАПРЕЩАТЬ ПОДТЯГИВАНИЕ НЕЛЬЗЯ — ЭТО ЗАКАЗ ВЛАДЕЛЬЦА.** Оно заведено против
  жалобы «в бой вступает только та часть отряда, что соприкоснулась с врагом».
  Замер показывает сделку целиком: при глубине 30 % от исходной в бою **100 %
  отряда** (пик 20 из 20). Поэтому проверка A4 сформулирована как СДЕЛКА —
  строй вправе уплотняться ровно настолько, насколько это вводит людей в бой;
  «сплющился, а дерутся единицы» покраснеет, «сплющился, и дерутся все» нет.
- **НАСТОЯЩИЙ ГАРАНТИРОВАННЫЙ ИНТЕРВАЛ В ИГРЕ — 0.333 м, А НЕ 0.417.**
  Расталкивание включается не на `SEP_MIN_DIST` (0.4167), а на
  `SEP_MIN_DIST − SEP_DEADZONE` (0.4167 − 0.0833). Мёртвая зона заведена
  намеренно — без неё строй дрожал бы, вечно поправляя сантиметры. Замеренные
  в контакте 0.35 м это ровно её граница, то есть расталкивание отработало как
  настроено. **Проверка, поставившая порогом 0.4167, краснеет на исправном
  коде** — на этом уже споткнулась первая версия A2.
- **ФИЗТЕЛ И `PhysicsServer` В ЭТОМ НЕ УЧАСТВУЮТ ВООБЩЕ.** `collision_mask`
  везде 0, `PHYSICS_3D_ACTIVE_OBJECTS` читает 0; искать «боковые импульсы
  коллизий» в первой линии негде — их не существует.

## Чистка красных стендов (31.08.2026): все три краснели на исправном коде

- **`qa_spawnlane` D1 «ворота прямо перед фасадом» — мерил не от того центра.**
  Ворота НАМЕРЕННО сдвинуты на `_draw_cx`, чтобы совпасть с серединой РИСУНКА
  (та же поправка, по которой кладётся кольцо на земле, `ring_center`). У замка
  это −0.01 м, и «худший снос» выходил ровно 0.0100 при требовании < 0.01 —
  ножевой красный на правильном коде. Отсчёт переведён на `ring_center()`:
  снос 0.0000, порог остался жёстким.
- **`qa_spear` A10 «пятно отряда близко к квадрату» — мигал ЗАМЕР, а не код.**
  Проверка ждала построения `wait_ms(600)`, то есть по СТЕННЫМ ЧАСАМ (правило
  12). На свободной машине отряд успевает построиться (1.29-1.36), в большом
  прогоне шлюза, когда рядом работают другие стенды, — нет, и замер заставал
  его на полпути: ровно 2.20 при пороге 2.2. Теперь ждёт СВОЙСТВО (пока строй
  встанет, `moved_recently()` у всех погас) с потолком в физкадрах.
- **`qa_ai` 5 «гарнизон = HOME_GUARD_PER_TYPE на тип» — требовал невозможного.**
  Гарнизон просит по 7 отрядов каждого типа, а `SQUAD_LIMIT` даёт копейщикам
  10, мечникам 6, лучникам 5. Семи отрядов лучников не существует в природе;
  замеренные 6 и 5 — ровно потолок, то есть ИИ делает максимум возможного.
  Ожидание выведено как `min(HOME_GUARD_PER_TYPE, squad_limit(type))`.
- **`qa_rally2` A1 «сквозь живую шеренгу не прошёл никто» — стена была РЕДКОЙ.**
  Стенд строил её с шагом 1.2 м, а личный круг между бойцами РАЗНЫХ отрядов
  равен `SEP_MIN_DIST × SEP_CROSS_SQUAD` = 0.4167 × 1.6 = 0.667 м: середина
  просвета отстоит от обоих соседей на 0.6 м, и двадцать напирающих такую щель
  находят. Настоящая шеренга в игре стоит по `squad_spacing` (0.55 м), то есть
  ВДВОЕ плотнее. Шаг взят у игры (правило 10).
- **`qa_rally2` F2 «отряд доходит до зажатой точки» — ОСТАЁТСЯ КРАСНЫМ, НО
  РАЗОБРАН НАПОЛОВИНУ.** Красный ПРЕ-СУЩЕСТВУЮЩИЙ: A/B по правке полос выхода
  дал 24.44 м и 24.38 м на обеих сторонах, то есть к ней отношения не имеет.
  **ЧТО УСТАНОВЛЕНО ЗОНДОМ (печатается самим стендом):** приказ ИСПРАВЕН —
  `has_rally = true`, точка сбора зажата верно (x = 128.5 = предел карты), и
  цель у КАЖДОГО бойца стоит ровно в ней. Отряд получает верный приказ и
  проходит около полутора метров из двадцати восьми.
  **ЧТО ИСКЛЮЧЕНО:** вода на пути. Гипотеза выглядела главной — барак стоял на
  жёстком (100, −45), между ним и краем карты двадцать восемь метров СЛУЧАЙНО
  сгенерированной карты, а шаг в воду не проходит и поиска пути в игре нет.
  Стенд теперь ищет полосу без воды до края карты и ставит барак на неё
  (правило «сценарий, зависящий от окружения, обязан его задавать»); с сухим
  коридором z = −60 результат тот же — 26.97 м из 28.5.
  **ЧТО ПРОВЕРЯТЬ ДАЛЬШЕ:** точка сбора здесь лежит РОВНО НА ПРЕДЕЛЕ КАРТЫ
  (x = 128.5 = `map_lim_x`), а слоты строя считаются ОТ неё и уезжают за него
  (в печати зонда цели видны как x = 129). Пакетный шаг зажимает координату в
  ±`map_lim_x`, то есть до такой цели не дойти по построению. Проверять надо
  это: снять зажим слотов или мерить приход с допуском на зажим. Ни одна
  другая проверка блока F так не стоит — F7/F8 с обычными точками сбора
  зелёные (ближний 0.18 и 0.16 м).
- **ЛОВУШКА, ПОЙМАВШАЯ МЕНЯ ДВАЖДЫ ЗА ОДИН ЗАХОД: `squad_spacing` — ПОЛЕ
  ЭКЗЕМПЛЯРА, А НЕ КОНСТАНТА.** Обращение `Building.squad_spacing` бросает
  исключение, а исключение внутри корутины стенда убивает её МОЛЧА: сцена
  живёт, вывод буферизован, прогон висит минутами без единого признака. Брать
  число надо у экземпляра (`Building.new().squad_spacing`, как в `qa_crowd`).
## Статус проекта на конец захода (31.08.2026)

**Заход 10.09.2026, двенадцатая часть — девять скриншотов: старт, туман,
башня, тролль, трава, овцы, меню:** партия открывается пятью рабочими без
крепости (её ставит рабочий там, где нет тумана), старт сдвинут от угла на
20 м, зелёные плашка зоны и рамка края карты сняты, на старте видно только
круг вокруг бригады, крепость даёт +10/+5 к лимитам и обзор 62 м; башня
прочная (6000) и дорогая, тридцать лучников внутри и десять на настиле,
стрелы бьют лучников (труп у подножия, резервист встаёт), пехота — только
стены; у тролля нет тени, кольцо овальное, стрелок целится в тушу, стрелы
торчат в спрайте, окружение пробивается и расталкивается, туша падает под
углом; боец рисуется поверх пепелища; трава чисто зелёная и вдвое мельче;
овца несётся целиком, десять ударов на кусок и пять ходок, размножается сама;
войска ИИ после боя рассеиваются; меню — заголовок и четыре кнопки, сложность
и отображение в опциях. Новый стенд `qa_start_build` 30/30; `qa_visual_smoke`
19/19, `qa_troll` 24/24, `qa_sheep` 17/17, `qa_map` 34/34, `qa_bugpass`
зелёные. Разбор — раздел «Двенадцатая часть 10.09.2026».

**Заход 10.09.2026, одиннадцатая часть — тактический разум гоблинов:**
командир штурма (`GoblinAttackCommander`) в фазе охоты: сбор на 30 м от края
базы, паттерны «провокатор» / «откатные волны» / «веер», отход всей армией
при запасе ниже 50 % в лагерь вне башен, лечение у костров, новая волна
другим паттерном; по уточнению владельца те же тактики распространены на
марш в центр (конный дозор впереди тела, штурм точки контакта) и оборону
деревни (рубеж у костров, фланговые уколы конницы, раненые у костров).
Стенд `qa_goblin_tactics` 25/25 (новый); `qa_goblin` 27/27, `qa_troll`
24/24. Разбор — раздел «Одиннадцатая часть 10.09.2026». Приёмка
владельца: спринты 10 и 11 приняты, сводка шлюзов #9/#10 — в
`docs/ENGINEERING_LOG.md`.
**Полный шлюз десятой части (60 сцен):** известные красные `qa_mega_battle`
B1-1, `qa_rally2` F2, `qa_ui9` 3, `qa_wall` A1; под нагрузкой краснели и
одиночно зелёные `qa_squad` 3 (в рамку попал 61-й боец — пробегавший
рабочий), `qa_mega_battle` B2-1 (один рыцарь сквозь щель 1.10 м);
`qa_volley` меняет красную проверку от прогона к прогону (E1 «пик 5 из 10
в кадре», D2 «совпавших пар 1 из 12») — разброс залпа случаен, стенд
мигает на замере; `qa_cavalry` блок C ронял `SCRIPT ERROR` (стенд: кабан с
тиком бил сам, контратака отряда стирала разметку — кабан заморожен).

**Заход 10.09.2026, десятая часть — шаги, живые иконки, реплики, агро троллей:**
марш −3.1 дБ; кнопки найма/стройки и узлы кузницы перекрашиваются по
сигналу склада (реестр `_afford_watch`, красная рамка узла), цена в плашке
узла иконками; «держать строй» через раз, окна реплик 9 с; тролль с целью
×1.2, первый удар по стражу зовёт двух помощников, выбитая группа — бонусную
пару со стандартной подмогой. Новые стенды `qa_troll_aggro` 15/15,
`qa_hud_afford` 13/13.

**Заход 10.09.2026, девятая часть — строитель, еда, кража овец, кузница:**
молоток только у стройки; содержание армии едой (10 домов = 30 рабочих +
15 отрядов, голод тает мораль до 40 %, «−N» в панели); рабочий крадёт овцу,
несёт над головой, режет ножом у склада — 60 мяса за тушу, задел под загон;
кузница строится вдвое дольше, ключ `bonus_build`, узлы D-колонки рабочего
стоят еду; лист камня чёрного рабочего пересобран. Новые стенды
`qa_worker_sheep` 15/15, `qa_food_economy` 16/16; `qa_ai` 27/27,
`qa_forge` 47/47, `qa_helpbuild` 24/24, `qa_balance` 21/21.
**Полный шлюз восьмой части (55 сцен):** известные красные `qa_mega_battle`
B1-1, `qa_rally2` F2, `qa_ui9`; `qa_balance` C7 и `qa_troll` B5/B6 краснели
только под нагрузкой шлюза (одиночно 21/21 и 24/24).

**Заход 10.09.2026, восьмая часть — скриншоты, тролли, поражение, золотой
рудник, рельеф, овцы:** стартовый запас 20 000 × 4 зафиксирован владельцем;
площадка сбора 18 м вплотную к воротам без рамки; тролль без пятен крови,
−30 % запаса, +20 % темп, подмога −30 %, клик по всему спрайту, кольцо ×4.2,
тело билбордом с доигрыванием ленты и падением прочь от удара; орда бьёт
постройки; поражение по двум правилам; золотой рудник (нейтральная сторона,
захват, флажок, рабочие внутри, перекраска при смене владельца, общая руина);
плато со спусками и река в низине в обоих языках, оттенки травы;
овцы у логова и голод тролля. Новые стенды `qa_gold_mine` 24/24, `qa_sheep`
17/17, `qa_map` 34/34, `qa_troll_impact` 28/28; `qa_visual_smoke` 19/19.
Разбор — раздел «Восьмая часть 10.09.2026» и `docs/ENGINEERING_LOG.md`.

**Заход 09.09.2026, седьмая часть — хаки №2 и №3:** хак №2 (стена-отрезок
вместо проверки тел) не построен — измерен потолок: выключение проверки тел
целиком даёт +0.09 мс тика в рубке (шум) и −0.16 на сближении; тик держат
решения боя в GDScript. Хак №3 сделан: бойцы появляются на площадке сбора
сразу на своих местах (`SPAWN_ON_ZONE`), рамка площадки у выделенного
здания, `qa_rally_zone` 12/12 (новый), `qa_spawnlane`/`qa_squad`
переписаны на новое свойство. Известный красный `qa_rally2` F2 на новой
карте — 123 м до зажатой точки при терпении стенда на 28 м.
Разбор — `docs/ENGINEERING_LOG.md`, «09.09.2026, седьмая часть».

**Заход 09.09.2026, шестая часть — событийная поза и декларативные стрелы:**
поза по событию (E3, `pose_events`; −0.07 мс на марше, выигрыш — в
отсутствии запаздывания такта), полёт стрел в ядре (`arrow_core`: 1.5 мс
GDScript-узлов → 0.4 мс C#-прохода, попадание тем же `EnemyAt`). Партия на
4000: экономика 53 FPS, рубка 33-45, физтик 11-13 мс — потолок прежний.
Шлюз 25 сцен: красные только известные (`qa_wall` A1, `qa_mega_battle`
B1-1; `qa_spear` C9 краснел под нагрузкой шлюза, отдельно 49/49 дважды).
Хаки №2 (отряд как стена-отрезок в C#) и №3 (площадка сбора) — не начаты.
Разбор — `docs/ENGINEERING_LOG.md`, «09.09.2026, шестая часть».

**Заход 09.09.2026, пятая часть — визуальный проход армии в ядре (этап E):**
кадр ленты листает `BatchVisual` (E1), кольца/тени/полоски ведёт ядро (E2),
стоящий с зацикленным покоем спит по картинке. `qa_visab` (новый A/B):
decal_core 79.2 → 7.8 мс кадра логики при 2000 выделенных; anim_core −0.2 мс
на марше. Окно: `qa_fps` 2000 на 2000 (окно 1920×1080; стенд не посеян, сравнивать только
сцены «выделена вся армия» и «марш» внутри одного прогона с базой того же
прогона): база стоя 66 FPS, **вся армия выделена 54-62 FPS в двух прогонах (15.1 → 18.5 и
14.5 → 16.1 мс; прежде 13.8 → 28.3 мс, то есть 35 FPS)**, та же армия на
марше 39-52 FPS (прежде 30); расхождение кольца со спрайтом 0.000 м —
обе точки пишет один проход. `qa_full_game_4k` (seed 11): экономика 46.7 FPS, кадр логики
**5.8 → 4.5 мс** на замороженной рубке; FPS рубки 33-39 — прежние: кадр
держит физтик 11-12 мс × 1.7 тика, визуальный проход в нём — не главная
статья. Метрика «метка под ногами» в `qa_fps` переведена на чтение буферов
ядра (`rb_slot` кольца и бакета): GDScript-словарь `_last_pos` у ведомых
ядром колец больше не ведётся, и старая метрика показывала 2.96 м на
исправной картинке.
 Шлюз стендов картинки (23 сцены) — 0
провалов. Стартовый запас возвращён к штатному 550/380/340/200
(`qa_balance` 21/21). Разбор — `docs/ENGINEERING_LOG.md`, «09.09.2026,
пятая часть».

**Заход 09.09.2026, четвёртая часть — тролль-таран:** скорость +20 %,
патруль +30 %, толчок 6.6 (конница × 1.2), топтание трёх по ходу при
ударе с разгона сквозь стенку копий, дубина дугой ±60° на шесть ближайших с
отлётом и сбиванием с ног на 2.5 с (лежат телом из слоя тел, встают и
дерутся), вспышка тролля 0.22 и пятна крови в шейдере; башня стреляет на
штатную дальность лучника, а не на обзор; портрет монаха по цвету стороны.
Стенды `qa_troll_impact` 17/17 (новый), `qa_tower` 47/47,
`qa_visual_smoke` 19/19. Разбор — `docs/ENGINEERING_LOG.md`, «09.09.2026,
четвёртая часть».

**Заход 09.09.2026, третья часть — аудит кадра после новой карты:**
жалоба «просел FPS, фризы» на стоячей сцене НЕ воспроизвелась (`qa_fps`
на HEAD 61.8 против 72.4 FPS на новой карте; но `qa_fps` не посеян и
между прогонами гуляет 63-137 FPS — для A/B годится только
`qa_full_game_4k` с seed 11). Настоящая партия на 4000: экономика 48 FPS,
рубка 32-38, физтик 11-12 мс × 1.7 тика на кадр, худший кадр 34-46 мс —
это кадры с двумя тиками, структурно. Снято: пересчёт тумана 4.59 → 1.56 мс
(источники в ядре, зонд `qa_fogperf`), −90 вызовов отрисовки (пена одним
мешем, камни MultiMesh), гарнизон снят из сетки ядра (был обстреливаемым
невидимкой), урон по укрытым идёт в здание, гарнизон башни стреляет с
площадки (35 м). **60-70 FPS в рубке на 4000 этим не берутся** — потолок
описан в `docs/PLAN_D_70FPS.md` (тик рубки и визуальный проход), туман и
земля стоят меньше миллисекунды. `qa_visual_smoke` прячет камни брода:
анимированный камень под квадом копейщика засчитывался ему в силуэт.
Разбор — `docs/ENGINEERING_LOG.md`, «09.09.2026, третья часть».

**Заход 09.09.2026, вторая часть:** босс-тролль с логовом (одышка, таран,
подмога на половине, 180 убийств за смерть), месть гоблинов, карта ×3 по
площади с рекой, бродом и горой на тайлсете, монах-целитель. Стенды
`qa_troll` 24/24, `qa_map` 24/24, `qa_monk` 13/13.

**Заход 09.09.2026 (сверху):** аудио-окно камеры (марш слышен в 15/40 м
от фокуса, гаснет зумом, темп шага +8 %, бег +10 % и без тишины в начале
файла), меню рабочего со всеми зданиями цветовой папки, башня с гарнизоном
лучников и часовым, стрелковая (лучники из бараков переехали), три дома и
лимит населения. Стенд `qa_tower` 41/41. Разбор — в двух разделах выше.

**Заход 03.09.2026:** стенд настоящей партии `qa_full_game_4k`
(экономика + рубка с потерями + цена подсистем на 4000), стрелы больше не
висят в воздухе (слот MultiMesh не переносился в точку втыкания), билет
отхода вернулся как билет НА ВЫХОД, билеты сужают тело до ядра 0.30 м
(`PassCoreFrac` 0.55 в обеих дорогах; A/B 148 → 9 тел внутри ядра), под
билетом — скольжение вдоль тела с четвертью шага наружу и правило «прочь
можно», ворота упора больше не исключают окно прохода. Цена ядра —
`qa_disengage` D2 (из кольца с просветом 0.29 м не выйти; промежуточного
ядра, разделяющего кольцо и шеренгу, не существует) — **вопрос владельцу**.
Разбор — `docs/ENGINEERING_LOG.md`, «03.09.2026».

**Что закрыто в этом заходе** (заказы владельца по скриншотам, четыре партии
подряд): ворота и гарнизон крепости, клик по зданию сквозь чужие спрайты,
раздача целей по соседним домам, белый флаг паники (два сценария, один узел),
чистка звука марша и клич только для боевых отрядов, таргетинг от знаменосца,
абсолютный приоритет ПКМ, мега-стенд на 1500 против 1500, положение лычки,
подпись «Интервал атаки», три карточки награды из одной тройки статов, пять
ступеней окровавленности, сортировка камней по земле, зеркало спрайта по
фактическому ходу и таблица статов в три колонки.

**Заход 31.08.2026, две части.** Первая — три открытых хвоста: перенос стены
строя на приказ АТАКИ доведён до конца, измерен A/B и СНЯТ (весь выигрыш
формации давала остановка боя: схватка 60 на 60 ломалась на 148-й секунде
вместо 32-й, а замок цели переставал работать вовсе); `qa_melee` F2 разобран
до конца и признан неразрешимым признаками бойца; `qa_wall` A1 перемерен A/B и
остался тем же мигающим красным.

Вторая часть — пятая партия правок по заказу: детектор зацикливания подключён
к «рабочим» состояниям (его там не было вовсе), полосы выхода из зданий
разведены ВГЛУБЬ вместо диагонали, пять правок интерфейса и три правки звука
марша. Разбор — в разделе «Пятая партия правок».
**Что откачено осознанно и с замерами** (подробности — в своих разделах,
начинать заново с нуля не надо):
- `AI_NO_RETREAT_RANGE` — правило написано и стоит в 0.0: единственный отход в
  этом ИИ это «разбитый отряд уходит домой», и запрет убивал бы его;
- слой «звон доспехов» первой версии — разбор в разделе «Реплики команд».

**Найдены и проверены A/B в заходе 31.08.2026 (красные на ОБЕИХ сторонах,
то есть не регрессии):** `qa_rally2` A1 «сквозь живую шеренгу не прошёл
никто» (11-14 прошло и с правкой полос, и без) и F2 «отряд доходит до зажатой
точки» (24.4 м с обеих сторон); `qa_spawnlane` D1 «ворота прямо перед
фасадом» — ножевой порог, худший снос ровно 0.0100 м при требовании < 0.01
(это боковая поправка рисунка замка `_draw_cx`, а не смещение ворот).
**Известные красные на 09.09.2026 (полный шлюз, 47 сцен):** `qa_mega_battle`
B1-1 (ножевой порог, мигает), `qa_wall` A1 (см. ниже), `qa_balance` E2 —
стартовый запас в конфиге владельца отладочный (22550/22380), `qa_ui9` 3 —
нижняя панель 748 px из 1280 после строки лимита населения (проверено с
обходом правки аватаров: то же число). Стенд `qa_troll_impact` замирает
тролля на время проверки подъёма и ждёт взмах на двоих — иначе мигал на
замере, не на коде.
**Известные красные, не регрессии:** `qa_ai` проверка 5 (гарнизон 18 против 21)
краснеет и на чистом дереве; **`qa_melee` F2** — приказ на марш выводит отряд
из боя (F3 зелёная, отряд уходит), но по дороге его снова ловит признак упора.
**РАЗДЕЛИТЕЛЬ ИСКАЛИ ЧЕТЫРЬМЯ СПОСОБАМИ, ВСЕ ЧЕТЫРЕ СНЯТЫ ЗАМЕРОМ** (мера
продвижения к точке, рост дистанции до конкретного держащего, рост дистанции до
любого ближайшего, угол на помеху) — и это не перебор наугад, а доказательство,
что **разделителя в свойствах бойца не существует**: прижатый СВОИМИ ЖЕ задними
рядами физически неотличим от упёршегося. Спорят не признаки, а СРОКИ — замок
приказа живёт три секунды, а марш на тридцать метров идёт десять; **вопрос к
владельцу, а не к коду: должен ли приказ на движение переживать контакт до
исполнения.** Пятой попытки по признакам быть не должно. Разбор по секундам и
все числа — в `docs/ENGINEERING_LOG.md`; **`qa_wall` A1** — 1-3 просочившихся из 48 в
четырёх прогонах из шести. У A1 в этом заходе починен САМ ЗАМЕР (он мерил
целость по всему фронту и судил о месте прорыва по нынешней точке беглеца,
отчего давал разброс 0-18 на неизменном коде); остаток разобран, зондами
исключены оба билета прохода, и упирается он в пакетный шаг: щель между
соседями по шеренге около 0.1 м. Полный разбор и следующий шаг — в
`docs/ENGINEERING_LOG.md`, раздел «Известные красные».

**Чем мерить дальше.** Стерильные стенды из десяти бойцов больше не считаются
доказательством боевой правки: обязательны `qa_wall` (стыки строя),
`qa_mass_siege` (700 на 700 с базой) и `qa_mega_battle` (1500 на 1500 с
приказом ПКМ по лучникам). Производительность — только `qa_ab` чередованием по
`tick_meter`, никогда по долям профиля.

## Спринт 13 (10.09.2026): декорации, гноллы, загон для овец, спецприёмы

Семь блоков заказа владельца. Стенд — **`qa_sprint13`** (80 проверок, headless:
декор, гноллы, загон, разделка, четвёртый слот кузницы, спецприёмы, ребаланс
мечника, монах). Ниже только то, что нельзя вывести из кода.

### Декорации Deco 01-18 сажаются ОБЩЕЙ отрисовкой, а не узлами

- **КАЖДЫЙ ФАЙЛ — ОДИН БАКЕТ `VegetationRenderer`** (`Main._scatter_deco`).
  Бакет заводится ПО ТЕКСТУРЕ, поэтому 759 декораций на карте стоят столько же
  вызовов отрисовки, сколько РАЗНЫХ картинок, а туман войны и подсветка
  достаются даром. Узел на штуку дал бы под тысячу вызовов — ровно та ошибка,
  из-за которой однажды пришлось переносить в MultiMesh пену брода и камни.
  Замер стендом: 26 бакетов на 5312 экземпляров (лес, кусты и декор вместе).
- **КАЧАТЬСЯ ИМ НЕЧЕМ ПО ПОСТРОЕНИЮ.** «Ветер» у растительности — это
  ЛИСТАНИЕ ЛЕНТЫ (`shaders/veg_multimesh`, ветка `frame_count > 1`), а не
  наклон геометрии; у всех восемнадцати Deco по одному кадру. Отдельного
  «статичного» материала под камни и кости заводить не нужно.
- **ДОРОГ НА КАРТЕ НЕТ, НО ЕСТЬ ГЛАВНЫЙ МАРШРУТ.** Указатели (16-17) ставятся
  вдоль прямой «база игрока → база ИИ» с чередованием стороны, пугала (18) —
  по периметру КРУПНЫХ зарезервированных зон (`DECO_CROW_MIN_ZONE` 18 м):
  в `_reserved` лежат ещё и пятачки под кучи руды радиусом в пару метров, и
  пугало у каждого камня читалось бы мусором, а не краем поля.
- **`WORLD_GEN_VERSION` ПОДНЯТА ДО 5.** Расстановка леса и руды не менялась,
  но декорации берут числа из ТОГО ЖЕ генератора случайных: поток сдвинулся, и
  одно зерно даёт другой мир. Та же причина, что у версий 2, 3 и 4.

### Гноллы: метатели у пня

- **ГНОЛЛ — ОБЫЧНЫЙ `Unit` С ЧЕТЫРЬМЯ ПЕРЕОПРЕДЕЛЕНИЯМИ**
  (`scripts/goblin/Gnoll.gd`; числа — `STATS["gnoll"]`, поведение —
  `goblin_config.GNOLL_*`). Смысл рода войск целиком в трёх числах: запас 55
  против 165 у гоблина-копейщика, урон 4 против 10, скорость 3.6 против 2.2 —
  он не убивает, он изматывает и гибнет от первого дошедшего копейщика.
- **КОСТЬ — ВТОРОЙ ЭКЗЕМПЛЯР СЛОЯ СТРЕЛ, А НЕ ВТОРАЯ МЕХАНИКА**
  (`GameManager.bones_mm`, `Arrow.bone`). `ArrowRenderer` держит ОДНУ текстуру
  на весь свой MultiMesh — материал общий на бакет, — поэтому подмешать кость
  в слой стрел нельзя ни модуляцией, ни подменой картинки: у первой же стрелы
  изменился бы вид. Второй экземпляр слоя стоит один дополнительный вызов
  отрисовки на все кости в воздухе и достаёт из ядра ТОТ ЖЕ пакетный проход
  полёта: у него свой `rb_create`, а `arrow_launch` принимает номер буфера
  аргументом. **Проход ядра при этом ОДИН на оба слоя** — полёты лежат в общем
  реестре, и второй `batch_arrows` посчитал бы кадр полёта дважды.
- **У КОСТИ СВОЙ ПУЛ** (`GameManager._bone_pool`), и это не придирка: слот
  общего MultiMesh узел берёт ОДИН РАЗ, в `_build_visual`, и держит его всю
  жизнь. Выдай стрелу из общего пула под кость — и кость нарисуется стрелой.
  Признак `bone` ставится ДО `_ready()`, там же, где создаётся узел.
- **ПРОМАХ — ЭТО ТОЧКА, А НЕ ВЕРОЯТНОСТЬ УРОНА** (`GNOLL_MISS_CHANCE` 0.3).
  Урон гнолла и так мал; «иногда промахивается» сделано ГЕОМЕТРИЕЙ — кость
  летит в землю рядом с целью и втыкается там. Так это видно на экране и не
  требует ни одной новой ветки в расчёте урона.
- **КАЙТ — ЭТО ПРИКАЗ, А НЕ СИЛА** (`Gnoll._tick_kite`). Подошла пехота ближе
  `GNOLL_KITE_IN` — гнолл получает обычный `command_move` прочь, не чаще раза
  в `GNOLL_KITE_SEC`: без окна он выдавал бы приказ каждый тик и не сделал бы
  ни одного броска. Стрелок кайтом не пугает (порог `GNOLL_MELEE_RANGE_MAX`):
  отбегать от лучника бессмысленно, он достанет и так. Точка отхода зажата
  кольцом вокруг пня — иначе стая по одному разошлась бы по всей карте.
- **ПОВОДОК ИНИЦИАТИВЫ СТАЛ СВОЙСТВОМ РОДА ВОЙСК** (`Unit.aggro_leash()`,
  у гнолла 7 м против общих 14). «Не агрятся всей толпой, когда бой далеко» —
  это и есть длина поводка, и другого способа выразить требование нет.
  Виртуал, а не второе поле: число читается один раз в ветке авто-агро, и
  только когда рядом кто-то нашёлся.
- **ФЛАНГ ПРИ ОБОРОНЕ ПНЯ** (`_tick_flank`): гнолл встаёт СБОКУ от линии
  «пень → враг», сторона крыла — от номера узла, а не случайно, иначе стая
  металась бы туда-сюда каждым тактом вместо двух крыльев.
- **ЧАСЫ ВОЛН ВЗВОДИТ УДАР, А НЕ НАЧАЛО ПАРТИИ** (`TrollLair.on_lair_attacked`).
  Заказ говорит «если по пню бьют», и бьют по нему в трёх местах: по самому
  пню (`take_damage`), по троллю (`on_guard_hit`) и по гноллу (`on_gnoll_hit`).
  Все три ведут в одну точку. Три отряда на старте, +3 через `GNOLL_WAVE_SEC`,
  ещё +3 через столько же, после третьей волны — откат `GNOLL_COOLDOWN_SEC`,
  пока пень жив.
- **ЧАСЫ — УЗЕЛ-ТАЙМЕР, А НЕ СВОЙ ТИК.** У логова нет `_process` вовсе
  (`_needs_tick` = false), и заводить его ради одного сложения раз в три
  минуты незачем — тот же выбор и та же причина, что у таймера стада овец.

### Загон для овец вместо золотого рудника

- **РУДНИК УБРАН ТОЛЬКО ИЗ МЕНЮ, МЕХАНИКА ЖИВА.** `worker_buildable` = false
  у записи `mine` — и кнопка исчезла сама, потому что меню рабочего целиком
  выводится из этого поля (`GameManager.worker_buildings`). Ничейные рудники
  на плато, захват пехотой, флажок и рабочие внутри (`Mine.gd`) не тронуты
  вовсе: их не заказывали убирать.
- **КАРТИНКА ЛЕЖИТ ПЛАШМЯ, А НЕ СТОИТ БИЛБОРДОМ.** `Wooden Fence_64x64
  tile.png` — это ГОТОВАЯ ОГРАДА СВЕРХУ: замкнутый прямоугольник 256×192 с
  проёмом снизу и впечатанной тенью. Общий загрузчик спрайта постройки
  (`Building._maybe_load_building_sprite`) ставит ВЕРТИКАЛЬНЫЙ квад, и вышла
  бы стена с нарисованным полом, сквозь которую видно небо. Поэтому у загона
  свой `_build_visual`, а общий загрузчик переопределён пустым.
- **МЕШ СЛЕДУЕТ РЕЛЬЕФУ, А НЕ ПОДНЯТ НАД НИМ.** Плоский квад в десять метров
  на волнистой земле (гармоники дают до ±0.85 м) наполовину уходит в грунт, а
  поднятый висит в воздухе. Сетка `MESH_CELLS_X × MESH_CELLS_Z`, высота каждой
  вершины — у `get_terrain_height`, сверху `MESH_LIFT` против z-fighting.
- **ВЫСОТЫ ПЕРЕСЧИТЫВАЮТСЯ ОТЛОЖЕННО.** В `_ready` точка узла ещё НУЛЕВАЯ:
  и стройплощадка, и загрузка партии ставят `global_position` уже ПОСЛЕ
  `add_child` (та же грабля, что у ворот замка и у ствола дерева). Собранный
  в `_ready` меш лёг бы по рельефу в начале координат карты.
- **ПЕРЕСБОРКА МЕША — ПРАВИЛО 6.** `remove_child` ПЕРЕД `queue_free`:
  освобождение отложено, старый узел до конца кадра остаётся ребёнком вместе
  со своим ИМЕНЕМ, и новый «PenFence» получал бы от движка суффикс. Стенд
  ищет ограду ПОЛЕМ (`_fence_mi`), а не именем — та же ловушка, что у знамён.
- **ПОТОЛОК СЧИТАЕТСЯ ПО ПРИВЯЗКЕ ОВЦЫ, А НЕ ПО КРУГУ НА КАРТЕ**
  (`Sheep.pen` / `Sheep.keep`). Круги двух соседних загонов пересекаются, и
  одна овца шла бы в зачёт обоим. Привязка задаёт три вещи разом: где пасётся
  (`graze_home` / `graze_radius`), с каким периодом плодится (`breed_sec`) и
  какой у стада потолок (`flock_limit`).
- **РАДИУС ВЫПАСА ШИРЕ САМОЙ ОГРАДЫ** (`SheepPen.ROAM_FACTOR` 1.35): заказ
  прямо просит, чтобы овца заходила и выходила. Ограда — не коллизия, физтел
  в проекте нет вовсе.
- **ЦЕННОСТЬ ЗАГОНА — В ДВУХ ЧИСЛАХ:** потолок 20 против 5 у зоны замка
  (`SHEEP_PEN_CAPACITY` / `CASTLE_GRAZE_LIMIT`) и удвоение раз в 150 с против
  300 (`SHEEP_BREED_PEN_SEC` / `SHEEP_BREED_CASTLE_SEC`). «Удвоение»
  получается само: период ЛИЧНЫЙ у каждой овцы, и каждая спокойная раз в
  период порождает одну.
- **ПОЛНЫЙ ЗАГОН В ВЫБОРЕ ЦЕЛИ НЕ УЧАСТВУЕТ ВОВСЕ** (`Worker._sheep_dest`
  спрашивает `has_room`): довести до него овцу и услышать «мест нет» значит
  потерять всю дорогу.

### Разделка: смерть и исчезновение — РАЗНЫЕ события

- **ПЯТЬ УДАРОВ НОЖОМ — И ОВЦА МЕРТВА** (`Worker.SHEEP_KILL_CUTS` = 5), а
  мясо выносится ходками ещё две минуты. Значит, туша обязана ЛЕЖАТЬ НА
  ЭКРАНЕ всё это время: `Sheep.kill_flip()` кладёт её на бок, `consume()`
  убирает, когда мясо кончилось. Прежний `butchered()` делал и то и другое
  одним движением и оставлен обёрткой над `consume`.
- **НА БОК КЛАДЁТСЯ УЗЕЛ, А НЕ ЛЕНТА.** Своего «мёртвого» арта у овцы нет, а
  билборд `cyl_billboard` разворачивает квад к камере сам и любой поворот
  съедает. Тот же приём, что у тела тролля: `world_fixed = 1` снимает билборд,
  после чего поворот вокруг Z доезжает до экрана. Сторона — от номера узла.
- **КРАСНОГО МИГАНИЯ БОЛЬШЕ НЕТ ВОВСЕ.** История ручки короткая и
  однонаправленная: (1.7, 0.45, 0.45) → мягкое (1.35, 0.85, 0.85) → снято.
  Причина та же, по которой его убавляли: ударов на тушу двадцать с лишним, и
  любая подкраска читается как ошибка отрисовки. Смысловая половина
  `wound_flash` осталась и она главная — удар СБРАСЫВАЕТ ПОКОЙ, а тревожная
  овца не плодится.
- **ЧИСЛА ЗАКАЗА СХОДЯТСЯ АРИФМЕТИЧЕСКИ:** 20 ходок × 15 еды = 300 с туши;
  по времени 5 + 20 × 10 ударов по 0.45 с = 92 с под ножом плюс дорога на
  двадцать ходок — те самые «2-3 минуты».

### Четвёртый слот кузницы и спецприёмы по двойному ПКМ

- **ПЛАТЫ ЗА ДОСТУП К СПОСОБНОСТИ БОЛЬШЕ НЕТ**
  (`forge_config.squad_unlock_cost` возвращает ноль). Заказ: «спец-бонус
  открывается сразу для всех юнитов соответствующего типа». Поля
  `squad_unlock_cost` в таблице оставлены историей — правка баланса ими больше
  ничего не включает. `GameManager.squad_has_ability` теперь отвечает `true`
  всякому отряду того же рода, у чьей фракции узел ИЗУЧЕН.
- **СПОСОБНОСТЬ-РЕЖИМ ВКЛЮЧЕНА ПО УМОЛЧАНИЮ** (`squad_ability_on` при
  отсутствии ключа отвечает `true`). Прямой заказ про залп лучников:
  «активна по умолчанию, можно отключить».
- **ДВОЙНОЙ ПКМ РАЗДАЁТ ПРИЁМ ПО ТИПУ ОТРЯДА, А НЕ ПО ИМЕНИ БОЙЦА**
  (`SelectionManager.DOUBLE_RMB_ABILITIES`, `_trigger_double_rmb_abilities`).
  Способности куплены на ОТРЯД, и спрашивать их надо один раз на отряд, а не
  по разу на каждого из шестидесяти копейщиков. Точка подключения —
  `_issue_formation_move`: туда сходятся ВСЕ ветки приказа на движение, там же
  живут клич и массовый марш. **Бег двойным ПКМ не отменён** — он остаётся у
  всех, у кого приёма нет вовсе или кто его выключил.
- **«ЯРОСТНЫЙ НАБЕГ» (`warrior_1d`) — СЕРИЯ ФИКСИРОВАННАЯ, А НЕ «КАЖДЫЙ
  ЧЕТВЁРТЫЙ»** (`Warrior.RAGE_STRONG` = [обычный, мощный, обычный, мощный,
  обычный]): заказ описывает её по шагам, и она обязана быть одинаковой каждый
  раз. Щит в набеге поднят БЕЗУСЛОВНО, и требование `auto_guard` тут не при
  чём — режим включил сам игрок. Множитель шага стоит ПОСЛЕ ветки щита:
  1.45 × 0.65 дало бы 0.94, то есть «рывок» медленнее марша.
- **ПОТОЛОК ПО ВРЕМЕНИ У ОБОИХ ПРИЁМОВ ОБЯЗАТЕЛЕН** (`RAGE_SEC`,
  `PHALANX_PUSH_SEC`): серия тратится только в бою, а до врага можно и не
  добежать — его убили, приказ сменили. Без потолка мечник ходил бы
  ускоренным под щитом до конца партии.
- **«НАТИСК ФАЛАНГИ» (`spearman_4d`) — ДВЕ ФАЗЫ, А НЕ ОДНА.** Пока
  `_push_left > 0` — НАТИСК: медленный ход и усиленный удар. Серия исчерпана —
  взводится ОТТЕСНЕНИЕ: удар обычный, зато напор выше и входящий урон ниже.
  В заказе они идут ОДНО ЗА ДРУГИМ, а не вместе.
- **СМЫКАНИЕ СТРОЯ ЗОВЁТ ТОТ, КТО ОТДАЁТ ПРИКАЗ, А НЕ БОЕЦ.** Оно отрядное
  (`GameManager.squad_close_ranks`); позвать его из бойца значило бы раздать
  `command_move` каждому по числу бойцов в отряде, то есть развалить сам строй.
- **ОТМЕТКА ИДУЩЕГО ПРИЁМА В ПАНЕЛИ — ГЛИФ В ПОДПИСИ** (`HUD._squad_ability_mark`),
  а не `TextureRect`: строка названия собирается одним `Label`, и разбирать её
  на контейнер ради одного значка незачем. Спрашивается СВОЙСТВО у самих
  бойцов (`rage_active` / `phalanx_push_active`) — второй источник правды о
  том, идёт ли приём, неминуемо разошёлся бы с первым.

### Экономика после спринта: где узкое место

| статья | число |
|---|---|
| дом | 2.0 еды / 8 с = **15 еды/мин** |
| десять домов | 2.5 еды/с = **150 еды/мин** |
| содержание рабочего | 1/30 еды/с = **2 еды/мин** |
| содержание отряда | 0.1 еды/с = **6 еды/мин** |
| туша овцы | **300 еды** за ~132 с работы одного рабочего = **136 еды/мин** |
| выпас у замка | 5 овец, удвоение 300 с |
| загон | 20 овец, удвоение 150 с, цена 180 дерева |

- **ТОЖДЕСТВО ДОМОВ НЕ ТРОНУТО:** 10 домов = 30 рабочих + 15 отрядов
  (2.5 = 30 × 1/30 + 15 × 0.1). Овцы в него не входили и не входят.
- **УЗКОЕ МЕСТО — РАБОЧИЙ, А НЕ СТАДО.** Прирост считается по СВОБОДНЫМ
  местам: полное стадо упирается в потолок и не плодится вовсе, а загон,
  который держат наполовину, отдаёт десяток овец за 150 с — около четырёх в
  минуту, то есть больше тысячи еды. Вынести их некому: один рабочий уносит
  136 еды/мин. Значит, приток еды от овец считается ЧИСЛОМ РАБОЧИХ НА
  РАЗДЕЛКЕ, а не размером стада, и загон нужен не ради потолка прироста, а
  ради того, чтобы стадо не кончалось. У зоны замка тот же счёт даёт одну
  овцу в минуту — вчетверо меньше, и это вся разница между ними.
- **ОДНА ТУША СТОИТ ДЕВЯТИ ДОМОВ НА ЭТУ МИНУТУ** (136 против 15 еды/мин), но
  требует рабочего, который в это время ничего не добывает. Это и есть выбор,
  который заказ создаёт: рабочий на разделке или рабочий на дереве.
