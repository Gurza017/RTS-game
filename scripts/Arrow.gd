extends Node3D
class_name Arrow

## ФИЗИЧЕСКИЙ СНАРЯД. Урон НЕ наносится в момент выстрела — стрела летит по
## баллистической дуге и списывает урон СТРОГО при касании юнита (Area3D →
## body_entered). Промахнувшаяся стрела втыкается в землю НАКОНЕЧНИКОМ.
##
## НАКЛОН ПО ВЕКТОРУ СКОРОСТИ. Каждый кадр ось стрелы выставляется ровно по
## производной параболы: на взлёте остриё смотрит вверх, в вершине дуги —
## горизонтально, на нисходящей ветви — вниз к цели. Разворот делает шейдер
## axis_billboard (квад лежит вдоль оси и крутится ВОКРУГ НЕЁ лицом к камере),
## поэтому наклон виден с любого ракурса и стрела не исчезает «с ребра».
##
## Прицеливание с УПРЕЖДЕНИЕМ и разбросом считает стрелок (см. Archer.gd):
## сюда приходит уже готовая точка попадания.

const _GobCfgA := preload("res://scripts/goblin/goblin_config.gd")
const _Opt := preload("res://scripts/perf_config.gd")
const _AXIS_SHADER := preload("res://shaders/axis_billboard.gdshader")
const _LayerS := preload("res://scripts/ArrowRenderer.gd")

## ── LEGACY-УЗЕЛ (BigStand-5, этап 3) ──────────────────────────────────────
## В штатной игре рядовой снаряд УЗЛА БОЛЬШЕ НЕ ИМЕЕТ: выстрел — запись
## полёта в ядре (GameManager.fire_projectile → ArmyCore.ProjectileFire),
## промах и торчание — там же, попадание — событие пачкой. Этот узел живёт
## под ручкой perf_config.projectile_core = false (A/B на одной сборке) и в
## стендах, испытывающих его напрямую; константы ниже читают и ядро-путь, и
## стенды — они остаются источником чисел

var _start_pos:  Vector3 = Vector3.ZERO
var _end_pos:    Vector3 = Vector3.ZERO
var _speed:      float   = 9.0    # замедлена — дуга читается глазом
var _progress:   float   = 0.0
var _dist:       float   = 0.0

# Доля дальности, уходящая в высоту дуги (задаёт Archer из unit_stats_config.gd)
var _arc_factor: float = 0.5
var _arc_height: float = 3.0

# ── Боевые параметры (задаёт стрелок перед add_child) ────────────────────────
var damage:  float   = 0.0
var shooter: Node3D  = null
## ── СНАЙПЕРСКАЯ СТРЕЛА (ТЗ 14.09.2026) ─────────────────────────────────────
## Ставится GameManager.spawn_arrow ДО launch: полёт прямой (дуга 0 приходит
## снаружи, arc_factor = 0), по пехоте — насмерть сверх брони, застревает в
## голове павшего (CorpseRenderer.stick_arrows(..., head = true))
var snipe: bool = false
var faction: int     = -1          # чьей фракции стрела: своих не задевает

# Радиус «наконечника»: попадание засчитывается при входе в этот объём
const HIT_RADIUS := 0.35
# ── ДВА РАЗНЫХ СРОКА, А НЕ ОДИН ─────────────────────────────────────────────
# Раньше срок был один на всё: 10.5 с от выстрела снимали со сцены и летящую
# стрелу, и торчащую. Теперь у них разные задачи, и мерить их одним числом
# нельзя.
#
# ЛЕТЯЩАЯ. Число осталось прежним и осталось страховкой: полёт занимает около
# секунды, и десять с половиной — это «стрела зависла по любой причине, снять
# её со сцены».
const MAX_FLIGHT_SEC := 10.5
# ТОРЧАЩАЯ (в грунте или в теле). Заказ владельца — 30-60 секунд; сорок пять
# посередине вилки. Это уже не страховка, а картина поля боя: место залпа
# обязано читаться щетиной стрел ещё долго после того, как бой ушёл дальше.
# Прежние 9.5 с стирали следы обстрела раньше, чем игрок успевал довести туда
# камеру
const STUCK_LIFETIME := 45.0
## ── КОСТЬ ЛЕЖИТ ПЯТЬ СЕКУНД, А НЕ СОРОК ПЯТЬ (заказ спринта 15) ───────────
## Гноллов у пня три отряда, и бросают они вчетверо чаще, чем стреляет
## лучник: за минуту боя поле вокруг пня превращалось в сплошной ковёр из
## костей (скриншот владельца). Срок у снаряда теперь СВОЙ по виду, а не
## общий; растворение сжато соразмерно — четыре секунды из пяти были бы не
## «лежит и тает», а «мигает с первой секунды»
const BONE_STUCK_LIFETIME := 5.0
const BONE_STUCK_FADE := 1.5
## Жёсткий потолок ПОЛЁТА кости (пятое письмо спринта 18): бросок гнолла на
## GNOLL_THROW_RANGE ~8.5 м длится меньше секунды, и всё, что летит дольше
## двух, — потерянное событие ядра, а не снаряд. Общий MAX_FLIGHT_SEC (10.5)
## рассчитан на длинную дугу стрелы и для кости слишком терпелив
const BONE_MAX_FLIGHT_SEC := 2.0

## Сколько этому снаряду лежать на земле и сколько из этого растворяться
func _life_total() -> float:
	return BONE_STUCK_LIFETIME if bone else STUCK_LIFETIME

func _fade_total() -> float:
	return BONE_STUCK_FADE if bone else STUCK_FADE
# Сколько из этого срока уходит на растворение. Мгновенно пропадающая стрела
# читается как сбой отрисовки — та же оговорка, что у трупов (CorpseRenderer)
const STUCK_FADE := 4.0
# Длина стрелы в метрах. Задаётся ЯВНО, а не из пикселей: сама картинка
# 64x64 почти пустая (полезная область 43x12), и вывод длины из размера листа
# давал квадрат 0.69x0.69 с крошечной стрелой посередине.
#
# 0.65 вместо прежних 0.75 — заказ владельца «убавить на 10-15%»: рядом с
# фигурой бойца в 0.97 м стрела в три четверти метра читалась бревном.
# Толщина следует за длиной сама: квад строится как (длина, длина/пропорции)
const ARROW_LENGTH := _LayerS.ARROW_LENGTH
## Кость короче стрелы: она метательная, а не стрелковая
const BONE_LENGTH := _LayerS.BONE_LENGTH
const BONE_SHEET := _LayerS.BONE_SHEET
# Какая доля длины стрелы ТОРЧИТ над землёй после промаха (остальное в грунте)
const STUCK_EXPOSED := 2.0 / 3.0
# ── МИНИМАЛЬНЫЙ НАКЛОН ВНИЗ ПРИ ВТЫКАНИИ ────────────────────────────────────
# Здесь стояло 0.35 — это всего двадцать градусов от горизонтали. Настильный
# выстрел давал почти лежащую стрелу, и при камере в 45° она читалась не как
# воткнувшаяся, а как ЗАВИСШАЯ В ВОЗДУХЕ: горизонтальное древко с наконечником
# посреди боя, ровно то, что владелец увидел на скриншоте. Стрел, воткнувшихся
# среди строя, это касается вдвойне — там древко ложится поверх спрайтов.
#
# 1.0 даёт РОВНО СОРОК ПЯТЬ градусов вниз, и это не то же самое, что «синус
# 1.0»: доворот ставит вертикальную составляющую, а потом вектор нормируется —
# при горизонтальной составляющей единичной длины это и есть биссектриса.
# Прежние 0.35 давали таким же путём девятнадцать градусов. Такая стрела
# опознаётся как торчащая из земли с любого ракурса, а на дуге навесного
# выстрела (arrow_arc 0.20) доворот вообще не срабатывает: стрела и так
# приходит круче
const STUCK_MIN_DOWN := 1.0
## Насколько близко к центру здания должна лечь стрела, чтобы засчитать
## попадание. Здания крупные (замок — десяток метров), а разброс стрелка
## считается по той же формуле, что и по бойцу: промах мимо стены обязан
## оставаться промахом, но попадание в стену — попаданием
const BUILDING_HIT_RADIUS := 6.0

# Пропорции полезной части картинки (ширина/высота), считаются при обрезке
var _arrow_aspect: float = 43.0 / 12.0

var _area: Area3D = null
var _spent: bool  = false          # уже попала/воткнулась — больше не летит

## ── СТРЕЛЫ ПЕРЕИСПОЛЬЗУЮТСЯ, А НЕ СОЗДАЮТСЯ ЗАНОВО ──────────────────────────
## Каждый выстрел стоил: узел Arrow + узел ArrowSprite + свой QuadMesh + свой
## ShaderMaterial + объект SceneTreeTimer на срок жизни. Залп трёх тысяч
## лучников — это тысячи таких комплектов в секунду, создаваемых и тут же
## выбрасываемых; на масштабе задания это отдельная статья расхода кадра и
## работы сборщика.
##
## Теперь отлетавшая стрела не освобождается, а ГАСНЕТ и уходит в общий пул
## (GameManager.spawn_arrow / recycle_arrow). Узлы, меш и материал у неё уже
## построены — повторный выстрел только переписывает числа полёта.
##
## СПЯЩАЯ СТРЕЛА ОСТАЁТСЯ В ДЕРЕВЕ, а не висит снятым узлом: снятый был бы
## orphan-узлом (стенд qa_arrow как раз печатает их число как утечку) и пережил
## бы смену сцены, ни за кем не числясь. В дереве её уберёт выгрузка сцены, как
## и любой другой узел.
##
## Срок жизни считается ПОЛЕМ, а не SceneTreeTimer'ом. Таймер тут был не только
## лишним объектом на выстрел — он и не пережил бы переиспользования: связь
## timeout → queue_free от прошлой жизни убила бы стрелу посреди нового полёта
var _life: float = 0.0             # секунд с момента выстрела
var _pooled: bool = false          # погашена и лежит в пуле
## Секунд с момента втыкания. Свой счёт, а не продолжение _life: у торчащей
## стрелы другой срок (STUCK_LIFETIME) и своё растворение
var _stuck_life: float = 0.0
## СТРЕЛА В ТЕЛЕ ЖИВЁТ СТОЛЬКО ЖЕ, СКОЛЬКО ТЕЛО. Свой сорокапятисекундный срок
## (STUCK_LIFETIME) остаётся только у воткнувшейся В ГРУНТ: та лежит сама по
## себе, и ограничить её надо. А торчащая в трупе обязана быть видна ровно пока
## виден труп и истаять ВМЕСТЕ с ним — иначе на поле остаются тела, из которых
## стрелы пропали, и это заметнее любой экономии.
##
## Пока признак стоит, _tick_stuck не считает ничего: погасить такую стрелу
## может только само тело (fade_out_in / despawn_now из CorpseRenderer)
var _in_corpse: bool = false
## Стрела уже догорает и место на поле освободит сама. Признак ПОЛЕМ, а не
## выводом из _stuck_life: гасить её могут с разным сроком (тело — своим,
## потолок числа торчащих — своим), и «осталось меньше STUCK_FADE» отвечало бы
## на этот вопрос неверно ровно в тех случаях, ради которых он и задаётся
var _fading: bool = false
## ── НЕПОДВИЖНАЯ ЦЕЛЬ, НАЗНАЧЕННАЯ СТРЕЛКОМ (здание) ────────────────────────
## Бойца стрела находит сама сканом сетки; здание в сетке не состоит, а
## перебирать группу зданий на каждую стрелу в каждом кадре полёта — та самая
## работа в горячем пути, которой в этом проекте не место. Стрелок знает свою
## цель и кладёт её сюда; проверка одна и в самом конце дуги
var _hit_node: Node3D = null
## ── ПОЛЁТ ВЕДЁТ ЯДРО (perf_config.arrow_core) ────────────────────────────
## Пока летит — узел без _process, запись в ArmyCore (ArrowLaunch). Ядро
## само ищет попадание тем же EnemyAt по XZ-радиусу и отдаёт событие на
## приземление или касание (см. ArrowRenderer.drain_events → core_event)
var _flight_id: int = -1
static var _next_flight_id: int = 1

## Событие от ядра: коснулась чужого (victim) или долетела (victim == null).
## Дальше — ровно то, что делал _process в этих же точках
func core_event(victim, pos: Vector3, axis: Vector3) -> void:
	_flight_id = -1
	if _spent or _pooled:
		return
	global_position = pos
	_axis_now = axis
	_life = maxf(_life, _dist / maxf(_speed, 0.01))
	if victim != null and is_instance_valid(victim):
		var u := victim as Unit
		if u != null and u != shooter and not u.is_dead():
			_strike(u)
			return
	_progress = 1.0
	if _hit_node != null and is_instance_valid(_hit_node) and damage > 0.0:
		var b := _hit_node as Building
		if b != null and not b.is_dead() 				and global_position.distance_to(b.global_position) <= BUILDING_HIT_RADIUS:
			AudioManager.play_3d("bow_impact", global_position)
			var who: Node = shooter if (is_instance_valid(shooter) 				and not shooter.is_queued_for_deletion()) else null
			b.take_damage(damage, who)
			_hit_node = null
			_land_on_building()
			return
	_stick_into_ground()
## Исходный порог среза альфы. На время растворения он снимается в ноль:
## срез по альфе не умеет гасить плавно, он просто выключает пиксель
var _scissor0: float = 0.2

const _ARROW_PATHS := _LayerS._ARROW_PATHS

func _ready() -> void:
	_build_visual()
	_build_hitbox()
	launch()

## ВЗВЕСТИ СТРЕЛУ НА ВЫСТРЕЛ. Зовётся и при рождении (из _ready), и при каждом
## изъятии из пула. Числа полёта к этому моменту уже проставлены снаружи
## (_start_pos/_end_pos/_dist/_speed/_arc_factor/damage/shooter/faction) — здесь
## обнуляется только СОСТОЯНИЕ: прогресс, срок жизни, признаки погашения.
##
## Всё, что строится один раз на всю жизнь узла (меш, материал, спрайт), сюда
## не входит — в этом весь смысл пула
func launch() -> void:
	_arc_height = _dist * _arc_factor
	_progress   = 0.0
	_life       = 0.0
	_stuck_life = 0.0
	_in_corpse  = false
	_fading     = false
	_hit_node   = null
	_spent      = false
	_pooled     = false
	visible     = true
	# Стрела из пула могла лежать в нём наполовину растаявшей — вернуть ей
	# полную непрозрачность; спрятанный слот заново получает полный базис
	_fade_now = 1.0
	if _slot_i >= 0:
		_layer().write(_slot_i, global_position, _axis_now, 1.0)
	_set_fade(1.0)
	_apply_axis(_velocity_dir(0.0))
	# ── ПОЛЁТ ВЕДЁТ ЯДРО (perf_config.arrow_core) ──────────────────────────
	# Запись в ArmyCore вместо _process: позицию, ось и попадание считает
	# BatchArrows одним проходом, сюда вернётся событие core_event
	if _Opt.arrow_core and _slot_i >= 0 and _dist >= 0.001 			and _layer().core_id >= 0:
		_flight_id = _next_flight_id
		_next_flight_id += 1
		_layer().register_flight(_flight_id, self)
		GameManager.army.arrow_launch(_flight_id, _layer().core_id,
			_slot_i, _start_pos, _end_pos, _arc_height, _speed / _dist, faction,
			axis_scale())
		set_process(false)
		return
	set_process(true)
	# Ось выставляем СРАЗУ: первый кадр полёта уже с правильным наклоном
	_apply_axis(_velocity_dir(0.0))

## ПОГАСИТЬ СТРЕЛУ И ВЕРНУТЬ ЕЁ В ПУЛ. Идемпотентно: попадание и истёкший срок
## могут прийти в одном кадре
func _despawn() -> void:
	if _pooled:
		return
	_spent  = true
	_pooled = true
	_in_corpse = false
	_fading = false
	visible = false
	if _flight_id >= 0:
		GameManager.army.arrow_cancel(_flight_id)
		_layer().unregister_flight(_flight_id)
		_flight_id = -1
	if _slot_i >= 0:
		_layer().hide(_slot_i)
	set_process(false)
	set_physics_process(false)
	# Из реестра торчащих — обязательно и до возврата в пул: иначе потолок
	# считал бы место занятым стрелой, которая уже лежит в пуле
	GameManager.forget_stuck_arrow(self)
	# Ссылку на стрелка держать нельзя: пока стрела лежит в пуле, он может
	# погибнуть, а пул пережил бы его и держал освобождённый объект
	shooter = null
	GameManager.recycle_arrow(self)

# ─────────────────────────────────────────────────────────────────────────────
# ПОПАДАНИЕ СЧИТАЕТСЯ ГЕОМЕТРИЕЙ, А НЕ ФИЗИКОЙ
#
# Здесь была Area3D с маской LAYER_UNITS и сигналом body_entered. Она перестала
# срабатывать, когда бойцы перестали быть физическими телами (см. шапку
# Unit.gd): ловить стало нечего, стрелы пролетали сквозь строй насквозь, и
# лучники не наносили урона вовсе.
#
# Возвращать телá ради этого нельзя — они стоили 5.7 мкс на бойца в кадр, ради
# чего всё и затевалось. Проверка перенесена на ту же пространственную сетку,
# по которой в проекте уже считаются и чужой строй, и поиск цели: на каждом
# шаге полёта смотрим, нет ли в HIT_RADIUS от наконечника живого чужого.
# Стрел в воздухе единицы, радиус меньше метра — это один-два просмотра ячейки.
func _build_hitbox() -> void:
	pass

## Оставлено для совместимости с местами, где стрела «снимается с физики»
## (втыкание в грунт): физики у неё больше нет вовсе
func _disable_physics() -> void:
	_area = null

## Проверка попадания на текущем шаге полёта. Зовётся из движения стрелы.
##
## ── ОДИН ПЕРЕХОД ГРАНИЦЫ И НИ ОДНОГО МУСОРНОГО МАССИВА ─────────────────────
## Здесь звался query_radius(), а он строит и возвращает НОВЫЙ Godot-массив
## кандидатов — на каждую стрелу, в каждом кадре её полёта. При скорострельности
## лучника (кулдаун 4 с) и секундном полёте в воздухе висит четверть всех
## стрелков разом: на пятистах лучниках это сто двадцать массивов в кадр плюс
## столько же разборов в GDScript, с приведениями типа и чтением чужих полей.
## Ровно та статья, которую правило №1 в CLAUDE.md запрещает.
##
## Теперь отбор целиком в солвере (ArmyCore.EnemyAt): он сам пропускает своих,
## мёртвых и стрелка, и возвращает ПЕРВОГО подходящего — одним значением,
## без массива. Правило выбора не изменилось ни на йоту
func _check_hit() -> void:
	if _spent:
		return
	# Тип ЯВНО: unit_grid — нетипизированное поле автозагрузки, и `:=` вывел бы
	# здесь Variant (правило №9 в CLAUDE.md)
	var n: Node3D = GameManager.unit_grid.enemy_at(global_position, HIT_RADIUS, faction)
	# Сетка может отдать УЖЕ ОСВОБОЖДЁННЫЙ узел (боец умер в этом же кадре,
	# запись из ячейки ещё не вычищена). Обращение к его полям — та самая
	# ошибка «previously freed», поэтому валидность проверяем ДО приведения типа
	if n == null or not is_instance_valid(n):
		return
	var u := n as Unit
	if u == null or u == shooter or u.is_dead():
		return
	_strike(u)

func _strike(u: Unit) -> void:
	if _spent:
		return
	# Цель могла погибнуть между выбором в _check_hit и этим вызовом (или прийти
	# из внешнего кода). take_damage на освобождённом объекте — краш «Invalid
	# type in function 'take_damage' in base ... (previously freed)». Проверяем
	# ОБА признака смерти узла: is_instance_valid ловит уже освобождённый объект,
	# is_queued_for_deletion — тот, что жив этот кадр, но уже помечен на удаление
	# (queue_free вызван, но объект ещё не снят с дерева)
	if not is_instance_valid(u) or u.is_queued_for_deletion():
		_despawn()
		return
	_spent = true
	_disable_physics()
	# Стрелок мог погибнуть, пока стрела летела: передавать освобождённый
	# объект в take_damage нельзя — движок ругается на «previously freed»
	# и ответная агрессия жертвы уходит в никуда
	var valid_shooter: Node = null
	if is_instance_valid(shooter) and not shooter.is_queued_for_deletion():
		valid_shooter = shooter
	# ПОПАДАНИЕ В ЖИВОГО — глухой удар в тело. Звучит В ТОЧКЕ ЦЕЛИ, а не у
	# стрелка: залп с 20 м должен приходить оттуда, куда он прилетел
	AudioManager.play_3d("bow_impact", global_position)
	# ── КРУПНАЯ МИШЕНЬ ЛОВИТ СТРЕЛУ БОЛЬНЕЕ (заказ 13.09.2026) ────────────
	# Множитель — свойство ЦЕЛИ (`Unit.ranged_damage_mult`), и читается он
	# здесь, в пути стрелы: в рукопашной его нет вовсе. У всех, кроме туш,
	# он равен единице, и умножение на неё ничего не меняет
	var dmg: float = damage * u.ranged_damage_mult()
	# ── «ГРОЗА ВЕЛИКАНОВ» (ТЗ 14.09.2026, ветка лучника 4b/5b) ────────────
	# Множитель по крупным (туша, тролль, конница) копится ключом bonus_giant
	# у рода войск стрелка: 0.5 → ×1.5, 1.0 → ×2.0. Читается раз на попадание
	if u.giant_class():
		var sid_shooter: String = "archer"
		if valid_shooter != null and valid_shooter is Unit:
			sid_shooter = (valid_shooter as Unit).stat_id
		dmg *= 1.0 + GameManager.unit_bonus(faction, sid_shooter, "bonus_giant")
	# ── СНАЙПЕРСКАЯ СТРЕЛА: ПО ПЕХОТЕ НАСМЕРТЬ (ТЗ 14.09.2026) ────────────
	# «Выстрел в голову»: гарантированно фатально сверх брони и щита —
	# сумма, которую не удержит ни один множитель снижения (MIN_DAMAGE_FRACTION
	# и щит режут долей, а не до нуля). Крупные (snipe_one_shot = false) берут
	# обычный урон с бронепробитием и множителем выше
	if snipe and u.snipe_one_shot():
		dmg = maxf(dmg, u.max_health * 1000.0 + 1000.0)
	strikes += 1
	if snipe:
		snipe_strikes += 1
	u.take_damage(dmg, valid_shooter)
	# ── СТРЕЛА, УБИВШАЯ БОЙЦА, ОСТАЁТСЯ В ТЕЛЕ ─────────────────────────────
	# Урон списан выше, и если он оказался смертельным, боец уже прошёл _die()
	# и уже положил своё тело на грунт (Unit._leave_corpse). Стрела втыкается
	# туда же и под тем же углом, под каким прилетела: это единственный след,
	# по которому видно, ЧЕМ был выбит отряд.
	#
	# Раненый, но живой, стрелу не уносит: он ходит, а стрела — не его узел и
	# за ним не поедет. Торчащая в бегущем бойце и отстающая от него стрела
	# выглядела бы хуже, чем её отсутствие
	if is_instance_valid(u) and u.is_dead():
		# СНАЙПЕРСКАЯ — В ГОЛОВУ ПАВШЕГО: у бойца с телом в общем слое точку
		# знает CorpseRenderer (head = true), у тролля — сам тролль
		if snipe:
			var hs: Vector3 = u.snipe_head_spot()
			if hs.x != INF:
				stick_decor(hs, _velocity_dir(_progress))
				snipe_head_pins += 1
				return
		_stick_into_corpse(u, snipe)
		return
	# ── ЗАСТРЯЛА В ТУШЕ ЖИВОГО (заказ 10.09.2026) ────────────
	# Оговорка выше («раненый стрелу не уносит: стрела за ним не
	# поедет») верна для всей пехоты и остаётся в силе: arrow_sockets() у неё
	# ноль. А вот тролль тикает каждый кадр и ведёт торчащие в нём стрелы сам
	# (Troll.stick_arrow → Arrow.move_stuck)
	if is_instance_valid(u) and not u.is_dead() and u.arrow_sockets() > 0:
		if u.stick_arrow(self):
			stick_decor(global_position, _velocity_dir(_progress))
			return
	_despawn()

# ── КАРТИНКА ЖИВЁТ В ОБЩЕМ MultiMesh (этап D3, см. ArrowRenderer) ───────────
# Узел остался ЛОГИКОЙ (полёт, попадание, сроки, пул); слот слоя держит
# позицию, ось и долю покрытия. Прежние _visual/_mat сняты вместе с их
# вызовом отрисовки на каждую стрелу
## ── КОСТЬ ГНОЛЛА ──────────────────────────────────────────────────────────
## Тот же снаряд по всей логике (дуга, попадание, урон, втыкание, срок), но
## СВОЙ слой отрисовки (GameManager.bones_mm) и своя картинка: материал у
## MultiMesh один на бакет, и подменить текстуру одной стреле нельзя. Признак
## ставится ДО _ready() (см. GameManager.spawn_arrow) и больше не меняется —
## слот слоя узел берёт один раз и держит пожизненно
var bone: bool = false

## Слой, которому принадлежит картинка этого снаряда
func _layer():
	return GameManager.bones_mm if bone else GameManager.arrows_mm

## ── МАСШТАБ ОСИ = ПРИЗНАК «Я СЕЙЧАС ЛЕЧУ И КУВЫРКАЮСЬ» ────────────────────
## Шейдер ось нормирует, поэтому её модуль свободен и несёт один бит даром:
## укороченная — снаряд в полёте, ровно единица — воткнулся и лежит смирно.
## Иначе пришлось бы либо занимать пятый канал instance-цвета (его нет), либо
## заводить костям второй слой отрисовки ради одного признака
func axis_scale() -> float:
	if bone and not _spent and not _pooled:
		return _GobCfgA.GNOLL_BONE_AXIS_K
	return 1.0

var _slot_i: int = -1
## Поколение слоя, у которого взят слот: слой пересобирается на смене сцены,
## и слот прошлого поколения возвращать некуда (см. ArrowRenderer.release)
var _slot_gen: int = -1
var _axis_now: Vector3 = Vector3.RIGHT
var _fade_now: float = 1.0

## ── СЛОТ ВОЗВРАЩАЕТСЯ ВМЕСТЕ С УЗЛОМ ───────────────────────────────────────
## Узел стрелы освобождают мимо пула (переполнение ARROW_POOL_MAX, выгрузка
## сцены), и слот общего MultiMesh при этом никто не отдавал: буфер рос по
## 64 слота на каждую пачку выброшенных узлов и целиком уезжал в рендер
## каждым грязным кадром. Именно это выглядело как «стрелы забивают буферы»
func _notification(what: int) -> void:
	if what == NOTIFICATION_PREDELETE and _slot_i >= 0:
		# На выходе из игры автозагрузка может уйти раньше стрелы
		if is_instance_valid(GameManager):
			var mm = _layer()
			if mm != null:
				mm.release(_slot_i, _slot_gen)
		_slot_i = -1

# Квад строится ВДОЛЬ ЛОКАЛЬНОЙ ОСИ X: size = (длина, толщина). Шейдер
# axis_billboard кладёт этот +X на переданную мировую ось — то есть на вектор
# скорости. Текстура нарисована остриём ВПРАВО (замер альфы: слева оперение
# толщиной 10-12 px, справа сходящий на нет наконечник 12→2 px), поэтому
# локальный +X = направление полёта, и разворот на 180° не нужен.
func _build_visual() -> void:
	var tex := _load_arrow_texture()
	if tex:
		var sz := tex.get_size()
		if sz.y > 0.0:
			_arrow_aspect = sz.x / sz.y
	else:
		_arrow_aspect = 6.0
	var length: float = BONE_LENGTH if bone else ARROW_LENGTH
	if _layer().ensure(get_parent() as Node3D, tex, length, _arrow_aspect):
		# КУВЫРОК ВКЛЮЧАЕТСЯ У СЛОЯ, А НЕ У СНАРЯДА: материал один на бакет,
		# и ставить ручку каждому снаряду значило бы обращаться в сервер
		# отрисовки на каждый выстрел. Кто именно сейчас летит, шейдер узнаёт
		# сам — по длине упакованной оси (см. axis_scale)
		if bone:
			_layer().set_spin(_GobCfgA.GNOLL_BONE_SPIN)
		_slot_i = _layer().acquire()
		_slot_gen = _layer().gen

# Картинки слоя (обрезка по рисунку, кэш) живут в ArrowRenderer — их
# берёт и путь без узла
func _load_bone_texture() -> Texture2D:
	return _LayerS.load_bone_texture()

func _load_arrow_texture() -> Texture2D:
	if bone:
		return _load_bone_texture()
	return _LayerS.load_arrow_texture()

# ── ПОЛЁТ ────────────────────────────────────────────────────────────────────

## Единичный вектор скорости в точке t дуги.
## pos(t) = lerp(start, end, t) + UP · sin(π·t) · h
## d/dt   = (end − start) + UP · π · cos(π·t) · h
func _velocity_dir(t: float) -> Vector3:
	var v := _end_pos - _start_pos
	v.y += PI * cos(t * PI) * _arc_height
	if v.length_squared() < 1e-8:
		return Vector3.FORWARD
	return v.normalized()

func _apply_axis(dir: Vector3) -> void:
	_axis_now = dir
	_push_visual()

## Слот слоя: позиция + ось + покрытие одной записью
func _push_visual() -> void:
	if _slot_i >= 0:
		_layer().write_flight(_slot_i, global_position,
			_axis_now * axis_scale(), _fade_now)

func _process(delta: float) -> void:
	# ЗДЕСЬ ЖИВЁТ ТОЛЬКО ЛЕТЯЩАЯ. Срок торчащей считает общий обход реестра
	# (GameManager._sweep_stuck_arrows), и свой _process ей выключают в тот же
	# миг, когда она втыкается, — в грунт (_stick_into_ground) или в тело
	# (stick_decor). Ранний выход оставлен страховкой на случай, если какой-то
	# путь оставит нотификацию включённой: считать срок ДВАЖДЫ хуже, чем не
	# считать его здесь вовсе
	if _spent:
		return
	# СРОК ПОЛЁТА — ПОЛЕМ, А НЕ ТАЙМЕРОМ (см. шапку у _life)
	_life += delta
	if _life >= MAX_FLIGHT_SEC:
		_despawn()
		return
	if _dist < 0.001:
		_despawn()
		return

	_progress = minf(_progress + _speed * delta / _dist, 1.0)
	var t := _progress

	# Параболическая дуга: горизонтально = lerp, вертикально = sin(π·t)·высота
	var straight := _start_pos.lerp(_end_pos, t)
	var arc_y    := sin(t * PI) * _arc_height
	global_position = Vector3(straight.x, straight.y + arc_y, straight.z)
	# НАКЛОН РОВНО ПО СКОРОСТИ — каждый кадр
	_apply_axis(_velocity_dir(t))

	# ПОПАДАНИЕ ПРОВЕРЯЕТСЯ ЗДЕСЬ, сразу после переноса наконечника: раньше это
	# делала Area3D сигналом body_entered, но у бойцов больше нет физических тел
	# (см. _build_hitbox)
	_check_hit()
	if _spent:
		return

	if _progress >= 1.0:
		# ДОЛЕТЕЛА ДО НАЗНАЧЕННОГО ЗДАНИЯ — СПИСЫВАЕМ УРОН. Проверка ровно
		# одна за всю жизнь стрелы и только у той, которой стрелок дал цель
		if _hit_node != null and is_instance_valid(_hit_node) and damage > 0.0:
			var b := _hit_node as Building
			if b != null and not b.is_dead() 					and global_position.distance_to(b.global_position) <= BUILDING_HIT_RADIUS:
				AudioManager.play_3d("bow_impact", global_position)
				var who: Node = shooter if (is_instance_valid(shooter) 					and not shooter.is_queued_for_deletion()) else null
				b.take_damage(damage, who)
				_hit_node = null
				_land_on_building()
				return
		_stick_into_ground()

# ПРОМАХ: стрела долетела до расчётной точки, никого не задев — ВТЫКАЕТСЯ
# в землю НАКОНЕЧНИКОМ вперёд и торчит там до конца MAX_LIFETIME, выступая
# над грунтом на STUCK_EXPOSED своей длины.
## ── НА ЗДАНИИ СТРЕЛА НЕ ТОРЧИТ, А РАСТВОРЯЕТСЯ (заказ спринта 15) ───────────
## Жалоба со скриншотом: пень тролля утыкан стрелами, как ёж. Втыкаться в
## стену нарисованной постройки некуда — древко висело в воздухе перед ней.
## Стрела ложится тем же путём, что и промах (учёт торчащих, потолок, пул),
## но срок ей ставится в доли секунды: тот же fade_out_in, которым вытесняют
## лишние. Второго пути «исчезнуть» не заведено
func _land_on_building() -> void:
	_stick_into_ground()
	fade_out_in(_GobCfgA.ARROW_BUILDING_FADE)

func _stick_into_ground() -> void:
	# ── СВОЙ _process ВЫКЛЮЧАЕТСЯ, СРОК СЧИТАЕТ ОБЩИЙ ОБХОД ────────────────
	# Здесь стояло прямо обратное («_process НЕ выключаем: на нём висит отсчёт
	# срока жизни»), и это была нотификация движка на КАЖДУЮ торчащую стрелу —
	# до MAX_STUCK_ARROWS = 160 узлов, каждый ради одного сложения float.
	# Реестр торчащих у GameManager уже есть (он держит потолок), и обойти его
	# одним циклом дешевле, чем сто шестьдесят раз войти в GDScript из движка.
	# Тот же приём и та же причина, что у тел (CorpseRenderer): декорация не
	# заводит своего тика.
	# ЗАМЕТЬТЕ: в реестр стрела становится в конце этой же функции, и обход
	# подхватит её со следующего кадра — терять при этом нечего, срок у неё
	# сорок пять секунд
	_spent = true
	set_process(false)
	set_physics_process(false)
	_disable_physics()
	# ВТЫКАНИЕ В ЗЕМЛЮ — сухой удар мимо цели. Тише попадания в тело (см.
	# SFX_LIMITS): промахов в залпе много, и они не должны забивать попадания
	AudioManager.play_3d("bow_block", global_position)

	# Направление удара — вектор скорости в конце дуги (смотрит вниз).
	# Страховка на настильный выстрел: совсем пологую стрелу доворачиваем вниз,
	# иначе она легла бы на грунт плашмя вместо того, чтобы воткнуться
	var dir := _velocity_dir(1.0)
	if dir.y > -STUCK_MIN_DOWN:
		dir.y = -STUCK_MIN_DOWN
		dir = dir.normalized()
	dir = _stick_jitter(dir)
	_apply_axis(dir)

	# Точка входа в грунт: остриё уходит под землю на (1 − STUCK_EXPOSED)
	# длины, оперение остаётся снаружи. Ось квада направлена по dir, остриё —
	# в его конце, поэтому центр = точка входа − dir · len · (exposed − 0.5)
	var gy := GameManager.get_terrain_height(global_position.x, global_position.z)
	var entry := Vector3(global_position.x, gy, global_position.z)
	global_position = entry - dir * (ARROW_LENGTH * (STUCK_EXPOSED - 0.5))
	# ── КАРТИНКА ЕДЕТ ЗА ТОЧКОЙ ЯВНО ───────────────────────────────────────
	# Пока стрела была узлом со своим спрайтом, перенос узла переносил и
	# картинку сам. В общем MultiMesh (этап D3) слот знает только то, что в
	# него записали, а записывала его _apply_axis ВЫШЕ — ещё в точке конца
	# дуги, на высоте груди цели. Стрела на экране оставалась висеть в
	# воздухе под углом втыкания до самого растворения (первая запись с
	# верной точкой шла из _set_fade, через сорок секунд). Это и был баг
	# «стрелы зависают пачками в воздухе»
	_push_visual()

	# Своего таймера у воткнувшейся стрелы нет: срок считает _tick_stuck.
	# А вот в общий реестр торчащих её поставить обязаны — он и держит
	# их число в пределах потолка (см. GameManager.note_stuck_arrow)
	GameManager.note_stuck_arrow(self)


## ── КАЖДАЯ СТРЕЛА ТОРЧИТ ПО-СВОЕМУ ─────────────────────────────────────────
## Заказ владельца: одинаковый наклон у всех торчащих стрел читается как
## штамп, а не как поле после обстрела. Разброс ±12° и по ВЕРТИКАЛИ, и вокруг
## неё: одного поворота вокруг вертикали мало — стрелы легли бы веером, но всё
## так же одинаково круто.
##
## Считается ОТ ТОЧКИ, а не через randf(): два прогона одного боя обязаны дать
## одно поле, иначе стенд не проверить. Та же причина, по которой углы тел и
## места стрел в туловище выведены из чисел (см. CorpseRenderer)
const STICK_JITTER := 0.21    # ≈12° в радианах

func _stick_jitter(dir: Vector3) -> Vector3:
	var gp := global_position
	var h1: float = fposmod(sin(gp.x * 12.9898 + gp.z * 78.233) * 43758.5453, 1.0)
	var h2: float = fposmod(sin(gp.x * 39.3468 + gp.z * 11.135) * 24634.6345, 1.0)
	# Вокруг вертикали — куда «смотрит» древко по земле
	var d := dir.rotated(Vector3.UP, (h1 - 0.5) * 2.0 * STICK_JITTER)
	# И наклон: ось поворота — горизонтальная, перпендикулярная древку
	var side := Vector3(d.z, 0.0, -d.x)
	if side.length_squared() > 1e-6:
		d = d.rotated(side.normalized(), (h2 - 0.5) * 2.0 * STICK_JITTER)
	# Доворот вниз обязан пережить разброс: стрела, задранная джиттером выше
	# порога, снова легла бы плашмя — ровно то, от чего лечит STUCK_MIN_DOWN
	if d.y > -STUCK_MIN_DOWN * 0.75:
		d.y = -STUCK_MIN_DOWN * 0.75
	return d.normalized()

## Стрела уже догорает? Спрашивает потолок числа торчащих: догорающая место на
## поле не занимает, иначе каждая новая стрела гасила бы ещё одну — ровно та
## лавина, которую в своё время поймал стенд на телах (см. CorpseRenderer.spawn)
func is_fading() -> bool:
	return _fading

## ── СТРЕЛУ ГАСИТ ТЕЛО, В КОТОРОМ ОНА ТОРЧИТ ────────────────────────────────
## Зовёт CorpseRenderer, когда тело начинает таять: стрела обязана истаять
## ВМЕСТЕ с ним, а не пережить его на свои сорок пять секунд. Оставшийся срок
## просто укорачивается — своя ветка «гаснуть по-другому» тут не нужна.
##
## Второй заказчик — потолок числа торчащих стрел (GameManager.note_stuck_arrow):
## самая старая уходит с поля тем же способом, только сроком покороче
func fade_out_in(sec: float) -> void:
	if _pooled or not _spent:
		return
	_fading = true
	var want: float = maxf(sec, 0.05)
	# Тело начало таять — стрела перестаёт быть «вечной» и догорает вместе с
	# ним. Ставим её на последние `want` секунд общего срока: дальше растворение
	# считает обычный _tick_stuck
	if _in_corpse:
		_in_corpse = false
		_stuck_life = _life_total() - want
		# Включать себе _process больше не нужно: счёт всем торчащим ведёт
		# общий обход реестра (GameManager._sweep_stuck_arrows), а в реестре
		# стрела состоит с самого втыкания
		return
	var left: float = _life_total() - _stuck_life
	if want < left:
		_stuck_life = _life_total() - want

## Тело догорело — снять стрелу немедленно. Через пул, а не queue_free:
## переиспользование стрел ради того и заведено
func despawn_now() -> void:
	_despawn()

## ВХОД ДЛЯ ОБЩЕГО ОБХОДА (GameManager._sweep_stuck_arrows). Летящая стрела
## сюда не попадает — в реестре торчащих её нет; проверка на всякий случай,
## потому что реестр держит узлы, а не состояния
func tick_stuck(delta: float) -> void:
	if _pooled or not _spent:
		return
	_tick_stuck(delta)

# ── ЖИЗНЬ ТОРЧАЩЕЙ СТРЕЛЫ ───────────────────────────────────────────────────
# Одинаково для воткнувшейся в грунт и оставшейся в теле: разницы между ними
# после втыкания нет никакой, и заводить две ветки было бы враньём про код
func _tick_stuck(delta: float) -> void:
	# В ТЕЛЕ СВОЕГО СРОКА НЕТ (см. _in_corpse): тело само погасит, когда начнёт
	# таять. До тех пор стрела видна целиком, а не выцветает раньше трупа
	if _in_corpse:
		return
	_stuck_life += delta
	if _stuck_life >= _life_total():
		_despawn()
		return
	var left: float = _life_total() - _stuck_life
	if left <= _fade_total():
		_fading = true
		_set_fade(left / _fade_total())

## Непрозрачность стрелы. Вместе с ней СНИМАЕТСЯ СРЕЗ АЛЬФЫ: срез отбрасывает
## пиксель целиком по порогу и потому не умеет гасить плавно — стрела с ним
## не растворялась бы, а осыпалась дырами и в конце прыжком исчезала
func _set_fade(k: float) -> void:
	# ── ГАСИМ ПОКРЫТИЕМ, А НЕ АЛЬФОЙ И НЕ ПОРОГОМ ──────────────────────────
	# Здесь стояло «снять порог среза в ноль, потому что срез не умеет гасить
	# плавно». Снятый порог и был чёрным прямоугольником под гаснущей стрелой:
	# материал со срезом Godot относит к НЕПРОЗРАЧНОМУ проходу, записанную
	# ALPHA там не читают вовсе, и порог оставался единственным, что отсекало
	# пустоту вокруг стрелы. Обнулили порог — весь квад поехал на экран своим
	# цветом (0,0,0).
	#
	# Теперь порог не трогаем никогда, а растворение делает дизер в шейдере
	# (см. axis_billboard.gdshader). modulate остаётся белым: его альфа в
	# непрозрачном проходе всё равно не читается, и держать её значило бы
	# делать вид, что она на что-то влияет
	_fade_now = k
	_push_visual()

## ── ВСТАТЬ В ТЕЛО: ТОЧКА И УГОЛ ЗАДАНЫ СНАРУЖИ ─────────────────────────────
## Зовёт CorpseRenderer.stick_arrows для стрелы, добившей бойца: куда именно
## она входит в туловище и под каким углом, знает тело, а не стрела.
##
## _process тут ВЫКЛЮЧАЕТСЯ. Стрела в теле не считает ничего — своего срока у
## неё нет, гасит её тело (см. _in_corpse), — а покадровый вызов на каждую
## торчащую стрелу это ровно та цена, из-за которой поле после большого залпа
## тормозило. Обратно счёт включит fade_out_in, когда тело начнёт таять
## ── СТРЕЛА ТОРЧИТ В ЖИВОЙ ТУШЕ (заказ 10.09.2026) ─────────────────────────
## Точку такой стрелы ведёт САМА ЖЕРТВА (Troll.tick_physics): она одна, тикает
## каждый кадр и знает, куда её несёт. Стрела при этом обычная торчащая — свой
## тик выключен, срок считает общий обход реестра
func move_stuck(at: Vector3, dir: Vector3) -> void:
	if _pooled:
		return
	var d := dir.normalized()
	_axis_now = d
	global_position = at - d * (ARROW_LENGTH * (STUCK_EXPOSED - 0.5))
	_push_visual()

func stick_decor(at: Vector3, dir: Vector3) -> void:
	_spent = true
	_in_corpse = true
	_stuck_life = 0.0
	_progress = 1.0
	set_physics_process(false)
	_disable_physics()
	var d := dir.normalized()
	if d.y > -STUCK_MIN_DOWN:
		d.y = -STUCK_MIN_DOWN
		d = d.normalized()
	_apply_axis(d)
	global_position = at - d * (ARROW_LENGTH * (STUCK_EXPOSED - 0.5))
	_push_visual()
	visible = true
	if _slot_i >= 0:
		_layer().write(_slot_i, global_position, _axis_now, _fade_now)
	set_process(false)
	GameManager.note_stuck_arrow(self)

# ── СТРЕЛА ОСТАЁТСЯ В ПАВШЕМ ────────────────────────────────────────────────
# Точка уже верная: стрела стоит ровно там, где догнала бойца. Меняется только
# высота — тело лежит НА грунте, и стрела обязана торчать из него, а не висеть
# на высоте груди убитого. Угол — вектор скорости в момент попадания, с той же
# страховкой от настильного выстрела, что и при промахе: почти горизонтальную
# стрелу доворачиваем вниз, иначе она легла бы плашмя
## Счётчик стрел, воткнутых в голову (стенд qa_archer_snipe)
static var snipe_head_pins: int = 0
## Стендам: попадания в живых (все стрелы и отдельно снайперские)
static var strikes: int = 0
static var snipe_strikes: int = 0

func _stick_into_corpse(victim: Unit, head: bool = false) -> void:
	# ── СТРЕЛА ПРИПИСЫВАЕТСЯ К КОНКРЕТНОМУ ТЕЛУ ────────────────────────────
	# Тело уже положено (Unit._die → _leave_corpse отработал внутри
	# take_damage), и стрела встаёт к нему в список: когда тело начнёт таять,
	# оно погасит и её. Стрела, пережившая своё тело и повисшая над пустым
	# лугом, заметнее любой экономии.
	#
	# В теле держится РОВНО ОДНА стрела (CorpseRenderer.MAX_ARROWS_PER_CORPSE);
	# добитый залпом лежит с той, что его добила, остальные ушли в пул ещё при
	# его жизни. Если место занято, эта тоже уходит в пул
	var body = victim.corpse_ref()
	if body == null:
		_despawn()
		return
	# ГЕОМЕТРИЮ СЧИТАЕТ ТЕЛО, А НЕ СТРЕЛА. Куда именно легло тело, под каким
	# углом и на какой высоте, знает CorpseRenderer; стрела знает только, что
	# попала. Признаки втыкания ставит stick_decor, которую тело и позовёт
	if not GameManager.corpses.stick_arrows(body, self,
			_velocity_dir(_progress), head):
		_despawn()
		return
	if head:
		snipe_head_pins += 1
