# Снятые пояснения конфигов (14.09.2026)

Полотна комментариев вынесены из конфигов по ТЗ 14.09.2026 (п. 1). Ниже — снятый текст с привязкой к строке кода, перед которой он стоял.


## scripts/unit_stats_config.gd

### (шапка файла)

```
## ЕДИНЫЙ КОНФИГ ХАРАКТЕРИСТИК ЮНИТОВ
## ═══════════════════════════════════════════════════════════════════════════
## Меняйте числа здесь — они подхватываются при спавне юнита, код трогать не надо.
## Подключение: const _UStats := preload("res://scripts/unit_stats_config.gd")
## (файл намеренно без class_name — так он не зависит от кэша глобальных классов)
##
## Смысл параметров:
##   health          — здоровье (HP)
##   movement_speed  — скорость движения, м/с
##   attack_1        — урон обычной (слабой) атаки
##   attack_2        — урон мощной атаки (Warrior бьёт: 3 раза attack_1, затем 1 раз attack_2)
##   attack_range    — дальность атаки, м (ближний бой ~1.6-1.8, лучник 18)
##   attack_range_cap— ЖЁСТКИЙ потолок дальности с учётом всех прибавок кузницы.
##                     Нет ключа — потолка нет (у ближнего боя он не нужен)
##   attack_cooldown — пауза между ударами, сек
##   defense         — защита: вычитается из входящего урона (растёт апгрейдами кузницы)
##   armor           — броня: дополнительное плоское снижение урона (базовое, от экипировки)
##   morale          — мораль 0..100: участвует в силе толкания (morale/100 добавляется к push_force)
##   push_force      — сила толкания: при ударе в ближнем бою юнит с большей суммой
##                     (push_force + morale/100) слегка отталкивает противника назад
##
## Только для ударной конницы (все поля необязательные, отсутствие = «не конница»):
##   charge_range       — за сколько метров до цели начинается разгон, м. 0 = разгона нет
##   charge_min_runup   — сколько метров надо РЕАЛЬНО проехать в разгоне, чтобы
##                        удар засчитался. Без этого натиск срабатывал бы и в
##                        плотном бою, где никто никуда не разгонялся
##   charge_speed_mult  — во сколько раз конница ускоряется на разгоне
##   charge_impact_frac — доля МАКСИМАЛЬНОГО запаса жизни цели, снимаемая ударом
##                        с разгона (0.5 = половина). Считается от max_health
##                        ЖЕРТВЫ, а не от урона всадника: это вес и скорость
##                        кабана, а не сила руки седока
##   charge_splash      — радиус первого ряда контакта, м: кого накрывает удар
##   charge_knockback   — на сколько метров разлетаются накрытые, м
##   charge_breakthrough — на сколько метров всадник вклинивается сквозь строй
##                         сразу после удара (0 — «ударил и встал»)
##   charge_counter_frac— доля СВОЕГО запаса жизни, которую конница теряет,
##                        налетев в лоб на стенку копий (см. Unit.repels_charge)
##   charge_push_mult   — множитель ПОСТОЯННОГО толчка в свалке (см. Unit._apply_push).
##                        К разгону отношения не имеет: это продавливание строя,
##                        которое идёт всё время, пока конница жива и бьётся
##
## Только для рабочего:
##   walk_speed_empty  — скорость ходьбы БЕЗ груза
##   walk_speed_loaded — скорость ходьбы С ресурсами (медленнее)
##   gather_time       — сколько секунд занимает один цикл добычи
##   gather_amount     — сколько ресурса приносит за один заход
##
## Только для лучника:
##   arrow_speed — скорость полёта стрелы, м/с (меньше = дольше летит)
##   arrow_arc   — высота дуги как доля дальности (0.5 = навесная траектория)
## Древо технологий кузницы живёт в отдельном файле: это самостоятельная
## балансная таблица на 4 вкладки × 20 узлов, и держать её здесь означало бы
## утопить характеристики юнитов в её объёме. Нужен он только одной функции —
## get_upgrade_slot(), которая отдаёт узлы древа как обычные слоты улучшений
```


### "warrior": {

```
	# Мечник стоял между копейщиком и кабаном: чуть крепче фаланги, чуть
	# дороже, и на поле читался как «копейщик с мечом». Теперь это ЭЛИТА:
	# вдвое живучее копейщика, броня вдвое выше, шаг НИЖЕ копейного на 0.1,
	# напор — как у лёгкой конницы. Платится это слотами (squad_slots = 2,
	# один отряд рыцарей занимает место двух пехотных), ценой и временем найма
```


### "name_genitive_plural": "мечников",

```
		# Родительный падеж множественного числа — для названия отряда
		# по рангу («Отряд ветеранов МЕЧНИКОВ», см. veteran_rank_name)
```


### "squad_slots": 2.0,

```
		# ОДИН ОТРЯД РЫЦАРЕЙ = ДВА СЛОТА (заказ владельца): и в лимите
		# населения (GameManager.pop_squads_used), и в весе морали
```


### "name_genitive_plural": "копейщиков",

```
		# Родительный падеж множественного числа — для названия отряда
		# по рангу («Отряд ветеранов КОПЕЙЩИКОВ», см. veteran_rank_name)
```


### "name_genitive_plural": "лучников",

```
		# Родительный падеж множественного числа — для названия отряда
		# по рангу («Отряд ветеранов ЛУЧНИКОВ», см. veteran_rank_name)
```


### "attack_range_cap": 24.0,

```
		# Дальность растёт от кузницы (bonus_range) и вписывается прямо в поле
		# бойца при рождении. Потолка у неё не было вовсе, и на прокачанной
		# кузнице лучник доставал через пол-экрана: «случайные сверхдальние
		# выстрелы через всю карту».
		#
		# Потолок ЖЁСТКИЙ и общий: он ограничивает СУММУ, а не прибавку, —
		# иначе каждое новое исследование пришлось бы согласовывать с ним
		# отдельно. 24 м это +20% к базовым двадцати: прокачка ощутима и при
		# этом не превращает стрелка в осадную машину
```


### "name_genitive_plural": "монахов",

```
		# Родительный падеж множественного числа — для названия отряда
		# по рангу («Отряд ветеранов МОНАХОВ», см. veteran_rank_name)
```


### "attack_1": 0.0,

```
		# все три ворота спрашивают «вооружён ли» по attack_damage > 0
```


### "goblin_spearman": {

```
	# Числа здесь, а не в goblin_config: это ХАРАКТЕРИСТИКИ БОЙЦА, и им место в
	# той же таблице, что и людям, — иначе сравнить копейщика с копейщиком
	# нельзя, не открыв два файла. В goblin_config живёт ПОВЕДЕНИЕ фракции
	# (волны, деревня, спячка), а не сила удара.
	#
	# Замысел: гоблин по одному слабее человека и берёт числом (отряд 100 против
	# 20), наездник на кабане — быстрый и бьющий больно, но хрупкий.
```


### "name_genitive_plural": "гоблинов",

```
		# Родительный падеж множественного числа — для названия отряда
		# по рангу («Отряд ветеранов ГОБЛИНОВ», см. veteran_rank_name)
```


### "big_goblin": {

```
	# 165 у гоблина-копейщика — два удара мечника), УРОН смешной (4 против
	# 10-15), зато СКОРОСТЬ выше всех пеших в игре (3.6 против 2.2 у орды и
	# 2.0 у людской пехоты). Он не должен убивать — он должен изматывать и
	# уводить за собой, а погибать от первого же дошедшего копейщика.
	# Дальность 11 м — ВДВОЕ КОРОЧЕ лучника (20): кость летит навесом и
	# недалеко. Мораль низкая: стая ломается раньше любого другого отряда
	## Увеличенный гоблин-копейщик, а не отдельный род войск по духу: те же
	## ленты, тот же бой, тот же строй. Отличий ровно четыре, и все они —
	## ЧИСЛА, выведенные из таблицы обычного гоблина, а не вписанные руками:
	##   запас  = ВЕСЬ стандартный отряд гоблинов (SQUAD_SIZE × health);
	##   шаг    = −30 % от гоблинского;
	##   удар   = ×3 от гоблинского;
	##   стрелы = ×2 урона по нему (ranged_damage_mult).
	## Выводить, а не вписывать, обязательно: владелец правит числа гоблина в
	## своей таблице, и туша обязана ехать за ними.
	##
	## ВНИМАНИЕ К ВЕЛИЧИНЕ ЗАПАСА. Стандартный отряд орды — СТО копейщиков
	## (goblin_config.SQUAD_SIZE), то есть буквальное прочтение заказа даёт
	## 100 × 165 = 16 500 HP на одну тушу — это в полтора раза больше тролля
	## (10 160) и 82 500 на отряд из пяти. Множитель BIG_GOBLIN_HP_SQUADS
	## оставлен ручкой ровно для того, чтобы это можно было поправить одним
	## числом, не трогая ни строчки кода
```


### "movement_speed": 2.5,

```
		# по экрану. Теперь гнолл лишь немного быстрее копейщика: он всё ещё
		# уходит от строя, но глаз успевает за ним следить
```


### "attack_1": 4.0,

```
		# КЛЮЧ ИМЕННО attack_1: _apply_config_stats читает урон оттуда
		# (общий контракт со всеми родами войск), "attack_damage" он не знает
```


### "attack_range": 8.5,

```
		# Короткий бросок (спринт 14): на длинной дистанции навес размазывался
		# и кость читалась летящей по прямой — разбор в goblin_config
```


### "attack_cooldown": 1.77,             # спринт 20: −15 % (было 2.08)

```
		# Кулдаун +30 % (заказ спринта 15): 1.6 → 2.08. Стая у пня бросала
		# костями сплошным потоком — на экране это читалось не броском, а
		# обстрелом
```


### "morale": 55.0,

```
		# Было 40 — стая срывалась, потеряв около десятой части состава, то
		# есть практически от первого же залпа. Порог паники общий для всех, и
		# хрупкость рода войск выводится ровно из этого числа (см. разбор
		# морали в CLAUDE.md): 55 отодвигает срыв примерно к четверти потерь —
		# гнолл остаётся ломким, но перестаёт паниковать постоянно
```


### "name_genitive_plural": "наездников",

```
		# Родительный падеж множественного числа — для названия отряда
		# по рангу («Отряд ветеранов КОПЕЙЩИКОВ», см. veteran_rank_name)
```


### "movement_speed": 4.6,

```
		# Было 3.4 — это 1.55 от пешего гоблина (2.2) и 1.7 от людской пехоты
		# (2.0). Замер (qa_cavalry, блок A) показал, что и в чистом поле, и в
		# плотном строю всадник идёт РОВНО свою скорость, без единого троттля:
		# «едут вровень с пехотой» было впечатлением от самого числа, а не от
		# кода. Поэтому лечится оно числом же.
		#
		# 4.6 — это 2.1 от пешего гоблина и 2.3 от людского копейщика: конница
		# обгоняет строй вдвое, то есть успевает обойти его с фланга за то
		# время, пока фаланга разворачивается
```


### "charge_push_mult": 5.5,

```
		# Одного push_force для этого мало, и вот почему. Смещение считается
		# как clampf(разница напора × 0.15, 0.04, 0.4) — то есть ПОТОЛОК 0.4 м
		# срабатывает уже при разнице в три единицы. У кабана против копейщика
		# разница около четырнадцати: пятнадцать в конфиге и три в конфиге
		# давали ОДНО И ТО ЖЕ смещение, потолок съедал всю разницу.
		#
		# charge_push_mult применяется ПОСЛЕ потолка — это и есть заказанное
		# «вдвое». push_every переводит толчок с «раз в десять ударов» на
		# «через один»: при кулдауне 1.0 с прежний строй продавливался на семь
		# сантиметров раз в десять секунд, чего на экране не видно вовсе
		# 3.5, а не 2.0: замер прорыва (qa_cavalry, блок E — три отряда кабанов
		# против трёх отрядов копейщиков) показал, что при двойке стадо в строй
		# ВХОДИТ, но насквозь не проходит — центр массы застревал в двух метрах
		# перед исходной линией. Заказ владельца прямой: «прорывая центр фаланги
		# насквозь», и мерится он именно этим числом
```


### "push_every": 1,

```
		# Толчок на КАЖДЫЙ удар. При «через один» кабан с кулдауном 1.0 с давил
		# раз в две секунды — на экране это не напор, а переминание
```


### "charge_range": 14.0,   # спринт 15: разгон длиннее — иначе 10 м пробега не набрать (было 

```
		# До этого «ударная конница» была только продавливанием: кабан подъезжал
		# обычным шагом и начинал теснить. Самого УДАРА не было вовсе — то есть
		# не было того единственного мгновения, ради которого конницу и держат.
		#
		# Разгон начинается за десять метров: этого хватает, чтобы игрок успел
		# увидеть рывок, и мало, чтобы стадо разгонялось через полкарты.
		# Ускорение 1.8 поверх и без того вдвое большей скорости — на экране
		# это бросок, а не «пошёл побыстрее».
		#
		# УРОН СЧИТАЕТСЯ ОТ ЗАПАСА ЖЕРТВЫ, а не от силы удара седока: сминает
		# не топор, а полтонны кабана на скорости. Половина — заказ владельца.
```


### "charge_min_runup": 10.0,            # спринт 15: ТОЛЬКО после 10 м бега по прямой

```
		# Жалоба владельца: «эффект разлёта срабатывает дважды за одно
		# соприкосновение, в том числе когда юниты уже сошлись в плотном бою
		# без разгона». Так и было: натиск взводился по одной лишь ДИСТАНЦИИ
		# до цели, а в свалке цель меняется каждые пару секунд, и новая нередко
		# оказывается за десять метров — то есть условие взвода выполнялось
		# снова, стоя на месте.
		#
		# Теперь мало оказаться далеко: надо ПРОЙТИ это расстояние. Всадник
		# запоминает точку, где начал разгон, и удар засчитывается, только если
		# он реально проехал charge_min_runup метров. В плотном бою столько не
		# проехать — и повторный разлёт не случается вовсе.
		# 5 м из заказанного диапазона «5-10», а не верхняя его граница: разгон
		# начинается за charge_range (10 м), а касание происходит примерно за
		# 2.5 м — то есть на полный проезд остаётся около семи с половиной
		# метров. Порог в шесть метров оставлял полтора метра запаса и на
		# стенде срабатывал через раз: цель успевала подойти сама, всадник
		# взводил разгон позже десяти метров и не добирал своего
```


### "squad_weight": 2.0,

```
		# Заказ владельца. Конный отряд численно меньше пехотного, и любой
		# счёт «по головам» считал его втрое слабее, чем он есть
```


### "charge_breakthrough": 1.8,

```
		# Ноль вернул бы прежнее «ударил и встал в первой шеренге»
```


### "charge_row_kill": 1,

```
		# Треть своего запаса за попытку смять фалангу в лоб. Числом это ровно
		# то же требование, что и «отбрасывания нет»: фронт копейщиков должен
		# быть местом, куда конницу гнать НЕЛЬЗЯ, а не местом, где она теряет
		# часть эффективности
		# Первая ячейка по ходу — гибель на месте (charge_row_kill бойцов),
		# вторая — отлёт, падение и charge_row2_frac максимального запаса,
		# третья и дальше — плавный толчок назад. Глубина ряда — charge_row_depth м
```


### "troll": {

```
	# Дубина накрывает брызгами всех рядом с целью (goblin_config.TROLL_SPLASH_*),
	# серия из TROLL_SWINGS ударов сменяется одышкой. Таран — те же charge_*,
	# что у кабанов, только тяжелее: разгон с 16 м, брызги 3.5 м, откидывает
	# на 4 м и вклинивается на 2.5 м. Копья держат навал и здесь, но берут с
	# тролля лишь малую долю (charge_counter_frac): пика для него — заноза
```


### "charge_push_mult": 6.6,

```
		# НА 20 % СИЛЬНЕЕ КОННИЦЫ (заказ 09.09.2026): множитель применяется ПОСЛЕ
		# потолка шага (см. _apply_push), поэтому «сильнее» — это он, а не
		# push_force: у кабана 5.5, у тролля 5.5 × 1.2
```


### "health": 55.0,

```
		# Заказ владельца: запас и удар на уровне базового гнолла (55 / 4),
		# бьёт топором/ножом, отвечает на удар, агрится сам и идёт в атаку по
		# ПКМ. Боевым отрядом он при этом НЕ считается (Unit.is_combatant):
		# ни клича, ни реплик, ни голосового «все» рабочим не положено
```


### "walk_speed_empty": 2.5,             # скорость БЕЗ груза, м/с

```
		# носит скромно; прокачка (worker 1a-5d) возвращает и превосходит
		# прежние числа (3.0 / 1.9 / 5 с / 6 ед.)
```


### "walk_speed_loaded": 1.6,            # скорость С грузом, м/с

```
		# Половина цикла добычи — это ходка, и именно ею экономика упирается в
		# кузницу: ветка «Крепкая обувь / Торные тропы» окупается сразу
```


### "gather_time": 6.0,                  # секунд на один цикл добычи

```
		# Заказ владельца: «игрок должен почувствовать нужду в улучшениях уже
		# на второй минуте». Полный цикл с ходкой — около десяти секунд, то
		# есть ОДИН рабочий приносит ~36 единиц в минуту. Вся арифметика темпа
		# (за сколько копится первое исследование) выведена из этого числа —
		# см. docs/BALANCE_MATH.md, раздел «Экономика»
```


### const STANCE_ATTACK  := "attack"

```
## Стойка переключается у КАЖДОГО юнита выделенной горячей группы и читается
## вживую при каждом ударе/шаге — как и бонусы кузницы.
##
##   push_mult         — множитель силы толкания (0.0 = пуш полностью выключен)
##   push_resist       — множитель ВХОДЯЩЕГО толчка (0.5 = отбрасывает вдвое слабее)
##   attack_speed_mult — множитель СКОРОСТИ атаки (кулдаун делится на него)
##   move_speed_mult   — множитель СКОРОСТИ ХОДЬБЫ. У обороны 0.65 (штраф −35%):
##                       строй со щитами и опущенными копьями идёт шагом. Раньше
##                       это число жило константой Unit.PHALANX_ATTACK_FACTOR и
##                       не действовало на бегу — боец в обороне, которому дали
##                       двойной ПКМ, разгонялся до 1.4 базовой, то есть БЫСТРЕЕ
##                       обычной ходьбы. Теперь штраф стойки применяется всегда
##   morale_mult       — множитель морали
##   defense_bonus     — плоская добавка к защите
##   holds_ground      — true: юнит не преследует цель и не подаётся вперёд
##                       при толкании (стоит намертво на своём месте)
##   lock_position     — true: ПОЗИЦИЯ ЗАЛОЧЕНА НАГЛУХО. Ни приказ на движение,
##                       ни приказ атаки, ни смыкание дыр в своём строю не
##                       сдвигают бойца ни на сантиметр; разрешён только разворот
##                       (facing) и удар по тому, кто сам вошёл в дальность.
##                       Единственное исключение — СМЫКАНИЕ РЯДОВ отряда
##                       (Unit.allow_reform_move): без него потери и проход
##                       союзников оставляли бы в строю вечные дыры
```


### const PUSH_GLOBAL_SCALE := 0.35

```
## 0.35 = 35% от прежней силы: бойцы больше не разлетаются от каждого тычка,
## шеренга давит плавно. Крутить баланс толкания следует ЗДЕСЬ.
```


### "lock_position":     false,

```
		#
		# Жалоба была: «в обороне крайние копейщики срываются с места и идут
		# вперёд на врага». Первым решением стал ГЛУХОЙ ЗАМОК (true) — не
		# двигаться вообще. Он закрыл жалобу и тут же создал другую: фаланга
		# перестала ходить строем ПО ПРИКАЗУ ИГРОКА, а это её штатная работа.
		#
		# Настоящая причина выбега оказалась не в том, что фаланге вообще
		# разрешено двигаться, а в ТОЧКЕ, куда её посылали: приказ атаки давал
		# КАЖДОМУ бойцу одну и ту же цель — координату врага. Строй честно
		# сходился в неё, то есть схлопывался, и первыми это видно на флангах
		# (им идти дальше всех). Лечится это точкой, а не запретом движения
		# (см. Unit.command_attack и _phalanx_march).
		#
		# Ручка оставлена: поставьте true, если понадобится «стоять намертво».
		# Стенды читают её из конфига и поедут за ней сами
```


### const ARMOR_SOFTNESS := 25.0

```
## БЫЛО ВЫЧИТАНИЕ: `урон − сумма_защиты`, с полом в MIN_DAMAGE_FRACTION. У
## вычитания два порока, и оба видны в игре:
##
##   1. ОНО СВЕРХЛИНЕЙНО. У копейщика с уроном 14 первые три очка брони
##      снимают 21 % урона, следующие три — уже 27 % от остатка, а на восьми
##      очках (броня 3 + стойка «оборона» +5) сквозь проходит 43 %. Ещё пара
##      исследований — и фаланга становится неубиваемой. Это и есть «юниты
##      при перекачке превращаются в терминаторов».
##   2. ОНО ЗАВИСИТ ОТ ВЕЛИЧИНЫ УДАРА. Одна и та же броня режет 60 % урона
##      лучника и 25 % урона рыцаря, то есть цена брони скачет от того, кто по
##      тебе бьёт. Балансировать такое нельзя.
##
## СТАЛО ДОЛЕВОЕ СНИЖЕНИЕ С ЗАТУХАНИЕМ:
##
##     прошло = урон × ARMOR_SOFTNESS / (ARMOR_SOFTNESS + защита)
##
## Свойства, ради которых оно и выбрано:
##   • НУЛЯ НЕ БЫВАЕТ НИКОГДА, при любой сумме защиты — бессмертия нет по
##     построению, а не по страховочному порогу;
##   • каждое следующее очко брони дешевеет: с 25 первое очко стоит 3.8 %
##     входящего урона, десятое — 1.1 %, двадцатое — 0.6 %. Перекачка перестаёт
##     быть выгодной сама собой, потолки не нужны;
##   • цена очка брони ОДИНАКОВА против любого оружия — доля не зависит от
##     величины удара.
##
## ARMOR_SOFTNESS — «во сколько очков защиты обходится половина урона»: при 25
## броня 25 режет ровно 50 %. Число подобрано под шкалу брони 0..8 у базовых
## войск и 0..20 у полностью прокачанного ветерана: 8 → −24 %, 20 → −44 %.
## Меньше — и броня станет решать всё; больше — и её перестанут замечать.
```


### const ARMOR_MIN_TOTAL := -12.0

```
## Насколько ОТРИЦАТЕЛЬНАЯ защита (пробитие брони, дебаффы) вправе усилить
## входящий урон. Без предела формула при защите −25 делит на ноль
```


### static func damage_after_armor(amount: float, total_defense: float) -> float:

```
## Урон после брони. ЕДИНСТВЕННОЕ место, где живёт эта формула: и бой, и
## стенды, и панель статов обязаны звать её, а не повторять арифметику
```


### const MORALE_MAX := 100.0

```
## Мораль — ЧИСЛО ОТРЯДА, а не бойца: паникует и бежит отряд целиком, поодиночке
## этого не бывает. Стартовое значение — средняя мораль состава (см. STATS)
## с множителем стойки; дальше оно только меняется событиями боя.
##
## ЧТО ЕЁ РОНЯЕТ:
##   • гибель своего — MORALE_LOSS_PER_DEATH за каждого павшего, доля от
##     ПОЛНОГО штата (отряд из шестидесяти теряет за одного меньше, чем отряд
##     из десяти, — и это верно: в толпе смерть соседа заметна слабее);
##   • попадание по отряду — MORALE_LOSS_PER_HIT, крохи, но непрерывно.
## ЧТО ЕЁ ПОДНИМАЕТ:
##   • время вне боя — MORALE_REGEN_PER_SEC;
##   • свои победы: гибель ЧУЖОГО от руки отряда (MORALE_GAIN_PER_KILL);
##   • легендарный отряд рядом (см. LEGEND_AURA_*).
```


### const MORALE_LOSS_PER_DEATH := 40.0

```
## «выбило пятую часть отряда — минус MORALE_LOSS_PER_DEATH × 0.2 × штат»
## ЧИТАЕТСЯ ТАК: «выбило ДОЛЮ отряда — снялась та же доля от этого числа».
##
## «Паника от потерь должна срабатывать не на 50 % отряда, а когда остаётся
## буквально три-семь моделек». При девяноста копейщик срывался на 39 %
## потерь, лучник на 17 % — то есть отряд разбегался, будучи ещё боеспособным.
## Сорок сдвигает срыв ПО МОРАЛИ далеко за половину состава, а «почти всех
## выбили» ловит теперь отдельное и прямое условие — PANIC_LAST_MEN.
## Разделение намеренное: мораль отвечает за «страшно», счёт живых — за
## «нас почти не осталось», и мерить второе через первое значило бы подгонять
## одно число под два разных требования
```


### const MORALE_GAIN_PER_KILL := 2.0

```
## Прибавка за убийство. Намеренно МАЛЕНЬКАЯ: в равном бою обе стороны
## набирают её поровну, и решает всё равно размен потерями. Большое число
## (пробовали 12) делало панику невозможной вовсе — отряд отыгрывал мораль
## быстрее, чем терял людей
```


### const PANIC_THRESHOLD := 0.30

```
##   • мораль упала ниже PANIC_THRESHOLD от максимума;
##   • мораль стала вдвое ниже морали того отряда, с которым идёт бой.
```


### const PANIC_LAST_MEN := 6

```
## Заказ владельца: срыв от потерь наступает, когда в отряде остаётся три-семь
## моделек. Это условие принципиально НЕ выражается через мораль: мораль —
## величина ДОЛЕВАЯ (доля потерь от штата), а здесь требование АБСОЛЮТНОЕ
## («осталось шестеро»), и у отряда из шестидесяти и из двенадцати оно
## наступает на совершенно разных долях. Через долю его пришлось бы подбирать
## отдельно каждому роду войск — то есть завести таблицу там, где хватает
## одного числа.
```


### const PANIC_LAST_MEN_MIN_ROSTER := PANIC_LAST_MEN * 2

```
## ЗАЩИТА ОТ САМОСРЫВА МАЛЫХ ОТРЯДОВ. Счёт живых применяется только к тем, кто
## изначально был ЗАМЕТНО крупнее порога: иначе отряд из шести человек (последки
## из барака, огрызок после боя, рабочий-одиночка) паниковал бы в тот же миг,
## как вступил в бой, не потеряв ни одного бойца
```


### const PANIC_STUN_SEC := 14.0

```
## Сколько секунд отряд «дуплится»: не слушает приказов, не атакует, броня
## срезана. Заказ владельца — двадцать секунд
## Было 20 с. Жалоба со скриншотом: в эндшпиле сотни паникующих копятся у
## края карты и висят там ступором почти полминуты. Двадцать секунд — это по
## меркам боя вечность: отряд либо добивают, либо он успевает уехать к
## границе и застрять. Четырнадцать оставляют потерю управления заметной,
## но не превращают беглецов в постоянное население края карты
```


### const PANIC_CONE_DEG := 30.0

```
## Разброс точек бегства был ПОЛНЫМ КРУГОМ вокруг общей точки: направление у
## каждого своё на все 360°. На экране это читается как «толпа сжалась и
## топчется», потому что половина отряда бежит НАВСТРЕЧУ своей же второй
## половине. Теперь разброс — конус ±PANIC_CONE_DEG вокруг вектора ОТ угрозы:
## отряд уходит в одну сторону, но веером, а не колонной
```


### const PANIC_EDGE_MARGIN := 18.0

```
## Точка бегства зажималась границей мира (`land_target` → `clamp_to_map`), и
## отряд, чей вектор бегства смотрит наружу, получал точку РОВНО НА КРОМКЕ —
## все до одного. Дальше туда же его возвращал подзыв связности, и у края
## копилась плотная колонна (скриншот владельца). Теперь бегущий держится не
## ближе PANIC_EDGE_MARGIN от кромки, а вектор, смотрящий наружу, ЗАВАЛИВАЕТСЯ
## ВДОЛЬ границы — беглец уходит по краю, а не втыкается в него
```


### const PANIC_FLEE_DIST := 20.0

```
## «ПРОЧЬ ОТ ВРАГА»: заказ владельца — «направление паники должно быть
## хаотичным, случайный вектор во все стороны, а не только вверх». Разбор,
## почему «хаотично» здесь не значит `randf()`, — в GameManager._start_panic
```


### const PANIC_SPREAD := 6.0

```
## ПОПРАВКА ВЛАДЕЛЬЦА: «бойцы одного отряда разлетаются слишком далеко друг от
## друга; ограничь максимальный радиус разбега от центра отряда».
##
## До поправки радиус разбега РАВНЯЛСЯ дальности бегства: каждый боец получал
## точку на диске радиусом PANIC_FLEE_DIST вокруг места срыва, то есть двое с
## противоположных краёв расходились на сорок метров (замер зондом qa_panic:
## габарит отряда 37.47 м при двенадцати бойцах — «на пол-экрана»).
##
## Теперь это ДВЕ РАЗНЫЕ величины: отряд убегает на PANIC_FLEE_DIST ОТ УГРОЗЫ
## как целое, а рассыпается внутри круга PANIC_SPREAD. Направления при этом
## остаются разными у каждого — иначе на экране опять получится строевой отход,
## от которого владелец уже отказывался однажды
```


### const PANIC_REGROUP_MULT := 1.6

```
## Во сколько раз дальше PANIC_SPREAD боец должен уехать от центра отряда,
## чтобы его позвали обратно. Не единица: на самой границе подзыв срабатывал бы
## постоянно и дёргал бы бойца каждый такт морали
```


### const PANIC_RECOVER_MORALE := 0.55

```
## Мораль, с которой отряд выходит из ступора. Не ноль и не максимум: иначе он
## либо паникует повторно в ту же секунду, либо становится свежим как из замка
```


### const LEGEND_AURA_RADIUS := 28.0

```
## Присутствие золотого штандарта рядом даёт союзникам +50 % морали и снимает
## часть морали у НОВОБРАНЦЕВ противника (у отряда без единой лычки)
```


### const LEGEND_PIERCE_FRAC := 0.30      ## «Пробитие брони»: игнорирует 30 % защиты

```
## Перк — это ПРАВИЛО, а не цифра в шаблоне модификаторов: иммунитет к панике
## числом не выразить. Поэтому у седьмого уровня выборы помечены id вида
## "legend_*", а код спрашивает GameManager.squad_has_perk(sid, id).
## Числа самих правил живут здесь.
```


### const SMITH_ICONS_DIR := "res://assets/factions/humans/icons/buildings/icons_for_smith/"

```
## Здесь крутится ВСЁ по зданиям: запас жизни, темп стройки, цена, габарит.
## Значения читаются каждым зданием в _ready() и рабочими при заказе стройки —
## код трогать не надо.
##
##   name             — подпись в интерфейсе
##   max_hp           — запас жизни постройки
##   build_time       — СЕКУНДЫ работы ОДНОГО рабочего до готовности.
##                      Артель ускоряет: n рабочих → ×(1 + (n-1)·0.6)
##                      (см. ConstructionSite.BUILDER_SPEEDUP).
##                      0.0 = здание не строится рабочими (Замок ставится сразу)
##   size             — габарит коллизии и спрайта, метры
##   cost_wood / cost_gold / cost_stone — цена (0.0 = ресурс не тратится)
##   worker_buildable — true: кнопка появляется на панели рабочего сама
##   icon             — картинка для кнопки и карточки характеристик
# ИКОНКИ КУЗНИЦЫ (улучшения и бонусы ветеранства)
#
# ЗАЧЕМ ОТДЕЛЬНАЯ КОНСТАНТА. Полный res://-путь, вписанный в каждую строку
# конфига, ломается от любого переезда папки — и ломается МОЛЧА: load()
# возвращает null, кнопка остаётся без картинки, ошибки нет. Ровно так и
# случилось: в VETERAN_LEVEL_BONUSES лежал путь
#   res://assets/factions/humans/buildings/icons/buildings/icons_for_smith/icon_sword
# где «buildings/icons» переставлены местами относительно настоящего
# «icons/buildings», да ещё и без расширения .png.
#
# Теперь в конфиге пишется ТОЛЬКО имя файла ("icon_sword.png"), а каталог
# задаётся здесь одной строкой. Переезд папки — правка одной константы.
```


### const DEFAULT_CLUSTER_GOLD  := 10000.0

```
## Куча — ЕДИНЫЙ логический объект с общим запасом (см. MineCluster), поэтому
## баланс месторождения меняется здесь ОДНОЙ ЦИФРОЙ. Раньше запас складывался из
## запасов отдельных камушков (Main.PIECE_CLASSES.amount × состав шаблона): чтобы
## «добавить золота», приходилось править раскладку кучи, то есть её ВНЕШНИЙ ВИД,
## и наоборот — правка вида молча меняла баланс.
##
## Числа общие на все кучи своего типа: разброс по величине месторождений — это
## отдельное решение, и делать его случайным побочным эффектом того, сколько
## кусков влезло мимо воды и пятачка базы, точно не стоит
```


### static func cluster_stock(kind: String) -> float:

```
## Constants.RESOURCE_*: конфиг — самостоятельная таблица и про перечисления
## движка ничего не знает, сопоставление делает вызывающий (Main)
```


### static func smith_icon_path(icon_name: String) -> String:

```
## Принимает и готовый res://-путь: тогда возвращает его как есть — старые
## записи конфига и чужие иконки продолжают работать без правок.
## Расширение можно не писать: ".png" подставится само
```


### static func smith_icon(icon_name: String) -> Texture2D:

```
## Текстура иконки кузницы. НИКОГДА не роняет интерфейс: нет файла или опечатка
## в имени — вернётся null, а в консоль уйдёт предупреждение с путём, по
## которому искали. Вызывающий рисует кнопку с подписью вместо картинки
```


### const PLAYER_STARTING_RESOURCES := {

```
# СТАРТОВЫЕ РЕСУРСЫ — ОТДЕЛЬНЫМИ БЛОКАМИ ДЛЯ ИГРОКА И ДЛЯ ИИ
#
# Раньше эти числа были ЗАШИТЫ в ResourceManager.reset_resources() — то есть
# балансная настройка жила в коде, а не в балансной таблице, и владельцу
# приходилось править автозагрузку, чтобы поменять стартовый запас.
#
# ДВА БЛОКА, А НЕ ОДИН. До этого запас у ИИ был выражен как
# `resources[ENEMY] = resources[PLAYER].duplicate()` — «равный старт» намеренно,
# потому что до этого у ИИ было на 50 дерева и 50 золота больше, и он закладывал
# постройку раньше, что читалось как «ресурсы падают с неба». Раздельные блоки
# сами по себе баланс НЕ МЕНЯЮТ: значения ниже одинаковы, равный старт сохранён.
# Теперь его просто можно настроить — например, дать ИИ форы на высокой сложности,
# не трогая ни строки кода.
#
# Ключи — Constants.RESOURCE_*. Не указанный ресурс считается нулём
# (см. starting_resources), поэтому убрать позицию можно просто удалив строку
## Здесь стояли двадцать с лишним тысяч каждого ресурса — отладочные числа, с
## которыми первые полчаса партии не существует вовсе: и кузница, и бараки, и
## три исследования покупаются в первую минуту. Это и есть «улучшения
## покупаются оптом за раз» из заказа.
##
## Новый запас рассчитан на ОДНО решение: замок (300/300/300) плюс три-четыре
## рабочих (50 дерева каждый). Дальше всё добывается
## «Никогда не сбрасывай и не меняй мои настройки ресурсов в конфигурации.
## Стартовый запас по 20 000 каждого ресурса для тестирования — и больше
## никогда его не трогай». Красный стенд, который опирается на «скромный
## стартовый запас», переписывается на запас ИИ (AI_STARTING_RESOURCES), а
## эти числа не трогаются ни при каком разборе
```


### const AI_STARTING_RESOURCES := {

```
## пресет сложности множит эти числа своим коэффициентом (см.
## game_difficulty_config.gd, ключ ai_resource_mult), не трогая игрока
```


### static func starting_resources(faction: int) -> Dictionary:

```
## в блоке конфига позицию можно опустить, и она будет нулём — иначе пропуск
## строки означал бы «ключа нет вовсе», и всё, что читает склад по ключу,
## получало бы отсутствующее значение вместо нуля
```


### "worker_buildable": true,

```
		# ЗАМОК ТОЖЕ В МЕНЮ РАБОЧЕГО (заказ 09.09.2026: «все здания из цветовой
		# папки»). Первый замок партии по-прежнему ставится стартовым флоу
		# бесплатно; этот — второй и дальше, за полную цену и через стройку
```


### "goblin_hut": {

```
	# ХИЖИНА ГОБЛИНОВ. Игроку не строится (worker_buildable = false): это
	# здание третьей стороны, оно расставляется генератором деревни
```


### "cost_wood": 320.0,                       # цена: дерево

```
		# Было 1000/500/1000 — при базовой добыче в 36 единиц на рабочего в
		# минуту это двадцать минут на одни бараки. Тайминг заказан другой:
		# бараки к первой минуте, кузница ко второй (см. docs/BALANCE_MATH.md)
```


### "archery": {

```
	# СТРЕЛКОВАЯ (заказ 09.09.2026): найм лучников переехал сюда из бараков,
	# в бараках осталась пехота (копейщики и мечники). См. TRAINING["archery"]
```


### "tower": {

```
	# ОБОРОНИТЕЛЬНАЯ БАШНЯ (заказ 09.09.2026). Наследует гарнизон Замка, но
	# принимает ТОЛЬКО лучников и ровно один отряд (TOWER_GARRISON_*), обзор
	# TOWER_VISION, с лучниками внутри на площадке стоит часовой (Tower.gd).
	# Габарит подобран ПОД ВЫСОТУ КУЗНИЦЫ: Tower.png 128×256 (0.5) против
	# Monastery.png 192×320 (0.6) при коробке 4.0 — ширина 3.4 даёт рисунок
	# 7.5 м высотой против 7.3 у кузницы. Картинки стройки и руин общие
	# «домовые», на башне они рисуются в долю construction_scale / ruin_scale
```


### "max_hp": 6000.0,                         # запас жизни постройки

```
		# Башня — единственная постройка, которую нельзя обойти вниманием: в
		# ней сидят тридцать лучников, и пехота не может достать их иначе, чем
		# развалив стены. Прежние 1200 запаса сносились одним отрядом мечников
		# за полминуты, а цена (150/60/220) была ниже бараков
```


### "construction_scale": 1.0,                # своя картинка стройки

```
		# Прежние 0.6 ужимали ОБЩУЮ «домовую» картинку: она широкая и низкая,
		# а башня узкая и высокая, и в полный размер пепелище лежало шире
		# самой башни. Со своими спрайтами (game_settings.PROCESS_SPRITES,
		# строка "tower") ужимать нечего — они нарисованы ровно по башне
```


### "cost_wood": 450.0,                       # цена: дерево

```
		# без неё исследований нет вовсе, и держать её за десятиминутным
		# барьером значит выключить всю ветку улучшений на полпартии
```


### "worker_buildable": false,                # кнопки на панели рабочего нет

```
		# Механика ничейных рудников на плато (Mine.gd, захват пехотой) ЖИВА и
		# не тронута: кнопка убрана только у рабочего, чтобы игрок не ставил
		# рудники сам. Меню рабочего целиком выводится из этого поля
		# (GameManager.worker_buildings), поэтому иконка исчезает сама
```


### "icon": "res://assets/factions/humans/icons/hud/Gold_Resource.png",

```
		# «выбираю вышку, строится домик»: кнопка с башней вела на рудник.
		# Своей картинки у рудника в паке нет; иконка — золото из HUD
```


### "house2": {

```
	# ДОМА 2 И 3 — ТОТ ЖЕ ДОМ С ДРУГОЙ КАРТИНКОЙ (заказ 09.09.2026: в меню все
	# три). Числа одинаковые намеренно: разный вид, один баланс. Все три дают
	# еду (HOUSE_FOOD_*) и слоты лимита (HOUSE_WORKER_SLOTS / HOUSE_SQUAD_SLOTS)
```


### "sheep_pen": {

```
	#
	# Ограда лежит ПЛАШМЯ, а камера смотрит под 45°: глубина на экране
	# сжимается в sin(45°) ≈ 0.71 раза. Коробка 10 × 7.5 повторяла пропорцию
	# рисунка 4 : 3 В МИРОВЫХ метрах и потому выходила на экран как 4 : 2.1 —
	# ровно тот «сплюснутый» вольер со скриншота владельца. Чтобы рисунок
	# читался своей пропорцией, глубина обязана быть БОЛЬШЕ ширины:
	# Z = 0.75 · X / sin(45°) = 1.06 · X.
	#
	# И ВДВОЕ МЕНЬШЕ (заказ спринта 14): X 10 → 5, отсюда Z = 5.3.
	# Высота 1.4 — только для формы попадания луча мыши.
	#
	# `instant_build` = true: ресурсы списываются в момент размещения, а на
	# карту встаёт готовая ограда — ни площадки, ни артели, ни ожидания.
	# Городить вокруг плетня стройплощадку размером с замок было нечем
	# оправдать: это забор, а не крепость
```


### "troll_lair": {

```
	# ЛОГОВО ТРОЛЛЯ — мёртвое дерево третьей стороны (TrollLair.gd). Габарит
	# под тролля: рисунок Dead Tree 384×320 при коробке 12 м выходит ~11 м
	# высотой, то есть в полтора тролльих роста
```


### const HOUSE_FOOD_INCOME   := 2.0   # сколько еды даёт один дом за начисление (4 → 2, 10.09

```
## ДОМ — ПАССИВНЫЙ ИСТОЧНИК ЕДЫ У ЛЮДЕЙ (у орды это хижина); второй — мясо
## украденных овец (Worker.command_steal_sheep)
```


### const FOOD_UPKEEP_SQUAD_PER_SEC  := 0.1

```
## 2.5 еды/с, и этого хватает РОВНО на 30 рабочих и 15 боевых отрядов:
## 15 × 0.1 + 30 × (1/30) = 2.5. Отряд копейщиков ест 60 еды за 10 минут —
## ориентир владельца. Стережёт qa_food_economy A1 (тождество из констант).
## Кто больше — еда кончается: склад пуст, и мораль отрядов вне боя не
## восстанавливается, а тает до STARVE_MORALE_FLOOR (выше порога паники —
## голод ослабляет, а не разгоняет армию). Красный ИИ содержание не платит
## (у него нет ни домов, ни кражи овец в плане) — список FOOD_UPKEEP_FACTIONS
```


### const SHEEP_PEN_CAPACITY  := 20

```
## CASTLE_GRAZE_RADIUS, удвоение раз в SHEEP_BREED_CASTLE_SEC. ЗАГОН поднимает
## потолок на SHEEP_PEN_CAPACITY голов и удваивает стадо ВДВОЕ ЧАЩЕ
## (goblin_config.SHEEP_BREED_PEN_SEC) — в этом и вся его ценность.
## Потолок считается ПО ПРИВЯЗКЕ овцы (Sheep.pen / Sheep.keep), а не по кругу
## на карте: круги двух соседних загонов пересекались бы, и одна овца
## засчитывалась бы обоим
```


### const POP_LIMIT_ENABLED   := true

```
## Каждый достроенный дом даёт игроку HOUSE_WORKER_SLOTS рабочих и
## HOUSE_SQUAD_SLOTS боевых отрядов ЛЮБОГО рода. База (POP_BASE_*) — то, что
## есть без единого дома: иначе первого рабочего нанять было бы негде.
## Считаются ЖИВЫЕ плюс ЗАКАЗАННЫЕ (очередь найма): иначе десять заказов
## подряд обходили бы лимит. Действует ТОЛЬКО на игрока: у красного ИИ свои
## потолки (ai_start_army_limit), а орда домов не строит. Включает партия
## (Main.start_game); стенды, собирающие сцену руками, его не видят —
## см. GameManager.pop_limit_enabled
## Партия открывается БЕЗ крепости и с пятью рабочими, поэтому база — ровно
## эта бригада: пять рабочих и ни одного боевого отряда. Первая же крепость
## даёт +CASTLE_WORKER_SLOTS рабочих и +CASTLE_SQUAD_SLOTS отрядов, дальше
## растут дома. Считаются ВСЕ живые крепости (вторая даёт столько же): своей
## ветки «только первая» здесь нет намеренно — она была бы правилом, которого
## игрок не видит
```


### const TOWER_VISION           := 35.0

```
## Обзор башни в тумане — выше стрелка (у лучника 20 × VISION_MULT = 32 м)
```


### const TOWER_VISIBLE_ARCHERS  := 10

```
## В башню входит ОТРЯД (тридцать человек, SQUAD_SIZE_ARCHERS), но на настиле
## стоят и стреляют только первые TOWER_VISIBLE_ARCHERS — остальные считаются
## резервом ВНУТРИ: их не видно, они не стреляют и подставляются под стрелы
## последними (см. Tower.take_damage). Огонь тоже ведут только видимые: тридцать
## стрел с одной башни в залп — это не башня, а батарея
```


### const TOWER_RETARGET_SEC     := 0.25

```
## обзор башни (TOWER_VISION) шире дальности лука, но стрела летит не дальше
## attack_range самого лучника — разворот от первой версии с 35 м (заказ
## владельца). Отдельной константы дальности у башни поэтому нет
## Как часто башня ищет новую цель, когда старой нет или она ушла
```


### const SQUAD_SIZE_SPEARMEN  := 60   # копейщики

```
## Одна цифра управляет всем: сколько бойцов выходит за один заказ, что
## считается «полным отрядом» у ИИ и до какого числа отряд будет пополняться.
## Меняйте здесь — ни здания, ни HUD, ни ИИ править не нужно.
```


### const SQUAD_SIZE_MONKS     := 1

```
## МОНАХ — ОДИНОЧНЫЙ ЮНИТ (заказ владельца, 09.09.2026): заказывается в замке
## по одному, у игрока не больше MONK_LIMIT живых и заказанных разом
```


### const BIG_GOBLIN_HORDE_SQUAD := 100   # = goblin_config.SQUAD_SIZE.goblin_spearman

```
## Отдельным файлом конфиг орды сюда тянуть нельзя (unit_stats_config грузят
## стенды без дерева сцены), поэтому размер стандартного отряда орды здесь
## назван числом и сверяется стендом с goblin_config.SQUAD_SIZE
```


### const BIG_GOBLIN_HP_SQUADS := 0.5     # 13.09.2026: запас порезан вдвое (было 1.0)

```
## Единица — буквальное прочтение заказа; уменьшать правкой ЭТОГО числа
```


### const BIG_GOBLIN_SPEED := 2.2 * 0.7

```
## Шаг: −30 % от гоблинского 2.2 (заказ). Второе прочтение из того же
## письма — «вдвое медленнее простого гоблина» (1.1) — даёт другое число;
## взято первое, оно названо точным процентом
```


### const MONK_LIMIT           := 3

```
## ТРОЕ (заказ владельца 10.09.2026): один лекарь на всю армию покрывал один
## участок фронта, и его гибель означала конец лечения до конца партии
```


### const MONK_HEAL_RADIUS     := 10.0

```
## Радиус ПОИСКА раненого (заказ владельца, спринт 15: «в радиусе 10 метров»)
```


### const MONK_CAST_RANGE      := 3.0

```
## Дистанция применения заклинания. Монах ПОДХОДИТ на неё и только тогда
## лечит: «подходит на дистанцию применения, проигрывает анимацию каста».
## Меньше радиуса поиска — иначе подходить было бы некуда и незачем
```


### const MONK_HEAL_LEASH      := 14.0

```
## Насколько цель вправе отойти, прежде чем монах её бросит. Больше радиуса
## поиска намеренно: раненый, отошедший на шаг за черту поиска, не должен
## отменять уже начатое лечение — иначе монах дёргается между целями
```


### const MONK_STEP_SEC        := 0.6

```
## Не чаще этого монах переиздаёт приказ идти к своей цели. Цель движется,
## и приказ на каждый такт будил бы бойца шестьдесят раз в секунду
```


### const MONK_HEAL_SEC_PER_MAN := 14.0

```
## «Благодать» (весь отряд разом) режет темп каждого вдвое (MONK_AOE_RATE) —
## раненый отряд лечится 25-30 с, а не за пару секунд
```


### const MONK_SPACING := 5.0

```
## Два монаха в одной точке кроют один фланг: свободный монах отходит от
## соседа-монаха на MONK_SPACING
```


### const MONK_RES_SEC := 20.0

```
## Павший поднимается с MONK_RES_HP запаса за MONK_RES_SEC канала (нарочно
## долго: баланс), не чаще раза в MONK_RES_COOLDOWN, только когда рядом нет
## раненых; «Двойное попечение» (monk_4d) режет канал в MONK_RES_PARALLEL_MULT
## и разрешает лечить во время канала
```


### const MONK_RES_SEARCH_RADIUS := 30.0

```
## тела передней шеренги лежат дальше, чем стоит монах), и воскрешение
## НЕ ЖДЁТ, пока рядом не останется раненых, — уступает лечению только
## тяжелораненому (доля запаса ниже MONK_RES_YIELD_FRAC). Так монах после
## боя поднимает ВСЕХ павших отряда — и погибших до изучения узла тоже
```


### const MONK_XP_HP_PER_KILL := 150.0

```
## Каждые MONK_XP_HP_PER_KILL восстановленных очков — «убийство» в счёт
## ветеранства; поднятый павший — MONK_RES_XP_KILLS сразу
```


### const MONK_AURA_BASE_R := 4.0

```
## MONK_AURA_TICK, действие держится MONK_AURA_HOLD_MS (чуть дольше такта)
```


### const MONK_AOE_RATE        := 0.5

```
## AOE-перк «Благодать» (forge monk_1d): лечит всех раненых в радиусе, каждого
## с этой долей одиночного темпа (заказ спринта 16: «в 2 раза медленнее»)
```


### const SQUAD_SIZE_HARD_CAP := 100

```
## Предохранитель от опечатки: заказ больше этого числа обрезается
## (Building.queue_unit). Поднимать вместе с SQUAD_SIZE_*.
## Потолок размера отряда. Поднят с 60 до 100 ради гоблинской орды: у неё в
## отряде сто копейщиков (goblin_config.SQUAD_SIZE). Людей это не касается —
## их размеры заданы своими числами и заметно ниже потолка
```


### const _VET_MONK_TEMPLATE := [

```
## Отряд копит убийства ЦЕЛИКОМ: фраг любого бойца идёт в общий счёт.
## Дошли до очередного порога — знамя отряда получает следующий грейд,
## а на его панели игроку предлагают выбрать одно улучшение из пяти.
##
## У КАЖДОГО БОЕВОГО ТИПА — СВОЙ БЛОК порогов и бонусов (запрос владельца:
## "разделить конфиг ветеранства по 4 типам, чтобы настраивать их порознь").
## Ключ верхнего уровня VET_CONFIG — unit_id, тот же, что в TRAINING/STATS.
## РАБОЧИЙ (worker) сюда НЕ входит вовсе — он полностью исключён из системы
## опыта: veteran_level_for_kills("worker", ...) и любые другие обращения с
## неизвестным/не боевым unit_id ниже просто возвращают "нет ветеранства"
## (0 / пустой массив), а не ошибку — так отсутствие рабочего в конфиге не
## обязано быть отдельной веткой в каждом вызывающем коде.
##
## Внутри каждого блока пока лежит ОДНА И ТА ЖЕ копия прежних общих чисел —
## значения ещё предстоит развести (владелец: "скопируй текущие значения как
## базовые, для будущей точной настройки"). Меняйте их здесь порознь, менять
## код нигде не нужно.
##
## Порогов ровно столько, сколько уровней: седьмой порог — последний.
## Убрать уровень = убрать число из "thresholds" И соответствующий блок из
## "bonuses" (длины должны совпадать).
##
## СЕМЬ ГРЕЙДОВ (как они выглядят над отрядом — см. VET_BANNER_TIERS ниже,
## общие для всех типов). Ранг показан ЗНАМЕНЕМ на копье знаменосца:
##   1-3 — красный вымпел: одна лычка, две, две с белым наконечником;
##   4-6 — синий «ласточкин хвост»: одна лычка, две, две с белым наконечником;
##   7   — прямоугольный штандарт с золотой бахромой и гербом.
##
## Поля бонуса:
##   id    — ключ выбора (латиницей)
##   name  — подпись кнопки
##   icon  — файл из SMITH_ICONS_DIR
##   далее — ПОЛНЫЙ ШАБЛОН МОДИФИКАТОРОВ (см. MODIFIERS): все двенадцать ключей,
##           ненужное нулём. Награда вправе давать сразу несколько — например
##           «+1 к урону и +0.2 к скорости»: обе строки уже есть, поставьте
##           числа. Бонус выдаётся КАЖДОЙ модели отряда.
##   stat / value — В ТАБЛИЦЕ ИХ НЕТ. Короткая пара выводится из модификаторов
##           при чтении (см. veteran_choices) — держать её здесь вторым
##           описанием того же числа значило бы завести источник расхождений.
##
## ВНИМАНИЕ по скорости: базовая скорость юнита ~2-3 м/с, поэтому +5 к скорости
## это очень много. Значение оставлено как заказано; если бег окажется
## неиграбельным — правьте value у выбора "speed" здесь, код трогать не надо.
## Один блок = пороги на 7 уровней + 7×5 бонусов на выбор. Ниже — четыре
## НЕЗАВИСИМЫХ литерала (не .duplicate() от одного шаблона: GDScript не даёт
## звать методы в инициализаторе const, да и независимые литералы честнее
## отражают "4 блока для будущей настройки порознь" — правка одного блока
## физически не может задеть другой)
## Монах не дерётся, и «Атака/Броня/Здоровье» ему ни к чему: на каждой
## ступени — три ауры на выбор, действующие на союзников в радиусе
## (MONK_AURA_BASE_R + аура радиуса). Броня — «Аура морали», удар — «Боевой
## клич» (только рукопашным), темп стрельбы — стрелкам, радиус — всем аурам.
## Три карточки на ступень, как у всех; лестница +1 / +2 по ступеням
```


### [

```
	# Правило матрицы, общее на все семь ступеней: у КАЖДОГО выбора ровно ТРИ
	# ненулевых параметра — один ГЛАВНЫЙ и два вторичных. Вторичные не всегда
	# в плюс: у брони это плата скоростью, у защиты — темпом удара. Именно она
	# и делает выбор выбором, а не «возьми всё подряд».
	#
	# ШАГ ПРИБАВКИ ЗАДАН ЛЕСТНИЦЕЙ, А НЕ ПОДОБРАН НА ГЛАЗ (СПРИНТ 18, «реже,
	# но мощнее»: пороги опыта ×1.3, шаг наград выше):
	#   лычки  (1-3) — +3 к главному параметру за ступень (было +2);
	#   флаги  (4-6) — +4 (было +3), плата парным параметром −2 (было −1);
	#   штандарт (7) — +4 и ПРАВИЛО (см. ниже, не тронут).
	# Вторичный запас жизни +8 / +10 (было +5 / +7), карточка здоровья +21 /
	# +30 (было +14 / +20). Итог по одной ветке: +25 к главному параметру
	# (было +13) — копейщик 14 → 39 урона; вместе с кузницей ×3.4 от базы
	# (было ×2.5). Потолок сдвинут СОЗНАТЕЛЬНО по заказу владельца («+2..+3 к
	# атаке за награду, ощутимо в бою»), а цена ему — 30 % больше убийств на
	# каждую ступень (455 вместо 350 у копейщика до штандарта) и стартовые
	# ветераны орды на грейд ниже при той же силе (goblin_config.START_SQUADS,
	# MINE_GUARD: vet 3 / picks 3 = +9, как прежние vet 4 / picks 4).
	# Разбор — docs/BALANCE_MATH.md, раздел 5.
	# ЗАКАЗ ВЛАДЕЛЬЦА: «выбор из пяти карточек создаёт информационный шум;
	# сократить всегда строго до ТРЁХ, каждая даёт РОВНО ТРИ параметра,
	# доступные статы — только Атака, Броня/Защита и Здоровье».
	#
	# БЫЛО 3/4/5 карточек по ступеням, а вторичные параметры брались из всех
	# двенадцати ключей (скорость, темп удара, мораль, напор). Правило «ровно
	# три ненулевых» соблюдалось и раньше — менялся не их СЧЁТ, а НАБОР: теперь
	# все три из одной тройки, и карточки стали сравнимы между собой. Раньше
	# игрок сравнивал «+2 атаки и +4 морали» с «+2 брони и +0.3 напора», то есть
	# величины из разных измерений; теперь вопрос один и тот же на всех
	# ступенях — во что вложить очко: в удар, в защиту или в живучесть.
	#
	# У каждой карточки один ГЛАВНЫЙ параметр в плюс, один вторичный в плюс и
	# один В МИНУС. Без минуса выбора нет вовсе — «возьми что больше нравится,
	# всё равно всё хорошее». Минус мал (единица) и всегда бьёт по тому стату,
	# который карточка не качает.
	#
	# ЛЕСТНИЦА ПРИБАВКИ ПРЕЖНЯЯ (разбор — выше): лычки (1-3) +2 к главному,
	# флаги (4-6) +3, штандарт (7) даёт ПРАВИЛО вместо цифры. Здоровье считается
	# в своих единицах (+14 и +20) — это та же доля от базы, что +2 и +3 у
	# атаки с бронёй, см. docs/BALANCE_MATH.md
```


### [

```
	# Тот же выбор из трёх, шаг главного параметра +3 вместо +2
```


### [

```
	# ЗДЕСЬ НАГРАДА — ПРАВИЛО, А НЕ ЦИФРА. Иммунитет к панике или пробитие
	# брони шаблоном модификаторов не выражаются вовсе, поэтому у каждого
	# выбора СВОЙ id вида "legend_*", а код спрашивает его напрямую:
	# GameManager.squad_has_perk(sid, "legend_pierce"). Числа самих правил —
	# в блоке LEGEND_* этого же файла.
	#
	# Три параметра у каждого перка ОСТАВЛЕНЫ НАМЕРЕННО: панель награды
	# собирает подпись из ненулевых модификаторов (см. HUD._show_vet_tip), и
	# перк без единого числа выглядел бы там пустой плашкой.
	#
	# ЛЮБОЙ отряд седьмого грейда, независимо от выбранного перка, светит
	# АУРОЙ МОРАЛИ (+50 % своим, −25 % чужим новобранцам): она привязана к
	# рангу, а не к выбору (см. LEGEND_AURA_*)
```


### const LEGEND_PERK_IDS := [

```
## ПЕРКИ — БИТ НА ПЕРК: ИХ СПРАШИВАЮТ ИЗ ГОРЯЧЕГО ПУТИ
## `GameManager.squad_has_perk` искал id ЛИНЕЙНЫМ ПЕРЕБОРОМ строк в списке
## выбранных наград (`squads[sid]["chosen"]`), а спрашивают его на КАЖДОЕ
## списание урона: `squad_armor_pierce` зовётся из `Unit.take_damage` жертвы.
## Список выбранных растёт с рангом отряда — то есть чем дольше живёт партия,
## тем дороже становится каждый удар, и сравниваются там СТРОКИ.
##
## Здесь — порядок битов. Он же и есть весь «реестр» перков: добавили строку
## сюда — перк проверяется за одно сравнение целых. ЗАБЫЛИ добавить — ничего
## не сломается: `squad_has_perk` для незнакомого id падает на прежний перебор
## (см. его шапку). Молчаливого «перка нет» этот список вызвать не может.
```


### static var _perk_bits: Dictionary = {}

```
## Таблица собирается ОДИН РАЗ при первом обращении: const-словарь GDScript в
## инициализаторе построить не даёт, а пересобирать её на каждый вопрос значило
## бы вернуть ту самую цену, ради ухода от которой всё и делается
```


### static func perk_mask_of(chosen: Array) -> int:

```
## их ловит запасной перебор в squad_has_perk
```


### static var VET_CONFIG: Dictionary = {

```
## static var, а не const: GDScript не разрешает звать методы (.duplicate) в
## инициализаторе const, а без глубокой копии все четыре ключа указывали бы
## на ОДИН И ТОТ ЖЕ вложенный массив _VET_BONUS_TEMPLATE (Array — ссылочный
## тип), и правка "attack" у Копейщиков задним числом поменяла бы то же
## число у Рыцарей/Лучников/Монахов. static-инициализатор выполняется один
## раз при загрузке скрипта, значения дальше можно менять по каждому ключу
## порознь, ничего не задевая
```


### "goblin_spearman": {"thresholds": [130, 260, 520, 780, 1040, 1300, 1690],

```
	# «сослаться на копейщика»: VET_CONFIG — static var, её правят вживую, и
	# общая ссылка означала бы, что правка гоблинам молча меняет людей
```


### const BANNER_PENNANT  := 0    ## вымпел: сужается к острию

```
## Ранг отряда показан знаменем на копье знаменосца. Таблица ниже — един-
## ственный источник правды: по ней рисуется картинка (BannerArt), по ней же
## подписывается ранг в панели отряда (HUD). Картинки в ассетах нет и не будет —
## она строится кодом (BannerArt).
##
## ЧТО ЗНАЧАТ ПОЛЯ:
##   rank        — ранг отряда в родительном падеже: «Отряд ОПЫТНЫХ копейщиков».
##                 Второе слово подставляется из STATS.name_genitive_plural того
##                 рода войск, которому знамя принадлежит (см. veteran_rank_name)
##   shape       — BANNER_PENNANT / BANNER_GUIDON / BANNER_STANDARD
##   cloth       — цвет полотнища
##   chevrons    — сколько лычек
##   chevron_dir — куда смотрят лычки: +1 в полёт (`>`), -1 к древку (`<`)
##   white_tip   — белый наконечник у свободного края
##
## ПОЧЕМУ ЦВЕТ ЛЫЧЕК И БЕЛОГО НАКОНЕЧНИКА ВЫНЕСЕН ИЗ ТАБЛИЦЫ. Он один на все
## грейды и меняться порознь не должен: разноцветные лычки превратили бы шкалу
## званий в набор непохожих значков, по которому нельзя понять, кто старше.
```


### const VET_BADGE_OUTLINE := Color(0.08, 0.08, 0.10, 0.9)

```
## ОБВОДКА ЗНАЧКА РАНГА В ПАНЕЛИ. Один тёмный контур на все грейды: значок
## лежит то на тёмном портрете, то на светлой карточке отряда, и без обводки
## любой из ярких цветов полотнища на одном из этих фонов теряется. Цвет не
## чёрный, а чуть синеватый — так он ложится в общую палитру арта
```


### static func veteran_banner_tier(lvl: int) -> Dictionary:

```
## Описание грейда знамени: то же, что лежит в таблице, плюс "key" (номер
## грейда — по нему кэшируется готовая картинка). lvl <= 0 или выше таблицы —
## зажимается в её границы, потому что «уровень выше седьмого» невозможен по
## построению (порогов ровно семь), но проверять это в каждом вызывающем месте
## незачем
```


### static func veteran_rank_name(unit_id: String, lvl: int) -> String:

```
##
## Собирается из ДВУХ конфигов, а не хранится готовой строкой на каждую пару
## «ранг × род войск»: рангов семь, боевых родов шесть, и таблица из сорока
## двух строк разъехалась бы при первой же правке одного из двух списков.
## Неизвестный род войск (в частности рабочий — он вне ветеранства навсегда)
## даёт пустую строку, а не ошибку
```


### static func veteran_badge_text(lvl: int) -> String:

```
## Панель не может показать знамя: значок там высотой в строку текста, и
## полотнище на нём превратилось бы в цветное пятно. Поэтому в интерфейсе ранг
## показан ЛЫЧКАМИ — тем единственным, что на знамени и так читается числом.
##
## ВЫВОДИТСЯ ИЗ ТОЙ ЖЕ ТАБЛИЦЫ, что и само знамя, и это главное: иначе значок и
## знамя разъехались бы при первой правке одного из них (ровно эта беда уже
## случалась с прежней шкалой, когда грейд в панели считали отдельно от
## грейда метки над отрядом).
##
## Белый наконечник получает свою черту, а не пропадает: без неё второй и
## третий грейды выглядели бы в панели одинаково, хотя это разные звания
```


### static func veteran_badge_color(lvl: int) -> Color:

```
## Цвет значка ранга. СВЕТЛЕЕ полотнища на четверть: знамя видно на траве и на
## небе, а значок лежит на тёмном портрете, и глухой бордовый на нём тонет
```


### static func _vet_thresholds(unit_type: String) -> Array:

```
## Пороги/бонусы конкретного боевого типа. Неизвестный unit_id (в частности
## "worker" — рабочие исключены из ветеранства навсегда) даёт пустые массивы,
## и все функции ниже честно возвращают "нет ветеранства", а не падают
```


### const _MOD_TO_STAT := {

```
## Награда за ветеранство описана ТЕМ ЖЕ шаблоном модификаторов, что и узел
## кузницы (см. MODIFIERS): все двенадцать ключей, ненужное нулём. Но панель,
## ИИ и старые стенды спрашивают у награды короткую пару «какая характеристика
## и на сколько», и держать её в таблице ВТОРОЙ РАЗ нельзя — два описания
## одного числа рано или поздно разъедутся. Поэтому пара ВЫВОДИТСЯ из
## модификаторов при чтении, а в таблице её нет вовсе
```


### static func veteran_choices(unit_type: String, level: int) -> Array:

```
## Каждый вариант дополняется выведенными stat/value (см. выше)
```


### for e in lst:

```
	# Дополняем ОДИН РАЗ и прямо в записи: VET_CONFIG живёт всю партию, а
	# пересобирать список на каждое открытие панели незачем
```


### static func veteran_choice_at(unit_type: String, level: int, choice_id: String) -> Diction

```
## Описание конкретного выбора НА КОНКРЕТНОМ УРОВНЕ (нужно, чтобы посчитать,
## сколько реально дал бонус: value одного и того же id по уровням разное).
## Пусто — такого id на этом уровне нет
```


### static func veteran_choice_info(unit_type: String, choice_id: String) -> Dictionary:

```
## Описание выбора по id БЕЗ привязки к уровню — для картинки и названия
## (они у одного id одинаковы на всех уровнях). Берётся первое совпадение
```


### const GARRISON_SQUAD_LIMIT   := 5     # сколько отрядов помещается в один Замок

```
## Отряд можно завести внутрь Замка: там раненые лечатся, а погибшие модели
## медленно восстанавливаются, пока отряд снова не станет полным (до squad_size).
##
## РАСЧЁТ ТЕМПА (заказ владельца): отряд из 30 человек, выбитый наполовину
## (15 потерь), доукомплектуется за 15 × GARRISON_REVIVE_SECONDS = 150 c.
## Лечение идёт параллельно с пополнением, но заметно медленнее пополнения.
```


### const GARRISON_AUTO_RELEASE  := true

```
## АВТО-ВЫХОД: отряд, у которого полный состав И полное здоровье, сам
## выкатывается из Замка наружу. false — сидит внутри до клика по слоту
```


### const CONSTRUCTION_MIN_SEC := 6.0

```
## Сколько минимум секунд видна стройплощадка, даже если build_time постройки
## равен нулю. Сейчас в BUILDINGS у ВСЕХ зданий build_time = 0 (отладочная
## настройка «строится мгновенно»), и без этого порога картинку стройки
## увидеть было бы негде. Поставить 0 — вернуть прежнее мгновенное появление
```


### const CASTLE_BUILD_SEC     := 8.0

```
## Сколько это занимает, если build_time замка равен нулю
```


### static func squad_weight(unit_id: String) -> float:

```
## СКОЛЬКО ПЕХОТИНЦЕВ «СТОИТ» ОДНА МОДЕЛЬ ЭТОГО РОДА ВОЙСК
## (заказ владельца: «1 всадник = 2 пехотинца при расчёте морали и силы
## отряда»).
##
## ЗАЧЕМ ЭТО НУЖНО. Вся бухгалтерия морали считает МОДЕЛИ: долю
## потерь от штата и число выживших (PANIC_LAST_MEN). Но отряд кабанов —
## десять моделей против шестидесяти у копейщиков, и порог «осталось
## шестеро» для него значит «потерял четверых», а для копейщиков —
## «потерял пятьдесят четырёх». Без веса конница паниковала бы от
## первых же потерь.
##
## ВЕС ЛЕЖИТ В STATS, А НЕ В КОДЕ, и это принципиально: заведётся
## второй род конницы — ему достаточно будет строки в таблице, а не
## ветки в GameManager
```


### static func squad_slots(unit_id: String) -> int:

```
## Число лежит в ТАБЛИЦЕ, а не в коде лимита: заведётся вторая элита — ей
## хватит строки. Считает GameManager.pop_squads_used
```


### var gs: Variant = _GobCfg.SQUAD_SIZE.get(unit_id)

```
	# «сколько бойцов положено отряду», — в том числе доукомплектование в
	# гарнизоне (Castle.garrison_missing). Если бы здесь осталась единица,
	# отряд гоблинов, ушедший лечиться, вышел бы обратно составом в одного
```


### const TRAINING := {

```
## Ключ первого уровня — building_id, второго — тип юнита.
##   time  — секунды до выхода отряда
##   squad — сколько бойцов в одном заказе. Ссылается на SQUAD_SIZE_* выше:
##           размер отряда задаётся ровно в одном месте
##   cols  — колонн в строю на выходе
##   cost_wood / cost_gold / cost_stone — цена заказа
## ПОЛЯ ЗАКАЗА НАЙМА:
##   cost_wood / cost_gold — цена ВСЕГО отряда, списывается в момент заказа
##   time  — секунд до выхода отряда (темп найма; сложность его множит)
##   squad — сколько моделей выходит за один заказ (см. SQUAD_SIZE_* выше)
##   cols  — ширина строя при выходе из здания, колонок
```


### "worker":   {"cost_wood":  50.0, "cost_gold":  0.0, "time":  16.0, "squad": 1, "cols": 4},

```
		# «скорость обучения» даёт кузница (bonus_train, Building.queue_unit)
```


### "warrior":  {"cost_wood":  600.0, "cost_gold": 1500.0, "time": 75.0, "squad": SQUAD_SIZE_S

```
		# ЭЛИТА ДОРОЖЕ И ДОЛЬШЕ (ребаланс 10.09.2026): втрое к золоту и вдвое
		# ко времени против прежних 100/200/30 — отряд рыцарей это событие
		# партии, а не очередная шеренга
		# Вдвое с половиной дороже тех же мечников из бараков (250/600) и
		# дольше: из Крепости выходит отряд УЖЕ С ЛЫЧКОЙ (KEEP_WARRIOR_VET),
		# то есть игрок платит не за людей, а за готовое ветеранство
```


### const SMITHY := {

```
## Оставлено для обратной совместимости: параметры самой постройки живут
## в BUILDINGS["smithy"], здесь только то, что относится к исследованиям.
```


### const DEFAULT_RESEARCH_TIME := 10.0

```
## 0.0 = мгновенная покупка (прежнее поведение).
```


### const UPGRADE_SLOTS := [

```
## Купленный апгрейд применяется МГНОВЕННО ко всем ТЕКУЩИМ и БУДУЩИМ юнитам
## указанных типов: бонусы читаются вживую при каждом ударе/шаге.
##
## Чтобы добавить своё улучшение — скопируйте любой блок и поменяйте поля:
##
##   id          — уникальный строковый ключ (латиницей, без пробелов)
##   name        — подпись на кнопке в Кузнице
##   desc        — вторая строка на кнопке (что даёт)
##   applies_to  — СПИСОК типов юнитов: ["spearman"], ["archer","warrior"]
##                 или ["all"] — всем боевым юнитам сразу
##   cost_gold / cost_wood / cost_stone — цена (0.0 = ресурс не тратится)
##   requires    — id апгрейда, который нужно купить раньше ("" = без условий)
##   research_time — СЕКУНДЫ исследования в Кузнице. Ресурсы списываются сразу,
##                 бонус применяется по готовности. Поле можно опустить — тогда
##                 берётся DEFAULT_RESEARCH_TIME; 0.0 = купить мгновенно.
##                 Кузница исследует ПО ОДНОЙ технологии за раз.
##   icon        — картинка кнопки (необязательно; пусто — на кнопке подпись)
##
## Бонусы (любой можно опустить — тогда он равен 0):
##   bonus_attack — + к урону удара
##   bonus_armor  — + к броне (плоское снижение входящего урона)
##   bonus_health — + к максимуму HP (текущим юнитам HP поднимается сразу)
##   bonus_speed  — + к скорости движения, м/с
##   bonus_push   — + к push_force (сила продавливания вражеской шеренги)
##   bonus_morale — + к морали (тоже участвует в силе толкания)
```


### const MODIFIERS := {

```
##
## ДВА ПОСЛЕДНИХ — ЭКОНОМИЧЕСКИЕ, ветка рабочего в кузнице (forge_config.UNITS.worker):
##   bonus_carry  — прибавка к грузу за одну ходку (Worker.gather_amount)
##   bonus_gather — СЕКУНДЫ ДОЛОЙ из цикла добычи (Worker._cycle_time). Знак тут
##                  такой же, как у всех: положительное значение — это хорошо,
##                  просто вычитается, а не прибавляется. Иначе балансная
##                  таблица держала бы один ключ с обратным знаком, и в ней
##                  рано или поздно ошиблись бы
## Список — ЕДИНСТВЕННОЕ место, где ключ объявляется: GameManager копит суммы,
## а всплывающие окна HUD печатают строки, перебирая именно его
## ЕДИНЫЙ ШАБЛОН МОДИФИКАТОРОВ — ОДИН НА ВСЮ ИГРУ
## Здесь перечислены ВСЕ модификаторы, какие в игре есть. Любой узел кузницы и
## любая награда за ветеранство несут ЭТОТ ЖЕ набор ключей целиком: то, что узел
## не даёт, стоит нулём. Смысл ровно в правке баланса вручную — чтобы добавить
## узлу дальность, не надо помнить, как называется ключ и куда его дописать:
## строка уже есть, в ней ноль, замените его числом.
##
## ЗНАК ВЕЗДЕ ОДИН: положительное значение — это ХОРОШО. Там, где по смыслу надо
## уменьшить (перезарядка, разброс, цикл добычи), число вычитается в коде, а в
## таблице стоит со знаком плюс. Одна колонка с обратным знаком в балансной
## таблице — гарантированная ошибка при правке.
##
##   bonus_attack    — прибавка к урону удара
##   bonus_armor     — прибавка к броне (гасит входящий урон)
##   bonus_defense   — прибавка к защите (второй слагаемый той же формулы)
##   bonus_health    — прибавка к запасу HP (выдаётся сразу и живым бойцам)
##   bonus_speed     — прибавка к скорости передвижения, м/с
##   bonus_range     — прибавка к ДАЛЬНОСТИ атаки, м
##   bonus_cooldown  — СЕКУНД ДОЛОЙ с перезарядки удара (то же, что «скорость
##                     атаки»), пол — MIN_COOLDOWN
##   bonus_spread    — НАСКОЛЬКО ПЛОТНЕЕ ложится стрельба: доля 0..1, на которую
##                     срезается случайный разброс стрелка (0.25 = «на четверть
##                     кучнее»). Ближнего боя не касается
##   bonus_push      — прибавка к напору в свалке стенка-на-стенку
##   bonus_morale    — прибавка к морали
##   bonus_carry     — прибавка к грузу рабочего за одну ходку
##   bonus_gather    — СЕКУНД ДОЛОЙ из цикла добычи, пол — Worker.MIN_CYCLE_TIME
##
## Порядок ключей здесь — это порядок строк во всплывающих окнах.
```


### const ARCHER_LEAD_FACTOR := 0.08

```
## ARCHER_LEAD_FACTOR — доля честного упреждения, которую стрелок реально
## выносит вперёд: точка = позиция цели + скорость × время_полёта × фактор.
##
## Значение 0.08 — заказ владельца (было 0.65) и оно подтверждается картинкой.
## Полное упреждение при дальности 20 м и скорости стрелы 9 м/с даёт время
## полёта ~2.2 с; бегущая цель за это время проходит ~9 м, и стрелы ложились
## ЗА ПОЛТОРА КОРПУСА ВПЕРЕДИ строя — на скриншотах владельца это «дорога из
## стрел» в чистом поле рядом с противником. Промахи по бегущему при малом
## упреждении никуда не деваются: за них отвечает разброс SCATTER_PER_SPEED,
## и это ровно то, что просили сохранить.
```


### const ARCHER_LEAD_MAX := 2.5

```
## Потолок выноса в метрах. Даже при странных скоростях цели упреждение не
## имеет права увести точку прицеливания в пустое поле
```


### const ARCHER_DRILL := [

```
## Индекс — уровень ветеранства отряда (0 = новобранцы). Значение сверх
## последней записи держится последним, как и в лестнице званий.
##   spread   — МНОЖИТЕЛЬ разброса: у новобранцев шире, у ветеранов кучнее
##   fire     — МНОЖИТЕЛЬ ТЕМПА стрельбы (кулдаун делится на него)
## Это отдельная лестница, а не bonus_spread/bonus_cooldown кузницы: те
## складываются по фракции, а эти зарабатывает КОНКРЕТНЫЙ отряд в бою
```


### const VOLLEY_MIN_SPREAD := 1.10

```
## Радиус «тучи» берётся по габариту вражеского строя, но НИЖЕ этого не падает
## никогда. Иначе залп по одиночке (и по отряду из одного бойца, и по зданию)
## схлопывался в одну точку: все стрелы входили в первого же встречного, а
## девятнадцать из двадцати списывались в уже мёртвого. Залп — это накрытие
## площади, и площадь у него есть всегда
```


### const SQUAD_COHESION_DIST := 14.0

```
## Дальше этого от центра отряда боец не остаётся: если он ничем не занят,
## отряд его подзывает (см. GameManager._cohesion_guard). Не сила и не поле —
## разовый приказ на возврат, редкий и с остыванием, как смыкание рядов
```


### const BONUS_KEYS := ["bonus_attack", "bonus_armor", "bonus_defense",

```
## HUD печатают строки. Это ровно ключи MODIFIERS, списком: перебирать словарь
## приходится в горячих местах, а массив ключей строится один раз
```


### "bonus_heal_rate", "bonus_heal_amount", "bonus_heal_radius",

```
					 # метры к радиусу. Ключи читает Monk через unit_bonus, как
					 # лучник читает bonus_range
```


### static func with_all_modifiers(src: Dictionary) -> Dictionary:

```
## Нужен для узлов, которые ещё не расписаны целиком, и для стендов
```


### const VISION_MULT := 1.6

```
## Радиус, на котором юнит раскрывает туман, — VISION_MULT дальностей его атаки,
## но не меньше VISION_MIN.
## Жалоба со скриншотом: «радиус открытия тумана от отряда слишком огромный,
## открывает почти полкарты за лесом». Так и было по арифметике: у лучника
## дальность 20, множитель 3 — обзор 60 м при полукарте в 130×73. Один отряд
## стрелков раскрывал круг диаметром 120 м, то есть почти всю ширину карты.
##
## Множитель урезан до 1.6. Ниже единицы его опускать НЕЛЬЗЯ ни при каких
## правках баланса: обзор обязан быть больше дальности оружия, иначе стрелок
## бьёт в темноту, а игрок не видит, по кому. 1.6 оставляет лучнику 32 м —
## полторы дистанции выстрела.
```


### const VISION_MIN  := 15.0

```
## Нижний предел, и он обязателен. У пехоты attack_range ≈ 1.6-2.2, то есть по
## голой формуле мечник видел бы на три метра вокруг себя: армия из одних
## мечников шла бы по карте практически вслепую. Предел не спорит с формулой,
## а лишь не даёт ей выродиться на ближнем бое
```


### const BUILDING_VISION := 34.0

```
## Обзор постройки. У зданий нет дальности атаки, поэтому число своё —
## примерно как у стрелка, чтобы база видела свои подступы.
## Сокращён вместе с множителем: с прежними сорока двумя метрами база из шести
## построек раскрывала пятно шире, чем вся армия вместе взятая
```


### const CASTLE_VISION := 62.0

```
## «При постройке Ратуши/Крепости круг тумана вокруг неё расширяется». Число
## выше обзора башни (TOWER_VISION 35): башня — это глаз на рубеже, а крепость
## — центр базы, вокруг которого игрок и должен видеть свою землю
```


### static func get_upgrade_slot(upgrade_id: String) -> Dictionary:

```
##
## УЗЛЫ ДРЕВА КУЗНИЦЫ — ТОЖЕ СЛОТЫ. forge_config собирает каждый узел в том же
## формате, что и запись UPGRADE_SLOTS (id/name/desc/icon/cost_*/research_time/
## applies_to/bonus_*), поэтому вся машинерия исследований — списание ресурсов,
## очередь кузницы, накопление бонусов, мгновенная добавка HP — работает с ними
## без единой отдельной ветки. Разница только в условии доступа: у старого слота
## это одиночное поле requires, у узла древа — массив prerequisites плюс, для
## колонки D, полный ряд (разбирается в GameManager.can_research).
```


### const KEEP_WARRIOR_VET := 1          # с чего начинает Крепость без улучшений

```
## Мечники из БАРАКОВ — новобранцы (ранг 0), из КРЕПОСТИ — готовые ветераны.
## Ранг, с которым они выходят, поднимается исследованиями в самой Крепости:
## по умолчанию I, дальше II, III, IV.
##
## ПОЧЕМУ РАНГ, А НЕ ОТДЕЛЬНЫЙ РОД ВОЙСК. Ветеранство в проекте уже умеет всё
## нужное: лычки на знамени (VET_BANNER_TIERS), звание, бонусы, значок в
## панели, сохранение партии (список выбранных наград в `chosen`). Заводить
## второй «элитный мечник» означало бы вторую таблицу статов, вторую ветку в
## найме и второй источник правды о том, что такое опытный боец.
```


### const KEEP_WARRIOR_PICKS_PER_VET := 1

```
## Сколько наград выдаётся отряду на каждую лычку. Та же раздача, что у
## стартовых ветеранов орды (Main.grant_squad_veterancy)
```


### const KNIGHT_UPGRADE_COST := 7000.0

```
## Заказ: «исследование дорогое (7000 золота), запускается кнопкой [ I ] под
## иконкой рыцаря, идёт со временем (круговой радар прогресса), по завершении
## цифра становится [ II ]». Прежняя лестница (1200/2400/4800 золота плюс
## дерево и камень, мгновенно) при стартовом запасе 20 000 покупалась тремя
## кликами и уводила ранг в IV — синий флаг у «первых же» рыцарей.
## Ступень I→II стоит KNIGHT_UPGRADE_COST, каждая следующая — в
## KNIGHT_UPGRADE_COST_MULT раз дороже; платится ТОЛЬКО золотом
```


### const KEEP_VET_UPGRADES := {

```
## Улучшения ранга в Крепости: id → цена и время. Ключ — РАНГ, который узел
## открывает, поэтому «докуда прокачано» это одно число, а не список флагов.
## Числа ВЫВЕДЕНЫ из ручек выше — правится одна цифра, а не три строки
```


### const KEEP_UPKEEP_FOOD_PER_VET := 0.25   # +25 % еды за каждую лычку

```
## Обычный боевой отряд ест только ЕДУ (FOOD_UPKEEP_SQUAD_PER_SEC). Отряд с
## лычками — ещё и ЗОЛОТО, и чем выше ранг, тем дороже обходится: это плата
## за то, что элита приходит готовой, а не растёт в боях.
##
## Множитель едой идёт ПОВЕРХ обычного содержания, а золото — отдельной
## статьёй: у них разные потребители и разный дефицит (голод тает мораль,
## нехватка золота бьёт по найму)
```


## scripts/goblin/goblin_config.gd

### (шапка файла)

```
## БЕЗ class_name НАМЕРЕННО. Глобальное имя класса живёт в кэше
## .godot/global_script_class_cache.cfg, и появление нового имени уже дважды
## ломало сборку этого проекта до перезапуска редактора. Все обращаются к
## гоблинским скриптам через preload() — этого достаточно и это надёжно.
## ═══════════════════════════════════════════════════════════════════════════
## ГОБЛИНЫ — ПОВЕДЕНИЕ ФРАКЦИИ. ВСЕ ЧИСЛА ЗДЕСЬ
## ═══════════════════════════════════════════════════════════════════════════
## Здесь живёт то, что делает гоблинов гоблинами: деревня, спячка, расписание
## волн, восстановление армии. СИЛА УДАРА И ЗАПАС ЖИЗНИ — не здесь, а в
## unit_stats_config.STATS рядом с людьми: иначе сравнить копейщика с
## копейщиком нельзя, не открыв два файла.
##
## Правило проекта: стенд не имеет права хардкодить эти числа — он обязан
## читать их отсюда и проверять СВОЙСТВО (см. qa_goblin).
# ── СОСТАВ ОРДЫ ─────────────────────────────────────────────────────────────
## Сколько бойцов в отряде каждого рода. Сто копейщиков — это заказ владельца,
## и ради него поднят unit_stats_config.SQUAD_SIZE_HARD_CAP
```


### "gnoll":           12,

```
	# Стая гноллов НЕБОЛЬШАЯ намеренно: их сила в подвижности, а не в числе,
	# и три стартовых отряда по двенадцать — это уже тридцать шесть метателей
```


### const SQUAD_SPACING := 0.65

```
## Шаг строя при выходе из хижины. Растянут вместе с PIXEL_SIZE (x1.445): шаг
## задаётся в метрах, а боец стал шире — оставь 0.45, и выходящий отряд
## перекрывался бы спрайтами вдвое
```


### static func army_composition() -> Array:

```
## START_SQUADS ниже), а не задаётся вторым списком. По этому составу орда
## ПОПОЛНЯЕТСЯ: вожак смотрит, какого рода войск не хватает до штата, и
## заказывает его в хижине. Два независимых списка неизбежно разъехались бы —
## правка стартовой орды молча меняла бы то, что она потом донанимает
```


### static func army_squads() -> int:

```
## Полная численность орды в отрядах. Ровно длина START_SQUADS — держится одним
## числом, чтобы «набрать десять отрядов» и «состав орды» не разъехались
```


### const HUT_TICK_SEC := 10.0

```
## Как часто хижина начисляет еду. Раз в 10 с, а не раз в минуту: доход должен
## капать непрерывно, иначе найм скачет ступенями по сто единиц
```


### const HUT_STEP := 6.0

```
## Хижины стоят СЕТКОЙ с шагом HUT_STEP, а не кольцом радиуса 26: кольцо
## разносило десять хижин на полсотни метров, деревня читалась как случайно
## разбросанные домики, а половина точек ещё и отсеивалась проверкой зазора.
## Шаг 6 м — это заказанные 5-7 м между соседями и при этом два метра чистого
## просвета между коллайдерами (габарит хижины 4 м).
```


### const HUT_ROWS := [4, 3, 3]

```
## Сколько хижин в ряду. 4 + 3 + 3 = десять: слегка неровные ряды читаются как
## деревня, ровный квадрат — как склад
```


### const VILLAGE_RADIUS := 16.0

```
## Радиус, в который заведомо укладывается вся деревня (нужен стенду и
## обороне). Выводится из сетки, а не задаётся отдельным числом
```


### const HUT_MIN_GAP := 5.0

```
## Минимальный зазор между хижинами, м. Сетка его выдерживает по построению,
## проверка осталась страховкой на случай правки раскладки
```


### const VILLAGE_ANCHOR := Vector2(0.72, -0.72)

```
## Правый верхний — это +X / −Z: у карты ось Z растёт «на юг» экрана
```


### const PIXEL_SIZE := 0.012282

```
##
## Кадр гоблина 256x256, рисунок в нём 88x126 (копейщик). У человека-копейщика
## кадр 320x320, рисунок 69x150. При ОБЩЕМ размере пикселя 0.0108 гоблин
## выходил 0.95 м шириной против 0.75 м у человека — то есть ШИРЕ человека,
## будучи ниже его: отсюда «приплюснутость». Вдобавок при шаге строя 0.45 м
## соседи перекрывались наполовину и сотня читалась как сплошной ковёр.
##
## Базовое 0.0085 было подобрано по ШИРИНЕ: 88 px x 0.0085 = 0.75 м, ровно как
## у человека. На карте это оказалось НЕЧИТАЕМО: рядом с рабочим гоблин терялся,
## а сотня с высоты камеры читалась как рябь, а не как орда.
##
## Размер подбирался в два захода. Сначала заказали +70% (SIZE_SCALE 1.7):
## гоблин стал читаться на карте, но вышел ВЫШЕ человека — 2.57 м против 2.29 м.
## Вторым заходом заказано снять с этого 15%, и это ровно тот размер, который
## был нужен: 1.7 x 0.85 = 1.445.
##
## 0.0085 x 1.445 = 0.012282. Гоблин выходит 88 x 0.012282 = 1.08 м шириной
## (человек 0.75 м) и 126 x 0.012282 x V_STRETCH = 2.18 м высотой (человек
## 2.29 м) — то есть ШИРЕ и ПРИЗЕМИСТЕЕ человека. Это и есть искомое: крупная
## заметная фигура, которая при этом ниже людской пехоты.
##
## ВАЖНО: это число нельзя менять в одиночку. От него зависят HORDE_SPOT (шаг
## толпы), SQUAD_SPACING (выход из хижины) и SEP_RADIUS (расталкивание) — все
## три растянуты тем же множителем 1.7. Высота центра спрайта НЕ константа, она
## пересчитывается по ленте в GoblinSpearman._apply_goblin_scale, поэтому ноги
## остаются на земле при любом размере пикселя.
```


### const SIZE_SCALE := 1.445

```
## ВО СКОЛЬКО РАЗ ГОБЛИН КРУПНЕЕ БАЗОВОГО НАБРОСКА. Одно число, от которого
## выведены и PIXEL_SIZE, и HORDE_SPOT, и SQUAD_SPACING, и радиус расталкивания
## (Unit.SEP_MIN_DIST x SIZE_SCALE, см. GoblinSpearman.sep_radius). Держится
## отдельной константой, чтобы следующая правка размера не разъехалась по
## четырём числам в трёх файлах.
##
## 1.445 = 1.7 x 0.85: сначала заказали +70%, затем снять с этого 15%
```


### const TROLL_PIXEL_SIZE := 0.0261   # спринт 15: −15 % (было 0.0307)

```
## РАЗМЕР: «примерно вчетверо крупнее копейщика». У копейщика рисунок 150 px
## при пикселе 0.0108 (1.62 м до растяжки), у тролля рисунок 211 px в кадре
## 384. Четыре роста копейщика = 6.5 м → 6.5 / 211 = 0.0307 м на пиксель
```


### const TROLL_FOOT_ALPHA := 0.5

```
## 0.5 отсекает впечатанную в арт полупрозрачную тень — иначе на точке земли
## стояла её кромка, и кольцо лежало в 0.8 м впереди ног (спринт 16,
## qa_troll_ring/Shot: «смещённый тонкий овал» со скриншота владельца)
```


### (конец файла)

```
## Патруль: радиус кольца вокруг логова и период смены точки, с.
## 23.4 = прежние 18 + 30 % (заказ 09.09.2026)
# ГНОЛЛЫ У ПНЯ (заказ владельца, спринт 13)
# Характеристики (запас, урон, скорость, дальность) — в
# unit_stats_config.STATS["gnoll"], как у всех: здесь только ПОВЕДЕНИЕ.
```


### const GNOLL_BONE_SPEED := 7.0

```
## Высота дуги считается как `дистанция × arc` (Arrow.launch), то есть на
## длинном броске она растёт вместе с дальностью — и кость, летящая на
## одиннадцать метров, читалась на экране почти прямой: подъём был, но
## размазан по всей длине. Лечение — В ДАЛЬНОСТИ, а не в множителе:
## `attack_range` гнолла срезан до GNOLL_THROW_RANGE, и на короткой дуге тот
## же множитель даёт видимый навес. Множитель при этом поднят: бросок руками
## обязан идти круче стрелкового выстрела
```


### const GNOLL_BONE_ARC := 0.18

```
## Было 1.35, то есть подъём дуги БОЛЬШЕ самой дальности броска: кость уходила
## свечой под облака и падала отвесно. Заказ прямо говорит «почти по прямой с
## минимальной дугой». Ноль ставить нельзя — совсем прямая кость на 8.5 м
## читается зависшей в воздухе палкой; 0.18 даёт подъём 1.5 м на полной
## дальности, то есть заметную, но пологую дугу
```


### const GNOLL_BONE_AXIS_K := 0.4

```
## Признак «кручусь» едет в ДЛИНЕ упакованной оси (см. mm_arrow.gdshader):
## шейдер ось всё равно нормирует, так что её модуль свободен и достаётся
## даром — ни нового канала instance-цвета (все четыре заняты), ни второго
## слоя отрисовки, ни своего тика у кости
```


### const GNOLL_THROW_FRAME := 4

```
## Кость, выпущенная в нулевом кадре, вылетала раньше самого броска. Номер
## кадра здесь, а секунды считаются из ТЕМПА САМОЙ ЛЕНТЫ (Gnoll._throw_delay):
## перерисуют ленту с другим fps — задержка доедет сама
```


### const GNOLL_KITE_IN := 6.0

```
## Пехота подошла ближе GNOLL_KITE_IN — гнолл отбегает на GNOLL_KITE_OUT, но
## не чаще раза в GNOLL_KITE_SEC: без окна он выдавал бы приказ каждый тик и
## не сделал бы ни одного броска. Порог «ближний бой» отсекает стрелков: от
## лучника отбегать бессмысленно, он достанет и так
```


### const GNOLL_KITE_GIVEUP := 1.0

```
## стая из трёх десятков стоит вплотную, отбежать физически некуда (свои тела
## держат), а кайт выдавал `command_move` раз в GNOLL_KITE_SEC — боец вечно
## оставался в состоянии MOVING, и обход марша честно держал ему луп шагов.
## Теперь проверяется ФАКТ: не сдвинулся за GNOLL_KITE_GIVEUP секунд больше
## чем на GNOLL_KITE_MIN_GAIN — кайт считается невозможным и глушится на
## GNOLL_KITE_BLOCK_SEC, гнолл стоит и метает. Свойство, а не таймер: «некуда
## бежать» иначе не выразить
```


### const GNOLL_PATROL_RADIUS := 26.0

```
## Поводок инициативы у гнолла ВДВОЕ КОРОЧЕ общего (Unit.AGGRO_LEASH = 14): это
## и есть заказанное «не агрятся всей толпой, когда бой далеко»
```


### const GNOLL_AGGRO_LEASH := 10.0

```
## «Патрулируют Пень, но при появлении врага в радиусе 10 метров становятся
## агрессивными». Это и есть длина поводка инициативы (Unit.aggro_leash):
## дальше него гнолл с места не сходит, ближе — берёт цель сам
```


### const GNOLL_DEFEND_RANGE := 34.0

```
## «пень → враг» (GNOLL_FLANK_SIDE в сторону, GNOLL_FLANK_BACK назад от него),
## не чаще раза в GNOLL_FLANK_SEC
```


### const GNOLL_FLANK_MISS_SEC := 1.0

```
## GNOLL_FLANK_SEC и GNOLL_KITE_SEC взводятся ТОЛЬКО когда враг НАЙДЕН. Никого
## в радиусе нет — таймер остаётся в нуле, и скан повторяется каждый физтик:
## у обороны пня это квадрат 34 м по сетке ядра с ячейкой 1 м, то есть около
## 4761 ячейки на каждую чужую сторону, шестьдесят раз в секунду, на каждого
## гнолла. Своё число нужно потому, что ждать полные четыре секунды после
## промаха нельзя — тогда подошедшего к пню врага стая замечает с опозданием.
## Фазы бойцов разведены по номеру узла (GNOLL_SCAN_PHASES): иначе вся стая
## сканирует в один и тот же кадр и горб просто съезжает, а не исчезает
```


### const GNOLL_RUN_BACK := 5.0

```
## 3 минуты (всего три волны); после третьей — откат 10 минут, если пень жив».
## Часы волн ЛИЧНЫЕ у логова и тикают ТОЛЬКО после первого удара по нему или
## по его страже: иначе подкрепления приходили бы к игроку, который логово
## вообще не трогал
## Гнолл патрулирует; получил урон — берёт обидчика целью (стрелка в первую
## очередь: ответ на удар в базе срабатывает только вплотную, а по гноллу
## бьют с двадцати метров), подбегает на бросок, метает и ОТХОДИТ на
## GNOLL_RUN_BACK к пню; снова ужалили — снова наскок. Ниже GNOLL_HIDE_HP
## запаса — бежит к ближайшему пню, прячется внутри (как гарнизон), лечится
## GNOLL_HIDE_HEAL_FRAC запаса в секунду и выходит на полном
```


### const TROLL_TRAMPLE_COUNT := 3

```
## Топтание: сколько бойцов ПЕРЕД тролем гибнет на месте при ударе с разгона
## (ближайшие по ходу в круге charge_splash). Стенка копий тролля не
## останавливает — он получает их укол (charge_counter_frac) и идёт дальше
```


### const TROLL_SWEEP_ARC_COS := 0.5

```
## …в секторе ±60° от взгляда (косинус половины угла) и не дальше
## attack_range + TROLL_SWEEP_REACH_PAD
```


### const TROLL_FLASH_PEAK := 0.07

```
## Вспышка попадания у тролля СЛАБЕЕ общей (Unit.HIT_FLASH_PEAK 0.85): по
## нему бьют десятки стрел в секунду, и полный подмес белого держал его
## засвеченным постоянно. Здесь — лёгкое мигание, только факт попадания
## Спринт 18 (второе письмо): вспышка урона «очень мягкая, едва заметная» —
## насыщенный красный проявляется не ударом, а по мере падения запаса
## (окровавленность шейдера, wound_hi/wound_lo). Было 0.5 (спринт 15)
```


### const TROLL_BLOOD_SPOTS := 0.0

```
## ВЫКЛЮЧЕНЫ (заказ 10.09.2026: «убрать процедурные пятна, тролль просто
## краснеет» — остаётся общая подкраска раненого по доле запаса)
```


### const TROLL_REINFORCE_AT := 0.35

```
## −30 % к выходу троллей из логова (заказ 10.09.2026): подмога ОДИН тролль
## вместо двух и позже — на трети запаса, а не на половине
```


### const TROLL_AGGRO_HELPERS := 0

```
## СРАЗУ выбегают TROLL_AGGRO_HELPERS. Когда вся эта группа выбита, логово
## мгновенно выпускает ещё TROLL_AGGRO_BONUS (один раз), и только для них
## действует «стандартный» алгоритм подмоги (TROLL_REINFORCE_* на трети
## запаса). В бою (есть цель или разгон) тролль ходит на
## TROLL_COMBAT_SPEED_MULT быстрее, на патруле — обычным шагом
## СПРИНТ 20: помощники по первому удару и бонусная пара СНЯТЫ (спавн −20 %),
## вместо них солидарность стражей — см. TROLL_SOLIDARITY_SEC
```


### const TROLL_AGGRO_RADIUS := 12.0

```
## Радиус агро тролля (спринт 20): «реагирует на врага в НЕБОЛЬШОМ радиусе»,
## а не на весь обзор орды
```


### const TROLL_CHASE_SEC := 10.0

```
## Погоня не дольше этого без нанесённого удара — агро сбрасывается, тролль
## возвращается к пню на патруль
```


### const TROLL_PICK_BODY_H := 2.55     # спринт 15: −15 %

```
## Зона клика — ВЕСЬ спрайт: поправка ракурса считается от половины высоты
## рисунка (м), а не от общей UNIT_BODY_H пехоты; плюс круг у ног
```


### const TROLL_RING_SCALE := 3.57      # спринт 15: −15 % вслед за спрайтом

```
## Кольцо выделения/наведения и тень под ногами — во столько раз крупнее
## пехотного (Unit.ring_scale)
```


### const TROLL_RING_OVAL := Vector2(1.45, 0.9)

```
## Тени под троллем больше нет вовсе (сплошной тёмный круг вчетверо шире
## пехотинца читался как зелёная клякса на траве), а кольцо стало ОВАЛОМ и
## шире прежнего: множители идут поверх TROLL_RING_SCALE
```


### const TROLL_FINE_RING_R := 1.32     # спринт 15: −15 % вслед за спрайтом

```
## Радиус красного обвода В МЕТРАХ. Он идёт МИМО TROLL_RING_SCALE: кольцо
## бойца — тор в 35 см о ДВЕНАДЦАТИ сегментах, и растянутое в 4.2 раза оно
## давало на экране толстые кривые дуги (жалоба со скриншотом). Крупная метка
## рисуется тем же тонким 64-сегментным мешем, что обвод здания
## (UnitVisuals.building_ring_mesh) — там эта же ошибка уже разобрана
```


### const TROLL_AIM_HEIGHT := 2.3       # спринт 15: −15 % вслед за спрайтом

```
## Куда целится стрелок: ЦЕНТР МАССЫ туши (живот/грудь), а не ступни и не
## точка на земле. Выводится из середины рисунка (TROLL_PICK_BODY_H): живот
## чуть ниже неё
```


### const TROLL_AIM_MISS_CHANCE := 0.22

```
## КАЖДАЯ стрела кончалась на высоте живота, и промахов на экране не было
## вовсе — щетина стрел в траве вокруг тролля не появлялась никогда
```


### const TROLL_SURROUND_FOES := 4

```
## Столько чужих в пределах дубины считается кольцом: тролль начинает пробивать
## тела шагом (Unit.breaks_bodies) и раз в TROLL_SHOVE_SEC расталкивает всех
## вокруг на TROLL_SHOVE_PUSH метров — даже на одышке, чтобы не выглядеть
## застывшим
```


### const TROLL_SWING_HIT_SEC := 0.3

```
## Урон и разлёт пехоты прежде списывались В МОМЕНТ НАЧАЛА замаха, а лента
## удара длится 600 мс: на экране пехота разлеталась раньше, чем дубина до неё
## доходила. Теперь удар — ОТЛОЖЕННОЕ событие: замах играется, и только через
## TROLL_SWING_HIT_SEC дубина «касается» — списывается урон, накрытые летят и
## падают. Число — доля длины ленты, а не кадр: лент у тролля несколько, а
## касание у них всех примерно на середине взмаха
```


### const TROLL_SWING_STEP := 0.6

```
## Микропродвижение: каждый удар серии тролль делает полшага к цели, и три
## удара читаются как одна связка, а не три отдельных замаха с места
```


### const TROLL_RESPAWN_SEC := 300.0     # 13.09.2026: +1 тролль раз в 5 минут

```
## Раньше тролли выходили только на старте и по агро-цепочке; выбитое логово
## стояло пустым до мести. Теперь раз в TROLL_RESPAWN_SEC логово доводит число
## живых стражей до TROLL_GUARDS_MAX
```


### const TROLL_DEFENSE_WAVE := 3

```
## Одного стража бьют — он и отвечает (плюс солидарность). Но стоит ВТОРОМУ
## стражу получить урон в окне TROLL_SOLIDARITY_SEC — из пня мгновенно
## выходят TROLL_DEFENSE_WAVE троллей сверх охраны; повтор не раньше чем
## через TROLL_DEFENSE_WAVE_CD
```


### const TROLL_LAIR_AIM_HEIGHT := 1.8

```
## Прицел стрелка по пню: центр самого пня над землёй, м (а не 0.8 у всех
## построек — стрелы шли в корни)
```


### const TROLL_FLASH_COLOR := Color(1.0, 0.22, 0.18)

```
## Цвет вспышки удара у тролля: общая вспышка БЕЛАЯ (mm_unit_sprite, flash_color),
## а заказ спринта 15 просит «усилить КРАСНЫЙ оттенок при получении урона»
```


### const TROLL_LAIR_RING_OVAL := Vector2(1.5, 1.0)

```
## Шире и ниже, чтобы САМ ПЕНЬ оказался в центре: растяжение по осям поверх
## радиуса рисунка и сдвиг центра К КАМЕРЕ (вниз по экрану) в долях радиуса
```


### const LAIR_GLADE_R0 := 9.0

```
## Глушь вокруг пня (спринт 20): грибы, камни, кости, кусты и рощицы по
## кольцу LAIR_GLADE_R0…R1 от центра пня; деревья — дальше, LAIR_GLADE_TREES_R
```


### const LAIR_SPOT := Vector2(105.0, 85.0)

```
## Первый пень — АБСОЛЮТНАЯ точка на правом берегу (земля орды: деревня
## вверху, пень внизу, в бывшем углу ИИ). LAIR_OFFSET — история: прежде пень
## стоял за горой от замка игрока, но обе базы людей переехали на левый
## берег, а их середину занял «красный пень»
```


### const LAIR_AI_OFFSET := Vector2(-104.0, -10.0)   # история, не читается

```
## Первая версия брала то же LAIR_OFFSET «зеркально» (−28 по X, +104 по Z), а
## якорь ИИ стоит у НИЖНЕГО края карты: +104 по Z зажималось краем поля, и
## пень оказывался в 42 м от крепости ИИ — тролль на патруле (23 м) и гноллы
## (агро 10 м) резали отряды ИИ у собственных ворот (qa_ai 4: у одного из
## отрядов лучников 27 из 30). Теперь — 104 м к реке (тот же путь, что у
## игрока до своего пня) и чуть вверх от нижней кромки; башни ИИ на 0.3 пути к
## игроку остаются в 45+ м
```


### const LAIR_AI_SPOT := Vector2(30.0, 0.0)

```
## 14.09.2026, п. 3): по Z ноль (середина = FORD_Z, напротив брода), по X —
## 30 м от кромки к реке, чтобы патруль тролля (TROLL_PATROL_RADIUS) и
## лесная зона гноллов не упирались в край поля
```


### const REVENGE_INTERVAL_SEC := 600.0

```
## REVENGE_SQUADS отрядов пехоты и идут окружать дерево тролля
```


### const HORDE_SPOT := 1.23

```
## Гоблины не держат шеренгу. Места в отряде раздаются по спирали золотого угла
## (равномерное заполнение диска при любом числе бойцов) плюс детерминированный
## сдвиг: получается неровная толпа, а не квадрат фаланги.
##
## Шаг между местами — не «расстояние между бойцами», а КОРЕНЬ ИЗ ПЛОЩАДИ НА
## БОЙЦА: на диске плотность задаётся именно так. Число держится в той же
## пропорции к ширине рисунка, что и раньше (0.85 при ширине 0.75 м), поэтому
## растянуто вместе с PIXEL_SIZE: 0.85 x 1.445 = 1.23 при ширине 1.08 м
```


### const HORDE_JITTER := 0.45

```
## Разброс места внутри своей ячейки, доля шага. Ноль — идеальная спираль,
## слишком правильная на глаз; 0.45 — уже «дикая орда»
```


### const START_SQUADS := [

```
## Одна строка таблицы = ОДИН отряд. Правится владельцем без единой строки кода:
## добавили строку — на карте стало на отряд больше, убрали — меньше.
##
## ПОЧЕМУ СПИСОК, А НЕ ТРИ ОТДЕЛЬНЫХ ЧИСЛА. Раньше стартовая орда описывалась
## связкой «состав ARMY_COMPOSITION + первым VETERAN_START_SQUADS отрядам выдать
## грейд VETERAN_TIER c VETERAN_AUTO_PICKS наградами». Это описание умело ровно
## один сценарий — «несколько первых отрядов одного и того же ранга»: дать
## кабанам ранг выше, чем пехоте, или вывести один отряд-легенду было НЕЛЬЗЯ,
## не переписав код спавна. Явная строка на отряд умеет и это, и прежний
## сценарий тоже (см. значения ниже — они воспроизводят его один в один).
##
## ПОЛЯ СТРОКИ:
##   unit  — id рода войск, ключ unit_stats_config.STATS ("goblin_spearman" /
##           "goblin_rider"). Незнакомый id строка пропускается молча
##   count — сколько моделей в отряде. 0 = взять уставное число из SQUAD_SIZE
##   vet   — УРОВЕНЬ ВЕТЕРАНСТВА ПРИ РОЖДЕНИИ, 1..7 по таблице
##           unit_stats_config.VET_BANNER_TIERS. 0 = новобранцы (знамени нет).
##           Уровень ставится напрямую, а не копится убийствами: отряд обязан
##           ВЫЙТИ ветераном
##   picks — сколько наград за ветеранство раздать автоматически. Раздаются тем
##           же путём, каким их берёт игрок (GameManager.apply_veteran_choice),
##           поэтому бонусы доходят до бойцов без второй реализации раздачи.
##           Больше, чем позволяет уровень, взять нельзя — лишнее отсекается
##
## ЗНАЧЕНИЯ НИЖЕ — ПРЕЖНИЙ ЗАКАЗ ВЛАДЕЛЬЦА СЛОВО В СЛОВО: десять отрядов
## (семь пеших, три кабаньих), первым пяти — четвёртый уровень (синий гвидон,
## бывшее «серебро») с четырьмя наградами. Это задуманный сценарий неравных
## условий, а не случайность: орда стартует опытной, игрок — с нуля
```


### const VETERAN_PREFERENCE := ["bonus_attack", "bonus_health", "bonus_defense",

```
## Что предпочитать при авто-выборе наград. Порядок = приоритет; чего нет в
## наградах уровня, то пропускается
```


### static func start_squads() -> Array:

```
## Возвращает список строк с ПОДСТАВЛЕННЫМИ умолчаниями: count = 0 уже развёрнут
## в уставной размер, отсутствующие поля заполнены. Спавн и стенды читают ОДНО
## И ТО ЖЕ — иначе стенд судил бы об одной орде, а на карту выходила бы другая
```


### const DORMANT_UNTIL_SEC := 0.0

```
## Тридцать минут — заказ владельца
## СПЯЧКИ БОЛЬШЕ НЕТ (спринт 17): владелец снял «скрипт 30-й минуты» — орда
## живёт естественным циклом с первой секунды (см. GoblinAI). Ноль — а не
## удаление ручки: механизм сна оставлен под стенды и сложность
```


### const GARRISON_SQUADS := 3

```
## Фазы: экспансия (центр + рудник на холме) → разведка и рейды → генеральное
## наступление → оборона деревни при потере поля → снова экспансия.
## ЭЛИТНЫЙ ГАРНИЗОН ДЕРЕВНИ: столько первых ветеранских отрядов копейщиков
## из START_SQUADS никогда не выходят из деревни
```


### const MINE_OFFSET := 25.0

```
## Рудник орды: в MINE_OFFSET метрах от деревни в сторону центра карты, с
## рождения захвачен ордой, при нём охрана MINE_GUARD (все ветераны)
```


### const MINE_GUARD := [

```
## СПРИНТ 18: ветеранские ряды орды переведены с vet 4 / picks 4 на vet 3 /
## picks 3 — при новой лестнице наград (+3 на лычках) это те же +9 к главному
## параметру, что давали прежние 2+2+2+3; иначе стартовые элиты орды стали бы
## сильнее на треть и сдвинули бы тайминги ранней игры
```


### const MINE_GUARD_PATROL_R := 9.0

```
## MINE_GUARD_PATROL_SEC
```


### const MINE_ALARM_SEC := 45.0

```
## идёт к руднику; каждый новый удар продлевает
```


### const RAID_INTERVAL_SEC := 40.0

```
## Рейды по экономике: интервал, размер (малый / крупный чередуются), радиус
## поиска целей вокруг известной базы, порог отхода по запасу, порог «сильное
## сопротивление» (боевых чужих в RAID_FLEE_RADIUS), потолок рейда по времени
```


### const HARASS_MIN_SEC := 90.0

```
## Сколько секунд орда рейдит и разведывает, прежде чем при полной армии
## собраться в генеральное наступление
```


### const PEACE_SEC := 600.0

```
## МИРНАЯ ФАЗА: первые PEACE_SEC орда отстраивается и патрулирует границы
## деревни (кольцо PATROL_BORDER_R за околицей), никуда не ходит. Ответ на
## нападение и тревога рудника действуют и в мире
```


### const TRUCE_SEC := PEACE_SEC

```
## Одно число с мирной фазой орды — это одно и то же соглашение
```


### const AI_PROTECT_SEC := 1800.0

```
## Орда не выносит ИИ-человека в начале партии: рейды, разведка боем и
## штурм по ЕГО базе — не раньше AI_PROTECT_SEC и только когда у ИИ уже есть
## крепость (иначе горстка рабочих вырезается за минуту)
```


### const SCOUT_UNITS := 2

```
## РАЗВЕДКА — МАЛЫЙ ОТРЯД (SCOUT_UNITS конных), свой, вне волны: обходит точки,
## нашёл базу — точечный харасс SCOUT_HARASS_SEC и ОТХОД домой, новая вылазка
## через SCOUT_SORTIE_SEC. В ноль никого не выбивает
```


### const BUILDING_HUNT_RADIUS := 70.0

```
## Постройки как цель орды (10.09.2026): нет чужого бойца рядом — отряд идёт
## сносить ближайший чужой дом не дальше этого от своего центра
```


### const ASSAULT_TACTICS := true

```
## Командир штурма (GoblinAttackCommander) ведёт орду в фазе охоты: сбор на
## дистанции, паттерн (провокатор / откатные волны / веер), отход и лечение.
## Выключено — прежняя лавина
```


### const ASSAULT_STANDOFF := 30.0

```
## Линия сбора: столько метров от БЛИЖАЙШЕЙ чужой постройки. Вне полёта
## стрелы и башни (20 м), внутри обзора построек (34 м) — «в зоне видимости»
```


### const ASSAULT_ASSEMBLE_RADIUS := 12.0

```
## Сбор состоялся: доля отрядов не дальше радиуса от своих мест на линии;
## иначе — по таймауту (застрявшие не держат штурм вечно)
```


### const PROBE_FLANK_OFFSET := 22.0

```
## Паттерн А «провокатор»: точка фланга — сдвиг поперёк оси от края базы;
## пехота щупает столько секунд, конница колет столько; пауза на линии между
## уколами; сколько векторов прощупать до общего навала
```


### const HR_CAVALRY_SEC := 6.0

```
## Паттерн Б «откатные волны»: конница бьёт в лоб столько секунд, потом
## откатывается к линии на столько, пехота заходит в брешь; затяжной замес
## (дольше HR_BRAWL_SEC) с потерей HR_LOSING_DROP доли запаса — отход всей армии
```


### const ASSAULT_RETREAT_HP := 0.5

```
## Отход и лечение: запас армии ниже доли — все в лагерь; лагерь — деревня,
## если она ближе ASSAULT_CAMP_FAR от базы, иначе передовая точка на оси
## отхода в ASSAULT_CAMP_DIST от края базы (вне башен); у костров лечатся
## CAMP_HEAL_PER_SEC в секунду в радиусе CAMP_HEAL_RADIUS; восстановились до
## ASSAULT_REARM_HP — новая волна другим паттерном
```


### const VANGUARD_LEAD := 25.0

```
## Марш в центр карты (уточнение владельца): не лавина, а тело с ДОЗОРОМ —
## конница идёт впереди тела на VANGUARD_LEAD м, замечает чужих в
## VANGUARD_SIGHT и отходит к телу, тело переходит в штурм; цель дозора
## переиздаётся, когда уехала дальше VANGUARD_REISSUE_DIST
```


### const DEFEND_ALERT_PAD := 20.0

```
## DEFEND_ALERT_PAD от деревни; пехота держит РУБЕЖ у костров — в
## DEFEND_LINE_DIST от центра деревни в сторону угрозы, бьёт чужих не дальше
## DEFEND_ENGAGE от своего места и за ними не бегает; конница заходит с
## фланга угрозы (DEFEND_CAV_FLANK поперёк), колет DEFEND_CAV_STRIKE_SEC и
## отходит к деревне на DEFEND_CAV_BACK_SEC, сторона чередуется; отряд с
## запасом ниже ASSAULT_RETREAT_HP отходит к кострам лечиться
```


### const SHEEP_BREED_CASTLE_SEC := 300.0

```
## Целая овца, которую SHEEP_CALM_SEC никто не трогал, раз в
## SHEEP_SELF_BREED_SEC приводит ещё одну (в пределах SHEEP_MAX). Это ПОМИМО
## общего прироста логова (SHEEP_BREED_SEC): тот отвечает за стадо как за
## декорацию карты, а этот — за «овцы размножаются сами»
## «Удвоение раз в 5 минут у замка против раза в 2.5 минуты в загоне». Период
## ЛИЧНЫЙ у каждой овцы, поэтому «удвоение» получается само: каждая спокойная
## овца раз в период порождает одну — стадо растёт вдвое за период, пока не
## упрётся в потолок привязки (unit_stats_config.SHEEP_PEN_CAPACITY /
## CASTLE_GRAZE_LIMIT). В этой паре чисел и вся ценность загона
```


### const SHEEP_PIXEL_SIZE := 0.0192

```
## Размер овцы. −20 % (заказ спринта 14): 0.024 → 0.0192
```


### const TROLL_HUNGER_SEC := 150.0

```
## TROLL_HUNGRY_FLOCK; ест с TROLL_EAT_RANGE м, съеденная овца лечит долю
## TROLL_EAT_HEAL от полного запаса
```


### const TROLL_GROWL_MIN_SEC := 12.0

```
## Марш: окно между попытками рыкнуть (сек хода) и шанс на попытку;
## бой: вероятность подрыкивания на удар
```


### const LAUGH_SIGHT := 18.0

```
## Смех орды над выбитым отрядом игрока: чужой последний боец погиб не дальше
## этого от какого-нибудь гоблина (обзор орды)
```


### const TROLL_RAID_SHEEP := 30

```
## больше TROLL_RAID_SHEEP овец — ближайший свободный тролль идёт к их стаду,
## съедает TROLL_RAID_EAT и уводит TROLL_RAID_HERD к своему пню; по дороге
## ломает постройки в TROLL_RAID_SMASH_R, кроме крепости (ратуши). Раз в
## TROLL_RAID_COOLDOWN_SEC на тролля, проверка раз в TROLL_RAID_CHECK_SEC
```


### const TROLL_RAID_SMASH_SEEK := 45.0

```
## Письмо 10 (спринт 19): наевшись, тролль ломает ХОТЯ БЫ ОДНУ постройку
## стороны в TROLL_RAID_SMASH_SEEK от стада (крепость — никогда) и только
## потом уводит отару; на снос — не дольше TROLL_RAID_SMASH_SEC
```


### const TROLL_RAID_SPAWN := 1

```
## её пень (снесённый) восстанавливается, из него выходит TROLL_RAID_SPAWN
## троллей-воров (если живых нет); рейд удался — пень выпускает ещё
## TROLL_RAID_REWARD троллей, и они принимаются за приведённых овец
```


### const RETREAT_STRENGTH := 0.15

```
## Разбитый отряд уходит лечиться. Порог — доля от ПИКОВОЙ численности отряда,
## а не от уставной: орда пополняется постепенно, и по уставному числу свежее
## пополнение вечно читалось бы как «разбиты» (та же ошибка уже была допущена
## и исправлена в EnemyAI — см. _squad_strength там)
```


### const NO_RETREAT_IN_MELEE := true

```
## Отходить можно ДО контакта и ПОСЛЕ него, но не ИЗ него: отряд, повернувший
## спину в рубке, не отступает, а гибнет
```


### const DORMANT_SLEEP_PHYSICS := true

```
## Что именно выключено у спящего бойца. Это не «заморозка»: боец жив, его
## можно ударить и убить, он рисуется и попадает в сетку соседей. Выключены
## ровно два дорогих потребителя кадра — физический тик (движение, поиск целей,
## авто-агро) и визуальный (пересчёт позы). Сто отрядов по сто бойцов иначе
## съели бы кадр за полчаса до того, как хоть раз пригодятся
```


### static func horde_offset(idx: int, total: int, seed_id: int) -> Vector2:

```
## idx — номер бойца в отряде, total — сколько их всего, seed — что угодно
## постоянное для отряда (его id): раскладка обязана быть ОДИНАКОВОЙ в двух
## прогонах, поэтому «случайность» здесь детерминированная, из чисел.
##
## Золотой угол раскладывает точки по диску равномерно при ЛЮБОМ их числе — то
## же свойство, ради которого он выбран для тучи стрел залпа (Archer).
```


### var h1: float = fposmod(sin(float(idx) * 12.9898 + float(seed_id) * 4.1414) * 43758.5453, 

```
	# Детерминированный сдвиг: два независимых псевдослучайных числа из
	# номера бойца и номера отряда
```


### const BIG_SIZE_SCALE := 2.4             # 13.09.2026: −20 % (было 3.0 — втрое крупнее людс

```
## «Клетка» в заказе — это МЕТР: тайловой игровой сетки в проекте нет вовсе,
## расстояния везде в метрах, а ячейка сетки соседей в ядре равна 1.0 м
## (ArmyCore.CellBase). Поэтому «3 клетки в ширину» = сектор 3 м, «отброс на
## 1-2 клетки» = 1.5 м, «атака 4-5 клеток» = attack_range 4.5 м.
```


### const BIG_SWEEP_ARC_COS := 0.5          # cos(60°): половина сектора 60°

```
## Дуга свипа: ±30° от взгляда даёт у предела досягаемости полосу около 4.5 м,
## то есть заказанные «три клетки» с запасом на промежутки строя
```


### const BIG_COMBO_EVERY := 3

```
## Каждый третий взмах — двойной: юнит не должен ощущаться замершим между
## четырёхсекундными ударами
```


### const BIG_SCAN_LEAD := 0.6

```
## Скан целей ЗАМОРОЖЕН на время перезарядки и будится за это время до
## готовности — успеть развернуть спрайт и начать замах (см. BigGoblin)
```


### const BIG_FINE_RING_R := 0.75

```
## Тонкий обвод туши (13.09.2026): вместо растянутого в 2.4 раза кольца бойца
## (жирные красные дуги на скриншоте) — тот же тонкий 64-сегментный тор, что
## у тролля и зданий; радиус по ступням, овал — как у тролля
```


### const BIG_WAVE_SQUADS := 1

```
##   родник (волна из логова) — один отряд;
##   база орды — три отряда;
##   удар по логову — ещё два отряда защиты, мгновенно
## 13.09.2026: туш меньше и только В СВЯЗКАХ с мелкой пехотой: волна пня —
## 3 отряда гноллов + 1 туш, база — 1 отряд среди пехоты и конницы, защита
## пня — 1; резерв за лагерем — 5 пехоты + 6 конных + 1 туш
```


### const BIG_AGGRO_RADIUS := 18.0

```
## BIG_AGGRO_RADIUS — бросает точку, переходит в погоню и добивает отряд
## целиком (стрелков на кайте — тоже: поводок BIG_PURSUIT_LIMIT вместо
## пехотных 7 м). Пока в BIG_HUNT_RADIUS есть чужие — ищет следующего; поле
## чисто — возвращается на прерванную точку марша. Скан — раз в BIG_HUNT_TICK
```


### const RESERVE_ENABLED := true

```
## Одноразовый спавн на 00:00 ЗА лагерем гоблинов (со стороны угла карты):
## элитная кавалерия, туши и пехота. Стоят неподвижно и спят (тот же механизм
## сна, что у стартовой орды: set_tick(false) + F_DORMANT), на дальние
## раздражители не агрятся. Просыпаются ТОЛЬКО на штурм лагеря
## (RESERVE_WAKE_FOES чужих боевых в RESERVE_WAKE_RADIUS от деревни или удар
## по хижине), отбивают и возвращаются на посты спать. Вожак вправе забрать
## из резерва RESERVE_LEND_MAX конных на вылазку через брод (hit & run).
## Строка: unit, count (0 — уставной), vet (ранг: 1-3 красный вымпел,
## 4-6 синий ласточкин хвост), picks — наград на ранг
```


### const RESERVE_BACK := 22.0

```
## Где стоит резерв: за деревней, дальше от центра карты, на RESERVE_BACK м
## от края деревни; отряды — в RESERVE_COLS колонн с шагом RESERVE_STEP
```


### const RESERVE_HEAL_FRAC := 0.03

```
## Лечение резерва на постах (доля запаса в секунду) — конница после
## вылазки восстанавливается здесь, а не в хижине
```


### const GNOLL_ZONE_RADIUS := 48.0

```
## Гноллы держат лес СЛЕВА, СПРАВА И НИЖЕ пня и НЕ лезут в сторону базы
## людей своей стороны (у пня игрока база — вверх по экрану): полуплоскость
## «к базе» дальше GNOLL_ZONE_TOWARD_BASE м от пня закрыта, зона — круг
## GNOLL_ZONE_RADIUS; брод закрыт всегда (полоса FORD_HALF + GNOLL_FORD_PAD).
## Цель, вышедшая из зоны, бросается, боец возвращается в зону
## Гноллы держатся исключительно лесных массивов вокруг родного пня; 70 м
## уводили стаю за пределы глуши (LAIR_GLADE_TREES_R 26 м + рощицы)
```


### const GNOLL_PATROL_STEP_DEG := 40.0

```
## GNOLL_PATROL_STEP_DEG за отрезок, направление обхода — по номеру узла;
## точка отрезка притягивается к ближайшему дереву в GNOLL_PATROL_TREE_SNAP —
## «лесной» патруль, а не прямые векторы по чистому полю
```


### const GNOLL_HOME_ADOPT_R := 60.0

```
## НЕ ДАЛЬШЕ этого радиуса; иначе остаётся у своего домашнего места (home_pos).
## Прежний безусловный «подхватить логово партии» и был миграцией стай через
## всю карту от одного пня к другому
```


### const TROLL_HOME_RADIUS := 55.0

```
## Поводок погони по расстоянию: тролль дальше этого от своего пня бросает
## цель и идёт домой (ТЗ 14.09.2026, п. 5 — изоляция пней). Не короче зоны
## гноллов: тролль стережёт тот же лес
```


### const GNOLL_HUNT_TICK := 0.75

```
## GNOLL_HUNT_RADIUS от пня (внутри зоны) и шлёт на каждого до
## GNOLL_HUNTERS_PER_PREY свободных гноллов; на охоте гнолл ПРЕСЛЕДУЕТ
## (у обычной цели — нет), от рабочего не кайтит и после броска не отходит
```


## scripts/ai_start_army_limit.gd


### (конец файла)

```
## ЛИМИТЫ АРМИИ ИИ — «AI START ARMY LIMIT»
## ЕДИНСТВЕННЫЙ файл, который нужно править, чтобы менять масштаб боя.
## Поднимаете лимиты — ИИ копит больше отрядов и позже идёт в атаку;
## опускаете — атакует раньше и меньшими силами.
##
## Файл намеренно без class_name (как unit_stats_config.gd) — подключается
## через preload, поэтому не зависит от кэша глобальных классов Godot.
## Подключение: const _AICfg := preload("res://scripts/ai_start_army_limit.gd")
##
## РАЗМЕР одного отряда задаётся НЕ здесь, а в unit_stats_config.gd
## (SQUAD_SIZE_SPEARMEN / SQUAD_SIZE_ARCHERS / SQUAD_SIZE_SWORDSMEN):
## тут указывается только СКОЛЬКО ОТРЯДОВ каждого типа нужно ИИ.
##
## Полная армия при значениях ниже: (5 + 5 + 5) отрядов × 30 моделей = 450 бойцов.
## Для стресс-теста «300 на 300» достаточно оставить как есть; для быстрой
## проверки логики поставьте по 1 отряду каждого типа.
```


### (конец файла)

```
# 1. ЛИМИТЫ: до чего ИИ копит силы, прежде чем идти в общую атаку
```


### const SQUAD_LIMIT := {

```
## Ключи — те же id, что в unit_stats_config.STATS.
```


### const START_WORKERS := 5

```
## Ровно столько же, сколько у игрока: старт обязан быть равным
```


### const START_SQUADS := []

```
## ПО УМОЛЧАНИЮ ЗДЕСЬ ПУСТО, И ЭТО ЗАДУМАННЫЙ БАЛАНС: ИИ начинает партию ровно
## как игрок — замок и рабочие, войск нет вовсе, армия появляется ТОЛЬКО через
## очередь найма и только за ресурсы. Бесплатные «волны усиления» из ранней
## версии были удалены намеренно, и стенд qa_ai (проверка 2) стережёт это.
##
## Таблица нужна для двух вещей: сценариев («ИИ начинает с гарнизоном») и
## СЛОЖНОСТИ — пресет Hard подставляет сюда свой список, не трогая ни строки
## кода (см. game_difficulty_config.gd, ключ ai_start_squads).
##
## ФОРМАТ СТРОКИ — ТОТ ЖЕ, ЧТО У ОРДЫ (goblin_config.START_SQUADS), и это не
## совпадение: спавн у обеих фракций идёт через одну функцию
## Main.grant_squad_veterancy, второй реализации раздачи наград быть не должно.
##   unit  — id рода войск, ключ unit_stats_config.STATS ("spearman" / "archer"
##           / "warrior" / "monk"). Незнакомый id строка пропускается молча
##   count — сколько моделей в отряде. 0 = уставное число (SQUAD_SIZE_* из
##           unit_stats_config), то есть «полный отряд этого рода»
##   vet   — УРОВЕНЬ ВЕТЕРАНСТВА ПРИ РОЖДЕНИИ, 1..7 по таблице
##           unit_stats_config.VET_BANNER_TIERS. 0 = новобранцы (знамени нет)
##   picks — сколько наград за ветеранство раздать автоматически; выбираются по
##           AI_VETERAN_PREFERENCE, чего на уровне не предлагают — пропускается
##
## Пример «ИИ выходит с обученным гарнизоном»:
##   const START_SQUADS := [
##       {"unit": "spearman", "count": 0, "vet": 2, "picks": 2},
##       {"unit": "archer",   "count": 0, "vet": 1, "picks": 1},
##   ]
```


### const START_VETERAN_PREFERENCE := ["bonus_attack", "bonus_health",

```
## Порядок предпочтений при авто-выборе наград СТАРТОВЫМ отрядам. Ключи —
## модификаторы (unit_stats_config.MODIFIERS), а не короткие имена статов:
## AI_VETERAN_PREFERENCE ниже описывает выбор ВЖИВУЮ, по ходу партии, и работает
## с короткими именами. Два списка потому, что и читаются они в разных местах
```


### static func start_squads() -> Array:

```
## Нормализованный стартовый состав: count = 0 уже развёрнут в уставной размер
## отряда, отсутствующие поля заполнены. Спавн и стенды читают ОДНО И ТО ЖЕ —
## иначе стенд судил бы об одной армии, а на карту выходила бы другая
```


### (конец файла)

```
# 2. ГАРНИЗОН И ИЗЛИШКИ
```


### const HOME_GUARD_PER_TYPE := 7

```
## Эти отряды переводятся в стойку «ЗАЩИТА» и с места не уходят.
## Рабочие в счёт не идут никогда.
```


### const SEND_SURPLUS_TO_LAKE := true

```
## Всё, что сверх гарнизона, ИИ отправляет в бой к озеру (центр карты),
## не дожидаясь полного лимита. Поставьте false, чтобы ИИ копил всё дома
## и выходил единственной большой волной.
```


### const USE_LAKE_AS_RALLY := true

```
## USE_LAKE_AS_RALLY=true берёт центр озера из Main.LAKE_CENTER,
## а иначе используется RALLY_POINT_OVERRIDE.
```


### const PUSH_BASE_AFTER_LAKE_WIN := true

```
## ИИ считает центр карты взятым и идёт добивать базу игрока.
## В оборонительном режиме (DEFENSIVE_MODE) этот пункт не работает.
```


### const AI_MUSTER_BEFORE_ATTACK := true

```
## Лимит по отрядам (SQUAD_LIMIT) отвечает на вопрос «хватает ли войск», но не
## на вопрос «собрались ли они». Отряды рождаются в бараке по одному и с этого
## момента шли на базу игрока каждый своим ходом — противник встречал их
## поштучно и так же поштучно перемалывал.
## Теперь атака начинается, только когда MUSTER_FRACTION боевых отрядов уже
## стоят на точке сбора. Признак «волна пошла» защёлкивается: собравшаяся волна
## не рассыпается обратно из-за одного отставшего.
```


### (конец файла)

```
# 2b. ОБОРОНИТЕЛЬНЫЙ РЕЖИМ
```


### const DEFENSIVE_MODE := true

```
## ИИ НЕ ХОДИТ В ЛОБОВУЮ НА БАЗУ ИГРОКА. Он развивает экономику, набирает
## войска, занимает свою половину карты (вместе с озером), держит там
## оборонительные порядки и патрулирует рубеж. В бой вступает только с тем,
## кто зашёл в его зону. false — прежнее поведение со штурмом базы.
```


### const MAP_CONTROL_FRACTION := 0.5

```
## Рубеж обороны проходит по этой доле пути от своего замка к базе игрока.
```


### const DEFENSE_ENGAGE_RADIUS := 30.0

```
## Радиус вокруг рубежа, внутри которого ИИ считает противника вторгшимся
## и отвечает контратакой (за пределы радиуса не преследует)
```


### const AI_COUNTER_ATTACK := true

```
## АВТО-ВЫБОР ВЕТЕРАНСКИХ НАГРАД. Получив уровень, отряд ИИ сам берёт бонус
## (игроку такое окно выбора показывает интерфейс). Порядок предпочтений —
## по идентификаторам из unit_stats_config.VETERAN_LEVEL_BONUSES; чего нет
## в списке уровня, берётся первым доступным
## КОНТРАТАКА В ОБОРОНЕ. false — прежнее поведение: заслон и патруль стоят
## столбами, даже когда рядом идёт бой (ровно на это и жаловался игрок).
## true — противник в DEFENSE_ENGAGE_RADIUS снимает секцию с поста, и она
## дерётся всем составом, а не ждёт, пока её обойдут
```


### const AI_RECAP_CENTER := true

```
## Жалоба владельца: игрок берёт центр карты, ИИ забивается в глухую оборону у
## замка и не выходит из неё НИКОГДА — даже восстановив армию до полного лимита.
## Так и было: DEFENSIVE_MODE знает ровно два состояния, «держать рубеж» и
## «последний рубеж у замка», и оба они оборонительные. Копить силы было не для
## чего.
##
## Третье макро-состояние: центр под игроком И армия набрана — вся орава
## выходит ЕДИНЫМ ФРОНТОМ отбивать центр. Дома остаётся RECAP_HOME_GUARD
## отрядов на род войск, а не полный гарнизон: «не оставлять лишних юнитов в
## бесконечной обороне» — прямое требование заказа.
```


### const RECAP_ARMY_FRACTION := 0.8

```
## 0.8 — «от 80% до 100%», как заказано
```


### const RECAP_ABORT_FRACTION := 0.45

```
## Ниже какой доли наступление сворачивается обратно в оборону. Заметно ниже
## порога входа: без этого зазора ИИ метался бы между обороной и атакой на
## каждом такте, теряя по бойцу
```


### (конец файла)

```
# 3. ТЕМП
```


### const PEACE_SECONDS := 600.0

```
## 0.0 — воевать с первой секунды.
## 10 минут (уточнение владельца 11.09.2026): все стороны отстраиваются
```


### const AI_ASSAULT_AT_SEC := 1800.0   # 13.09.2026: 30-я минута (было 2100)

```
## Экспансия: один отряд идёт на ближайший не свой рудник, захватывает его и
## остаётся там резервом; рудник отбит — идёт на следующий
## ГЕНЕРАЛЬНЫЙ ШТУРМ базы игрока всей армией — не раньше этой секунды партии
## (30-я минута) и только с набранным лимитом: до того ИИ в оборонительном
## режиме держит центр, рудники и ходит микро-рейдами
```


### const AI_TRUCE_SEC := 600.0

```
## Перемирие первых минут (спринт 20): до этого срока ИИ не рейдит
## экономику игрока. Одно число с goblin_config.PEACE_SEC
```


### const AI_BUILD_TOWERS := true

```
## Башни на границе: AI_TOWERS штук на TOWER_BORDER_FRACTION пути от замка к
## базе игрока, разнесены вбок на TOWER_SIDE. Строятся после стрелковой
```


### const AI_RAIDS := true

```
## Рейды по экономике игрока: раз в RAID_INTERVAL_SEC один отряд (мечники —
## первыми) идёт бить рабочих и одиночные постройки у базы игрока; при
## сильном сопротивлении (боевых игрока в RAID_FLEE_RADIUS) или по сроку —
## отход в замок лечиться (тот же путь, что у разбитого отряда)
```


### const RAID_MIN_FIELD := 1

```
## Рейд идёт, пока после рудника остаётся хоть один свободный отряд, а всего
## отрядов не меньше RAID_MIN_ARMY — замок без охраны не оставляем
```


### const MAX_QUEUED_ORDERS := 1

```
## 1 — строго по одному отряду за раз (отряды выходят чередой, а не пачкой).
```


### const REBUILD_BARRACKS_FREE := true

```
## АВАРИЙНОЕ ВОССТАНОВЛЕНИЕ. Если армия выбита, барак отстраивается
## в приоритете и бесплатно, чтобы ИИ не залипал без войск навсегда.
```


### const TACTICS := [

```
## ИИ выбирает тактику на каждую новую волну (по кругу или случайно).
## Смысл полей: для каждого типа юнита задано смещение относительно центра
## волны в «локальных» осях — depth (вдоль курса атаки: + вперёд, − назад)
## и flank (в стороны: 0 — по центру, 1 — развести по обоим краям).
##
##   "archers_front" — лучники впереди, копейщики за ними, рыцари в тылу
##   "spears_back"   — рыцари впереди, лучники по краям, копейщики сзади
##   "one_mass"      — все идут одной кучей сразу (никакого построения)
## Прежняя таблица меняла местами САМИ РОДА ВОЙСК: «archers_front» выставляла
## лучников перед копейщиками, «spears_back» — рыцарей впереди всех. Требование
## теперь прямое и обратное этому: первая линия — копейщики фронтом к игроку,
## вторая — лучники СТРОГО за их спинами, мечники по краям. Строй, в котором
## стрелки стоят перед стеной копий, — это не тактика, а способ отдать стрелков
## даром, и на большой армии это было хорошо видно.
##
## Тактики остались, но различаются теперь ГЛУБИНОЙ И ШИРИНОЙ, а не порядком
## родов: насколько далеко отстают лучники, насколько широко расходятся фланги.
## Порядок «копья — луки — фланги» во всех трёх одинаков и является инвариантом
## (его проверяет qa_ai #6).
##
## depth > 0 — БЛИЖЕ К ИГРОКУ вдоль курса волны; flank — доля FLANK_SPREAD,
## знак чередуется по отрядам одного рода, то есть «по обоим краям»
```


### const AI_FACE_PLAYER_ARMY := true

```
## Курс волны считался от своего замка к ЗАМКУ игрока, и строй разворачивался
## по этой линии независимо от того, где стоит армия противника. Пока игрок
## сидит дома, разницы нет; стоит ему встретить волну в поле сбоку — и стена
## копий встречает его боком, а лучники оказываются не за спинами своих, а
## сбоку от них. Требование владельца — строиться ОТНОСИТЕЛЬНО АРМИИ ИГРОКА.
## Если армии поблизости не видно, целью остаётся база: это и есть «идём туда,
## где противник обязан появиться»
```


### const TACTICS_IN_ORDER := true

```
## true — тактики идут по кругу (предсказуемо, удобно тестировать),
## false — выбираются случайно
```


### (конец файла)

```
# 5. ТАКТИКА НА УРОВНЕ ОТРЯДА
# Всё, что ниже, считается РАЗ В THINK_INTERVAL и РАЗ НА ОТРЯД — не на бойца.
# Это принципиально: армия ИИ это десяток отрядов и пара сотен человек, и
# тактика, посчитанная поимённо, стоила бы в двадцать раз дороже, ничего не
# добавляя — решения всё равно принимаются за отряд целиком.
```


### const AI_RESEARCH_FORGE := true

```
## ИИ покупал только ПЛОСКИЕ слоты (UPGRADE_SLOTS) — наследство от старой
## кузницы. Древо (forge_config, 4 рода войск × 20 узлов) он не трогал вовсе,
## то есть почти вся наука в игре проходила мимо него, и на длинной партии
## прокачанный игрок встречал ИИ с базовыми характеристиками
```


### const AI_FORGE_WEIGHTS := {

```
## Что ИИ ценит в узле древа, в порядке убывания. Узел оценивается суммой
## своих бонусов с этими весами, берётся лучший из ДОСТУПНЫХ
## Веса подобраны так, чтобы типичный узел любой ветки давал СОПОСТАВИМЫЙ балл:
## боевой «+3 к урону» это 9, экономический «+3 к грузу» — 6, «−0.2 с к циклу» —
## 5. Иначе экономика с её мелкими числами не выигрывала бы у войны никогда, и
## ветка рабочего осталась бы у ИИ такой же мёртвой, какой было всё древо
```


### const AI_FORGE_EXTRA_BRANCHES := ["worker"]

```
## Рода войск, чьи ветки ИИ качает В ДОПОЛНЕНИЕ к тем, что реально нанимает
## (_AICfg.combat_types()). Рабочий в combat_types() отсутствует по определению,
## а ветка у него есть, и она — единственная, которая приносит пользу с первой
## минуты партии: экономика работает и тогда, когда воевать ещё нечем
```


### const AI_RESEARCH_GOLD_RESERVE := 300.0

```
## Сколько золота ИИ держит нетронутым на найм. Без резерва он вкладывал бы
## всё в науку и переставал пополнять выбитые отряды — прокачанная армия из
## трёх человек проигрывает необученной из тридцати
```


### const AI_BUY_SQUAD_ABILITIES := true

```
## Исследование в кузнице открывает способность фракции, но каждый отряд ещё
## докупает её за золото. Без этого ИИ качал бы «Залповый огонь» и не применял
## его ни разу: у игрока покупку делает клик по панели, а у ИИ — некому.
```


### const AI_ABILITY_GOLD_RESERVE := 500.0

```
## Резерв золота под докупку. Отдельный от исследовательского: способность
## разовая и дорогая, а найм пополнения важнее любого режима стрельбы
```


### const AI_WARRIOR_FLANK := true

```
## Его дело — обойти стену по дуге и добраться до стрелков в тылу
```


### const RANGED_ATTACK_RANGE := 6.0

```
## Дальность оружия, выше которой юнит считается СТРЕЛКОМ (метры). Берётся
## порогом, а не списком типов: новый род стрелков подхватится сам
```


### const AI_ARCHER_KITE := true

```
## Лучник в ближнем бою бесполезен и умирает первым. Подошла пехота — отряд
## отходит за спину своих копейщиков и продолжает стрелять оттуда
```


### const AI_SQUAD_RETREAT := true

```
## Выбитый отряд, оставленный в поле, доедают бесплатно. Отправленный в замок
## он лечится и пополняется (см. Castle.request_garrison) и возвращается в строй
```


### const RETREAT_STRENGTH := 0.35

```
## Доля от полного отряда (живых бойцов × их здоровье), ниже которой отряд
## уходит на восстановление
```


### const RETREAT_STRENGTH_VETERAN := 0.30

```
## Заказ владельца: «опытный отряд не должен тупо гибнуть; если у него остаётся
## около 30 % и он не зажат в плотном бою — организованно отходить в замок».
##
## ПОЧЕМУ ЭТО ОТДЕЛЬНОЕ ЧИСЛО, А НЕ ПРОСТО ПОДНЯТЫЙ RETREAT_STRENGTH. Порог
## отхода — это торг между «сберёг отряд» и «оголил рубеж». Новобранец стоит
## одного найма, и уводить его рано незачем; отряд с лычками несёт накопленные
## награды, и его потеря необратима — уровень заново не купить. Одно число на
## двоих означало бы либо разбазаривание ветеранов, либо оборону, которая
## разбегается от первой же царапины.
##
## Порог берётся ПО РАНГУ, а не по составу: сам ранг и есть мера вложенного.
```


### const RETREAT_THREAT_RADIUS := 26.0

```
## Отряд не отступает, пока рядом нет противника: уводить с поста целый отряд
## из-за старых ран незачем. Радиус поиска угрозы, метры
```


### const AI_NO_RETREAT_IN_MELEE := true

```
## ЗАВЯЗАЛСЯ В РУКОПАШНОЙ — ДЕРЁТСЯ ДО КОНЦА. Отряд, развернувшийся спиной
## посреди схватки, не отступает, а гибнет бесплатно: режим отхода глушит и
## авто-агро, и ответный удар. Отходить можно ДО контакта и ПОСЛЕ него, но не
## ИЗ него. Полноценной системы морали и паники в игре пока нет — когда появится,
## этот тумблер станет её частным случаем, а не отдельным правилом
```


### const AI_NO_RETREAT_RANGE := 0.0

```
## ЗАКАЗ ВЛАДЕЛЬЦА: «отряды ИИ бегут в атаку, через секунду разворачиваются и
## отступают, потом снова в атаку — бесконечный цикл метаний. Запрети отмену
## приказа атаки на полпути, если отряд уже вошёл в зону контакта ИЛИ находится
## ближе пятнадцати метров к игроку».
##
## Половина требования уже стояла: AI_NO_RETREAT_IN_MELEE запрещает отход тому,
## кто сцепился. Но КОНТАКТ — это уже удар мечом, а разворот происходит РАНЬШЕ:
## отряд подошёл на дистанцию выстрела, получил стрелу, пересчитал силы и пошёл
## обратно — не успев ни разу ударить. Пятнадцать метров это и закрывают.
##
## Включённым (15 м) оно немедленно ломает qa_ai2 E3/E4 — «под угрозой выбитый
## отряд уходит в замок», — и ломает по существу, а не по формальности.
##
## ОТСТУПЛЕНИЕ У ЭТОГО ИИ РОВНО ОДНО: `_try_retreat` срабатывает ТОЛЬКО у
## отряда, чья сила упала ниже RETREAT_STRENGTH, и уводит его в замок
## доукомплектовываться. То есть «отмены атаки на полпути» в коде нет вовсе —
## есть спасение разбитого остатка. Запрет вблизи противника отменяет именно
## его: отряд, которого домолотили в пятнадцати метрах от чужой шеренги (а
## где ещё его домолотят?), обязан был бы стоять и погибать.
##
## Описанный цикл «атака - отход - снова атака» при этом закрыт ДРУГИМ:
## решение об отходе фиксируется на RETREAT_MIN_SEC = 12 с (_retreat_locked),
## а сцепившийся отряд не отступает вовсе (AI_NO_RETREAT_IN_MELEE). Замер
## qa_mega_battle B5: двадцать отрядов ИИ, НОЛЬ смен роли за тридцать секунд
## при принудительно отменённой мирной фазе — чехарда не воспроизвелась.
##
## Ноль означает «правила нет». Если владелец увидит метания снова — поднять
## это число, и тогда придётся решать, чем платит спасение разбитых отрядов
```


### const PHALANX_RANKS := 4

```
## Сколько шеренг держит отряд копейщиков. Число колонок из него выводится
## (cols = ceil(людей / шеренг)), поэтому ряды получаются РОВНЫМИ при любом
## размере отряда, а не «три полных и один огрызок»
```


### const AI_SPEAR_PHALANX_ON_MARCH := true

```
## именно она опускает копья (см. Spearman._spear_leveled). Без этого блок
## шёл щетиной вверх и опускал копья только по приказу атаки
```


### const CONTACT_RADIUS := 24.0

```
## Ближе этого противник считается «контактом»: копейщики переходят в АТАКУ,
## лучники начинают кайт
```


### const RETREAT_MIN_SEC := 12.0

```
## Жалоба владельца: отряды ИИ подходят, получают урон от стрел, начинают
## убегать, разворачиваются назад, снова идут в атаку и умирают от стрел в
## спину. То есть решение «отходим» принималось и тут же отменялось следующим
## же тактом размышления — на экране это «качели».
##
## Решение об отходе теперь ДЕРЖИТСЯ: сколько бы такт ни пересчитывал цели,
## отступающему отряду другой роли не выдают. Отменить отход могут ровно две
## вещи, обе заказаны владельцем:
##   • его ДОГНАЛИ и навязали ближний бой (отходить из контакта нельзя — см.
##     AI_NO_RETREAT_IN_MELEE, отряд спиной к врагу просто гибнет);
##   • вышло время: отряд отступал непрерывно и всё ещё цел.
## Держится ТЕМ ЖЕ числом, что и замок отхода у самого бойца
## (Unit.RETREAT_LOCK_SEC): решение одно, и два его замка обязаны кончаться
## вместе — иначе роль отпустит раньше исполнения или наоборот
```


### const AI_BASE_SCREEN := true

```
## копейщики впереди, стрелки за их спинами, мечники по краям
```


### const SCREEN_SQUAD_GAP := 2.2

```
## ЗДЕСЬ И БЫЛ ИСТОЧНИК «КУЧИ В ЦЕНТРЕ». Стояло SCREEN_WIDTH = 18 м на весь
## фронт, а место отряда считалось как доля от неё: при HOME_GUARD_PER_TYPE = 7
## семь отрядов копейщиков получали центры в 18/7 ≈ 2.6 м друг от друга. Отряд
## же из шестидесяти человек занимает около трёх метров в ширину и почти шесть
## в глубину — то есть места СИЛЬНО перекрывались, четыреста бойцов сходились в
## одну точку, расталкивание разбрасывало их, а следующий такт снова гнал всех
## в те же перекрытые точки. Ровно тот бесконечный цикл «сжатие → выталкивание
## → сжатие», на который пожаловался владелец.
##
## Ширина фронта теперь ВЫВОДИТСЯ: шаг между соседними отрядами не может быть
## меньше ширины самого отряда плюс просвет (см. EnemyAI._screen_step).
## Отсюда же и второй эшелон: если по фронту столько отрядов не помещается,
## лишние встают ВТОРОЙ ШЕРЕНГОЙ позади первой, а не втискиваются в неё.
## Просвет между соседними отрядами заслона, метры
```


### const SCREEN_MAX_WIDTH := 64.0

```
## Сколько метров фронта ИИ готов занять под одну линию заслона. Больше —
## оборона растягивается в нитку и её обходят; меньше — отряды не помещаются и
## уходят во второй эшелон
```


### (конец файла)

```
# ХЕЛПЕРЫ
```


### static func total_army_cap() -> int:

```
## Полный лимит армии В БОЙЦАХ, а не в отрядах. Нужен решению об отвоевании
## центра: доля «80% лимита» обязана считаться по людям — отряды у разных родов
## разного размера, и десять отрядов копейщиков это вдвое больше народу, чем
## десять отрядов лучников
```


### static func tactic_offset(tactic: Dictionary, unit_id: String, course: Vector3, side: floa

```
## course — единичный вектор курса атаки в плоскости XZ.
## side: -1 или +1 — на какой край уходит фланговый отряд.
```


### const SORTIE_ARMY_FRACTION := 0.30

```
## Фазы партии: мир 0-PEACE_SECONDS (экономика, башни, мелкие вылазки не
## больше SORTIE_ARMY_FRACTION армии и только вне баз), затем БОРЬБА ЗА
## РУДНИК У БРОДА (ударная группа FORD_SPEAR_SQUADS копейщиков + FORD_ARCHER_SQUADS
## лучников), после — «кошмарить соседей» рейдами. Генеральный штурм базы
## игрока — не раньше AI_ASSAULT_AT_SEC (30-я минута; выше это же число)
```


### const AI_TOWER_PERIMETER := true

```
## Периметр базы: копейщики и лучники гарнизона стоят У БАШЕН, а не только
## на линии заслона. У каждой башни — копейщики впереди (к противнику),
## лучники позади; пары разносятся вдоль фронта башни на PERIMETER_SIDE
```


### const AI_FORD_FIRST := true

```
## Борьба за брод: рудник у брода — ПЕРВАЯ цель экспансии; на него идёт
## группа (3 копейщика + 1 лучники), а не одиночный отряд
```


### const FORD_MIN_SQUADS := 6

```
## Группа формируется, когда армия НАБРАНА: не меньше FORD_MIN_SQUADS
## тактических отрядов (группа + рудник + рейд) — иначе она съедала бы
## рудник и рейд у маленькой армии (qa_ai_loop D: рейдов 0)
```


### const PHALANX_RANKS_MARCH := 2

```
## Строй ударной группы: на марше копейщики идут ШИРОКО (PHALANX_RANKS_MARCH
## шеренги), ближе FORD_DEEP_RANGE м к броду перестраиваются в ГЛУБОКИЙ
## строй (PHALANX_RANKS шеренг); лучники держатся позади на дистанции
## прикрытия (tactic_offset)
```


### const AI_TROLL_HUNT := true

```
## Вылазки на троллей ради опыта: раз в HUNT_INTERVAL_SEC отряд копейщиков
## (плюс лучники, если есть) идёт бить троллей у СВОЕГО пня, не дольше
## HUNT_MAX_SEC; запас ниже HUNT_RETREAT_STRENGTH — отход в замок лечиться.
## Разрешены и в мирную фазу (это «стычка вне баз»)
```


### const RECON_EVERY := 3

```
## Разведка боем 2 : 1 — каждый RECON_EVERY-й рейд чистая разведка (добежать
## на RECON_DEPTH м к базе игрока, вскрыть туман, уйти), остальные — разведка
## боем по самым слабым: рабочие, одиночные и раненые отряды
```


## scripts/forge_config.gd

### (шапка файла)

```
## ═══════════════════════════════════════════════════════════════════════════
## КОНФИГУРАЦИЯ КУЗНИЦЫ: ДРЕВО ТЕХНОЛОГИЙ
## ═══════════════════════════════════════════════════════════════════════════
##
## Это БАЛАНСНАЯ ТАБЛИЦА ВЛАДЕЛЬЦА, как и unit_stats_config.gd: цены, время,
## бонусы, тексты, ИКОНКИ и СВЯЗИ правятся здесь и только здесь. Стенды обязаны
## читать числа отсюда, а не хардкодить их у себя.
##
## У каждого типа войск (вкладка в панели кузницы) своя сетка 5 РЯДОВ × 4
## КОЛОНКИ. Колонки A/B/C — ветки характеристик, колонка D — отдельный столбец,
## который открывается не стрелкой, а полностью изученным рядом A+B+C.
##
## ── ОДНА ТАБЛИЦА, А НЕ ДВЕ (переделано по заказу владельца) ─────────────────
## Раньше здесь было ДВЕ таблицы: GRID хранил ФОРМУ графа (иконка, prereq, link)
## одну на все вкладки, а UNITS — только числа. Экономия была настоящая, но у
## неё было следствие, которое и попросили убрать: сетка была ОБЩЕЙ, то есть
## поменять иконку или стрелку у лучника, не тронув мечника, было НЕВОЗМОЖНО в
## принципе — все четыре вкладки рисовались одними и теми же картинками и одним
## и тем же графом. Теперь `icon`, `prereq` и `link` лежат В ЯЧЕЙКЕ каждого
## юнита, рядом со своими числами: дерево каждого рода войск полностью
## самостоятельно, и у рабочего оно честно другой формы, а не копия боевого.
##
## Плата за это известна и принята: форма графа боевых веток повторена четыре
## раза, и при правке САМОГО ДРЕВА её надо править во всех. Взамен любую
## отдельную вкладку можно менять, ничего не ломая у соседей.
##
## ── ID УЗЛА ─────────────────────────────────────────────────────────────────
## "<тип>_<ячейка>": warrior_1a, archer_3d. Идентификатор ГЛОБАЛЬНО уникален,
## поэтому узел древа подставляется в существующую систему исследований как
## обычный слот улучшения (GameManager.researched/researching, накопление
## бонусов, очередь кузницы) без единой дополнительной ветки в их коде.
```


### const UNIT_TABS := ["worker", "warrior", "spearman", "archer", "monk"]

```
## РАБОЧИЙ СТОИТ ПЕРВЫМ: экономика раньше войны, и его ветка нужна с первых
## минут партии, тогда как боевые — уже после первой казармы
```


### const UNITS := {

```
## ТАБЛИЦА ВКЛАДОК — ФОРМА И ЧИСЛА ВМЕСТЕ
##
## Поля ячейки (любое можно опустить):
##   icon          — файл из unit_stats_config.SMITH_ICONS_DIR (ТОЛЬКО имя!)
##   prereq        — ЯЧЕЙКИ (не id узлов), которые нужно изучить прежде. Пустой
##                   список = узел открыт с самого начала. Разворачивается в
##                   полные id внутри вкладки: "1a" → "warrior_1a".
##                   Условие И: нужны ВСЕ перечисленные, не любой из них.
##   link          — ячейки, с которыми узел соединён ГОРИЗОНТАЛЬНОЙ стрелкой.
##
##                   ЭТО НАСТОЯЩИЙ ПУТЬ ПО ГРАФУ, но АЛЬТЕРНАТИВНЫЙ, а не
##                   дополнительный (заказ владельца: «переход разрешён строго
##                   при наличии видимой стрелки; есть стрелка вбок — можно
##                   шагнуть вбок»). Узел открывается, если изучено ЛИБО всё,
##                   что перечислено в prereq (шаг вниз), ЛИБО хотя бы один
##                   сосед по link (шаг вбок) — см. GameManager.research_blockers.
##
##                   РАЗНИЦА ПРИНЦИПИАЛЬНА. Сделать link ТРЕБОВАНИЕМ (И вместе
##                   с prereq) нельзя: стрелки на макете двусторонние, 2a
##                   требовал бы 2b, а 2b — 2a, и обе ячейки заперты навсегда.
##                   Как АЛЬТЕРНАТИВА (ИЛИ) двусторонность безопасна по
##                   построению: она только разрешает, никогда не запрещает,
##                   поэтому взаимной блокировки не существует в принципе.
##                   Проверяется в qa_forge.
##
##   name          — подпись в карточке и всплывающем окне
##   desc          — строка эффекта («+5 к урону»); показывается крупно
##   cost_gold / cost_wood / cost_stone — цена, списывается при ЗАКАЗЕ
##   research_time — секунды в кузнице
##
##   Ключи те же, что у наград за ветеранство (unit_stats_config.BONUS_KEYS),
##   и копятся они так же — СЛОЖЕНИЕМ. Все двенадцать пишутся в каждой ячейке,
##   ненужное нулём: пропуск ключа и ноль означают одно и то же, но полный
##   список видно глазом, и опечатка в имени сразу бросается в глаза.
##
##   ЗНАК ВЕЗДЕ «БОЛЬШЕ — ЛУЧШЕ», включая те два ключа, где само число при этом
##   УМЕНЬШАЕТСЯ (см. cooldown и spread): иначе владельцу пришлось бы помнить,
##   где ставить минус, а стенду — знать это за него.
##
##     bonus_attack   — + к урону КАЖДОГО удара, единиц (и слабого, и мощного)
##     bonus_armor    — + к броне: плоское снижение входящего урона, единиц
##     bonus_defense  — + к защите: второе плоское снижение, поверх брони
##     bonus_health   — + к максимуму запаса жизни. ЕДИНСТВЕННЫЙ, что поднимает
##                      и ТЕКУЩЕЕ здоровье уже стоящим на карте: остальные
##                      читаются вживую, а этот прочитать «на лету» нельзя
##     bonus_speed    — + к скорости ходьбы, м/с (базовая у пехоты 2.0)
##     bonus_range    — + к дальности оружия, м. У лучника СУММА зажата
##                      потолком STATS.archer.attack_range_cap
##     bonus_cooldown — НА СКОЛЬКО СЕКУНД КОРОЧЕ пауза между ударами. Число
##                      положительное, вычитается; пол — unit_stats_config.MIN_COOLDOWN
##     bonus_spread   — НА СКОЛЬКО ТУЖЕ кучность стрельбы (число положительное,
##                      разброс уменьшается). Ближнего боя не касается вовсе
##     bonus_push     — + к напору: сила продавливания чужой шеренги
##     bonus_morale   — + к морали. Она тоже идёт в напор (morale/100)
##     bonus_carry    — + к грузу рабочего за одну ходку, единиц ресурса
##     bonus_gather   — НА СКОЛЬКО СЕКУНД КОРОЧЕ цикл добычи (число
##                      положительное, время уменьшается)
##   squad_unlock_cost — сколько золота стоит докупить способность КОНКРЕТНОМУ
##                   отряду уже после её исследования. ИМЕННО ЭТО ПОЛЕ, а не
##                   колонка, делает узел «способностью»: у рабочего колонка D
##                   тоже есть, но покупать её отрядом нечего — это обычные
##                   пассивные улучшения, просто дорогие и за целый ряд
##
## Колонка D зависимостей не перечисляет вовсе: её открывает полный ряд
## (см. ability_row_cells / GameManager.can_research).
##
## Значения ниже — тестовые (заказ владельца: «заполни тестовые параметры»).
```


### "worker": {

```
# Своя форма графа, а не копия боевой. Экономика ветвится меньше: горизонтальных
# переходов всего две пары (2a↔2b и 4a↔4b) вместо цепочки на каждый ряд —
# «перескочить с вместимости на скорость» это осмысленный размен, а вот прыгать
# туда-сюда каждый ряд незачем.
#
# Колонки: A — ВМЕСТИМОСТЬ (сколько уносит за ходку), B — ТЕМП ДОБЫЧИ (короче
# цикл), C — ХОДЬБА И ЖИВУЧЕСТЬ, D — крупные экономические улучшения за ряд.
```


### "1d": {"icon": "icon_hammer.png", "prereq": [], "link": [],

```
	# bonus_train — доля к темпу найма рабочих в замке (Building.queue_unit),
	# bonus_build — доля к темпу стройплощадки (ConstructionSite)
```


### "1d": {"icon": "icon_dual_sword.png", "prereq": [], "link": [],

```
	# Прежнее имя узла — «Град ударов», «Серия быстрых атак»: то же самое
	# другими словами, и кода за ним не стояло вовсе. Теперь у него есть
	# поведение (Warrior.start_rage_dash): рывок с ЗАФИКСИРОВАННЫМИ щитами и
	# серия из пяти быстрых ударов «обычный → мощный → обычный → мощный →
	# обычный». Включается двойным ПКМ по земле (SelectionManager)
```


### "1d": {"icon": "icon_trap_spikes.png", "prereq": [], "link": [], "toggle": true,

```
	# Прежний «Плотный строй» (контратака только в «Защите» по цели всадника,
	# ×2 от копий) снят целиком: он не срабатывал, когда всадник целил не в
	# копейщика, и не касался ни тролля, ни бегущей пехоты
```


### "4d": {"icon": "icon_skull.png", "prereq": [], "link": [],

```
	# Прежнее имя узла — «Стойкость», «Не отступает»: кода за ним тоже не
	# стояло. Теперь: строй смыкается, идёт МЕДЛЕННО И СЛАЖЕННО, первые
	# PHALANX_PUSH_HITS ударов усилены, после них отряд переходит в
	# ОТТЕСНЕНИЕ — растёт напор и падает входящий урон
```


### "1d": {"icon": "icon_rain_of_arrows.png", "prereq": [], "link": [], "toggle": true,

```
	# ВКЛЮЧАЕТСЯ игроком и висит режимом (как «стена щитов» у мечника). Признак
	# держится в конфиге, а не проверкой «если id == archer_1d» в панели: панель
	# не должна знать имён способностей, иначе следующая такая же потребует
	# правки интерфейса
```


### "1a": {"icon": "icon_hand.png", "prereq": [], "link": [],

```
	## ОБЪЁМ лечения, C — СТОЙКОСТЬ самого монаха. Столбец D — БОНУС РЯДА:
	## цены у него нет вовсе и времени тоже, он выдаётся САМ, как только
	## изучены все три узла своего ряда (GameManager._grant_row_bonuses).
	## Цена и время — ПО РЯДУ, а не по узлу: 100/15 с, 250/30, 500+50 дерева/45,
	## 1000+150 камня/60, 2000+300 камня/90.
```


### (конец файла)

```
## СБОРКА
```


### static var _tree_cache: Dictionary = {}

```
## Собирается один раз за запуск: панель кузницы перестраивается на каждый
## клик по вкладке, а сборка 20 ячеек там ни к чему
```


### static func ability_row_cells(cell: String) -> Array:

```
## Именно их полное изучение открывает саму D (см. GameManager.can_research)
```


### static func tree(unit_id: String) -> Array:

```
## Готовое древо одной вкладки: список узлов, каждый — самодостаточный словарь
## в формате слота улучшения (unit_stats_config.get_upgrade_slot вернёт его же).
##
## ЕДИНСТВЕННЫЙ ЧИТАТЕЛЬ UNITS. Панель кузницы, стрелки, всплывающие окна и
## GameManager берут узел ОТСЮДА, а не лазают в таблицу сами: иначе «иконку
## берём из UNITS» пришлось бы повторить в пяти местах и они бы разъехались.
## Разворачивание ячеек в полные id и вычисление ворот ряда живут тоже здесь
```


### node["applies_to"] = [unit_id]

```
		# Бонусы копятся ТОЛЬКО своему типу войск: вкладка мечника не должна
		# усиливать лучников (в отличие от старых общих слотов с ["all"])
```


### var links: Array = []

```
		# "link" остаётся ЯЧЕЙКАМИ: по ним рисуются стрелки внутри сетки, а
		# сетка мыслит ячейками. "link_ids" — те же связи ПОЛНЫМИ id: по ним
		# GameManager проверяет доступность (см. research_blockers), а он про
		# ячейки не знает и знать не должен
```


### if float(data.get("squad_unlock_cost", 0.0)) > 0.0:

```
			# СПОСОБНОСТЬЮ УЗЕЛ ДЕЛАЕТ ЦЕНА ВЫКУПА, А НЕ КОЛОНКА. У боевых веток
			# это одно и то же (в каждой их ячейке D есть squad_unlock_cost), а
			# вот у рабочего колонка D — обычные пассивные улучшения: покупать
			# способность «артели» отдельным отрядом бессмысленно, и кнопка
			# выкупа за нулевую цену в его панели была бы просто мусором
```


### var gate: Array = []

```
			# Ворота ряда — у ВСЕЙ колонки D, независимо от выкупа: это правило
			# раскладки («колонку D открывает закрытый ряд»), а не свойство
			# способности
```


### static func get_node(id: String) -> Dictionary:

```
## Узел по глобальному id ({} — такого нет). Разбирает id сам, чтобы не
## перебирать все деревья
```


### static func squad_unlock_cost(_node: Dictionary) -> float:

```
## Было так: исследование в кузнице ОТКРЫВАЛО способность роду войск, а каждый
## отряд потом покупал её себе отдельно за `squad_unlock_cost` золота. Владелец
## это отменил: «спец-бонус открывается сразу для всех юнитов соответствующего
## типа, плату за применение убрать». Поля в таблице оставлены историей —
## правка баланса ими больше ничего не включает
```


### static func is_toggle_ability(node: Dictionary) -> bool:

```
## Обычная способность срабатывает сама; эта висит переключателем на панели
## отряда (см. HUD._maybe_add_ability_buttons и GameManager.squad_ability_on)
```


### static func toggle_ability_of(unit_id: String) -> Dictionary:

```
## Нужна тем, кто спрашивает «а есть ли у отряда режим», не зная его id:
## поиск идёт по конфигу, а не по захардкоженному имени
```


### const _BONUS_VIEW := {

```
# ЕДИНЫЙ ВИД УЗЛА ДЛЯ ЧТЕНИЯ ИЗВНЕ
#
# Внутри файла узел хранится «плоско»: name/desc, cost_gold/cost_wood/cost_stone,
# bonus_attack/bonus_armor/…, зависимости — ячейками. Так удобно ПРАВИТЬ (узел
# целиком виден глазом в двух строках), но неудобно ЧИТАТЬ снаружи: приходится
# знать, какие именно ключи бывают.
#
# node_view() отдаёт тот же узел в развёрнутом, самоописательном виде:
#   id, title, description, icon, research_time,
#   cost         — {"gold": …, "wood": …, "stone": …} (только ненулевое)
#   prerequisites— ПОЛНЫЕ id узлов, от которых зависит этот (вертикальные стрелки)
#   links        — полные id соседей по горизонтальным стрелкам (шаг вбок)
#   stat_bonus   — {"attack": 2, "armor": 1, …} (только ненулевое)
#
# ЭТО ИМЕННО ВИД, А НЕ ВТОРАЯ КОПИЯ ДАННЫХ. Числа по-прежнему живут в UNITS в
# одном экземпляре — иначе две таблицы разъехались бы при первой же правке
# баланса. Правим UNITS, читаем node_view().
## Человеческие имена бонусов: ключ конфига → ключ во «вью»
```


### static func _UCfgRef():

```
## Иконки узлов лежат в папке кузницы и в конфиге записаны ОДНИМ ИМЕНЕМ файла;
## полный путь собирает unit_stats_config.smith_icon_path. Грузим его лениво:
## forge_config подключается из unit_stats_config, и preload в обратную сторону
## замкнул бы цикл
```


## scripts/game_difficulty_config.gd

### (шапка файла)

```
## ═══════════════════════════════════════════════════════════════════════════
## СЛОЖНОСТЬ ПАРТИИ: ЛЁГКАЯ / ОБЫЧНАЯ / ТЯЖЁЛАЯ
## ═══════════════════════════════════════════════════════════════════════════
## ЕДИНСТВЕННЫЙ файл, который нужно править, чтобы поменять разницу между
## уровнями сложности. Правит владелец, как unit_stats_config.gd и
## ai_start_army_limit.gd; стенды обязаны читать числа отсюда, а не хардкодить.
##
## Файл намеренно без class_name (как остальные конфиги) — подключается через
## preload, поэтому не зависит от кэша глобальных классов Godot.
## Подключение: const _Diff := preload("res://scripts/game_difficulty_config.gd")
##
## ── ЧТО ЗДЕСЬ НЕ ЛЕЖИТ И ПОЧЕМУ ────────────────────────────────────────────
## ЗДЕСЬ НЕТ НИ ОДНОГО БАЗОВОГО ЧИСЛА. Пресет описан МНОЖИТЕЛЯМИ и ЗАМЕНАМИ
## поверх базовых таблиц (ai_start_army_limit, goblin_config,
## unit_stats_config), а не своей копией баланса. Копия неизбежно разъехалась
## бы: владелец правит лимит в ai_start_army_limit, а на «обычной» сложности
## ничего не меняется, потому что там лежит вторая, забытая цифра.
##
## Отсюда правило чтения: НИКТО не читает PRESETS напрямую. Все спрашивают
## функции внизу файла — они принимают базовое значение и возвращают
## изменённое. На «обычной» сложности они возвращают базу БЕЗ ИЗМЕНЕНИЙ
## (все множители = 1.0), то есть выбор Normal обязан быть полностью
## эквивалентен прежней игре без сложностей — это стережёт qa_difficulty.
##
## ── ЧТО ИМЕННО КРУТИТСЯ ────────────────────────────────────────────────────
## Сложность в RTS — это НЕ читы по урону. Ни одна ручка ниже не трогает силу
## удара, запас жизни и броню: боец на Hard дерётся ровно так же, как на Easy.
## Крутятся ТОЛЬКО ЛИМИТЫ И ТЕМП — сколько противник может накопить и как
## быстро он это делает. Тогда «сложнее» означает «больше и раньше», а не
## «у врага другие правила», и любой замер боя остаётся сравнимым.
```


### const EASY   := "easy"

```
## Идентификаторы уровней. Строки, а не числа: они уходят в файл настроек и
## в сохранение партии, где число «1» через полгода никто не расшифрует
```


### const PRESETS := {

```
## ПОЛЯ ЗАПИСИ (любое можно опустить — тогда берётся нейтральное значение,
## то есть множитель 1.0 / пустой список):
##
##   name                — подпись на кнопке в меню
##   hint                — строка под кнопкой: что игрок получит
##
##   ai_squad_mult       — множитель ЛИМИТА ОТРЯДОВ каждого рода войск
##                         (ai_start_army_limit.SQUAD_LIMIT). Это и есть
##                         главная ручка масштаба: 0.6 — вдвое меньшая армия
##                         под конец партии, 1.4 — заметно больше
##   ai_worker_mult      — множитель потолка рабочих (WORKER_LIMIT): чем больше
##                         рабочих, тем быстрее ИИ выходит на свой лимит армии
##   ai_resource_mult    — множитель СТАРТОВОГО запаса ИИ
##                         (unit_stats_config.AI_STARTING_RESOURCES)
##   ai_train_mult       — множитель ВРЕМЕНИ найма отряда: 0.8 — на 20% быстрее.
##                         Меньше единицы = быстрее, это множитель ВРЕМЕНИ,
##                         а не темпа. Игрока не касается вовсе
##   ai_build_mult       — то же для ВРЕМЕНИ отстройки зданий ИИ
##   ai_peace_mult       — множитель мирной фазы (AICfg.PEACE_SECONDS): на Hard
##                         ИИ начинает воевать раньше
##   ai_start_squads     — СТАРТОВЫЕ ОТРЯДЫ ИИ. Непустой список ЗАМЕНЯЕТ
##                         ai_start_army_limit.START_SQUADS целиком. Формат
##                         строки тот же: {"unit", "count", "vet", "picks"}
##
##   goblin_dormant_mult — множитель МИРНОЙ ФАЗЫ орды (goblin_config.PEACE_SEC,
##                         спринт 17; спячки DORMANT_UNTIL_SEC больше нет — 0):
##                         на Easy орда выходит к центру позже, на Hard — раньше
##   goblin_food_mult    — множитель дохода хижины (HUT_FOOD_PER_MIN), то есть
##                         темпа, с которым орда восполняет потери
##   goblin_vet_shift    — СДВИГ РАНГА стартовых отрядов орды: +1 поднимает
##                         ранг каждому, у кого он есть, −1 опускает. Отряды с
##                         рангом 0 (новобранцы) не трогаются НИКОГДА: иначе на
##                         Hard вся орда вышла бы ветеранской, а это уже другой
##                         сценарий, а не сложность
```


### "ai_squad_mult":    1.0,

```
		# ВСЕ РУЧКИ НЕЙТРАЛЬНЫ. Это не заглушка «на потом»: Normal обязан быть
		# в точности прежней игрой, иначе появление сложностей молча сдвинуло
		# бы весь баланс, который владелец настраивал без них
```


### "ai_start_squads": [

```
		# START_SQUADS: без него «сложнее» означало бы только «дольше ждать»,
		# потому что первые минуты партии у ИИ всё равно пусто
```


### static var _current: String = DEFAULT

```
## ХРАНИТСЯ СТАТИЧЕСКИ, А НЕ В GameManager, и это осознанно: сложность нужна
## ДО того, как автозагрузка успевает что-либо решить (главное меню строится
## первым), и нужна она конфигам — а конфиг, лезущий в автозагрузку за
## значением, тянет за собой всё дерево зависимостей движка в стенд, который
## его и не поднимал. Статика переживает смену сцены: скрипт держится в памяти
## preload'ами и никуда не выгружается.
```


### const SETTINGS_PATH := "user://difficulty.cfg"

```
## Куда пишется выбор, чтобы пережить перезапуск игры. Файл крошечный и
## отдельный от аудио-настроек: у них разная судьба при сбросе настроек
```


### static func current() -> String:

```
## Текущий уровень сложности. Всегда возвращает ЗНАКОМЫЙ ключ: битый файл
## настроек или опечатка схлопываются в DEFAULT, а не оставляют игру без правил
```


### static func set_current(id: String) -> void:

```
## Сменить сложность. Действует со следующей НОВОЙ ПАРТИИ: лимиты читаются
## вживую, а стартовые ресурсы и стартовые отряды раздаются один раз при старте
```


### (конец файла)

```
# РУЧКИ: базовое значение на входе, изменённое на выходе
# ВСЕ ФУНКЦИИ НИЖЕ БЕРУТ БАЗУ АРГУМЕНТОМ. Ни одна не знает базовых чисел сама —
# это и есть гарантия, что правка ai_start_army_limit или goblin_config
# доезжает до всех трёх сложностей разом.
```


### static func ai_squad_limit(unit_id: String) -> int:

```
## Округление ВВЕРХ и пол в единицу: на Easy при множителе 0.55 лимит лучников
## (5) даёт 2.75 — округли вниз, и получилось бы 2, а строку конфига «лучники
## нужны» это молча превратило бы в «почти не нужны»
```


### static func ai_total_army_cap() -> int:

```
## Полный лимит армии ИИ В БОЙЦАХ. Нужен решению об отвоевании центра: отряды
## у разных родов войск разного размера, и считать долю по отрядам нельзя
```


### static func starting_resources(faction: int, base: Dictionary) -> Dictionary:

```
## ИГРОКА НЕ КАСАЕТСЯ ВОВСЕ: сложность меняет противника, а не правила игрока —
## иначе «лёгкая» превратилась бы в «дать игроку денег», и сравнить две партии
## между собой стало бы нельзя
```


### static func train_time(faction: int, base: float) -> float:

```
## Пол в 0.5 с обязателен: множитель правит владелец, и ноль здесь означал бы
## отряд, выходящий в тот же кадр, — то есть очередь найма без очереди
```


### static func ai_start_squads() -> Array:

```
## ЗАМЕНА, А НЕ ДОБАВКА: «Hard добавляет отряды к тем, что в базе» означало бы,
## что стоит владельцу вписать гарнизон в ai_start_army_limit — и на Hard он
## удвоится. Заменой пресет остаётся предсказуемым
```


### static func goblin_peace_sec() -> float:

```
## тот же множитель сложности, что прежде крутил спячку
```


### static func goblin_start_squads() -> Array:

```
## сложность двигает ЗАСЛУЖЕННЫЙ ранг, а не раздаёт его тем, кому он не положен
```


### d["vet"] = clampi(v + shift, 1, 7)

```
			# Пол в единицу, а не в ноль: отряд, задуманный ветеранским,
			# на Easy становится младшим ветераном, но знамя не теряет
```


### static func load_saved() -> void:

```
## Прочитать сохранённый выбор. Молчалива по построению: нет файла, битый файл,
## незнакомый ключ — всё это означает «играем на обычной», а не ошибку
```


### static func save() -> void:

```
## Записать выбор на диск. Зовётся из меню сразу по клику: файл крошечный, а
## отдельная кнопка «сохранить настройки» — лишний шаг для игрока
```
